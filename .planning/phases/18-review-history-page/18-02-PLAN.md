---
phase: 18-review-history-page
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - lib/dashboard.ts
  - app/habits/page.tsx
  - components/HabitsClient.tsx
autonomous: true
requirements: [HIST-04, HIST-07]
must_haves:
  truths:
    - "The Habits page shows a 'Card progress' section with four tiles (New / Learning / Review / Relearning) counting cards by FSRS state (D-07)"
    - "Tapping anywhere on the Card progress section navigates to /history (D-08 — the whole section is one Link, no per-state filter links)"
    - "The counts are real on first server-rendered paint (RSC + DTO hydration, no loading flash) — HIST-07"
  artifacts:
    - lib/dashboard.ts (getStats gains a cardReview.groupBy by state, mapped to StatsDTO.cardsByState)
    - app/habits/page.tsx (passes initialCardsByState from stats.cardsByState)
    - components/HabitsClient.tsx (new 'Card progress' section wrapped in <Link href="/history">)
  key_links:
    - "getStats() must return cardsByState for HabitsClient's tiles to render — a missing/renamed field yields undefined tiles"
    - "The section renders all four state tiles even when a state has zero cards (groupBy omits empty states) — a lookup with ?? 0 default is required"
    - "The entire section is the Link target (D-08); do NOT add per-tile links (per-state filtering was explicitly rejected — Deferred Ideas)"
---

<objective>
Add the "Card progress" FSRS-state breakdown to the Habits page as the value-adding entry point into `/history` (D-06/D-07/D-08). This reframes the history view as a drill-in behind a first-class card-collection-health summary, not a buried nav link.

Purpose: Delivers the "reachable from an existing surface" half of HIST-04 (the Habits page already owns a bottom-nav tab, so this satisfies "no new bottom-nav tab") and applies the RSC+DTO hydration guarantee (HIST-07) to the new stats section. Per D-08 the whole section is one `<Link href="/history">`; per the Deferred Ideas note, per-state filter links are explicitly out of scope.
Output: `cardsByState` groupBy added to `lib/dashboard.ts:getStats()`; `initialCardsByState` prop threaded through `app/habits/page.tsx`; a new tappable "Card progress" section in `components/HabitsClient.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-review-history-page/18-UI-SPEC.md
@.planning/phases/18-review-history-page/18-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (18-02) creates/extends:
- `lib/dashboard.ts:getStats()` now returns `cardsByState` (uses `prisma.cardReview.groupBy({ by: ['state'], _count: true })` + `State[]` label mapping)
- `app/habits/page.tsx` passes a new `initialCardsByState={stats.cardsByState}` prop
- `components/HabitsClient.tsx` gains a new `initialCardsByState` prop and a "Card progress" section (four FSRS-state tiles wrapped in `<Link href="/history">`)

Depends on 18-01's `StatsDTO.cardsByState` type addition.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add cardsByState groupBy to getStats()</name>
  <files>lib/dashboard.ts</files>
  <read_first>
    - lib/dashboard.ts (the existing `getStats()` — its `Promise.all` array with `cardsByType` groupBy at line 23, and the returned object at line 27; note FSRS `state` lives on `CardReview`, so the new groupBy is on `prisma.cardReview`, same table already queried for `dueCards`/`masteredCount`)
    - lib/fsrs.ts (the `State` enum re-export at line 5 — used to label the numeric state 0→'New', 1→'Learning', 2→'Review', 3→'Relearning')
    - lib/dto.ts (StatsDTO.cardsByState shape added in 18-01: `{ state: number; stateLabel: string; _count: number }[]`)
    - .planning/phases/18-review-history-page/18-PATTERNS.md (the `lib/dashboard.ts` pattern section with the exact groupBy + map to add)
  </read_first>
  <action>
    In `lib/dashboard.ts`: import `State` from `@/lib/fsrs` (add to imports). Inside `getStats()`, add `prisma.cardReview.groupBy({ by: ['state'], _count: true })` as a sixth query in the existing `Promise.all` array (mirroring the `cardsByType` groupBy already there; per D-07), destructuring its result as `cardsByStateRaw`. After the `Promise.all`, map it to the DTO shape: `const cardsByState = cardsByStateRaw.map((r) => ({ state: r.state, stateLabel: State[r.state], _count: r._count }))`. Add `cardsByState` to the returned object. Do not change the existing returned fields (`totalCards`, `dueCards`, `totalLessons`, `cardsByType`, `masteredCount`) — `app/page.tsx`, `/wrapped`, and `GET /api/stats` all consume them. This groupBy runs against the same Turso-backed client that already runs `cardsByType` without incident (RESEARCH).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "lib/dashboard.ts" || echo "dashboard typechecks clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "cardReview.groupBy" lib/dashboard.ts` returns a match with `by: ['state']`
    - `grep -n "stateLabel: State\[" lib/dashboard.ts` returns a match (numeric state mapped to its enum name, not a hand-rolled table)
    - `grep -n "cardsByState" lib/dashboard.ts` appears in both the map and the returned object
    - The `getStats()` return still includes all of `totalCards`, `dueCards`, `totalLessons`, `cardsByType`, `masteredCount` (no existing field removed)
    - `npx tsc --noEmit` reports no error in `lib/dashboard.ts`
  </acceptance_criteria>
  <done>`getStats()` returns `cardsByState` labelled by FSRS state name, alongside all existing stats.</done>
</task>

<task type="auto">
  <name>Task 2: Thread the prop and render the tappable Card progress section</name>
  <files>app/habits/page.tsx, components/HabitsClient.tsx</files>
  <read_first>
    - app/habits/page.tsx (the RSC that awaits `getStats()`/`getActivityData()` and passes `initialX` props to `HabitsClient`; `export const dynamic = 'force-dynamic'` is already present)
    - components/HabitsClient.tsx (the `Props` interface lines 35-40, the existing `initialX` prop convention, the "All-time totals" 4-tile grid lines 145-165 to clone visually, and the "My Korean summary" whole-card `<Link>` lines 260-269)
    - .planning/phases/18-review-history-page/18-UI-SPEC.md (Habits page addition section — heading "Card progress", 2-col grid of 4 tiles, `text-2xl font-bold` count + `text-xs text-muted` label, whole section wrapped in `<Link href="/history">` with `hover:shadow-lg transition-shadow`, trailing "View history →" affordance in `text-xs text-button`; insert after "All-time totals" or between "Averages & consistency" and "Last 30 days")
    - .planning/phases/18-review-history-page/18-PATTERNS.md (the `components/HabitsClient.tsx` +Card progress section pattern block)
  </read_first>
  <action>
    In `app/habits/page.tsx`: add `initialCardsByState={stats.cardsByState}` to the `<HabitsClient .../>` props. No other change (getStats already awaited).

    In `components/HabitsClient.tsx`: add `initialCardsByState: StatsDTO['cardsByState']` to the `Props` interface and destructure it in the component signature (import `type { StatsDTO } from '@/lib/dto'`). Build a fixed-order tile list so all four states always render even when the groupBy omits an empty state: define `const stateOrder = [{ state: 0, label: 'New' }, { state: 1, label: 'Learning' }, { state: 2, label: 'Review' }, { state: 3, label: 'Relearning' }]` and a lookup `const countFor = (s: number) => initialCardsByState.find((r) => r.state === s)?._count ?? 0`. Render a new `<Link href="/history">` section styled `bg-surface-1 rounded-2xl shadow-md p-6 flex flex-col gap-3 hover:shadow-lg transition-shadow` (matching sibling sections and `HabitTracker.tsx`'s whole-card Link pattern; per D-08 the ENTIRE section is the tap target — add NO per-tile links). Header row: `<h2 className="text-lg font-semibold text-foreground">Card progress</h2>` plus a right-aligned `<span className="text-xs text-button">View history →</span>`. Body: `grid grid-cols-2 gap-3` of the four tiles, each `bg-surface-2 rounded-xl p-4` with `<p className="text-2xl font-bold text-foreground">{countFor(state)}</p>` and `<p className="text-xs text-muted mt-0.5">{label}</p>`. Place the section immediately after the "All-time totals" `</section>` so it reads as a first-class feature, not buried. Use only semantic tokens (surface-1/2, text-foreground/muted, text-button) — no literal color utilities in this section.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "HabitsClient|habits/page" || echo "habits typechecks clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "initialCardsByState" app/habits/page.tsx` returns a match on the `<HabitsClient` props
    - `grep -n "initialCardsByState" components/HabitsClient.tsx` appears in both the `Props` interface and the destructure
    - `grep -n "Card progress" components/HabitsClient.tsx` returns a match, and the heading sits inside a `<Link href="/history"` element
    - `grep -c "href=\"/history\"" components/HabitsClient.tsx` is exactly 1 (whole section is one Link; no per-tile links — D-08)
    - The four labels "New", "Learning", "Review", "Relearning" all appear in `components/HabitsClient.tsx`
    - `npm run lint` passes (no react-hooks/purity violations — the count lookup is pure, computed during render from props)
  </acceptance_criteria>
  <done>The Habits page shows a real-data "Card progress" 4-tile section on first paint; tapping it navigates to `/history`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a server-side aggregate query and a client `<Link>`; it introduces no new user-input boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Information Disclosure | `cardsByState` counts on Habits page | low | accept | Single-tenant app; the FSRS-state counts are the user's own aggregate data, already sitting behind the existing `middleware.ts` session-cookie gate like every other Habits-page query. No new boundary (RESEARCH Security Domain: V2/V3/V4 do not apply). |

No new npm packages are introduced (RESEARCH: zero new dependencies) — no supply-chain legitimacy checkpoint applies.
</threat_model>

<verification>
- `npm test` green (no regression; this plan adds no unit tests — behavior is UI/aggregate wiring)
- `npx tsc --noEmit` clean for all three files
- `npm run lint` clean
- Manual (per VALIDATION.md HIST-07): `npm run build && npm start`, load `/habits`, confirm the "Card progress" tiles show real counts in the initial HTML with no loading flash, and tapping the section lands on `/history`
</verification>

<success_criteria>
- HIST-04: the history view is reachable from the existing Habits surface (no new bottom-nav tab) via the whole-section Link
- HIST-07: the FSRS-state counts render on first server-rendered paint via the RSC+DTO hydration pattern
- All four state tiles render even when a state has zero cards
</success_criteria>

<output>
Create `.planning/phases/18-review-history-page/18-02-SUMMARY.md` when done.
</output>
