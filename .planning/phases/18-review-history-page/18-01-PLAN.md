---
phase: 18-review-history-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/fsrs.ts
  - lib/dto.ts
  - lib/review-history.ts
  - tests/review-history.test.ts
  - app/api/reviews/route.ts
autonomous: true
requirements: [HIST-04, HIST-05, HIST-06]
must_haves:
  truths:
    - "getReviewHistory returns ReviewLog rows newest-first with grade name + mastery phrase pre-formatted, so the UI renders ready-made strings (D-01)"
    - "Passing a cursor (last-seen row id) returns the NEXT page with no duplicate of the cursor row and no skipped row (skip:1 + compound orderBy)"
    - "Passing a cardId narrows results to that one card only (HIST-05); omitting it returns all cards"
    - "GET /api/reviews validates cursor/cardId as non-empty bounded strings before Prisma and returns ReviewLogDTO[] JSON"
    - "The query is a pure read with zero undo-awareness — undone reviews are returned like any other row (HIST-06)"
  artifacts:
    - lib/fsrs.ts (masteryPhrase exported)
    - lib/dto.ts (ReviewLogDTO added; StatsDTO extended with cardsByState)
    - lib/review-history.ts (getReviewHistory + PAGE_SIZE + pure buildReviewWhere + pure mapReviewLogToDTO)
    - tests/review-history.test.ts (Wave 0 unit tests for grade name/mastery mapping + where clause)
    - app/api/reviews/route.ts (GET ?cursor=&cardId= delegating to getReviewHistory)
  key_links:
    - "orderBy [{createdAt:desc},{id:desc}] + cursor:{id} + skip:1 is the exact tuple that makes pagination stable — dropping skip:1 duplicates the cursor row (RESEARCH Pitfall 2), dropping the id tiebreak skips same-timestamp rows"
    - "masteryPhrase MUST gain `export` in lib/fsrs.ts or lib/review-history.ts fails to compile — previewIntervalLabels already calls it internally, so no logic changes"
    - "getReviewHistory is the single shared fn backing BOTH app/history/page.tsx (18-03) and GET /api/reviews (D-05) — mirrors lib/study-cards.ts:getStudyCards()"
---

<objective>
Build the server-only review-history data pipeline and its paginated HTTP endpoint: a shared `getReviewHistory()` function (cursor-paginated, single-card-filterable, newest-first) plus the DTO/type plumbing and the `GET /api/reviews` route that backs client-side infinite scroll. This is the data foundation both Wave-2 UI plans depend on.

Purpose: Delivers the query mechanics for HIST-04 (reverse-chronological cursor pagination), HIST-05 (single-card filter via `?cardId=`), and HIST-06 (append-only read — the query has no undo-awareness, so undone rows appear unmarked). Per D-05 the same `lib/` function backs both the RSC first page and the API route's subsequent pages, mirroring `lib/study-cards.ts:getStudyCards()`.
Output: `masteryPhrase` exported from `lib/fsrs.ts`; `ReviewLogDTO` + extended `StatsDTO` in `lib/dto.ts`; `lib/review-history.ts` (`getReviewHistory`, `PAGE_SIZE`, pure helpers); `tests/review-history.test.ts`; `app/api/reviews/route.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-review-history-page/18-RESEARCH.md
@.planning/phases/18-review-history-page/18-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (18-01) creates the following symbols consumed by 18-02 and 18-03:
- `masteryPhrase(due: Date, now: Date): string` — now **exported** from `lib/fsrs.ts` (was module-private)
- `ReviewLogDTO` interface in `lib/dto.ts` — fields: `id, createdAt (ISO string), cardId, cardFront, cardType, rating (number), gradeName (string), mastery (string)`
- `StatsDTO.cardsByState: { state: number; stateLabel: string; _count: number }[]` — new field on the existing `StatsDTO`
- `PAGE_SIZE = 25` constant, `ReviewHistoryParams` interface, and `getReviewHistory(params): Promise<ReviewLogDTO[]>` in `lib/review-history.ts`
- `buildReviewWhere(cardId?): { cardId: string } | {}` and `mapReviewLogToDTO(raw): ReviewLogDTO` — pure, unit-tested helpers in `lib/review-history.ts`
- `GET /api/reviews` route handler at `app/api/reviews/route.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Export masteryPhrase + add ReviewLogDTO and StatsDTO.cardsByState</name>
  <files>lib/fsrs.ts, lib/dto.ts</files>
  <read_first>
    - lib/fsrs.ts (see `masteryPhrase` at line 39 — currently `function masteryPhrase(...)`, no export; `Rating`/`State` are already re-exported at line 5)
    - lib/dto.ts (see the `ReviewDTO`/`SentenceDTO` ISO-string convention lines 8-32 and the existing `StatsDTO` lines 71-79 with its `cardsByType` field shape)
    - .planning/phases/18-review-history-page/18-PATTERNS.md (the `lib/dto.ts` and `lib/fsrs.ts` pattern sections)
  </read_first>
  <action>
    In `lib/fsrs.ts`: add the `export` keyword to the existing `masteryPhrase` declaration at line 39 (change `function masteryPhrase(due: Date, now: Date): string` to `export function masteryPhrase(due: Date, now: Date): string`). Make NO other change — `previewIntervalLabels` at line 52 already calls it internally and is unaffected. This satisfies D-01's requirement that history rows reuse the exact same mastery-language function rather than duplicating the logic.

    In `lib/dto.ts`: add a new exported `ReviewLogDTO` interface with fields `id: string`, `createdAt: string` (ISO string per RSC-05), `cardId: string`, `cardFront: string`, `cardType: string`, `rating: number`, `gradeName: string`, `mastery: string`. Place it near the existing `ReviewDTO`. Then extend the existing `StatsDTO` interface with one new field `cardsByState: { state: number; stateLabel: string; _count: number }[]` (mirrors the existing `cardsByType` field shape exactly; per D-07). Do not remove or reorder existing `StatsDTO` fields — 18-02 depends on the full existing shape plus this addition.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "lib/(fsrs|dto)\.ts" || echo "no type errors in fsrs/dto"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function masteryPhrase" lib/fsrs.ts` returns exactly one match
    - `grep -c "previewIntervalLabels" lib/fsrs.ts` is unchanged (masteryPhrase body untouched, only the modifier added)
    - `grep -n "interface ReviewLogDTO" lib/dto.ts` returns one match, and the interface contains all of: `gradeName`, `mastery`, `cardFront`, `cardType`, and `createdAt: string`
    - `grep -n "cardsByState" lib/dto.ts` returns a match inside the `StatsDTO` interface with shape `{ state: number; stateLabel: string; _count: number }[]`
    - `npx tsc --noEmit` reports no new errors originating in `lib/fsrs.ts` or `lib/dto.ts`
  </acceptance_criteria>
  <done>`masteryPhrase` is importable from `@/lib/fsrs`; `ReviewLogDTO` and `StatsDTO.cardsByState` exist as typed exports for downstream plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/review-history.ts pipeline + Wave 0 unit tests</name>
  <files>lib/review-history.ts, tests/review-history.test.ts</files>
  <behavior>
    - mapReviewLogToDTO({rating:3, nextReview, createdAt, card:{front,type}, ...}) → DTO whose `gradeName` is `Rating[3]` === "Good" and whose `mastery` equals `masteryPhrase(nextReview, createdAt)` (e.g. a 3-day-out nextReview yields a "Memory strengthening → 3d" style string)
    - mapReviewLogToDTO maps rating 1→"Again", 2→"Hard", 3→"Good", 4→"Easy" via the `Rating` enum reverse-lookup (no hand-rolled table)
    - mapReviewLogToDTO serializes `createdAt` (a Date) to an ISO string; `cardFront`/`cardType` come from the nested `card`
    - buildReviewWhere("abc123") → `{ cardId: "abc123" }`; buildReviewWhere(null) and buildReviewWhere(undefined) → `{}`
  </behavior>
  <read_first>
    - lib/study-cards.ts (the exact shape to mirror: params interface + shared async fn + serialize-Dates-to-ISO map at the end; lib throws on DB error, caller catches — do NOT wrap the Prisma call in try/catch here)
    - lib/fsrs.ts (Rating enum re-export + the now-exported masteryPhrase from Task 1)
    - lib/dto.ts (ReviewLogDTO shape from Task 1)
    - prisma/schema.prisma (model ReviewLog, lines 100-120 — fields id, createdAt, cardId, rating, nextReview, and the `card` relation with `front`/`type`; @@index([cardId]) and @@index([createdAt]) already exist)
    - tests/sequence.test.ts (the project's pure-function test convention: hand-constructed fixture objects, `describe`/`it`/`expect`, no Prisma mocking)
    - .planning/phases/18-review-history-page/18-RESEARCH.md (Pattern 1 cursor pagination + Pitfall 2 skip:1 + Pitfall 5 include-is-not-N+1)
  </read_first>
  <action>
    Create `lib/review-history.ts` as a server-only module (leading comment `// No 'use client' — this module runs server-side only.`, mirroring `lib/study-cards.ts`). Import `prisma` from `@/lib/prisma`, `Rating` and `masteryPhrase` from `@/lib/fsrs`, and `type ReviewLogDTO` from `@/lib/dto`. Export `const PAGE_SIZE = 25` (per RESEARCH Open Question 2 — middle of the "~20-30/page" range; single source both the RSC page and the route reference). Export `interface ReviewHistoryParams { cardId?: string | null; cursor?: string | null; take?: number }`.

    Factor two PURE, exported helpers so they are unit-testable without a live DB (per VALIDATION.md Wave 0): (a) `buildReviewWhere(cardId?: string | null)` returns `cardId ? { cardId } : {}`; (b) `mapReviewLogToDTO(raw)` takes a raw ReviewLog row that includes `{ card: { front, type } }` and returns a `ReviewLogDTO` — set `gradeName: Rating[raw.rating]`, `mastery: masteryPhrase(raw.nextReview, raw.createdAt)`, `createdAt: raw.createdAt.toISOString()`, `cardFront: raw.card.front`, `cardType: raw.card.type`, plus `id`, `cardId`, `rating`. Define a small input type for the raw row (id, createdAt: Date, cardId, rating: number, nextReview: Date, card: { front: string; type: string }).

    Export `async function getReviewHistory(params: ReviewHistoryParams): Promise<ReviewLogDTO[]>`: destructure `{ cardId, cursor, take = PAGE_SIZE }`; call `prisma.reviewLog.findMany` with `where: buildReviewWhere(cardId)`, `orderBy: [{ createdAt: 'desc' }, { id: 'desc' }]`, `take`, spread `...(cursor ? { cursor: { id: cursor }, skip: 1 } : {})`, and `include: { card: { select: { id: true, front: true, type: true } } }`; return `logs.map(mapReviewLogToDTO)`. Do NOT wrap the Prisma call in try/catch (the route catches — matches the project convention). The `skip: 1` is load-bearing (RESEARCH Pitfall 2): without it the cursor row re-appears as a duplicate on every subsequent page.

    Create `tests/review-history.test.ts` importing from `../lib/review-history` and `../lib/fsrs`. Cover the four behaviors listed in `<behavior>`: a "gradeName" describe block asserting rating→name mapping for all of 1/2/3/4 and that `mastery` equals `masteryPhrase(nextReview, createdAt)` for a fixed fixture; a "where clause" describe block asserting `buildReviewWhere` returns `{ cardId }` for a non-empty id and `{}` for null/undefined. Use hand-constructed fixture objects (fixed `new Date('2026-07-04T...')` values) exactly like `tests/sequence.test.ts` — no Prisma client, no DB.
  </action>
  <verify>
    <automated>npx vitest run tests/review-history.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/review-history.test.ts` passes with tests named to match both `-t "gradeName"` and `-t "where clause"`
    - `grep -n "export const PAGE_SIZE = 25" lib/review-history.ts` returns one match
    - `grep -n "skip: 1" lib/review-history.ts` returns one match (guards RESEARCH Pitfall 2)
    - `grep -n "orderBy: \[{ createdAt: 'desc' }, { id: 'desc' }\]" lib/review-history.ts` returns a match (compound tiebreak present)
    - `grep -n "Rating\[" lib/review-history.ts` returns a match (enum reverse-lookup, not a hand-rolled label table)
    - `grep -n "export function buildReviewWhere\|export function mapReviewLogToDTO" lib/review-history.ts` returns two matches (pure helpers are exported for testing)
    - `getReviewHistory` contains no `try`/`catch` (lib throws; route catches)
  </acceptance_criteria>
  <done>`getReviewHistory` returns newest-first, cursor-paginated, optionally card-filtered `ReviewLogDTO[]`; the pure helpers pass their Wave-0 unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: GET /api/reviews route with V5 input validation</name>
  <files>app/api/reviews/route.ts</files>
  <read_first>
    - app/api/cards/due/route.ts (the exact try/catch → 500 shape + the module-level regex validation idiom to adapt; note this uses INTEGER_RE for integers — cursor/cardId are opaque cuid strings, so adapt the idiom to a length/non-empty check, not INTEGER_RE)
    - lib/review-history.ts (getReviewHistory + PAGE_SIZE from Task 2)
    - .planning/phases/18-review-history-page/18-RESEARCH.md (Security Domain → V5 Input Validation row + the `GET /api/reviews/route.ts` code example + the DoS/tampering threat table)
  </read_first>
  <action>
    Create `app/api/reviews/route.ts` exporting an async `GET(req: NextRequest)`. Inside a single top-level try/catch: read `cursor` and `cardId` from `req.nextUrl.searchParams` (both may be null when absent). Apply V5 input validation (per RESEARCH Security Domain): define a module-level `const MAX_ID_LEN = 64` and a small guard `isValidOpaqueId(v: string)` returning `v.length > 0 && v.length <= MAX_ID_LEN`. If `cursor` is present (non-null) and fails the guard, OR `cardId` is present and fails the guard, return `NextResponse.json({ error: 'invalid parameter' }, { status: 400 })` — this mirrors the 400-on-bad-param idiom in `app/api/cards/due/route.ts` and bounds the input before it reaches Prisma. Do NOT read or accept a client-controlled `take`/limit param — call `getReviewHistory({ cardId, cursor, take: PAGE_SIZE })` with the server-side `PAGE_SIZE` constant only (prevents the unbounded-take DoS in the threat table). Return `NextResponse.json(logs)`. On any thrown error, `catch` returns `NextResponse.json({ error: 'Database error' }, { status: 500 })`. Import `getReviewHistory, PAGE_SIZE` from `@/lib/review-history` and `NextRequest, NextResponse` from `next/server`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "app/api/reviews/route.ts" || echo "route typechecks clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MAX_ID_LEN" app/api/reviews/route.ts` returns a match and the guard rejects empty and over-length strings (V5 validation present before Prisma)
    - `grep -n "status: 400" app/api/reviews/route.ts` returns a match (invalid param path) and `grep -n "status: 500" app/api/reviews/route.ts` returns a match (DB-error path)
    - `grep -n "take: PAGE_SIZE" app/api/reviews/route.ts` returns a match AND `grep -c "searchParams.get('take')\|searchParams.get(\"take\")" app/api/reviews/route.ts` is 0 (take is never client-controlled)
    - The whole handler body is wrapped in one try/catch (mirrors `app/api/cards/due/route.ts`)
    - `npx tsc --noEmit` reports no error in `app/api/reviews/route.ts`
  </acceptance_criteria>
  <done>`GET /api/reviews?cursor=&cardId=` returns `ReviewLogDTO[]` JSON, validates both opaque-id params, and never exposes a client-controlled page size.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/reviews | Untrusted `cursor` and `cardId` query strings cross into the server and reach Prisma |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Denial of Service | `GET /api/reviews` page size | medium | mitigate | `take` is never read from `searchParams`; the route always calls `getReviewHistory({ take: PAGE_SIZE })` with the server-side constant (25). No client-controlled limit exists (RESEARCH Security Domain). |
| T-18-02 | Tampering | `cursor`/`cardId` query params → Prisma `where`/`cursor` | low | mitigate | V5 validation: `isValidOpaqueId` enforces non-empty, ≤64-char strings before Prisma; Prisma's parameterized query builder prevents SQL injection by construction; a garbage id simply matches zero rows (safe no-op). |
| T-18-03 | Information Disclosure | `cardId` for a non-existent/other card | low | accept | Single-tenant app, no per-user row ownership (V4 N/A per RESEARCH); a non-existent `cardId` yields an empty result set — no different in kind from any other not-found lookup already in this codebase. |

No new npm packages are introduced in this plan (RESEARCH: zero new dependencies), so no supply-chain (T-*-SC) legitimacy checkpoint applies.
</threat_model>

<verification>
- `npx vitest run tests/review-history.test.ts` green (pure helpers correct)
- `npm test` green (no regression to the existing suite)
- `npx tsc --noEmit` clean for all five files
- `npm run lint` clean (react-hooks rules unaffected — this plan is all server-side)
</verification>

<success_criteria>
- HIST-04: `getReviewHistory` returns reverse-chronological rows with stable cursor pagination (skip:1 + compound orderBy) and pre-formatted grade + mastery strings
- HIST-05: `?cardId=` narrows results to a single card through `buildReviewWhere`
- HIST-06: the query is a pure read with no undo-awareness — undone rows are returned unmarked
- The route validates both opaque-id params (V5) and returns `ReviewLogDTO[]`
</success_criteria>

<output>
Create `.planning/phases/18-review-history-page/18-01-SUMMARY.md` when done.
</output>
