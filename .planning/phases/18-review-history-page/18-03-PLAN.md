---
phase: 18-review-history-page
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - app/history/page.tsx
  - app/history/loading.tsx
  - components/HistoryClient.tsx
  - components/CardEditor.tsx
autonomous: true
requirements: [HIST-04, HIST-05, HIST-06, HIST-07]
must_haves:
  truths:
    - "Opening /history shows a reverse-chronological feed of reviews, each row showing 'GradeName — mastery phrase' (not a raw 1-4) plus the card front + timestamp (D-01)"
    - "Scrolling to the bottom auto-loads the next page via an IntersectionObserver sentinel — no 'Load more' button (D-04)"
    - "Weak grades (Again red / Hard amber) visually stand out; Good/Easy stay neutral (D-02/D-03)"
    - "Opening /history?cardId=X shows only that card's reviews with an 'All cards ×' chip that clears back to the full feed (D-10/D-11, HIST-05)"
    - "CardEditor has a 'View history →' link opening /history?cardId={card.id} (D-10)"
    - "Real rows appear on the first server-rendered paint — no loading flash (HIST-07); undone reviews appear unmarked (HIST-06)"
  artifacts:
    - app/history/page.tsx (thin async RSC — reads ?cardId=, calls getReviewHistory, renders HistoryClient)
    - app/history/loading.tsx (static skeleton, client-nav fallback only)
    - components/HistoryClient.tsx ('use client' — rows, grade colors, IntersectionObserver infinite scroll, All-cards chip, empty/error states)
    - components/CardEditor.tsx (+"View history →" link to /history?cardId=)
  key_links:
    - "HistoryClient's loadMore fetches /api/reviews?cursor=<lastId>&cardId= — the route (18-01) must exist and return ReviewLogDTO[] with matching field names"
    - "A useRef in-flight guard (loadingRef), not useState alone, prevents duplicate page fetches under fast scroll (RESEARCH Pitfall 3); skip:1 in the route prevents duplicate rows (RESEARCH Pitfall 2)"
    - "hasMore is seeded from initialLogs.length === pageSize and updated from each fetched page's length — drives whether the sentinel keeps observing"
---

<objective>
Build the `/history` page: a thin RSC that hydrates real rows on first paint, plus the `HistoryClient` shell that renders the reverse-chronological feed with mastery-language grades, color-coded weak grades, IntersectionObserver-driven infinite scroll, and the single-card filter chip — and add the "View history" entry link inside `CardEditor`.

Purpose: Delivers the visible surface for HIST-04 (paginated reverse-chron feed), HIST-05 (single-card filter via the chip + CardEditor deep-link), HIST-06 (undone reviews render unmarked — the client just displays what the query returns), and HIST-07 (RSC+DTO+client-shell hydration, no loading flash). Follows the `/habits` structural precedent exactly (D-09).
Output: `app/history/page.tsx`, `app/history/loading.tsx`, `components/HistoryClient.tsx`, and a "View history →" link in `components/CardEditor.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-review-history-page/18-UI-SPEC.md
@.planning/phases/18-review-history-page/18-RESEARCH.md
@.planning/phases/18-review-history-page/18-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (18-03) creates:
- `app/history/page.tsx` — async RSC, `export const dynamic = 'force-dynamic'`, reads `searchParams` `?cardId=`, calls `getReviewHistory({ cardId, cursor: null })`, resolves the filter card front, renders `<HistoryClient>`
- `app/history/loading.tsx` — static skeleton (header + N `bg-surface-3 animate-pulse` row blocks)
- `components/HistoryClient.tsx` — `'use client'` shell: list state, IntersectionObserver sentinel, grade-color rows, "All cards ×" chip, empty + error states, spinner sentinel
- `components/CardEditor.tsx` — a "View history →" `<Link>` to `/history?cardId={card.id}`

Consumes 18-01's `getReviewHistory`, `PAGE_SIZE`, `ReviewLogDTO`, and `GET /api/reviews`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: /history RSC page + loading skeleton</name>
  <files>app/history/page.tsx, app/history/loading.tsx</files>
  <read_first>
    - app/habits/page.tsx (the thin async RSC shape to mirror exactly: `export const dynamic = 'force-dynamic'`, await data via lib fn, render one `*Client` with `initialX` props)
    - app/habits/loading.tsx (the static skeleton convention: no 'use client', no hooks, `bg-surface-3 animate-pulse` blocks shaped like the real content)
    - lib/review-history.ts (getReviewHistory + PAGE_SIZE from 18-01)
    - lib/prisma.ts (for the filter-card-front lookup)
    - .planning/phases/18-review-history-page/18-PATTERNS.md (the `app/history/page.tsx` + `app/history/loading.tsx` pattern sections, including the Next.js 16 `searchParams: Promise<...>` await note)
  </read_first>
  <action>
    Create `app/history/page.tsx` as a server component (no 'use client'): `export const dynamic = 'force-dynamic'` (live DB data, same reasoning as `app/habits/page.tsx`). The default export is `async function HistoryPage({ searchParams }: { searchParams: Promise<{ cardId?: string }> })` — Next.js 16 passes `searchParams` as a Promise, so `await` it. Read `cardId` (default null when absent). Call `getReviewHistory({ cardId, cursor: null })` for the first page. Resolve the chip label: `const filterCardFront = cardId ? (initialLogs[0]?.cardFront ?? (await prisma.card.findUnique({ where: { id: cardId }, select: { front: true } }))?.front ?? null) : null` (first row's front is free; only fall back to a single indexed PK lookup when the filtered card has zero history). Render `<HistoryClient initialLogs={initialLogs} cardId={cardId ?? null} cardFront={filterCardFront} pageSize={PAGE_SIZE} />`. Import `getReviewHistory, PAGE_SIZE` from `@/lib/review-history`, `prisma` from `@/lib/prisma`, and `HistoryClient` from `@/components/HistoryClient`.

    Create `app/history/loading.tsx` as a static server component (no 'use client', no hooks) matching `app/habits/loading.tsx`'s idiom: a header row (`h-8 w-32` title block + `h-5 w-24` back-link block, both `rounded bg-surface-3 animate-pulse`) then a `flex flex-col gap-1` column of ~8 `h-14 w-full rounded-xl bg-surface-3 animate-pulse` row placeholders. Per CLAUDE.md, `loading.tsx` only covers client-side nav transitions — first load is handled by RSC hydration.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "app/history/(page|loading)" || echo "history route typechecks clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const dynamic = 'force-dynamic'" app/history/page.tsx` returns a match
    - `grep -n "searchParams: Promise" app/history/page.tsx` returns a match and `cardId` is read from the awaited value
    - `grep -n "getReviewHistory" app/history/page.tsx` returns a match with `cursor: null` for the first page
    - `grep -n "cardId ?" app/history/page.tsx` shows the filterCardFront resolution (first-row front with a findUnique fallback)
    - `app/history/loading.tsx` contains `bg-surface-3 animate-pulse` and has no `'use client'` and no hook imports
    - `npx tsc --noEmit` reports no error in either file
  </acceptance_criteria>
  <done>`/history` server-renders the first page of real rows (optionally card-filtered) and passes them to `HistoryClient`; a client-nav skeleton exists.</done>
</task>

<task type="auto">
  <name>Task 2: HistoryClient — feed rows, grade colors, infinite scroll, filter chip, empty/error states</name>
  <files>components/HistoryClient.tsx</files>
  <read_first>
    - components/HabitsClient.tsx (the 'use client' shell shape: `Props` interface, `initialX`→`useState` seeding, the `<main className="max-w-2xl mx-auto px-4 py-8 flex flex-col gap-6">` container + header + back-link idiom to mirror)
    - components/AudioButton.tsx (lines 105-113 — the ONLY reusable spinner idiom in this codebase: `<span className="inline-block border-2 border-button border-t-transparent rounded-full animate-spin ...">`; RESEARCH Pitfall 4 confirms SyncPanel.tsx has NO spinner — do not reference it)
    - components/LessonRangeFilter.tsx (lines 67-74 — the clear-filter chip/button style `text-xs text-button hover:text-button-hover px-3 min-h-11 inline-flex items-center rounded-md hover:bg-button-soft` to reuse for the "All cards ×" chip)
    - components/FlashcardMode.tsx (line 220 — the exact "Again" red literal `bg-red-50 text-red-500 dark:bg-red-500/10 dark:text-red-400` already established for the Again grade button)
    - components/CardEditor.tsx (lines 164-181 — the existing `amber-400/amber-500`/`text-amber-500 dark:text-amber-400` warnUnsafe precedent that the Hard grade row reuses)
    - lib/dto.ts (ReviewLogDTO shape from 18-01)
    - .planning/phases/18-review-history-page/18-RESEARCH.md (Pattern 2 — the full ESLint-purity-safe IntersectionObserver code, including the `loadingRef` race guard; Pitfall 1 — Hard=amber not the miscited hue; Pitfall 3 — useRef guard; Pitfall 5)
    - .planning/phases/18-review-history-page/18-UI-SPEC.md (Color grade-row mapping table, Copywriting Contract for row copy + empty/error/chip states, Layout & Component Notes for /history)
    - .planning/phases/18-review-history-page/18-PATTERNS.md (the `components/HistoryClient.tsx` pattern block — props, IntersectionObserver effect, gradeRowClass map, spinner, chip, page shell all specified)
  </read_first>
  <action>
    Create `components/HistoryClient.tsx` with `'use client'` as the very first line, then imports: `useEffect, useRef, useState, useCallback` from `react`, `Link` from `next/link`, `useRouter` from `next/navigation`, and `type { ReviewLogDTO } from '@/lib/dto'`. `Props`: `{ initialLogs: ReviewLogDTO[]; cardId: string | null; cardFront: string | null; pageSize: number }`.

    State: `rows` (`useState<ReviewLogDTO[]>(initialLogs)`), `hasMore` (`useState(initialLogs.length === pageSize)`), `loadingMore` (`useState(false)`), plus `loadingRef = useRef(false)` (the synchronous in-flight guard) and `sentinelRef = useRef<HTMLDivElement>(null)`. Add an `error` boolean state for the fetch-failure UI. Implement `loadMore` as the `useCallback` from RESEARCH Pattern 2 verbatim: early-return if `loadingRef.current || !hasMore`; set both `loadingRef.current = true` and `setLoadingMore(true)`; build the query string from the last row's `id` (`cursor`) plus `cardId` when present; `fetch('/api/reviews?' + qs)`; on `!res.ok` throw; append the parsed `ReviewLogDTO[]` to `rows` and set `hasMore` from `next.length === pageSize`; on catch set the `error` flag (leave `hasMore` as-is so a retry can re-fire); in `finally` clear both guards. Wire the IntersectionObserver in a `useEffect` per Pattern 2: observe `sentinelRef.current` with `{ rootMargin: '200px' }`, call `loadMore()` on intersection, and `return () => observer.disconnect()` for cleanup. Everything impure (observer construction, fetch) runs inside the effect/callback — nothing during render (react-hooks/purity); `setRows`/`setLoadingMore` only fire inside the async `loadMore` (post-await), never synchronously in the effect body (react-hooks/set-state-in-effect).

    Row rendering: define `const gradeRowClass: Record<string, string>` mapping `Again` → the FlashcardMode red literal, `Hard` → the amber warnUnsafe literal from CardEditor (`bg-amber-50 text-amber-600 dark:bg-amber-500/10 dark:text-amber-400` — the deliberate choice confirmed in UI-SPEC/RESEARCH Pitfall 1; do NOT invent a different warm hue), `Good` and `Easy` → `bg-surface-2 text-foreground` (neutral, per D-03 — no separate FSRS-state badge). Each row: a `divide-y divide-border` list of items, each `p-4 flex flex-col gap-1`; top line = a grade pill (grade-colored, `text-xs font-semibold rounded px-2 py-0.5`) showing `row.gradeName` + the card front (`hangul` class, `text-sm font-medium text-foreground truncate`); bottom line = `text-xs text-muted` showing `row.mastery` + a formatted timestamp from `row.createdAt` (format via `new Date(row.createdAt).toLocaleString('en-US', { month:'short', day:'numeric', year:'numeric', hour:'numeric', minute:'2-digit' })` inside render — this is pure given the ISO string prop, no `new Date()` with no args). Render `${row.gradeName} — ${row.mastery}` semantics per D-01 (e.g. "Good — Memory strengthening → 3d").

    Filter chip (D-11): when `cardId` is set, render above the list an "All cards ×" clear chip using the LessonRangeFilter button style; its label is the `cardFront` (Korean, `hangul`) followed by `×`, and `onClick` calls `router.push('/history')` to clear the filter.

    Sentinel + states: after the list, render `<div ref={sentinelRef} className="h-4" />`; while `loadingMore`, show a centered spinner reusing AudioButton's idiom (`<span className="inline-block border-2 border-button border-t-transparent rounded-full animate-spin h-4 w-4" />`) — no "Load more" text (D-04). Empty state (rows.length === 0): "No reviews yet" heading + "Start a study session to build your review history." body, OR "No reviews for this card yet." when `cardId` is set (per UI-SPEC copy). Error state: when `error`, show "Couldn't load review history. Check your connection and try again." with a "Retry" button (`bg-button text-button-foreground`) that clears `error` and calls `loadMore()`.

    Page shell: `<main className="max-w-2xl mx-auto px-4 py-8 flex flex-col gap-6">` (identical to HabitsClient) with a header `<h1 className="text-2xl font-bold text-foreground">History</h1>` + a "← Habits" back-link (`text-sm text-muted hover:text-muted-foreground underline underline-offset-2`, `href="/habits"`). Consume `rows` in server order — no client re-sort (the query already returns createdAt DESC).
  </action>
  <verify>
    <automated>npm run lint 2>&1 | grep -E "HistoryClient" || echo "HistoryClient lint clean"</automated>
  </verify>
  <acceptance_criteria>
    - First line of `components/HistoryClient.tsx` is exactly `'use client'`
    - `grep -n "loadingRef = useRef" components/HistoryClient.tsx` returns a match (synchronous in-flight guard — RESEARCH Pitfall 3) and `grep -n "IntersectionObserver" components/HistoryClient.tsx` returns a match with a `disconnect()` cleanup
    - `grep -n "rootMargin: '200px'" components/HistoryClient.tsx` returns a match
    - `grep -c "amber-" components/HistoryClient.tsx` is >= 1 (Hard row uses the amber warnUnsafe precedent) and `grep -c "bg-red-50" components/HistoryClient.tsx` is >= 1 (Again row)
    - `grep -n "animate-spin" components/HistoryClient.tsx` returns the AudioButton spinner idiom (bordered circle with `border-t-transparent`), and `grep -c "SyncPanel" components/HistoryClient.tsx` is 0
    - `grep -n "router.push('/history')" components/HistoryClient.tsx` returns a match (clear-filter chip — D-11)
    - `grep -n "No reviews yet\|No reviews for this card yet" components/HistoryClient.tsx` returns matches (both empty states) and `grep -n "Couldn't load review history" components/HistoryClient.tsx` returns the error state
    - `npm run lint` passes with no react-hooks/purity or set-state-in-effect violation in this file
  </acceptance_criteria>
  <done>`HistoryClient` renders the color-coded reverse-chron feed, auto-loads pages on scroll without duplicates, supports the single-card chip filter, and handles empty/error states.</done>
</task>

<task type="auto">
  <name>Task 3: CardEditor "View history" deep-link</name>
  <files>components/CardEditor.tsx</files>
  <read_first>
    - components/CardEditor.tsx (the action-buttons row at the bottom, lines 221-237 (Cancel/Save); `card.id` is always available on the `card` prop; the file is already `'use client'`)
    - .planning/phases/18-review-history-page/18-UI-SPEC.md (CardEditor addition D-10 — plain text link `text-sm text-button hover:text-button-hover`, "View history →", secondary navigation, not a button, placed in a sensible existing action-row location)
    - .planning/phases/18-review-history-page/18-PATTERNS.md (the `components/CardEditor.tsx` +View history link pattern block)
  </read_first>
  <action>
    In `components/CardEditor.tsx`: import `Link` from `next/link`. Add a "View history →" link that navigates to `/history?cardId=${card.id}` (D-10 — deep-links into the pre-filtered history view). Style it as a plain text link, NOT a button: `className="text-sm text-button hover:text-button-hover"`. Place it in the bottom action-buttons row (the `flex gap-2 justify-end` container with Cancel/Save): change that row to `justify-between` and put the "View history →" `<Link>` on the left, keeping the Cancel/Save group on the right (wrap Cancel+Save in their own `<div className="flex gap-2">`). This keeps it as secondary navigation next to the primary actions without adding a heavy button. Do not alter the save/cancel behavior.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "CardEditor" || echo "CardEditor typechecks clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "history?cardId=" components/CardEditor.tsx` returns a match using the template literal `card.id`
    - `grep -n "View history" components/CardEditor.tsx` returns a match with `className` containing `text-button` (plain link, not `bg-button`)
    - `grep -n "import Link from 'next/link'" components/CardEditor.tsx` returns a match
    - The existing Save button (`disabled={saving || !front || !back}`) and Cancel handler are unchanged
    - `npm run lint` passes for `components/CardEditor.tsx`
  </acceptance_criteria>
  <done>The card editor exposes a "View history →" link into `/history` pre-filtered to that card.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/reviews (client fetch) | `HistoryClient.loadMore` sends a `cursor` derived from a server-provided row `id` plus the current `cardId`; the boundary itself is validated server-side in plan 18-01 (T-18-01/T-18-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-05 | Denial of Service | client infinite-scroll loop | low | mitigate | `loadingRef` synchronous guard + `hasMore` gating prevent duplicate/runaway fetches (RESEARCH Pitfall 3); the observer disconnects on cleanup and stops firing once `hasMore` is false. |
| T-18-06 | Tampering | `cursor`/`cardId` in the client fetch URL | low | mitigate | Validation lives at the server boundary in 18-01 (`isValidOpaqueId`); the client only forwards a server-issued row id and the current URL `cardId`. Nothing security-sensitive is trusted client-side. |

No new npm packages are introduced (native `IntersectionObserver`, RESEARCH: zero new dependencies) — no supply-chain legitimacy checkpoint applies.
</threat_model>

<verification>
- `npm run lint` clean (react-hooks/purity + set-state-in-effect satisfied per RESEARCH Pattern 2)
- `npx tsc --noEmit` clean for all four files
- `npm test` green (no regression)
- Manual UAT (per VALIDATION.md, no live-DB test harness exists):
  - HIST-04/07: `npm run build && npm start`, load `/history` directly, confirm real rows in the initial HTML with no loading flash; seed 50+ ReviewLog rows and scroll through 2+ pages confirming no `ReviewLog.id` appears twice and none is skipped
  - HIST-05: open a card in CardEditor → "View history →" → confirm the feed is filtered to that card and the "All cards ×" chip clears it
  - HIST-06: grade a card, undo it, open `/history` → the review still appears, unmarked
</verification>

<success_criteria>
- HIST-04: reverse-chronological, cursor-paginated feed (~25/page) with mastery-language grades, reachable and auto-loading on scroll
- HIST-05: single-card filter via CardEditor deep-link + clearable chip
- HIST-06: undone reviews render unmarked (client displays exactly what the append-only query returns)
- HIST-07: real rows on first server-rendered paint, no loading flash
</success_criteria>

<output>
Create `.planning/phases/18-review-history-page/18-03-SUMMARY.md` when done.
</output>
