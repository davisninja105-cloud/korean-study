---
phase: 21-card-database-quality-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/audit-checks.ts
  - tests/audit-checks.test.ts
autonomous: true
requirements: [AUDIT-02]

must_haves:
  truths:
    - "npx vitest run tests/audit-checks.test.ts passes, with fixtures covering all six audit check classes plus malformed-JSON handling"
    - "classifyBlankSafety returns zero-sentences / zero-safe / unsafe-first / ok exactly as sentenceMatch().safeToBlank dictates — no reimplemented blank-safety logic"
    - "Gloss-paren fronts like '~(으)로 (direction particle)' are NOT flagged as romanization; Latin letters inside a sentence's korean field ARE flagged"
    - "Malformed distractors or components JSON produces a finding value, never a thrown error"
    - "lib/audit-checks.ts imports only './sentence-match', './card-key', and './filter-components' — no Prisma, no fs, no Anthropic SDK"
    - "npm test full suite stays green (145+ tests as of Phase 20 close)"
  artifacts:
    - "lib/audit-checks.ts"
    - "tests/audit-checks.test.ts"
  key_links:
    - "lib/audit-checks.ts calls sentenceMatch from ./sentence-match for every blank-safety decision"
    - "lib/audit-checks.ts calls normalizeFront from ./card-key for romanization gloss-stripping and normalizedFront recompute"
    - "lib/audit-checks.ts calls filterComponents from ./filter-components for the stale-components summary count (splitParticle is reused transitively through it)"
    - "tests/audit-checks.test.ts imports from '../lib/audit-checks' (relative, no .js extension)"
---

<objective>
Create the pure, Vitest-covered audit module `lib/audit-checks.ts` that implements every check class for the Phase 21 deck audit by reusing production helpers (`sentenceMatch`, `splitParticle` via `filterComponents`, `normalizeFront`, `filterComponents`) rather than reimplementing them (AUDIT-02).

Purpose: audit truth must be identical to production truth — Phase 22's prompt revisions and corpus fixes will be judged against these exact predicates, so they must be the same code paths `normalizeExtractedCards` enforces at extraction time.
Output: `lib/audit-checks.ts` (pure module, named exports, zero I/O) + `tests/audit-checks.test.ts` (Vitest, node env, real Korean fixtures). This module is the sole home of check logic; the Wave 2 script is a thin I/O adapter over it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-card-database-quality-audit/21-RESEARCH.md
@.planning/phases/21-card-database-quality-audit/21-PATTERNS.md
@lib/sentence-match.ts
@lib/card-key.ts
@lib/filter-components.ts
@tests/filter-components.test.ts
@scripts/find-duplicates.mjs
</context>

## Artifacts this phase produces

Phase 21 creates exactly these symbols/files (this plan creates the first two; Plan 21-02 creates the rest):

- **`lib/audit-checks.ts`** (this plan) — named exports:
  - Types: `AuditSentence`, `AuditCardInput`, `CardRef`, `BlankSafetyClass`, `DistractorAnomaly`, `AuditFindings`
  - Functions: `classifyBlankSafety`, `notFoundSentenceIndices`, `frontHasRomanization`, `sentenceHasRomanization`, `checkDistractors`, `normalizedFrontMismatch`, `frontUntrimmed`, `superNormalize`, `clusterNearDuplicates`, `countStaleComponents`, `runAuditChecks`
- **`tests/audit-checks.test.ts`** (this plan) — Vitest coverage for every exported function
- **`scripts/audit-cards.mts`** (Plan 21-02) — read-only script: Prisma read → `runAuditChecks` → console summary + dated markdown report
- **`.planning/audits/`** (Plan 21-02, created at script runtime via recursive mkdir) — holds `card-audit-YYYY-MM-DD.md`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Module scaffold + blank-safety classification (adapter over sentenceMatch)</name>
  <files>lib/audit-checks.ts, tests/audit-checks.test.ts</files>
  <read_first>
    - .planning/phases/21-card-database-quality-audit/21-PATTERNS.md (sections: lib/audit-checks.ts analog, tests analog — contains the exact header-comment, import, and test-structure excerpts to replicate)
    - lib/filter-components.ts (the structural analog: header contract JSDoc, relative sibling imports, named export, plain-data params)
    - lib/sentence-match.ts (the MatchResult interface and sentenceMatch/splitParticle signatures — the predicates being adapted, never reimplemented)
    - tests/filter-components.test.ts (test file template: describe-per-function, behavior-sentence it names, Korean fixtures with comments)
    - .planning/phases/21-card-database-quality-audit/21-RESEARCH.md (section "Concrete Definitions of the Six Check Classes", item 1; section "Code Examples" — blank-safety classification example)
  </read_first>
  <behavior>
    - classifyBlankSafety([]) returns 'zero-sentences'
    - classifyBlankSafety where every sentence fails safeToBlank (e.g. targetForm is 1 char, or occurs twice in korean) returns 'zero-safe'
    - classifyBlankSafety where sentences[0] fails safeToBlank but a later sentence passes returns 'unsafe-first'
    - classifyBlankSafety where sentences[0] passes safeToBlank returns 'ok'
    - notFoundSentenceIndices returns the indices of sentences whose targetForm does not appear verbatim in korean (sentenceMatch found === false), and [] when all are found
    - Fixtures are real Korean (e.g. targetForm 먹다 in 저는 밥을 먹다 for ok; single-char targetForm 가 for unsafe; a targetForm absent from its korean for not-found) with explanatory comments, matching the tests/filter-components.test.ts style; test names reference (AUDIT-02)
  </behavior>
  <action>
    Write the failing tests first in a new tests/audit-checks.test.ts (imports: describe/it/expect from vitest; functions from '../lib/audit-checks' — relative path, NO .js extension per test convention). Then create lib/audit-checks.ts to make them pass.

    Module scaffold (per the lib/filter-components.ts analog in 21-PATTERNS.md): opening JSDoc contract block stating this is the single source of truth for Phase 21 audit predicates, that every predicate delegates to production helpers, that the module is pure (plain data in, findings out; safe server AND client; no side effects), and listing call sites (scripts/audit-cards.mts, tests/audit-checks.test.ts). Imports are relative sibling imports only, following the analog exactly: sentenceMatch from './sentence-match', normalizeFront from './card-key', filterComponents from './filter-components'. Never import from lib/extract-cards.ts (it pulls the Anthropic SDK at module top — RESEARCH.md anti-pattern) and never import lib/prisma.ts, fs, or any Node builtin.

    Define and export the shared plain-data types used by all tasks in this plan and by the Wave 2 script:
    - interface AuditSentence with fields korean (string), targetForm (string), orderIndex (number)
    - interface AuditCardInput with fields id, type, front, back (strings), notes (string or null), normalizedFront (string), components (string or null — RAW JSON column, unparsed), distractors (string or null — RAW JSON column, unparsed), clozeSentence (string or null), lessonId (string or null), sentences (AuditSentence[], contract: pre-sorted ascending by orderIndex by the caller)
    - interface CardRef with fields id, type, front, back — the echo shape every finding carries (ids are mandatory so Phase 22 can fix in place by id, per the STATE.md v1.5 hard rule)
    - type BlankSafetyClass as the union of the four literals 'zero-sentences', 'zero-safe', 'unsafe-first', 'ok'

    Implement exactly per the RESEARCH.md Code Examples section:
    - classifyBlankSafety(sentences: AuditSentence[]): BlankSafetyClass — maps each sentence through sentenceMatch(s.korean, s.targetForm) and classifies on the safeToBlank fields; the three non-ok outcomes correspond to Phase 22's three distinct fix strategies (RESEARCH Pitfall 2), which is why they must stay separate classes
    - notFoundSentenceIndices(sentences: AuditSentence[]): number[] — indices where sentenceMatch(...).found is false (these sentences render un-highlighted in the UI and are reported as their own blank-safety sub-class)

    Every exported function gets a JSDoc block with @param/@returns naming which production helper it delegates to (filter-components.ts style).
  </action>
  <verify>
    <automated>npx vitest run tests/audit-checks.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/audit-checks.test.ts` exits 0 with at least 5 passing tests covering the four BlankSafetyClass outcomes and notFoundSentenceIndices
    - `grep -cE "^import \{[^}]*sentenceMatch[^}]*\} from './sentence-match'" lib/audit-checks.ts` returns 1 (blank-safety delegates to the production predicate via an import-line, not a copy)
    - `! grep -E "^import .*(prisma|anthropic|extract-cards|node:|'fs'|\"fs\")" lib/audit-checks.ts` succeeds (purity: no I/O-capable imports)
    - lib/audit-checks.ts contains no occurrence of the substring `.includes(` applied to blank-safety decisions — the safeToBlank field of MatchResult is the only blank-safety authority (spot-check: the classifyBlankSafety body references `safeToBlank`)
  </acceptance_criteria>
  <done>classifyBlankSafety and notFoundSentenceIndices are exported, tested against real-Korean fixtures for all outcome classes, and delegate every decision to sentenceMatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Romanization heuristics + distractor anomaly checks</name>
  <files>lib/audit-checks.ts, tests/audit-checks.test.ts</files>
  <read_first>
    - lib/audit-checks.ts (as left by Task 1 — extend, do not restructure)
    - lib/card-key.ts (normalizeFront implementation — its Hangul/ASCII paren heuristic is what makes the front-romanization check gloss-safe)
    - .planning/phases/21-card-database-quality-audit/21-RESEARCH.md (check classes 3 and 4; Pitfall 3 — gloss false-positives; Pitfall 6 — legacy distractor shapes; Code Examples — romanization functions)
    - .planning/phases/21-card-database-quality-audit/21-PATTERNS.md (scope-boundary test pattern; try/catch-every-JSON-parse pattern)
  </read_first>
  <behavior>
    - frontHasRomanization('~(으)로 (direction particle)') is false — the trailing English gloss is stripped by normalizeFront before the Latin test (RESEARCH Pitfall 3); include a scope-boundary test documenting that a trailing ASCII paren group is never flagged (the heuristic cannot distinguish an English gloss from trailing-paren romanization — documented limitation, per the PATTERNS.md scope-boundary test convention)
    - frontHasRomanization on a front with Latin letters outside parens (e.g. a front containing the fragment kkujunhada with no parens) is true
    - sentenceHasRomanization('저는 apple을 먹어요') is true; sentenceHasRomanization on an all-Hangul sentence is false (no gloss allowance inside korean)
    - checkDistractors(null, back) returns ['null']
    - checkDistractors('not json', back) returns ['malformed-json'] — JSON.parse is try/catch wrapped, never throws (V5 input validation: DB JSON columns are untrusted)
    - checkDistractors('{"a":1}', back) returns ['malformed-json'] (parses but is not an array)
    - checkDistractors on an array containing a non-string entry includes 'not-string-array'
    - checkDistractors on arrays of length 2 and length 4 both include 'count-mismatch' (legacy rows can exceed 3 — RESEARCH Pitfall 6)
    - checkDistractors on an array with two identical entries includes 'duplicate-entries'
    - checkDistractors where an entry strictly equals the back argument includes 'equals-back'
    - checkDistractors on a valid 3-distinct-strings array none equal to back returns []
  </behavior>
  <action>
    Tests first, then implement in lib/audit-checks.ts:

    - Module-level constant LATIN defined as the Latin-alphabet character class regex (same alphabet class normalizeFront uses for gloss detection — see lib/card-key.ts)
    - frontHasRomanization(front: string): boolean — returns the LATIN test applied to normalizeFront(front); the gloss is already stripped by the production helper, so surviving Latin is leakage. Do NOT hand-roll paren stripping — that is normalizeFront's job (AUDIT-02)
    - sentenceHasRomanization(korean: string): boolean — LATIN test on the raw string; sentences have no gloss allowance
    - type DistractorAnomaly as the union of the six literals 'null', 'malformed-json', 'not-string-array', 'count-mismatch', 'duplicate-entries', 'equals-back'
    - checkDistractors(distractors: string | null, back: string): DistractorAnomaly[] — returns ALL anomalies present (empty array means healthy). Rules: null column yields ['null'] alone; a JSON.parse failure or non-array parse yields ['malformed-json'] alone; otherwise accumulate: any non-string entry adds 'not-string-array', parsed length not exactly 3 adds 'count-mismatch' (both directions — under AND over), any exact-string repeat adds 'duplicate-entries', any entry strictly equal to back adds 'equals-back'. Grounding: the extraction prompt demands exactly 3, Phase 20 warns under 3 but persists, and components/StudySession.tsx pads/slices at render — stored legacy rows can hold any shape

    JSDoc on each function names its grounding (normalizeFront regex reuse for romanization; StudySession.tsx parse/pad behavior for distractors).
  </action>
  <verify>
    <automated>npx vitest run tests/audit-checks.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/audit-checks.test.ts` exits 0, including a test asserting frontHasRomanization('~(으)로 (direction particle)') === false and one asserting checkDistractors('not json', '...') deep-equals ['malformed-json']
    - `grep -cE "^import \{[^}]*normalizeFront[^}]*\} from './card-key'" lib/audit-checks.ts` returns 1 (gloss stripping delegates to the production helper)
    - `grep -c "JSON.parse" lib/audit-checks.ts | awk '{exit !($1>=1)}'` — the parse exists in the module, and every occurrence sits inside a try block (executor eyeball + the malformed-json unit test proves no-throw behavior)
    - The full targeted test file covers all six DistractorAnomaly literals at least once each
  </acceptance_criteria>
  <done>Romanization checks are gloss-safe via normalizeFront; distractor checks classify every legacy anomaly shape without ever throwing on malformed JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: normalizedFront mismatch, superNormalize lift, duplicate clustering, stale-components count, runAuditChecks aggregator</name>
  <files>lib/audit-checks.ts, tests/audit-checks.test.ts</files>
  <read_first>
    - lib/audit-checks.ts (as left by Task 2)
    - scripts/find-duplicates.mjs (lines 47-62: the superNormalize source being lifted — preserve the commented-out slash-removal decision verbatim; lines 79-81: cluster filter/sort)
    - lib/filter-components.ts (filterComponents contract — powers the stale-components count)
    - .planning/phases/21-card-database-quality-audit/21-RESEARCH.md (check classes 2, 5, 6; optional finding 7 recommendation — include as summary count only; Code Examples — superNormalize and normalizedFrontMismatch)
  </read_first>
  <behavior>
    - superNormalize('~(으)면') === superNormalize('(으)면') — the parity fixture from the VALIDATION.md map: tilde and paren-group stripping make these cluster together
    - superNormalize preserves slashes: an input containing 아/어 keeps the slash (meaning-bearing; the rejected slash-removal decision is preserved as a comment in the lifted code)
    - superNormalize lowercases, NFC-normalizes, and collapses internal whitespace
    - normalizedFrontMismatch(front, stored) is true iff normalizeFront(front) !== stored (recompute with the current production helper)
    - frontUntrimmed(front) is true iff front differs from its trimmed self (legacy rows may predate Phase 20's WR-01 trim)
    - clusterNearDuplicates returns only groups of 2+ cards sharing a superNormalize key, sorted by member count descending, each member carrying id/type/front/back
    - countStaleComponents(null, deckSet) reports zero stale and not-malformed; countStaleComponents('broken', deckSet) reports malformed=true without throwing; countStaleComponents on a valid JSON array counts entries dropped by filterComponents against the deckSet
    - runAuditChecks over a small mixed fixture deck returns an AuditFindings object whose per-class arrays and counts are all simultaneously correct (one integration-style test wiring every class together), and a card whose classifyBlankSafety is 'zero-sentences' appears in the zero-sentence section (with a hasLegacyCloze flag when clozeSentence is non-null) rather than in the blank-safety section
  </behavior>
  <action>
    Tests first, then implement:

    - superNormalize(front: string): string — LIFT the local function from scripts/find-duplicates.mjs into this module as a named export (the one permitted lift per RESEARCH.md: NFC normalize, strip all tilde characters, strip ALL paren groups including Hangul ones, collapse whitespace, trim, lowercase — and keep the original's commented-out slash-removal note explaining slashes are left as-is because removing them changes meaning)
    - normalizedFrontMismatch(front: string, storedNormalizedFront: string): boolean — strict inequality between normalizeFront(front) and the stored value
    - frontUntrimmed(front: string): boolean — front !== front.trim()
    - clusterNearDuplicates(cards: CardRef[] & { normalizedFront: string }-bearing inputs): group by superNormalize(normalizedFront || front), keep groups with 2+ members, sort descending by size, return array of { key, members: CardRef[] } (mirrors find-duplicates.mjs filter/sort)
    - countStaleComponents(components: string | null, deckSet: Set<string>): { stale: number; malformed: boolean } — try/catch JSON.parse (array-of-strings check like the retro-filter-cleanup.mts precedent); on success, stale = parsed.length minus filterComponents(parsed, deckSet).length. This is the summary-count-only optional finding 7 (RESEARCH Open Question 1 recommendation) and the literal reuse of filterComponents mandated by AUDIT-02; splitParticle is thereby reused through filterComponents' stem-resolution step — do not reimplement particle logic anywhere in this module
    - interface AuditFindings and runAuditChecks(cards: AuditCardInput[]): AuditFindings — the single aggregator the Wave 2 script calls. It derives deckSet internally as the Set of every card's normalizedFront, then routes each card through every check: blankSafety buckets (zeroSafe / unsafeFirst as CardRef arrays, notFound as CardRef-plus-indices entries) for cards WITH sentences; zeroSentenceCards as its own section carrying a hasLegacyCloze boolean (clozeSentence non-null — a possible Phase 22 fix source); romanization split into flagged fronts and flagged sentences (with sentence indices); distractor findings as CardRef-plus-anomalies entries (only cards with non-empty anomaly arrays); normalizedFrontMismatches carrying the recomputed expected value; untrimmedFronts; nearDuplicateClusters; staleComponents as counts only (cardsAffected, totalStaleEntries, plus CardRefs for malformed components JSON). Include totalCards. Field names are executor's discretion but every finding entry MUST carry the card id
  </action>
  <verify>
    <automated>npx vitest run tests/audit-checks.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/audit-checks.test.ts` exits 0 including the superNormalize parity test (tilde/paren variants cluster) and the runAuditChecks integration fixture
    - `npm test` exits 0 (full suite stays green — no regression in the existing 145+ tests)
    - `grep -cE "^import \{[^}]*filterComponents[^}]*\} from './filter-components'" lib/audit-checks.ts` returns 1
    - `! grep -E "^import .*(prisma|anthropic|extract-cards|node:|'fs'|\"fs\")" lib/audit-checks.ts` succeeds (module is still pure after all three tasks)
    - `grep -c "export function" lib/audit-checks.ts | awk '{exit !($1>=10)}'` — all ten check functions plus the aggregator are named exports (superNormalize, runAuditChecks included)
  </acceptance_criteria>
  <done>runAuditChecks aggregates all six check classes plus the stale-components summary over plain-data card inputs; the module is complete, pure, and fully covered; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored DB JSON columns → audit parser | `Card.components` and `Card.distractors` are nullable JSON-string columns written by past extraction runs — treated as untrusted input (malformed rows exist in production per retro-filter-cleanup.mts precedent) |
| Pure module → Wave 2 script | Findings objects cross into the report-rendering script; module must expose only card data, never environment state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | lib/audit-checks.ts | high | mitigate | Module imports no Prisma at all (grep-gated on import lines in every task's acceptance criteria) — it is structurally incapable of a DB write |
| T-21-03 | Denial of Service (self) | checkDistractors / countStaleComponents JSON parsing | low | mitigate | Every JSON.parse of a DB column value is try/catch wrapped (V5 input validation, ASVS L1); malformed JSON becomes a 'malformed-json' / malformed=true finding, proven by unit tests — never a crash |
| T-21-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase (RESEARCH.md Package Legitimacy Audit: none installed); Vitest/dotenv/Prisma already in package.json |
</threat_model>

<verification>
- `npx vitest run tests/audit-checks.test.ts` — all audit-check unit tests green (per-task sampling command from 21-VALIDATION.md)
- `npm test` — full suite green (per-wave gate from 21-VALIDATION.md)
- Purity grep: `! grep -E "^import .*(prisma|anthropic|extract-cards|node:|'fs'|\"fs\")" lib/audit-checks.ts` succeeds
- Helper-reuse greps: import lines for sentenceMatch ('./sentence-match'), normalizeFront ('./card-key'), filterComponents ('./filter-components') each present exactly once
</verification>

<success_criteria>
- All exported check functions exist and are covered: classifyBlankSafety, notFoundSentenceIndices, frontHasRomanization, sentenceHasRomanization, checkDistractors, normalizedFrontMismatch, frontUntrimmed, superNormalize, clusterNearDuplicates, countStaleComponents, runAuditChecks (AUDIT-02)
- Every predicate delegates to a production helper; zero reimplementation of blank-safety, gloss-stripping, particle, or component-resolution rules
- Malformed JSON in either JSON column is a finding class, never an exception
- ROADMAP success criterion 3 satisfied: pure, Vitest-covered lib/audit-checks.ts reusing sentenceMatch / splitParticle (via filterComponents) / normalizeFront / filterComponents
</success_criteria>

<output>
Create `.planning/phases/21-card-database-quality-audit/21-01-SUMMARY.md` when done. The SUMMARY must list the final exported API of lib/audit-checks.ts (names + signatures) — Plan 21-02's script consumes it.
</output>
