---
phase: 12-home-habits-hydration
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - components/HabitTracker.tsx
  - components/HomeClient.tsx
  - app/page.tsx
autonomous: true
requirements: [RSC-03]
tags: [rsc, client-shell, hydration, pull-to-refresh, band-up, dto]

must_haves:
  truths:
    - "app/page.tsx is an async server component with no client directive; it awaits Promise.all([getStats(), getActivityData()]) from lib/dashboard.ts and renders <HomeClient initialStats={stats} initialActivity={activity} />"
    - "components/HomeClient.tsx begins with the React client-component directive as its literal first line and defines interface Props { initialStats: StatsDTO; initialActivity: ActivityDTO }"
    - "HomeClient initializes stats/activityData from initialStats/initialActivity props (useState<StatsDTO>(initialStats), useState<ActivityDTO>(initialActivity)) — NO null state, NO initial-load useEffect fetch of /api/stats or /api/activity (Pitfall 1)"
    - "HomeClient computes greeting + today + heroState in useEffect via Promise.resolve().then(() => setState(...)) — satisfies react-hooks/purity and react-hooks/set-state-in-effect (D-01, D-02, Pitfalls 2+3)"
    - "heroState='loading' (the h-28 animate-pulse bg-surface-2 rounded-xl block) shows for one effect tick, then the real A/B/C hero renders (D-02 — the intentional pulse, NOT an empty-state flash)"
    - "The band-up check runs on the first mount effect against computeProficiency(initialStats.masteredCount).band vs localStorage['lastBand'], AND re-runs inside loadStats after a pull-to-refresh refetch (Pitfall 5 — first-load band-up must fire)"
    - "handleSync (pull-to-refresh) preserves POST /api/sync then loadStats() + loadActivity() (the surviving GET routes per D-08) — success criteria #3"
    - "components/HabitTracker.tsx accepts optional initialDays/initialToday/initialGoal props (D-09); when all three are present, the useEffect self-fetch is skipped; defensive self-fetch fallback preserved when props absent"
    - "HomeClient passes <HabitTracker initialDays={initialActivity.days} initialToday={today} initialGoal={initialActivity.dailyGoalSeconds} /> — passing its own already-computed today avoids desync (Pitfall 6)"
    - "DevTools Network tab shows NO /api/stats or /api/activity request on first load of / (only the lazy canvas-confetti chunk IF a band-up fires) — the duplicate /api/activity fetch on home is eliminated"
  artifacts:
    - components/HomeClient.tsx
    - app/page.tsx (rewritten as async RSC)
    - components/HabitTracker.tsx (D-09 optional initialActivity props)
  key_links:
    - "app/page.tsx -> HomeClient (initialStats/initialActivity props)"
    - "HomeClient -> HabitTracker (initialDays/initialToday/initialGoal props — D-09)"
    - "HomeClient -> /api/stats + /api/activity (post-sync refetch ONLY, via handleSync — D-08)"
    - "HomeClient -> lib/dashboard.ts indirectly via RSC props; -> lib/proficiency.ts (computeProficiency band-up); -> lib/copy.ts (bandUpMessage); -> lib/usePullToRefresh.ts; -> lib/habit.ts (habitDateStr)"
---

<objective>
Convert the home dashboard from a `'use client'` page that mounts in an empty state and runs two `useEffect` fetches, to an async RSC that server-fetches stats + activity (D-07) and hands them to a new `HomeClient` shell that starts populated (RSC-03 — no empty hero flash). Also add optional `initialActivity` props to `HabitTracker` (D-09) to eliminate the duplicate `/api/activity` fetch on home. Pull-to-refresh sync + band-up confetti + time-aware greeting are preserved verbatim (success criteria #3, #4).

Purpose: Eliminate the empty-hero flash on first load of `/` by moving data acquisition to the server render; the only permitted loading is the one-tick `heroState='loading'` pulse while client-local `today` resolves (D-02 — the Vercel server runs UTC and cannot compute the habit-day or the greeting).
Output: `components/HomeClient.tsx` (new client shell), `app/page.tsx` (async RSC), `components/HabitTracker.tsx` (D-09 props).
</objective>

<execution_context>
@/Users/main/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/main/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-home-habits-hydration/12-CONTEXT.md
@.planning/phases/12-home-habits-hydration/12-RESEARCH.md
@.planning/phases/12-home-habits-hydration/12-PATTERNS.md
@.planning/phases/12-home-habits-hydration/12-UI-SPEC.md
@.planning/phases/12-home-habits-hydration/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add D-09 optional initialActivity props to HabitTracker</name>
  <files>components/HabitTracker.tsx</files>
  <read_first>
    - components/HabitTracker.tsx (current 205-line component — line 22 `export default function HabitTracker()` with no props; lines 23-25 the useState initializers `days: DayRecord[] | null = null`, `today = ''`, `goal = DEFAULT_GOAL_SECONDS`; lines 30-40 the self-fetch useEffect with empty deps `[]`)
    - lib/habit.ts (DayRecord interface, DEFAULT_GOAL_SECONDS, DEFAULT_DAY_START_HOUR, habitDateStr — the values the self-fetch seeds)
    - app/page.tsx (the single call site — line 6 import + line 232 `<HabitTracker />` usage; confirm there are no other call sites)
    - components/StudyClient.tsx (Phase 11 skip-fetch-when-initial-data-present pattern reference)
  </read_first>
  <!-- planner-discipline-allow: 'use client' -->
  <action>
Modify components/HabitTracker.tsx (per D-09). Preserve the existing client directive as the literal first line (the file already starts with it — do not remove it). Add an `interface Props { initialDays?: DayRecord[]; initialToday?: string; initialGoal?: number }` (CLAUDE.md convention: `interface Props`, not `HabitTrackerProps`). Change the default export signature to destructure the props: `export default function HabitTracker({ initialDays, initialToday, initialGoal }: Props)`.

Seed state from props when present (replace lines 23-25): `const [days, setDays] = useState<DayRecord[] | null>(initialDays ?? null)`, `const [today, setToday] = useState(initialToday ?? '')`, `const [goal, setGoal] = useState(initialGoal ?? DEFAULT_GOAL_SECONDS)`.

In the existing self-fetch useEffect (lines 30-40), add an early return as the FIRST statement inside the effect body: `if (initialDays !== undefined && initialToday !== undefined && initialGoal !== undefined) return` — this skips the /api/activity fetch when all three initialActivity props are present (D-09). Keep the defensive self-fetch fallback (the fetch('/api/activity') + .then + .catch chain) intact for when props are absent. Update the useEffect dependency array from `[]` to `[initialDays, initialToday, initialGoal]`.

Do NOT change anything else in HabitTracker: milestone detection, haptic on ring close, comeback pill (comebackMessage from lib/copy.ts), at-risk nudge, 7-day week strip, and the render block all stay verbatim. The only observable change is the absence of a duplicate /api/activity request when the props are provided (which HomeClient will do in Task 2).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && bash -c "rg -c 'interface Props' components/HabitTracker.tsx | rg -q '^1$' && rg -q 'initialDays' components/HabitTracker.tsx && rg -q 'initialToday' components/HabitTracker.tsx && rg -q 'initialGoal' components/HabitTracker.tsx && head -1 components/HabitTracker.tsx | rg -q 'use client'"</automated>
  </verify>
  <acceptance_criteria>
    - HabitTracker declares `interface Props` with optional `initialDays?: DayRecord[]`, `initialToday?: string`, `initialGoal?: number`
    - State seeded from props: `useState<DayRecord[] | null>(initialDays ?? null)`, `useState(initialToday ?? '')`, `useState(initialGoal ?? DEFAULT_GOAL_SECONDS)`
    - Self-fetch useEffect early-returns when all three props are present (`if (initialDays !== undefined && initialToday !== undefined && initialGoal !== undefined) return`)
    - Defensive self-fetch fallback preserved (runs when props absent — component stays usable in isolation)
    - useEffect deps array is `[initialDays, initialToday, initialGoal]`
    - First line is the client directive (preserved); rest of HabitTracker logic (milestone/haptic/comeback/render) unchanged
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0
  </acceptance_criteria>
  <done>HabitTracker accepts optional initialDays/initialToday/initialGoal props (D-09); skips self-fetch when all three present; defensive fallback preserved; type-check + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create components/HomeClient.tsx client shell</name>
  <files>components/HomeClient.tsx</files>
  <read_first>
    - app/page.tsx (current 252-line client home page — ALL logic to port: lines 3-13 imports; lines 15-40 state; lines 44-74 loadStats/loadActivity useCallbacks incl. the band-up check at lines 50-64; lines 82-103 the greeting + heroState effects with Promise.resolve().then(setState); lines 106-133 handleSync + usePullToRefresh; lines 137-251 the render block incl. heroState='loading' pulse at lines 173-175 and `<HabitTracker />` at line 232)
    - components/StudyClient.tsx (Phase 11 shell shape — `'use client'` first line; `interface Props { initialCards, initialLessons }`; `useState(initialX)` seeding; NO mount-effect fetch)
    - lib/dto.ts (StatsDTO + ActivityDTO from Plan 1 Task 1)
    - lib/proficiency.ts (computeProficiency(masteredCount) -> { band, ... } for the band-up check)
    - lib/copy.ts (bandUpMessage(band, masteredCount))
    - lib/usePullToRefresh.ts (usePullToRefresh hook + PULL_THRESHOLD)
    - lib/habit.ts (habitDateStr(dayStartHour) for the today computation)
    - components/HabitTracker.tsx (the D-09 props from Task 1)
  </read_first>
  <!-- planner-discipline-allow: 'use client' -->
  <action>
Create components/HomeClient.tsx as a NEW client shell. The React client-component directive MUST be the literal first line, before any imports (Pitfall 8).

Port ALL imports from app/page.tsx lines 3-13: `useEffect, useRef, useState, useCallback` from react; `CheckCircle2` from lucide-react; `HabitTracker` from `@/components/HabitTracker`; `StatsBar` from `@/components/StatsBar`; `ProficiencyArc` from `@/components/ProficiencyArc`; `usePullToRefresh` from `@/lib/usePullToRefresh`; `computeProficiency` from `@/lib/proficiency`; `bandUpMessage` from `@/lib/copy`; `habitDateStr` from `@/lib/habit`. Add `import type { StatsDTO, ActivityDTO } from '@/lib/dto'`.

Define `interface Props { initialStats: StatsDTO; initialActivity: ActivityDTO }`. Default export: `export default function HomeClient({ initialStats, initialActivity }: Props)`.

Initialize state from props (NO null state — Pitfall 1 / RESEARCH.md Assumption A3): `const [stats, setStats] = useState<StatsDTO>(initialStats)`, `const [activityData, setActivityData] = useState<ActivityDTO>(initialActivity)`. Port the remaining state verbatim from app/page.tsx lines 15-40: heroState (init `'loading'`), greeting (init `''`), syncMsg, bandUpMsg, isMountedRef.

Extract the band-up check into a `useCallback` named `checkBandUp` that takes a `masteredCount: number`: compares `computeProficiency(masteredCount).band` to `localStorage['lastBand']`; on mismatch (and stored is non-null), sets `bandUpMsg` to `bandUpMessage(band, masteredCount)` and lazy-imports confetti via `import('canvas-confetti').then((m) => { if (!isMountedRef.current) return; m.default({ particleCount: 80, spread: 60, origin: { y: 0.4 } }) }).catch(() => {})`; always writes `localStorage['lastBand'] = band` after the check (Pitfall 5). Port `loadStats` (app/page.tsx lines 44-49, 64-74) as a useCallback that fetches `/api/stats`, sets stats, and calls `checkBandUp(data.masteredCount)` after the refetch resolves. Port `loadActivity` (lines 44-74) as a useCallback that fetches `/api/activity` and sets activityData. CRITICAL: `loadStats` and `loadActivity` are called ONLY from `handleSync` — never from a mount effect (D-08, Pitfall 1).

Mount + heroState useEffect (port app/page.tsx lines 82-103, adapted): REMOVE the `loadStats()` and `loadActivity()` calls (props now provide initial data). Keep the greeting: `const h = new Date().getHours(); const g = h < 5 ? 'Good evening' : h < 12 ? 'Good morning' : h < 18 ? 'Good afternoon' : 'Good evening'; Promise.resolve().then(() => setGreeting(g))`. Also call `checkBandUp(initialStats.masteredCount)` here on mount (Pitfall 5 — first-load band-up). The heroState effect: REMOVE the `if (!stats || !activityData) return` guard (props always non-null — A3); compute `const todayStr = habitDateStr(activityData.dayStartHour)`, store it in a `today` state variable (needed for HabitTracker's `initialToday` — Pitfall 6), compute `const todaySeconds = activityData.days.find((d) => d.date === todayStr)?.seconds ?? 0`, `const goalMet = todaySeconds >= activityData.dailyGoalSeconds && activityData.dailyGoalSeconds > 0`, then `Promise.resolve().then(() => setHeroState(stats.dueCards > 0 ? 'A' : goalMet ? 'B' : 'C'))`. Deps: `[stats, activityData]` (re-runs after a pull-to-refresh refetch updates state).

Port `handleSync` verbatim (app/page.tsx lines 106-133): POST /api/sync, setSyncMsg (`Synced — N new card[s]` / `Up to date` / `Sync failed — try again from Settings`), then `loadStats()` + `loadActivity()`. Wire `const { pullDistance, refreshing } = usePullToRefresh(handleSync)`.

Render block (port app/page.tsx lines 137-251 verbatim). The `heroState === 'loading'` branch renders `<div className="h-28 animate-pulse bg-surface-2 rounded-xl" />` for one tick (D-02), then the real A/B/C hero. Replace the `<HabitTracker />` usage (line 232) with `<HabitTracker initialDays={initialActivity.days} initialToday={today} initialGoal={initialActivity.dailyGoalSeconds} />` (D-09; pass HomeClient's own `today` — Pitfall 6). Preserve ALL UI-SPEC copy strings verbatim: `Study now →`, `Study ahead →`, `Goal met today`, `You've hit your daily goal. Nice work.` (with `&apos;`), `All caught up`, `Keep going to hit your daily goal.`, `Pull to sync`, `Release to sync`, `Syncing…`, `Up to date`, `Sync failed — try again from Settings`, `My Korean` / `All-time stats & progress summary`, dismiss `aria-label="Dismiss"` + `✕`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && bash -c "head -1 components/HomeClient.tsx | rg -q 'use client' && rg -c 'interface Props' components/HomeClient.tsx | rg -q '^1$' && rg -q 'initialStats' components/HomeClient.tsx && rg -q 'initialActivity' components/HomeClient.tsx && rg -q 'useState<StatsDTO>(initialStats)' components/HomeClient.tsx && rg -c 'Promise.resolve().then' components/HomeClient.tsx | rg -q '^[3-9]$|[1-9][0-9]+$' && rg -c 'fetch..?/api/stats.|fetch..?/api/activity.' components/HomeClient.tsx | rg -q '^2$' && rg -q 'initialToday={today}' components/HomeClient.tsx && rg -q 'Study now' components/HomeClient.tsx && rg -q 'Goal met today' components/HomeClient.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - components/HomeClient.tsx begins with the client directive as literal first line (Pitfall 8)
    - `interface Props { initialStats: StatsDTO; initialActivity: ActivityDTO }` declared
    - `useState<StatsDTO>(initialStats)` + `useState<ActivityDTO>(initialActivity)` — no null state
    - No mount-effect fetch: loadStats/loadActivity called only from handleSync (exactly 2 fetch calls in the file — both inside the post-sync refetch path)
    - Greeting computed in useEffect via `Promise.resolve().then(() => setGreeting(...))` (purity + set-state-in-effect safe — D-01)
    - heroState derived in useEffect via `Promise.resolve().then(() => setHeroState(...))` from initialStats + initialActivity + client-local today; the `if (!stats || !activityData) return` guard removed (A3)
    - `heroState='loading'` renders the `h-28 animate-pulse bg-surface-2 rounded-xl` pulse block for one tick (D-02)
    - Band-up check extracted to `checkBandUp(masteredCount)` useCallback; called on mount with `initialStats.masteredCount` AND inside loadStats after refetch (Pitfall 5)
    - handleSync preserves POST /api/sync + loadStats + loadActivity + the 3 syncMsg strings
    - `<HabitTracker initialDays={initialActivity.days} initialToday={today} initialGoal={initialActivity.dailyGoalSeconds} />` wired (D-09, Pitfall 6)
    - All UI-SPEC copy strings present verbatim (Study now →, Study ahead →, Goal met today, All caught up, Pull to sync, Release to sync, Syncing…, Up to date, Sync failed — try again from Settings, My Korean)
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0
  </acceptance_criteria>
  <done>HomeClient client shell created: state seeded from RSC props, no mount-effect fetch, one-tick heroState pulse, band-up check on first mount + after refetch, pull-to-refresh + HabitTracker D-09 props wired; all copy preserved; type-check + lint clean.</done>
</task>

<task type="auto">
  <name>Task 3: Convert app/page.tsx to async RSC rendering HomeClient</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (current 252-line client page — to be replaced entirely)
    - app/study/page.tsx (Phase 11 RSC reference — 15-line async server component shape: `import { getStudyCards } from '@/lib/study-cards'` + `import StudyClient from '@/components/StudyClient'` + `export default async function StudyPage() { const [...] = await Promise.all([...]); return <StudyClient ... /> }`)
    - lib/dashboard.ts (getStats + getActivityData from Plan 1 Task 2)
    - components/HomeClient.tsx (the shell from Task 2 — confirms the Props signature)
  </read_first>
  <!-- planner-discipline-allow: 'use client' -->
  <action>
Replace app/page.tsx entirely. Remove the client directive from line 1 (the page becomes an async server component — Pitfall 8: the directive moves WITH the client logic into components/HomeClient.tsx, which already has it from Task 2). Set the file header comment to exactly `// Server component — no client directive` (this exact string — the Phase 11 11-02 SUMMARY documented that a comment quoting the directive string literal trips the negative grep; do NOT write a comment that contains the directive string).

Imports (only these two): `import { getStats, getActivityData } from '@/lib/dashboard'` and `import HomeClient from '@/components/HomeClient'`.

Default export: `export default async function Home() { const [stats, activity] = await Promise.all([getStats(), getActivityData()]); return <HomeClient initialStats={stats} initialActivity={activity} /> }`.

No hooks, no client directive, no try/catch (STATE.md convention: "No try/catch at RSC page level — Next.js error boundaries handle getStudyCards throws" — applies identically to getStats/getActivityData). The `await Promise.all` runs the two lib/dashboard.ts getters concurrently.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && bash -c "! rg -q \"^'use client'\" app/page.tsx && rg -c 'async function Home' app/page.tsx | rg -q '^1$' && rg -q 'getStats' app/page.tsx && rg -q 'getActivityData' app/page.tsx && rg -q 'HomeClient' app/page.tsx && rg -c 'Promise.all' app/page.tsx | rg -q '^1$'"</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx has no client directive (`grep "^'use client'" app/page.tsx` anchored to line start returns 0 — won't match comments)
    - app/page.tsx is `export default async function Home()` (server component)
    - Calls `Promise.all([getStats(), getActivityData()])`
    - Renders `<HomeClient initialStats={stats} initialActivity={activity} />`
    - No try/catch at the RSC page level (Next.js error boundary handles lib/dashboard.ts throws)
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0; `npm run build` succeeds (home route prerendered — confirms RSC-03)
  </acceptance_criteria>
  <done>Home page is an async RSC that server-fetches stats + activity and renders HomeClient with DTO props; no client directive; build succeeds with the home route prerendered (RSC-03 satisfied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → home RSC (GET /) | Existing auth gate (middleware.ts `ks_auth` cookie) unchanged; the RSC runs server-side behind the same gate |
| HomeClient → /api/stats + /api/activity (post-sync refetch) | Existing GET routes (now delegated to lib/dashboard.ts per Plan 1 / D-08); called only from handleSync after a pull-to-refresh |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04 | Information Disclosure | HomeClient receives server-fetched stats/activity via DTO props | low | mitigate | Props typed `StatsDTO`/`ActivityDTO` (Plan 1) — only fields already served by the GET routes. No new data crosses the boundary. The RSC fetches behind the auth gate. |
| T-12-05 | Tampering | band-up confetti fires on stale/ manipulated localStorage['lastBand'] | low | accept | localStorage is client-trusted by design (existing behavior since v1.1). The check writes `lastBand` after comparison; worst case is a missed/duplicate celebration — UX only, no security impact (success criteria #4, non-destructive dismiss). |
| T-12-06 | Information Disclosure | HabitTracker self-fetch fallback path leaks /api/activity when props absent | low | accept | Defensive fallback (D-09) only triggers when initialActivity props are absent. On home, props ARE provided (HomeClient wires them), so the self-fetch is skipped and the duplicate request is eliminated. The fallback path is the unchanged v1.1 behavior for isolated component use. |

No threat meets the `high` blocking threshold (security_block_on: high) — no blocking human checkpoints required.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (all three tasks)
- `npm run lint` exits 0 (all three tasks — esp. react-hooks/purity + react-hooks/set-state-in-effect in HomeClient)
- `npm run build` succeeds (Task 3 — home route shows as prerendered; no Prisma-in-client-bundle error; RSC serialization constraint surfaces at build)
- End-of-phase manual UAT (success criteria #1, #3, #4): load `/` in a production build (`next build && next start`); confirm no empty hero flash (one-tick pulse only); confirm pull-to-refresh still syncs + updates the hero; confirm band-up confetti fires when localStorage['lastBand'] is cleared and masteredCount crosses a band; confirm DevTools Network tab shows NO /api/stats or /api/activity request on first load (only the lazy canvas-confetti chunk IF a band-up fires)
</verification>

<success_criteria>
- Home page renders the hero + stats + activity with the server render (RSC-03 — no empty hero flash on first load)
- HomeClient starts populated from props; the only loading is the one-tick heroState='loading' pulse (D-02)
- Pull-to-refresh still triggers a sync and updates the data after mount (success criteria #3)
- Band-up confetti/banner still fires client-side on a CEFR level-up (success criteria #4)
- No Date object crosses the boundary (StatsDTO/ActivityDTO from Plan 1 — RSC-05)
- The duplicate /api/activity fetch on home is eliminated (HabitTracker D-09 props)
</success_criteria>

<output>
Create `.planning/phases/12-home-habits-hydration/12-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**components/HomeClient.tsx (NEW file):**
- `HomeClient` default export — `'use client'` shell
- `Props` interface — `{ initialStats: StatsDTO; initialActivity: ActivityDTO }`
- `checkBandUp(masteredCount: number)` useCallback (extracted band-up check — Pitfall 5)

**app/page.tsx (REWRITTEN):**
- `Home()` — async server component (default export); `Promise.all([getStats(), getActivityData()])` → `<HomeClient initialStats initialActivity />`

**components/HabitTracker.tsx (MODIFIED):**
- `Props` interface (NEW) — `{ initialDays?: DayRecord[]; initialToday?: string; initialGoal?: number }` (D-09)
- Default export signature changed to accept the destructured props
- State initializers changed to seed from props (`initialDays ?? null`, `initialToday ?? ''`, `initialGoal ?? DEFAULT_GOAL_SECONDS`)
- Self-fetch useEffect gains an early-return guard + deps `[initialDays, initialToday, initialGoal]`
