---
phase: 12-home-habits-hydration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/dto.ts
  - lib/dashboard.ts
  - app/api/stats/route.ts
  - app/api/activity/route.ts
autonomous: true
requirements: [RSC-03, RSC-04]
tags: [dto, server-pipeline, rsc, shared-lib, refactor]

must_haves:
  truths:
    - "lib/dashboard.ts exports getStats() returning Promise<StatsDTO> and getActivityData() returning Promise<ActivityDTO>"
    - "getStats() runs 5 Prisma queries via Promise.all: prisma.card.count, prisma.cardReview.count({ where: { nextReview: { lte: now } } }), prisma.lesson.count, prisma.card.groupBy({ by: ['type'], _count: true }), prisma.cardReview.count({ where: { state: 2, scheduledDays: { gte: 21 } } })"
    - "getActivityData() runs 3 parallel queries via Promise.all: prisma.studyDay.findMany({ orderBy: { date: 'desc' }, take: 400, select: { date, seconds, reviews } }), getDailyGoalSeconds(), getDayStartHour()"
    - "lib/dto.ts exports StatsDTO and ActivityDTO interfaces; ActivityDTO.days[] element type is DayRecord imported from @/lib/habit (no new ActivityDayDTO type invented)"
    - "No raw Date object crosses the server->client boundary (RSC-05): StatsDTO is all number + groupBy result shape; ActivityDTO.days[].date is a YYYY-MM-DD string, not a DateTime"
    - "GET /api/stats delegates to getStats() wrapped in try/catch returning 500 on failure; JSON response shape is byte-identical to the current inline implementation (so /wrapped still works)"
    - "GET /api/activity delegates to getActivityData() wrapped in try/catch returning 500 on failure; the POST /api/activity handler (study-time logging) is untouched"
    - "lib/dashboard.ts is server-only: header guard comment present, no client directive, imports prisma (Node-only) which fails at build time if accidentally client-imported"
  artifacts:
    - lib/dashboard.ts
    - lib/dto.ts (extended with StatsDTO + ActivityDTO)
    - app/api/stats/route.ts (GET delegated)
    - app/api/activity/route.ts (GET delegated, POST preserved)
  key_links:
    - "lib/dashboard.ts -> imported by app/api/stats/route.ts GET + app/api/activity/route.ts GET (and by the Wave 2 RSC pages app/page.tsx + app/habits/page.tsx)"
    - "lib/dto.ts StatsDTO + ActivityDTO -> imported by lib/dashboard.ts + both API routes"
---

<objective>
Extract the server-side dashboard data layer into `lib/dashboard.ts` (D-05) and add `StatsDTO` + `ActivityDTO` to `lib/dto.ts` (D-06), then delegate the `/api/stats` + `/api/activity` GET routes to the shared module (D-08). This is the foundation both Wave 2 RSC pages (`app/page.tsx`, `app/habits/page.tsx`) depend on — it mirrors the Phase 11 `lib/study-cards.ts` extraction precedent exactly.

Purpose: One source of truth per query set, shared by the RSC pages and the surviving GET API routes (the GET routes stay per D-08 to serve HomeClient's post-sync refetch). No duplication.
Output: `lib/dashboard.ts` (new server-only module), `lib/dto.ts` (extended), both API GET routes thinned to delegating handlers.
</objective>

<execution_context>
@/Users/main/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/main/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-home-habits-hydration/12-CONTEXT.md
@.planning/phases/12-home-habits-hydration/12-RESEARCH.md
@.planning/phases/12-home-habits-hydration/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend lib/dto.ts with StatsDTO + ActivityDTO</name>
  <files>lib/dto.ts</files>
  <read_first>
    - lib/dto.ts (current 63-line pure type module — see the existing header comment + DTO interface shapes; this file has NO client directive and NO Prisma imports — preserve both)
    - lib/habit.ts lines 8-12 (the DayRecord interface: `{ date: string; seconds: number; reviews?: number }` — date is a YYYY-MM-DD string, already serializable)
    - app/api/stats/route.ts (the GET return shape — `{ totalCards, dueCards, totalLessons, cardsByType, masteredCount }`; cardsByType is the raw prisma.card.groupBy result)
    - app/api/activity/route.ts (the GET return shape — `{ days, dailyGoalSeconds, dayStartHour }`)
  </read_first>
  <action>
Add `import type { DayRecord } from '@/lib/habit'` at the top of lib/dto.ts (the file currently has no import statements — it is pure type definitions; add this as the first line below the existing header comment block). Append two new exported interfaces alongside the existing CardDTO / SentenceDTO / ReviewDTO / LessonDTO / LessonRefDTO (per D-06):

`StatsDTO` with fields: `totalCards: number`, `dueCards: number`, `totalLessons: number`, `cardsByType: { type: string; _count: number }[]`, `masteredCount: number`.

`ActivityDTO` with fields: `days: DayRecord[]`, `dailyGoalSeconds: number`, `dayStartHour: number`.

The `cardsByType` element shape `{ type: string; _count: number }` matches the Prisma 7 `groupBy({ by: ['type'], _count: true })` return for the current schema (RESEARCH.md Open Question 1 / Assumption A1 — verify at implementation time by reading the actual runtime value or by adding a compile-time type assertion in Task 2's getStats(); home/habits do not consume cardsByType, but the /api/stats JSON shape must stay byte-identical for /wrapped). Do NOT install the `server-only` package. Do NOT add a client directive to lib/dto.ts (it stays a pure type module importable both sides). Follow the existing file header comment convention (do not alter it).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && bash -c "rg -c 'export interface StatsDTO' lib/dto.ts | rg -q '^1$' && rg -c 'export interface ActivityDTO' lib/dto.ts | rg -q '^1$' && rg -q 'DayRecord' lib/dto.ts"</automated>
  </verify>
  <acceptance_criteria>
    - lib/dto.ts exports `StatsDTO` with fields totalCards, dueCards, totalLessons, cardsByType (typed `{ type: string; _count: number }[]`), masteredCount
    - lib/dto.ts exports `ActivityDTO` with fields days (typed `DayRecord[]`), dailyGoalSeconds, dayStartHour
    - lib/dto.ts contains `import type { DayRecord } from '@/lib/habit'`
    - lib/dto.ts has no client directive (unchanged from current — pure type module)
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>StatsDTO + ActivityDTO exported from lib/dto.ts with DayRecord reused as the ActivityDTO.days element type; type-check + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create lib/dashboard.ts + delegate both API GET routes</name>
  <files>lib/dashboard.ts, app/api/stats/route.ts, app/api/activity/route.ts</files>
  <read_first>
    - lib/study-cards.ts (Phase 11 server-only extraction precedent — header comment shape `// lib/study-cards.ts — server-only ...` + `// No 'use client — this module runs server-side only.`; the export-async-function-getXxx-returning-Promise<DTO> shape; the no-try/catch-around-Prisma convention)
    - app/api/stats/route.ts (current GET — the exact 5 Promise.all queries to move into getStats(); the `now = new Date()` + return object literal)
    - app/api/activity/route.ts (current GET — the exact 3 Promise.all queries to move into getActivityData(); PLUS the POST handler + DATE_RE constant + NextRequest import that MUST be preserved)
    - app/api/cards/due/route.ts (Phase 11 API delegation precedent — `try { return NextResponse.json(await getStudyCards(...)) } catch { return NextResponse.json({ error: 'Database error' }, { status: 500 }) }`)
    - lib/settings.ts (getDailyGoalSeconds + getDayStartHour signatures — both async returning Promise<number>)
    - lib/dto.ts (StatsDTO + ActivityDTO from Task 1)
  </read_first>
  <!-- planner-discipline-allow: 'use client' -->
  <action>
Create lib/dashboard.ts (NEW file). Header comments (exact phrasing — the Phase 11 11-02 SUMMARY documented that a comment containing the directive string literal trips the negative grep, so use these exact guard lines, NOT a comment that quotes the directive): line 1 `// lib/dashboard.ts — server-only dashboard data layer`, line 2 `// No 'use client — this module runs server-side only.`, line 3 a one-line note that it extracts the stats + activity queries from /api/stats + /api/activity so the RSC pages and the GET API routes call one shared, DRY function (per D-05).

Imports: `import { prisma } from '@/lib/prisma'`, `import { getDailyGoalSeconds, getDayStartHour } from '@/lib/settings'`, `import type { StatsDTO, ActivityDTO } from '@/lib/dto'`.

Export `async function getStats(): Promise<StatsDTO>`: `const now = new Date()`; `const [totalCards, dueCards, totalLessons, cardsByType, masteredCount] = await Promise.all([ prisma.card.count(), prisma.cardReview.count({ where: { nextReview: { lte: now } } }), prisma.lesson.count(), prisma.card.groupBy({ by: ['type'], _count: true }), prisma.cardReview.count({ where: { state: 2, scheduledDays: { gte: 21 } } }) ])`; `return { totalCards, dueCards, totalLessons, cardsByType, masteredCount }`. These 5 queries are byte-identical to the current app/api/stats/route.ts GET body. Use `Promise.all` (NOT `Promise.allSettled`) — every query is critical (RESEARCH.md Pattern 6). No try/catch in the lib module — throws propagate to the RSC error boundary and to the API route's own catch.

Export `async function getActivityData(): Promise<ActivityDTO>`: `const [days, dailyGoalSeconds, dayStartHour] = await Promise.all([ prisma.studyDay.findMany({ orderBy: { date: 'desc' }, take: 400, select: { date: true, seconds: true, reviews: true } }), getDailyGoalSeconds(), getDayStartHour() ])`; `return { days, dailyGoalSeconds, dayStartHour }`. These 3 queries are byte-identical to the current app/api/activity/route.ts GET body. No `.toISOString()` mapping needed — StudyDay.date is a String column (YYYY-MM-DD), not DateTime (RESEARCH.md Assumption A2 — verify in prisma/schema.prisma at implementation time).

Then modify app/api/stats/route.ts GET: replace the inline 5-query body with `try { return NextResponse.json(await getStats()) } catch { return NextResponse.json({ error: 'Database error' }, { status: 500 }) }`. Add `import { getStats } from '@/lib/dashboard'`. Remove the now-unused `prisma` import and the `const now = new Date()` line from this route (they live only inside getStats now). Keep the `NextResponse` import.

Then modify app/api/activity/route.ts GET: replace the inline 3-query body with the same try/catch delegation shape calling `getActivityData()`. Add `import { getActivityData } from '@/lib/dashboard'`. KEEP the POST handler (study-time logging — `prisma.studyDay.upsert`) verbatim. Do NOT remove the `DATE_RE` constant, the `NextRequest` import, or the `export async function POST` declaration (POST still needs them). Remove `prisma` + `getDailyGoalSeconds`/`getDayStartHour` imports from this file ONLY if no remaining reference exists after the GET body is replaced (the POST handler does not use them — verify with a grep before deleting each import).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && bash -c "rg -c 'export async function getStats' lib/dashboard.ts | rg -q '^1$' && rg -c 'export async function getActivityData' lib/dashboard.ts | rg -q '^1$' && rg -c 'Promise.all' lib/dashboard.ts | rg -q '^2$' && rg -q 'getStats' app/api/stats/route.ts && rg -q 'getActivityData' app/api/activity/route.ts && rg -c 'export async function POST' app/api/activity/route.ts | rg -q '^1$' && rg -q 'DATE_RE' app/api/activity/route.ts && ! rg -q \"^'use client'\" lib/dashboard.ts"</automated>
  </verify>
  <acceptance_criteria>
    - lib/dashboard.ts exports `getStats(): Promise<StatsDTO>` and `getActivityData(): Promise<ActivityDTO>`
    - getStats runs the 5 Prisma queries via Promise.all (same shapes as the old /api/stats GET)
    - getActivityData runs 3 parallel queries (studyDay.findMany take:400 + getDailyGoalSeconds + getDayStartHour) via Promise.all
    - lib/dashboard.ts has no client directive (guard comment uses the exact phrasing above — `grep -v '^//'` shows no directive)
    - GET /api/stats delegates to `getStats()` wrapped in try/catch returning 500 on failure
    - GET /api/activity delegates to `getActivityData()` wrapped in try/catch returning 500 on failure
    - POST /api/activity handler unchanged: DATE_RE constant + NextRequest import + `export async function POST` all still present
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0; `npm run build` succeeds
  </acceptance_criteria>
  <done>lib/dashboard.ts is the single server-side source of truth for stats + activity (getStats + getActivityData); both API GET routes delegate to it via try/catch; POST /api/activity untouched; type-check + lint + build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (GET /api/stats, GET /api/activity) | Existing auth gate (middleware.ts `ks_auth` cookie) unchanged; routes now delegate to lib/dashboard.ts |
| RSC server → lib/dashboard.ts | Server-internal call (behind the same auth gate); no new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information Disclosure | lib/dashboard.ts RSC→client prop serialization | low | mitigate | StatsDTO + ActivityDTO include ONLY fields the existing /api/stats + /api/activity GET routes already return — both DTOs are exactly the existing JSON response shapes. No new data crosses the boundary. (RESEARCH.md Security Domain) |
| T-12-02 | Information Disclosure | lib/dashboard.ts accidentally imported client-side | low | mitigate | `// No 'use client` guard comment (codebase precedent: lib/study-cards.ts, lib/settings.ts); the module imports `prisma` (Node-only) which fails at build time if client-imported — defense in depth. `server-only` package NOT installed (no-new-packages constraint). |
| T-12-03 | Tampering | npm package installs | low | accept | Phase 12 installs zero packages (STATE.md: no new npm packages; RESEARCH.md Package Legitimacy Audit table is empty). No install tasks → no supply-chain risk. The only existing dynamic import (canvas-confetti) is already vetted from v1.1 and preserved unchanged. |

No threat meets the `high` blocking threshold (security_block_on: high) — no blocking human checkpoints required.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (both tasks)
- `npm run lint` exits 0 (both tasks — no react-hooks/purity or set-state-in-effect violations in this plan; lib/dashboard.ts has no hooks)
- `npm run build` succeeds (Task 2 — confirms lib/dashboard.ts is server-only and not pulled into any client bundle)
- GET /api/stats and GET /api/activity still return the same JSON shapes (load /wrapped after conversion to confirm cardsByType + days shapes are byte-identical)
</verification>

<success_criteria>
- lib/dashboard.ts exists with getStats() + getActivityData() server-only functions (D-05)
- lib/dto.ts exports StatsDTO + ActivityDTO (D-06)
- GET /api/stats + GET /api/activity delegate to lib/dashboard.ts (D-08); POST /api/activity untouched
- Foundation ready for Wave 2: app/page.tsx + app/habits/page.tsx can import getStats/getActivityData and the DTO types
</success_criteria>

<output>
Create `.planning/phases/12-home-habits-hydration/12-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**lib/dashboard.ts (NEW file):**
- `getStats(): Promise<StatsDTO>` — async server-only function (5 parallel Prisma queries)
- `getActivityData(): Promise<ActivityDTO>` — async server-only function (3 parallel queries)

**lib/dto.ts (MODIFIED — new exports):**
- `StatsDTO` interface (fields: totalCards, dueCards, totalLessons, cardsByType, masteredCount)
- `ActivityDTO` interface (fields: days: DayRecord[], dailyGoalSeconds, dayStartHour)
- New import: `import type { DayRecord } from '@/lib/habit'`

**app/api/stats/route.ts (MODIFIED):**
- `GET()` — now delegates to `getStats()` via try/catch (replaces inline 5-query body)

**app/api/activity/route.ts (MODIFIED):**
- `GET()` — now delegates to `getActivityData()` via try/catch (replaces inline 3-query body)
- `POST()` — UNCHANGED (study-time logging; DATE_RE + NextRequest preserved)
