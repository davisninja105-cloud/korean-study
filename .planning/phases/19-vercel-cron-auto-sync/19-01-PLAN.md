---
phase: 19-vercel-cron-auto-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sync.ts
  - app/api/sync/route.ts
autonomous: true
requirements: [SYNC-02]
must_haves:
  truths:
    - "Triggering a manual sync via Settings ▸ Advanced ▸ Sync now returns the exact same response shape and behavior as before the refactor."
    - "A single sync request still processes at most one new lesson (MAX_LESSONS_PER_SYNC = 1, unchanged)."
    - "Both the manual route and (later) the cron route can call one shared runSync() function."
  artifacts:
    - "lib/sync.ts exporting runSync(documentId) and the SyncResult interface"
    - "app/api/sync/route.ts reduced to a thin POST wrapper that calls runSync()"
  key_links:
    - "app/api/sync/route.ts imports runSync from @/lib/sync"
    - "MAX_LESSONS_PER_SYNC constant lives in lib/sync.ts; maxDuration route-segment export stays in app/api/sync/route.ts"
---

<objective>
Extract the entire `POST /api/sync` handler body into a reusable server-only `lib/sync.ts:runSync(documentId)` function, mirroring the established `lib/study-cards.ts` / `lib/dashboard.ts` shared-core pattern. The manual route becomes a thin wrapper. This is a PURE REFACTOR — the manual sync path must behave identically (same response shape, same MAX_LESSONS_PER_SYNC=1 bounding, same per-lesson failure reporting, same `remaining` count). No cron code, no auth, no timestamp in this plan.

Purpose: Establishes the single shared sync function that the new `GET /api/cron/sync` route (Plan 03) will call, without changing manual-sync behavior. Extraction-first is the research-recommended sequencing (verify manual sync unchanged before layering cron on top).
Output: `lib/sync.ts` (new), `app/api/sync/route.ts` (thin wrapper).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-vercel-cron-auto-sync/19-RESEARCH.md
@.planning/phases/19-vercel-cron-auto-sync/19-PATTERNS.md

# The file whose body moves wholesale into runSync():
@app/api/sync/route.ts
# The shared-core precedent to mirror (server-only header comment + exported fn returning a plain object):
@lib/study-cards.ts
</context>

<artifacts_produced>
- `runSync(documentId: string): Promise<SyncResult>` — exported from `lib/sync.ts`
- `SyncResult` interface — exported from `lib/sync.ts`
- `app/api/sync/route.ts` — thin `POST` wrapper (no longer contains extraction logic)
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create lib/sync.ts with runSync() + SyncResult (verbatim body move)</name>
  <files>lib/sync.ts</files>
  <read_first>
    - app/api/sync/route.ts (the full POST handler — lines 22-329 are what moves)
    - lib/study-cards.ts (server-only module header convention + exported-function-returns-plain-object shape)
    - .planning/phases/19-vercel-cron-auto-sync/19-PATTERNS.md (section "lib/sync.ts" — exact source-body line references and SyncResult interface)
  </read_first>
  <action>
    Create server-only `lib/sync.ts` with the header comment convention from lib/study-cards.ts: a leading `// No 'use client' — this module runs server-side only.` plus a one-line note that this was extracted from app/api/sync/route.ts so both the manual route and GET /api/cron/sync call one shared function. Move the imports currently at the top of app/api/sync/route.ts that the body needs: createHash from crypto, fetchGoogleDoc, extractCardsFromNotes, normalizeFront, resolveDependencyEdges, lessonExcerpt, prisma. Do NOT import NextRequest/NextResponse into this file. Declare the MAX_LESSONS_PER_SYNC constant here (value 1, unchanged) with its existing explanatory comment. Export a SyncResult interface with fields: synced (boolean), newLessons (number), newCards (number), remaining (number), failed (number), failures (optional string array), message (optional string). Export async function runSync(documentId: string): Promise<SyncResult> containing the exact business logic from app/api/sync/route.ts (the content-hash new-lesson filter, the bounded batch slice, the existingNormalizedFronts load, the Promise.allSettled extraction, the orderBase aggregate, the per-lesson persist loop with keyToId seeding, the dedup-by-normalizeFront, the create/update card branches, the two-phase resolveDependencyEdges linking, and the failures bookkeeping) — copied verbatim. Convert every `return NextResponse.json({...})` inside the moved body into a plain `return {...}` object literal that satisfies SyncResult (the early "no new content" return and the final success return both become plain objects; include `failed` — the count of failures — and spread `failures`/`message` conditionally exactly as the route does today). Do NOT move the outer try/catch — per RESEARCH Pattern 1, runSync() lets errors propagate and the thin caller catches them (mirrors getStudyCards()). Do NOT read process.env inside runSync — documentId is a parameter.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - source: `lib/sync.ts` exports `runSync` and `SyncResult`; imports neither `NextRequest` nor `NextResponse`.
    - behavior: `MAX_LESSONS_PER_SYNC` is declared in lib/sync.ts with value 1 (unchanged).
    - behavior: every branch of runSync returns a plain object assignable to SyncResult (no NextResponse in this file).
    - test-command: `npm run build` succeeds (prisma generate + typecheck + next build) with no type errors from the moved code.
  </acceptance_criteria>
  <done>lib/sync.ts compiles, exports runSync + SyncResult, contains the full extraction pipeline as plain returned objects, and pulls MAX_LESSONS_PER_SYNC=1 in unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reduce app/api/sync/route.ts to a thin POST wrapper</name>
  <files>app/api/sync/route.ts</files>
  <read_first>
    - app/api/sync/route.ts (current full handler)
    - .planning/phases/19-vercel-cron-auto-sync/19-PATTERNS.md (section "app/api/sync/route.ts" and the "Thin wrapper pattern after extraction")
  </read_first>
  <action>
    Replace the body of app/api/sync/route.ts with the thin-wrapper shape from RESEARCH Pattern 1: import runSync from @/lib/sync; keep `export const maxDuration = 300` on the route file (it is a Next.js route-segment config export and must stay here, NOT in lib/sync.ts — see PATTERNS.md rationale). The POST handler parses `documentId` from the request JSON, returns a 400 `{ error: 'documentId is required' }` when it is missing (preserve this exact validation and message), calls `await runSync(documentId)`, returns `NextResponse.json(result)` on success, and wraps everything in the existing try/catch that logs `console.error('Sync error:', err)` and returns 500 `{ error: 'Sync failed — see server logs for details' }` (preserve message verbatim). Remove all the now-migrated imports (createHash, fetchGoogleDoc, extractCardsFromNotes, normalizeFront, resolveDependencyEdges, lessonExcerpt, prisma) and the MAX_LESSONS_PER_SYNC constant from this file — they live in lib/sync.ts now. Keep the NextRequest/NextResponse imports.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm test</automated>
    <human-check>Deploy locally (npm run build && npm start), open Settings ▸ Advanced ▸ Sync now, trigger a manual sync, and confirm the response shape (synced / newLessons / newCards / remaining / failed / optional failures / optional message) and behavior are identical to before the refactor. Verify at most 1 new lesson is processed per tap.</human-check>
  </verify>
  <acceptance_criteria>
    - source: app/api/sync/route.ts imports `runSync` from `@/lib/sync` and no longer imports the extraction dependencies (createHash, fetchGoogleDoc, extractCardsFromNotes, normalizeFront, resolveDependencyEdges, lessonExcerpt, prisma).
    - source: `export const maxDuration = 300` remains present in app/api/sync/route.ts.
    - behavior: missing `documentId` still returns HTTP 400 with `{ error: 'documentId is required' }`.
    - behavior: a thrown error still yields HTTP 500 with `{ error: 'Sync failed — see server logs for details' }`.
    - test-command: `npm test` (full Vitest suite) stays green; `npm run lint` clean.
    - human-check: manual "Sync now" regression — identical response shape and 1-lesson-per-request behavior (SYNC-02 manual path unchanged).
  </acceptance_criteria>
  <done>app/api/sync/route.ts is a thin wrapper delegating to runSync(); build, lint, and full test suite pass; manual sync behaves identically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (SyncPanel) → POST /api/sync | Authenticated request (existing HMAC cookie gate, unchanged by this plan) crossing into the sync pipeline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-04 | Tampering | app/api/sync/route.ts + lib/sync.ts (refactor) | low | mitigate | Pure refactor — no auth/behavior change. Full Vitest suite plus a manual "Sync now" regression confirm identical response shape; runSync() takes documentId as a trusted parameter, reads no attacker-controlled env/input. |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies this phase (verified in RESEARCH Package Legitimacy Audit); no install task exists. |
</threat_model>

<verification>
- `npm run build` succeeds (typecheck clean after the body move).
- `npm run lint` clean (no react-hooks/purity or unused-import violations).
- `npm test` full suite green (no existing test regressed by the extraction).
- Manual: Settings ▸ Advanced ▸ Sync now returns the identical response shape and processes ≤1 lesson.
</verification>

<success_criteria>
runSync() is the single shared sync core, app/api/sync/route.ts is a thin wrapper, MAX_LESSONS_PER_SYNC=1 is preserved in lib/sync.ts, and the manual sync path is behaviorally unchanged (SYNC-02 manual path).
</success_criteria>

<output>
Create `.planning/phases/19-vercel-cron-auto-sync/19-01-SUMMARY.md` when done.
</output>
