---
phase: 19-vercel-cron-auto-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/settings.ts
  - app/api/settings/route.ts
  - app/settings/page.tsx
autonomous: true
requirements: [SYNC-04]
must_haves:
  truths:
    - "Settings ▸ Advanced shows a 'last auto-synced' timestamp so a silently-failing daily cron is noticeable."
    - "The timestamp value persists across requests via the existing generic Setting table — no schema change, no Turso DDL."
    - "The timestamp is read-only from the client's perspective: it appears in GET /api/settings but a client PUT can never overwrite it."
  artifacts:
    - "lib/settings.ts exporting getLastAutoSyncedAt() and setLastAutoSyncedAt(iso)"
    - "GET /api/settings response includes lastAutoSyncedAt"
    - "app/settings/page.tsx Advanced section renders the formatted timestamp next to SyncPanel"
  key_links:
    - "getLastAutoSyncedAt() reads the 'lastAutoSyncedAt' key from the Setting table"
    - "app/settings/page.tsx reads d.lastAutoSyncedAt from the existing GET /api/settings fetch (no new fetch)"
---

<objective>
Persist and display the "last auto-synced" timestamp (SYNC-04). Add `getLastAutoSyncedAt`/`setLastAutoSyncedAt` to `lib/settings.ts` (stored as a raw ISO string under a new key in the EXISTING generic `Setting` table — confirmed no schema change per RESEARCH), surface it read-only in `GET /api/settings`, and render it (formatted at render time) inside the Settings ▸ Advanced disclosure next to `SyncPanel`. The setter is called only by the cron route (Plan 03); this plan wires the persistence + display so Plan 03 has a place to write.

Purpose: Gives the user an at-a-glance signal that the unattended daily cron is still running. The field is deliberately read-only for clients — only the cron route ever writes it.
Output: `lib/settings.ts` (getter/setter added), `app/api/settings/route.ts` (GET returns the field), `app/settings/page.tsx` (Advanced section displays it).

SCHEMA NOTE: SYNC-04 requires NO schema change. The timestamp is a new key in the existing `Setting` key/value table (same pattern as `dailyGoalSeconds`). `prisma/schema.prisma` is NOT touched, so no `npx prisma generate` / Turso DDL step is needed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-vercel-cron-auto-sync/19-RESEARCH.md
@.planning/phases/19-vercel-cron-auto-sync/19-PATTERNS.md

@lib/settings.ts
@app/api/settings/route.ts
@app/settings/page.tsx
</context>

<artifacts_produced>
- `getLastAutoSyncedAt(): Promise<string | null>` — exported from `lib/settings.ts`
- `setLastAutoSyncedAt(iso: string): Promise<void>` — exported from `lib/settings.ts`
- `lastAutoSyncedAt` field on the `GET /api/settings` JSON response
- "Last auto-synced" status line in the Settings ▸ Advanced disclosure
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add getLastAutoSyncedAt / setLastAutoSyncedAt to lib/settings.ts</name>
  <files>lib/settings.ts</files>
  <read_first>
    - lib/settings.ts (existing key-constant + getter/setter convention, e.g. GOAL_KEY / getDailyGoalSeconds / setDailyGoalSeconds)
    - .planning/phases/19-vercel-cron-auto-sync/19-PATTERNS.md (section "lib/settings.ts" — exact getter/setter shape, and the note on the void return-type asymmetry)
  </read_first>
  <action>
    Add a new module-level key constant LAST_AUTO_SYNCED_KEY set to the string 'lastAutoSyncedAt', alongside the other *_KEY constants at the top of lib/settings.ts. Add async getLastAutoSyncedAt(): Promise<string | null> that reads prisma.setting.findUnique for that key and returns row?.value ?? null (a raw ISO string, or null if the cron has never run successfully). Add async setLastAutoSyncedAt(iso: string): Promise<void> that upserts the key with { value: iso } (create + update both set value to iso). No numeric clamping and no HEX validation — the value is an opaque ISO string stored as-is. The setter returns void (unlike the other setters that echo their clamped value) because its only caller already holds the string it wrote and does not round-trip it — document this asymmetry in a brief comment.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - source: lib/settings.ts exports `getLastAutoSyncedAt` and `setLastAutoSyncedAt`.
    - behavior: getter returns `null` when the key row is absent, otherwise the stored ISO string unchanged.
    - behavior: setter upserts under key `lastAutoSyncedAt` and returns void.
    - source: no change to `prisma/schema.prisma` (reuses the existing Setting model).
    - test-command: `npm run build` clean.
  </acceptance_criteria>
  <done>Getter/setter exist, follow the existing key-constant convention, store a raw ISO string with no schema change, and typecheck cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Surface lastAutoSyncedAt read-only in GET /api/settings</name>
  <files>app/api/settings/route.ts</files>
  <read_first>
    - app/api/settings/route.ts (existing GET Promise.all pattern and PUT conditional-set-else-read-through pattern)
    - .planning/phases/19-vercel-cron-auto-sync/19-PATTERNS.md (section "app/api/settings/route.ts" — the read-only asymmetry requirement)
  </read_first>
  <action>
    Import getLastAutoSyncedAt from @/lib/settings. In the GET handler, add getLastAutoSyncedAt() to the existing Promise.all array and include lastAutoSyncedAt in the returned JSON object alongside the other settings. Do NOT add it to the PUT handler at all: do not destructure it from the request body, do not add a `has*` validation flag for it, and do not call its setter from PUT. This enforces (defense in depth beyond the UI not exposing a control) that a client PUT can never overwrite the timestamp — only the cron route writes it. Leave the entire PUT handler otherwise unchanged.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
    <human-check>Run locally, curl GET /api/settings, and confirm the JSON body contains a lastAutoSyncedAt key (null before the cron has ever run). Then PUT an object attempting to set lastAutoSyncedAt to an arbitrary value and confirm the response does NOT reflect that value (the field is ignored by PUT).</human-check>
  </verify>
  <acceptance_criteria>
    - source: GET /api/settings resolves getLastAutoSyncedAt() and includes `lastAutoSyncedAt` in its JSON response.
    - source: the PUT handler does not reference lastAutoSyncedAt (no destructure, no has-flag, no setter call).
    - behavior: a PUT carrying a lastAutoSyncedAt field is a no-op for that field (cannot overwrite the stored value).
    - test-command: `npm run build` clean; `npm run lint` clean.
  </acceptance_criteria>
  <done>GET returns lastAutoSyncedAt; PUT cannot write it; build and lint pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Display the timestamp in Settings ▸ Advanced</name>
  <files>app/settings/page.tsx</files>
  <read_first>
    - app/settings/page.tsx (the useEffect GET-fetch-and-populate block ~lines 38-53, and the Advanced <details> section ~lines 458-467)
    - .planning/phases/19-vercel-cron-auto-sync/19-PATTERNS.md (section "app/settings/page.tsx" — visual weight guidance: a muted caption inside the existing disclosure, not a new card)
  </read_first>
  <action>
    Add a new client state `const [lastAutoSyncedAt, setLastAutoSyncedAt] = useState<string | null>(null)` near the other useState hooks. Inside the EXISTING useEffect that fetches /api/settings, add a line reading `setLastAutoSyncedAt(d.lastAutoSyncedAt ?? null)` — no new fetch, it rides along the same GET payload. Inside the Advanced <details> block, in the same `<div className="mt-2">` that holds `<SyncPanel onSynced={() => {}} />`, add a passive status line: a `text-xs text-muted` paragraph reading "Last auto-synced:" followed by the value. Format the ISO string at render time to a human-readable local date/time (e.g. via new Date(lastAutoSyncedAt).toLocaleString()) ONLY when it is non-null; when null, show a "Never" / "not yet run" fallback. Because ESLint react-hooks/purity forbids constructing dates impurely during render only for time-of-now reads, note: parsing a fixed stored ISO string with `new Date(isoString)` (an argument-bearing constructor) is permitted — it is the no-arg `new Date()` / Date.now() form that is banned; do not introduce any no-arg date or Math.random call. Keep it a passive caption inside the disclosure (no bg-surface card, no action button) per the "back-office plumbing" section intent.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
    <human-check>Run locally (npm run build && npm start), open Settings ▸ Advanced, and confirm a "Last auto-synced" line appears next to the Sync panel showing "Never"/fallback when the Setting is unset. After Plan 03 lands and a cron route call writes the timestamp, reload and confirm it renders the formatted local date/time.</human-check>
  </verify>
  <acceptance_criteria>
    - source: app/settings/page.tsx has a lastAutoSyncedAt state populated from d.lastAutoSyncedAt inside the existing /api/settings effect (no additional fetch).
    - behavior: Advanced section renders "Last auto-synced: <formatted>" when set, and a "Never"/"not yet run" fallback when null.
    - source: no no-arg `new Date()` / `Date.now()` / `Math.random()` introduced during render (react-hooks/purity stays clean).
    - test-command: `npm run lint` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <done>The Advanced section shows the last-auto-synced status line, formatted at render, lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PUT /api/settings | Authenticated client can write settings; the new timestamp must NOT be among the writable fields. |
| client → GET /api/settings | Authenticated client reads the timestamp (informational, non-sensitive). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-05 | Tampering | app/api/settings/route.ts (client could overwrite lastAutoSyncedAt) | medium | mitigate | Field is GET-only. It is omitted entirely from the PUT destructure/validation/setter path, so a crafted client PUT cannot forge a "fresh" timestamp that would mask a silently-failing cron. Only the cron route (Plan 03) writes it. |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies this phase (verified in RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `npm run build` and `npm run lint` clean across all three files.
- GET /api/settings includes lastAutoSyncedAt; PUT cannot set it.
- Settings ▸ Advanced shows the timestamp (fallback when unset, formatted local date/time when set).
- No schema change: `prisma/schema.prisma` untouched (Setting table reused).
</verification>

<success_criteria>
The "last auto-synced" timestamp persists in the Setting table (no DDL), is exposed read-only via GET /api/settings, and is visible in Settings ▸ Advanced — satisfying SYNC-04's storage + display, ready for Plan 03's cron route to write into.
</success_criteria>

<output>
Create `.planning/phases/19-vercel-cron-auto-sync/19-02-SUMMARY.md` when done.
</output>
