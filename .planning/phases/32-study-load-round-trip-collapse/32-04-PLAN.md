---
phase: 32-study-load-round-trip-collapse
plan: 04
type: execute
wave: 4
depends_on: ["32-01", "32-02", "32-03"]
files_modified:
  - tests/study-roundtrips.test.ts
  - e2e/perf.spec.ts
  - e2e/study-cache-invalidation.spec.ts
  - .planning/phases/32-study-load-round-trip-collapse/32-BASELINE.md
autonomous: true
requirements: [STUDY-01, STUDY-03]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A warm-cache `/study` load issues at most 2 physical libSQL round trips; the first load after a cold process start or after a sync/relink version bump issues at most 3; and the very next load returns to at most 2. All three numbers come from the committed counter harness, not from inspection (STUDY-01, success criterion #1, D-01)."
    - "\"Round trip\" is defined once and measured one way — one physical libSQL HTTP request counted at the `@libsql/client` boundary — and the assertion reports Prisma's own `$on('query')` count alongside it, failing loudly rather than silently averaging if the two disagree (research Assumption A4)."
    - "The `/study` page-load budget and the `/api/cards/due` round-trip budget in `e2e/perf.spec.ts` are tightened from measured medians, each carrying a comment naming the measured number it was derived from and the fixture-scale caveat that forbids reading it as a production-latency claim."
    - "Starting a fresh sync or relink from a separate process and then reloading `/study` in the already-running server surfaces the newly created `CardDependency` edge in session sequencing — no restart, no redeploy, no stale prerequisite (STUDY-03, success criterion #3)."
    - "Session composition survives the whole phase: `tests/sequence.test.ts`, `tests/known-words.test.ts`, `tests/sentence-selection.test.ts`, `tests/study-cards.test.ts` and `e2e/grade-flow.spec.ts` are all green with their behavioral assertions unchanged (success criterion #4)."
    - "`FreshnessWatcher` still exists and `export const dynamic = 'force-dynamic'` is still on `app/study/page.tsx` — neither was removed as newly redundant."
  artifacts:
    - "tests/study-roundtrips.test.ts — the committed STUDY-01 proof"
    - "e2e/study-cache-invalidation.spec.ts — the cross-process no-redeploy proof"
    - "e2e/perf.spec.ts — per-path API budgets, tightened /study and /api/cards/due thresholds"
    - "32-BASELINE.md — before and after numbers in one file"
  key_links:
    - "The counter harness from Plan 01 is what makes success criterion #1 falsifiable — if it is ever deleted, the ≤2 claim reverts to an unverifiable comment."
    - "`e2e/perf.spec.ts` currently shares one `API_BUDGET_MS` across three endpoints; tightening `/api/cards/due` alone requires converting it to a per-path record without loosening `/api/stats` or `/api/activity`."
    - "The invalidation spec must mutate the DB from a separate process (the `e2e/run-mutate.ts` harness), because an in-process mutation would not prove the cross-process claim that `scripts/local-resync.mts` depends on."
  prohibitions:
    - "The performance claim this phase makes must not be rendered unfalsifiable or flattering: the counter harness must not be deleted once the phase goes green, the cache-miss count must not be omitted when the warm-cache count is quoted, and a number measured against the ~8-card e2e fixture must never be presented as evidence about production-scale (~1056-card) latency."
---

<objective>
Turn the phase's claims into committed, runnable proofs: the round-trip count, the tightened budgets, and the cross-process freshness guarantee.

Purpose: success criterion #1 explicitly demands the ≤2 result be "demonstrable from query instrumentation rather than asserted by inspection", and success criterion #3 demands freshly synced lessons sequence correctly "with no redeploy". Neither is satisfied by working code alone — both need an artifact that fails when the property stops holding. D-01 also requires the assertion to distinguish the steady-state guarantee from the acceptable cache-miss case rather than asserting one unconditional number.

Output: a committed round-trip assertion test, a cross-process invalidation e2e spec, tightened per-path perf budgets derived from measured medians, and a baseline file carrying both the before and after numbers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-study-load-round-trip-collapse/32-RESEARCH.md
@.planning/phases/32-study-load-round-trip-collapse/32-CONTEXT.md
@.planning/phases/32-study-load-round-trip-collapse/32-BASELINE.md
@.planning/phases/32-study-load-round-trip-collapse/32-01-SUMMARY.md
@.planning/phases/32-study-load-round-trip-collapse/32-02-SUMMARY.md
@.planning/phases/32-study-load-round-trip-collapse/32-03-SUMMARY.md
</context>

## Artifacts this phase produces

Complete phase-wide list of net-new symbols and files. The plan-review source-grounding pass should exclude every entry below from drift verification — none of these existed before Phase 32.

| Artifact | Kind | Plan |
|---|---|---|
| `lib/query-counter.ts` | new file | 01 |
| `countingLibsqlClient(client)` | new function | 01 |
| `resetQueryCount()` / `getQueryCounts()` / `notePrismaQueryEvent()` | new functions | 01 |
| `QUERY_COUNTER_ENV` (`STUDY_QUERY_COUNTER`) | new const / new env var | 01 |
| `scripts/measure-study-roundtrips.mts` | new file | 01, extended in 03 |
| `--probe-json` / `--dump-order` | new script flags | 01 / 03 |
| `.planning/phases/32-.../32-BASELINE.md` | new file | 01, updated in 04 |
| `lib/study-cache.ts` | new file | 02 |
| `StudyInvariants` | new interface | 02 |
| `getStudyCache()` / `refreshStudyCache(version)` / `resetStudyCacheForTests()` | new functions | 02 |
| `STUDY_CACHE_VERSION_KEY` / `SETTING_KEYS.studyCacheVersion` | new const / new Setting key (`studyCacheVersion`) | 02 |
| `bumpStudyCacheVersion()` | new function | 02 |
| `tests/study-cache.test.ts` | new file | 02 |
| `StudyCardsResult` | new interface | 03 |
| `PoolRow` | new local type | 03 |
| `tests/study-roundtrips.test.ts` | new file | 04 |
| `e2e/study-cache-invalidation.spec.ts` | new file | 04 |

Modified pre-existing files across the phase: `lib/prisma.ts`, `lib/settings.ts`, `lib/sync.ts`, `lib/relink-dependencies.ts`, `lib/study-cards.ts`, `app/study/page.tsx`, `app/api/cards/due/route.ts`, `tests/study-cards.test.ts`, `tests/relink-dependencies.test.ts`, `e2e/perf.spec.ts`. No schema change: `prisma/schema.prisma` is untouched — the existing `Setting` table (`key String @id`, `value String`) already holds the new key.

## Phase-level decisions recorded

- **Assumption-delta (pluralization, `second`)** — the phase scope says "removing (or proving non-duplicative) **the second** full-row `card.findMany`". Decision: **add alongside, not promote or collapse.** Both queries are kept as two distinct reads with disjoint column sets, because 32-RESEARCH.md verified by direct column comparison that `lib/sequence.ts`'s `SeqCard` needs exactly three fields while `lib/dto.ts`'s `CardDTO` needs eleven more plus `sentences[]`. STUDY-02 therefore resolves as "confirmed non-duplicative" and neither query is merged into or replaced by the other. Recorded here rather than left implicit.
- **API-coverage checkpoint** — the deterministic detector returned `detected: false` against this phase's scope. This phase integrates no external API, SDK, or service: all work is internal query-shape and caching against Prisma/libSQL already in production use. No `COVERAGE.md` and no coverage checkpoint.
- **Schema-push gate** — not triggered. No file under `prisma/` is in any plan's `files_modified`, so the Turso DDL procedure documented in CLAUDE.md does not apply.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The committed round-trip assertion — warm, cold, and back to warm</name>
  <precondition>`lib/query-counter.ts` and `scripts/measure-study-roundtrips.mts` from Plan 01 exist, and the Plan 03 rewrite is landed.</precondition>
  <files>tests/study-roundtrips.test.ts, .planning/phases/32-study-load-round-trip-collapse/32-BASELINE.md</files>
  <read_first>
    - `lib/query-counter.ts` (the counting contract and the exact accessor names)
    - `lib/study-cache.ts` (`resetStudyCacheForTests()` — the only way to simulate a cold instance)
    - `lib/study-cards.ts` (the finished two-request shape being asserted)
    - `tests/relink-dependencies.test.ts` lines 1-95 (the real-temp-SQLite-DB + env-first-dynamic-import harness this test must copy; a mocked Prisma cannot count physical requests)
    - `tests/study-cache.test.ts` (Plan 02's real-DB block — reuse its seeding approach rather than inventing a second one)
    - `.planning/phases/32-study-load-round-trip-collapse/32-CONTEXT.md` § D-01 (the steady-state reading this assertion must encode)
    - `.planning/phases/32-study-load-round-trip-collapse/32-BASELINE.md` (Plan 01's before-numbers, which this task appends to)
  </read_first>
  <behavior>
    - Cold snapshot (`resetStudyCacheForTests()` called, no stored invariants): one `getStudyCards()` call costs at most 3 physical requests.
    - Immediately repeating the same call with the snapshot now warm costs at most 2 physical requests.
    - After `bumpStudyCacheVersion()` runs, the next call costs at most 3, and the call after that returns to at most 2.
    - The Prisma `$on('query')` event count is captured on the same runs; when it differs from the physical count the test fails with both numbers in the message rather than passing on the lower one.
    - The physical count is never 0 — a zero means the counting Proxy detached and must fail, not read as a perfect score.
  </behavior>
  <action>
Create `tests/study-roundtrips.test.ts` as the committed proof of STUDY-01. Copy the harness structure of `tests/relink-dependencies.test.ts` exactly: `mkdtempSync` a temp directory, set `process.env.DATABASE_URL` to its `file:` URL and `process.env.STUDY_QUERY_COUNTER` to `'1'` BEFORE any of `../lib/prisma`, `../lib/study-cards`, `../lib/study-cache`, `../lib/settings`, `../lib/query-counter` are imported, then dynamic-import all five inside `beforeAll`. Static imports of those modules are forbidden — ESM hoisting would bind the Prisma singleton to the wrong database and construct it without the counting Proxy. Apply the real DDL the same way the analog test does, seed enough cards with review rows and sentences that the pool is non-empty, and restore the previous env plus `rmSync` the temp dir in `afterAll`.

Implement the four cases from `<behavior>`. Encode D-01's steady-state reading literally: assert at most 2 on warm runs and at most 3 on the cache-miss run — do not assert one unconditional number across both, and do not quietly relax the warm assertion to 3 to make a flaky run pass. Around each measured call, use `resetQueryCount()` before and `getQueryCounts()` after; drive the cache-miss cases with `resetStudyCacheForTests()` and with `bumpStudyCacheVersion()` respectively, so both miss triggers named in D-01 (cold instance, post-sync bump) are covered.

Add the cross-check case: on one warm run, read both `physical` and `prismaEvents` from `getQueryCounts()` and assert they are equal, with a failure message printing both numbers and naming the physical count as authoritative. Research Assumption A4 flags that Prisma's query events may not fire 1:1 through the driver-adapter path in some configurations — this test is where that gets resolved for this project rather than assumed. If they turn out to differ consistently, do not delete the case: change it to assert the specific observed relationship and record the finding in `32-BASELINE.md`.

Head the file with a comment block, in the style of `tests/study-cards.test.ts`'s header, stating the round-trip definition, why counting happens at the `@libsql/client` boundary rather than at Prisma's, and that D-01's warm/cold split is deliberate rather than a loosened assertion.

Append an `## After` section to `32-BASELINE.md` recording the measured warm-cache count, the cache-miss count, the Prisma-event cross-check result, and the before-to-after delta for the total. Quote both the warm and the miss number wherever the improvement is stated — never the warm number alone. Restate the fixture-scale caveat in that section: these are round-trip counts, which are fixture-size independent, and nothing here is a latency claim about the ~1056-card production deck.
  </action>
  <verify>
    <automated>npx vitest run tests/study-roundtrips.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/study-roundtrips.test.ts` passes with all four behavior cases plus the cross-check executing (no `.skip`, no `.todo`).
    - `grep -cE "toBeLessThanOrEqual\(2\)" tests/study-roundtrips.test.ts` is at least 2 (the two warm assertions) and `grep -cE "toBeLessThanOrEqual\(3\)" tests/study-roundtrips.test.ts` is at least 2 (the two miss assertions).
    - `grep -nE "toBeGreaterThan\(0\)" tests/study-roundtrips.test.ts` matches (the zero-count guard).
    - `grep -nE "^import .*(lib/prisma|lib/study-cards|lib/study-cache|lib/query-counter)" tests/study-roundtrips.test.ts` returns 0 value-import matches; `grep -cE "await import\('\.\./lib/" tests/study-roundtrips.test.ts` is at least 5.
    - `32-BASELINE.md` contains an `## After` heading whose body quotes both the warm-cache and the cache-miss counts and restates the fixture-scale caveat.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>The ≤2 claim is a test that fails when it stops being true, and the cache-miss cost is written down next to it rather than omitted.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten the `/study` and `/api/cards/due` budgets from measured medians</name>
  <files>e2e/perf.spec.ts</files>
  <read_first>
    - `e2e/perf.spec.ts` (all 139 lines — the D-06/D-07/D-08/D-09 comment conventions at 1-59, the `/cards` worked example at 26-52 showing exactly how a tightened budget is derived and documented, and the shared `API_BUDGET_MS` at 59)
    - `.planning/ROADMAP.md` § Phase 32 success criterion 2 (the 2.9s mode-select and 4.7s lesson-range on-device baselines this is judged against)
    - `.planning/phases/32-study-load-round-trip-collapse/32-BASELINE.md` (round-trip counts — note these are counts, not latencies; the budgets below come from fresh Playwright medians, not from that file)
    - `e2e/seed.ts` lines 38-60 (the fixture's actual card count, needed for the caveat comment)
  </read_first>
  <action>
Convert the single shared `API_BUDGET_MS` constant into a per-path record keyed by the same three paths the loop at line 112 iterates (`/api/cards/due`, `/api/stats`, `/api/activity`), so `/api/cards/due` can be tightened without loosening or tightening the other two. Leave `/api/stats` and `/api/activity` at their current 1000ms value and say so in a comment — this phase touched neither.

Measure before choosing numbers. Run the perf spec and read the per-sample `[perf]` lines it already logs for `/study` and `/api/cards/due`, taking the median of five as the file's own `median()` helper does. Derive each new budget with the same formula the `/cards` entry documents at line 51: measured median times 1.5, rounded up to the nearest 100ms. Do not pick a round number that looks good and do not back-fill a number from the ROADMAP's on-device baselines — those were measured on a real device against the production deck and are not comparable to this fixture.

Write the derivation into the comment block above each changed budget, following the `/cards` entry's structure exactly: the pre-change measured samples and median, the post-change measured samples and median, the arithmetic producing the budget, and the fixture-scale caveat naming the e2e fixture's actual card count against the ~1056-card production deck. If the post-change median is not meaningfully better than the pre-change median at this fixture's scale — which is the expected outcome, since round-trip *count* is what this phase reduced and an 8-card local SQLite fixture has near-zero per-request latency — record that honestly in the comment exactly as the `/cards` entry does at lines 41-52, and set the budget from the honest measured number rather than a flattering one. Do not smooth over an unexpected result.

Leave `/` and `/habits` budgets untouched.
  </action>
  <verify>
    <automated>npx playwright test e2e/perf.spec.ts</automated>
    <human-check>Read the new comment blocks in `e2e/perf.spec.ts` and confirm each tightened number's stated derivation matches the `[perf]` sample lines printed in the test output — the budget should be traceable to real logged samples, not asserted.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/perf.spec.ts` passes all seven budget tests.
    - `grep -n "API_BUDGET_MS" e2e/perf.spec.ts` shows a per-path record, not a bare number; `/api/stats` and `/api/activity` still map to 1000.
    - The `/study` entry in `PAGE_BUDGETS_MS` is strictly less than 3000, and the `/api/cards/due` entry is strictly less than 1000.
    - The comment block above each changed budget contains: pre-change samples and median, post-change samples and median, the times-1.5-rounded-up arithmetic, and the fixture card count with the production-deck caveat.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Both budgets are tighter, and each number can be traced back to logged samples rather than taken on faith.</done>
</task>

<task type="auto">
  <name>Task 3: Prove no-redeploy freshness end to end, from a separate process</name>
  <files>e2e/study-cache-invalidation.spec.ts</files>
  <read_first>
    - `e2e/freshness-fresh-paths.spec.ts` (the closest existing spec shape: mutate out-of-band, reload, assert the running server reflects it)
    - `e2e/run-mutate.ts` and `e2e/helpers/mutate.ts` (the established separate-process DB mutation harness — this is what makes the cross-process claim real rather than simulated)
    - `e2e/seed.ts` lines 160-190 (the existing single `CardDependency` edge in the fixture and the card shapes to build on)
    - `e2e/perf.spec.ts` lines 12-24 (the `beforeAll(resetToBaseline)` ordering safeguard convention specs in this suite follow)
    - `lib/relink-dependencies.ts` and `lib/settings.ts` (the bump this spec is proving reaches a running server)
    - `.planning/phases/32-study-load-round-trip-collapse/32-CONTEXT.md` § D-02 (what this spec must NOT assert about review writes)
  </read_first>
  <action>
Create `e2e/study-cache-invalidation.spec.ts`. Begin with `beforeAll(resetToBaseline)` following the ordering-safeguard convention `e2e/perf.spec.ts` documents at lines 12-24, so the spec measures deterministic seeded content regardless of which spec ran before it.

The spec loads `/study` once to warm the running server's snapshot, then mutates the database from a SEPARATE process via the `e2e/run-mutate.ts` harness — never through an in-process Prisma call from inside the Playwright worker, because an in-process mutation would not exercise the claim that matters (`scripts/local-resync.mts` runs outside the server and CLAUDE.md documents it as the normal path for bulk syncs). The out-of-band mutation inserts a new card whose `components` JSON names an existing seeded card's `normalizedFront`, gives it a due `CardReview` row, and then calls `relinkAllDependencies()`, which both creates the edge and bumps the change token.

Then reload `/study` in the same, still-running server — no restart, no rebuild — and assert the new prerequisite relationship is reflected in session sequencing: the prerequisite card must appear before the new dependent card in the rendered session order. Prefer asserting on rendered order via the accessibility tree or on the `/api/cards/due` response body, whichever this suite's existing helpers already support; do not add a debug-only endpoint to make the assertion easier.

Add a second case locking D-02's boundary: grade one card through the existing review path so its FSRS state moves to 1, reload `/study`, and assert the change token did NOT move. Comment it as locking a deliberate decision — a failure here means D-02 was reopened, not that a bug appeared — and reference 32-CONTEXT.md D-02 by name.

Add a comment near the top naming this spec as the artifact for success criterion #3 ("no redeploy and no stale-prerequisite behavior") and stating why the mutation is out-of-band.
  </action>
  <verify>
    <automated>npx playwright test e2e/study-cache-invalidation.spec.ts e2e/grade-flow.spec.ts</automated>
    <human-check>Run `npm run build && npm start`, load `/study` and note the first cards, then in another terminal run `npx tsx scripts/relink-dependencies.mts` against the same DB, reload `/study` without restarting the server, and confirm the session reflects the relinked prerequisites.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/study-cache-invalidation.spec.ts` passes both cases (no `.skip`, no `.fixme`).
    - The spec performs its mutation through `e2e/run-mutate.ts` — `grep -n "run-mutate" e2e/study-cache-invalidation.spec.ts` matches — and does not import `@/lib/prisma` or `../lib/prisma` directly for the mutation.
    - The spec contains no server restart, rebuild, or `webServer` bounce between the mutation and the reload.
    - The D-02 case asserts the change token is unchanged after a review write and cites `32-CONTEXT.md` D-02 in a comment.
    - `npx playwright test` (full suite) passes, including `e2e/grade-flow.spec.ts`, `e2e/freshness-*.spec.ts`, and `e2e/perf.spec.ts`.
    - `grep -rn "FreshnessWatcher" components app | wc -l` is unchanged from before the phase, and `grep -c "force-dynamic" app/study/page.tsx` returns 1.
  </acceptance_criteria>
  <done>A freshly relinked edge, created by a separate process, shows up in the next `/study` load of a server that was never restarted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| separate `tsx` process → isolated e2e DB | The invalidation spec's out-of-band mutation crosses this boundary deliberately, mirroring `scripts/local-resync.mts` |
| Playwright browser context → running Next.js server | The authenticated `ks_auth` storageState context used by the perf and invalidation specs |
| test harness → real credentials | New tests construct Prisma clients and read env |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-10 | Repudiation | a performance claim with no failing-capable artifact | medium | mitigate | The ≤2 / ≤3 assertion is a committed Vitest test in `tests/study-roundtrips.test.ts`, and the no-redeploy claim is a committed Playwright spec — neither is a comment. `32-BASELINE.md` records the counting method and both the before and after numbers so the claim is reproducible. |
| T-32-01 | Tampering | raw SQL regression after this phase | high | mitigate | The `prisma\.\$(query\|execute)RawUnsafe\(` call-form grep is re-run as part of this plan's verification, not only at the point of introduction in Plan 03, so a later edit reintroducing the unsafe form is caught by the phase gate. |
| T-32-12 | Information Disclosure | new tests connecting to a database | low | mitigate | `tests/study-roundtrips.test.ts` pins `DATABASE_URL` to a `mkdtempSync` temp file before importing `lib/prisma` and restores the previous value in `afterAll`; the e2e spec runs against the isolated `e2e/.tmp/e2e-test.db`. Neither touches production Turso credentials. |
| T-32-17 | Tampering | a debug-only endpoint added to make an e2e assertion easier | medium | mitigate | Explicitly forbidden in Task 3's action: the assertion must use rendered order or the existing `/api/cards/due` response, so no new unauthenticated or introspection surface is added to the app to satisfy a test. |
| T-32-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new dependencies this phase (32-RESEARCH.md § "Package Legitimacy Audit"). No install task exists, so no legitimacy checkpoint applies. |
</threat_model>

<verification>
- `npm test` fully green, including the new `tests/study-roundtrips.test.ts`.
- `npx playwright test` fully green, including the new `e2e/study-cache-invalidation.spec.ts`, the tightened `e2e/perf.spec.ts`, and the untouched `e2e/grade-flow.spec.ts` and `e2e/freshness-*.spec.ts`.
- `npx tsc --noEmit` and `npm run lint` clean.
- `grep -nE "prisma\.\$(query|execute)RawUnsafe\(" lib app` returns 0 matches.
- `32-BASELINE.md` carries both a `## Phase B verdict` section and an `## After` section quoting the warm and the cache-miss counts.
- `force-dynamic` present on `app/study/page.tsx`; `FreshnessWatcher` present and unmodified.
</verification>

<success_criteria>
- STUDY-01's ≤2 is demonstrated by a test that would fail if it stopped holding, with the cache-miss cost recorded beside it.
- STUDY-03's no-redeploy freshness is demonstrated by a cross-process e2e spec against a server that is never restarted.
- Both tightened budgets trace to logged measurements and carry their fixture-scale caveat.
- Session composition is unchanged: every pre-existing unit and e2e behavioral assertion still passes.
</success_criteria>

<output>
Create `.planning/phases/32-study-load-round-trip-collapse/32-04-SUMMARY.md` when done. Quote the warm-cache and cache-miss counts together, the two new budget numbers with their derivations, and confirm `force-dynamic` and `FreshnessWatcher` survived.
</output>
