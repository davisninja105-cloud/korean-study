---
phase: 15-studysession-refactor-sentence-selection-memoization
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - components/FlashcardMode.tsx
  - components/MultipleChoiceMode.tsx
  - components/FillBlankMode.tsx
  - components/StudySession.tsx
autonomous: false
requirements: [REFACTOR-01]
must_haves:
  truths:
    - "`StudySession` renders each study mode through a dedicated sub-component — `FlashcardMode`, `MultipleChoiceMode`, or `FillBlankMode` — selected by the `mode` prop; the giant per-mode conditional JSX block (current lines 739-1062) no longer lives inline in `StudySession` (REFACTOR-01)."
    - "Each mode sub-component renders its OWN card-face wrapper AND its OWN sticky action bar with mode-specific buttons (Flashcard: Show Answer + 4 grade buttons calling `onGrade(1..4)`; MultipleChoice: options grid + single Next calling `onAdvance`; FillBlank: Check Answer + Wrong/Correct calling `onGrade(1)`/`onGrade(3)`) — no shared render-prop/slot abstraction is introduced (REFACTOR-01, RESEARCH Pitfall 3)."
    - "Session-owned state and the Phase 13 hardening stay entirely in `StudySession` (the parent): `queue`, `cursor`, `stats`, `seenCount`/`seenCardIdsRef`, `canUndo`/`undoRef`, `saveError`/`isMountedRef`, `submitReview`, `handleUndo`, `postReviewWithRetry`, `handleReveal`, `handleKeyDown`, and both `useMemo`s (`mcOptions`, `chosenIdx`) are unchanged; mode components receive only already-derived values + callbacks as props (RESEARCH Pitfall 4)."
    - "A live study session behaves identically to before: flip, grade all four ratings, undo, Exposure/Recall toggle, and switching between the three modes all work with no regression (REFACTOR-01, ROADMAP Success Criterion 4)."
    - "`npm run lint` is clean (react-hooks/purity respected) and `npm test` passes (including `tests/sentence-selection.test.ts`)."
  artifacts:
    - components/FlashcardMode.tsx
    - components/MultipleChoiceMode.tsx
    - components/FillBlankMode.tsx
  key_links:
    - "`components/StudySession.tsx` return body dispatches `mode === 'flashcard' ? <FlashcardMode/> : mode === 'multiple-choice' ? <MultipleChoiceMode/> : <FillBlankMode/>`, passing `currentCard`, `chosenSentence`, `displayedSentence`, `recallBlanked`, `showBareFront`, `fillSentence`/`fillTranslation`/`fillAnswer`/`fillCorrect`, `mcOptions`, `hints`, `revealed`, `cursor`, the flashcard measurement refs, and the `onReveal`/`onGrade`/`onAdvance`/`onSelect`/`onCycleExample`/`onInputChange` callbacks."
    - "`FlashcardMode` attaches the parent-owned `frontRef`/`backRef` (height measurement) and `againBtnRef` (reveal focus) received as props; the measuring `useLayoutEffect` and `handleReveal`'s `requestAnimationFrame` focus call stay in the parent and keep working."
---

<objective>
Split `components/StudySession.tsx`'s three inline study-mode UI blocks into dedicated sub-components — `FlashcardMode`, `MultipleChoiceMode`, `FillBlankMode` — leaving `StudySession` as the orchestrator that owns session state, the FSRS submit/undo pipeline, and mode dispatch (REFACTOR-01). This runs AFTER Plan 01 (per the locked sequencing note) so the sentence-selection logic is already extracted + memoized and is not restructured twice.

Purpose: Collapse the 300-line per-mode conditional (current lines 739-1062) into three focused, self-contained components so each mode's rendering is readable and independently modifiable, while the Phase-13-hardened review pipeline stays in exactly one place.

Output: three new client components under `components/`, and a slimmed `StudySession.tsx` whose return body is header + progress + a three-way mode dispatch. Behavior is identical to before (verified by live UAT).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-studysession-refactor-sentence-selection-memoization/15-RESEARCH.md
@.planning/phases/15-studysession-refactor-sentence-selection-memoization/15-PATTERNS.md
@components/StudySession.tsx
@components/ModeSelector.tsx
@components/AudioButton.tsx
@components/HighlightedSentence.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FlashcardMode, MultipleChoiceMode, FillBlankMode presentational components</name>
  <files>components/FlashcardMode.tsx, components/MultipleChoiceMode.tsx, components/FillBlankMode.tsx, components/StudySession.tsx</files>
  <read_first>
    - components/StudySession.tsx — the exact JSX to move: flashcard flip front/back faces (lines 741-865) + flashcard action bar (lines 981-1030); the non-flashcard wrapper + MC block (lines 866-922) + MC action bar (lines 1032-1039); the fill-blank block (lines 924-974) + fill action bar (lines 1041-1061). Also read the local `interface Sentence` (16-22) and `interface Card` (24-43) — these get an `export` keyword in this task. Note `normalizeAnswer` (61-63) and `MC_ADVANCE_MS` (54) stay in the parent (they feed props), not the components.
    - components/ModeSelector.tsx — `'use client'` first line, `interface Props` (never `type`, never `ComponentNameProps`), `export default function ComponentName({...}: Props)`, and `export type FlashcardSubMode`.
    - components/AudioButton.tsx — the `<AudioButton text=... aria-label=... size=... />` calling contract each mode must satisfy verbatim.
    - components/HighlightedSentence.tsx — the `korean`/`targetForm`/`cardType`/`className`/`onWordTap` prop contract used by FlashcardMode.
    - .planning/phases/15-studysession-refactor-sentence-selection-memoization/15-RESEARCH.md — Pitfall 3 (each mode owns its full sticky action-bar wrapper; no slot pattern) and Pitfall 4 (no session state may move into these components); Architectural Responsibility Map.
    - .planning/phases/15-studysession-refactor-sentence-selection-memoization/15-PATTERNS.md — the mode-component Props/`'use client'` guidance.
  </read_first>
  <action>
    First, in `components/StudySession.tsx`, add the `export` keyword to `interface Sentence` and `interface Card` (declarations only — no body change) so the mode components can `import type { Card, Sentence } from './StudySession'`. This is a type-only cycle (erased at build) — safe. Do NOT change any other line of StudySession in this task.

    Then create three `'use client'` client components. Each: `'use client'` as the first line; a single `interface Props { ... }` declared directly above the function; `export default function <Name>({ ... }: Props)`. Each component renders a React fragment containing (1) `<div key={cursor} className="animate-card-in w-full">` wrapping its card face, then (2) its own `<div className="sticky bottom-[calc(4.5rem+var(--sab,0px))] sm:bottom-2 bg-surface-1/95 backdrop-blur-md saturate-150 -mx-4 px-4 pt-2 pb-2 w-[calc(100%+2rem)]">` action bar — copy these wrapper classNames verbatim from the current file (lines 740 and 980) so the DOM structure and animation are byte-identical (REFACTOR-01, RESEARCH Pitfall 3). All Tailwind classes on the moved inner JSX must be copied verbatim.

    `components/FlashcardMode.tsx` — imports: `HighlightedSentence`, `AudioButton`, `useWordTap` from `./GlossProvider`, `import type { Card, Sentence } from './StudySession'`, `import { type FlashcardSubMode } from './ModeSelector'`. It calls `const onWordTap = useWordTap()` internally (do NOT prop-drill it — matches PATTERNS). Props: `card: Card`, `typeBadgeColor: string`, `revealed: boolean`, `cardHeight: number`, `frontRef: React.RefObject<HTMLDivElement | null>`, `backRef: React.RefObject<HTMLDivElement | null>`, `againBtnRef: React.RefObject<HTMLButtonElement | null>`, `cursor: number`, `flashcardSubMode: FlashcardSubMode`, `showBareFront: boolean`, `chosenSentence: Sentence | null`, `displayedSentence: Sentence | null`, `recallBlanked: string | null`, `hasMultipleSentences: boolean`, `hints: { short: string; mastery: string }[] | null`, `onReveal: () => void`, `onGrade: (rating: number) => void`, `onCycleExample: () => void`. The face is the flip container (lines 742-864) with `ref={frontRef}` on the front face div and `ref={backRef}` on the back face div and `style={{ height: cardHeight }}` on `.card-flip-inner`; the "See another example →" button calls `onCycleExample`; the action bar renders "Show Answer" (calls `onReveal`) when `!revealed`, else the four grade buttons (Again/Hard/Good/Easy) with `ref={againBtnRef}` on Again and each calling `onGrade(1|2|3|4)` — preserve every `aria-label`, `hints[n].short`/`hints[n].mastery` usage, and className.

    `components/MultipleChoiceMode.tsx` — imports: `AudioButton`, `import type { Card } from './StudySession'`. Props: `card: Card`, `typeBadgeColor: string`, `cursor: number`, `options: string[]`, `selected: string | null`, `onSelect: (opt: string) => void`, `onAdvance: () => void`. The face is the non-flashcard wrapper `<div className="w-full bg-surface-1 rounded-2xl shadow-md p-8 min-h-[220px] flex flex-col items-center justify-center gap-4">` (copy verbatim from line 868) containing the type badge, the front word + `AudioButton`, and the options grid (lines 880-917) — each option button computes `isCorrect`/`isSelected`/`hasAnswered` locally from `selected`, `card.back`, and the option, keeps the exact `btnClass` logic and the numeric index badge, and calls `onSelect(opt)` on click (the parent owns the auto-advance timer). Show `card.notes` when `selected` is set (lines 918-920). The action bar renders the "Next →" button calling `onAdvance` only when `selected` is set.

    `components/FillBlankMode.tsx` — imports: `AudioButton`, `import type { Card, Sentence } from './StudySession'`. Props: `card: Card`, `typeBadgeColor: string`, `cursor: number`, `fillSentence: string | null`, `fillTranslation: string | null`, `fillAnswer: string`, `fillInput: string`, `fillCorrect: boolean`, `revealed: boolean`, `chosenSentence: Sentence | null`, `hints: { short: string; mastery: string }[] | null`, `onInputChange: (value: string) => void`, `onReveal: () => void`, `onGrade: (rating: number) => void`. The face is the same non-flashcard wrapper `<div className="w-full bg-surface-1 rounded-2xl ...">` containing the type badge and the fill block (lines 926-972): sentence+translation+"Fill in the blank (___)" when `fillSentence !== null` else the "Type the Korean for:" prompt; the text input bound to `fillInput`/`onInputChange`, `disabled={revealed}`, `autoFocus`, with `onKeyDown` calling `onReveal` on Enter when `!revealed`; and the revealed reveal-panel showing `Correct answer: {fillAnswer}` colored by `fillCorrect`, the `chosenSentence` "Hear it" `AudioButton`, and `card.notes`. The action bar renders "Check Answer" (calls `onReveal`) when `!revealed`, else Wrong (`onGrade(1)`) / Correct (`onGrade(3)`) buttons — preserve every `aria-label`/`hints` usage and className.

    Per RESEARCH Pitfall 4: do NOT add any `useState`/`useRef` inside these components for session state (`queue`, `stats`, `seenCount`, `undoRef`, `saveError`, `fillInput`, `mcSelected`, `revealed`, etc.) — they receive derived values + callbacks only. `useWordTap()` in FlashcardMode is the sole hook call permitted.
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` is clean and `npm run build` succeeds with all three new files present.
    - Each new file has `'use client'` as its first line: `head -1 components/FlashcardMode.tsx components/MultipleChoiceMode.tsx components/FillBlankMode.tsx` each shows `'use client'`.
    - Each new file declares `interface Props` and `export default function`: `grep -c "interface Props" components/FlashcardMode.tsx components/MultipleChoiceMode.tsx components/FillBlankMode.tsx` returns 1 for each; same for `grep -c "export default function"`.
    - `components/StudySession.tsx` now exports the shared types: `grep -c "export interface Card\|export interface Sentence" components/StudySession.tsx` returns 2.
    - No session-state hook was introduced in the mode components: `grep -c "useState\|useRef" components/MultipleChoiceMode.tsx components/FillBlankMode.tsx` returns 0 for each, and FlashcardMode's only hook is `useWordTap` (`grep -c "useState\|useRef" components/FlashcardMode.tsx` returns 0).
    - Each mode component renders its own sticky action bar: `grep -c "sticky bottom-" components/FlashcardMode.tsx components/MultipleChoiceMode.tsx components/FillBlankMode.tsx` returns 1 for each.
  </acceptance_criteria>
  <done>Three self-contained mode components exist, each rendering its own card face + sticky action bar with mode-specific buttons and callbacks; StudySession exports the shared `Card`/`Sentence` types; lint + build green. No session state moved into the components.</done>
</task>

<task type="auto">
  <name>Task 2: Slim StudySession to header + progress + three-way mode dispatch</name>
  <files>components/StudySession.tsx</files>
  <read_first>
    - components/StudySession.tsx — the whole file, with focus on the return body (lines 687-1064). Identify what stays in the parent (containerRef + `tabIndex` + `onKeyDown={handleKeyDown}` wrapper at 688-693, the `saveError` Toast at 695-700, the header undo/End block at 701-724, the progress ring at 725-737) and what is replaced (the `<div key={cursor} animate-card-in>` at 740 through the closing action-bar `</div>` at 1062).
    - components/FlashcardMode.tsx, components/MultipleChoiceMode.tsx, components/FillBlankMode.tsx — the Props contracts created in Task 1 (the exact prop names to pass).
    - .planning/phases/15-studysession-refactor-sentence-selection-memoization/15-RESEARCH.md — Pitfall 4 (parent keeps all session state + Phase 13 pipeline; undo-snapshot ordering at line 542 must not move).
  </read_first>
  <action>
    Rewrite ONLY the return body of `components/StudySession.tsx` to dispatch to the Task 1 components (REFACTOR-01). Everything above the `return (` — all `useState`/`useRef`/`useMemo`/`useEffect`/`useLayoutEffect`, `submitReview`, `advanceMc`, `handleUndo`, `handleReveal`, `handleKeyDown`, `postReviewWithRetry`, the derived values (`chosenSentence`, `showBareFront`, `displayedSentence`, `recallBlanked`, `fillSentence`, `fillTranslation`, `fillAnswer`, `fillCorrect`, `mcRating`, `hints`, `typeBadgeColor`), and the measuring `useLayoutEffect` + refs (`frontRef`/`backRef`/`cardHeight`/`againBtnRef`/`containerRef`) — stays exactly as-is (RESEARCH Pitfall 4). Do NOT move the undo-snapshot ordering (the `undoRef.current = {...}` capture BEFORE queue advance, current line 542-543).

    Add a parent handler `handleMcSelect(opt: string)` that reproduces the current inline MC option onClick (lines 901-905): set `mcSelected` to `opt`, then `advanceTimer.current = setTimeout(() => advanceMc(opt === currentCard.back ? 3 : 1), MC_ADVANCE_MS)`. This keeps `advanceTimer`, `MC_ADVANCE_MS`, and `advanceMc` in the parent.

    Replace the region from the `<div key={cursor} className="animate-card-in w-full">` opener through the end of the sticky action-bar `</div>` (current lines 740-1062) with a single three-way dispatch expression inside the still-present outer container `<div ref={containerRef} tabIndex={0} onKeyDown={handleKeyDown} ...>`:
    - `mode === 'flashcard'` → `<FlashcardMode card={currentCard} typeBadgeColor={typeBadgeColor} revealed={revealed} cardHeight={cardHeight} frontRef={frontRef} backRef={backRef} againBtnRef={againBtnRef} cursor={cursor} flashcardSubMode={flashcardSubMode} showBareFront={showBareFront} chosenSentence={chosenSentence} displayedSentence={displayedSentence} recallBlanked={recallBlanked} hasMultipleSentences={cardSentences.length > 1} hints={hints} onReveal={handleReveal} onGrade={submitReview} onCycleExample={() => setExampleOffset((o) => o + 1)} />`
    - `mode === 'multiple-choice'` → `<MultipleChoiceMode card={currentCard} typeBadgeColor={typeBadgeColor} cursor={cursor} options={mcOptions} selected={mcSelected} onSelect={handleMcSelect} onAdvance={() => advanceMc(mcRating)} />`
    - else → `<FillBlankMode card={currentCard} typeBadgeColor={typeBadgeColor} cursor={cursor} fillSentence={fillSentence} fillTranslation={fillTranslation} fillAnswer={fillAnswer} fillInput={fillInput} fillCorrect={fillCorrect} revealed={revealed} chosenSentence={chosenSentence} hints={hints} onInputChange={setFillInput} onReveal={handleReveal} onGrade={submitReview} />`

    Add the three imports at the top: `import FlashcardMode from './FlashcardMode'`, `import MultipleChoiceMode from './MultipleChoiceMode'`, `import FillBlankMode from './FillBlankMode'`. Keep the `HighlightedSentence`/`AudioButton` imports only if still referenced by the parent after the move; if the parent no longer references them, remove those now-unused imports so lint stays clean.

    Preserve the header (undo/End), the `saveError` Toast, and the progress ring exactly where they are, inside the outer container, before the mode dispatch.
  </action>
  <verify>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` clean, `npm test` full suite green (incl. `tests/sentence-selection.test.ts`), `npm run build` succeeds.
    - StudySession dispatches to all three components: `grep -c "<FlashcardMode\|<MultipleChoiceMode\|<FillBlankMode" components/StudySession.tsx` returns 3 (one each).
    - The inline mode JSX is gone from the parent: `grep -c "card-flip-inner" components/StudySession.tsx` returns 0, `grep -c "grid grid-cols-2 gap-3" components/StudySession.tsx` returns 0, and `grep -c "Fill in the blank" components/StudySession.tsx` returns 0.
    - Phase 13 hardening is intact and in the parent: `grep -c "postReviewWithRetry\|isMountedRef\|undoRef.current = {" components/StudySession.tsx` returns 3 or more, and the undo-snapshot capture still precedes the `setQueue(nextQueue)` advance in `submitReview`.
    - Both `useMemo`s remain in the parent: `grep -c "const mcOptions = useMemo\|selectSentence(cardSentences" components/StudySession.tsx` returns 2.
    - No now-unused imports remain (lint enforces this).
  </acceptance_criteria>
  <done>StudySession's return body is header + progress + a three-way mode dispatch; all session state, both memos, and the Phase 13 review/undo pipeline are untouched in the parent; lint/test/build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live UAT — study session behaves identically to before</name>
  <files>components/StudySession.tsx, components/FlashcardMode.tsx, components/MultipleChoiceMode.tsx, components/FillBlankMode.tsx</files>
  <action>
    Pause for human verification of behavior preservation (REFACTOR-01, ROADMAP Success Criterion 4). Because this repo has no component-rendering test infrastructure (`vitest` runs `environment: 'node'`; no `@testing-library/react`/jsdom, and UI component tests are Out of Scope per REQUIREMENTS.md), correctness of the mode split can only be confirmed by a live study session. Ensure `npm run lint`, `npm test`, and `npm run build` are green first, then present the how-to-verify steps below and wait for the resume signal before marking the phase complete.
  </action>
  <what-built>
    REFACTOR-01 is complete in code: `StudySession.tsx` now renders each study mode through `FlashcardMode` / `MultipleChoiceMode` / `FillBlankMode`, with sentence selection extracted + memoized (Plan 01). `npm run lint`, `npm test`, and `npm run build` are all green. Because this repo has no component-rendering test infrastructure (`vitest` runs `environment: 'node'`; no `@testing-library/react`/jsdom — and UI component tests are explicitly Out of Scope per REQUIREMENTS.md), the behavior-preservation guarantee (ROADMAP Success Criterion 4) requires a live study-session UAT.
  </what-built>
  <how-to-verify>
    Run `npm run build && npm start` (production build — RSC first-paint behavior differs from `next dev`), open http://localhost:3000/study, and confirm NO regression versus the previous behavior:
    1. Flashcard mode: start a session, tap "Show Answer" (card flips with dynamic height, no clipping), then grade with each of Again / Hard / Good / Easy (interval hints show under each button); confirm the next card appears instantly (optimistic advance).
    2. Undo: after grading, tap the ↩ Undo control in the header — the previous card reappears with the queue, "Card N of N" counter, and progress ring all restored.
    3. Exposure/Recall toggle: return to /study mode-select, start Flashcards in Recall — confirm the front shows the blanked sentence ("Recall the missing word") and a blank-safe sentence is chosen; then start in Exposure and confirm bare-word-first for new cards and least-unknown sentence otherwise.
    4. Multiple Choice: options grid renders 4 choices, selecting one marks correct/incorrect coloring and auto-advances after ~1.8s; the "Next →" button also advances immediately. Keyboard 1-4 selects an option.
    5. Fill in the Blank: the blanked sentence (or "Type the Korean for:" fallback) shows, typing + Enter (or "Check Answer") reveals the correct answer with pass/fail coloring and the "Hear it" audio; Wrong/Correct buttons grade. Keyboard 1/3 grade.
    6. Audio + tap-to-gloss: an AudioButton plays on a sentence/word; tapping a word in a flashcard sentence opens the gloss popover.
    7. Confirm the save-failure toast still appears if you grade while offline (optional), and that `react-hooks/purity` lint stayed clean.
  </how-to-verify>
  <resume-signal>Type "approved" if all six modes/flows behave identically to before, or describe any regression (which mode, which step, what differed).</resume-signal>
  <verify>
    <human-check>Human confirms flip, grade (all 4 ratings), undo, Exposure/Recall toggle, and mode switching behave identically to the pre-refactor session; lint + full test suite + build already green.</human-check>
  </verify>
  <done>Human types "approved" after confirming no regression across all three modes and the undo/toggle flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (`/api/review`, `/api/review/undo`, `/api/activity`, `/api/settings`, `/api/tts`, `/api/gloss`) | Existing, unchanged — every one of these calls remains in `StudySession.tsx` (the parent) or in already-shipped leaf components (`AudioButton`, `GlossProvider`); this plan moves presentational JSX only and introduces no new fetch, endpoint, or request shape. |
| user input → fill-blank comparison | `fillInput` and its `normalizeAnswer`-based `fillCorrect` comparison move verbatim; `fillCorrect` is computed in the parent and passed down as a boolean prop. No new validation surface (RESEARCH Security Domain, V5 unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-02 | Tampering / Information Disclosure | `components/FlashcardMode.tsx`, `components/MultipleChoiceMode.tsx`, `components/FillBlankMode.tsx`, `components/StudySession.tsx` | low | accept | No new attack surface (RESEARCH Security Domain): a pure structural decomposition — zero new packages, no new API route, no new user-input path, no new external-data ingestion. All data rendered by the mode components originates from already-trusted server-fetched `CardDTO`/`SentenceDTO` shapes the current code already renders, passed down as props. Accepted with no new mitigation required. |
</threat_model>

<verification>
- `npm run lint` clean; `npm test` full suite green (incl. `tests/sentence-selection.test.ts`); `npm run build` succeeds.
- `grep -c "<FlashcardMode\|<MultipleChoiceMode\|<FillBlankMode" components/StudySession.tsx` returns 3.
- Inline per-mode JSX removed from the parent (`card-flip-inner`, `grid grid-cols-2 gap-3`, `Fill in the blank` all absent from `StudySession.tsx`).
- Phase 13 hardening present and unchanged in the parent (`postReviewWithRetry`, `isMountedRef`, `undoRef.current = {` snapshot before queue advance).
- Live UAT (checkpoint task) confirms flip / grade all 4 ratings / undo / Exposure↔Recall / mode switching behave identically to before (REFACTOR-01, Success Criterion 4).
</verification>

<success_criteria>
- REFACTOR-01: `StudySession` renders each mode through a dedicated `FlashcardMode`/`MultipleChoiceMode`/`FillBlankMode` sub-component.
- Behavior preserved: live UAT passes with no regression; lint + full test suite + build green.
- Phase 13 hardening (`postReviewWithRetry`, `isMountedRef`, `saveError`/`Toast`, atomic `handleUndo` ordering) unchanged.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New symbols created by this plan:
- `components/FlashcardMode.tsx` — `export default function FlashcardMode(props: Props)` (flip card, Exposure/Recall front, Show Answer + 4 grade buttons).
- `components/MultipleChoiceMode.tsx` — `export default function MultipleChoiceMode(props: Props)` (options grid, Next button).
- `components/FillBlankMode.tsx` — `export default function FillBlankMode(props: Props)` (fill input, Wrong/Correct grade buttons).
- `components/StudySession.tsx` (modified) — now `export interface Card` / `export interface Sentence`, imports the three mode components, and dispatches on `mode`.

Depends on Plan 01 artifacts: `selectSentence`, `hashStr`, `SelectableSentence` (`lib/sentence-selection.ts`).
</artifacts_produced>

<output>
Create `.planning/phases/15-studysession-refactor-sentence-selection-memoization/15-02-SUMMARY.md` when done.
</output>
