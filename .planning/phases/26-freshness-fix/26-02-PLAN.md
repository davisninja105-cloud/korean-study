---
phase: 26-freshness-fix
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - components/FreshnessWatcher.tsx
  - app/layout.tsx
  - components/HomeClient.tsx
  - components/HabitsClient.tsx
autonomous: true
requirements: [FRESH-04, FRESH-05, FRESH-06]

must_haves:
  truths:
    - "Back/forward navigation to / and resume on / and /habits show fresh data — the corresponding rewritten acceptance tests pass"
    - "/habits back-forward shows fresh data (FreshnessWatcher fetch + direct prop reads)"
    - "smoke.spec.ts and freshness-fresh-paths.spec.ts stay green: no first-load regression, no loading-state flash, no extra fetch on plain-Link navigation"
    - "One boundary event produces at most one router.refresh(); hiding a tab produces none"
  artifacts:
    - path: "components/FreshnessWatcher.tsx"
      provides: "Boundary-refresh client component: visibilitychange (hidden→visible), popstate (deferred a macrotask), pageshow (persisted) → router.refresh(), with a short coalesce cooldown"
      exports: ["default"]
      min_lines: 30
    - path: "app/layout.tsx"
      provides: "FreshnessWatcher mounted once for all routes"
      contains: "FreshnessWatcher"
    - path: "components/HomeClient.tsx"
      provides: "Ungated render-phase adoption of initialStats/initialActivity on prop-reference change"
    - path: "components/HabitsClient.tsx"
      provides: "Direct prop reads for days/goal/masteredCount — read-only useState copies removed"
  key_links:
    - from: "app/layout.tsx"
      to: "components/FreshnessWatcher.tsx"
      via: "JSX mount in the root layout body alongside ThemeWatcher"
      pattern: "<FreshnessWatcher"
    - from: "components/FreshnessWatcher.tsx"
      to: "next/navigation"
      via: "useRouter().refresh() called inside boundary event handlers only"
      pattern: "router\\.refresh\\(\\)"
    - from: "components/HomeClient.tsx"
      to: "app/page.tsx"
      via: "initialStats/initialActivity props re-delivered by router.refresh() and adopted via prev-reference comparison"
      pattern: "prevInitial"
---

<objective>
Introduce the boundary-refresh mechanism — a `FreshnessWatcher` client component mounted once in the root layout that calls `router.refresh()` on back/forward (popstate), tab/PWA resume (visibilitychange hidden→visible), and bfcache restore (pageshow persisted) — and make the two ungated shells (`HomeClient`, `HabitsClient`) adopt the fresh server props those refreshes deliver.

Purpose: This is the half of the fix that guarantees a server round-trip at every real staleness boundary (fixing the 0-fetch Router-Cache-reuse mechanism) plus prop-adoption for the two shells with no in-flight interaction to protect. It flips 4 of the 8 red acceptance tests green: `/ back-forward`, `/ resume`, `/habits resume`, `/habits back-forward`.

Output: `components/FreshnessWatcher.tsx` (new), layout mount, HomeClient adoption, HabitsClient de-state-copying.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26-freshness-fix/26-01-PLAN.md
@app/layout.tsx
@components/HomeClient.tsx
@components/HabitsClient.tsx
@components/ThemeWatcher.tsx
@e2e/helpers/resume.ts
@e2e/freshness-router-cache.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FreshnessWatcher and mount it in the root layout</name>
  <files>components/FreshnessWatcher.tsx, app/layout.tsx</files>
  <read_first>
    - components/ThemeWatcher.tsx (the render-nothing watcher pattern this component mirrors)
    - e2e/helpers/resume.ts (simulateResume dispatches visibilitychange on document and overrides document.visibilityState — the listener must read document.visibilityState and attach to document)
    - app/layout.tsx (mount point, next to ThemeWatcher inside body)
  </read_first>
  <behavior>
    - Backgrounding then resuming a tab (visibilitychange to hidden, then to visible) triggers exactly one router.refresh(); the hidden event alone triggers none — exercised by the 4 resume acceptance tests in e2e/freshness-router-cache.spec.ts
    - Browser back/forward (popstate) triggers one router.refresh() scheduled after Next's own popstate handling — exercised by the back-forward acceptance tests
    - A bfcache restore (pageshow with persisted=true) triggers one router.refresh() — real-device iOS-PWA hardening; not exercised by the harness, harmless under it
    - Rapid event bursts within the cooldown window collapse to one refresh (T-26-02 mitigation)
  </behavior>
  <action>
    Create components/FreshnessWatcher.tsx: a `'use client'` component, default export `FreshnessWatcher`, that renders `null` (mirror ThemeWatcher's shape and file-header comment style). Implementation contract:
    - `const router = useRouter()` from `next/navigation` (NOT next/router).
    - One mount effect registering three listeners, all removed in cleanup:
      a. `document.addEventListener('visibilitychange', ...)` — fire only when `document.visibilityState === 'visible'` at event time (the hidden edge fires nothing). Must attach to `document` and read `document.visibilityState`, matching both real browsers and the harness's simulateResume override.
      b. `window.addEventListener('popstate', ...)` — schedule the refresh via `setTimeout(..., 0)` so Next.js's own popstate handling (Router Cache restore of the target URL) processes first and the refresh re-fetches the RESTORED route, not the departed one. Comment this ordering rationale.
      c. `window.addEventListener('pageshow', ...)` — fire only when the event's `persisted` flag is true (full bfcache restore on real devices, chiefly iOS Safari PWA; the Playwright harness never takes this path).
    - All three funnel through one internal `refresh()` closure holding a coalesce guard: a `useRef<number>(0)` timestamp; skip when less than 300ms since the last fired refresh; otherwise update the ref and call `router.refresh()`. `Date.now()` is read ONLY inside event handlers (react-hooks/purity forbids it in render). No state, no ref writes during render.
    - The component must not process any user/event-derived data into the DOM and must not use dangerouslySetInnerHTML (T-26-03).
    In app/layout.tsx: import FreshnessWatcher and mount `<FreshnessWatcher />` immediately after `<ThemeWatcher />` inside body. No other layout changes. Do NOT modify next.config.ts or any app/*/page.tsx (server pages are already force-dynamic; cache-time tuning is out of scope per REQUIREMENTS.md).
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit 2>&1 | grep -v 'tests/study-cards.test.ts' | grep -c 'error TS' | grep -qx '0' && test $(grep -c 'FreshnessWatcher' app/layout.tsx) -ge 2 && grep -q "'use client'" components/FreshnessWatcher.tsx && grep -qc 'router.refresh()' components/FreshnessWatcher.tsx</automated>
  </verify>
  <acceptance_criteria>
    - components/FreshnessWatcher.tsx exists, is a client component rendering null, and registers/removes visibilitychange + popstate + pageshow listeners in one effect
    - Refresh fires only on hidden→visible, popstate (macrotask-deferred), and persisted pageshow; a coalesce ref suppresses bursts within ~300ms
    - app/layout.tsx mounts the watcher once (import + JSX = grep count ≥ 2); no other files touched by this task
    - lint and tsc (excluding the pre-existing tests/study-cards.test.ts debt) are clean
  </acceptance_criteria>
  <done>Every route now receives a real RSC re-fetch at back/forward, resume, and bfcache-restore boundaries; no shell adopts it yet except via remount.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HomeClient ungated adoption + HabitsClient direct prop reads</name>
  <files>components/HomeClient.tsx, components/HabitsClient.tsx</files>
  <read_first>
    - components/HomeClient.tsx (setStats/setActivityData already exist for pull-to-refresh; the heroState/today effect derives from [stats, activityData] and needs no change)
    - components/HabitsClient.tsx (days/goal/masteredCount are read-only useState copies; today stays client state; the HabitsLoading gate on empty today must be preserved)
    - .planning/phases/26-freshness-fix/26-01-PLAN.md design_decisions section (gating decisions 4a/4b bind this task)
  </read_first>
  <behavior>
    - `/ back-forward serves fresh data after boundary refresh (FRESH-04)` passes: HomeClient remounted from the cached payload adopts the refresh-delivered props
    - `/ resume serves fresh data after boundary refresh (FRESH-05)` and `/habits resume serves fresh data after boundary refresh (FRESH-05)` pass: mounted shells adopt new props delivered to the same instance
    - `/habits back-forward serves fresh data after boundary refresh (FRESH-04)` passes
    - `/study resume` and `/cards resume` acceptance tests remain red (their shells gain adoption in Plan 26-03)
    - smoke + fresh-paths stay fully green (no flash, no extra fetch on plain-Link navigation)
  </behavior>
  <action>
    HomeClient (per design decision 4a — UNGATED): add render-phase prop adoption using the official React "adjusting state when props change" pattern. Hold the previously-seen prop reference in its own useState (name them prevInitialStats / prevInitialActivity); during render, when the incoming prop reference differs, update the prev-holder and call the existing setter (setStats / setActivityData) with the new prop. Two independent blocks, one per prop. Add a brief comment naming the pattern and why no gate exists (no clobberable in-flight interaction; pull-to-refresh's own refetches deliver equivalent truth). Do not touch the pull-to-refresh path, checkBandUp, or the heroState effect — they recompute automatically from the adopted state and existing dependency arrays.

    HabitsClient (per design decision 4b): delete the three read-only useState copies and read props directly — replace them with plain consts of the same names (days from initialDays, goal from initialGoal, masteredCount from initialMasteredCount) so all downstream JSX and useMemo bodies are unchanged; the useMemo dependency arrays keep the same identifiers and now track the props. `today` remains client state and the one-tick HabitsLoading gate on an empty today string stays exactly as is (no new loading state, no flash). Add a brief comment: read-only prop→state copies were the non-resync bug (24-DIAGNOSIS.md); direct prop reads make every router.refresh() adopt automatically.

    Lint fallback (both shells, only if needed): if the eslint react-hooks rules in this repo reject the guarded setState-during-render pattern, fall back to a useEffect keyed on the prop reference that consumes each payload exactly once via a lastAdopted ref and defers the setter with the codebase's established microtask pattern (the Promise-resolve-then shape already used in HomeClient/HabitsClient effects). Record whichever pattern shipped in the SUMMARY — Plan 26-03 must use the same one.

    After both edits: kill any stale port-3100 server (a reused server would serve pre-edit code), then run the targeted acceptance subset and the regression net.
  </action>
  <verify>
    <automated>npm run lint && (lsof -ti:3100 | xargs kill -9 2>/dev/null; npx playwright test e2e/freshness-router-cache.spec.ts e2e/freshness-client-shell.spec.ts --grep "(/ back-forward serves)|(/ resume serves)|(/habits resume serves)|(/habits back-forward serves)" --reporter=line) && npx playwright test e2e/freshness-fresh-paths.spec.ts e2e/smoke.spec.ts --reporter=line && ! npx playwright test e2e/freshness-router-cache.spec.ts --grep "(/study resume serves)|(/cards resume serves)" --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - The 4 targeted acceptance tests (/ back-forward, / resume, /habits resume, /habits back-forward) pass
    - fresh-paths 7/7 and smoke 4/4 still pass — plain-Link cells still capture exactly one fetch (their toHaveLength(1) assertions prove the watcher adds no fetch to Link navigation), and first-paint content assertions prove no flash was introduced (FRESH-06)
    - /study resume and /cards resume remain red (expected until Plan 26-03)
    - HabitsClient contains no useState wrapping initialDays/initialGoal/initialMasteredCount; HomeClient adoption fires only on prop-reference change
  </acceptance_criteria>
  <done>Home and Habits are fresh at every boundary; the watcher is proven to fire exactly at boundaries and never on plain-Link or first load.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser events → server fetch | popstate/visibilitychange/pageshow now trigger authenticated RSC re-fetches app-wide via FreshnessWatcher |
| client → RSC endpoint | Every refresh fetch crosses middleware.ts's ks_auth cookie gate (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Information Disclosure | router.refresh() on resume (FreshnessWatcher) | low | accept | Refresh re-fetches only data the authenticated session already sees; middleware still gates every RSC request; on a shared device the exposure is identical to a manual reload; single-user app with a single shared password by design |
| T-26-02 | Denial of Service | event-driven refresh triggers | low | mitigate | Visible-transition-only + persisted-only + macrotask-deferred popstate, funneled through a 300ms coalesce ref — bursts collapse to one refresh; each refresh is one bounded RSC render (equivalent cost to one navigation) |
| T-26-03 | Tampering (injection surface) | FreshnessWatcher listeners | low | mitigate | Renders null, processes no event-derived data into the DOM, no dangerouslySetInnerHTML; handlers call router.refresh() with no arguments derived from event payloads |
| T-26-SC | Tampering (supply chain) | npm installs | low | accept | No new dependencies; no package-install tasks in this plan |

Spoofing/Repudiation/Elevation: not applicable — no new endpoints, auth paths, or privilege logic.
</threat_model>

<verification>
- Targeted green subset: / back-forward, / resume, /habits resume, /habits back-forward pass
- Regression net: fresh-paths 7/7 + smoke 4/4 pass (FRESH-06: no flash, plain-Link fetch count still exactly 1)
- Expected-red subset: /study resume, /cards resume still fail (adoption lands in 26-03)
- lint + tsc clean; only the 4 declared files modified
</verification>

<success_criteria>
- FreshnessWatcher fires router.refresh() at exactly the three boundary types and nowhere else
- HomeClient and HabitsClient reflect fresh server data at every boundary without any gate (none needed)
- Zero regression across the 7 confirmed-fresh cells and 4 smoke first-paint assertions
</success_criteria>

<artifacts_produced>
- `components/FreshnessWatcher.tsx` — new client component, default export `FreshnessWatcher`; listeners: visibilitychange (document), popstate (window, setTimeout-0 deferred), pageshow persisted (window); 300ms coalesce ref
- `app/layout.tsx` — `<FreshnessWatcher />` mounted after `<ThemeWatcher />`
- `components/HomeClient.tsx` — prevInitialStats/prevInitialActivity render-phase adoption blocks
- `components/HabitsClient.tsx` — days/goal/masteredCount converted from useState copies to direct prop reads
</artifacts_produced>

<output>
Create `.planning/phases/26-freshness-fix/26-02-SUMMARY.md` when done, recording which adoption pattern shipped (render-phase vs effect fallback) — Plan 26-03 must match it.
</output>
