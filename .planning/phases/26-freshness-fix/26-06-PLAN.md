---
phase: 26-freshness-fix
plan: 06
type: execute
wave: 5
depends_on: ["26-04", "26-05"]
files_modified: []
autonomous: true
gap_closure: true
requirements: [FRESH-02, FRESH-04, FRESH-05]

must_haves:
  truths:
    - "Resuming the app after it was backgrounded (tab/PWA focus) refreshes stale data, including the case where the daily cron sync ran while the app was backgrounded (FRESH-05) — proven by repeated runs, not a single lucky green"
    - "Browser back/forward navigation to a previously-visited route shows fresh data, not a stale cached RSC payload (FRESH-04)"
    - "Prop re-sync never clobbers an in-flight interaction — an active study session or an open editor sheet is preserved mid-interaction (FRESH-02) — proven by the new automated gate cells running inside the combined sweep"
    - "First-load render speed is unchanged and no loading-state flash reappears on any of the four main routes (FRESH-06 regression guard stays green under the merged changes)"
  artifacts:
    - path: ".planning/phases/26-freshness-fix/26-06-SUMMARY.md"
      provides: "The formal acceptance record: per-run tables for 2 combined sweeps + 5x isolated /study resume + 5x isolated /study back-forward + 5x isolated /habits resume, in the same methodology as 26-VERIFICATION.md's Behavioral Spot-Checks table"
  key_links:
    - from: "the combined sweep"
      to: "e2e/freshness-gate.spec.ts"
      via: "the new FRESH-02 cells (Plan 26-04) run inside the same sweep as the FRESH-04/05 cells, post-merge with Plan 26-05's backstop"
      pattern: "freshness-gate"
---

<objective>
Run the formal, phase-level acceptance protocol the locked user decision mandates — mirroring 26-VERIFICATION.md's and deferred-items.md's exact methodology so the next verification pass has an apples-to-apples comparison. This runs post-merge of both wave-4 plans, so the combined sweep includes Plan 26-04's new FRESH-02 gate cells exercising Plan 26-05's backstop delivery.

This plan modifies zero source files. Its sole output is evidence. HONESTY RULE (locked decision, non-negotiable): if the flake persists in any run, that is a finding for the next verification pass to surface honestly — do NOT weaken assertions, add retries/annotations, re-tune timeouts, insert an accepted-risk override, or re-run until lucky and report only the green runs. Every executed run appears in the SUMMARY table, pass or fail.

Purpose: FRESH-02/04/05 gap-closure acceptance evidence at the reliability bar 26-VERIFICATION.md measured against.
Output: 26-06-SUMMARY.md with complete per-run tables.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26-freshness-fix/26-VERIFICATION.md
@.planning/phases/26-freshness-fix/26-04-PLAN.md
@.planning/phases/26-freshness-fix/26-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Combined acceptance sweep ×2 (all four freshness spec files)</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - .planning/phases/26-freshness-fix/26-VERIFICATION.md (Behavioral Spot-Checks table — the sweep command and reporting format to mirror; its run 1 was 14/17, run 2 was 16/17)
    - .planning/phases/26-freshness-fix/26-05-SUMMARY.md (Task 3's per-run results — if that task reported failures, this plan documents the discrepancy rather than hiding it)
  </read_first>
  <action>
    First kill any stale port-3100 server (`lsof -ti:3100 | xargs kill -9`) so the webServer rebuilds from the merged wave-4 code — a reused pre-merge server would invalidate every result below.

    Run the combined sweep TWICE, capturing full line-reporter output each time:
    `npx playwright test e2e/freshness-router-cache.spec.ts e2e/freshness-client-shell.spec.ts e2e/freshness-fresh-paths.spec.ts e2e/freshness-gate.spec.ts --reporter=line`

    This is 26-VERIFICATION.md's exact 3-file sweep plus Plan 26-04's freshness-gate.spec.ts (2 new FRESH-02 cells) — 18+ cells total. Required: every cell green in BOTH runs. Record each run's pass/fail count and any failing cell names verbatim in the SUMMARY, formatted like 26-03-SUMMARY's observed-sweep-results table with a baseline column citing 26-VERIFICATION.md's runs (14/17 and 16/17).
  </action>
  <verify>
    <automated>lsof -ti:3100 | xargs kill -9 2>/dev/null; npx playwright test e2e/freshness-router-cache.spec.ts e2e/freshness-client-shell.spec.ts e2e/freshness-fresh-paths.spec.ts e2e/freshness-gate.spec.ts --reporter=line && npx playwright test e2e/freshness-router-cache.spec.ts e2e/freshness-client-shell.spec.ts e2e/freshness-fresh-paths.spec.ts e2e/freshness-gate.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Two complete combined sweeps executed against a freshly-built post-merge server; both fully green (including both freshness-gate FRESH-02 cells)
    - Both runs' results recorded in the SUMMARY table with the 26-VERIFICATION.md baseline for comparison
    - If either run has any failure: the failing cells are named verbatim in the SUMMARY, no third make-up run is substituted for a failed one, and the plan is reported blocked — no override is written anywhere
  </acceptance_criteria>
  <done>The combined sweep — the same bar on which the phase failed verification — is green twice consecutively, now including the FRESH-02 gate cells.</done>
</task>

<task type="auto">
  <name>Task 2: Isolated repeated runs of the flaky cells + full regression net</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - .planning/phases/26-freshness-fix/26-VERIFICATION.md (isolated-run baselines: /study resume 0/3 isolated + 0/2 in sweeps = 0/7 total; /habits resume 1/2; /study back-forward 2/2 isolated but failing under combined load)
  </read_first>
  <action>
    Run each cell in an explicit counted loop (server stays warm via reuseExistingServer — do NOT kill it between iterations; it was freshly built in Task 1):

    1. `/study resume` ×5: loop `npx playwright test e2e/freshness-router-cache.spec.ts --grep "/study resume serves" --reporter=line` five times, counting passes. Required: 5/5 (baseline 0/7).
    2. `/study back-forward` ×5: loop `npx playwright test e2e/freshness-client-shell.spec.ts --grep "/study back-forward serves" --reporter=line` five times. Required: 5/5.
    3. `/habits resume` ×5: loop `npx playwright test e2e/freshness-router-cache.spec.ts --grep "/habits resume serves" --reporter=line` five times. Required: 5/5 (baseline ~50%).
    4. Regression net: `npx playwright test e2e/smoke.spec.ts --reporter=line` (5/5 — FRESH-06 first-load guard), `npm run lint` (0 errors), filtered `npx tsc --noEmit` (0 errors excluding the pre-existing tests/study-cards.test.ts debt), `npm test` (241/241).

    Record every individual run in the SUMMARY per-run table (cell × run number × pass/fail), then a final requirements-status line for FRESH-02/04/05 mapping each to its evidence. Same honesty rule as Task 1: failures are recorded verbatim and the plan reports blocked; no retries beyond the counted loops, no assertion or timeout changes, no overrides.
  </action>
  <verify>
    <automated>P=0; for i in 1 2 3 4 5; do npx playwright test e2e/freshness-router-cache.spec.ts --grep "/study resume serves" --reporter=line && P=$((P+1)); done; echo "study-resume $P/5"; test "$P" -eq 5 && npx playwright test e2e/smoke.spec.ts --reporter=line && npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - /study resume 5/5, /study back-forward 5/5, /habits resume 5/5 — all recorded per-run in the SUMMARY with baselines
    - smoke 5/5, lint 0 errors, filtered tsc 0 errors, vitest 241/241
    - git status shows zero source/spec/e2e modifications from this plan (evidence-only)
    - SUMMARY includes the FRESH-02/FRESH-04/FRESH-05 → evidence mapping so the next verification pass can score the phase directly from it
  </acceptance_criteria>
  <done>The gap-closure work is proven at (and beyond) the exact reliability bar 26-VERIFICATION.md measured, with a complete honest run record either way.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan: runs existing test suites against the isolated e2e SQLite DB; no production code, endpoints, or data touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-11 | Repudiation | acceptance evidence integrity | low | mitigate | Every executed run (pass or fail) is recorded in the SUMMARY table; the honesty rule forbids selective reporting, make-up runs, or silent overrides — the record is the mitigation |
| T-26-SC | Tampering (supply chain) | npm installs | low | accept | No new dependencies; no package-install tasks in this plan |

Spoofing/Information Disclosure/DoS/Elevation: not applicable — no code changes.
</threat_model>

<verification>
- 2× combined 4-file sweep fully green against a fresh post-merge build
- 5/5 × 3 isolated flaky-cell loops green; smoke/lint/tsc/vitest clean
- SUMMARY contains the complete per-run tables + requirement-to-evidence mapping
</verification>

<success_criteria>
- The locked decision's acceptance bar is met exactly as specified: combined sweep at least twice + the specific /study resume and /study back-forward cells in isolation at least 5 times each
- Evidence methodology matches 26-VERIFICATION.md so re-verification is apples-to-apples
- Any residual flake is surfaced honestly, never papered over
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `.planning/phases/26-freshness-fix/26-06-SUMMARY.md` — formal acceptance record (per-run tables, baselines, requirement-to-evidence mapping)
- No code, spec, or config artifacts — verification-only plan
</artifacts_produced>

<output>
Create `.planning/phases/26-freshness-fix/26-06-SUMMARY.md` when done, containing: the 2 combined-sweep result tables (with 26-VERIFICATION.md baseline column), the 3 isolated-cell per-run tables, the regression-net results, and the FRESH-02/04/05 evidence mapping.
</output>
