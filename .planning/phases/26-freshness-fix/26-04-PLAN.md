---
phase: 26-freshness-fix
plan: 04
type: execute
wave: 4
depends_on: ["26-03"]
files_modified:
  - e2e/freshness-gate.spec.ts
autonomous: true
gap_closure: true
requirements: [FRESH-02]

must_haves:
  truths:
    - "Prop re-sync never clobbers an in-flight interaction — an active study session or an open editor sheet is preserved mid-interaction (FRESH-02), and this is now proven by automated e2e cells, not human inspection"
    - "A boundary-refresh event fired mid-study-session leaves the active card, revealed state, and session progress visibly unchanged"
    - "A boundary-refresh event fired while the CardEditor sheet is open neither closes the sheet, wipes typed edits, nor overwrites the card list with the boundary payload — and after save/close the optimistic list survives (no deferred late adoption)"
  artifacts:
    - path: "e2e/freshness-gate.spec.ts"
      provides: "Two automated FRESH-02 gate cells: '/study mid-session' and '/cards open-sheet' boundary non-clobber, using the same resetToBaseline/simulateResume/waitForRscFetch harness as the existing freshness specs"
      min_lines: 60
  key_links:
    - from: "e2e/freshness-gate.spec.ts"
      to: "e2e/helpers/resume.ts"
      via: "simulateResume(page, true/false) — the identical boundary trigger the existing freshness specs use"
      pattern: "simulateResume"
    - from: "e2e/freshness-gate.spec.ts"
      to: "e2e/helpers/rsc.ts"
      via: "waitForRscFetch(page, route) awaited before the negative-assertion settle window, so 'nothing changed' is proven AFTER delivery provably occurred"
      pattern: "waitForRscFetch"
---

<objective>
Close 26-VERIFICATION.md's `behavior_unverified` gap: FRESH-02's clobber-prevention gates (StudyClient's `phase === 'select-mode' && !isFilterLoading && isFullSpan(...)`; CardsClient's `editingId === null && !showAdd && !adding && deletingIds.size === 0`) read correctly by inspection but have zero automated e2e coverage — VERIFICATION.md routed them to human verification for exactly this reason. This plan adds the two concrete scenarios named in VERIFICATION.md's `behavior_unverified_items`/`human_verification` blocks as automated Playwright cells in a NEW spec file, so FRESH-02 no longer needs a human to check by hand.

This is a normal test-writing task, not a research problem. The new cells must pass against the CURRENT shipped code (the gates landed in 26-03 and are structurally verified) — they encode the existing contract, they do not wait on Plan 26-05's delivery fix. A new file (rather than edits to freshness-client-shell.spec.ts) keeps the three existing acceptance spec files byte-identical, preserving the apples-to-apples comparison baseline that Plan 26-06's formal re-run requires.

Purpose: FRESH-02 gap closure — automated, repeatable proof that boundary refreshes never clobber in-flight interactions.
Output: `e2e/freshness-gate.spec.ts` with 2 test cells, both green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26-freshness-fix/26-VERIFICATION.md
@e2e/freshness-router-cache.spec.ts
@e2e/freshness-client-shell.spec.ts
@e2e/fixture.ts
@e2e/helpers/resume.ts
@e2e/helpers/rsc.ts
@e2e/helpers/mutate.ts
@e2e/helpers/readers.ts
@components/StudyClient.tsx
@components/CardsClient.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: /study mid-session boundary non-clobber cell (FRESH-02)</name>
  <files>e2e/freshness-gate.spec.ts</files>
  <read_first>
    - e2e/freshness-router-cache.spec.ts (the resume-cell trigger sequence to mirror exactly: mutate → create waitForRscFetch promise → simulateResume(true) → 150ms → simulateResume(false) → await promise → settle; also the file-header discipline: plain expect() only, never test.fail/fixme/skip)
    - e2e/helpers/resume.ts, e2e/helpers/rsc.ts (simulateResume + waitForRscFetch contracts — the promise MUST be created before the trigger)
    - e2e/helpers/mutate.ts (flipOneReviewDueState — subprocess-delegating mutator; call it directly from the test body like the existing specs do)
    - e2e/fixture.ts (FIXTURE constants: 3 due cards — the select-mode count and 'Card 1 of 3' progress label derive from this)
    - components/StudyClient.tsx (select-mode UI: 'Start studying →' button opens the mode Sheet containing ModeSelector; the gate block at ~line 134-141 is the code under test)
    - components/ModeSelector.tsx (mode buttons inside the Sheet — the 'Flashcards' button starts the session; its accessible name includes the label + desc text, so use a regex name match)
    - components/StudySession.tsx (progress label shape: `Card {n} of {initialCount}`; queue is a lazy useState initializer — prop changes do not reset it, which is why the assertions below target visible session continuity: card face, revealed state, progress label, and absence of the select-mode screen)
    - components/FlashcardMode.tsx (the 'Show Answer' action button at ~line 206-209; the Again/Hard/Good/Easy grade buttons at ~line 215+ with aria-labels starting 'Again'/'Hard' — pick concrete, stable locators from the real JSX, e.g. getByRole('button', { name: /^Again/ }))
  </read_first>
  <action>
    Create e2e/freshness-gate.spec.ts. File header comment: these are the FRESH-02 gate cells from 26-VERIFICATION.md's behavior_unverified_items — they encode the discard-when-gated contract shipped in Plan 26-03 and MUST pass against that code as-is (they do not depend on Plan 26-05's delivery backstop; they must stay green after it too). Same discipline as the sibling freshness specs: plain expect()/expect.poll() only, no expected-failure or skip annotations, `test.beforeEach(resetToBaseline)`.

    Test 1 title: `/study mid-session boundary refresh never clobbers the active session (FRESH-02)`. Flow:
    1. `page.goto('/study')` + networkidle. Select-mode shows the due count (FIXTURE.dueCards = 3).
    2. Start a real session: click the 'Start studying →' button, then inside the opened Sheet click the Flashcards mode button (role button, name matching /Flashcards/). Wait for the flashcard face: the 'Show Answer' button visible.
    3. Capture the session-progress label text (the `Card 1 of 3`-shaped paragraph — locate via getByText(/^Card \d+ of \d+$/)) and capture the visible card-face text (pick a concrete container locator from FlashcardMode.tsx during implementation; fall back to capturing the full text of the card region and re-asserting it verbatim later).
    4. Click 'Show Answer' — assert the grade bar appeared (button with accessible name starting 'Again' visible). `revealed` is client-only state: any remount/reset/clobber flips the card back to its front face, making this the sharpest observable.
    5. Mutate the DB mid-session: `await flipOneReviewDueState()` — the due pool a wrongly-adopted payload would carry now differs from the in-session queue.
    6. Create `const rscFetch = waitForRscFetch(page, '/study')` BEFORE the trigger, then `simulateResume(page, true)` → 150ms → `simulateResume(page, false)` → `await rscFetch` → `await page.waitForTimeout(800)`. The 800ms settle (wider than the sibling specs' 300ms) deliberately gives Plan 26-05's JSON backstop response time to land post-merge, so the negative assertions below hold in both pre- and post-26-05 worlds — delivery provably occurred (rscFetch resolved) and was provably discarded.
    7. Assert non-clobber, all AFTER the settle: (a) the Again grade button is still visible (revealed state survived — no remount/reset); (b) the card-face text equals the step-3 capture (same card, no reorder/jump); (c) the progress label equals the step-3 capture; (d) the select-mode screen did NOT return — expect the 'Start studying →' button to have count 0 and the select-mode count locator (`p.text-5xl.font-bold.animate-reveal`, same locator readStudySelectModeState uses) to have count 0.

    Do not modify any existing spec file, helper, or component. Do not add test.fail/fixme/skip anywhere.
  </action>
  <verify>
    <automated>npm run lint && npx playwright test e2e/freshness-gate.spec.ts --grep "mid-session" --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - e2e/freshness-gate.spec.ts exists with the mid-session cell; `npx playwright test e2e/freshness-gate.spec.ts --grep "mid-session" --reporter=line` passes against current (pre-26-05) code
    - The waitForRscFetch promise is created before simulateResume fires (grep order in file: `waitForRscFetch` appears before `simulateResume(page, true)` within the test body)
    - The test asserts all four non-clobber conditions (grade bar visible, card face unchanged, progress label unchanged, select-mode absent) after both `await rscFetch` and a settle wait — never before
    - Zero occurrences of expected-failure/skip annotations in the new file
    - No existing file modified (git status shows only the new spec file)
  </acceptance_criteria>
  <done>An automated cell drives a real mid-session boundary event and proves the delivered payload is discarded while the StudyClient gate is closed — the first of VERIFICATION.md's two human_verification items is automated.</done>
</task>

<task type="auto">
  <name>Task 2: /cards open-sheet boundary non-clobber cell (FRESH-02)</name>
  <files>e2e/freshness-gate.spec.ts</files>
  <read_first>
    - components/CardsClient.tsx (the gate block at ~line 76-82 is the code under test; the 'Cards (N)' view-toggle button is the live list-count observable — it derives from `cards` state during render, so a broken gate visibly changes it; the Edit button per card row opens the CardEditor Sheet)
    - components/CardEditor.tsx (front input at ~line 111 `value={front}`; Save/Cancel buttons at ~line 232-239 — confirm the Save button's exact accessible name from the JSX; handleSave PUTs /api/cards/[id] then calls onSave which optimistically merges into the list and closes the sheet)
    - e2e/helpers/readers.ts (readCardsCount — role-based reader for the 'Cards (N)' label; reusable here because the Sheet is an overlay and the underlying toggle stays in the DOM)
    - e2e/helpers/mutate.ts (createMutationCard — adds one card server-side, so live DB count becomes FIXTURE.totalCards + 1 while the gated client must keep showing FIXTURE.totalCards)
    - e2e/fixture.ts (FIXTURE.totalCards = 8 — build expected strings from this constant, never hardcode 8/9 as bare literals without the FIXTURE reference)
  </read_first>
  <action>
    Append the second cell to e2e/freshness-gate.spec.ts. Test title: `/cards open-sheet boundary refresh never clobbers in-flight edits (FRESH-02)`. Flow:
    1. `page.goto('/cards')` + networkidle. Assert `readCardsCount(page)` returns `Cards (${FIXTURE.totalCards})`.
    2. Open the edit sheet: click the first 'Edit' button (getByRole('button', { name: 'Edit' }).first()). Wait for the sheet dialog (role dialog) visible.
    3. Type an in-progress edit: append a distinctive Hangul marker (e.g. the string 수정됨) to the card-front input inside the dialog (locate the input whose current value is a fixture card front; CardEditor renders it as the first text input in the sheet). Capture the resulting input value.
    4. Mutate server-side while the sheet is open: `await createMutationCard()` — live DB truth is now `Cards (${FIXTURE.totalCards + 1})`.
    5. Boundary trigger with delivery evidence: `const rscFetch = waitForRscFetch(page, '/cards')` created BEFORE the trigger; `simulateResume(page, true)` → 150ms → `simulateResume(page, false)` → `await rscFetch` → `await page.waitForTimeout(800)` (same both-worlds settle rationale as Task 1).
    6. Assert while the sheet is still open: (a) the dialog is still visible; (b) the front input still contains the typed marker value from step 3; (c) `readCardsCount(page)` STILL returns `Cards (${FIXTURE.totalCards})` — the gated client did not adopt the (provably delivered) post-mutation payload.
    7. Save and close: click the Save button, wait for the dialog to be hidden (the PUT round-trip completes before onSave closes it).
    8. Assert post-close, after a further short settle (300ms): (a) the edited front (with the marker) is visible in the card list — the optimistic local merge survived; (b) `readCardsCount(page)` STILL returns `Cards (${FIXTURE.totalCards})` — the payload consumed while gated is never late-adopted after the gate reopens (consume-always/adopt-conditionally contract), so no stale pre-mutation snapshot and no deferred adoption overwrites the user's edit.

    Note for the executor: step 8(b) is the sharpest assertion in this plan — CardsClient's documented contract (see its gate-block comment) is that a payload arriving while a sheet is open is consumed but never deferred-adopted, so the count must remain at the fixture value until the NEXT boundary event, which this test deliberately never fires.
  </action>
  <verify>
    <automated>npm run lint && npx playwright test e2e/freshness-gate.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/freshness-gate.spec.ts --reporter=line` passes 2/2 against current (pre-26-05) code
    - The open-sheet cell asserts all three sheet-open conditions (dialog visible, typed marker intact, count unchanged) AND both post-close conditions (edited front visible, count still at fixture value)
    - Expected count strings are built from FIXTURE.totalCards, not free-standing numeric literals
    - npm run lint passes with 0 errors (only the pre-existing StudySession.tsx warning permitted)
    - Only e2e/freshness-gate.spec.ts was created/modified in this plan (git diff --stat confirms)
  </acceptance_criteria>
  <done>Both VERIFICATION.md human_verification scenarios are automated and green; FRESH-02's runtime non-clobber guarantee is behaviorally exercised on every future suite run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → isolated SQLite DB | All mutations run against TEST_DB_URL only (playwright.config.ts pins it; build-guard rejects any libsql:// URL) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-07 | Tampering | e2e mutators against production data | low | mitigate | Existing Phase 25 controls unchanged: assertLocalDb fail-fast on any libsql:// URL, TEST_DB_URL pinned in webServer env, DATABASE_AUTH_TOKEN blanked — this plan adds no new DB access path, only new callers of existing mutators |
| T-26-SC | Tampering (supply chain) | npm installs | low | accept | No new dependencies; no package-install tasks in this plan |

Spoofing/Repudiation/Information Disclosure/DoS/Elevation: not applicable — test-only plan, no production code or endpoints touched.
</threat_model>

<verification>
- `npx playwright test e2e/freshness-gate.spec.ts --reporter=line` → 2/2 pass against pre-26-05 code
- Both cells await waitForRscFetch (delivery evidence) before asserting non-change — no vacuous negative assertions
- Existing spec files, helpers, and components untouched (git diff --stat shows only the new file)
- npm run lint clean
</verification>

<success_criteria>
- FRESH-02's two behavior_unverified scenarios from 26-VERIFICATION.md are covered by automated, deterministic e2e cells
- The cells are trigger-mechanism-agnostic: they stay green before and after Plan 26-05's JSON backstop lands (delivery is awaited, adoption is asserted absent)
- Plan 26-06's combined sweep can include this file for the full phase acceptance proof
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `e2e/freshness-gate.spec.ts` — NEW spec file, 2 test cells:
  - `/study mid-session boundary refresh never clobbers the active session (FRESH-02)`
  - `/cards open-sheet boundary refresh never clobbers in-flight edits (FRESH-02)`
- No new helpers, no new exports elsewhere — reuses `simulateResume`, `waitForRscFetch`, `readCardsCount`, `flipOneReviewDueState`, `createMutationCard`, `resetToBaseline`, `FIXTURE`
</artifacts_produced>

<output>
Create `.planning/phases/26-freshness-fix/26-04-SUMMARY.md` when done, recording the concrete locators chosen for the card face and CardEditor front input (Plan 26-06 re-runs this file in its combined sweep).
</output>
