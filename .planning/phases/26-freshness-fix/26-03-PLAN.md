---
phase: 26-freshness-fix
plan: 03
type: execute
wave: 3
depends_on: ["26-01", "26-02"]
files_modified:
  - components/StudyClient.tsx
  - components/CardsClient.tsx
autonomous: true
requirements: [FRESH-02, FRESH-03, FRESH-04, FRESH-05, FRESH-06]

must_haves:
  truths:
    - "All 9 previously-red freshness cells pass and all 7 fresh-paths cells still pass — the full 16-cell matrix is green with zero expected-failure/skip masking"
    - "A mid-session study queue is never replaced by a boundary refresh: adoption is gated to select-mode with no filter fetch in flight"
    - "An open CardEditor or Add-Card sheet is never clobbered: gated payloads are discarded, never deferred-adopted"
    - "A lesson-range-filtered study view never adopts the unfiltered server pool"
    - "First-load render is unchanged: smoke stays green and no loading state was added to any shell"
  artifacts:
    - path: "components/StudyClient.tsx"
      provides: "Gated adoption of initialCards (select-mode AND not filter-loading AND full-span), discard-when-gated"
      contains: "prevInitialCards"
    - path: "components/CardsClient.tsx"
      provides: "Gated adoption of initialCards (no sheet open, no add/delete in flight), discard-when-gated"
      contains: "prevInitialCards"
  key_links:
    - from: "components/StudyClient.tsx"
      to: "components/LessonRangeFilter.tsx"
      via: "isFullSpan() used inside the adoption gate"
      pattern: "isFullSpan\\("
    - from: "components/StudyClient.tsx"
      to: "app/study/page.tsx"
      via: "initialCards re-delivered by router.refresh() and adopted only when the gate is open"
      pattern: "prevInitialCards"
    - from: "components/CardsClient.tsx"
      to: "app/cards/page.tsx"
      via: "initialCards re-delivered by router.refresh() and adopted only when no sheet/mutation is active"
      pattern: "prevInitialCards"
---

<objective>
Add gated prop re-sync to the two interaction-heavy shells (`StudyClient`, `CardsClient`) — the FRESH-02 core — then prove the whole phase: all 16 freshness-matrix tests green, smoke green, lint/types/unit tests clean.

Purpose: These are the shells where naive adoption would clobber real user state (an in-progress study session; an open editor sheet). The gates implement design decisions 3 and 4c/4d from Plan 26-01's design_decisions section: adopt at real boundaries when idle, discard otherwise, never double-fetch against the existing lesson-filter refetch.

Output: Gated adoption in both shells; a fully green freshness suite as the phase's acceptance proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26-freshness-fix/26-01-PLAN.md
@.planning/phases/26-freshness-fix/26-02-SUMMARY.md
@components/StudyClient.tsx
@components/CardsClient.tsx
@components/LessonRangeFilter.tsx
@e2e/freshness-client-shell.spec.ts
@e2e/freshness-router-cache.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StudyClient gated adoption of initialCards</name>
  <files>components/StudyClient.tsx</files>
  <read_first>
    - components/StudyClient.tsx (phase state machine: loading/select-mode/loading-practice/studying/complete; loadDue()'s existing select-mode-guarded adoption; buildParams; maxOrder derivation from lessons)
    - .planning/phases/26-freshness-fix/26-01-PLAN.md design_decisions (decisions 3 and 4c bind this task verbatim)
    - .planning/phases/26-freshness-fix/26-02-SUMMARY.md (which adoption pattern shipped in 26-02 — this task must use the same one)
  </read_first>
  <behavior>
    - `/study resume serves fresh data after boundary refresh (FRESH-05)` passes: select-mode due-count adopts the refresh payload
    - `/study back-forward serves fresh data after boundary refresh (FRESH-04)` passes: the remounted-from-cache shell adopts the refresh payload
    - Mid-session (phase === 'studying'), a delivered payload is discarded — the StudySession queue is untouched (FRESH-02); no automated cell covers this, so it is enforced by the gate condition itself plus the code comment
    - Under a narrowed lesson-range filter, a delivered payload is discarded — the filtered card list never silently widens
  </behavior>
  <action>
    Implement gated adoption per design decision 4c, using the same adoption pattern Plan 26-02 shipped (render-phase prev-reference comparison, or its effect fallback — match 26-02-SUMMARY). Hold the previously-seen initialCards reference in prevInitialCards. When the incoming initialCards reference differs, ALWAYS consume it (update prevInitialCards exactly once, adopted or not), then adopt only when ALL of: phase === 'select-mode', !isFilterLoading, and isFullSpan(lessonFrom, lessonTo, maxOrder) where maxOrder derives from the lessons array the same way the existing code computes it (the adoption block may need to compute it locally if placed above the current maxOrder declaration). On adopt: setStudyCards(initialCards) and setScope('due') — mirroring loadDue's post-fetch state shape so the select-mode UI branches identically.

    Discard-when-gated rationale (add as a code comment, condensed): mid-session the queue owns the cards and every path back to select-mode either re-fetches (startAhead) or crosses a navigation boundary that produces a fresh payload; under a narrowed filter the server payload is always the UNFILTERED full-span due pool (app/study/page.tsx queries with null lesson bounds), so adopting it would silently widen the user's chosen range.

    Double-fetch guard (design decision 3): do NOT add any visibilitychange/popstate listener or any new fetch call to StudyClient — FreshnessWatcher is the single boundary-refresh owner, and loadDue remains triggered only by the user's filter Apply. The one race window (refresh in flight while the user taps Apply) is benign: loadDue's guarded response wins for the filtered view and the refresh payload is discarded by the full-span gate.

    Keep untouched: the lessons useState copy and lessonFrom/lessonTo initializers (filter-bounds data, out of this phase's tested scope), loadDue, startAhead, handleModeSelect, the settings/activity mount effect, and all render branches. react-hooks/purity: the adoption logic reads no clocks and no randomness.
  </action>
  <verify>
    <automated>npm run lint && (lsof -ti:3100 | xargs kill -9 2>/dev/null; npx playwright test e2e/freshness-router-cache.spec.ts e2e/freshness-client-shell.spec.ts --grep "(/study resume serves)|(/study back-forward serves)" --reporter=line)</automated>
  </verify>
  <acceptance_criteria>
    - Both /study acceptance tests pass
    - Adoption fires only in select-mode with no filter fetch in flight and the full lesson span selected; gated payloads update prevInitialCards without touching studyCards
    - No new event listener and no new fetch call added to StudyClient (FreshnessWatcher remains the single boundary-refresh owner)
    - lint clean
  </acceptance_criteria>
  <done>/study is fresh at resume and back-forward boundaries while an active session and a filtered view are provably never clobbered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CardsClient gated adoption of initialCards</name>
  <files>components/CardsClient.tsx</files>
  <read_first>
    - components/CardsClient.tsx (editingId/showAdd/adding/deletingIds state; handleSave/handleAdd/handleDelete optimistic local merges; filters derived at render from cards)
    - .planning/phases/26-freshness-fix/26-01-PLAN.md design_decisions (decision 4d binds this task verbatim)
  </read_first>
  <behavior>
    - `/cards resume serves fresh data after boundary refresh (FRESH-05)` passes
    - `/cards back-forward serves fresh data after boundary refresh (FRESH-04)` passes
    - With the edit or add sheet open, or a delete/add in flight, a delivered payload is discarded — the sheet contents and optimistic local merges survive (FRESH-02)
  </behavior>
  <action>
    Implement gated adoption per design decision 4d, using the same pattern as Tasks above. Hold prevInitialCards; when the incoming initialCards reference differs, consume it exactly once, then adopt via setCards(initialCards) only when ALL of: editingId === null, !showAdd, !adding, deletingIds.size === 0.

    Discard-when-gated rationale (add as a code comment, condensed): never defer-adopt a payload that arrived while a sheet was open — that payload was produced before the user's save/add/delete completed, so adopting it after the sheet closes would overwrite the optimistic local merge with pre-mutation data (the exact clobber FRESH-02 exists to prevent). Freshness returns at the next real boundary event.

    Client-side view state is adoption-safe by construction and needs no extra gating: search text, type filter, lesson range, collapsed groups, and activeView are separate state, and filteredCards/groupedCards/allSentences are derived during render from cards — note this in the comment rather than adding gates for them. Keep the lessons useState copy untouched (same reasoning as StudyClient). No new listeners, no new fetches.
  </action>
  <verify>
    <automated>npm run lint && (lsof -ti:3100 | xargs kill -9 2>/dev/null; npx playwright test e2e/freshness-router-cache.spec.ts e2e/freshness-client-shell.spec.ts --grep "(/cards resume serves)|(/cards back-forward serves)" --reporter=line)</automated>
  </verify>
  <acceptance_criteria>
    - Both /cards acceptance tests pass
    - Adoption fires only with no sheet open and no add/delete in flight; gated payloads are consumed without touching cards
    - No new event listener or fetch call in CardsClient
    - lint clean
  </acceptance_criteria>
  <done>/cards is fresh at resume and back-forward boundaries while open sheets and in-flight mutations are provably never clobbered.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite verification sweep and non-regression proof</name>
  <files>none (verification-only task — zero file modifications; produces the final 16-cell green table for the SUMMARY)</files>
  <read_first>
    - .planning/phases/24-freshness-diagnosis-spike/24-DIAGNOSIS.md (Confirmed-Fresh Paths section — the 7 cells that must be proven unregressed)
  </read_first>
  <action>
    Kill any stale port-3100 server, then run the complete acceptance sweep in one pass: all three freshness spec files together (16 tests — 9 previously-red now green, 7 fresh-paths still green), then smoke.spec.ts (4 first-paint assertions — the FRESH-06 no-flash/first-load guard), then npm run lint, the filtered tsc check, and npm test (Vitest unit suite, confirms no lib regressions).

    Confirm from the fresh-paths output that the 4 plain-Link cells still pass their exact-one-fetch assertions — this is the machine proof that FreshnessWatcher adds zero fetches to plain-Link navigation and the confirmed-fresh paths are untouched (24-DIAGNOSIS Confirmed-Fresh Paths / Phase 24 D-09).

    Record the final 16-cell green table (cell → test title → pass) in the SUMMARY next to Plan 26-01's baseline table, so the phase SUMMARY shows the literal red→green flip per cell.

    If ANY fresh-paths or smoke test regressed: the fix is wrong (most likely the watcher firing on a non-boundary event or an adoption gate misfiring) — fix the production code and re-run; NEVER weaken a fresh-paths/smoke assertion, and never add expected-failure or skip annotations anywhere.
  </action>
  <verify>
    <automated>(lsof -ti:3100 | xargs kill -9 2>/dev/null; npx playwright test e2e/freshness-router-cache.spec.ts e2e/freshness-client-shell.spec.ts e2e/freshness-fresh-paths.spec.ts --reporter=line) && npx playwright test e2e/smoke.spec.ts --reporter=line && npm run lint && npx tsc --noEmit 2>&1 | grep -v 'tests/study-cards.test.ts' | grep -c 'error TS' | grep -qx '0' && npm test && ! grep -rn 'test\.fail(\|test\.fixme(\|test\.skip(' e2e/freshness-router-cache.spec.ts e2e/freshness-client-shell.spec.ts e2e/freshness-fresh-paths.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The combined freshness run reports 16 passed, 0 failed; smoke reports 4 passed (FRESH-06)
    - lint, filtered tsc, and the Vitest suite all pass
    - Zero expected-failure/skip annotations across the three freshness spec files
    - Final 16-cell green table recorded for the SUMMARY, paired against the 26-01 baseline
  </acceptance_criteria>
  <done>Phase acceptance proven: red→green across all 9 previously-stale cells with the 7 confirmed-fresh cells and first-load behavior intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser events → server fetch | Boundary events trigger authenticated RSC re-fetches (established in 26-02); this plan only controls how shells consume the results |
| client → RSC endpoint | Every refresh fetch crosses middleware.ts's ks_auth cookie gate (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Information Disclosure | adopted refresh payloads in StudyClient/CardsClient | low | accept | Payloads contain only the authenticated session's own data; adoption changes when data is displayed, not what data is accessible |
| T-26-02 | Denial of Service | prop-adoption re-renders | low | mitigate | Adoption fires at most once per delivered payload (prev-reference consumption) and only when gates are open; no adoption path issues any network request |
| T-26-04 | Tampering (state integrity) | in-flight user interaction vs adopted payload | medium | mitigate | Discard-when-gated: mid-session queues and open-sheet edits can never be overwritten by a boundary payload; deferred adoption is explicitly banned because it would replay pre-mutation data over an optimistic local merge (this is FRESH-02's mitigation, on the phase's primary integrity boundary) |
| T-26-SC | Tampering (supply chain) | npm installs | low | accept | No new dependencies; no package-install tasks in this plan |

Spoofing/Repudiation/Elevation: not applicable — no new endpoints, auth paths, or privilege logic.
</threat_model>

<verification>
- Targeted per-shell greens after each task; full 16/16 + smoke 4/4 in Task 3
- FRESH-02 gates verified structurally (gate conditions + discard semantics in code) since no automated cell drives a mid-session/open-sheet boundary event
- lint, filtered tsc, Vitest all clean; only the 2 declared files modified
</verification>

<success_criteria>
- `npx playwright test e2e/freshness-router-cache.spec.ts e2e/freshness-client-shell.spec.ts e2e/freshness-fresh-paths.spec.ts` exits 0 (16 passed)
- `npx playwright test e2e/smoke.spec.ts` exits 0 (first-load guard, FRESH-06)
- StudyClient and CardsClient adopt at boundaries when idle and provably discard when an interaction is in flight (FRESH-02)
- No expected-failure/skip annotations anywhere in the freshness specs
</success_criteria>

<artifacts_produced>
- `components/StudyClient.tsx` — prevInitialCards gated adoption block (gate: select-mode + !isFilterLoading + isFullSpan; on adopt: setStudyCards + setScope('due'); discard-when-gated)
- `components/CardsClient.tsx` — prevInitialCards gated adoption block (gate: editingId null + !showAdd + !adding + empty deletingIds; discard-when-gated)
- Final 16-cell green acceptance table (SUMMARY content, not a repo file)
</artifacts_produced>

<output>
Create `.planning/phases/26-freshness-fix/26-03-SUMMARY.md` when done, including the final 16-cell green table paired against Plan 26-01's baseline table.
</output>
