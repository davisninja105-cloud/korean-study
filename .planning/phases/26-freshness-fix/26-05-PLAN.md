---
phase: 26-freshness-fix
plan: 05
type: execute
wave: 4
depends_on: ["26-02", "26-03"]
files_modified:
  - components/FreshnessWatcher.tsx
  - app/layout.tsx
  - components/StudyClient.tsx
  - components/CardsClient.tsx
  - components/HabitsClient.tsx
autonomous: true
gap_closure: true
requirements: [FRESH-04, FRESH-05]

must_haves:
  truths:
    - "Resuming the app after it was backgrounded (tab/PWA focus) refreshes stale data, including the case where the daily cron sync ran while the app was backgrounded (FRESH-05) — reliably on /study, /cards, and /habits, not just on a lucky router.refresh() delivery"
    - "Browser back/forward navigation to a previously-visited route shows fresh data, not a stale cached RSC payload (FRESH-04)"
    - "First-load render speed is unchanged and no loading-state flash reappears on any of the four main routes; the plain-Link exact-one-fetch cells and smoke first-paint cells stay green (FRESH-06 regression guard)"
    - "One boundary event still produces at most one router.refresh() plus at most one route-scoped JSON backstop fetch — coalesced together; hiding a tab and plain-Link navigation produce neither"
  artifacts:
    - path: "components/FreshnessWatcher.tsx"
      provides: "Boundary provider: existing visibilitychange/popstate/pageshow → router.refresh() handling now ALSO dispatches a route-aware JSON backstop fetch (existing /api endpoints) whose payload is exposed via React context (useFreshPayload), sidestepping the RSC/Suspense delivery path entirely"
      exports: ["default", "useFreshPayload", "FreshPayloads", "HabitsFreshPayload"]
      min_lines: 90
    - path: "app/layout.tsx"
      provides: "FreshnessWatcher mounted once as a provider wrapping the app subtree (children), replacing the self-closing sibling mount"
      contains: "</FreshnessWatcher>"
    - path: "components/StudyClient.tsx"
      provides: "Gated render-phase adoption of the JSON backstop payload (prevFreshStudy block) — identical gate to the existing prevInitialCards block"
      contains: "useFreshPayload"
    - path: "components/CardsClient.tsx"
      provides: "Gated render-phase adoption of the JSON backstop payload (prevFreshCards block) — identical gate to the existing prevInitialCards block"
      contains: "useFreshPayload"
    - path: "components/HabitsClient.tsx"
      provides: "Ungated backstop adoption via a freshOverride derived-read layer: override wins until fresh RSC props arrive, props clear the override"
      contains: "useFreshPayload"
  key_links:
    - from: "components/FreshnessWatcher.tsx"
      to: "app/api/cards/due (and /api/cards, /api/activity, /api/stats)"
      via: "fetch() from the coalesced boundary handler — window.location.pathname read at event time selects the route's endpoint(s)"
      pattern: "/api/cards/due"
    - from: "components/StudyClient.tsx"
      to: "components/FreshnessWatcher.tsx"
      via: "useFreshPayload().study consumed through the established prev-reference render-phase comparison, adopted only when the existing 3-part gate is open"
      pattern: "prevFreshStudy"
    - from: "components/HabitsClient.tsx"
      to: "components/FreshnessWatcher.tsx"
      via: "useFreshPayload().habits → freshOverride; days/goal/masteredCount/dayStartHour/cardsByState derive from override-else-props"
      pattern: "freshOverride"
---

<objective>
Close 26-VERIFICATION.md's FRESH-04/FRESH-05 gaps (single root cause: Next.js 16.2.1 intermittently never applies a successfully-fetched `router.refresh()` RSC payload to the mounted client tree on routes with `loading.tsx` — `/study` failed 7/7 verification runs) by implementing the LOCKED USER DECISION: the JSON re-fetch alternative named in deferred-items.md's own recommendation. The boundary handlers in `FreshnessWatcher` now SUPPLEMENT `router.refresh()` with an explicit client-side `fetch()` of the route's existing JSON endpoint(s); the response is delivered to the mounted shell via React context and adopted through the SAME gated `prevInitialX`-style render-phase pattern already proven correct in every shell — the RSC Flight/Suspense path is no longer the load-bearing delivery mechanism for freshness.

Supplement (not replace) is the surgical variant the locked decision permits: (1) the acceptance specs' network-evidence assertions (`pathname === routePath && isRsc`) keep passing without touching a single spec file — deferred-items.md item 2 proved the router.refresh() FETCH always fires and the server always recomputes; only client-side application drops; (2) `/` (no `loading.tsx`, never failed in 10+ runs) stays completely untouched; (3) when RSC delivery works, the Router Cache stays fresh as defense in depth. Both delivery paths funnel into the same gates, so double delivery is idempotent.

PROHIBITED (six rejected approaches, full detail in deferred-items.md — do NOT re-attempt any): startTransition wrap, setTimeout-deferred refresh, second coalesced refresh, Nav prefetch disabling, router.push(), router.replace(), and the Next.js 16.2.10 patch bump. Also prohibited: any automatic fallback-to-accepted-risk-override mechanism — the user explicitly declined that option. If the flake somehow persists after this change, Plan 26-06 reports it honestly.

Purpose: FRESH-04/FRESH-05 gap closure via a delivery path that cannot be dropped by the Suspense/Segment-Cache interaction.
Output: FreshnessWatcher provider + context hook, layout wrap, backstop adoption in StudyClient/CardsClient/HabitsClient. Zero server-side changes — no new API route, no middleware change, no spec-file change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26-freshness-fix/26-VERIFICATION.md
@.planning/phases/26-freshness-fix/deferred-items.md
@.planning/phases/26-freshness-fix/26-03-SUMMARY.md
@components/FreshnessWatcher.tsx
@components/StudyClient.tsx
@components/CardsClient.tsx
@components/HabitsClient.tsx
@components/GlossProvider.tsx
@app/layout.tsx
@lib/dto.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FreshnessWatcher becomes a provider with a route-aware JSON backstop fetch</name>
  <files>components/FreshnessWatcher.tsx, app/layout.tsx</files>
  <read_first>
    - .planning/phases/26-freshness-fix/deferred-items.md (MANDATORY — the six rejected approaches you must not reintroduce, and the recommendation this task implements; evidence item 5: removing loading.tsx made delivery 100% reliable, isolating the failure to the Suspense path this backstop bypasses)
    - components/FreshnessWatcher.tsx (current listeners, coalesce ref, popstate macrotask deferral — ALL of this behavior is preserved verbatim; this task only adds to it)
    - components/GlossProvider.tsx (the codebase's provider + hook single-file precedent: context created in-file, provider component exported, hook exported alongside)
    - app/layout.tsx (current sibling mount `<FreshnessWatcher />` after ThemeWatcher; GlossProvider already wraps the Nav + main subtree — mirror that wrapping shape)
    - lib/dto.ts (CardDTO, ActivityDTO, StatsDTO — type-only imports, client-safe)
  </read_first>
  <behavior>
    - A resume/back-forward/bfcache boundary event on /study, /cards, or /habits produces exactly one router.refresh() AND exactly one route-scoped JSON backstop fetch (both inside the same coalesce guard); on / and any other pathname it produces router.refresh() only
    - The backstop payload appears on useFreshPayload() with a new object reference for that route's slice; other slices are untouched
    - Plain-Link navigation, first load, and tab-hide produce zero refreshes and zero backstop fetches (fresh-paths exact-one-fetch cells and smoke stay green)
    - A backstop response arriving after the user navigated away is dropped (pathname re-checked at response time), never written into another route's slice
  </behavior>
  <action>
    Rework components/FreshnessWatcher.tsx (keep the filename and the default export name — every prior plan/verification artifact references them):

    1. New exported types (import CardDTO/ActivityDTO/StatsDTO as type-only from '@/lib/dto'): `HabitsFreshPayload` = { days: ActivityDTO['days']; dailyGoalSeconds: number; dayStartHour: number; masteredCount: number; cardsByState: StatsDTO['cardsByState'] }. `FreshPayloads` = { study: CardDTO[] | null; cards: CardDTO[] | null; habits: HabitsFreshPayload | null }. Module-level `const EMPTY_PAYLOADS: FreshPayloads` with all-null slices; a React context created with EMPTY_PAYLOADS as default; exported hook `useFreshPayload(): FreshPayloads` returning useContext of it (safe default outside the provider, mirroring useWordTap's tolerance).

    2. Default export signature becomes `FreshnessWatcher({ children }: { children: React.ReactNode })`; render the context provider around children instead of returning null. `const [payloads, setPayloads] = useState<FreshPayloads>(EMPTY_PAYLOADS)` is the provider value.

    3. Inside the existing coalesced `refresh()` closure — keep the Date.now() coalesce ref, the visible-edge-only visibilitychange, the macrotask-deferred popstate, and the persisted-only pageshow EXACTLY as they are — after `router.refresh()`, call a new `fetchBackstop()` function defined in the same effect scope:
       - Read `const path = window.location.pathname` at call time (never a closure-captured pathname — popstate fires after the URL updates, so this is the RESTORED route).
       - path === '/study': fetch('/api/cards/due') → if res.ok, json → if Array.isArray(result) AND window.location.pathname is still '/study', `setPayloads(prev => ({ ...prev, study: result as CardDTO[] }))`.
       - path === '/cards': same shape against fetch('/api/cards') into the cards slice. (Shape parity is already guaranteed: the GET handler uses the identical include-set as app/cards/page.tsx and JSON serialization emits ISO date strings satisfying CardDTO. Do NOT modify any API route.)
       - path === '/habits': Promise.all of fetch('/api/activity') and fetch('/api/stats') → both ok → compose a HabitsFreshPayload from activity.days/dailyGoalSeconds/dayStartHour + stats.masteredCount/cardsByState → same still-on-route guard → set the habits slice.
       - any other path (including '/'): no backstop fetch. Comment the rationale: '/' has no loading.tsx and never exhibited the delivery flake across 10+ isolated runs (deferred-items.md); router.refresh() remains its sole, reliable delivery path.
       - Every promise chain ends in `.catch(() => {})` — a failed backstop degrades to today's behavior (router.refresh() alone), never surfaces an error.
    4. Purity: fetch/Date.now()/window.location reads happen only inside event-handler-triggered closures and async callbacks; setPayloads only via functional updates in .then callbacks; nothing impure during render. Effect dependency array stays [router]. No dangerouslySetInnerHTML, no event-derived data written to the DOM (T-26-10).
    5. Update the file-header comment: document the dual delivery (router.refresh() for Router-Cache hygiene + JSON backstop for guaranteed client-tree delivery), name deferred-items.md as the origin, and state explicitly that the backstop exists because the RSC delivery path is unreliable on loading.tsx routes — not as a second freshness trigger (still one coalesced event source).

    In app/layout.tsx: replace the self-closing `<FreshnessWatcher />` sibling with a wrap — `<FreshnessWatcher>` opens after `<ThemeWatcher />` and `</FreshnessWatcher>` closes after `</GlossProvider>`, so the entire GlossProvider/Nav/main subtree (including {children}) is inside the provider. No other layout changes.

    After edits, kill any stale port-3100 server (a reused server would serve pre-edit code), then run the regression net to prove this task alone changes nothing observable (no shell consumes the context yet — payloads are set but unused; behavior must be byte-for-byte identical to today).
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit 2>&1 | grep -v 'tests/study-cards.test.ts' | grep -c 'error TS' | grep -qx '0' && grep -q 'useFreshPayload' components/FreshnessWatcher.tsx && grep -q '</FreshnessWatcher>' app/layout.tsx && (lsof -ti:3100 | xargs kill -9 2>/dev/null; npx playwright test e2e/freshness-fresh-paths.spec.ts e2e/smoke.spec.ts --reporter=line)</automated>
  </verify>
  <acceptance_criteria>
    - components/FreshnessWatcher.tsx exports default FreshnessWatcher (accepting children), useFreshPayload, FreshPayloads, HabitsFreshPayload; all three original listeners + coalesce guard + popstate deferral preserved
    - app/layout.tsx wraps the GlossProvider subtree in FreshnessWatcher (grep finds a closing </FreshnessWatcher> tag)
    - The backstop fetch maps exactly: /study → /api/cards/due; /cards → /api/cards; /habits → /api/activity + /api/stats; all other paths → none; pathname re-checked at response time before any setPayloads
    - fresh-paths + smoke fully green after the change (no flash, plain-Link fetch count still exactly 1 — the provider adds no fetch outside boundary handlers)
    - lint and filtered tsc clean; zero API-route or middleware files modified
  </acceptance_criteria>
  <done>Every boundary event on a loading.tsx route now produces a second, Suspense-independent delivery of fresh data into React context; no shell consumes it yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backstop adoption in StudyClient, CardsClient, HabitsClient</name>
  <files>components/StudyClient.tsx, components/CardsClient.tsx, components/HabitsClient.tsx</files>
  <read_first>
    - components/StudyClient.tsx (the existing prevInitialCards gated block at ~line 116-141 — the new block mirrors it verbatim, including the always-consume/conditionally-adopt shape and its comment discipline)
    - components/CardsClient.tsx (existing prevInitialCards block at ~line 61-82 with the 4-part gate)
    - components/HabitsClient.tsx (direct prop reads at ~line 60-69 — the 26-02 design being extended, NOT reverted; the today effect at ~line 80-82 carries the CR-01 fix whose semantics must be preserved; the HabitsLoading gate on empty today at ~line 134 must be untouched)
    - .planning/phases/26-freshness-fix/26-03-SUMMARY.md (key-decisions: gate conditions are exact, placement below derived consts, render-phase pattern shipped and lint-clean — reuse it, no effect-based fallback needed)
  </read_first>
  <behavior>
    - /study resume and /study back-forward acceptance cells (freshness-router-cache.spec.ts / freshness-client-shell.spec.ts) pass: even when the RSC payload application is dropped by Next.js, the backstop payload lands in select-mode and updates the due count
    - /cards and /habits resume + back-forward cells pass by the same mechanism
    - Mid-session or open-sheet backstop deliveries are consumed but never adopted (Plan 26-04's freshness-gate.spec.ts cells stay green)
    - A backstop slice left over from a previous visit is never adopted at mount (mount-time seeding), so a plain-Link navigation with fresh RSC props is never regressed to older backstop data
  </behavior>
  <action>
    All three shells: import useFreshPayload (plus HabitsFreshPayload where needed) from '@/components/FreshnessWatcher' and add a render-phase adoption block using the SAME prev-reference pattern as the existing prop blocks. Critical shared rule: the prev-holder is seeded with the slice's CURRENT value at mount (`useState(freshSlice)`), so only deliveries arriving AFTER mount are ever adopted — this is what makes cross-visit context leftovers inert (a remounted shell's boundary fetch always lands after the remount because it needs a server round-trip, while a Link-navigated shell with fresh RSC props sees no post-mount slice change at all and adopts nothing). The incoming reference is ALWAYS consumed (prev updated) exactly like prevInitialCards; only adoption is conditional.

    StudyClient — insert immediately after the existing prevInitialCards block: `const { study: freshStudy } = useFreshPayload()`; prev-holder named prevFreshStudy; on reference change, consume, then adopt (`setStudyCards(freshStudy)` + `setScope('due')`) only when `freshStudy !== null && phase === 'select-mode' && !isFilterLoading && isFullSpan(lessonFrom, lessonTo, maxOrder)` — the identical 3-part gate, plus the null check. Comment: JSON backstop delivery for the same payload the RSC path delivers (both /api/cards/due and the RSC page call getStudyCards with identical args), same gate verbatim, mount-seeded prev prevents cross-visit adoption.

    CardsClient — insert immediately after the existing prevInitialCards block: `const { cards: freshCards } = useFreshPayload()`; prev-holder prevFreshCards; adopt (`setCards(freshCards)`) only when `freshCards !== null && editingId === null && !showAdd && !adding && deletingIds.size === 0` — the identical 4-part gate plus null check. Same comment discipline.

    HabitsClient — this shell has direct prop reads (no setters to call), so adoption uses a derived-read override layer instead of reverting 26-02's design:
    1. `const { habits: freshHabits } = useFreshPayload()`; prev-holder prevFreshHabits seeded to freshHabits at mount; `const [freshOverride, setFreshOverride] = useState<HabitsFreshPayload | null>(null)`. Render-phase: on freshHabits reference change → consume, and if non-null → setFreshOverride(freshHabits).
    2. Props-win block: prev-holder prevInitialDays seeded to initialDays; on initialDays reference change (a successful RSC delivery — strictly newer data) → consume and setFreshOverride(null), letting the fresh props shine through.
    3. Replace the direct-read consts: days/goal/masteredCount now derive `freshOverride ? freshOverride.X : initialX` (X = days/dailyGoalSeconds→goal/masteredCount); add `const dayStartHour = freshOverride ? freshOverride.dayStartHour : initialDayStartHour` and `const cardsByState = freshOverride ? freshOverride.cardsByState : initialCardsByState`; point countFor at cardsByState. Downstream JSX/useMemo bodies keep the same identifiers and need no edits.
    4. The today effect (CR-01): change the body to `habitDateStr(dayStartHour)` and the dependency array to [dayStartHour, days, masteredCount] — same recompute-on-fresh-data semantics as the CR-01 fix, now also firing on backstop adoption (the overnight-resume habit-day rollover works through either delivery path). The HabitsLoading empty-today gate stays exactly as is.
    5. Update the direct-prop-reads comment block to document the two-source derivation (props by default, backstop override until fresher props arrive).

    Purity note (all three): the new blocks are reference comparisons + guarded setState during render — the exact pattern already shipped and lint-clean in all four shells; no Date.now()/fetch/effects added. No new listeners, no new fetch calls in any shell — FreshnessWatcher remains the single boundary-refresh owner (design decision 3, unchanged).

    After edits: kill any stale port-3100 server, then run the six targeted acceptance cells plus the regression net.
  </action>
  <verify>
    <automated>npm run lint && (lsof -ti:3100 | xargs kill -9 2>/dev/null; npx playwright test e2e/freshness-router-cache.spec.ts e2e/freshness-client-shell.spec.ts --grep "(/study resume serves)|(/cards resume serves)|(/habits resume serves)|(/study back-forward serves)|(/cards back-forward serves)|(/habits back-forward serves)" --reporter=line) && npx playwright test e2e/freshness-fresh-paths.spec.ts e2e/smoke.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - All six targeted acceptance cells (/study, /cards, /habits × resume, back-forward) pass in a single run
    - fresh-paths and smoke stay fully green (FRESH-06: no flash, plain-Link fetch count unchanged, first-paint content assertions intact)
    - StudyClient/CardsClient backstop gates are character-identical to their prop-adoption gates (plus the null check); HabitsClient's HabitsLoading gate and CR-01 recompute semantics are preserved with the widened trigger set
    - npm run lint passes with 0 errors; no spec file, API route, or middleware file modified
  </acceptance_criteria>
  <done>All three loading.tsx-route shells adopt boundary-fresh data through a delivery path that does not depend on Next.js's Suspense/Segment-Cache handling; / remains on its proven router.refresh() path untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Targeted reliability proof — repeated isolated runs of the previously-flaky cells</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - .planning/phases/26-freshness-fix/26-VERIFICATION.md (the Behavioral Spot-Checks table — this task mirrors its exact commands so Plan 26-06 and the next verification pass get an apples-to-apples comparison; /study resume was 0/7 there)
  </read_first>
  <action>
    Prove the fix against the exact cells that failed verification, using repeated isolated runs (reuseExistingServer keeps the built server across iterations — kill the port-3100 server once BEFORE the first run so the post-Task-2 build is what serves):

    1. /study resume ×5: `npx playwright test e2e/freshness-router-cache.spec.ts --grep "/study resume serves" --reporter=line`, run in a counted loop of 5; record pass count. Required: 5/5 (verification baseline: 0/7).
    2. /study back-forward ×5: `npx playwright test e2e/freshness-client-shell.spec.ts --grep "/study back-forward serves" --reporter=line`, loop of 5. Required: 5/5.
    3. /habits resume ×3: `npx playwright test e2e/freshness-router-cache.spec.ts --grep "/habits resume serves" --reporter=line`, loop of 3. Required: 3/3 (baseline: ~50%).
    4. One full combined sweep of the three existing files: `npx playwright test e2e/freshness-router-cache.spec.ts e2e/freshness-client-shell.spec.ts e2e/freshness-fresh-paths.spec.ts --reporter=line` — all cells pass.
    5. Unit + static net: `npm test` (241 vitest tests), `npm run lint`, filtered `npx tsc --noEmit`.

    Record the per-run table (run number × result) in the SUMMARY, mirroring 26-03-SUMMARY's observed-sweep-results table format. HONESTY RULE (locked decision): if any run fails, do NOT weaken an assertion, add a retry/annotation, re-tune a timeout to force green, or insert any accepted-risk override — record the failure verbatim in the SUMMARY with the run table and mark the plan blocked for Plan 26-06/the next verification pass to surface. The full formal acceptance protocol (2× combined sweep including Plan 26-04's new cells, 5× isolated runs) runs post-merge in Plan 26-06.
  </action>
  <verify>
    <automated>lsof -ti:3100 | xargs kill -9 2>/dev/null; P=0; for i in 1 2 3 4 5; do npx playwright test e2e/freshness-router-cache.spec.ts --grep "/study resume serves" --reporter=line && P=$((P+1)); done; echo "study-resume $P/5"; test "$P" -eq 5</automated>
  </verify>
  <acceptance_criteria>
    - /study resume: 5/5 isolated passes (loop output shows "study-resume 5/5")
    - /study back-forward: 5/5 isolated passes; /habits resume: 3/3 isolated passes
    - One combined 3-file sweep fully green; npm test 241/241; lint 0 errors; filtered tsc 0 errors
    - Per-run results table recorded in the SUMMARY; zero spec files modified during this task (git diff --stat empty for e2e/)
  </acceptance_criteria>
  <done>The previously 0/7 cell now passes repeatedly in isolation; the phase-level formal acceptance re-run (including Plan 26-04's new FRESH-02 cells) is handed to Plan 26-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser boundary events → JSON API fetch | visibilitychange/popstate/pageshow now also trigger authenticated same-origin fetches to existing /api endpoints |
| client → /api/cards/due, /api/cards, /api/activity, /api/stats | Every backstop fetch crosses middleware.ts's ks_auth cookie gate exactly like the shells' existing client fetches — NO new endpoint is created and NO route is added to the middleware allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-08 | Information Disclosure | JSON backstop fetches (FreshnessWatcher) | low | accept | The backstop re-fetches only data the authenticated session's RSC props already deliver, from endpoints that already exist and are already cookie-gated by middleware.ts; single-tenant app with one shared password by design — exposure identical to the existing pull-to-refresh/filter fetches |
| T-26-09 | Denial of Service | extra fetch per boundary event | low | mitigate | Backstop shares the existing 300ms coalesce guard and fires only for the current route (1-2 bounded queries, same cost the RSC render already incurs); boundary events are rare user actions, never timers |
| T-26-10 | Tampering (payload → UI injection) | context payload rendered by shells | low | mitigate | Responses validated (res.ok + Array.isArray for card slices) before entering state; rendered exclusively through JSX text interpolation (auto-escaped); no dangerouslySetInnerHTML anywhere in the touched files; payload originates from the app's own authenticated same-origin API |
| T-26-SC | Tampering (supply chain) | npm installs | low | accept | No new dependencies; no package-install tasks in this plan |

Spoofing/Repudiation/Elevation: not applicable — no new endpoints, auth paths, or privilege logic; middleware.ts untouched.
</threat_model>

<verification>
- Task 1 alone is observably inert (regression net green with no consumer wired)
- Task 2 turns all six /study,/cards,/habits boundary cells green in a single run without touching any spec
- Task 3 proves repeatability on the exact cells 26-VERIFICATION.md measured: /study resume 5/5 (was 0/7), /study back-forward 5/5, /habits resume 3/3
- fresh-paths + smoke green throughout (FRESH-06 guard); lint/tsc/vitest clean; git diff confined to the 5 declared files
</verification>

<success_criteria>
- Boundary freshness on /study, /cards, /habits no longer depends on Next.js's Suspense/Segment-Cache applying an RSC payload — the JSON backstop delivers through plain React state
- The six rejected approaches from deferred-items.md were not reintroduced in any form; no automatic accepted-risk fallback exists anywhere
- The existing gated/ungated adoption patterns were extended, not redesigned; / is untouched
- All acceptance evidence produced with unmodified spec files (apples-to-apples with 26-VERIFICATION.md)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `components/FreshnessWatcher.tsx` — default export `FreshnessWatcher` now accepts `children` and renders a context provider; NEW named exports: `useFreshPayload(): FreshPayloads` hook, `FreshPayloads` interface ({ study, cards, habits } nullable slices), `HabitsFreshPayload` interface ({ days, dailyGoalSeconds, dayStartHour, masteredCount, cardsByState }); NEW internal `fetchBackstop()` route→endpoint dispatcher inside the coalesced boundary handler
- `app/layout.tsx` — `<FreshnessWatcher>` converted from self-closing sibling to a wrapper around the GlossProvider subtree
- `components/StudyClient.tsx` — `prevFreshStudy` gated render-phase backstop-adoption block (gate identical to prevInitialCards)
- `components/CardsClient.tsx` — `prevFreshCards` gated render-phase backstop-adoption block (gate identical to prevInitialCards)
- `components/HabitsClient.tsx` — `prevFreshHabits`/`prevInitialDays` consume blocks + `freshOverride` state; `days`/`goal`/`masteredCount`/`dayStartHour`/`cardsByState` become override-else-props derived reads; today effect retriggered by the derived values
- No new API routes, no new files, no new dependencies, no spec changes
</artifacts_produced>

<output>
Create `.planning/phases/26-freshness-fix/26-05-SUMMARY.md` when done, including the Task 3 per-run results table and confirmation that no rejected approach or auto-fallback was introduced.
</output>
