---
phase: 31
slug: cards-list-pagination-virtualization
status: draft
shadcn_initialized: false
preset: none
created: 2026-08-07
---

# Phase 31 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — existing hand-rolled semantic-token system in `app/globals.css` (established v1.1 "Design System Tokens & Sweep"). shadcn is **not** initialized here: `components.json` is absent, the project has a mature bespoke component set (`Sheet.tsx`, `SwipeRow.tsx`, `HighlightedSentence.tsx`, etc.), and `CLAUDE.md` explicitly forbids literal color utilities in favor of the token system. Introducing shadcn now would contradict established project conventions and is unrelated to this phase's goal (pagination/virtualization performance). |
| Preset | not applicable |
| Component library | none (bespoke React components) |
| Icon library | `lucide-react` (already a dependency; `SlidersHorizontal` used today) |
| Font | `'Pretendard Variable'` (Korean, via `--font-korean` / `.hangul` utilities) + Geist Sans (`--font-sans`, UI chrome/English) |

---

## Spacing Scale

Declared values (must be multiples of 4) — matches the scale already in use across `CardsClient.tsx`:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge internal padding, skeleton bar gaps |
| sm | 8px | Compact element spacing (`gap-2`), group header gaps |
| md | 16px | Default element spacing (`p-4` card rows, `gap-4` page sections) |
| lg | 24px | N/A — not used in existing Cards page; no new use introduced here |
| xl | 32px | N/A — not introduced by this phase |
| 2xl | 48px | N/A — not introduced by this phase |
| 3xl | 64px | N/A — not introduced by this phase |

Exceptions: `min-h-11` / `min-w-11` (44px) touch targets — existing project-wide convention for tap targets (Filter icon button, Edit link, grade/segment buttons); 44 is a multiple of 4 so this is not a true scale exception, just called out because it's a fixed floor, not a spacing gap.

New elements this phase adds (all reuse the existing scale, no new spacing values):
- Skeleton card row: same `p-4` container padding as a real card row (visual continuity — the skeleton must occupy the exact footprint of the row it stands in for, preventing layout shift when real data replaces it).
- Infinite-scroll sentinel: `h-px` (1px, effectively 0) — invisible, spacing-neutral trigger element positioned after each group's last loaded row.
- Inline search-loading spinner: `gap-2` from the search input's trailing edge (sits inside the sticky bar's existing `gap-2` flex row).

---

## Typography

Existing established hierarchy (unchanged, reused — this phase does not touch card front/back typography):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`text-sm`) | 400 regular | 1.5 (Tailwind default `leading-normal`) |
| Label | 12px (`text-xs`) | 600 semibold | 1.4 — badges, group counts, uppercase filter section labels |
| Heading | 16px (`text-base`) | 700 bold | 1.5 — card front (`.hangul font-bold`), pre-existing, not modified by this phase |
| Display | N/A | N/A | N/A — not used on `/cards`; no display-scale text introduced |

**New UI this phase introduces (loading/error/empty copy) is constrained to 2 weights only** — 400 regular (body/copy) and 600 semibold (counts, labels) — per contract discipline, even though the pre-existing card-front bold (700) remains untouched elsewhere on the page:

| New element | Size | Weight |
|---|---|---|
| "Loading more cards…" / "Searching…" inline status text | 12px (`text-xs`) | 400 regular, `text-muted` |
| "Couldn't load more. Try again." retry link | 14px (`text-sm`) | 600 semibold, `text-button` (matches existing Edit-link treatment) |
| Empty-state heading (search/filter no-results) | 14px (`text-sm`) | 400 regular, `text-muted` (matches existing empty-state copy exactly) |
| "No more cards" end-of-list marker | 12px (`text-xs`) | 400 regular, `text-muted` |

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--background)` (`#f9fafb` light / `#0b0f1a` dark) | Page background behind the whole `/cards` scroll area |
| Secondary (30%) | `var(--surface-1)` (`#ffffff` light / `#1c2030` dark) | Card rows, Sheet bodies (Filter/Add/Edit), sticky search bar backdrop is `bg-background` not surface-1 (existing) |
| Accent (10%) | `var(--button)` (`#3b82f6` blue) | See reserved-for list below — unchanged from existing page, no new accent usage introduced |
| Destructive | Tailwind `red-500` / dark `red-400` | Swipe-to-delete reveal (existing `SwipeRow`), "Clear all filters" link (existing) — no new destructive actions in this phase |

Accent (`--button`) reserved for — explicit list, matches existing page + this phase's new elements:
- "Add Card" button (existing)
- Filter icon fill + active-filter-count badge when filters are active (existing)
- "Edit" link on each card row (existing)
- Active filter pill in the Filter Sheet (existing)
- "Done" button in Filter Sheet, submit button in Add/Edit sheets (existing)
- **New:** the inline search-loading spinner (reuses the exact existing spinner pattern: `border-2 border-button border-t-transparent rounded-full animate-spin`, seen today in `AudioButton.tsx`, `GlossProvider.tsx`, `HistoryClient.tsx`)
- **New:** "Try again" retry link text color, on a failed batch-load or failed search request

`--skeleton-bg` (`bg-skeleton`, shipped Phase 30) reserved for:
- **New:** skeleton card rows on `/cards` initial load and on Reading practice initial load (same token, same `animate-pulse`, matching the Phase 30 pattern already used elsewhere — do not invent a new gray)
- **New:** skeleton rows appended during infinite-scroll batch loading (same token, smaller count)

No new color values are introduced. Every new element in this phase draws exclusively from tokens already defined in `app/globals.css`.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Add Card" (unchanged from existing — this phase does not add a new primary CTA; there is deliberately **no** "Load more" button per D-03, auto-load replaces it) |
| Empty state — no cards at all | "No cards yet. Sync your Google Doc to get started." (unchanged, existing copy) |
| Empty state — search active, zero matches | "No results for "{query}"." (new — more specific than today's generic "No cards match your search."; surfaces the actual query so the user can see what was searched, important now that search runs server-side across the full ~1056-card deck rather than filtering an in-memory page) |
| Empty state — lesson-range/type filter active (no search), zero matches | "No cards match this filter." (new — distinguishes filter-caused emptiness from search-caused emptiness so the user knows which control to change) |
| Empty state — Reading practice, filtered to zero sentences | "No example sentences match your filter." / "No example sentences yet. Sync a lesson to generate them." (existing wording pattern, extended to the new independent Reading practice fetch) |
| Loading — initial page load | No text; skeleton rows only (per Phase 30's visible-skeleton convention — skeletons communicate loading, no redundant caption needed) |
| Loading — infinite-scroll batch in flight | "Loading more…" (new, `text-xs text-muted`, appears below the last loaded row of the group currently fetching, above the skeleton rows) |
| Loading — search request in flight (debounced) | "Searching…" (new, small inline spinner + text, appears next to/below the sticky search input; existing rows stay visible and dim to `opacity-60` rather than disappearing, avoiding a blank flash between keystroke-settled and results-arrived) |
| End of list | "You've reached the end." (new — shown once a group's server-reported total count equals its loaded count; muted, centered, `text-xs`) |
| Error — batch load fails (network/server error mid-scroll) | "Couldn't load more cards. Check your connection and try again." with an inline "Try again" retry link that re-issues the same page request (new) |
| Error — search/filter request fails | "Couldn't search right now. Try again." with an inline "Try again" retry link (new) |
| Error — Edit sheet fails to fetch a card's sentences | "Couldn't load this card's sentences. Try again." with an inline "Try again" retry link inside the sheet, above the (otherwise sentence-less) edit form (new — required because sentences move to an on-demand fetch, see Claude's Discretion in CONTEXT.md) |
| Destructive confirmation | Delete card: existing native `confirm('Delete this card?')` via `SwipeRow` — unchanged, not in scope for this phase |

---

## UI Considerations

Applicable state considerations resolved: 13 covered, 1 backstop, 0 unresolved.

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | Cards list (list-collection) | ✅ covered | Three distinct empty copies per cause — zero cards overall, zero search matches, zero filter matches (see Copywriting Contract) — so the user always knows which control emptied the list. |
| empty | Reading practice list (list-collection) | ✅ covered | Reuses existing two-case wording (no sentences synced yet vs. filtered to zero), now applied to Reading practice's own independent fetch per D-07. |
| loading | Cards list — initial load (list-collection) | ✅ covered | Skeleton card rows (`bg-skeleton`, `animate-pulse`), same footprint (`p-4 rounded-xl`) as a real card row, grouped under the same section headers so the page shape doesn't shift when real data lands — matches the Phase 30 visible-skeleton pattern (PERCEPT-01). Count: 4 skeleton rows in the Vocabulary group (starts expanded per D-02), collapsed groups show only their header + count, no skeleton rows underneath until expanded. |
| loading | Cards list — infinite-scroll batch (list-collection) | ✅ covered | 2–3 skeleton rows appended below the last real row of the group being extended, preceded by "Loading more…" caption; sentinel `IntersectionObserver` triggers the next fetch at `rootMargin: '0px 0px 600px 0px'` (approaching, not touching, the bottom) per D-03. |
| loading | Search input — debounced request in flight (interactive-control) | ✅ covered | Inline spinner (existing `border-2 border-button border-t-transparent rounded-full animate-spin` pattern) appears at the search input's trailing edge; previously-loaded rows dim to `opacity-60` rather than being cleared, avoiding a blank-list flash during the ~300ms debounce + round trip. |
| loading | Edit sheet — on-demand sentence fetch (form) | 🧪 backstop | Opening the Edit sheet for a card now requires a fetch for that card's full `sentences[]] (dropped from the list query per CARDS-01/D-01's premise). The sheet must show a loading placeholder (skeleton lines matching the sentence-editor's field shapes) between opening and the fetch resolving, never a flash of "no sentences" — flagged as backstop because there is no existing precedent component for this exact in-sheet fetch-loading shape; verify at execution that the placeholder renders before the real data, not after an empty state. |
| error | Cards list — batch load failure (list-collection) | ✅ covered | Inline "Couldn't load more cards…" + retry link, positioned where the skeleton rows would have appeared; does not clear already-loaded rows above it (partial-failure tolerant). |
| error | Search/filter request failure (interactive-control) | ✅ covered | Inline "Couldn't search right now…" + retry link near the search bar; previous results remain visible (same non-destructive pattern as the loading state). |
| error | Edit sheet sentence fetch failure (form) | ✅ covered | Inline retry copy inside the sheet (see Copywriting Contract); the rest of the edit form (front/back/notes, which ARE already in memory from the list) still renders and is still editable — only the sentence editor section is blocked pending retry. |
| populated | Cards list at typical volume (list-collection) | ✅ covered | Unchanged from today's row layout (badge, lesson tag, review count, Edit link, front/back/notes, indented sentence previews) — this phase changes data-loading mechanics, not the populated row's visual design. |
| populated | Newly added card visibility (list-collection) | ✅ covered | A card added via "Add Card" appears at the top of its type group (existing `setCards(prev => [created, ...prev])` behavior) using the existing `.animate-card-in` fade-in; if that group is currently collapsed (Grammar/Phrase/Other start collapsed per D-02), the group auto-expands so the user sees confirmation their card was saved, rather than a silent no-op behind a collapsed header. |
| overflow | Cards list — full ~1056-card deck scroll (list-collection) | ✅ covered | Windowed/virtualized rendering keeps the DOM node count bounded regardless of how many pages have loaded (CARDS-02) — the visual contract is that scrolling must look and feel identical to the current unvirtualized list (same row spacing, same group headers, same sticky search bar), with no visible seam where virtualization begins. |
| zero-one-many | Type groups (Vocabulary/Grammar/Phrase/Other) (list-collection) | ✅ covered | Existing "N card(s)" pluralization pattern is preserved; group header counts are now server-aggregated full-deck totals (not "loaded so far") per the CONTEXT.md decision, so a collapsed group still reads correctly (e.g. "Grammar · 214 cards") even though zero of its rows are loaded yet. |

Deliberately out of scope for this phase (not re-litigated, existing behavior unchanged):
- Long-text overflow on card fronts/backs — existing `.hangul` `word-break: keep-all` wrap behavior, untouched.
- Destructive confirmation UX (native `confirm()`) — untouched, not part of pagination/virtualization work.
- Nav/tab-toggle long-text at extreme counts — "Cards (1056)" already renders correctly today per the existing smoke-test assertion; unaffected by pagination.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | none — shadcn not initialized this phase | not applicable |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
