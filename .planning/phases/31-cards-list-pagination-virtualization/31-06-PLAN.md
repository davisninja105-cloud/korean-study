---
phase: 31-cards-list-pagination-virtualization
plan: 06
type: execute
wave: 1
depends_on: ["31-05"]
files_modified:
  - lib/cards-list.ts
  - lib/dto.ts
  - components/CardsClient.tsx
  - tests/cards-list.test.ts
  - e2e/cards-tab-switch-scroll.spec.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CARDS-01, CARDS-02, CARDS-03]
gap_closure: true

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every rendered card row displays its real per-card sentence count, sourced from a server-computed `_count` aggregate — never from a loaded `sentences` array length, and never by loading the actual `Sentence` rows (CARDS-01's 'without loading the sentences themselves' constraint holds for every group and for Reading Practice's nested card data)."
    - "A card with zero sentences renders `sentenceCount: 0` end-to-end (cardSelect._count -> getCardsPage/getSentencesPage mapping -> CardDTO.sentenceCount -> renderCardRow's '0 sentences' badge) — not hidden, not omitted, not a crash. [edge-probe CARDS-01/empty: covered]"
    - "Switching Cards -> Reading Practice -> Cards -> Reading Practice preserves each view's exact window scroll offset (window.scrollY) and triggers zero additional /api/cards (page-1) requests and no more than the one lazy first-visit /api/cards/sentences request, proven by an automated Playwright spec — not just code inspection. [edge-probe CARDS-02/unclassified: covered]"
    - "npx vitest run tests/cards-list.test.ts passes including the new sentence-count assertions, and the full existing suite (npm test, the previously-green e2e specs touching /cards) stays green — no regression to any of 31-01 through 31-05's already-verified behavior."
    - ".planning/REQUIREMENTS.md's CARDS-01/CARDS-02/CARDS-03 checklist lines and Traceability table rows reflect the actual, current, verified state of the codebase at the time of the edit — never a premature 'Complete' unbacked by passing verification."
    - "The code-level condition WINDOWS.md entry #6 describes (no per-card sentence-count signal exists anywhere) no longer holds once Task 1 lands — closing the ledger entry itself is the standard post-execution verification workflow's job, not a task in this plan."
    - "No prisma/schema.prisma change — the `_count` aggregate is a query-level Prisma select addition against the already-existing Sentence relation, not a schema/migration change (Schema Push Detection Gate confirmed against the current schema file)."
  artifacts:
    - lib/cards-list.ts
    - lib/dto.ts
    - components/CardsClient.tsx
    - tests/cards-list.test.ts
    - e2e/cards-tab-switch-scroll.spec.ts
    - .planning/REQUIREMENTS.md
  key_links:
    - "lib/cards-list.ts:cardSelect._count -> lib/cards-list.ts:getCardsPage/getSentencesPage (sentenceCount mapping)"
    - "lib/dto.ts:CardDTO.sentenceCount -> components/CardsClient.tsx:renderCardRow (badge render, card.sentenceCount ?? card.sentences.length fallback)"
    - "components/CardsClient.tsx:switchView (getState/restoreStateFrom) -> e2e/cards-tab-switch-scroll.spec.ts (round-trip proof)"
    - "Task 1 + Task 2 <verify> outcomes + WINDOWS.md entry #1 'waived' status -> Task 3's .planning/REQUIREMENTS.md edit (gated, not unconditional)"
  prohibitions:
    - requirement_id: "CARDS-02"
      category: "transparency"
      status: "resolved"
      verification: "judgment"
      statement: "MUST NOT mark CARDS-01/CARDS-02/CARDS-03 'Complete' in REQUIREMENTS.md unless Task 1's and Task 2's <verify> steps actually passed in this execution session and WINDOWS.md entry #1 still reads 'waived' (not silently reclassified as 'fixed', and not reverted to 'open') — no overstating completion status in the project's own tracking docs."
---

<objective>
Close the one remaining open gap from `31-VERIFICATION.md`'s `gaps:` block (WINDOWS.md entry #6):
add a real, server-computed per-card sentence-count signal so a collapsed card row can show its
reading-practice/sentence count without ever loading the actual `Sentence` rows (ROADMAP Phase 31
Success Criterion 4, second clause). Also close `31-VERIFICATION.md`'s `behavior_unverified_items`
entry (D-08, tab-switch scroll/state preservation) with a durable automated regression spec — a
user-approved addition to this run's scope, since the reachability blocker (G-31-2) that previously
prevented even attempting this check is now fixed and re-verified. Finally, sync
`.planning/REQUIREMENTS.md`'s stale CARDS-01/CARDS-02/CARDS-03 status lines to the codebase's
actual, current, verified state — but only once the first two tasks' own verification evidence
exists, never as an assumed-true edit.

Purpose: this is the last piece of Phase 31 (Cards List Pagination & Virtualization) still open per
`31-VERIFICATION.md`'s `gaps_remaining`/`behavior_unverified_items` — closing it lets the phase
close with zero open WINDOWS.md items and zero unverified behaviors.

Output: a per-card sentence-count badge wired end-to-end (Prisma select -> DTO -> UI), a new
permanent Playwright regression spec proving D-08's round-trip scroll/state preservation, and an
accurate REQUIREMENTS.md.

<planning_notes>
- **API Coverage Decision Checkpoint:** this plan integrates no external API/SDK — confirmed by
  re-running the detector over this plan's actual scope (a Prisma query-select change, a UI badge,
  an e2e spec, and a markdown doc edit). Declaration written to
  `.planning/phases/31-cards-list-pagination-virtualization/COVERAGE.md`.
- **Assumption-Delta Architecture Checkpoint:** `gsd_run query assumption-delta scan "31" --json`
  returns `detected: true` on the ROADMAP's "paints its first rows in under a second" — a false
  positive (the word "second" is a time duration, not a singular->plural identity-model
  transition). This gap-closure plan (a count field + a badge + a test + a doc edit) introduces no
  new pluralization/optional/chosen transition either. Disposition: no-change.
- **Schema Push Detection Gate:** confirmed against the current `prisma/schema.prisma` — this plan
  adds `_count: { select: { sentences: true } }` to an existing Prisma `select`, a query-level
  change against the already-defined `Sentence` relation, not a schema/migration change. No
  `[BLOCKING]` schema-push task needed.
</planning_notes>
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/31-cards-list-pagination-virtualization/31-VERIFICATION.md
@.planning/phases/31-cards-list-pagination-virtualization/31-UAT.md
@lib/cards-list.ts
@lib/dto.ts
@components/CardsClient.tsx
@tests/cards-list.test.ts
@e2e/cards-sticky-header.spec.ts
@e2e/fixture.ts
@e2e/helpers/readers.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Per-card sentence-count signal — cardSelect._count, CardDTO.sentenceCount, renderCardRow badge</name>
  <files>lib/cards-list.ts, lib/dto.ts, components/CardsClient.tsx, tests/cards-list.test.ts</files>
  <read_first>
    lib/cards-list.ts (full file — cardSelect lines 18-33, getCardsPage lines 123-156,
    getSentencesPage lines 195-256); lib/dto.ts (CardDTO lines 54-69); components/CardsClient.tsx
    lines 1105-1170 (renderCardRow) and 1121-1133 (the header-right badge cluster);
    tests/cards-list.test.ts lines 32-52 (makeRow helper) and 353-369 (makeSentenceRow, which
    reuses makeRow for its nested card).
  </read_first>
  <action>
    In `lib/cards-list.ts`'s `cardSelect` object (currently lines 18-33), add
    `_count: { select: { sentences: true } }` immediately after the existing `review: true,` line,
    and extend the adjacent `// sentences: DROPPED...` comment to note this `_count` field is the
    replacement signal for CARDS-01's dropped rows (ROADMAP Success Criterion 4, second clause;
    WINDOWS.md entry #6). In `getCardsPage`'s card-mapping block (the `cards: CardDTO[] =
    page.map((c) => ({...}))` block, currently ending around line 153), add
    `sentenceCount: c._count.sentences,` alongside the existing `sentences: [],` line. In
    `getSentencesPage`'s nested-card mapping block (the `card: { ...s.card, ..., sentences: [] }`
    object inside the `sentences: (SentenceDTO & { card: CardDTO })[] = page.map(...)` block,
    currently ending around line 251), add `sentenceCount: s.card._count.sentences,` alongside its
    own `sentences: [],` line — the nested `card` goes through the same `cardSelect` via
    `include: { card: { select: cardSelect } }`, so `_count` is present there too.

    In `lib/dto.ts`'s `CardDTO` interface, add `sentenceCount?: number` immediately after
    `sentences: SentenceDTO[]`, with a one-line comment: populated only by list-query paths
    (`getCardsPage`/`getSentencesPage`'s nested card) that intentionally return `sentences: []`;
    every other `CardDTO` producer (`POST /api/cards`, `GET`/`PUT /api/cards/[id]`,
    `lib/study-cards.ts`) already returns the real `sentences` array and leaves this field
    `undefined` — consumers must read `card.sentenceCount ?? card.sentences.length`, never assume
    the field is always present. Keep it optional; do not make it required.

    In `components/CardsClient.tsx`'s `renderCardRow` function (currently starting at line 1105),
    compute `const sentenceCount = card.sentenceCount ?? card.sentences.length` as the function's
    first statement. Render a new badge inside the existing header-right cluster
    (`<div className="flex items-center gap-1">`, currently lines 1121-1133), positioned before the
    existing `card.review &&` reps badge, using the exact same styling as that badge
    (`className="text-xs text-muted mr-1"`) with text
    `` `${sentenceCount} sentence${sentenceCount !== 1 ? 's' : ''}` ``. Render it unconditionally —
    every card, including a genuine 0-sentence card, shows the badge; do not gate it behind
    `sentenceCount > 0`.

    In `tests/cards-list.test.ts`, add `_count: { sentences: 0 },` to the `makeRow()` helper's
    returned object (currently lines 34-52), placed immediately before the `...overrides` spread —
    every existing `makeRow`/`makeSentenceRow` call gets a safe default without individual updates.
    Then add three new tests: (a) under `describe('getCardsPage', ...)`, assert
    `callArgs.select._count` equals `{ select: { sentences: true } }` on the Prisma `findMany` call,
    and that a row built via `makeRow('1', { _count: { sentences: 3 } })` maps to
    `result.cards[0].sentenceCount === 3`; (b) a second test asserting a row built via
    `makeRow('2', { _count: { sentences: 0 } })` maps to `result.cards[0].sentenceCount === 0`
    (present and exactly `0`, never `undefined`, never omitted — this is the CARDS-01 'empty'
    edge-probe case); (c) under `describe('getSentencesPage', ...)`, assert a sentence row built via
    `makeSentenceRow('1', { card: makeRow('card-1', { _count: { sentences: 2 } }) })` maps to
    `result.sentences[0].card.sentenceCount === 2`.
  </action>
  <verify>
    <automated>npx vitest run tests/cards-list.test.ts</automated>
  </verify>
  <done>
    getCardsPage/getSentencesPage return a real, server-computed sentenceCount on every card,
    including exactly 0 for a 0-sentence card; renderCardRow shows a "N sentence(s)" badge on every
    card row without adding a new query for that card's Sentence rows.
  </done>
  <acceptance_criteria>
    - cardSelect includes `_count: { select: { sentences: true } }`
    - getCardsPage's returned cards each carry `sentenceCount: number` equal to the real per-card
      sentence count
    - getSentencesPage's nested card carries the same
    - CardDTO.sentenceCount is optional (`?:`), not required
    - renderCardRow renders the badge for every card, 0-sentence cards included, with no new Prisma
      query added
    - `npx vitest run tests/cards-list.test.ts` passes, including the 3 new cases
    - `npm run build` (Turbopack + TypeScript) and `npm run lint` both clean
  </acceptance_criteria>
  <reversibility rating="reversible">A Prisma select-field + DTO-field + UI-badge addition — reverting is a single revert commit, no data migration, no schema change.</reversibility>
</task>

<task type="auto">
  <name>Task 2: D-08 tab-switch scroll/state-preservation regression spec</name>
  <files>e2e/cards-tab-switch-scroll.spec.ts</files>
  <read_first>
    e2e/cards-sticky-header.spec.ts (full file — the pattern to mirror); e2e/fixture.ts (FIXTURE
    shape, `totalCards: 8`, 6 of 8 cards carry exactly 1 sentence each); e2e/helpers/readers.ts
    lines 24-50 (waitVisible/dumpUnrecognizedState signatures); e2e/freshness-fresh-paths.spec.ts
    lines 30-51 (registerRequestLog, the request-log dual-evidence convention this suite already
    uses); components/CardsClient.tsx lines 686-703 (switchView's getState/restoreStateFrom pair),
    lines 795-816 (confirms no useEffect refetches on activeView change), lines 1390-1433 (the two
    Virtuoso instances + the two toggle buttons' exact JSX/aria-pressed wiring).
  </read_first>
  <action>
    Create `e2e/cards-tab-switch-scroll.spec.ts`, mirroring `e2e/cards-sticky-header.spec.ts`'s
    structure and house conventions: `test.use({ viewport: { width: 390, height: 500 } })`,
    `resetToBaseline()` in `test.beforeEach`, `waitVisible`/`dumpUnrecognizedState` from
    `./helpers/readers` before any assertion on a locator that could be missing/hidden, `FIXTURE`
    imported from `./fixture` for the exact `` `Cards (${FIXTURE.totalCards})` `` accessible name.
    This closes `31-VERIFICATION.md`'s `behavior_unverified_items` entry for D-08 — the existing
    `cards-sticky-header.spec.ts` only proves the toggle is reachable/tappable, not that the
    round-trip scroll/state actually restores.

    Register a request log via `page.on('request', ...)` BEFORE `page.goto('/cards')`, recording
    each request's `new URL(req.url()).pathname` — a local, self-contained function in this file
    (mirroring `e2e/freshness-fresh-paths.spec.ts`'s `registerRequestLog` convention, not importing
    it, since that helper is not exported from that file).

    Test body: navigate to `/cards`, `waitForLoadState('networkidle')`. Scroll the Cards tab's
    Vocabulary group via `page.mouse.wheel(0, 300)`, settle with a short `waitForTimeout`, read
    `const cardsScroll = await page.evaluate(() => window.scrollY)`, assert `cardsScroll > 0`
    (sanity — proves the scroll actually happened, avoiding a vacuous 0-to-0 comparison later).
    Click the `page.getByRole('button', { name: 'Reading practice' })` toggle and assert its
    `aria-pressed` attribute becomes `'true'`; wait for `networkidle` so the lazy first-visit fetch
    resolves. Scroll Reading Practice to a DIFFERENT depth via `page.mouse.wheel(0, 150)`
    (deliberately a different delta from the Cards scroll), settle, read
    `const readingScroll = await page.evaluate(() => window.scrollY)`, assert `readingScroll > 0`.
    Switch back to Cards (click `page.getByRole('button', { name: \`Cards (${FIXTURE.totalCards})\` })`,
    assert `aria-pressed` becomes `'true'`), and assert
    `(await page.evaluate(() => window.scrollY)) === cardsScroll` exactly — the core D-08
    regression check. Switch to Reading Practice again (click, assert `aria-pressed` `'true'`), and
    assert `(await page.evaluate(() => window.scrollY)) === readingScroll` exactly.

    Finally assert against the request log: exactly one logged request with
    `pathname === '/api/cards/sentences'` across the whole test (the one lazy first-visit fetch —
    proves no re-fetch on the second Reading Practice visit), and zero logged requests with
    `pathname === '/api/cards'` at any point (the RSC page's own initial data arrives server-side,
    never via a client `GET /api/cards` call in this flow — proves neither tab switch re-fetches the
    Cards page-1 data).
  </action>
  <verify>
    <automated>npx playwright test e2e/cards-tab-switch-scroll.spec.ts --project=chromium</automated>
  </verify>
  <done>
    A new, permanent Playwright spec exists proving D-08's round-trip scroll/state preservation —
    confirmed to FAIL when `components/CardsClient.tsx`'s `getState`/`restoreStateFrom` calls in
    `switchView` are temporarily reverted to a no-op (fail-first proof, matching this suite's own
    established discipline per 31-04/31-05's precedent), then confirmed to PASS against the real
    code before committing — closing `31-VERIFICATION.md`'s `behavior_unverified_items` entry.
  </done>
  <acceptance_criteria>
    - e2e/cards-tab-switch-scroll.spec.ts exists and runs as part of the e2e suite (not
      skipped/xfail)
    - The spec performs 4 view-toggle actions: Cards -> Reading Practice -> Cards -> Reading
      Practice, scrolling both Cards and Reading Practice to distinct, non-zero depths
    - Asserts window.scrollY is restored EXACTLY (not merely "greater than 0" or "close to") on both
      return trips
    - Asserts at most one `/api/cards/sentences` request and zero `/api/cards` page requests occur
      across the whole test
    - The spec was confirmed to FAIL against a temporarily-reverted getState/restoreStateFrom before
      being confirmed to pass against the real code (fail-first proof, not asserted-but-untested)
    - `npx playwright test e2e/cards-tab-switch-scroll.spec.ts --project=chromium` passes
    - `npx playwright test e2e/cards-sticky-header.spec.ts --project=chromium` still passes (no
      regression)
  </acceptance_criteria>
  <reversibility rating="reversible">A new, additive Playwright spec file — deleting it fully reverts, no other code touched.</reversibility>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS.md doc sync — CARDS-01/02/03 status</name>
  <files>.planning/REQUIREMENTS.md</files>
  <precondition>WINDOWS.md entry #1 (keyboard/screen-reader reachability) status reads "waived" — `grep -n '^| 1 | 31 |' .planning/WINDOWS.md` shows `| waived |` in the status column, not `| open |`.</precondition>
  <read_first>
    .planning/REQUIREMENTS.md (full file — checklist lines 20-24 under "### Cards List Performance
    (P3.2)", Traceability table lines 76-98); .planning/WINDOWS.md (entries 1 and 6); this plan's
    own Task 1 and Task 2 outcomes (must both have passed their `<verify>` commands in this
    execution session before this task runs).
  </read_first>
  <action>
    Only after Task 1's and Task 2's `<verify>` commands both pass in this execution session, and
    after confirming the precondition above still holds: read the current
    `.planning/REQUIREMENTS.md` fresh (do not rely on a cached/remembered version — its exact
    current text may have drifted since this plan was authored). Using the `Edit` tool (scoped
    replacement only — never `Write` a full-file replacement of `REQUIREMENTS.md`), make exactly
    two classes of edit: (1) under the `### Cards List Performance (P3.2)` heading, flip each of the
    three checklist lines from `- [ ] **CARDS-0N**: ...` to `- [x] **CARDS-0N**: ...` for N in
    1, 2, 3 — the text after the colon is unchanged, only the checkbox flips; (2) in the
    `## Traceability` table, change the `Status` column for the `CARDS-01`, `CARDS-02`, and
    `CARDS-03` rows (their current values as of this plan's authoring were `Gaps Found`, `Pending`,
    `Gaps Found` respectively — re-confirm the actual current values at execution time, do not
    assume) to `Complete`. Do not touch any other row in either section (`PERCEPT-*`, `LAYOUT-01`,
    `STUDY-*`, `VERS-*`, `REGION-01`, `LOCAL-*`, `OFFLINE-*` all stay exactly as they are). Do not
    touch `.planning/ROADMAP.md` — marking Phase 31 itself complete in the roadmap/STATE.md is the
    execute/verify workflow's responsibility, not this doc-sync task's.

    If, at execution time, Task 1 or Task 2 did not actually pass verification, or WINDOWS.md entry
    #1 no longer reads `waived`, leave the corresponding REQUIREMENTS.md line(s) unchanged and
    record exactly why in this plan's SUMMARY.md instead of writing a status that does not match
    reality (see must_haves.prohibitions — no overstating completion status).
  </action>
  <verify>
    <automated>git diff --stat .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    REQUIREMENTS.md's CARDS-01/02/03 lines accurately reflect the real, currently-verified state of
    the codebase — written only after, and contingent on, the actual verification evidence (Task 1's
    tests, Task 2's e2e spec, WINDOWS.md's waived status) existing.
  </done>
  <acceptance_criteria>
    - Exactly 3 checkbox flips (`- [ ]` -> `- [x]`) under Cards List Performance (P3.2), for
      CARDS-01/02/03 only
    - Exactly 3 Traceability table Status-cell edits, for the CARDS-01/02/03 rows only, to
      `Complete`
    - No other REQUIREMENTS.md line touched (`git diff .planning/REQUIREMENTS.md` shows only these
      6 changed lines)
    - Edit tool used (scoped replacement), not Write
    - This task only ran after confirming Task 1 + Task 2 verify passed and WINDOWS.md entry #1
      reads `waived`
  </acceptance_criteria>
  <reversibility rating="reversible">A markdown status edit — reverting is a one-line git revert with no code or data impact.</reversibility>
</task>

</tasks>

## Artifacts This Plan Produces

Every symbol this plan creates or adds (new files noted; all others are additions to existing files):

- `lib/cards-list.ts` — `cardSelect._count: { select: { sentences: true } }` (new select field);
  `getCardsPage()`'s returned `CardDTO.sentenceCount` (new mapped field); `getSentencesPage()`'s
  nested `card.sentenceCount` (new mapped field)
- `lib/dto.ts` — `CardDTO.sentenceCount?: number` (new optional interface field)
- `components/CardsClient.tsx` — `renderCardRow`'s `sentenceCount` local const and its new
  "N sentence(s)" `<span>` badge in the header-right cluster (new render output, no new
  props/state/exports)
- `tests/cards-list.test.ts` — `makeRow()`'s new default `_count: { sentences: 0 }` field; 3 new
  `it(...)` test cases (2 under `describe('getCardsPage', ...)`, 1 under
  `describe('getSentencesPage', ...)`)
- `e2e/cards-tab-switch-scroll.spec.ts` — **new file**: one Playwright `test(...)` proving D-08's
  round-trip scroll/state preservation, with a local `registerRequestLog`-style request-log helper
  function
- `.planning/REQUIREMENTS.md` — 3 checkbox flips (CARDS-01/02/03) + 3 Traceability table Status-cell
  edits (no new symbols, existing lines updated)
- `.planning/phases/31-cards-list-pagination-virtualization/COVERAGE.md` — **new file**, written by
  the planner (not a task in this plan): API Coverage Decision Checkpoint declaration

<specless_probe_notes>
## Specless Probe Fallback — Disposition (no SPEC.md exists for this phase)

### Edge coverage (5 rows, all input `unresolved` — resolved here against this plan's actual scope)

| Requirement | Category | Disposition | Resolution |
|---|---|---|---|
| CARDS-01 | empty | **covered** -> `must_haves.truths` | A card with 0 sentences renders `sentenceCount: 0` end-to-end — directly relevant to Task 1's badge addition; a real, testable truth (see truths list above and Task 1's test (b)). |
| CARDS-01 | adjacency | **flagged assumption** | Cursor page-boundary adjacency (exact-touch merge/collide/separate) is untouched by this plan — no cursor/orderBy logic is modified. Existing coverage from 31-01's `tests/cards-list.test.ts` remains authoritative and unchanged. |
| CARDS-01 | ordering | **flagged assumption** | Deterministic `[{createdAt:'desc'},{id:'desc'}]` tie-break ordering is untouched by this plan — no `orderBy` clause is modified. Existing 31-01 coverage remains authoritative and unchanged. |
| CARDS-02 | unclassified | **covered** -> `must_haves.truths` | Resolved manually: the one CARDS-02-relevant edge this plan actually closes is D-08's tab-switch round-trip scroll/state preservation (Task 2's new e2e spec). All other CARDS-02 sub-behaviors (DOM-bounding, empty/loading states, keyboard/screen-reader accessibility) are unchanged/untouched by this plan — already addressed or formally waived (WINDOWS.md entry #1) in 31-01 through 31-05, not re-litigated here. |
| CARDS-03 | unclassified | **flagged assumption** | This plan's scope (sentence-count badge, D-08 e2e spec, REQUIREMENTS.md sync) does not touch CARDS-03 (search/lesson-filter query logic) at all — untouched, no new edge surfaced. Existing 31-02 coverage remains authoritative. |

No-silent-drop check: 5 probe rows in -> 2 authored as `must_haves.truths` + 3 authored as explicit
flagged assumptions above = 5. Equality holds.

### Prohibition recall (Stage 1 recall -> Stage 2 precision, no `## Prohibitions` section existed either)

Stage 1 over-produced routine-engineering candidates for CARDS-01/02/03 in this plan's narrow scope
(e.g. "must not mutate the loaded-groups array", "must not throw on a card with no lesson", "must
not regress the query's cost profile") — all DROPPED per Stage 2 (normal correctness/hygiene,
already owned by the edge-probe / `<acceptance_criteria>` / existing test suite, not a values/safety/
ethics concern). One genuine `transparency`-category item survived Stage 2 and is KEPT, authored
descriptor-less (no `check_*` scalars, per the fallback's `--auto` divergence — there is no human to
wire a mechanical check for a judgment call about documentation honesty) into
`must_haves.prohibitions` above: REQUIREMENTS.md must not claim CARDS-01/02/03 are "Complete" unless
the actual verification evidence exists at the time of the edit.

No canon-referral breadcrumb needed — the one privacy-adjacent candidate considered (does surfacing
a sentence count disclose anything new) was resolved as a STRIDE Information Disclosure item already
addressed in `<threat_model>` below (T-31-11, disposition: accept), not a values-category
prohibition, so it is not duplicated here.

Coverage: `{ applicable: 1, resolved: 1, unresolved: 0, byVerification: { test: 0, judgment: 1 } }`
</specless_probe_notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Authenticated client -> `GET /api/cards` / `GET /api/cards/sentences` | Unchanged from 31-01/31-03 — same `middleware.ts` HMAC-cookie gate; this plan adds a read-only aggregate field to the existing response shape, no new input surface, no new endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-11 | Information Disclosure | `lib/cards-list.ts` (`cardSelect._count`) | low | accept | The count is a scalar aggregate over `Sentence` rows the requesting (already-authenticated) session can already read in full via the existing `GET /api/cards/[id]`/Edit-sheet flow (T-31-05's disposition, 31-03-PLAN.md) — surfacing only the count via the list/collapsed-row response discloses strictly less than what is already reachable through an existing route. No new trust boundary crossed, no new query parameter, no user-controlled input involved in computing this field. |

No other threats introduced — Task 2 (a new e2e spec, test-only, no production code) and Task 3 (a
markdown documentation edit) touch no trust boundary at all.
</threat_model>

<verification>
Run in order after all three tasks land:

1. `npx vitest run tests/cards-list.test.ts` — new sentence-count assertions pass.
2. `npm test` — full unit suite stays green (300+ tests).
3. `npm run build` — Turbopack + TypeScript clean (confirms `CardDTO.sentenceCount?` compiles across
   every existing consumer).
4. `npm run lint` — 0 new errors.
5. `npx playwright test e2e/cards-tab-switch-scroll.spec.ts --project=chromium` — new D-08 spec
   passes.
6. `npx playwright test e2e/cards-sticky-header.spec.ts e2e/cards-search-clear.spec.ts e2e/smoke.spec.ts e2e/perf.spec.ts --project=chromium` —
   no regression to Phase 31's prior plans.
7. `git diff --stat .planning/REQUIREMENTS.md` — exactly the 6 described line changes, nothing else.
</verification>

<success_criteria>
- A collapsed (or expanded) card row shows an accurate "N sentence(s)" count, including 0, without
  the list query ever loading the real `Sentence` rows for that card (WINDOWS.md entry #6 closeable).
- D-08's tab-switch round-trip scroll/state preservation has a durable, fail-first-proven automated
  regression spec (31-VERIFICATION.md's `behavior_unverified_items` entry closeable).
- `.planning/REQUIREMENTS.md` accurately reflects CARDS-01/02/03's real, current, verified state —
  written only when that state is actually true, never assumed.
- Zero regressions to any of 31-01 through 31-05's already-shipped, already-verified behavior.
</success_criteria>

<output>
Create `.planning/phases/31-cards-list-pagination-virtualization/31-06-SUMMARY.md` when done
</output>
