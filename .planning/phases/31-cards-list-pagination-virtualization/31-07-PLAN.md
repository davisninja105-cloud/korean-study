---
phase: 31-cards-list-pagination-virtualization
plan: 07
type: execute
wave: 1
depends_on: ["31-06"]
files_modified:
  - app/api/cards/[id]/route.ts
  - components/CardEditor.tsx
  - components/CardsClient.tsx
  - tests/cards-id-route.test.ts
  - e2e/cards-edit-regression.spec.ts
  - .planning/WINDOWS.md
autonomous: true
gap_closure: true
requirements: [CARDS-01, CARDS-02, CARDS-03]

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Editing an existing card's sentences (text change + one added sentence) AND its type in a single CardEditor save updates an already-loaded Reading Practice row's displayed sentence text with no page reload (CR-01) — proven behaviorally, not by presence/wiring: the fix is verified by a real PUT round trip whose response sentence ids are stable for unchanged rows, not merely by the existence of the id-based patch code that shipped (and silently never worked) in 31-04."
    - "The same save relocates the card out of its old type-group bucket and into the new one, decrementing the old type's group-header count and incrementing the new type's group-header count in the same render pass, with no full-page reload (CR-02) — proven by reading the actual rendered 'N card(s)' header text before and after the save, not by asserting bumpGroupCount is merely called."
    - "The same save recomputes the 'N sentences' badge from the freshly-saved sentences array (CR-03), so a card that gained a sentence shows the new, larger count immediately after save with no reload — proven by reading the rendered badge text, not by inspecting merge()'s source for a sentenceCount key."
    - "A single new Playwright spec (e2e/cards-edit-regression.spec.ts) exercises all three of the above in one test, one CardEditor save, and was confirmed to FAIL against the pre-fix code before any fix landed, then confirmed to PASS against the fixed code — both runs' evidence (failing assertion / passing run) recorded in the plan's SUMMARY. This is a hard acceptance criterion per 31-VERIFICATION.md's gap entry, not ordinary test coverage: the prior fix for this exact code path (WR-03, commit 2ffeedb) was marked done on unit+lint+build alone with no behavioral test and shipped broken — that gap is CR-01 itself."
    - "PUT /api/cards/[id] preserves a Sentence's id across a save when the client echoes that id back unchanged (upsert-by-id), creates a fresh id only for entries with no id, and deletes only the ids genuinely absent from the incoming array — proven by a new PUT-route unit test in tests/cards-id-route.test.ts, not merely by reading the diff."
    - "Editing a card down to zero sentences (removing every sentence in CardEditor and saving) persists correctly — PUT accepts sentences: [] and returns 200 with an empty sentences array, no crash, no orphaned Reading Practice row for that card. [edge-probe CARDS-01/empty: covered]"
    - "A client-supplied sentence id that does not belong to the card being edited (a foreign/mismatched id) is never used to update that foreign row — the upsert path only ever matches ids already scoped to the target card's own existing-sentences query, and the Prisma update's own where clause is additionally scoped by both id and cardId (defense in depth) — proven by a dedicated unit test seeding two cards and attempting the cross-card id, per the threat_model's IDOR-shaped check."
    - "Cursor-boundary pagination behavior (equal/adjacent cursor values, stable ordering) is unaffected by this plan — lib/cards-list.ts is not modified by any task here, and 31-VERIFICATION.md SC1/SC3 already verified this behavior; not re-tested in this plan. [edge-probe CARDS-01/adjacency: covered by reference] [edge-probe CARDS-01/ordering: covered by reference]"
    - "After a type-changing edit, the card row appears exactly once in the virtualized list (in its new type-group bucket only, once expanded), with no duplicate render and no DOM growth from the fix itself — general CARDS-02 virtualization/overflow mechanics (DOM node bounding under scroll) are otherwise untouched by this plan and remain as previously verified in 31-VERIFICATION.md SC2. [edge-probe CARDS-02/unclassified: covered (germane part) / unresolved (general virtualization mechanics, out of this plan's scope, flagged assumption)]"
    - "During an active search, editing a result's sentence-count badge updates immediately via the same shared merge() fix used by the grouped view (CR-03's fix is written once in merge(), consumed by setGroups, setSearchResults, and the Reading Practice patch alike) — general CARDS-03 server-side search/filter query correctness (buildCardsWhere, debouncing) is untouched by this plan and remains as previously verified in 31-VERIFICATION.md SC3/SC4. [edge-probe CARDS-03/unclassified: covered (germane part) / unresolved (general search/filter query correctness, out of this plan's scope, flagged assumption)]"
    - "No UI-SPEC visual/copy contract changes: CR-01/02/03 are logic-only corrections to already-✅-covered UI states (31-UI-SPEC.md E1 'populated', E5 'populated') — badge markup, group-header markup, and Reading Practice row markup are byte-for-byte unchanged; only the data feeding them becomes correct. Nothing backstop/unresolved in UI-SPEC is germane to this plan's scope."
    - "WINDOWS.md entry #6 (per-card sentence-count signal) is marked fixed, and three new WINDOWS.md entries (one per CR-0N) are appended and then marked fixed once Task 1/Task 2's fixes are verified in this same plan — open_count reads 0 in WINDOWS.md's frontmatter tally after Task 3."
    - "npm test (full suite, including the new PUT-route assertions), npm run lint, and the existing /cards-touching e2e specs (cards-search-clear.spec.ts, cards-sticky-header.spec.ts, cards-tab-switch-scroll.spec.ts, perf.spec.ts, smoke.spec.ts) all stay green — zero regression to any previously-verified Phase 31 behavior."
    - ".planning/REQUIREMENTS.md's CARDS-01/CARDS-02/CARDS-03 status cells are NOT touched by any task in this plan — that determination belongs solely to the next /gsd-verify-phase re-verification pass, per this gap-closure's explicit instruction (two prior gap-closure rounds in this same phase's history prematurely marked these rows 'Complete' and had to be reverted)."
  artifacts:
    - app/api/cards/[id]/route.ts
    - components/CardEditor.tsx
    - components/CardsClient.tsx
    - tests/cards-id-route.test.ts
    - e2e/cards-edit-regression.spec.ts
    - .planning/WINDOWS.md
  key_links:
    - "components/CardEditor.tsx:handleSave (PUT payload) -> app/api/cards/[id]/route.ts:PUT (upsert-by-id sentenceOps) -> components/CardsClient.tsx:handleSave's Reading Practice patch (id-based match now actually matches)"
    - "components/CardsClient.tsx:handleSave's merge() (sentenceCount recompute) -> renderCardRow's 'N sentences' badge (components/CardsClient.tsx:1106)"
    - "components/CardsClient.tsx:handleSave's oldKeyEntry/newKey relocation -> setGroups + bumpGroupCount -> renderRow's group-header 'N card(s)' text (components/CardsClient.tsx:1190)"
    - "e2e/cards-edit-regression.spec.ts -> all three CR-0N fixes, one test, one CardEditor save (31-VERIFICATION.md gap.missing mandatory item)"
  prohibitions:
    - requirement_id: "CARDS-01"
      category: "safety"
      status: "unverified"
      flagged: true
      statement: "The upsert-by-id fix MUST NOT silently delete a card's sentences beyond what the user explicitly removed in the CardEditor UI due to an id-matching fault (e.g., treating a legitimate still-present sentence id as 'not found -> delete' because of a comparison bug) — sentence data loss must only ever be a direct, visible consequence of the user's own edit action, never a side effect of the id round-trip mechanism itself."
---

<objective>
Close the single remaining open gap from `31-VERIFICATION.md` (`gaps:` block, ROADMAP Success
Criterion 5's edit sub-clause): three independently-confirmed Critical defects (CR-01, CR-02, CR-03),
all rooted in `components/CardsClient.tsx`'s `handleSave`/`merge()` and the `PUT /api/cards/[id]`
route it calls, that make an ordinary "edit a card" action silently show wrong data to the user with
no error surfaced. Per `31-VERIFICATION.md`'s `gap.missing` list this closure is mandatory-with-proof:
a single new end-to-end Playwright spec must exercise all three defects in one CardEditor save, be
confirmed to FAIL against the pre-fix code first, then confirmed to PASS against the real fixed code
— the prior fix for this exact code path (WR-03, commit `2ffeedb`) was marked done on unit+lint+build
alone with no behavioral test and shipped broken anyway; that gap IS this phase's CR-01.

Purpose: this is the only artifact standing between Phase 31 and a clean re-verification pass. All
of Success Criteria 1-4 and SC5's non-edit clauses (add, delete, swipe-to-delete, tap-to-gloss,
group-collapse-toggle-itself) are already verified working and are explicitly NOT re-touched here.

Output: `PUT /api/cards/[id]` upserts sentences by id instead of blanket delete+recreate;
`CardEditor.tsx` round-trips each sentence's existing id in its save payload; `CardsClient.tsx`'s
`handleSave`/`merge()` relocates a type-changed card between group buckets (with correct
`bumpGroupCount` bookkeeping) and recomputes `sentenceCount` on every save; a new fail-first-then-green
regression spec proves all three fixes together; `tests/cards-id-route.test.ts` gains direct PUT-route
coverage (including the empty-sentences and cross-card-id edge cases); `WINDOWS.md`'s ledger is synced.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-cards-list-pagination-virtualization/31-VERIFICATION.md
@.planning/phases/31-cards-list-pagination-virtualization/31-REVIEW.md
@app/api/cards/[id]/route.ts
@components/CardEditor.tsx
@components/CardsClient.tsx
@tests/cards-id-route.test.ts
@e2e/cards-tab-switch-scroll.spec.ts
@e2e/cards-search-clear.spec.ts
@e2e/helpers/readers.ts
@e2e/fixture.ts
@e2e/seed.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Fail-first e2e regression spec + fix CR-01 (stable sentence ids across a save)</name>
  <files>e2e/cards-edit-regression.spec.ts, app/api/cards/[id]/route.ts, components/CardEditor.tsx, tests/cards-id-route.test.ts</files>
  <read_first>
    31-VERIFICATION.md lines 22-35 (the `gaps:` entry — CR-01/02/03 text, and the mandatory
    fail-first e2e spec requirement in `missing[3]`, which names commit `2ffeedb`/WR-03 as the prior
    failure mode this plan must not repeat). 31-REVIEW.md lines 50-112 (CR-01: exact root cause at
    `app/api/cards/[id]/route.ts:116-134` and the broken consumer at
    `components/CardsClient.tsx:928-945`, plus the review's own suggested upsert-by-id Prisma snippet
    at lines 91-111 — this task's fix should match that shape). `app/api/cards/[id]/route.ts` lines
    1-160 (current PUT handler, `sentencesInclude` const, existing WR-02 field validation you must
    not remove). `components/CardEditor.tsx` lines 8-13 (`LocalSentence.id` — empty string for new,
    unsaved sentences) and lines 73-97 (`handleSave`'s current PUT payload construction, which
    currently drops `id` entirely). `tests/cards-id-route.test.ts` in full (existing GET-only test
    file and its DB-fixture-setup pattern — env-var ordering, dynamic imports in `beforeAll`, temp
    SQLite file via `prisma migrate diff --from-empty`).
  </read_first>
  <behavior>
    - PUT with a sentences array containing one entry whose `id` matches an existing row for that
      card: that row's `Sentence.id` is unchanged in the response; its `korean`/`targetForm`/
      `translation`/`orderIndex` are updated to the new values.
    - PUT with a sentences array containing one entry with no `id` (or `id: ''`): a new `Sentence`
      row is created with a fresh id; existing rows not referenced by any incoming id are deleted.
    - PUT with `sentences: []` on a card that has existing sentences: all existing sentences are
      deleted, response returns 200 with `sentences: []`, no crash.
    - PUT with a sentences array containing an `id` belonging to a DIFFERENT card (seeded separately
      in the test): that foreign row is left completely untouched (not updated, not deleted); the
      entry is instead treated as "no matching existing id for this card" and creates a new row on
      the target card.
  </behavior>
  <action>
    Step 1 — write the spec first, confirm it fails: create `e2e/cards-edit-regression.spec.ts`
    following `e2e/cards-tab-switch-scroll.spec.ts`'s house conventions exactly (390x500 viewport via
    `test.use`, `resetToBaseline()` in `beforeEach`, `waitVisible`/`dumpUnrecognizedState` before any
    locator assertion that could be missing). Single test titled describing the CR-01/02/03 combined
    regression. Use the fixture's `학교` due card (front `학교`, back `school`, one sentence: korean
    `저는 매일 학교에 가요`, targetForm `학교`, translation `I go to school every day`) as the edit
    target — it is the only fixture card whose front/example-sentence pair contains `학교` uniquely
    identifiable via an exact-text locator (`page.getByText('학교', { exact: true })`), scoped to its
    row container via Playwright's `has:` locator option against `div.bg-surface-1.rounded-xl.shadow-sm.p-4`
    (avoids the ambiguity that `가다`'s example sentence also contains the substring `학교`). Sequence:
    (1) goto `/cards`, wait `networkidle`; assert the `Cards (8)` toggle and the `Vocabulary` group
    header (role button, name matching `/Vocabulary/`, text containing `8 card`) are visible; assert
    no `Grammar`-named button exists yet (`toHaveCount(0)`). (2) Within the `학교` row (the `has:`-scoped
    container above), assert its sentence-count text reads `1 sentence`. (3) Click the `Reading practice`
    toggle (`getByRole('button', { name: 'Reading practice' })`), wait `networkidle`; locate that
    card's Reading Practice row via `page.locator('div.cursor-pointer', { hasText: '학교 — school' })`
    and assert it `toContainText('저는 매일 학교에 가요')` (captures the PRE-edit text — this row must
    be already-loaded before the edit, exactly the "already-loaded Reading Practice row" scenario CR-01
    describes). (4) Click the `Cards` toggle to switch back. (5) Within the `학교` row, click its
    `Edit` button (`getByRole('button', { name: 'Edit' })` scoped to that row); wait for the Sheet
    (`getByRole('dialog')`) to be visible, then wait for the loaded sentence's korean input
    (`getByPlaceholder('Korean sentence (e.g. 저는 학교에 가요)')`, first) to actually hold the value
    `저는 매일 학교에 가요` (proves `GET /api/cards/[id]` resolved — not just the instant
    front/back/notes draft). (6) In the dialog: `selectOption('grammar')` on the type `<select>`;
    fill the first korean-sentence input with `저는 오늘도 학교에 가요` (still contains `학교`, so
    `targetForm` auto-fill stays valid, no warning triggered); click `+ Add sentence`; fill the
    SECOND korean-sentence input (`.nth(1)` of the same placeholder locator) with `학교가 정말 크네요`
    (targetForm auto-fills to `학교` again, already correct) and its translation input with
    `The school is really big`. Click `Save`; wait for the dialog to fully unmount
    (`getByRole('dialog')).toHaveCount(0)` — `Sheet.tsx` returns `null` when closed, so this is a
    real "save completed" signal, not a cosmetic hide). (7) CR-01 assertions first (so a
    CR-01-only-fixed intermediate state can be distinguished from a still-fully-broken one): click
    `Reading practice` again (second visit — no refetch per D-08, this is testing the in-memory
    patch); re-locate the same row via the same `학교 — school` locator; assert it now
    `toContainText('저는 오늘도 학교에 가요')` AND `not.toContainText('저는 매일 학교에 가요')`.
    (8) CR-02 assertions: click `Cards` to switch back; assert the `Vocabulary` header now contains
    `7 card` (down from 8) and a `Grammar`-named header button is now visible containing `1 card`.
    (9) CR-03 assertion: click the `Grammar` header to expand it (D-02: starts collapsed, but its
    `loaded` array is already populated by the CR-02 fix's optimistic insert, so no new network
    fetch fires); within the now-visible `학교` row, assert its type badge text is `grammar` and its
    sentence-count text now reads `2 sentences`. Run `npx playwright test
    e2e/cards-edit-regression.spec.ts --project=chromium` against the CURRENT, unmodified code and
    confirm it fails — record the exact failing assertion/line and Playwright's output verbatim for
    the SUMMARY (this is the mandatory RED baseline per 31-VERIFICATION.md's gap; do not skip
    capturing it).

    Step 2 — fix CR-01 root cause in `app/api/cards/[id]/route.ts`'s PUT handler: inside the
    `Array.isArray(data.sentences)` branch (currently lines ~116-131), BEFORE building `sentenceOps`,
    add an awaited query `const existing = await prisma.sentence.findMany({ where: { cardId: id },
    select: { id: true } })` and `const existingIds = new Set(existing.map((s) => s.id))`. Type the
    incoming payload as `const incoming = data.sentences as { id?: string; korean: string; targetForm:
    string; translation: string }[]`. Build `const keepIds = new Set(incoming.filter((s) => s.id &&
    existingIds.has(s.id)).map((s) => s.id as string))`. Replace the current `sentenceData.map(...)`+
    `deleteMany`+`create-all` construction with: `prisma.sentence.deleteMany({ where: { cardId: id,
    id: { notIn: [...keepIds] } } })` as the first op, followed by, for each `(s, i)` of `incoming`:
    when `s.id && existingIds.has(s.id)` → `prisma.sentence.update({ where: { id: s.id, cardId: id },
    data: { korean: s.korean ?? '', targetForm: s.targetForm ?? '', translation: s.translation ?? '',
    orderIndex: i } })` — the compound `where` (both `id` AND `cardId`) is the IDOR-shaped scoping
    required by this plan's threat_model, do not narrow it to `{ id: s.id }` alone even though
    `existingIds` is already cardId-scoped (defense in depth); otherwise → `prisma.sentence.create({
    data: { korean: s.korean ?? '', targetForm: s.targetForm ?? '', translation: s.translation ?? '',
    cardId: id, orderIndex: i } })`. Leave the `: []` else-branch (no `sentences` key in the payload
    at all) completely unchanged — no new query when the client isn't touching sentences. Leave the
    `$transaction([cardUpdate, ...sentenceOps])` call and everything after it (the final
    `findUniqueOrThrow` + response) unchanged.

    Step 3 — fix the payload side in `components/CardEditor.tsx`'s `handleSave` (~line 84-87): change
    the sentences-array `.map(({ korean, targetForm, translation }) => ({ korean, targetForm,
    translation }))` to `.map(({ id, korean, targetForm, translation }) => ({ id, korean, targetForm,
    translation }))` so every entry's existing `id` (or empty string for a never-saved sentence) is
    round-tripped to the server — this is the payload half of the fix; without it the server has
    nothing to upsert against.

    Step 4 — extend `tests/cards-id-route.test.ts` with a new `describe('PUT /api/cards/[id]')`
    block, importing `PUT` alongside the existing `GET` import from `../app/api/cards/[id]/route`
    (add it to the same dynamic-import line in `beforeAll`). Cover, against the same real temp-SQLite
    fixture card already set up in that file's `beforeAll` (plus one freshly-seeded second card for
    the cross-card case): the four `<behavior>` cases listed above. Each PUT call constructs its own
    `fakeParams(cardId)` and a `NextRequest`-shaped stub carrying a real `.json()` method (mirror
    however `GET`'s test stubs `NextRequest` today, extended with a body) — do not import a real
    `NextRequest` constructor if the existing GET tests avoid one; keep the same lightweight stubbing
    style already established in this file.

    Step 5 — re-run `npx playwright test e2e/cards-edit-regression.spec.ts --project=chromium`. Do
    NOT expect a full green run yet — expect it to now pass steps 1-7 (through the CR-01 assertions)
    and fail at step 8 (the CR-02 `Vocabulary` header count assertion), since CR-02/CR-03 are fixed in
    Task 2. Record this partial-progress result (which specific assertion now passes vs. still fails)
    in the SUMMARY as the tracer's "first fix verified end-to-end" evidence.
  </action>
  <reversibility rating="reversible">Upsert-by-id vs. a full-refetch fallback were both viable per 31-REVIEW.md; this changes only query shape and payload shape, no schema/migration, trivially revertable to blanket delete+recreate.</reversibility>
  <verify>
    <automated>npx playwright test e2e/cards-edit-regression.spec.ts --project=chromium; npx vitest run tests/cards-id-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/cards-edit-regression.spec.ts` exists, was run against pre-fix code and failed (captured
      in the SUMMARY with the exact failing line/assertion), then re-run after Steps 2-3 and now
      progresses through every CR-01-specific assertion (step 7 above) before failing at the CR-02
      assertion (step 8) — not failing earlier than step 7 and not fully green yet.
    - `npx vitest run tests/cards-id-route.test.ts` passes with the 4 new PUT assertions
      (id-preserved-on-update, new-row-created-for-no-id, row-deleted-when-omitted,
      empty-array-deletes-all) plus the cross-card foreign-id case, alongside the pre-existing 2 GET
      tests (6+ total in the file).
    - `app/api/cards/[id]/route.ts`'s PUT `update`/`delete` Prisma calls scope every sentence
      mutation by both `id` and `cardId` (grep confirms `cardId: id` appears in the same `where`
      object as any `id: s.id`/`id: { notIn` clause inside the PUT handler).
    - `components/CardEditor.tsx`'s sentences-array PUT payload includes each entry's `id` field
      (grep `{ id, korean, targetForm, translation }` in the `handleSave` map call).
  </acceptance_criteria>
  <done>CR-01's root cause is fixed in both the PUT route and the CardEditor payload; the mandatory e2e spec exists and its pre-fix RED baseline is captured; the spec's CR-01-specific assertions pass against the fixed code; PUT-route unit tests cover the upsert/create/delete/empty-array/cross-card-id behaviors directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix CR-02 (group-bucket relocation + count bookkeeping) and CR-03 (stale sentenceCount) — confirm the spec goes fully green</name>
  <files>components/CardsClient.tsx</files>
  <read_first>
    31-REVIEW.md lines 114-200 (CR-02's exact root cause at `components/CardsClient.tsx:892-917`
    plus its suggested fix at lines 138-161; CR-03's exact root cause at lines 892-908/1105-1106 plus
    its suggested fix at lines 191-199 — this task's fixes should match those shapes).
    `components/CardsClient.tsx` lines 884-978 (the full current `handleSave`, `merge()`, and
    `handleAdd` — `handleAdd` at ~949-978 is the CORRECT reference implementation for group-bucket
    placement + `bumpGroupCount` usage that `handleSave` must be brought in line with) and lines
    509-518 (`bumpGroupCount`'s definition) and 166-168 (`groupKeyForType`'s definition). 1105-1106
    (`renderCardRow`'s `card.sentenceCount ?? card.sentences.length` fallback that CR-03 exploits).
  </read_first>
  <behavior>
    - Editing a card's `type` from `vocabulary` to `grammar` (no other field changed): the card is
      removed from `groups.vocabulary.loaded`, inserted into `groups.grammar.loaded`,
      `groupCounts.byType` shows `vocabulary` count decremented by 1 and `grammar` count incremented
      by 1, `groupCounts.total` unchanged.
    - Editing a card's front/back/notes only (type unchanged): no bucket relocation, no
      `bumpGroupCount` call at all (a same-type resave must not touch counts).
    - Editing a card's sentences to add one (2 sentences total, was 1): `merge()`'s returned card has
      `sentenceCount: 2`, consumed identically by `setGroups`, `setSearchResults`, and the Reading
      Practice patch's `merge(row.card)` call (single shared fix, three call sites).
  </behavior>
  <action>
    In `handleSave` (components/CardsClient.tsx, ~884-947): first, fix CR-03 by adding
    `sentenceCount: (updated.sentences ?? []).length` as a new key in `merge()`'s returned object
    literal (alongside the existing `sentences: (...)` mapping) — since `merge()` is the single
    function every call site (`setGroups`, `setSearchResults`, `setReadingPractice`'s `merge(row.card)`)
    already uses, this one-line addition fixes the badge everywhere at once. Second, fix CR-02: before
    the `setGroups` call, compute `const oldKeyEntry = GROUP_KEYS.map((key) => ({ key, card:
    groups[key].loaded.find((c) => c.id === updated.id) })).find((e) => e.card)` and `const newKey =
    groupKeyForType(updated.type)`. Replace the `setGroups` block's current per-key `.map()`-in-place
    patch with a two-phase update: for every `key` in `GROUP_KEYS`, filter `updated.id` OUT of
    `next[key].loaded` (`next[key] = { ...next[key], loaded: next[key].loaded.filter((c) => c.id !==
    updated.id) }`); then, if `oldKeyEntry?.card` is truthy, prepend `merge(oldKeyEntry.card)` into
    `next[newKey].loaded`. After the `setGroups` call, call `bumpGroupCount(oldKeyEntry.card!.type,
    -1)` and `bumpGroupCount(updated.type, 1)` ONLY when `oldKeyEntry && oldKeyEntry.key !== newKey`
    (guard this with an `if` — a same-type resave, or a card whose current bucket lookup came up
    empty, must not call `bumpGroupCount` at all, mirroring the `<behavior>` block's second case).
    Leave the `setSearchResults` block and the Reading Practice patch block exactly as they are apart
    from picking up the `merge()` fix automatically — no other changes needed there. Re-run
    `npx playwright test e2e/cards-edit-regression.spec.ts --project=chromium` — expect ALL
    assertions (steps 1-9 from Task 1) to pass now; this is the full GREEN run. Record this alongside
    Task 1's RED and partial-progress runs in the SUMMARY (three data points total: pre-fix RED,
    post-CR-01-only partial pass, post-all-three-fixes full GREEN). Then run the full regression
    battery: `npm test` (every existing + the new PUT-route unit tests, zero regressions), `npm run
    lint` (zero new errors — the file already has zero errors per 31-VERIFICATION.md, keep it that
    way), and the existing `/cards`-touching e2e specs by name (not the whole suite, to keep this
    fast and targeted): `cards-search-clear.spec.ts`, `cards-sticky-header.spec.ts`,
    `cards-tab-switch-scroll.spec.ts`, `perf.spec.ts`, `smoke.spec.ts`.
  </action>
  <reversibility rating="reversible">Relocate-and-recount vs. leaving stale group state is a pure client-state bookkeeping fix; no migration, no contract change, trivially revertable.</reversibility>
  <verify>
    <automated>npx playwright test e2e/cards-edit-regression.spec.ts e2e/cards-search-clear.spec.ts e2e/cards-sticky-header.spec.ts e2e/cards-tab-switch-scroll.spec.ts e2e/perf.spec.ts e2e/smoke.spec.ts --project=chromium; npm test; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/cards-edit-regression.spec.ts --project=chromium` passes all
      assertions (full GREEN, all 9 steps from Task 1's description).
    - The five named existing `/cards`-touching e2e specs and the full `npm test`/`npm run lint`
      battery all stay green — zero regressions.
    - `components/CardsClient.tsx`'s `merge()` function body contains a `sentenceCount:` key deriving
      from `updated.sentences` (grep confirms `sentenceCount: (updated.sentences ?? []).length` or
      equivalent inside `handleSave`'s `merge` definition).
    - `components/CardsClient.tsx`'s `handleSave` calls `bumpGroupCount` at least twice conditionally
      (grep confirms two `bumpGroupCount(` call sites inside `handleSave`, distinct from the
      pre-existing calls inside `handleAdd`/`handleDelete`).
  </acceptance_criteria>
  <done>CR-02 and CR-03 are fixed; the mandatory e2e spec is fully green against the real fixed code (all three defects proven closed in one test); zero regression to any previously-passing unit or e2e suite.</done>
</task>

<task type="auto">
  <name>Task 3: WINDOWS.md ledger sync — close entry #6, file and close the three CR-0N entries</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    .planning/WINDOWS.md in full (current state — entry #6 still `open` despite 31-06 having closed
    its underlying gap; 31-VERIFICATION.md's "Anti-Patterns Found" section, lines 176-186, explicitly
    flags both actions this task performs). 31-VERIFICATION.md lines 22-35 (the CR-01/02/03 text to
    source each new entry's `--description` from).
  </read_first>
  <action>
    Run `gsd-tools windows fixed 6` (via this project's standard `gsd_run` wrapper resolution, same
    launcher pattern used elsewhere in this workflow) to close the stale sentence-count-signal entry
    — 31-06 already closed the underlying gap in the codebase; this syncs the ledger's `status` field
    only, no code change. Then file three new entries, one per defect, each via `gsd-tools windows
    append --kind deviation --phase 31 --file <path> --description "<text>"`: (1) CR-01 — `--file
    app/api/cards/[id]/route.ts`, description summarizing "PUT sentence replace-all regenerated every
    Sentence.id on every save, breaking CardsClient's id-based Reading Practice patch — fixed via
    upsert-by-id"; (2) CR-02 — `--file components/CardsClient.tsx`, description summarizing
    "handleSave never relocated a type-changed card between group buckets or called bumpGroupCount —
    fixed"; (3) CR-03 — `--file components/CardsClient.tsx`, description summarizing "handleSave's
    merge() never recomputed sentenceCount after a sentence add/remove, badge showed stale count —
    fixed". Capture each append call's returned new entry id, then immediately mark each `fixed` via
    `gsd-tools windows fixed <id>` — append-then-fix in the same plan is acceptable here specifically
    because Task 1/Task 2's fixes already landed and were verified (by their own `<verify>` runs)
    before this task executes.
  </action>
  <verify>
    <automated>cat .planning/WINDOWS.md | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/WINDOWS.md`'s YAML frontmatter shows `open_count: 0`.
    - Entry #6's `status` is `fixed` with a non-null `resolved_at`.
    - Exactly three new entries exist (one per CR-0N), each `status: fixed` with a non-null
      `resolved_at` and a `description` naming the specific defect.
    - `.planning/REQUIREMENTS.md` is NOT modified by this task (grep the git diff for this plan's
      commit — `.planning/REQUIREMENTS.md` must not appear).
  </acceptance_criteria>
  <done>WINDOWS.md accurately reflects zero open defects for Phase 31 as of this plan's completion; REQUIREMENTS.md's CARDS-0x status cells are untouched, left for the next /gsd-verify-phase pass to adjudicate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (CardEditor) → `PUT /api/cards/[id]` | Authenticated (existing `middleware.ts` HMAC cookie gate, unchanged) but still untrusted input — the client now echoes back sentence `id` values that this plan's fix uses to decide update-vs-create-vs-delete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-12 | Tampering (IDOR-shaped) | `app/api/cards/[id]/route.ts` PUT — upsert-by-id sentence ops | high | mitigate | A client-supplied sentence `id` is only ever eligible for `update` when it appears in `existingIds`, a set computed from `prisma.sentence.findMany({ where: { cardId: id } })` — i.e. scoped to the target card's OWN sentences only, never a global id lookup. The `update` call's own `where` is additionally, redundantly scoped by both `id` AND `cardId` (defense in depth, matches 31-REVIEW.md's suggested fix). A foreign card's sentence id sent in the payload falls through to the `create` branch (creates a harmless new row on the target card) rather than mutating the foreign row. Proven by Task 1's dedicated cross-card unit test, not merely asserted. |
| T-31-13 | Tampering (data-loss-shaped) | `app/api/cards/[id]/route.ts` PUT — `deleteMany({ id: { notIn: [...keepIds] } })` | medium | mitigate | An id-matching bug in `keepIds` construction could over-delete sentences the user never removed. Mitigated by scoping `keepIds` from the same cardId-scoped `existingIds` set (never a global set) and by Task 1's empty-array/foreign-id unit tests, which would fail if `notIn` matched more rows than intended. See also `must_haves.prohibitions` (CARDS-01/safety). |
| T-31-14 | Elevation of Privilege | New WINDOWS.md CLI invocations (`gsd-tools windows append/fixed`) | low | accept | Local-only CLI tooling operating on a project-internal markdown ledger, not a runtime/user-facing surface; no auth boundary crosses here. |
| T-31-15 | Information Disclosure | None — no new error paths, no new response fields | low | accept | This plan adds no new API surface (same PUT endpoint, same response shape) and no new logged data; existing WR-02 disclosure posture (generic 500, server-side `console.error` only) is unchanged. |

No new unauthenticated surface is introduced — this plan touches only the existing `PUT /api/cards/[id]` route (already behind `middleware.ts`'s HMAC cookie gate) and client-side component logic. Single-shared-password app; no per-user access control model applies.
</threat_model>

<verification>
Run in order: (1) `npx vitest run tests/cards-id-route.test.ts` — new PUT-route behavioral proof.
(2) `npx playwright test e2e/cards-edit-regression.spec.ts --project=chromium` — the mandatory
combined CR-01/02/03 regression, must be fully green after Task 2. (3) `npx playwright test
e2e/cards-search-clear.spec.ts e2e/cards-sticky-header.spec.ts e2e/cards-tab-switch-scroll.spec.ts
e2e/perf.spec.ts e2e/smoke.spec.ts --project=chromium` — zero regression to previously-verified
Phase 31 behavior. (4) `npm test` — full unit suite, zero regressions. (5) `npm run lint` — zero new
errors. (6) `cat .planning/WINDOWS.md` — confirm `open_count: 0`. (7) `git diff --stat` on this
plan's commit — confirm `.planning/REQUIREMENTS.md` does NOT appear in the changed-files list.
</verification>

<success_criteria>
- The mandatory e2e spec (`e2e/cards-edit-regression.spec.ts`) exists, was confirmed to FAIL against
  pre-fix code (evidence in SUMMARY), and passes fully against the fixed code.
- CR-01, CR-02, and CR-03 are each fixed at their documented root cause (not papered over at a
  downstream symptom) and each has direct automated proof (unit test for CR-01's route behavior, the
  combined e2e spec for all three end-to-end).
- Zero regression: `npm test`, `npm run lint`, and every previously-green `/cards`-touching e2e spec
  stay green.
- `WINDOWS.md` open_count is 0; `.planning/REQUIREMENTS.md` is untouched by this plan.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New:
- `e2e/cards-edit-regression.spec.ts` — the mandatory single combined CR-01/02/03 regression spec (Task 1).

Modified (no new exported symbols; existing functions gain fixed logic in place):
- `app/api/cards/[id]/route.ts` — PUT handler's `sentenceOps` construction (CR-01 fix: upsert-by-id instead of blanket delete+recreate); adds one new awaited query (`prisma.sentence.findMany` for `existingIds`), no new exports.
- `components/CardEditor.tsx` — `handleSave`'s PUT payload now includes each sentence's `id` field; no new exports, `LocalSentence`/`Card`/`Props` types unchanged.
- `components/CardsClient.tsx` — `handleSave`'s `merge()` gains a `sentenceCount` key (CR-03); `handleSave`'s `setGroups` block gains bucket-relocation logic plus two new conditional `bumpGroupCount` calls (CR-02). No new exported symbols, no new component props.
- `tests/cards-id-route.test.ts` — new `describe('PUT /api/cards/[id]')` block with 5 new test cases (4 `<behavior>` cases + cross-card-id case).
- `.planning/WINDOWS.md` — entry #6 closed; 3 new entries appended and closed (via `gsd-tools windows` CLI, not direct edit).

Explicitly NOT modified: `.planning/REQUIREMENTS.md`, `lib/cards-list.ts`, `lib/dto.ts`, `app/api/cards/route.ts`, `app/api/cards/sentences/route.ts` (no pagination/query-shape changes in this plan).
</artifacts_produced>

<output>
Create `.planning/phases/31-cards-list-pagination-virtualization/31-07-SUMMARY.md` when done — must
include, verbatim, (a) the exact failing assertion/output from Task 1's pre-fix RED run, (b) the
specific assertion Task 1's post-CR-01-only partial run reached before failing, and (c) confirmation
of Task 2's full GREEN run — per 31-VERIFICATION.md's explicit "record both runs' results in the
SUMMARY" mandate (this plan records three: RED, partial, GREEN).
</output>
