---
phase: 20-extraction-pipeline-hardening
plan: 2
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - package.json
  - package-lock.json
  - lib/extract-cards.ts
  - tests/extract-cards.test.ts
  - CLAUDE.md
autonomous: true
requirements: [EXTRACT-01]
must_haves:
  truths:
    - "extractCardsFromNotes passes zodOutputFormat(ExtractionSchema) via output_config.format on the existing stream call and returns cards from message.parsed_output on the happy path — no regex/text parsing (EXTRACT-01)"
    - "A stream.on('text') accumulator is registered BEFORE awaiting finalMessage(), and a finalMessage() rejection with non-empty accumulated text falls back to parseExtractionResponse (truncation salvage reachable)"
    - "The wire schema keeps additionalProperties:false on every object, sentences minItems === 1, and notes optional — and stays permissive everywhere else (no client-side whole-response throw risk from per-card-droppable constraints)"
    - "Happy-path object input and salvaged text input produce identical normalized output through the shared normalizeExtractedCards pipeline"
    - "model claude-opus-4-8, max_tokens 32000, thinking adaptive, and the extractCardsFromNotes signature are all unchanged (caller compatibility: lib/sync.ts, scripts/local-resync.mts)"
  artifacts:
    - path: "package.json"
      provides: "zod promoted to explicit dependency"
      contains: "\"zod\""
    - path: "lib/extract-cards.ts"
      provides: "ExtractionSchema export + structured-outputs call with salvage branching"
      exports: ["ExtractionSchema"]
      contains: "output_config"
    - path: "tests/extract-cards.test.ts"
      provides: "schema-shape snapshot, SDK parse-throw drift guard, happy-path/text-path equivalence tests"
      contains: "schema shape (EXTRACT-01)"
  key_links:
    - from: "lib/extract-cards.ts:extractCardsFromNotes"
      to: "lib/extract-cards.ts:ExtractionSchema"
      via: "output_config.format: zodOutputFormat(ExtractionSchema)"
      pattern: "zodOutputFormat\\(ExtractionSchema\\)"
    - from: "lib/extract-cards.ts:extractCardsFromNotes (catch block)"
      to: "lib/extract-cards.ts:parseExtractionResponse"
      via: "salvage fallback on AnthropicError with accumulated rawText"
      pattern: "parseExtractionResponse\\(rawText"
    - from: "lib/extract-cards.ts:extractCardsFromNotes (happy path)"
      to: "lib/extract-cards.ts:normalizeExtractedCards"
      via: "normalizeExtractedCards(message.parsed_output.cards, deckSet)"
      pattern: "parsed_output"
---

<objective>
Migrate `extractCardsFromNotes` to native structured outputs (EXTRACT-01): add `zod` as an explicit dependency, define the permissive `ExtractionSchema`, pass it via `output_config.format` + `zodOutputFormat()` on the existing stream call, read `message.parsed_output` on the happy path, and reach the Plan-01 salvage parser through a try/catch with a pre-registered `stream.on('text')` accumulator. Refresh CLAUDE.md's extraction section.

Purpose: replaces "ask for JSON in prose and hope" with server-side constrained decoding — the response is schema-valid by construction except for truncation/refusal, which the salvage fallback (Plan 01) covers.

Output: zod in `package.json`, `ExtractionSchema` export, rewritten `extractCardsFromNotes` branching, EXTRACT-01 tests (schema shape, SDK-throw drift guard, path equivalence), updated CLAUDE.md.
</objective>

## Artifacts this phase produces

See the phase-wide artifacts table in `20-01-PLAN.md`. This plan's contributions:

| Symbol | Kind | Notes |
|--------|------|-------|
| `ExtractionSchema` | new zod-schema export, `lib/extract-cards.ts` | `z.object({ cards: z.array(ExtractedCardSchema) })` — exported for the schema-shape/drift tests |
| `ExtractedSentenceSchema`, `ExtractedCardSchema` | module-private zod schemas | Deliberately permissive (see Task 1); NOT exported |
| `extractCardsFromNotes` | rewritten export, same signature | `output_config.format`, `on('text')` accumulator, try/catch salvage branching |
| `zod` `^4.3.6` | `package.json` dependency | Promoted from transitive; `@anthropic-ai/sdk` stays at `^0.80.0` |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/20-extraction-pipeline-hardening/20-RESEARCH.md
@.planning/phases/20-extraction-pipeline-hardening/20-PATTERNS.md
@.planning/phases/20-extraction-pipeline-hardening/20-VALIDATION.md
@.planning/phases/20-extraction-pipeline-hardening/20-01-SUMMARY.md
@lib/extract-cards.ts
@tests/extract-cards.test.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add zod dependency, define ExtractionSchema, add schema-shape + SDK-parse-throw drift-guard tests (EXTRACT-01)</name>
  <files>package.json, package-lock.json, lib/extract-cards.ts, tests/extract-cards.test.ts</files>
  <read_first>
    - .planning/phases/20-extraction-pipeline-hardening/20-RESEARCH.md §"Recommended Zod Schema (Question A)" (exact schema + design rules), §"SDK Behavior Findings" items 1-5 (throw semantics, keyword whitelist, minItems retention, optional handling), §"Common Pitfalls" 2/3/6
    - lib/extract-cards.ts (top of file: current imports + ExtractedCard/ExtractedSentence interfaces)
    - package.json (dependencies block — alphabetized, caret ranges)
  </read_first>
  <behavior>
    - Test (schema shape): `zodOutputFormat(ExtractionSchema).schema` — root is a wrapper object whose only property is a cards array; EVERY object node (root, card, and any `$defs` entries) carries `additionalProperties: false`; the sentences array keeps `minItems: 1`; `notes` is absent from the card object's `required` list
    - Test (drift guard): `zodOutputFormat(ExtractionSchema).parse(truncatedText)` throws `AnthropicError` for a mid-card truncated wrapper string
    - Test (drift guard): `zodOutputFormat(ExtractionSchema).parse(validJsonViolatingSchema)` — e.g. a card with `sentences: []` — throws `AnthropicError` (client-side zod enforces minItems even though the branching never relies on it)
    - Test: parse of a valid wrapper string returns the data with unknown extra keys stripped (zod v4 strips unknown keys — documents the SDK behavior the pipeline sees)
  </behavior>
  <action>
    1. Run `npm install zod@^4.3.6`. This adds `"zod": "^4.3.6"` to `dependencies` in alphabetical position (after `ts-fsrs`) and updates `package-lock.json`. Note: `postinstall` runs `prisma generate` — expected, not an error. Do NOT upgrade `@anthropic-ai/sdk` (RESEARCH.md Pitfall 6: latest 0.110.0 may change the transform/parse semantics this design is verified against; keep `^0.80.0`).

    2. In lib/extract-cards.ts, add imports: `zodOutputFormat` from `@anthropic-ai/sdk/helpers/zod` and `z` from `zod`. Define module-level schemas exactly per RESEARCH.md §Recommended Zod Schema: module-private `ExtractedSentenceSchema` (korean, targetForm, translation — all plain `z.string()`), module-private `ExtractedCardSchema` (type as `z.enum` of vocabulary/grammar/phrase; front/back `z.string()`; `notes: z.string().optional()`; `distractors: z.array(z.string())`; `sentences: z.array(ExtractedSentenceSchema).min(1)`; `components: z.array(z.string())`), and EXPORTED `ExtractionSchema = z.object({ cards: z.array(ExtractedCardSchema) })`. Tag the block "EXTRACT-01" and document WHY it is deliberately permissive: no array max/length constraints and no string min-length anywhere — those are demoted to description-only hints server-side while client-side zod would throw for the WHOLE response on a single violating card, degrading fifty good cards to salvage quality (Pitfall 2). Only `sentences` minItems 1 is a real server-enforced constraint. "EXACTLY 3 distractors" remains prompt-side + the existing post-hoc slice in `normalizeExtractedCards`. Keep the exported `ExtractedCard`/`ExtractedSentence` TypeScript interfaces byte-identical — the zod schemas are additive, not replacements.

    3. In tests/extract-cards.test.ts, add `describe('schema shape (EXTRACT-01)')` implementing the behavior block. New imports: `ExtractionSchema` from `../lib/extract-cards`, `zodOutputFormat` from `@anthropic-ai/sdk/helpers/zod`, `AnthropicError` from `@anthropic-ai/sdk`. For the additionalProperties assertion, walk the generated schema's object nodes including `$defs` (the sentence object becomes a `$defs` ref per the empirically-generated wire schema in RESEARCH.md). These tests guard against SDK-upgrade drift: if a future SDK version stops throwing on truncation or starts keeping enum server-side, this suite fails visibly instead of silently breaking the branching design. No API call, no API key needed.
  </action>
  <verify>
    <automated>npx vitest run tests/extract-cards.test.ts && node -e "const p=require('./package.json'); if(!p.dependencies.zod) { console.error('zod missing from dependencies'); process.exit(1) }; if(p.dependencies['@anthropic-ai/sdk'] !== '^0.80.0') { console.error('sdk version drifted'); process.exit(1) }"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contain `"zod": "^4.3.6"` and `"@anthropic-ai/sdk": "^0.80.0"` (unchanged)
    - All schema-shape and drift-guard tests in the behavior block pass with no network access
    - `grep -c 'export const ExtractionSchema' lib/extract-cards.ts` returns 1
    - No `.max(` and no `.length(` call appears on any of the three zod schema definitions (source assertion — permissiveness rule)
    - Full extract-cards test file green
  </acceptance_criteria>
  <done>zod is an explicit dependency, ExtractionSchema exists and generates the verified wire shape, and the drift-guard tests lock the SDK semantics the Task 2 branching depends on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate extractCardsFromNotes to output_config.format with accumulator + try/catch salvage branching; trim prompt; update CLAUDE.md (EXTRACT-01)</name>
  <files>lib/extract-cards.ts, tests/extract-cards.test.ts, CLAUDE.md</files>
  <read_first>
    - .planning/phases/20-extraction-pipeline-hardening/20-RESEARCH.md §"Happy-Path / Fallback Branching (Question E)" (the complete verified branching code — follow it exactly, including edge-case resolutions), §"SDK Behavior Findings" item 2 (why finalMessage() REJECTS on truncation), §"Common Pitfalls" 1 and 4, §"Open Questions" item 1
    - lib/extract-cards.ts (extractCardsFromNotes as it exists after Plan 01 + Task 1: the stream call at ~lines 51-165, the prompt template, the post-response no-text guard)
    - CLAUDE.md §"Exhaustive extraction & dedup" (the paragraph to refresh)
  </read_first>
  <behavior>
    - Test (path equivalence): feeding the same raw card objects to `normalizeExtractedCards(cards, deckSet)` directly (happy-path object input) and to `parseExtractionResponse(JSON.stringify({cards}), deckSet)` (salvage-path text input) produces deeply-equal output — the V5 validation pipeline is identical regardless of parse source
    - Existing suite: all Plan-01 and Task-1 tests remain green (no behavior change to the pure layer)
  </behavior>
  <action>
    In lib/extract-cards.ts (tag changed blocks "EXTRACT-01"):

    1. Restructure `extractCardsFromNotes` per RESEARCH.md §Happy-Path/Fallback Branching, keeping the signature `(notes, existingNormalizedFronts = [], emphasized = []) => Promise<ExtractedCard[]>` and the call fields `model: 'claude-opus-4-8'`, `max_tokens: 32000`, `thinking: { type: 'adaptive' }`, and the system string all byte-identical. Changes: (a) add `output_config: { format: zodOutputFormat(ExtractionSchema) }` to the request; (b) split the chained `.finalMessage()` — assign the stream to a const, then register `stream.on('text', ...)` appending each delta to a `rawText` accumulator BEFORE awaiting. This ordering is load-bearing (Pitfall 1): on truncation the SDK's format parse throws inside the message_stop handler, `finalMessage()` REJECTS, no message object exists, and the raw text is otherwise unrecoverable; (c) wrap `await stream.finalMessage()` in try/catch. On resolve with non-null `message.parsed_output`, return `normalizeExtractedCards(message.parsed_output.cards, deckSet)` — do not check stop_reason on this path (a resolved non-null parsed_output is by construction complete and schema-valid). On resolve with null `parsed_output` (zero text blocks — pre-output refusal/empty), throw a descriptive Error using the existing "No text response from Claude" message stem plus the message's stop_reason; (d) in the catch: when the error is `instanceof AnthropicError` (import it from the package root; note `APIError extends AnthropicError`, so mid-stream connection drops also salvage — intended, per research) AND `rawText.trim()` is non-empty, call `parseExtractionResponse(rawText, deckSet)`; otherwise rethrow. Resolving RESEARCH.md Open Question 1 (planner decision): wrap the salvage call in its own try/catch and, when salvage itself throws, rethrow the ORIGINAL AnthropicError — preserving the SDK diagnostic for the sync failure report; add a one-line comment recording this choice.

    2. Delete the now-dead post-response guard (the `message.content.find` text-block lookup and its throw) — replaced by the `parsed_output` null check.

    3. Prompt edit (Pitfall 4 — remove ONLY formatting, keep ALL semantics): delete the single final formatting-instruction line immediately before the "Lesson notes:" section — the one demanding a bare JSON array response with no fences or preamble. Constrained decoding now guarantees shape. Every other prompt section stays: exhaustiveness, one-card-per-base-form, skip rules, existing-deck exclusion, field semantics, components rules, SENTENCE RULES / blank-safety guarantee, general rules. Do not move field descriptions into `.describe()` calls this phase (avoid duplication drift).

    4. In tests/extract-cards.test.ts, add the path-equivalence test from the behavior block (import `normalizeExtractedCards` from `../lib/extract-cards`). This is the VALIDATION.md EXTRACT-01 row "happy path consumes a raw {cards:[...]} object via normalizeExtractedCards with identical output to the text path" — pure function test, no API call.

    5. Update CLAUDE.md's "Exhaustive extraction & dedup" section (Edit tool, scoped to that paragraph — never rewrite the whole file): describe that extraction now uses native structured outputs (`zodOutputFormat(ExtractionSchema)` via `output_config.format`), reads `parsed_output` on the happy path, salvages truncations from a `stream.on('text')` accumulator through the wrapper-aware depth-2 parser, and code-enforces blank-safety (safe-first sentence partition; cards with zero blank-safe sentences are dropped before persistence). Also update the `lib/extract-cards.ts` bullet in the Key files list to match.

    6. Run the full gate: `npm test && npm run lint`. TypeScript note: `finalMessage()` returns a `ParsedMessage` typed by the format param — if the compiler needs help, follow the SDK's own typings in `resources/messages/messages.d.ts` rather than casting to `any`.
  </action>
  <verify>
    <automated>npm test && npm run lint && grep -c 'output_config' lib/extract-cards.ts</automated>
    <human-check>Phase-end live check (criterion #4 live + assumption A1), before /gsd-verify-work: run `npx tsx scripts/local-resync.mts` against one lesson (requires ANTHROPIC_API_KEY + Turso creds in .env). Inspect the resulting cards: populated components[], no duplicate normalizedFront, sentences[0] blank-safe for every card, and no 400 from the API about the schema. Contingency if a 400 cites optional-field handling (RESEARCH.md assumption A1): change notes to z.string().nullable() and coerce null to undefined in normalizeExtractedCards.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test` green (full suite: extract-cards + all other test files) and `npm run lint` clean
    - `grep -c 'output_config' lib/extract-cards.ts` returns >= 1 and `grep -c "stream.on('text'" lib/extract-cards.ts` returns 1
    - The accumulator registration line appears BEFORE the `finalMessage()` await in the source (source-order assertion)
    - `grep -v '^\s*//' lib/extract-cards.ts | grep -v '^\s*\*' | grep -c 'ONLY a JSON array'` returns 0 (formatting instruction removed from the prompt)
    - `grep -c 'BLANK-SAFETY GUARANTEE' lib/extract-cards.ts` returns 1 (semantic prompt sections retained)
    - Path-equivalence test passes; `extractCardsFromNotes` signature unchanged (callers lib/sync.ts and scripts/local-resync.mts compile untouched)
    - CLAUDE.md extraction section describes the structured-outputs pipeline (no stale bare-JSON-array contract description)
  </acceptance_criteria>
  <done>extractCardsFromNotes returns cards via parsed_output on the happy path, reaches the Plan-01 salvage parser only through the AnthropicError catch with pre-accumulated text, the prompt keeps all semantics minus the formatting instruction, docs match reality, and suite + lint are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM response → extractCardsFromNotes | Untrusted model output enters via parsed_output (zod-validated) or the raw-text accumulator (unvalidated until normalizeExtractedCards) |
| Google Doc lesson content → extraction prompt | Tutor-doc text may attempt indirect prompt injection steering card fields |
| npm registry → node_modules | New dependency installation (zod) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04 | Tampering (V5 Input Validation) | extractCardsFromNotes happy path | high | mitigate | SDK zod safeParse runs before parsed_output is populated (shape), AND normalizeExtractedCards' isValidExtractedCard + coercion still run (z.string() admits empty strings; minLength is not server-enforceable) — both layers execute before any Prisma write |
| T-20-05 | Tampering (prompt injection via lesson content) | extraction prompt / response | medium | mitigate | Structured outputs constrains the response to the schema (no non-schema payloads can ride along); per-card validation drops malformed items; single-tenant app limits blast radius |
| T-20-06 | Information disclosure | ANTHROPIC_API_KEY | low | accept | Server-only lib module (no 'use client'), no new env vars, no client exposure — posture unchanged by this phase |
| T-20-07 | DoS | salvage on accumulated rawText | low | mitigate | rawText bounded by max_tokens 32000; single O(n) salvage scan (Plan 01); per-lesson Promise.allSettled isolation in lib/sync.ts |
| T-20-SC | Tampering (supply chain) | npm install zod | high | mitigate | Package Legitimacy Audit in 20-RESEARCH.md: zod verdict OK/Approved (211.6M weekly downloads, no postinstall script, github.com/colinhacks/zod, SDK's own peer dep); installing the exact version already resolved transitively (^4.3.6); @anthropic-ai/sdk deliberately NOT upgraded (Task 1 verify asserts this) |
</threat_model>

<verification>
- `npx vitest run tests/extract-cards.test.ts` per task commit; `npm test && npm run lint` at plan end
- Schema drift guards from Task 1 stay green after Task 2 (branching design depends on throw-on-truncation semantics)
- Manual phase gate (before /gsd-verify-work): one live single-lesson extraction via `npx tsx scripts/local-resync.mts` — populated components[], unique normalizedFront values, blank-safe sentences[0] per card (phase criterion #4 live half; also confirms RESEARCH.md assumption A1 on optional-field acceptance)
</verification>

<success_criteria>
- Phase criterion #1: extractCardsFromNotes returns cards via native structured outputs (zod schema + output_config.format/zodOutputFormat), reading parsed_output on the happy path instead of regex/salvage-parsing text (VALIDATION.md EXTRACT-01 rows: schema shape, parse-throw drift guard, path equivalence)
- Phase criterion #4 (live half): sample-lesson extraction still dedups by normalizedFront and applies filterComponents — no regression versus current output (manual check above)
- EXTRACT-02's salvage fallback is actually reachable in production: accumulator-before-await + AnthropicError catch wiring proven by source-order assertion and drift-guard tests
</success_criteria>

<output>
Create `.planning/phases/20-extraction-pipeline-hardening/20-02-SUMMARY.md` when done.
</output>
