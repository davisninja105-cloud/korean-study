---
phase: 14-sync-failure-visibility-caching-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/gloss.ts
  - app/api/gloss/preload/route.ts
  - components/GlossProvider.tsx
autonomous: false
requirements: [PERF-02]
must_haves:
  truths:
    - "On mount, GlossProvider preloads previously-resolved gloss entries from the DB Setting table (`gloss:` prefix) into its in-memory cache, bounded to at most GLOSS_PRELOAD_LIMIT entries (PERF-02)."
    - "A word looked up in a previous session resolves from the preloaded in-memory cache on a fresh page load with no `/api/gloss` round-trip and no LLM call (PERF-02)."
    - "The preload endpoint returns only `gloss:`-prefixed Setting rows, bounded by a server-side take limit, and never exposes other app-config settings (buttonColor, dailyGoalSeconds, etc.) (security T-14-04/05)."
    - "GlossProvider's in-memory cache key and the DB `gloss:` cache key are the SAME normalized form (normalizeFront) so a preloaded entry is a hit on the next tap of that word (PERF-02)."
  artifacts:
    - lib/gloss.ts
    - app/api/gloss/preload/route.ts
    - components/GlossProvider.tsx
  key_links:
    - "GET /api/gloss/preload → getRecentGlosses(limit) → prisma.setting.findMany({ where: { key: { startsWith: 'gloss:' } }, take: limit }) → strips the `gloss:` prefix → returns { word (normalized), entry } list."
    - "GlossProvider's mount effect populates cache.current (a ref) from the preload response without any setState in the effect body (react-hooks/set-state-in-effect safe); showGloss reads/writes cache.current under key normalizeFront(word)."
---

<objective>
Make previously-glossed words resolve instantly on a fresh page load by preloading the persistent DB gloss cache into GlossProvider's in-memory map on mount — closing the gap where the `Setting` table cache (`gloss:` prefix) is written by the server but never read back in bulk, so popular unknowns are re-fetched from the LLM every session.

Purpose: Success Criterion 3 (PERF-02) of Phase 14 — on a fresh load, a word looked up before resolves from a DB-preloaded cache with no LLM round-trip.
Output: A bounded `getRecentGlosses()` reader in `lib/gloss.ts`, a new `GET /api/gloss/preload` route, and a mount-time preload + normalized cache key in `components/GlossProvider.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

@components/GlossProvider.tsx
@lib/gloss.ts
@app/api/gloss/route.ts
@lib/card-key.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bounded getRecentGlosses() reader + GET /api/gloss/preload route (PERF-02)</name>
  <files>lib/gloss.ts, app/api/gloss/preload/route.ts</files>
  <read_first>
    - lib/gloss.ts (the whole file — `CACHE_KEY_PREFIX = 'gloss:'`, the `GlossResult` interface, `getCachedGloss`/`setCachedGloss` and their JSON.parse-with-try/catch pattern to mirror, and `MAX_GLOSS_CACHE_ENTRIES = 2000`)
    - app/api/gloss/route.ts (the sibling POST handler — reuse its import style and note it caches unknowns under `setCachedGloss(normalizeFront(word), entry)`, so DB keys are `gloss:${normalizeFront(word)}`)
    - prisma/schema.prisma (the `Setting` model — key/value only, NO timestamp column, so "recent" cannot be time-ordered; the reader takes up to a limit in the DB's natural key order)
    - .planning/codebase/CONVENTIONS.md (API route error-handling: wrap the handler body in try/catch, return NextResponse.json; server-only lib modules carry the `// No 'use client'` note)
  </read_first>
  <action>
    Add a bounded bulk reader to `lib/gloss.ts` and expose it via a new authenticated GET route.

    In `lib/gloss.ts`: export a constant `GLOSS_PRELOAD_LIMIT = 300` and an async function `getRecentGlosses(limit: number = GLOSS_PRELOAD_LIMIT): Promise<{ word: string; entry: GlossResult }[]>`. It queries `prisma.setting.findMany({ where: { key: { startsWith: CACHE_KEY_PREFIX }, }, take: limit })`, then for each row: recover the normalized word with `row.key.slice(CACHE_KEY_PREFIX.length)`, and JSON.parse `row.value` inside a try/catch (skip and `console.warn` malformed rows, mirroring `getCachedGloss`). Return the `{ word, entry }[]` list of successfully parsed rows. The `startsWith: 'gloss:'` filter guarantees only gloss cache rows are read — never other Setting keys (buttonColor, dailyGoalSeconds, sessionSize, …). The `take: limit` bound guarantees the payload can never grow unbounded even as the cache approaches `MAX_GLOSS_CACHE_ENTRIES`.

    Create `app/api/gloss/preload/route.ts`: a server route exporting `async function GET()`. Wrap the body in try/catch per the project's route convention. On success return `NextResponse.json({ entries: await getRecentGlosses() })`. On error, `console.error` server-side and return `NextResponse.json({ entries: [] })` (a preload failure is non-critical — the gloss feature must degrade to normal per-tap fetching, so return an empty list rather than a 500 that the client would have to special-case). Import `getRecentGlosses` from `@/lib/gloss`. Do not accept any request body or query params — the limit is fixed server-side (never client-controllable) so the response size stays bounded.

    Do NOT change `getCachedGloss`, `setCachedGloss`, `lookupViaLLM`, or the existing `POST /api/gloss` handler — this task only adds the reader and the new GET route.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && npm run lint 2>&1 | tail -20 && test -f app/api/gloss/preload/route.ts && grep -q "getRecentGlosses" lib/gloss.ts && grep -q "startsWith" lib/gloss.ts && grep -q "take:" lib/gloss.ts && grep -q "getRecentGlosses" app/api/gloss/preload/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `lib/gloss.ts` exports `getRecentGlosses` and `GLOSS_PRELOAD_LIMIT`; the query uses `where: { key: { startsWith: CACHE_KEY_PREFIX } }` and a `take: <limit>` bound.
    - Source assertion: each row's `value` is JSON.parsed inside a try/catch that skips malformed entries (no unguarded `JSON.parse`).
    - Source assertion: `app/api/gloss/preload/route.ts` exports a `GET` handler wrapped in try/catch that returns `{ entries: [...] }` and returns `{ entries: [] }` on error (no 500 leaked, no client-supplied limit param).
    - Behavior assertion: the endpoint returns at most `GLOSS_PRELOAD_LIMIT` entries and only rows whose key began with `gloss:` (never app-config settings).
    - CLI: `npm run build` succeeds and `npm run lint` reports zero errors.
  </acceptance_criteria>
  <done>PERF-02 (server half) satisfied: a bounded, gloss-scoped `getRecentGlosses()` reader is exposed via `GET /api/gloss/preload`, degrading to an empty list on error. Build and lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Preload the cache on mount + align the in-memory cache key with the DB key (PERF-02)</name>
  <files>components/GlossProvider.tsx</files>
  <read_first>
    - components/GlossProvider.tsx (the whole `GlossProvider` function ~lines 220-290 — the `cache = useRef<Map<string, GlossEntry>>(new Map())` at line 222 and the `showGloss` callback at lines 224-257 that currently keys the cache by `word.trim()` and also uses that same value as the popover `word` and the POST body)
    - lib/card-key.ts (normalizeFront — pure, no imports, explicitly "Safe to call in server AND client contexts"; this is the SAME function the server uses to key the DB cache, so keying the in-memory cache with it makes preloaded entries hit)
    - app/api/gloss/route.ts (confirms the server caches unknowns under `normalizeFront(word)` — the client cache key must match this to hit)
    - .planning/codebase/CONVENTIONS.md ("React Hooks Purity Rules" — no setState synchronously in an effect body; writing to a ref in an effect/async callback is allowed)
  </read_first>
  <action>
    Populate the session cache from the DB on mount, and make the cache key the normalized form so preloaded entries are hits.

    Import `normalizeFront` from `@/lib/card-key` at the top of the file.

    Split the raw display word from the cache key inside `showGloss`: keep `const raw = word.trim()` (guard `if (!raw) return`), and derive `const cacheKey = normalizeFront(raw)`. Use `cacheKey` for BOTH the `cache.current.get(...)` read and the `cache.current.set(...)` write. Keep using `raw` for the popover `word` field and for the `/api/gloss` POST body (`JSON.stringify({ word: raw })`) so server-side resolution is unchanged. This aligns the in-memory key with the DB key `gloss:${normalizeFront(word)}`.

    Add a mount-time preload effect: a `useEffect(() => { ... }, [])` that calls `fetch('/api/gloss/preload')`, `.then(r => r.json())`, and for each `{ word, entry }` in `data.entries ?? []` does `cache.current.set(word, entry)` (the `word` from the endpoint is already the normalized key), with a trailing `.catch(() => {})` so a preload failure is silent and non-fatal. This writes to the `cache.current` ref only — it never calls setState in the effect body, so it is `react-hooks/set-state-in-effect` safe. React StrictMode double-invocation is harmless here (idempotent Map writes). Do not gate rendering on the preload; the popover keeps working from the first tap whether or not preload has resolved yet.

    Do NOT change: the `GlossEntry`/`PopoverState` types, `handleAddCard`, `handleClose`, the popover UI component, or the portal mount. Do NOT add any npm dependency.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && npm run lint 2>&1 | tail -20 && grep -q "gloss/preload" components/GlossProvider.tsx && grep -q "normalizeFront" components/GlossProvider.tsx && grep -q "cache.current.set" components/GlossProvider.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `GlossProvider` imports `normalizeFront` from `@/lib/card-key` and uses `normalizeFront(raw)` as the key for both `cache.current.get` and `cache.current.set`, while the POST body / popover still use the raw trimmed word.
    - Source assertion: a `useEffect(..., [])` fetches `/api/gloss/preload` and populates `cache.current` from `data.entries`, with a `.catch` so failure is silent; no setState is called in the effect body.
    - Behavior assertion: tapping a word already resolved in a previous session, after a fresh page load, shows the gloss with no loading spinner and no `/api/gloss` network request (served from the preloaded cache).
    - Behavior assertion: a never-before-seen word still fetches via POST `/api/gloss` and caches under its normalized key.
    - CLI: `npm run build` succeeds; `npm run lint` reports zero errors (`react-hooks/purity`, `react-hooks/set-state-in-effect` respected).
  </acceptance_criteria>
  <done>PERF-02 (client half) satisfied: GlossProvider preloads the DB gloss cache on mount and keys its in-memory cache by the normalized form, so previously-glossed words resolve instantly on fresh load. Build and lint clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live verification that preloaded glosses resolve instantly on fresh load (PERF-02)</name>
  <read_first>
    - components/GlossProvider.tsx (the mount preload effect + normalized cache key under test — the exact behavior automated build/lint/grep cannot exercise)
    - app/api/gloss/preload/route.ts (the endpoint feeding the preload)
  </read_first>
  <action>Pause for human verification — the executor facilitates the live check below (build + start the app, walk the operator through tap → reload → re-tap) and waits for the resume signal. No source changes occur in this task. It exists because the load-bearing behavior (a preloaded entry being a cache HIT under the normalized key with no network round-trip) has a silent failure mode — a key mismatch would make preload a no-op that no static check can catch — and API/UI test coverage is explicitly out of scope for this milestone.</action>
  <what-built>Mount-time preload of the persistent DB gloss cache into GlossProvider's in-memory map, keyed by the normalized word form so previously-looked-up words resolve instantly on a fresh load with no LLM/API round-trip (PERF-02).</what-built>
  <how-to-verify>
    1. Run `npm run build && npm start`, log in, and open `/study` (or `/cards` reading practice) — any screen with tappable Korean example sentences.
    2. Prime the cache: tap a word that is NOT in your deck (so it resolves via the LLM). Confirm the popover shows a brief "Looking up…" spinner the first time, then the gloss.
    3. Full reload the page (Cmd-R / pull-to-refresh), open DevTools ▸ Network, then tap that SAME word again. Confirm the gloss appears immediately with NO "Looking up…" spinner and NO `POST /api/gloss` request in the Network tab (it should be served from the preloaded cache). You should see the single `GET /api/gloss/preload` request fire once on load.
    4. Control check: tap a brand-new word never looked up before — confirm it still shows the spinner and fires `POST /api/gloss` (normal fallback path intact).
    5. Confirm no console errors and that a fresh install with an empty gloss cache behaves normally (preload returns an empty list; nothing breaks).
  </how-to-verify>
  <resume-signal>Type "approved" if a previously-glossed word resolves instantly (no spinner, no POST) after reload while new words still fetch, or describe what misbehaved (still spinning, still POSTing, console errors).</resume-signal>
  <acceptance_criteria>
    - After reload, a previously-resolved word shows its gloss immediately with no spinner and no `POST /api/gloss` request (served from the preloaded cache); exactly one `GET /api/gloss/preload` fires on load.
    - A brand-new word still shows the spinner and fires `POST /api/gloss` (fallback path intact).
    - No console errors; an empty gloss cache degrades gracefully (empty preload, normal behavior).
    - Operator replies "approved".
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New files / symbols:
- `app/api/gloss/preload/route.ts` — NEW authenticated route exporting `GET`; returns `{ entries: { word: string; entry: GlossResult }[] }`, bounded, `{ entries: [] }` on error.
- `getRecentGlosses(limit?: number)` — NEW exported async function in `lib/gloss.ts`; bounded, `gloss:`-scoped bulk reader of the Setting cache.
- `GLOSS_PRELOAD_LIMIT` — NEW exported constant in `lib/gloss.ts` (default 300).

Modified in `components/GlossProvider.tsx`:
- New import `normalizeFront` from `@/lib/card-key`.
- `showGloss` splits `raw` (display / POST body) from `cacheKey = normalizeFront(raw)` (cache get/set key).
- New mount `useEffect(..., [])` that preloads `GET /api/gloss/preload` into `cache.current`.

No new npm dependencies. No schema changes (the `Setting` table already stores `gloss:`-prefixed rows).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `GET /api/gloss/preload` | Authenticated by the shared-password HMAC cookie (middleware.ts). No request body/params — the client cannot influence how much data is returned. |
| server → client (preload response) | Returns cached gloss entries from the `Setting` table; only `gloss:`-prefixed rows are read, so app-config settings are never exposed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Information Disclosure | `GET /api/gloss/preload` response size | low | mitigate | `getRecentGlosses` uses a fixed server-side `take: GLOSS_PRELOAD_LIMIT` (300) with no client-controllable limit — the response is bounded even as the cache approaches `MAX_GLOSS_CACHE_ENTRIES` (2000). |
| T-14-05 | Information Disclosure | Setting table scoping | low | mitigate | The query filters `where: { key: { startsWith: 'gloss:' } }` so only gloss cache rows are returned; other Setting keys (buttonColor, dailyGoalSeconds, sessionSize, habitDayStartHour) are never read or serialized. |
| T-14-06 | Denial of Service | malformed cached JSON | low | mitigate | Each `row.value` JSON.parse is wrapped in try/catch (mirroring `getCachedGloss`); a corrupt cache row is skipped + `console.warn`ed, never throwing and never crashing the preload. |
| T-14-07 | Information Disclosure | gloss cache contents | low | accept | Cached glosses are the sole authenticated user's own prior lookups (single-tenant, one shared password per .claude/CLAUDE.md). Returning them to that same user is the intended feature. Accepted. |
| T-14-SC | Tampering | dependency installs | n/a | accept | No package-manager installs — the reader/route/effect use existing Prisma, Next, React, and the pure `normalizeFront` helper; no supply-chain surface added. |

No threat in this register is High severity; the ASVS L1 block-on-high gate is not triggered.
</threat_model>

<verification>
- `npm run build` succeeds (new route + lib function + provider changes typecheck).
- `npm run lint` reports zero errors (`react-hooks/set-state-in-effect` respected — the mount effect writes only to a ref).
- Source greps confirm: `getRecentGlosses`/`startsWith`/`take:` in `lib/gloss.ts`; the `GET` handler in `app/api/gloss/preload/route.ts`; `gloss/preload`, `normalizeFront`, `cache.current.set` in `GlossProvider.tsx`.
- Human-verify checkpoint confirms the runtime behavior automated checks cannot: a preloaded word resolves with no spinner and no POST after reload; new words still fall back to the LLM; empty-cache degrades gracefully.
</verification>

<success_criteria>
- PERF-02: On a fresh page load, previously-glossed words resolve instantly from a cache preloaded from the DB `Setting` table on mount — no LLM round-trip for words already looked up.
- The preload endpoint is bounded and gloss-scoped (no unbounded payload, no other settings exposed).
- Build and lint clean; new-word fallback and empty-cache paths still work.
</success_criteria>

<output>
Create `.planning/phases/14-sync-failure-visibility-caching-performance/14-02-SUMMARY.md` when done.
</output>
