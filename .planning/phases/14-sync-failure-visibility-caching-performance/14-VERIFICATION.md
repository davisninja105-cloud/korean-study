---
phase: 14-sync-failure-visibility-caching-performance
verified: 2026-07-02T19:30:00Z
status: human_needed
score: 7/8 must-haves verified
behavior_unverified: 1 # Count of PRESENT_BEHAVIOR_UNVERIFIED truths (present + wired, behavior not exercised); each is detailed in behavior_unverified_items below (and in human_verification when status is human_needed)
overrides_applied: 0
behavior_unverified_items:
  - truth: "A sync creates the same CardDependency edges it created before the refactor for the common case (only cards with components participate; self-edges skipped; link failures stay non-fatal) — behavior preserved (PERF-01)."
    test: "Run a live multi-lesson sync (or a single MAX_LESSONS_PER_SYNC=1 sync of a lesson whose cards carry components referencing other cards with components) against the tutor's Google Doc, then inspect the CardDependency table for the newly-created edges and compare to the edges the pre-refactor per-lesson re-query would have produced."
    expected: "Edges created for the common case (card with components → prerequisite card with components, self-edges skipped, link failures non-fatal) match the pre-refactor behavior. Note WR-01: a card that LOSES its components in this sync's UPDATE branch remains resolvable as a prerequisite for the remainder of the request (stale keyToId entry) — an edge the original post-upsert re-query would NOT have created. This edge case is outside the must_have's explicit 'common case' scope but is a documented behavior difference."
    why_human: "Edge creation is a DB state transition no test exercises (the test suite has no sync-route or CardDependency coverage). Structural greps prove the wiring (seed findMany before loop, linkTargets in both branches gated on components.length>0, prereqId===id skip, per-edge try/catch console.warn, compound key cardId_prerequisiteId preserved) but cannot prove the runtime edges match the pre-refactor output. Requires the tutor's live Google Doc + auth + running server."
human_verification:
  - test: "SYNC-01 live sync against a deliberately-failing lesson: with the dev server running and authenticated, tap Sync in Settings ▸ Advanced against the tutor's Google Doc with a lesson that fails extraction (or yields 0 cards, or fails the DB write)."
    expected: "The SyncPanel failures list shows the lesson's content excerpt (its first non-empty line, ~48 chars, quoted) plus a safe category — e.g. \"<first line>…\" — extraction failed (will retry on next sync) — NOT 'Lesson 1' and NOT the raw Prisma/extraction error message. The raw error appears only in the server console."
    why_human: "The excerpt wiring is fully grep-verified (lessonExcerpt(batch[i].text) → failures.push → response → SyncPanel renders data.failures verbatim at components/SyncPanel.tsx:76-78), but proving the user actually SEES the excerpt in the rendered UI when a real lesson fails requires a live sync against the tutor's Google Doc + running server + auth. No automated test exercises the sync route or SyncPanel rendering."
  - test: "PERF-01 live multi-card sync: tap Sync against a lesson whose cards carry `components` referencing other cards (with components) already in the deck; then inspect the CardDependency table (or trigger a study session that exercises prerequisite ordering)."
    expected: "CardDependency edges form for the common case (card with components → prerequisite card with components; self-edges skipped; link failures non-fatal). Edges match the pre-refactor per-lesson re-query behavior for the common case."
    why_human: "The structural refactor is fully grep-verified (keyToId built once at line 99 before the loop at line 105; no prisma.card.findMany inside the loop body; linkTargets populated in both CREATE/UPDATE branches gated on card.components.length>0; linking loop skips prereqId===id; per-edge try/catch console.warn; compound key cardId_prerequisiteId preserved). But 'same edges created' is a DB state transition no test exercises — requires a live multi-card sync to confirm at runtime."
  - test: "PERF-01 WR-01 edge case (optional, lower priority): sync a lesson where a pre-existing card X (currently with components) is re-extracted with ZERO components in this sync's UPDATE branch, AND another card Y in the same lesson has components referencing X."
    expected: "With MAX_LESSONS_PER_SYNC=1 this is limited to intra-lesson forward references. The new code creates edge Y→X (stale keyToId entry); the original post-upsert re-query would NOT have created it (X lost its components → excluded by where:{components:{not:null}}). This is a documented behavior difference, not a regression in the common case. If observed, the fix from 14-REVIEW.md WR-01 is to add `keyToId.delete(nf)` in the UPDATE branch when card.components.length === 0."
    why_human: "This is the documented WR-01 edge case from the code review. It is outside the plan 14-01 must_have's explicit 'common case (only cards with components participate; self-edges skipped; link failures stay non-fatal)' scope, so it does not fail the must_have. Confirming it requires a live sync engineered to trigger component-loss + cross-reference in the same lesson."
---

# Phase 14: Sync Failure Visibility & Caching Performance — Verification Report

**Phase Goal:** Sync problems are diagnosable at a glance, and the data layer stops repeating expensive per-lesson and per-session lookups.
**Verified:** 2026-07-02T19:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                                                                                                                                              | Status                          | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | When a lesson fails during sync, each entry in the response `failures[]` names that specific lesson by a content excerpt of the lesson body, not a batch-relative `Lesson N` index or a generic remaining count (SYNC-01).                                                                                                          | ✓ VERIFIED                      | `app/api/sync/route.ts:28-36` defines `lessonExcerpt(text)` (first non-empty line, whitespace-collapsed, truncated ~48 chars with `…`, fallback `(untitled lesson)`). Called once per iteration at `:109` with `batch[i].text`. Three `failures.push` sites (`:114`, `:123`, `:268`) lead with `"${excerpt}" — <safe category>`. Response shape (`failed`/`failures`/`remaining`/`newLessons`/`newCards`/`synced`) unchanged at `:279-287`. `components/SyncPanel.tsx:76-78` renders `data.failures` verbatim (`failures.map((f,i) => <li key={i}>{f}</li>)`) — no edit needed.                                                                                                                                                                                                                     |
| 2   | Sync failure strings returned to the client contain only a safe category plus the user's own lesson excerpt; the raw extraction/Prisma error text is logged server-side via console.error only and is never included in `failures[]` (SYNC-01, security T-14-01).                                                                  | ✓ VERIFIED                      | `msg` appears only at `app/api/sync/route.ts:112` (assignment) and `:113` (`console.error(... msg)`) — NOT in any `failures.push`. `dbMsg` appears only at `:266` (assignment) and `:267` (`console.error(... dbMsg)`) — NOT in any `failures.push`. The three `failures.push` strings (`:114`, `:123`, `:268`) interpolate only `${excerpt}` + a literal safe category. T-14-01 mitigated. (Note: WR-02 — the outer catch at `:288-292` still leaks `err.message` to the client on a 500; this is pre-existing, NOT introduced by Phase 14, and outside this must_have's `failures[]` scope — see Warnings.)                                                                                                                                                                                                  |
| 3   | The `normalizedFront → cardId` map used to resolve CardDependency lemmas is built exactly once per sync request, before the per-lesson loop, and is augmented incrementally as cards upsert — the full-deck `prisma.card.findMany` no longer runs inside the loop (PERF-01).                                                         | ✓ VERIFIED                      | `app/api/sync/route.ts:98-103` — `const keyToId = new Map()` + `seedCards = await prisma.card.findMany({ select:{id,normalizedFront}, where:{components:{not:null}} })` + `for (const c of seedCards) keyToId.set(c.normalizedFront, c.id)`. Loop header at `:105` (`for (let i = 0; i < extractResults.length; i++)`). Line-number assertion: seed findMany (`:99`) precedes loop header (`:105`) ✓. Grep confirms NO `prisma.card.findMany` inside the loop body (only `prisma.card.findUnique` at `:159` — the existing per-card dedup check, a different single-row query, not the full-deck lookup). `keyToId.set` augmentation at `:188` (CREATE) and `:222` (UPDATE).                                                                                                                                                          |
| 4   | A sync creates the same CardDependency edges it created before the refactor for the common case (only cards with components participate; self-edges skipped; link failures stay non-fatal) — behavior preserved (PERF-01).                                                                                                          | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED  | Structural wiring VERIFIED: `linkTargets.push` in CREATE branch (`:189`) and UPDATE branch (`:223`), both gated on `card.components.length > 0` (`:187`, `:221`); linking loop at `:235-259` skips `!prereqId || prereqId === id` (`:239`); per-edge `try { ... } catch (linkErr) { console.warn(...) }` (`:240-257`) keeps link failures non-fatal; compound-key `cardDependency.upsert` with `cardId_prerequisiteId` preserved (`:241-253`). BUT edge creation is a DB state transition no test exercises (test suite has zero sync-route / CardDependency coverage). WR-01 (14-REVIEW.md): a card that LOSES components in the UPDATE branch keeps its stale `keyToId` entry (no `keyToId.delete` anywhere — confirmed by grep), so it remains resolvable as a prerequisite for the remainder of the request — an edge the original post-upsert re-query would NOT have created. This edge case is outside the must_have's explicit "common case" scope but is a documented behavior difference. Runtime edge-creation behavior deferred to live UAT. |
| 5   | On mount, GlossProvider preloads previously-resolved gloss entries from the DB Setting table (`gloss:` prefix) into its in-memory cache, bounded to at most GLOSS_PRELOAD_LIMIT entries (PERF-02).                                                                                                                                  | ✓ VERIFIED                      | `lib/gloss.ts:64` — `export const GLOSS_PRELOAD_LIMIT = 300`. `lib/gloss.ts:85-102` — `getRecentGlosses(limit = GLOSS_PRELOAD_LIMIT)` queries `prisma.setting.findMany({ where:{ key:{ startsWith: CACHE_KEY_PREFIX } }, take: limit })` and per-row `JSON.parse` in try/catch (skip + `console.warn` malformed). `app/api/gloss/preload/route.ts:22-29` — `GET()` calls `getRecentGlosses()`, returns `{ entries }` on success / `{ entries: [] }` on error. `components/GlossProvider.tsx:232-241` — mount `useEffect(..., [])` fetches `/api/gloss/preload`, populates `cache.current` from `data.entries ?? []`, `.catch(() => {})` non-fatal, NO setState in effect body (ref-only writes).                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 6   | A word looked up in a previous session resolves from the preloaded in-memory cache on a fresh page load with no `/api/gloss` round-trip and no LLM call (PERF-02).                                                                                                                                                                 | ✓ VERIFIED (via checkpoint)     | Plan 14-02 Task 3 (`checkpoint:human-verify`, gate=blocking) — operator replied **"approved"**. Recorded in `14-02-SUMMARY.md` (Task 3 commit notes + coverage D1/D2 `manual_procedural` entries, status: pass) and `.planning/STATE.md` Blockers/Concerns: "PERF-02 was live-verified at the plan level (Task 3 checkpoint, operator 'approved': preloaded cache HIT post-reload, new-word fallback intact, no console errors)." Live evidence: after a full reload, a previously-resolved word showed its gloss immediately with NO "Looking up…" spinner and NO `POST /api/gloss` in the Network tab (served from the preloaded cache); exactly one `GET /api/gloss/preload` fired on load; a brand-new word still fetched via POST (fallback intact); no console errors.                                                                                                                                       |
| 7   | The preload endpoint returns only `gloss:`-prefixed Setting rows, bounded by a server-side take limit, and never exposes other app-config settings (buttonColor, dailyGoalSeconds, etc.) (security T-14-04/05).                                                                                                                    | ✓ VERIFIED                      | `lib/gloss.ts:89` — `where: { key: { startsWith: CACHE_KEY_PREFIX } }` (CACHE_KEY_PREFIX = `'gloss:'` at `:23`) scopes to gloss cache rows only; app-config settings (buttonColor, dailyGoalSeconds, sessionSize, habitDayStartHour) never read/serialized. `lib/gloss.ts:90` — `take: limit` (limit defaults to GLOSS_PRELOAD_LIMIT=300, fixed server-side). `app/api/gloss/preload/route.ts:22` — `GET()` accepts NO request body/query params; the limit is not client-controllable (T-14-04). T-14-05 mitigated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 8   | GlossProvider's in-memory cache key and the DB `gloss:` cache key are the SAME normalized form (normalizeFront) so a preloaded entry is a hit on the next tap of that word (PERF-02).                                                                                                                                               | ✓ VERIFIED                      | `components/GlossProvider.tsx:27` — `import { normalizeFront } from '@/lib/card-key'`. `:247-249` — `const raw = word.trim(); if (!raw) return; const cacheKey = normalizeFront(raw)`. `:252` — `cache.current.get(cacheKey)` (read). `:268` — `cache.current.set(cacheKey, data)` (write). `:254`, `:259` — popover `word` field uses `raw`. `:264` — POST body `JSON.stringify({ word: raw })`. Server DB key is `gloss:${normalizeFront(word)}` (per `app/api/gloss/route.ts` + `lib/gloss.ts:51`). Preloaded `word` from the endpoint is `row.key.slice(CACHE_KEY_PREFIX.length)` = the normalized form (`lib/gloss.ts:96`), stored via `cache.current.set(word, entry)` (`GlossProvider.tsx:237`). Keys match → preloaded entry is a HIT.                                                                                                       |

**Score:** 7/8 truths verified (1 present, behavior-unverified)

### Required Artifacts

| Artifact                          | Expected                                                                                     | Status     | Details                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------------------------- | -------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/api/sync/route.ts`           | lessonExcerpt helper + hoisted keyToId + linkTargets + sanitized failures[] (SYNC-01, PERF-01) | ✓ VERIFIED  | Exists (293 lines). Substantive: `lessonExcerpt` at `:28-36`, `keyToId` Map + seed findMany at `:98-103`, `linkTargets` at `:146`/`:189`/`:223`, three sanitized `failures.push` at `:114`/`:123`/`:268`, `cardDependency.upsert` compound-key at `:241-253`. Wired: imported by Next.js route table (`/api/sync` registered in build output); consumed by `components/SyncPanel.tsx:24` (`fetch('/api/sync', ...)`). Response shape unchanged. |
| `lib/gloss.ts`                    | getRecentGlosses + GLOSS_PRELOAD_LIMIT, startsWith+take, try/catch JSON.parse (PERF-02)      | ✓ VERIFIED  | Exists (143 lines). Substantive: `GLOSS_PRELOAD_LIMIT = 300` at `:64`, `getRecentGlosses` at `:85-102` with `startsWith: CACHE_KEY_PREFIX` + `take: limit` + per-row try/catch. Wired: imported by `app/api/gloss/preload/route.ts:2` and (transitively) consumed by `components/GlossProvider.tsx` via the preload endpoint. `getCachedGloss`/`setCachedGloss`/`lookupViaLLM` untouched.                                                                                                                       |
| `app/api/gloss/preload/route.ts`  | GET handler, try/catch, {entries:[...]} on success / {entries:[]} on error, no client limit (PERF-02) | ✓ VERIFIED  | Exists (30 lines, NEW file). Substantive: `export async function GET()` at `:22`, try/catch at `:23-29`, returns `{ entries }` on success (`:25`) and `{ entries: [] }` on error (`:28`), no request body/query params. Wired: registered in build route table (`/api/gloss/preload` ✓); fetched by `components/GlossProvider.tsx:233` (`fetch('/api/gloss/preload')`).                                                                                                                                                                              |
| `components/GlossProvider.tsx`    | normalizeFront import + cacheKey usage for get/set, raw for popover+POST, mount useEffect fetches /api/gloss/preload into cache.current with .catch, no setState in effect body (PERF-02) | ✓ VERIFIED  | Exists (313 lines). Substantive: `normalizeFront` import at `:27`, mount `useEffect(..., [])` at `:232-241` (fetch → `.then(r => r.json())` → `.then(data => for (...) cache.current.set(word, entry))` → `.catch(() => {})`, ref-only), `showGloss` splits `raw`/`cacheKey` at `:247-249`, `cacheKey` used for get (`:252`) + set (`:268`), `raw` for popover (`:254`/`:259`) + POST body (`:264`). Wired: mounted in app layout; `useWordTap()` consumed by sentence components. No setState in effect body. |
| `components/SyncPanel.tsx`        | renders data.failures verbatim (no edit needed)                                              | ✓ VERIFIED  | Exists (85 lines, unmodified by Phase 14). `:44` reads `data.failures ?? [...]`; `:76-78` renders `failures.map((f, i) => <li key={i}>{f}</li>)` — renders the excerpt strings verbatim. Wired: `fetch('/api/sync', ...)` at `:24`.                                                                                                                                                                                                  |
| `lib/card-key.ts`                 | normalizeFront — the shared key form                                                          | ✓ VERIFIED  | Exists (40 lines, unchanged). `normalizeFront` at `:25-39` — pure, no imports, NFC + trim + collapse + trailing-English-gloss strip. Shared by `app/api/sync/route.ts:5` (sync upsert + component resolution) and `components/GlossProvider.tsx:27` (client cache key).                                                                                                                                                                                              |

### Key Link Verification

| From                              | To                                | Via                                                                                                                                | Status   | Details                                                                                                                                                                                                                                                                                                                                                     |
| --------------------------------- | --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `components/SyncPanel.tsx`        | `app/api/sync/route.ts`           | `fetch('/api/sync', { method:'POST', body: JSON.stringify({documentId}) })` → reads `data.failed`/`data.failures` → renders `failures.map(...)` | ✓ WIRED  | SyncPanel `:24-29` POSTs to `/api/sync`; `:43-44` reads `data.failed`/`data.failures`; `:76-78` renders verbatim. Route returns `failed: failures.length` + conditional `failures` array (`app/api/sync/route.ts:284-285`). Excerpt flows through with no client edit.                                                                                                       |
| `app/api/sync/route.ts`           | `prisma.cardDependency.upsert`    | `linkTargets` iteration → `keyToId.get(normalizeFront(comp))` → `prisma.cardDependency.upsert({ where:{cardId_prerequisiteId:{cardId,prerequisiteId}} })` | ✓ WIRED  | `:235-259` linking loop iterates `linkTargets`, resolves `prereqId` from shared `keyToId`, skips `!prereqId \|\| prereqId === id`, upserts with compound key. Per-edge try/catch console.warn (non-fatal).                                                                                                                                                                       |
| `app/api/sync/route.ts`           | seed `prisma.card.findMany` (once-per-request) | `keyToId` Map seeded before the per-lesson loop, augmented during upserts                                                          | ✓ WIRED  | Seed findMany at `:99` (before loop at `:105`); `keyToId.set` at `:103` (seed), `:188` (CREATE), `:222` (UPDATE). No `prisma.card.findMany` inside the loop body.                                                                                                                                                                                                      |
| `components/GlossProvider.tsx`    | `app/api/gloss/preload/route.ts`  | mount `useEffect(..., [])` → `fetch('/api/gloss/preload')` → `.then(r => r.json())` → `cache.current.set(word, entry)` for each entry | ✓ WIRED  | `GlossProvider.tsx:232-241` mount effect fetches preload endpoint and populates `cache.current` ref. Endpoint `app/api/gloss/preload/route.ts:22-29` returns `{ entries }`. `.catch(() => {})` non-fatal. No setState in effect body.                                                                                                                                                  |
| `app/api/gloss/preload/route.ts`  | `lib/gloss.ts:getRecentGlosses`   | `import { getRecentGlosses } from '@/lib/gloss'` → `await getRecentGlosses()` → `NextResponse.json({ entries })`                   | ✓ WIRED  | `app/api/gloss/preload/route.ts:2` imports `getRecentGlosses`; `:24` calls it; `:25` returns `{ entries }`. `lib/gloss.ts:85` exports `getRecentGlosses`.                                                                                                                                                                                                                          |
| `lib/gloss.ts:getRecentGlosses`   | `prisma.setting.findMany` (gloss:-scoped, take-bounded) | `prisma.setting.findMany({ where:{ key:{ startsWith: CACHE_KEY_PREFIX } }, take: limit })` → per-row `JSON.parse` in try/catch      | ✓ WIRED  | `lib/gloss.ts:88-91` query with `startsWith: 'gloss:'` + `take: limit`; `:93-100` per-row try/catch JSON.parse; `:96` recovers `word = row.key.slice(prefix.length)` (normalized form).                                                                                                                                                                                |
| `components/GlossProvider.tsx`    | `lib/card-key.ts:normalizeFront`  | `import { normalizeFront } from '@/lib/card-key'` → `cacheKey = normalizeFront(raw)` for `cache.current.get`/`.set`                | ✓ WIRED  | `GlossProvider.tsx:27` import; `:249` `cacheKey = normalizeFront(raw)`; `:252` get; `:268` set. Server DB key `gloss:${normalizeFront(word)}` matches → preloaded entry is a HIT.                                                                                                                                                                                              |

### Data-Flow Trace (Level 4)

| Artifact                          | Data Variable       | Source                                                                                            | Produces Real Data | Status      |
| --------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------- | ------------------ | ----------- |
| `app/api/sync/route.ts` (failures) | `failures: string[]` | `lessonExcerpt(batch[i].text)` + safe category literals at three push sites                       | Yes (user's own doc content) | ✓ FLOWING   |
| `app/api/sync/route.ts` (keyToId) | `keyToId: Map`       | `prisma.card.findMany({ select:{id,normalizedFront}, where:{components:{not:null}} })` + upsert augmentation | Yes (real DB rows) | ✓ FLOWING   |
| `lib/gloss.ts:getRecentGlosses`   | `out: {word,entry}[]` | `prisma.setting.findMany({ where:{ key:{startsWith:'gloss:'} }, take:limit })` + per-row JSON.parse | Yes (real DB cache rows) | ✓ FLOWING   |
| `app/api/gloss/preload/route.ts`  | `entries`            | `await getRecentGlosses()`                                                                        | Yes (delegates to lib/gloss DB query) | ✓ FLOWING   |
| `components/GlossProvider.tsx` (cache) | `cache.current: Map` | mount `useEffect` fetch of `/api/gloss/preload` → `data.entries` → `cache.current.set(word, entry)` | Yes (real preload response, ref-only writes) | ✓ FLOWING   |

### Behavioral Spot-Checks

| Behavior                                                                       | Command                                                                                      | Result                                                                                       | Status   |
| ------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | -------- |
| Build succeeds with new route registered                                       | `npm run build 2>&1 \| tail -25`                                                             | `✓ Compiled successfully in 3.5s`; `✓ Generating static pages (25/25)`; `/api/gloss/preload` in route table; exit 0 | ✓ PASS   |
| Lint reports zero errors (incl. `react-hooks/set-state-in-effect`)              | `npm run lint 2>&1 \| tail -20`                                                              | eslint: zero output, exit 0                                                                  | ✓ PASS   |
| Test suite passes                                                              | `npm test 2>&1 \| tail -25`                                                                   | 6 test files, 58 tests passed, exit 0 (no sync/gloss-route/edge-creation coverage — confirms T4 behavior-unverified) | ✓ PASS   |
| `lessonExcerpt` defined + called with `batch[i].text`                          | `grep -n "lessonExcerpt\|failures.push" app/api/sync/route.ts`                                | `:28` definition, `:109` `lessonExcerpt(batch[i].text)`, `:114`/`:123`/`:268` push sites      | ✓ PASS   |
| Seed findMany precedes loop; no findMany inside loop                            | `grep -n "prisma.card.findMany\|for (let i = 0; i < extractResults.length" app/api/sync/route.ts` | seed at `:99`, loop at `:105` (99<105); no findMany inside loop body                         | ✓ PASS   |
| `linkTargets` in both branches gated on `card.components.length > 0`           | `grep -n "card.components.length > 0\|linkTargets.push" app/api/sync/route.ts`                | gates at `:187`/`:221`; pushes at `:189` (CREATE)/`:223` (UPDATE)                              | ✓ PASS   |
| Compound-key `cardDependency.upsert` + self-edge skip + per-edge try/catch     | `grep -n "cardDependency.upsert\|prereqId === id\|console.warn" app/api/sync/route.ts`        | upsert at `:241`, skip at `:239`, console.warn at `:256`                                       | ✓ PASS   |
| `getRecentGlosses` uses startsWith + take + try/catch JSON.parse               | `grep -n "startsWith\|take:\|JSON.parse\|try\|catch" lib/gloss.ts`                            | startsWith `:89`, take `:90`, try/catch `:94-99`                                               | ✓ PASS   |
| GET preload returns `{entries}` / `{entries:[]}`, no client limit               | read `app/api/gloss/preload/route.ts`                                                        | `:25` success, `:28` error, no body/query params                                              | ✓ PASS   |
| GlossProvider imports normalizeFront + cacheKey for get/set + mount preload    | `grep -n "normalizeFront\|cache.current.set\|gloss/preload\|useEffect" components/GlossProvider.tsx` | import `:27`, cacheKey `:249`/`:252`/`:268`, mount effect `:232-241`, fetch `:233`             | ✓ PASS   |
| No `keyToId.delete` anywhere (WR-01 confirmation)                              | `grep -n "keyToId.delete" app/api/sync/route.ts`                                              | no matches (exit 1) — stale entries NOT removed on component-loss                              | ℹ️ INFO (WR-01) |

### Probe Execution

No probes declared in plan frontmatter or conventional `scripts/*/tests/probe-*.sh` paths. Plan 14-02 Task 3 was a `checkpoint:human-verify` (gate=blocking), not a probe script — it was operator-approved and recorded in `14-02-SUMMARY.md` + `STATE.md`. No probe execution required.

### Requirements Coverage

| Requirement | Source Plan    | Description                                                                                              | Status      | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------- | -------------- | -------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SYNC-01     | 14-01-PLAN.md  | Sync failures surface which specific lesson(s) failed (not just a generic "remaining" count) in the SyncPanel | ✓ SATISFIED (structural; UI behavior → human) | `failures[]` entries now name the lesson by content excerpt + safe category (Truth #1 VERIFIED); raw error text confined to server-side console (Truth #2 VERIFIED); SyncPanel renders `data.failures` verbatim (artifact VERIFIED). REQUIREMENTS.md marks SYNC-01 Complete (traceability row). The UI rendering behavior (user sees excerpt when a lesson fails) is human_verification — requires a live sync against a failing lesson.                                                                                                                                                                                                      |
| PERF-01     | 14-01-PLAN.md  | Sync resolves CardDependency lemmas via a cached normalizedFront → cardId map instead of re-querying the full deck per lesson | ✓ SATISFIED (structural; edge-creation behavior → human) | Map built once before the loop, no findMany inside loop (Truth #3 VERIFIED); common-case structural elements preserved (Truth #4 structural wiring VERIFIED, runtime edge-creation behavior PRESENT_BEHAVIOR_UNVERIFIED). REQUIREMENTS.md marks PERF-01 Complete (traceability row). Live multi-card sync UAT deferred.                                                                                                                                                                                                                                                                                                       |
| PERF-02     | 14-02-PLAN.md  | Tap-to-gloss cache preloads recent entries from the DB Setting table on mount                            | ✓ SATISFIED (incl. runtime via checkpoint)  | Server reader + endpoint (Truths #5/#7 VERIFIED); client mount preload + cache-key alignment (Truths #8 VERIFIED); runtime behavior (preloaded word resolves instantly, no LLM round-trip) VERIFIED via operator-approved Task 3 checkpoint (Truth #6). REQUIREMENTS.md marks PERF-02 Complete (traceability row).                                                                                                                                                                                                                                                                                                                              |

**Orphaned requirements check:** REQUIREMENTS.md maps exactly SYNC-01, PERF-01, PERF-02 to Phase 14 (traceability rows `:61-63`); all three appear in plan `requirements:` frontmatter (14-01: SYNC-01, PERF-01; 14-02: PERF-02). No orphaned requirements. PERF-03/REFACTOR-01/REFACTOR-02 are correctly mapped to Phase 15.

### Anti-Patterns Found

| File                              | Line | Pattern                                                                                                                                 | Severity | Impact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------------------------------- | ---- | --------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/api/sync/route.ts`           | 288-292 | Outer catch returns raw `err.message` to client on 500 (`{ error: message }`) — inconsistent with SYNC-01 sanitization goal             | ⚠️ Warning (WR-02) | Pre-existing (NOT introduced by Phase 14 — confirmed by 14-REVIEW.md). Outside Truth #2's literal `failures[]` scope, so Truth #2 is satisfied. But inconsistent with the phase's broader "sync problems diagnosable at a glance + raw error text never reaches the client (T-14-01)" goal. Proposed fix in 14-REVIEW.md: return generic `'Sync failed — see server logs for details'`, keep `console.error` server-side. Non-blocking — does not fail any Phase 14 must_have.                                                                                                                                                |
| `app/api/sync/route.ts`           | 192-225 | Stale `keyToId` entry persists when a card loses its components in the UPDATE branch (no `keyToId.delete(nf)` when `card.components.length === 0`) | ⚠️ Warning (WR-01) | Documented behavior difference from the original post-upsert re-query: a card that loses components remains resolvable as a prerequisite for the remainder of this request, so an edge the original would NOT have created MAY be created. Trigger conditions: pre-existing card X with components is re-extracted with zero components AND another card Y in the same lesson references X (with MAX_LESSONS_PER_SYNC=1, intra-lesson forward refs only). Outside Truth #4's explicit "common case (only cards with components participate; self-edges skipped; link failures stay non-fatal)" scope, so Truth #4's scoped claim is not failed — but the "behavior preserved" assertion is inaccurate for this edge case. Proposed fix in 14-REVIEW.md: add `keyToId.delete(nf)` in the UPDATE branch when `card.components.length === 0`. Non-blocking — does not fail any Phase 14 must_have (scope is explicitly "common case"). |
| `app/api/sync/route.ts`           | 89/166 | Variable shadowing: outer `let created = 0` (lesson counter) shadows inner `const created = await prisma.card.create({...})` (Card row) | ℹ️ Info (IN-01) | Pre-existing (NOT introduced by Phase 14). TypeScript block scoping resolves correctly (`++created` at `:132` = counter, `created.id` at `:188` = Card), but a maintenance hazard. Proposed fix: rename inner to `newCard`/`createdCard`. Non-blocking.                                                                                                                                                                                                                                                                                                            |
| `lib/gloss.ts`                    | 85-102 | `getRecentGlosses` returns rows in undefined (insertion) order, not recency order — name/docstring imply "recent" but Setting has no timestamp column | ℹ️ Info (IN-02) | For cache warming any 300 entries are valid, but the most-recently-glossed words (most likely to be re-tapped) may not be in the preloaded set, reducing hit rate. Accepted trade-off (schema change out of scope). Non-blocking — does not fail any must_have (must_have #5 says "bounded to at most GLOSS_PRELOAD_LIMIT entries", not "most recent").                                                                                                                                                                                                                                                              |

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers in any of the four Phase 14 source files (grep exit 1 — no matches). No stub implementations, no hardcoded empty data flowing to render, no console.log-only handlers.

### Human Verification Required

Three items need human testing (1 behavior-unverified truth + 2 ROADMAP-success-criteria UI/runtime behaviors). PERF-02's runtime behavior was already live-verified via the operator-approved Plan 14-02 Task 3 checkpoint and is NOT listed here.

### 1. SYNC-01 — Live sync against a deliberately-failing lesson

**Test:** With the dev server running and authenticated, tap Sync in Settings ▸ Advanced against the tutor's Google Doc with a lesson that fails extraction (or yields 0 cards, or fails the DB write).
**Expected:** The SyncPanel failures list shows the lesson's content excerpt (its first non-empty line, ~48 chars, quoted) plus a safe category — e.g. `"<first line>…" — extraction failed (will retry on next sync)` — NOT `Lesson 1` and NOT the raw Prisma/extraction error message. The raw error appears only in the server console.
**Why human:** The excerpt wiring is fully grep-verified (`lessonExcerpt(batch[i].text)` → `failures.push` → response → SyncPanel renders `data.failures` verbatim at `components/SyncPanel.tsx:76-78`), but proving the user actually SEES the excerpt in the rendered UI when a real lesson fails requires a live sync against the tutor's Google Doc + running server + auth. No automated test exercises the sync route or SyncPanel rendering.

### 2. PERF-01 — Live multi-card sync confirming CardDependency edges still form

**Test:** Tap Sync against a lesson whose cards carry `components` referencing other cards (with components) already in the deck; then inspect the CardDependency table (or trigger a study session that exercises prerequisite ordering).
**Expected:** CardDependency edges form for the common case (card with components → prerequisite card with components; self-edges skipped; link failures non-fatal). Edges match the pre-refactor per-lesson re-query behavior for the common case.
**Why human:** The structural refactor is fully grep-verified (keyToId built once at line 99 before the loop at line 105; no `prisma.card.findMany` inside the loop body; `linkTargets` populated in both CREATE/UPDATE branches gated on `card.components.length > 0`; linking loop skips `prereqId === id`; per-edge try/catch `console.warn`; compound key `cardId_prerequisiteId` preserved). But "same edges created" is a DB state transition no test exercises — requires a live multi-card sync to confirm at runtime. (This is the behavior-unverified Truth #4.)

### 3. PERF-01 WR-01 edge case (optional, lower priority)

**Test:** Sync a lesson where a pre-existing card X (currently with components) is re-extracted with ZERO components in this sync's UPDATE branch, AND another card Y in the same lesson has components referencing X.
**Expected:** With `MAX_LESSONS_PER_SYNC=1` this is limited to intra-lesson forward references. The new code creates edge Y→X (stale `keyToId` entry); the original post-upsert re-query would NOT have created it (X lost its components → excluded by `where:{components:{not:null}}`). This is a documented behavior difference, not a regression in the common case. If observed, the fix from `14-REVIEW.md` WR-01 is to add `keyToId.delete(nf)` in the UPDATE branch when `card.components.length === 0`.
**Why human:** This is the documented WR-01 edge case from the code review. It is outside the plan 14-01 must_have's explicit "common case (only cards with components participate; self-edges skipped; link failures stay non-fatal)" scope, so it does not fail the must_have. Confirming it requires a live sync engineered to trigger component-loss + cross-reference in the same lesson.

### Gaps Summary

No gaps block Phase 14 goal achievement. All three requirements (SYNC-01, PERF-01, PERF-02) are structurally SATISFIED with build/lint/test gates green (build exit 0 with `/api/gloss/preload` registered; lint exit 0; tests 58/58 passed). All eight plan must_haves are either VERIFIED (7) or PRESENT_BEHAVIOR_UNVERIFIED (1 — Truth #4, PERF-01 edge-creation runtime behavior). PERF-02's runtime behavior was already live-verified via the operator-approved Plan 14-02 Task 3 checkpoint.

The `human_needed` status reflects two runtime behaviors that grep/build/lint cannot exercise and that no automated test covers (the test suite has zero sync-route / CardDependency / gloss-preload coverage):
1. **SYNC-01 UI rendering** — the SyncPanel actually showing the lesson excerpt when a real lesson fails (wiring complete, runtime unexercised).
2. **PERF-01 edge creation** — a live multi-card sync actually creating the same CardDependency edges as the pre-refactor code (structural wiring complete, runtime state-transition unexercised — Truth #4).

Both are deferred to the optional end-of-phase UAT noted in `STATE.md` (a live sync against the tutor's Google Doc with a deliberately-failing/zero-card lesson and a multi-card lesson). Neither is a regression or a stub — the code is present, substantive, wired, and data flows are real.

**Warnings (non-blocking, documented in 14-REVIEW.md):**
- **WR-01** (stale `keyToId` on component-loss in UPDATE): a documented behavior difference in an edge case outside Truth #4's explicit "common case" scope. The must_have's scoped claim is not failed, but the "behavior preserved" assertion is inaccurate for component-loss. Proposed fix: `keyToId.delete(nf)` in the UPDATE branch when `card.components.length === 0`.
- **WR-02** (outer catch leaks `err.message` to client on 500): pre-existing (not introduced by Phase 14), outside Truth #2's literal `failures[]` scope (so Truth #2 is satisfied), but inconsistent with the phase's broader SYNC-01/T-14-01 sanitization goal. Proposed fix: return generic message, keep `console.error` server-side.

**Info (non-blocking):** IN-01 (variable shadowing `created`, pre-existing), IN-02 (`getRecentGlosses` returns insertion order not recency — accepted trade-off, schema change out of scope).

---

_Verified: 2026-07-02T19:30:00Z_
_Verifier: the agent (gsd-verifier)_
