---
phase: 13-review-api-hardening-save-reliability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/StudySession.tsx
  - components/Toast.tsx
autonomous: false
requirements: [REVIEW-04, REVIEW-05]
must_haves:
  truths:
    - "A failed background POST /api/review is retried a bounded number of times before any user-visible message appears (REVIEW-04)."
    - "A toast appears only after every review-save retry is exhausted — a save that succeeds on any attempt shows nothing (REVIEW-04)."
    - "Undoing a review restores the previous card, queue, seen-card set, and session stats together; an unmount mid-undo leaves no partially-restored state (REVIEW-05)."
  artifacts:
    - components/StudySession.tsx
    - components/Toast.tsx
  key_links:
    - "submitReview's background save calls the bounded retry wrapper instead of a bare fetch().catch(() => {})."
    - "The retry wrapper's exhaustion callback (guarded by the mount ref) is the only trigger for the save-failure toast."
    - "handleUndo's success-path restoration — including the seenCardIdsRef write — is guarded so it applies as one atomic unit or is fully skipped when unmounted."
---

<objective>
Make the study client recover from a failed review save instead of silently losing it, and make undo restoration atomic so an interrupted undo can never leave the session half-restored.

Purpose: Success Criteria 4-5 of Phase 13 — the fire-and-forget `POST /api/review` gains bounded silent retries with a toast only on final failure (REVIEW-04), and the undo restore becomes a single all-or-nothing unit (REVIEW-05).
Output: A new minimal `components/Toast.tsx`; a bounded retry wrapper + mount guard + toast wiring in `components/StudySession.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

@components/StudySession.tsx
@app/api/review/route.ts
@app/api/review/undo/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create minimal Toast component + bounded silent-retry review save (REVIEW-04)</name>
  <files>components/Toast.tsx, components/StudySession.tsx</files>
  <read_first>
    - components/StudySession.tsx (module-level helpers around lines 51-93 for where to add the retry function; submitReview at lines 397-504, specifically the background save at lines 473-478; the return/JSX outer div at lines 593-599 for where to mount the toast; state declarations at lines 138-162)
    - components/Sheet.tsx (reference ONLY for the 'use client' + portal idiom and design-token usage — the toast must be far simpler: no spring animation, no focus trap, no backdrop)
    - app/globals.css (confirm the semantic tokens: bg-surface-1, shadow, text colors, --sab safe-area var; the toast must use these, never literal color utilities)
  </read_first>
  <action>
    Create `components/Toast.tsx`: a `'use client'` presentational component (first line `'use client'`, before imports). Props interface named `Props` with `message: string`, `onDismiss: () => void`, and `duration?: number` (default 4000). It self-dismisses via a `useEffect` that calls `setTimeout(onDismiss, duration)` and clears the timer in cleanup — calling `onDismiss` inside the timeout callback (NOT synchronously in the effect body) so it does not violate `react-hooks/set-state-in-effect`. Render a single fixed-position element near the bottom of the viewport (respect the safe area using the `--sab` pattern already used at StudySession.tsx:598), styled with semantic tokens only (`bg-surface-1`, rounded, `shadow-md`, readable text token, sensible padding). Give it `role="status"` and `aria-live="polite"` for screen-reader announcement, and include a dismiss control with `aria-label` and a >=44px tap target that calls `onDismiss`. Do NOT use `bg-orange-*`/`bg-blue-*` or any literal color utility. Do NOT add any npm dependency — hand-roll it.

    In `components/StudySession.tsx`, add a module-level async helper alongside the existing pure helpers (near seededShuffle/hashStr, ~line 93): `async function postReviewWithRetry(cardId: string, rating: number, onExhausted: () => void): Promise<void>`. It performs up to 3 total attempts (1 initial + 2 retries) of `fetch('/api/review', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ cardId, rating }) })`. An attempt counts as failed when the fetch rejects OR `res.ok` is false. Between attempts wait a short backoff (attempt 1→2: ~500ms, attempt 2→3: ~1500ms) via an awaited setTimeout promise. If all 3 attempts fail, call `onExhausted()` once. On any success, return without calling onExhausted. This runs from an event-handler flow (submitReview), never during render, so setTimeout usage is purity-safe.

    Wire it in: add `const [saveError, setSaveError] = useState<string | null>(null)` to the component state. Add a mount ref `const isMountedRef = useRef(true)` plus an effect `useEffect(() => { isMountedRef.current = true; return () => { isMountedRef.current = false } }, [])` (ref writes in an effect body are allowed; this is not setState). Replace the bare `fetch('/api/review', ...).catch(() => {})` at lines 474-478 with `postReviewWithRetry(cardId, rating, () => { if (isMountedRef.current) setSaveError("Couldn't save your last review — check your connection.") })`. (setSaveError is called from an async callback, not render/effect body — allowed.) Finally, render `{saveError && <Toast message={saveError} onDismiss={() => setSaveError(null)} />}` inside the top-level returned `<div>` (after line 599), and import Toast at the top.

    The retry attempt count and backoff timings are Claude's discretion (the values above are a sensible default); the toast must fire ONLY after the final failed attempt, never before. The toast is scoped to this background-save failure path only — do NOT wire it to handleUndo's existing fetch at lines 517-530 (that path has its own re-arm/return handling).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && npm run lint 2>&1 | tail -20 && test -f components/Toast.tsx && grep -q "role=\"status\"" components/Toast.tsx && grep -q "postReviewWithRetry" components/StudySession.tsx && grep -q "isMountedRef" components/StudySession.tsx && ! grep -q "fetch('/api/review', {" components/StudySession.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `components/Toast.tsx` exists, starts with `'use client'`, exposes a `Props` interface with `message`/`onDismiss`/`duration?`, uses `role="status"`, self-dismisses via a `setTimeout` in a cleaned-up effect, and uses only semantic tokens (no literal `bg-orange-*`/`text-blue-*`).
    - Source assertion: `StudySession.tsx` no longer contains the bare `fetch('/api/review', {...}).catch(() => {})` in submitReview; the background save now goes through `postReviewWithRetry`.
    - Source assertion: `postReviewWithRetry` makes multiple attempts and invokes its `onExhausted` callback only after the last failure.
    - Behavior assertion: when `/api/review` fails transiently and then succeeds within the retry budget, NO toast appears; when it fails on every attempt, exactly one toast appears after the final attempt.
    - CLI: `npm run build` succeeds; `npm run lint` reports zero errors (including `react-hooks/purity` and `react-hooks/set-state-in-effect`).
  </acceptance_criteria>
  <done>REVIEW-04 satisfied: the background review save retries silently within a bounded budget and surfaces a single toast (new components/Toast.tsx) only after all retries are exhausted. Build and lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Make undo state restoration atomic (REVIEW-05)</name>
  <files>components/StudySession.tsx</files>
  <read_first>
    - components/StudySession.tsx (handleUndo at lines 511-540 — the awaited fetch, the failure re-arm at 524-530, and the success-path restoration at 532-539; the undoRef shape at lines 150-157; seenCardIdsRef at line 161; the isMountedRef added in Task 1)
    - .planning/codebase/CONCERNS.md ("Undo State Restoration Incomplete" — the source concern: the success block runs 6+ sequential ops including an unconditional ref mutation that can desync from the setState calls if the component unmounts between the awaited fetch resolving and the restore)
  </read_first>
  <action>
    Make handleUndo's success-path restoration a single atomic unit. After the awaited `fetch('/api/review/undo', ...)` resolves successfully (after line 530), guard the entire restoration block with the mount ref added in Task 1: if `!isMountedRef.current`, return immediately WITHOUT touching `seenCardIdsRef.current` or calling any setter — so the unconditional ref mutation (`seenCardIdsRef.current = prevSeenCardIds`, currently line 535) can no longer run while the state setters silently no-op post-unmount. When mounted, apply all restorations together (`setStats(prevStats)`, `setQueue(prevQueue)`, `seenCardIdsRef.current = prevSeenCardIds`, `setSeenCount(prevSeenCount)`, `setCursor((c) => c + 1)`, `setRevealed(false)`, `setIntervalHints(null)`) — React 18/19 auto-batches these setState calls into one commit, and the mount guard makes the ref write share the same all-or-nothing gate. The acceptance criterion is atomicity (all-or-nothing), not that a reducer specifically exists.

    Do not change the failure path (lines 524-530) — it already re-arms `undoRef.current` + `setCanUndo(true)` and returns in one block. Do not change submitReview's undo-snapshot capture at line 470. Do not alter the undoRef shape unless a chosen approach requires it.

    (Acceptable heavier alternative, only if you judge it cleaner: consolidate queue/stats/seenCount/seenCardIds/cursor/revealed/intervalHints into a single `useReducer` and restore via one dispatched action — but prefer the mount-guard + batched-block approach above, since Phase 15 owns the larger StudySession structural refactor and this phase should minimize churn while locking behavior.)
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && npm run lint 2>&1 | tail -20 && grep -q "isMountedRef.current" components/StudySession.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: handleUndo's success path checks `isMountedRef.current` (or restores via a single reducer dispatch) so the `seenCardIdsRef` write and the state setters cannot apply partially.
    - Behavior assertion: undoing a review restores the previous card at queue[0] with stats, seen-count, and seen-card set all matching their pre-review snapshot.
    - Behavior assertion: if the component unmounts between the undo fetch resolving and restoration, no ref is left mutated while state is stale (the whole restore is skipped).
    - Behavior assertion: the undo failure path still re-arms the undo button so the user can retry.
    - CLI: `npm run build` succeeds; `npm run lint` reports zero errors.
  </acceptance_criteria>
  <done>REVIEW-05 satisfied: undo restoration is a single atomic unit guarded against unmount; queue, stats, seen-count, and seen-card set are restored together or not at all. Build and lint clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live verification of retry/toast + atomic undo (REVIEW-04, REVIEW-05)</name>
  <read_first>
    - components/Toast.tsx (the new component under test — confirm token styling + role="status")
    - components/StudySession.tsx (submitReview retry wiring + handleUndo atomic restore — the behaviors being exercised live)
  </read_first>
  <action>Pause for human verification — the executor facilitates the live check below (build + start the app, walk the operator through the offline/undo flows) and waits for the resume signal. No source changes occur in this task; it exists to confirm the network-dependent and unmount-dependent behaviors that automated build/lint/grep cannot exercise (API/UI test coverage is out of scope this milestone).</action>
  <what-built>Bounded silent retry + toast on final review-save failure (REVIEW-04) and atomic undo restoration (REVIEW-05) in the live study session. These behaviors depend on network conditions and session state that automated checks cannot fully exercise (API/UI test coverage is explicitly out of scope for this milestone), so a short live check confirms no regression to flip/grade/undo.</what-built>
  <how-to-verify>
    1. Run `npm run build && npm start`, log in, open `/study`, and start a session (Flashcards).
    2. Normal path: flip a card, grade it, confirm the next card appears instantly and NO toast shows. Tap the ↩ undo button — the previous card must reappear with the "Card N of N" counter and session stats restored (not double-counted, not reset).
    3. Retry/toast path: open DevTools → Network → set to Offline, grade a card. Confirm the next card still advances instantly (optimistic) and, after a few seconds (retries exhausted), a single toast appears with a connection message and then auto-dismisses. Set Network back to Online and grade another card — confirm NO toast appears.
    4. Undo-during-flux: grade a card, immediately tap undo, and confirm stats/queue stay consistent (no partially-restored state, no console errors).
    5. Confirm the toast uses the app's surface/dark-mode styling (toggle Settings ▸ Appearance to Dark and re-trigger offline to check contrast).
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe what misbehaved (toast timing, undo desync, styling).</resume-signal>
  <acceptance_criteria>
    - Normal grade shows the next card instantly with no toast; undo restores the previous card with stats + "Card N of N" counter intact (not double-counted, not reset).
    - Offline grade advances optimistically; exactly one toast appears only after retries are exhausted, then auto-dismisses; a subsequent online grade shows no toast.
    - Undo tapped immediately after grading leaves queue/stats consistent with no console errors.
    - Toast renders with app surface tokens and remains legible in Dark mode.
    - Operator replies "approved".
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New files / symbols:
- `components/Toast.tsx` — NEW `'use client'` component; default export `Toast`; `Props { message: string; onDismiss: () => void; duration?: number }`; self-dismissing, token-styled, `role="status"` status message. No new npm dependency.
- `postReviewWithRetry(cardId, rating, onExhausted)` — NEW module-level async helper in `components/StudySession.tsx` (bounded silent-retry wrapper around the background review save).

Modified in `components/StudySession.tsx`:
- New state `saveError: string | null` + new ref `isMountedRef` (with a mount/unmount effect).
- `submitReview` background save switched from bare `fetch().catch(() => {})` to `postReviewWithRetry(...)`.
- `handleUndo` success path guarded by `isMountedRef` so restoration (including the `seenCardIdsRef` write) is atomic.
- `<Toast>` rendered conditionally on `saveError`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| study client → `/api/review` | The optimistic client fires a background save across the network; the retry wrapper is self-initiated client traffic (authenticated via the shared-password cookie). No new untrusted-input surface is added — payload is the same `{ cardId, rating }` already validated server-side by Plan 01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Denial of Service | `postReviewWithRetry` retry loop | low | mitigate | Retries are hard-bounded (3 total attempts) with backoff — a persistent failure cannot spin an unbounded request loop against the app's own API / Vercel quota. |
| T-13-06 | Tampering / state integrity | handleUndo restoration | low | mitigate | REVIEW-05 mount-guarded atomic restore prevents an interrupted undo from leaving inconsistent client session state (queue vs seen-set vs stats) that could mis-report review counts. Not a cross-boundary exploit — single-tenant, client-local state only. |
| T-13-07 | Information Disclosure | save-failure toast copy | low | accept | Toast shows a generic connection message only; it does not surface server error details or card contents. Accepted. |
| T-13-SC | Tampering | dependency installs | n/a | accept | No package-manager installs — the toast is hand-rolled with existing React + Tailwind tokens; no supply-chain surface added. |

No threat in this register is High severity; the ASVS L1 block-on-high gate is not triggered.
</threat_model>

<verification>
- `npm run build` succeeds; `npm run lint` reports zero errors (`react-hooks/purity` and `react-hooks/set-state-in-effect` respected).
- Source greps confirm: `components/Toast.tsx` exists with `role="status"`; `postReviewWithRetry` and `isMountedRef` present in StudySession; the bare `fetch('/api/review', {...}).catch` background save is gone.
- Human-verify checkpoint confirms live behavior: silent retry, single toast only on exhaustion, atomic undo, dark-mode toast styling, and no regression to flip/grade/undo/mode-switch.
</verification>

<success_criteria>
- REVIEW-04: The background review save retries silently within a bounded budget; a toast appears only after all retries are exhausted, and never when a save eventually succeeds.
- REVIEW-05: After undo, the previous card reappears with queue, seen-card set, and stats restored consistently; an interrupted (unmounted) undo leaves no partial state.
- Build and lint clean; live study session behaves without regression.
</success_criteria>

<output>
Create `.planning/phases/13-review-api-hardening-save-reliability/13-02-SUMMARY.md` when done.
</output>
