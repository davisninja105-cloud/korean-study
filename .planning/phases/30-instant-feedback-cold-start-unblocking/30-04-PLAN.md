---
phase: 30-instant-feedback-cold-start-unblocking
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/settings/backfill-cookie/route.ts
  - app/settings/page.tsx
  - components/SettingsClient.tsx
  - tests/settings-backfill-cookie-route.test.ts
  - tests/settings-page-render-safety.test.ts
  - e2e/settings-flash.spec.ts
autonomous: true
requirements: [LAYOUT-01]
gap_closure: true
gap_ids: [G-30-2]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Navigating to `/settings` on an authenticated session, on a production build (`npm run build && npm start`), returns HTTP 200 and renders the real Settings UI — never the generic 'This page couldn't load. A server error occurred.' error boundary that G-30-2 diagnosed, following the fix direction recorded in `.planning/debug/settings-page-server-error.md`."
    - "`/settings` still shows the user's real saved values (button/reward color, reading scale, reading aid, goal, session size, day-start hour) immediately on first paint, sourced server-side from the same `getAllSettings()` call the page already made pre-fix — no new client round trip gates the page's own real-data render (preserves the debug session's explicit non-goal: 'must NOT make /settings itself dependent on client-side round-trip before showing real settings values')."
    - "After a `/settings` visit completes its mount effect, the non-httpOnly `ks_settings` cookie is re-seeded with the current DB-persisted buttonColor/buttonFg/rewardColor/rewardFg/readingTextScale/readingAid — preserving CR-01's original backfill intent (30-REVIEW-FIX.md) so that any pre-existing DB-persisted-but-cookie-less session gets caught up, and `app/layout.tsx`'s pre-paint script has values to read on the next navigation."
    - "`RootLayout` in `app/layout.tsx` is untouched by this plan — still a plain (non-async) function with no `await` in its body (LAYOUT-01 stays intact; verified by the pre-existing `tests/root-layout-sync.test.ts` staying green with zero edits to `app/layout.tsx`)."
    - "No Server Component render body anywhere in `app/settings/page.tsx` calls `cookies().set()` or `cookies().delete()` — the class of bug G-30-2 diagnosed (mutating cookies outside Next.js's 'action' phase) cannot be silently reintroduced, enforced by a permanent grep-based regression-guard unit test."
    - "`POST /api/settings/backfill-cookie` is a genuine Route Handler (valid 'action'-phase cookie mutation, matching the working pattern already used by `PUT /api/settings`) that writes the cookie from caller-supplied, already-known values — it makes zero new Prisma/DB calls, per the debug session's explicit fix-direction note ('reusing the already-fetched getAllSettings() result — no new DB query needed')."
  artifacts:
    - "app/api/settings/backfill-cookie/route.ts — new POST Route Handler, sets ks_settings cookie only, no DB access"
    - "app/settings/page.tsx — CR-01's render-body `cookies().set()` call removed; getAllSettings() fetch + prop-passing unchanged"
    - "components/SettingsClient.tsx — mount effect POSTs initial* prop values to the new route, fire-and-forget"
    - "tests/settings-backfill-cookie-route.test.ts — new unit test proving the route's cookie contract"
    - "tests/settings-page-render-safety.test.ts — new regression-guard unit test (no render-body cookie mutation)"
    - "e2e/settings-flash.spec.ts — extended with a direct /settings-loads-successfully + cookie-backfilled assertion"
  key_links:
    - "The new route's cookie payload shape (buttonColor/buttonFg/rewardColor/rewardFg/readingTextScale/readingAid) must exactly match what app/layout.tsx's pre-paint script parses — same shape PUT /api/settings already produces, no drift."
    - "SettingsClient.tsx's mount effect must fire unconditionally on every /settings mount (not gated on cookie presence) to preserve CR-01's 're-seed on every visit' intent, but must never block or gate the page's own already-rendered real settings values."
    - "middleware.ts's ks_auth gate covers the new route automatically (matcher excludes only /login, /api/login, and static assets) — this plan adds no new public-path exemption."
  prohibitions:
    - requirement_id: LAYOUT-01
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT call cookies().set()/.delete() from inside any Server Component's render body anywhere in this codebase — Next.js 16.2.1 only permits cookie mutation during the 'action' phase (Server Actions / Route Handlers), and calling it from render throws ReadonlyRequestCookiesError on every request (exactly G-30-2's reproduced crash). Cookie writes belong exclusively in Route Handlers (e.g. PUT /api/settings, POST /api/settings/backfill-cookie) or Server Actions, never in a plain `async function Page()` body."
---

<objective>
Fix G-30-2: `/settings` throws a server error on every visit in production because CR-01's cookie backfill (30-REVIEW-FIX.md) calls `cookies().set()` inside `app/settings/page.tsx`'s Server Component render body — a phase Next.js 16.2.1 forbids (cookie mutation is only valid during the 'action' phase). Move the backfill into a genuine Route Handler invoked once from the `/settings` client shell on mount, reusing the values the page already fetched server-side — closing the crash while preserving CR-01's original intent (re-seed `ks_settings` for any pre-existing DB-persisted session that hasn't written the cookie yet) and LAYOUT-01's constraints (no blocking `await` reintroduced in `RootLayout`; no new client-round-trip gate on `/settings`'s own real-data render).

Purpose: `/settings` is completely unusable in production right now (deterministic 500 on every visit) — this is the sole remaining blocker from Phase 30 UAT.
Output: `app/api/settings/backfill-cookie/route.ts` (new), `app/settings/page.tsx` (fixed), `components/SettingsClient.tsx` (wired), plus unit + e2e regression coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/settings-page-server-error.md
@.planning/phases/30-instant-feedback-cold-start-unblocking/30-UAT.md
@app/settings/page.tsx
@app/api/settings/route.ts
@components/SettingsClient.tsx
@app/layout.tsx
@middleware.ts
@lib/color.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Move ks_settings backfill into a valid Route Handler + wire it from the client shell</name>
  <files>app/api/settings/backfill-cookie/route.ts, app/settings/page.tsx, components/SettingsClient.tsx, tests/settings-backfill-cookie-route.test.ts</files>
  <read_first>app/settings/page.tsx (the exact render-body cookies().set() block to remove, lines 1-51), app/api/settings/route.ts (PUT handler — the valid res.cookies.set() pattern to mirror, lines 46-87), components/SettingsClient.tsx (existing mount-effect convention for the theme resolver, lines 66-71; Props interface, lines 21-30), lib/color.ts (readableForeground), tests/review-route.test.ts (fakeRequest-against-a-real-handler unit test convention, lines 85-115 — this task's test needs no DB, unlike that file's)</read_first>
  <behavior>
    - tests/settings-backfill-cookie-route.test.ts, Test 1: `POST` with a valid body (`{ buttonColor: '#111111', rewardColor: '#222222', readingTextScale: 1.2, readingAid: true }`) returns status 200; `res.cookies.get('ks_settings')` is defined, its `httpOnly` is `false`, `sameSite` is `'lax'`, `path` is `'/'`, `maxAge` is `60 * 60 * 24 * 365`; `JSON.parse(res.cookies.get('ks_settings')!.value)` deep-equals `{ buttonColor: '#111111', buttonFg: readableForeground('#111111'), rewardColor: '#222222', rewardFg: readableForeground('#222222'), readingTextScale: 1.2, readingAid: true }`.
    - Test 2: `POST` with a body missing one required field (e.g. no `readingAid`) returns status 400 and `res.cookies.get('ks_settings')` is `undefined` (no cookie set on the invalid path).
  </behavior>
  <action>
    Create `app/api/settings/backfill-cookie/route.ts` as a genuine Route Handler (`export async function POST(req: NextRequest)`), mirroring `app/api/settings/route.ts`'s `PUT` cookie-write code exactly in shape/options but with NO Prisma import and NO DB read/write of any kind — it only ever writes the cookie from values the caller already knows (per the debug session's fix direction: "reusing the already-fetched getAllSettings() result — no new DB query needed"). Parse `{ buttonColor, rewardColor, readingTextScale, readingAid }` from `await req.json().catch(() => ({}))`; validate `typeof buttonColor === 'string'`, `typeof rewardColor === 'string'`, `typeof readingTextScale === 'number' && Number.isFinite(readingTextScale)`, `typeof readingAid === 'boolean'` — on any failure return `NextResponse.json({ error: '...' }, { status: 400 })` before touching cookies. On success, build `const res = NextResponse.json({ ok: true })`, then `res.cookies.set('ks_settings', JSON.stringify({ buttonColor, buttonFg: readableForeground(buttonColor), rewardColor, rewardFg: readableForeground(rewardColor), readingTextScale, readingAid }), { httpOnly: false, secure: process.env.NODE_ENV === 'production', sameSite: 'lax', path: '/', maxAge: 60 * 60 * 24 * 365 })`, `return res`. Import `readableForeground` from `@/lib/color`.

    In `app/settings/page.tsx`: delete the `import { cookies } from 'next/headers'` line and the entire CR-01 block — the `const jar = ...` assignment through the closing of the jar-mutation call that follows it — along with its large explanatory comment (all now describing dead/invalid code). Do NOT touch anything else: `getAllSettings()` must still be called and its result must still be passed to `<SettingsClient>` exactly as before, so the page keeps rendering real settings values on first paint with zero new client-side gate. Replace the deleted comment with a short one explaining that the backfill now lives in `app/api/settings/backfill-cookie/route.ts`, invoked from `components/SettingsClient.tsx` on mount — because Next.js 16.2.1 only permits cookie mutation during its request-handling 'action' phase (Server Actions / Route Handlers), never during a Server Component's render (this is exactly what G-30-2 diagnosed). Describe the mechanism and the reason in the new comment; do not restate the specific removed function-call syntax verbatim — the goal is an explanation of *why*, not a copy of the deleted line.

    In `components/SettingsClient.tsx`: add a new `useEffect` (separate from the existing theme-resolution effect at lines 69-71 — do not merge them) that fires once per mount: `fetch('/api/settings/backfill-cookie', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ buttonColor: initialButtonColor, rewardColor: initialRewardColor, readingTextScale: initialReadingTextScale, readingAid: initialReadingAid }) }).catch(() => {})` with a dependency array of `[initialButtonColor, initialRewardColor, initialReadingTextScale, initialReadingAid]`. Use the `initial*` props directly (not the mutable `buttonColor`/`rewardColor`/etc. state) — this call exists purely to re-seed the cookie from what the server already rendered, matching CR-01's original "re-seed on every visit" intent; it is fire-and-forget (`.catch(() => {})`, matching the project's established non-blocking-write convention used by `submitReview`'s background `POST /api/review`) and never gates or delays anything already on screen.

    Create `tests/settings-backfill-cookie-route.test.ts` (Vitest, matching `tests/review-route.test.ts`'s "import the real POST handler, call it with a fake request object exposing only `.json()`" convention — no DB setup needed here since this route never touches Prisma) implementing the two Test cases in `<behavior>` above.
  </action>
  <verify>
    <automated>npm test -- settings-backfill-cookie-route</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "next/headers" app/settings/page.tsx | cat` returns 0 (the import is gone) and `grep -Ec "\.set\(|\.delete\(" app/settings/page.tsx | cat` returns 0 (no cookie-jar mutation call remains in the page) — a prose mention of the word "cookie" in the replacement comment is fine and expected, only the import path and the mutation call are checked.
    - `grep -c "getAllSettings" app/settings/page.tsx` returns 1 (server-side settings fetch preserved, unchanged render path).
    - `grep -c "res.cookies.set" app/api/settings/backfill-cookie/route.ts` returns 1.
    - `grep -c "prisma\|@/lib/settings" app/api/settings/backfill-cookie/route.ts | cat` returns 0 (no DB access in the new route).
    - `grep -c "backfill-cookie" components/SettingsClient.tsx` returns at least 1 (mount effect wired).
    - `npm test -- settings-backfill-cookie-route` passes with 2 passing test cases.
    - `npm run lint` passes clean.
  </acceptance_criteria>
  <done>`app/settings/page.tsx` no longer mutates cookies during render; a new `POST /api/settings/backfill-cookie` Route Handler re-seeds `ks_settings` from caller-supplied values with zero new DB calls; `components/SettingsClient.tsx` fires it once on mount.</done>
  <reversibility rating="reversible">Pure code relocation (render-body cookie write → Route Handler + client fetch) — fully revertible via git, no schema change, no data migration.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Regression-guard unit test + e2e proof that /settings loads (closes G-30-2)</name>
  <files>tests/settings-page-render-safety.test.ts, e2e/settings-flash.spec.ts</files>
  <read_first>tests/root-layout-sync.test.ts (static source-text-assertion convention this new unit test mirrors, in full), e2e/settings-flash.spec.ts (existing spec + beforeAll/resetToBaseline convention, in full — this task appends to it, does not replace it), e2e/smoke.spec.ts (page.goto + assertion-on-real-content convention, lines 33-45), app/settings/page.tsx and app/api/settings/backfill-cookie/route.ts (both as they exist after Task 1 — read the final state, not the pre-fix state)</read_first>
  <action>
    Create `tests/settings-page-render-safety.test.ts` (Vitest, mirroring `tests/root-layout-sync.test.ts`'s `fs.readFileSync` + regex-assertion style exactly — no jsdom, no Next.js runtime). Read `app/settings/page.tsx` as text and assert: (1) it does not match `/cookies\s*\(\s*\)\s*\.set\s*\(/` (no direct render-body cookie mutation of any kind — the exact class of bug G-30-2 diagnosed, phrased broadly enough to catch a re-introduction under a different variable name than `jar`); (2) it does not import `cookies` from `'next/headers'` (`/from\s+['"]next\/headers['"]/` should not appear, or more precisely assert the specific `import.*cookies.*next\/headers` pattern is absent); (3) it still contains `getAllSettings()` (the server-side real-data fetch stays intact — protects against a future "fix" that regresses the "no flash-of-defaults on Settings itself" constraint by deleting the server fetch instead of just the cookie write). Also read `app/api/settings/backfill-cookie/route.ts` as text and assert it contains `res.cookies.set(` (confirms the valid action-phase Route Handler pattern is what actually replaced the invalid one, not just that the old code vanished).

    In `e2e/settings-flash.spec.ts`, add one new `test(...)` (place it after the existing "saved settings persist..." test and before the "cleanup" test, inside the same file — do not create a second spec file): `'GET /settings renders the real Settings UI, not a server error (G-30-2 regression guard)'`. Body: `const response = await page.goto('/settings')`; assert `response?.status()` is `200`; assert `page.getByRole('heading', { name: 'Settings' })` is visible (`await expect(...).toBeVisible()`); then `await page.waitForResponse((res) => res.url().includes('/api/settings/backfill-cookie') && res.status() === 200)` to let the mount-effect backfill land; then read `document.cookie` via `page.evaluate` and assert it contains `'ks_settings='` — this closes CR-01's original re-seed intent end-to-end, not just via unit test. This test needs no `beforeAll` changes — it reuses the file's existing `resetToBaseline()` call.
  </action>
  <verify>
    <automated>npm test -- settings-page-render-safety</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- settings-page-render-safety` passes with all assertions green.
    - `npx playwright test e2e/settings-flash.spec.ts` passes with 3 passing tests (the 2 pre-existing + this task's new one).
    - The new e2e test asserts on the HTTP status of the actual `/settings` navigation, not just DOM presence — a regression back to the render-body `cookies().set()` bug would make this assertion fail on `response?.status()`, not merely time out.
  </acceptance_criteria>
  <done>A permanent unit test forbids reintroducing render-body cookie mutation in `app/settings/page.tsx`; an e2e test proves `/settings` genuinely loads (200 + real heading) on the exact prod-build path the debug session reproduced the crash on, and that the cookie backfill actually lands.</done>
  <reversibility rating="reversible">New/extended test files only — no production code touched, trivially deletable.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated client → `POST /api/settings/backfill-cookie` | Protected by the existing `middleware.ts` `ks_auth` gate (matcher excludes only `/login`, `/api/login`, static assets) — unchanged by this plan, no new exemption added |
| Browser (document.cookie) → `app/layout.tsx` pre-paint script | Unchanged consumer; still wrapped in `try/catch`, still degrades silently on malformed input (this plan does not touch that script) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-10 | Tampering | `POST /api/settings/backfill-cookie` (client-supplied `buttonColor`/`rewardColor`/`readingTextScale`/`readingAid`) | low | mitigate | Requires the existing `ks_auth` session (unchanged middleware gate); values are type-validated the same way `PUT /api/settings` already validates its body, and are only ever consumed by `style.setProperty()`/`classList.add()` in the pre-paint script (never `innerHTML`/`eval`) — an authenticated client can at most corrupt their own visual theme, not execute code or affect other sessions' data |
| T-30-11 | Information Disclosure | Non-httpOnly `ks_settings` cookie written by the new route (same cookie `PUT /api/settings` already writes, same accepted deviation as T-30-02 in 30-01-PLAN.md) | low | accept | Cosmetic-only UI preference data, never auth/session-related; unchanged risk profile from the already-shipped `PUT /api/settings` cookie write |
| T-30-12 | Elevation of Privilege | New route mistakenly treated as an auth/identity write path by future code | low | mitigate | The route touches only `ks_settings`, never `ks_auth`; `middleware.ts`'s gate remains the sole access-control mechanism, enforced by this plan's `must_haves.prohibitions` entry and unchanged `middleware.ts` matcher |

</threat_model>

<verification>
- `npm test -- settings-backfill-cookie-route` passes.
- `npm test -- settings-page-render-safety` passes.
- `npx playwright test e2e/settings-flash.spec.ts` passes (3 tests).
- `npm run lint` passes clean.
- Manual: `npm run build && npm start`, log in, navigate to `/settings` — page renders normally, no error boundary.
</verification>

<success_criteria>
`/settings` loads successfully on every visit in production (G-30-2 closed); CR-01's original cookie-backfill intent is preserved via a valid Route Handler instead of an invalid render-body mutation; `RootLayout`/LAYOUT-01 remains untouched and intact.
</success_criteria>

<output>
Create `.planning/phases/30-instant-feedback-cold-start-unblocking/30-04-SUMMARY.md` when done
</output>
