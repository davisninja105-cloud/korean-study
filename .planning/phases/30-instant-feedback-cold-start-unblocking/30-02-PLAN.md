---
phase: 30-instant-feedback-cold-start-unblocking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/study/loading.tsx
  - app/cards/loading.tsx
  - app/habits/loading.tsx
  - app/history/loading.tsx
  - components/StudyClient.tsx
  - tests/skeleton-token.test.ts
  - e2e/study-filter-skeleton.spec.ts
autonomous: true
requirements: [PERCEPT-01, PERCEPT-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Dark-mode `--skeleton-bg` (#1c2030) is defined in all 3 required app/globals.css locations (light `:root`, `@media (prefers-color-scheme: dark) { :root { ... } }`, `:root[data-theme=\"dark\"] { ... }`), with the two dark-mode blocks byte-identical, and is distinct from dark `--background` (#0b0f1a) — verified by tests/skeleton-token.test.ts (PERCEPT-01)."
    - "`bg-skeleton` is exposed as a Tailwind utility via `@theme inline { --color-skeleton: var(--skeleton-bg); }`, sibling to the existing `--color-surface-1/2/3` entries."
    - "All 4 route-level `loading.tsx` files (study, cards, habits, history) and the 2 inline `StudyClient.tsx` pulse blocks (`phase === 'loading'`, `phase === 'loading-practice'`) use `bg-skeleton` exclusively — zero remaining `bg-surface-3` occurrences in any of these 6 locations — while every other `bg-surface-3` consumer in the codebase (Nav.tsx, Toast.tsx, GlossProvider.tsx, ProficiencyArc.tsx, HabitTracker.tsx, CardEditor.tsx, Sheet.tsx, HabitHeatmap.tsx, AudioButton.tsx, ModeSelector.tsx, CardsClient.tsx, HabitsClient.tsx, HomeClient.tsx, FlashcardMode.tsx, StudySession.tsx, SettingsClient.tsx) is left byte-for-byte unchanged."
    - "The `isFilterLoading` branch in `components/StudyClient.tsx` renders a content-shaped skeleton (two pulsing bars inside the unchanged `h-16` slot, plus a `min-h-14 rounded-2xl` button-shaped placeholder) instead of the bare `Loader2` spinner, wrapped in the identical `flex flex-col items-center gap-6 py-6` container the real due-count/button content uses — verified structurally and via e2e/study-filter-skeleton.spec.ts bounding-box comparison (PERCEPT-03, D-04)."
    - "PERCEPT-03 adjacency: the skeleton's outer wrapper classes are byte-identical to the real content's wrapper classes at the point where one replaces the other, so the transition never shifts position — verified by e2e/study-filter-skeleton.spec.ts."
    - "PERCEPT-03 empty: the isFilterLoading skeleton markup contains no reference to `studyCards.length` or any card-count-derived value — it renders identically regardless of whether the pending fetch resolves to zero, one, or many cards, and correctly hands off to the existing (unmodified) `noDue` empty-state branch when the result is zero — verified by `grep -v studyCards.length` scoped to the isFilterLoading JSX block returning no count reference inside it."
    - { statement: "PERCEPT-03 ordering: the skeleton's three elements (count-number bar, count-label bar, button placeholder) have a fixed JSX order that never varies with data — there is no dynamic/sortable collection inside this skeleton, so no ordering-stability question applies beyond the fixed structure already reviewed in code.", verification: backstop }
    - "[FLAGGED ASSUMPTION — edge-probe unclassified, not auto-resolved per the spec-less fallback's 'unclassified stays unresolved' rule] PERCEPT-01 was probed with a generic unclassified shape; resolved substantively by the token-distinctness truth above (dark `--skeleton-bg` ≠ dark `--background`, unit-tested). Flagged for manual review if a different edge shape becomes apparent during execution or verification."
    - "UI-SPEC 'loading' row (Route-level skeletons): all 4 `loading.tsx` files + 2 `StudyClient.tsx` inline pulse states migrate to `bg-skeleton`, which is dark-visible by construction (#1c2030 against #0b0f1a background) — resolves 'never an empty void or outline-only frame' (Success Criterion 1)."
    - "UI-SPEC 'loading' row (Lesson-filter apply): bare `Loader2` spinner replaced by a pixel-matched two-bar + button skeleton inside the same `h-16` slot — content-shaped, not generic loading noise."
    - "UI-SPEC 'overflow' row (Due-count skeleton bar): the bar width is a fixed implementation-detail default and does not depend on the real due-count's digit length — it never reflows because it disappears before real content renders, so there is no overflow risk."
    - "UI-SPEC 'zero-one-many' row (Due-count skeleton bar vs digit range): not applicable in practice — the skeleton is time-bounded (shown only during the fetch) and never represents a specific digit count; its fixed placeholder width is a deliberate simplification, not a correctness gap."
  artifacts:
    - "app/globals.css — new `--skeleton-bg` CSS custom property (3 locations) + `@theme inline` exposure"
    - "app/{study,cards,habits,history}/loading.tsx — `bg-surface-3` → `bg-skeleton` token swap, no structural change"
    - "components/StudyClient.tsx — isFilterLoading skeleton rewritten; 2 inline pulse blocks token-swapped; Loader2 import removed if unused"
    - "tests/skeleton-token.test.ts — new unit test (CSS-as-text value assertions)"
    - "e2e/study-filter-skeleton.spec.ts — new e2e spec (skeleton presence + no-layout-shift assertion)"
  key_links:
    - "app/globals.css's `--skeleton-bg` token must exist before any consumer's `bg-skeleton` class has visible effect — both land in the same wave/commit set for this plan, so no consumer ever ships without the token"
    - "components/StudyClient.tsx's isFilterLoading skeleton wrapper classes must stay dimensionally identical to the real due-count/button content it precedes (~line 300-316) — flagged as a 'these two blocks must be edited together' review checkpoint per 30-RESEARCH.md Pitfall 4"
---

<objective>
Make loading states visible and honest across the app: dark-mode skeletons that are actually distinct from the background (PERCEPT-01), and a content-shaped lesson-filter skeleton instead of a bare spinner (PERCEPT-03).

Introduce one new isolated CSS token, `--skeleton-bg` (D-01–D-03, locked) — dark value `#1c2030` (reuses `--surface-1` dark), light value `#f3f4f6` (reuses `--surface-3` light) — used exclusively by loading-state placeholders, migrate the 4 route-level `loading.tsx` files plus `StudyClient.tsx`'s 2 inline pulse blocks onto it, and replace `StudyClient.tsx`'s bare `Loader2` spinner (`isFilterLoading` branch) with a pixel-matched two-bar-plus-button skeleton (D-04, locked) inside the exact same wrapper as the real content it stands in for.

Purpose: dark `--surface-3` (#0b0f1a) currently equals dark `--background` (#0b0f1a), making every existing skeleton invisible — this plan fixes visibility without touching any of the 17+ non-skeleton `bg-surface-3` consumers.
Output: `app/globals.css`, 4 `loading.tsx` files, `components/StudyClient.tsx`, `tests/skeleton-token.test.ts`, `e2e/study-filter-skeleton.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-instant-feedback-cold-start-unblocking/30-CONTEXT.md
@.planning/phases/30-instant-feedback-cold-start-unblocking/30-RESEARCH.md
@.planning/phases/30-instant-feedback-cold-start-unblocking/30-PATTERNS.md
@.planning/phases/30-instant-feedback-cold-start-unblocking/30-UI-SPEC.md
@app/globals.css
@components/StudyClient.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `--skeleton-bg` token in globals.css (PERCEPT-01, D-01, D-02, D-03)</name>
  <files>app/globals.css, tests/skeleton-token.test.ts</files>
  <read_first>app/globals.css (all 3 existing `--surface-1/2/3` definitions and the `@theme inline` block, lines 21-153), tests/card-key.test.ts (Vitest style convention)</read_first>
  <behavior>
    - tests/skeleton-token.test.ts, Test 1: reading `app/globals.css` as text, exactly 3 occurrences of the pattern `--skeleton-bg:` exist.
    - Test 2: the light `:root` block's `--skeleton-bg` value is `#f3f4f6`.
    - Test 3: the `@media (prefers-color-scheme: dark)` block's `--skeleton-bg` value is `#1c2030`, and the `:root[data-theme="dark"]` block's `--skeleton-bg` value is also `#1c2030` (byte-identical to each other).
    - Test 4: the dark `--skeleton-bg` value (`#1c2030`) is not equal to the dark `--background` value (`#0b0f1a`) — the exact defect this token exists to fix.
    - Test 5: `@theme inline` contains `--color-skeleton: var(--skeleton-bg);`.
  </behavior>
  <action>
    Add `--skeleton-bg: #f3f4f6;` inside the light `:root` block (app/globals.css, alongside the existing `--surface-1/2/3` lines ~33-35) — the light value locked by D-03. Add `--skeleton-bg: #1c2030;` inside the `@media (prefers-color-scheme: dark) { :root { ... } }` block (alongside `--surface-1/2/3` dark lines ~65-68) — the dark value locked by D-02 (reuses the existing `--surface-1` dark shade). Add `--skeleton-bg: #1c2030;` again, byte-identical, inside the `:root[data-theme="dark"] { ... }` block (alongside `--surface-1/2/3` lines ~86-89). Add `--color-skeleton: var(--skeleton-bg);` inside the `@theme inline` block, sibling to `--color-surface-1/2/3` (lines ~130-133). Create `tests/skeleton-token.test.ts` implementing the 5 Test cases in `<behavior>` via `fs.readFileSync('app/globals.css', 'utf-8')` + regex extraction (no jsdom needed).
  </action>
  <verify>
    <automated>npm test -- skeleton-token</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\-\-skeleton-bg:" app/globals.css` returns exactly 3.
    - `grep -c "\-\-color-skeleton: var(--skeleton-bg)" app/globals.css` returns exactly 1.
    - `npm test -- skeleton-token` passes with all 5 test cases green.
  </acceptance_criteria>
  <done>`--skeleton-bg` exists in all 3 required globals.css blocks with dark value #1c2030 / light value #f3f4f6, exposed as `bg-skeleton`; tests/skeleton-token.test.ts passes.</done>
  <reversibility rating="reversible">Additive-only CSS custom property with no existing consumer changed by this task — trivially removable.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Migrate the 4 route-level loading.tsx files to bg-skeleton (PERCEPT-01)</name>
  <files>app/study/loading.tsx, app/cards/loading.tsx, app/habits/loading.tsx, app/history/loading.tsx</files>
  <read_first>app/study/loading.tsx, app/cards/loading.tsx, app/habits/loading.tsx, app/history/loading.tsx (all 4, full contents already in context from prior reads — re-verify current state before editing)</read_first>
  <action>
    In each of `app/study/loading.tsx`, `app/cards/loading.tsx`, `app/habits/loading.tsx`, `app/history/loading.tsx`: replace every occurrence of the class token `bg-surface-3` with `bg-skeleton` (find-and-replace only — no structural JSX changes, no change to `animate-pulse`, sizing, or layout classes). `app/cards/loading.tsx` has this token in 8 places (3 top-bar placeholders, 1 view-toggle pill, and inside the `Array.from({ length: 5 })` card-row skeleton block × 4 divs per row template); `app/study/loading.tsx` has 3; `app/habits/loading.tsx` has 9 (2 header, 1 streak hero, 1 all-time-totals heading, 1 inside its `Array.from({ length: 4 })` block, 2 in the 30-day trend section, 2 in the heatmap section); `app/history/loading.tsx` has 3. Nothing else in these 4 files changes — they remain server components with no `'use client'` and no hooks.
  </action>
  <verify>
    <automated>grep -rc "bg-surface-3" app/study/loading.tsx app/cards/loading.tsx app/habits/loading.tsx app/history/loading.tsx | awk -F: '{s+=$2} END {exit s!=0}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bg-surface-3" app/study/loading.tsx` returns 0; same for `app/cards/loading.tsx`, `app/habits/loading.tsx`, `app/history/loading.tsx`.
    - `grep -c "bg-skeleton" app/study/loading.tsx` returns 3; `app/cards/loading.tsx` returns 8; `app/habits/loading.tsx` returns 9; `app/history/loading.tsx` returns 3 (exact 1:1 swap, no count drift).
    - `npm run lint` passes clean.
  </acceptance_criteria>
  <done>All 4 route-level loading.tsx files use `bg-skeleton` exclusively; zero remaining `bg-surface-3` occurrences in these files.</done>
  <reversibility rating="reversible">Pure class-name token swap in static files — trivially revertible.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: StudyClient.tsx — lesson-filter content-shaped skeleton + inline pulse migration (PERCEPT-03, D-04)</name>
  <files>components/StudyClient.tsx, e2e/study-filter-skeleton.spec.ts</files>
  <read_first>components/StudyClient.tsx (full file, especially the `isFilterLoading` branch ~line 269-272, the real due-count/button content ~line 300-316, the two inline pulse blocks at `phase === 'loading'` ~line 235-243 and `phase === 'loading-practice'` ~line 346-355, and the `Loader2`/`SlidersHorizontal` import at line 10), 30-UI-SPEC.md § Phase 30 Component Notes (exact target markup for the isFilterLoading replacement), e2e/active-flow.spec.ts (page.route usage pattern, lines 150-165), e2e/smoke.spec.ts (beforeAll reset convention)</read_first>
  <action>
    In `components/StudyClient.tsx`: replace the `isFilterLoading` branch's current bare-spinner markup (the `<div className="h-16 flex items-center justify-center" role="status" aria-label="Loading cards">` wrapping a `Loader2` spinner, ~line 269-272) with the exact target shape from 30-UI-SPEC.md's Component Notes: an outer `<div className="flex flex-col items-center gap-6 py-6" role="status" aria-label="Loading cards" data-testid="filter-loading-skeleton">` (add the `data-testid` — not in the UI-SPEC markup but needed for e2e targeting, consistent with this file's existing `data-testid="due-count"` / `data-testid="start-studying-btn"` convention) containing: an inner `<div className="h-16 flex flex-col items-center justify-center gap-1">` with two children — a `<div className="h-12 w-16 rounded-lg bg-skeleton animate-pulse" />` (stands in for the `text-5xl font-bold` due-count number) and a `<div className="h-4 w-24 rounded bg-skeleton animate-pulse" />` (stands in for the "card(s) ready" label) — followed by a sibling `<div className="w-full max-w-sm min-h-14 rounded-2xl bg-skeleton animate-pulse" />` (stands in for the "Start studying →" button). The outer wrapper's `flex flex-col items-center gap-6 py-6` classes MUST be byte-identical to the real content's wrapper at ~line 300 (`<div className="flex flex-col items-center gap-6 py-6">`) — this is the "nothing shifts" contract. Do not reference `studyCards.length` or any count value anywhere inside this new block.

    Migrate the 2 pre-existing inline `animate-pulse` skeleton blocks — `phase === 'loading'` (3 divs, ~line 238-240) and `phase === 'loading-practice'` (4 divs, ~line 349-352) — from `bg-surface-3` to `bg-skeleton` (7 total occurrences; token swap only, no structural change). These are separate from the `isFilterLoading` branch — do not conflate the two edits. Leave the one remaining `hover:bg-surface-3` on the filter-trigger button (~line 261) untouched — it is not a loading-state consumer and is explicitly out of D-01's migration list.

    Remove the `Loader2` import from the `lucide-react` import line (line 10) since it is no longer used anywhere in this file after the isFilterLoading rewrite — confirm via grep before removing (keep `SlidersHorizontal`).

    Create `e2e/study-filter-skeleton.spec.ts` (follow `e2e/smoke.spec.ts`'s `test.beforeAll(async () => { await resetToBaseline() })` convention). Test: `await page.goto('/study')`; use `page.route('**/api/cards/due*', async (route) => { await new Promise((r) => setTimeout(r, 600)); await route.continue() })` to artificially delay the filter re-fetch so the loading state is reliably observable (matching e2e/active-flow.spec.ts's `page.route` convention); open the lesson filter sheet (click the button showing the current range label, rendered only when `lessons.length >= 2` — the seeded fixture has 2 lessons), narrow the range by selecting a different value on the `aria-label="To lesson"` `<select>`, click "Apply"; immediately after the click, assert `page.getByTestId('filter-loading-skeleton')` becomes visible and capture its `boundingBox()`; after the delayed fetch resolves and the skeleton disappears, assert the real content container (`page.getByTestId('due-count')`'s ancestor `flex flex-col items-center gap-6 py-6` div, or read its bounding box via `page.locator('[data-testid="due-count"]').locator('..').locator('..')` — adjust the locator to the actual DOM nesting once written) occupies the same `x`/`width` as the skeleton's bounding box (allow the real content's `y` to differ only if the filter-trigger button above it changes height, which it does not in this flow) — this is the automated "nothing shifts" proof (Success Criterion 3).
  </action>
  <verify>
    <automated>npx playwright test e2e/study-filter-skeleton.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bg-surface-3" components/StudyClient.tsx` returns 1 (the one remaining, explicitly-unchanged `hover:bg-surface-3` on the filter-trigger button at ~line 261 — D-01 leaves this consumer untouched).
    - `grep -c "bg-skeleton" components/StudyClient.tsx` returns 10 (3 in isFilterLoading's new markup + 3 in the `phase === 'loading'` block + 4 in the `phase === 'loading-practice'` block).
    - `grep -c "Loader2" components/StudyClient.tsx` returns 0 (import and all usages removed).
    - `grep -A3 'data-testid="filter-loading-skeleton"' components/StudyClient.tsx | grep -c "studyCards.length"` returns 0 (confirms the PERCEPT-03 "empty" edge-case truth: the skeleton never inspects card count).
    - `npx playwright test e2e/study-filter-skeleton.spec.ts` passes.
    - `npm run lint` passes clean.
  </acceptance_criteria>
  <done>The lesson-filter loading state is a content-shaped, pixel-matched skeleton with no layout shift into the real content; both StudyClient.tsx inline pulse states use `bg-skeleton`; Loader2 import removed.</done>
  <reversibility rating="reversible">Client-component markup change only, git-revertible; no data/schema/API surface touched.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — this plan is pure CSS-token and static/client-component markup work with no new user input path, no new server code, and no new data flow.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-05 | Tampering | `app/globals.css` `--skeleton-bg` value | low | accept | Static CSS custom property, no user input, no injection vector — a wrong value only produces a miscolored/invisible skeleton, never a security issue |
| T-30-06 | Denial of Service (minor) | `page.route` delay used only inside `e2e/study-filter-skeleton.spec.ts` | low | accept | Test-only artificial delay via Playwright's route interception; does not exist in production code, no runtime attack surface introduced |

</threat_model>

<verification>
- `npm test -- skeleton-token` passes.
- `npx playwright test e2e/study-filter-skeleton.spec.ts` passes.
- `npm run lint` passes clean.
- Manual dark-mode visual check: `npm run build && npm start`, toggle Settings → Appearance → Dark, confirm skeleton shapes are visibly distinct from the page background on `/study`, `/cards`, `/habits`, `/history`.
</verification>

<success_criteria>
Every existing skeleton is visibly distinct from the dark background; the lesson-filter loading state previews the shape of the real content it precedes with zero layout shift.
</success_criteria>

<output>
Create `.planning/phases/30-instant-feedback-cold-start-unblocking/30-02-SUMMARY.md` when done
</output>

<!--
Artifacts this phase produces (Plan 30-02):
- New CSS custom property `--skeleton-bg` (3 locations) in `app/globals.css`, plus `--color-skeleton: var(--skeleton-bg);` in `@theme inline`.
- New Tailwind utility class family `bg-skeleton` (and sibling `text-skeleton`, `border-skeleton`, etc. generated by the same `@theme inline` registration).
- `bg-surface-3` → `bg-skeleton` migration in `app/study/loading.tsx`, `app/cards/loading.tsx`, `app/habits/loading.tsx`, `app/history/loading.tsx`.
- New `data-testid="filter-loading-skeleton"` attribute on the isFilterLoading skeleton wrapper in `components/StudyClient.tsx`.
- Rewritten isFilterLoading skeleton markup in `components/StudyClient.tsx` (two-bar count-slot skeleton + button-shaped placeholder).
- `bg-surface-3` → `bg-skeleton` migration on `components/StudyClient.tsx`'s 2 inline pulse blocks (`phase === 'loading'`, `phase === 'loading-practice'`).
- `Loader2` import removed from `components/StudyClient.tsx`.
- New test file `tests/skeleton-token.test.ts`.
- New e2e spec `e2e/study-filter-skeleton.spec.ts`.
-->
