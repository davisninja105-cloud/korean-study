---
phase: 27-e2e-coverage-performance-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/FlashcardMode.tsx
  - components/StudyClient.tsx
  - components/ModeSelector.tsx
  - e2e/helpers/readers.ts
  - e2e/helpers/mutate.ts
  - e2e/run-mutate.ts
  - e2e/grade-flow.spec.ts
autonomous: true
requirements: [E2E-05]

must_haves:
  truths:
    - "A full flashcard study session driven exclusively through data-testid locators runs reveal → grade → queue advance → 'Session complete!' end-to-end against the seeded DB (E2E-05, D-01/D-02)"
    - "The grade loop is a BOUNDED loop-until-complete (hard cap, break on completion heading) — never a fixed grade count: FSRS 'Good' on the seeded lastReview:null cards yields a 10-minute interval that requeues each card in-session, so ~6 taps clear 3 due cards and the exact count is wall-clock-dependent near the 2am habit-day boundary"
    - "Reveal → grade is asserted as two positive state transitions: reveal-btn visible, then after click grade-good visible — not the absence of something"
    - "Queue advance is content-anchored: the set of distinct card fronts seen during the session contains every FIXTURE.cards.due front (안녕, 학교, 감사하다) — fixture-traceable, never hardcoded counts"
    - "Grade count is asserted >= FIXTURE.dueCards, never an exact total (requeue count is time-dependent)"
    - "A non-inferable DB backstop passes: an expect.poll'd tsx-subprocess query proves all 3 seeded due CardReview rows transitioned from their seeded reps=0/lastReview=null state to reps >= 1 AND lastReview != null — optimistic grading renders the completion screen regardless of whether the background POST /api/review saves landed, so UI state alone cannot prove persistence"
    - "npm run lint passes after the data-testid pass; existing aria-labels are untouched (they carry dynamic FSRS hint text for screen readers per WR-02)"
    - "readStudySelectModeState in e2e/helpers/readers.ts anchors on the new due-count testid instead of the presentational p.text-5xl.font-bold.animate-reveal locator, closing the KNOWN-FRAGILE gap the Phase 25 comment deferred to this phase"
  artifacts:
    - "e2e/grade-flow.spec.ts — new E2E-05 spec"
    - "components/FlashcardMode.tsx — data-testid on Show Answer, 4 grade buttons, back-face card-front word"
    - "components/StudyClient.tsx — data-testid on due-count, Start studying, session-complete heading, Study N more"
    - "components/ModeSelector.tsx — value-parameterized data-testid on mode buttons"
    - "e2e/helpers/mutate.ts — seededDueReviewsPersistedDirect() + seededDueReviewsPersisted() wrapper"
    - "e2e/run-mutate.ts — 'seededDueReviewsPersisted' entry in the OPS registry"
  key_links:
    - "grade-flow.spec.ts getByTestId(...) calls → the exact data-testid string values added in Task 1 (a typo in either side hangs the loop at the visibility assert — the intended fail-fast)"
    - "grade-flow.spec.ts → resetToBaseline() (e2e/seed.ts) in beforeAll — mandatory, this spec mutates FSRS state in the shared test DB"
    - "seededDueReviewsPersisted() → runMutateOp('seededDueReviewsPersisted') → e2e/run-mutate.ts OPS map → seededDueReviewsPersistedDirect() — the two-layer subprocess shape; in-worker getTestPrisma() throws (documented twice in mutate.ts:8-23)"
---

<objective>
Close the known-fragile-locator gap with an additive `data-testid` pass across the grade-flow path (D-05, D-06), then deliver the E2E-05 study grade-flow spec: a bounded loop-until-complete flashcard session (D-01/D-02/D-03) against the existing D-13 seed baseline (D-04), with a non-inferable subprocess DB backstop proving the fire-and-forget review saves persisted.

Purpose: E2E-05 is the last uncovered study-flow behavior in the v1.6 suite — reveal, grading, REQUEUE_GAP queue advance, and session completion have zero automated coverage today, and the Phase 25 harness deliberately deferred grade-button locators to this phase.
Output: `e2e/grade-flow.spec.ts` (green), testid-instrumented grade-flow components (zero behavior change), one new subprocess DB op, updated `readStudySelectModeState`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-e2e-coverage-performance-validation/27-CONTEXT.md
@.planning/phases/27-e2e-coverage-performance-validation/27-RESEARCH.md
@.planning/phases/27-e2e-coverage-performance-validation/27-PATTERNS.md
</context>

## Artifacts this phase produces (plan 01 share)

**New files:**
- `e2e/grade-flow.spec.ts` — E2E-05 spec (bounded loop-until-complete flashcard session + DB backstop)

**New `data-testid` values (all kebab-case, additive-only):**

| Value | Element | File |
|-------|---------|------|
| `reveal-btn` | "Show Answer" button | `components/FlashcardMode.tsx` (~line 204) |
| `grade-again` | Again button (`onGrade(1)`, ~line 218) | `components/FlashcardMode.tsx` |
| `grade-hard` | Hard button (`onGrade(2)`, ~line 227) | `components/FlashcardMode.tsx` |
| `grade-good` | Good button (`onGrade(3)`, ~line 236) | `components/FlashcardMode.tsx` |
| `grade-easy` | Easy button (`onGrade(4)`, ~line 245) | `components/FlashcardMode.tsx` |
| `card-front-word` | Back-face `<p class="hangul ...">{card.front}</p>` — BOTH back-face branches (~lines 155 and 183), back face ONLY | `components/FlashcardMode.tsx` |
| `due-count` | `<p class="text-5xl font-bold animate-reveal">` due-count (~line 305) | `components/StudyClient.tsx` |
| `start-studying-btn` | "Start studying →" button (~line 312) | `components/StudyClient.tsx` |
| `session-complete-heading` | `<h2>{heading}</h2>` in SessionComplete (~line 431) | `components/StudyClient.tsx` |
| `study-more-btn` | "Study N more →" button (~line 473) | `components/StudyClient.tsx` |
| `mode-${mode.value}` (→ `mode-flashcard`, `mode-multiple-choice`, `mode-fill-blank`) | mode buttons in the `modes.map` loop (~line 31) | `components/ModeSelector.tsx` |

**New helper functions:**
- `seededDueReviewsPersistedDirect(): Promise<string>` in `e2e/helpers/mutate.ts` (tsx-subprocess-only Prisma layer)
- `seededDueReviewsPersisted(): Promise<string>` public wrapper in `e2e/helpers/mutate.ts`
- `'seededDueReviewsPersisted'` op registered in `e2e/run-mutate.ts`'s `OPS` map

<tasks>

<task type="auto">
  <name>Task 1: Additive data-testid pass across the grade-flow path + close the readers.ts fragile-locator gap</name>
  <files>components/FlashcardMode.tsx, components/StudyClient.tsx, components/ModeSelector.tsx, e2e/helpers/readers.ts</files>
  <read_first>
    - components/FlashcardMode.tsx (whole file — front/back face structure, Show Answer ~204-211, grade buttons ~216-252 with their aria-labels, back-face card.front paragraphs ~155 and ~183)
    - components/StudyClient.tsx (due-count block ~302-318, SessionComplete ~425-483)
    - components/ModeSelector.tsx (modes array ~14, modes.map loop ~29-37)
    - e2e/helpers/readers.ts (readStudySelectModeState + its KNOWN-FRAGILE doc comment ~63-80; waitVisible convention ~24-31)
    - .planning/phases/27-e2e-coverage-performance-validation/27-PATTERNS.md (component sections — exact JSX excerpts to modify)
  </read_first>
  <action>
    Add the data-testid attributes listed in the "Artifacts" table above, per D-05 and D-06. Rules:

    1. `components/FlashcardMode.tsx`: add `data-testid="reveal-btn"` to the Show Answer button; `data-testid="grade-again"` / `"grade-hard"` / `"grade-good"` / `"grade-easy"` to the four grade buttons (matched by their onGrade(1..4) handlers — Again=1, Hard=2, Good=3, Easy=4). Add `data-testid="card-front-word"` to the back-face card-front `<p className="hangul ...">{card.front}</p>` element in BOTH back-face branches (the with-sentence branch and the no-sentence branch) — back face only, NOT the front-face bare-word paragraphs, so exactly one `card-front-word` element exists in the DOM per mounted card. This gives the spec a content-anchored read of which card is being graded regardless of whether the front face shows a bare word or a sentence (research note blesses this as within D-05/D-06's grade-flow-path scope).
    2. `components/StudyClient.tsx`: add `data-testid="start-studying-btn"` to the "Start studying →" button; `data-testid="session-complete-heading"` to the SessionComplete `<h2>{heading}</h2>`; `data-testid="study-more-btn"` to the "Study N more →" button (all per D-06). Also add `data-testid="due-count"` to the `<p className="text-5xl font-bold animate-reveal">` due-count element — this is the exact presentational locator `readStudySelectModeState` flagged as KNOWN-FRAGILE in Phase 25; CONTEXT.md's canonical_refs names closing that gap as part of this phase, and exact testid values beyond D-05/D-06's named list are Claude's discretion.
    3. `components/ModeSelector.tsx`: the mode buttons render in a `modes.map` loop, so use a value-parameterized testid — `data-testid` set to the template `mode-${mode.value}` — yielding `mode-flashcard` for the button the spec clicks. Exposure is already the `useState` default sub-mode (D-03), so no sub-toggle testid is needed.
    4. `e2e/helpers/readers.ts`: switch `readStudySelectModeState`'s count locator from `page.locator('p.text-5xl.font-bold.animate-reveal')` to `page.getByTestId('due-count')`; keep the zero-due-state text fallbacks and `dumpUnrecognizedState` call unchanged. Rewrite the KNOWN-FRAGILE doc comment to record that Phase 27 closed the gap with the `due-count` testid (keep the historical pointer to 25-RESEARCH.md Ambiguity 3 for provenance).

    Hard constraints: attribute-only changes — no className, handler, JSX-structure, or aria-label modifications ('use client' stays line 1; aria-labels carry dynamic FSRS hint text for screen readers per WR-02 and D-05's additive-only note). Playwright's default testIdAttribute is `data-testid`, so zero `playwright.config.ts` changes.
  </action>
  <verify>
    <automated>npm run lint && npx playwright test e2e/smoke.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-testid' components/FlashcardMode.tsx` returns 7 (reveal-btn + 4 grade buttons + 2 card-front-word instances); `grep -c 'data-testid' components/StudyClient.tsx` returns 4; `grep -c 'data-testid' components/ModeSelector.tsx` returns 1
    - `git diff components/` shows only added `data-testid` attributes — no removed or modified lines other than the attribute insertions (aria-labels, classNames, handlers byte-identical)
    - `grep -n 'getByTestId' e2e/helpers/readers.ts` shows readStudySelectModeState using `due-count`; the string `p.text-5xl.font-bold.animate-reveal` no longer appears as a live locator argument in readers.ts
    - `npm run lint` exits 0 (project hard constraint)
    - `npx playwright test e2e/smoke.spec.ts --reporter=line` passes 4/4 — proves the readers.ts locator swap still reads the real due-count on the production build
  </acceptance_criteria>
  <done>All 12 testid instances present, diff is attribute-only, lint clean, smoke suite green with the testid-anchored reader.</done>
</task>

<task type="auto">
  <name>Task 2: E2E-05 grade-flow spec — bounded loop-until-complete + subprocess DB persistence backstop</name>
  <files>e2e/grade-flow.spec.ts, e2e/helpers/mutate.ts, e2e/run-mutate.ts</files>
  <read_first>
    - .planning/phases/27-e2e-coverage-performance-validation/27-RESEARCH.md (Pattern 1 — verified spec skeleton; Pitfall 1 — FSRS sub-day requeue empirics; Pitfall 4 — fire-and-forget save race; Pattern 4 — subprocess rule)
    - e2e/smoke.spec.ts (imports + beforeAll resetToBaseline skeleton to copy)
    - e2e/fixture.ts (FIXTURE.dueCards, FIXTURE.cards.due fronts — the assertion anchors)
    - e2e/helpers/mutate.ts (whole file — two-layer Direct/wrapper shape, RESULT_PREFIX parsing, runMutateOp's pinned DATABASE_URL: TEST_DB_URL env)
    - e2e/run-mutate.ts (OPS registry shape for the new op)
    - e2e/seed.ts (due-card CardReview create at ~line 108: state=1, stability=1, nextReview past — reps and lastReview left at defaults 0/null, which is what makes the backstop a real state transition)
    - prisma/schema.prisma (CardReview model — `card` relation, `reps`, `lastReview` fields)
  </read_first>
  <action>
    Part A — DB backstop op (subprocess pattern, mandatory shape). In `e2e/helpers/mutate.ts` add `seededDueReviewsPersistedDirect(): Promise<string>`: import FIXTURE from `../fixture`, query `prisma.cardReview.findMany` filtered to the 3 seeded due-card fronts (via the `card` relation filter `card: { front: { in: dueFronts } }`, including `card.front` for diagnostics). Return the literal string `all-persisted` iff exactly 3 rows exist and every row has `reps >= 1` and `lastReview !== null`; otherwise return a diagnostic string listing each unpersisted front with its reps/lastReview values (e.g. `pending: 안녕(reps=0,lastReview=null)`). Add the public wrapper `seededDueReviewsPersisted()` delegating to `parseMutateResult(runMutateOp('seededDueReviewsPersisted'))`, exactly like the existing `expectedDueState` pair. Register the op in `e2e/run-mutate.ts`'s `OPS` map following the existing entries. Never import getTestPrisma into the spec/worker process — the two-layer split is the documented hard constraint (mutate.ts:8-23).

    Part B — the spec. Create `e2e/grade-flow.spec.ts` following 27-RESEARCH.md Pattern 1 (a verified skeleton) with these exact behaviors, citing D-01 (Flashcards only), D-02 (full session to completion), D-03 (Exposure default — no sub-toggle interaction), D-04 (resetToBaseline, no new fixture code):

    1. Header comment: state the E2E-05 contract, the bounded-loop rationale (seeded cards have lastReview null → first "Good" yields a 10-minute ts-fsrs interval → in-session requeue → ~6 grades for 3 cards, wall-clock-dependent near the 2am habit-day boundary — 27-RESEARCH.md Pitfall 1), and the per-file `beforeAll(resetToBaseline)` ordering safeguard (this spec mutates FSRS state; smoke.spec.ts:9-13 documents the convention).
    2. `test.beforeAll` calls `resetToBaseline()` from `./seed`.
    3. One test: navigate to `/study`, click `getByTestId('start-studying-btn')` (opens the mode Sheet), click `getByTestId('mode-flashcard')` (starts the session in Exposure).
    4. Bounded loop, `MAX_GRADES = 25`, counter `grades`, accumulator `frontsSeen: Set<string>`. Each iteration: (a) wait for whichever of `reveal-btn` OR `session-complete-heading` becomes visible first — use Playwright's `locator.or()` composition (or the readers.ts `waitVisible` helper) so the iteration never races the post-grade card remount or the completion-screen mount; (b) if the completion heading is visible, break; (c) click `reveal-btn`; (d) positively assert `expect(getByTestId('grade-good')).toBeVisible()` — the reveal→grade-bar transition as a REAL state assertion; (e) read `getByTestId('card-front-word')` textContent (back face — always renders card.front) and add the trimmed value to frontsSeen; (f) click `grade-good`; increment grades. Loop exit without the heading (i.e. hitting MAX_GRADES) must fail the test — that is the intended queue-advance-regression fail-fast.
    5. Post-loop assertions: `expect(getByTestId('session-complete-heading')).toHaveText('Session complete!')` (exact text — the heading is 'Going the extra mile!' for scope=ahead, so exact text also proves the due-scope path); `expect(getByTestId('study-more-btn')).toBeVisible()` (completion-screen anchor); `expect(grades).toBeGreaterThanOrEqual(FIXTURE.dueCards)` — NEVER an exact count (anti-pattern per research: the 2am boundary makes requeue time-dependent); for each front in FIXTURE.cards.due assert frontsSeen contains it (content-anchored queue-advance proof — all 3 of 안녕/학교/감사하다 were actually shown).
    6. DB backstop: `await expect.poll(() => seededDueReviewsPersisted(), { timeout: 15_000 }).toBe('all-persisted')` — the background POST /api/review saves use bounded retry (~2s backoff), so a synchronous post-completion query races (Pitfall 4); polling converges as fast as the saves land. This is the only non-inferable persistence proof: optimistic grading renders completion before, and regardless of whether, the saves land.

    Use only `getByTestId` for the new interaction locators (Task 1's values). No `page.waitForTimeout` sleeps anywhere — locator auto-waiting plus the or()-composition wait covers every transition (reducedMotion: 'reduce' is already set in playwright.config.ts contextOptions).
  </action>
  <verify>
    <automated>npx playwright test e2e/grade-flow.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/grade-flow.spec.ts --reporter=line` exits 0 with 1 passed test (plus the setup project)
    - The spec contains a `MAX_GRADES` bound and a loop that breaks on the completion heading — `grep -n 'MAX_GRADES' e2e/grade-flow.spec.ts` non-empty; no call to `page.waitForTimeout` anywhere in the file
    - Assertions reference `FIXTURE.dueCards` and `FIXTURE.cards.due` (fixture-traceable), and use `toBeGreaterThanOrEqual` for the grade count — `grep -n 'toBeGreaterThanOrEqual(FIXTURE.dueCards)' e2e/grade-flow.spec.ts` non-empty
    - The completion assertion is `toHaveText('Session complete!')` — exact text, not a visibility-only check
    - `grep -n 'seededDueReviewsPersisted' e2e/run-mutate.ts e2e/helpers/mutate.ts e2e/grade-flow.spec.ts` shows the op registered, the Direct+wrapper pair defined, and the spec polling it via `expect.poll`
    - The spec file imports nothing from `@/lib` or `@prisma` and never calls `getTestPrisma` — all DB access goes through the `seededDueReviewsPersisted()` wrapper (subprocess boundary respected)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Grade-flow spec green on the production-build harness: full session driven to 'Session complete!' via testids only, all 3 seeded fronts seen, grade count >= 3, and the polled subprocess backstop returns all-persisted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright worker → tsx subprocess → test SQLite | The only DB access path for the new backstop op; the test DB is isolated `file:` SQLite behind the Phase 25 allow-list guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | new `seededDueReviewsPersisted` subprocess op | low | mitigate | Op runs exclusively through the existing `runMutateOp` spawn, which pins `DATABASE_URL: TEST_DB_URL` in the child env (never ambient) and inherits `playwright.config.ts`'s `assertLocalDb` guards — no new spawn path, no new connection string |
| T-27-02 | Information disclosure | `data-testid` attributes in production DOM | low | accept | Inert markup naming UI controls already visible to any user; exposes no data, no handlers, no state |

No high-severity threats. This plan adds no production behavior, no new dependencies, and no new network surface.
</threat_model>

<verification>
- `npm run lint` — clean (CLAUDE.md hard constraint on the testid pass)
- `npx playwright test e2e/grade-flow.spec.ts --reporter=line` — 1 passed
- `npx playwright test e2e/smoke.spec.ts --reporter=line` — 4 passed (readers.ts swap non-regression)
- `git diff` on the three components shows attribute-only insertions
</verification>

<success_criteria>
- E2E-05 satisfied: reveal → grade → queue advance → session completion covered end-to-end against the seeded DB, in Flashcards/Exposure mode (D-01/D-03), full session to completion (D-02), on the D-13 baseline via resetToBaseline (D-04)
- The spec is structurally immune to FSRS interval changes: bounded loop, `>= FIXTURE.dueCards` count assertion, completion detected by exact heading text
- Persistence proven non-inferably via the polled subprocess backstop (seeded reps=0/lastReview=null → reps>=1/lastReview set on all 3 due cards)
- The Phase 25 KNOWN-FRAGILE locator debt in readers.ts is retired
</success_criteria>

<output>
Create `.planning/phases/27-e2e-coverage-performance-validation/27-01-SUMMARY.md` when done. Record: final testid value list (Plan 27-02 and future specs consume these), the observed grade count in the passing run (expected ~6), and the backstop op name.
</output>
