---
phase: 27-e2e-coverage-performance-validation
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - e2e/perf.spec.ts
autonomous: true
requirements: [PERF-04, PERF-05]

must_haves:
  truths:
    - "Each of the 4 main routes (/, /study, /cards, /habits) has a page-load budget test: 5 fresh page.goto navigations, median domContentLoaded asserted < 3000ms (PERF-04, D-08 — generous guard rail, not a target)"
    - "Every individual page sample is asserted > 0 BEFORE the median comparison — the per-sample vacuity guard that kills the PerformanceEntry-serializes-to-{} pitfall (a raw entry returned across the evaluate boundary reads as undefined/NaN and must fail loudly, never pass silently)"
    - "Each of the 3 API routes (/api/cards/due, /api/stats, /api/activity) has a timing test: 5 same-origin fetches from an authenticated page via page.evaluate, timed with performance.now() with body consumption inside the timed window, median asserted < 1000ms (PERF-05, D-09/D-10)"
    - "Every individual API sample asserts res.ok === true AND non-empty body — a fast 401/500 must FAIL the budget test, never vacuously pass it"
    - "Every individual sample (page and API) is console.log'd to the line-reporter output, not just the median — a real regression vs. a single noisy run is distinguishable by eye (D-08/D-10 logged-samples requirement)"
    - "Falsifiability proven during execution: with budgets temporarily set to 1ms every perf test goes red; restored to 3000/1000 they all pass (the committed file carries 3000/1000)"
    - "The full suite (4 freshness specs + smoke + grade-flow + perf) is green in one npx playwright test run — the two new spec files coexist under the alphabetical-order + per-file-reset regime"
  artifacts:
    - "e2e/perf.spec.ts — new PERF-04 + PERF-05 spec (4 page-budget tests + 3 API-budget tests)"
  key_links:
    - "perf.spec.ts → resetToBaseline() (e2e/seed.ts) in beforeAll — grade-flow.spec.ts sorts alphabetically BEFORE perf.spec.ts and mutates FSRS state; the reset makes perf measurements deterministic (Pitfall 5)"
    - "API fetches → chromium project storageState (playwright/.auth/user.json) — the ks_auth cookie auto-attaches to same-origin page.evaluate fetches after one page.goto, which is what makes res.ok a real auth-inclusive assertion (D-10)"
    - "page.evaluate returns → picked plain fields only (responseStart/domContentLoadedEventEnd/loadEventEnd) — never a raw PerformanceEntry (Pitfall 2)"
---

<objective>
Deliver the dedicated performance-budget spec `e2e/perf.spec.ts` (D-07): median-of-5 page-load budgets on the 4 main routes via the Navigation Timing API (PERF-04, D-08) and median-of-5 API round-trip budgets on 3 data-heavy GET routes via authenticated `page.evaluate(fetch)` timing (PERF-05, D-09/D-10) — guard rails that are genuinely capable of failing but resistant to flaking.

Purpose: v1.6's freshness fix was gated on "no first-load regression" by feel-adjacent informational timing only; this makes the speed guarantee a failing-capable assertion for every future change, per the project's established "generous budgets + logged timings" stance (REQUIREMENTS.md Out of Scope, strict-ms row).
Output: `e2e/perf.spec.ts` green, full 7-spec-file suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-e2e-coverage-performance-validation/27-CONTEXT.md
@.planning/phases/27-e2e-coverage-performance-validation/27-RESEARCH.md
@.planning/phases/27-e2e-coverage-performance-validation/27-PATTERNS.md
</context>

## Artifacts this phase produces (plan 02 share)

**New files:**
- `e2e/perf.spec.ts` — PERF-04 + PERF-05 spec

**New helper functions (file-local to perf.spec.ts):**
- `median(xs: number[]): number` — sorted-middle picker (N=5 → index 2, true median)
- `sampleNav(page, route): Promise<NavSample>` — one navigation + plain-field Navigation Timing extraction (`{ ttfb, dcl, load }`)

**New constants (file-local):**
- `PAGE_BUDGET_MS = 3000` (D-08), `API_BUDGET_MS = 1000` (D-09), `SAMPLES = 5`

**No `data-testid` values, no config changes, no production-code changes.**

<tasks>

<task type="auto">
  <name>Task 1: Page-load budget tests — Navigation Timing median-of-5 on 4 routes (PERF-04)</name>
  <files>e2e/perf.spec.ts</files>
  <read_first>
    - .planning/phases/27-e2e-coverage-performance-validation/27-RESEARCH.md (Pattern 2 — the verified page-budget skeleton to copy; Pitfall 2 — PerformanceEntry serialization; Pitfall 6 — 30s default test timeout; Pitfall 9 — cold-start skew)
    - e2e/smoke.spec.ts (beforeAll skeleton + captureNavTiming's serialization-safe evaluate at ~28-31 + the [nav-timing] logging style)
    - playwright.config.ts (no custom `timeout` → 30s default applies; workers: 1; reuseExistingServer)
    - e2e/seed.ts (resetToBaseline export)
  </read_first>
  <action>
    Create `e2e/perf.spec.ts` per D-07 (dedicated file — `smoke.spec.ts`'s informational `captureNavTiming` stays untouched) implementing 27-RESEARCH.md Pattern 2:

    1. Header comment: state the PERF-04/PERF-05 contract, the "generous guard rails, not targets" stance (D-08, REQUIREMENTS.md Out of Scope strict-ms row), and the per-file `beforeAll(resetToBaseline)` ordering safeguard (grade-flow.spec.ts sorts before this file and mutates FSRS state — the reset keeps measured pages rendering deterministic seeded content).
    2. `test.beforeAll` calls `resetToBaseline()` from `./seed` (Claude's-discretion item resolved: yes, match smoke.spec.ts's pattern).
    3. File-local `median()` and `sampleNav()` helpers and a `NavSample` type per Pattern 2. `sampleNav` does `page.goto(route)` (default waitUntil 'load' populates loadEventEnd) then a `page.evaluate` that reads `performance.getEntriesByType('navigation')[0]` and returns ONLY picked plain numeric fields — `ttfb` from responseStart, `dcl` from domContentLoadedEventEnd, `load` from loadEventEnd. Never return the raw entry: it serializes to an empty object across the evaluate boundary (Pitfall 2).
    4. `PAGE_BUDGET_MS = 3000`, `SAMPLES = 5` (D-08 exact numbers). One `test(...)` PER route over `['/', '/study', '/cards', '/habits']` — 4 separate tests, not one mega-test, keeping each comfortably inside the 30s default timeout (Pitfall 6) and making failures route-attributable.
    5. In each test: collect 5 samples; for each sample, `console.log` a line-reporter-friendly record of the form `[perf] {route} sample {n}: ttfb=..ms dcl=..ms load=..ms` AND assert `dcl > 0` (per-sample vacuity guard — a zero/NaN sample means the timing entry wasn't ready or serialization broke, and must fail loudly); then assert `median(dcl samples) < PAGE_BUDGET_MS`. Assert the median of DCL (the user-facing milestone) and log TTFB alongside — research Open Question 2's recommended resolution of D-08's "TTFB / domContentLoaded" wording.
  </action>
  <verify>
    <automated>npx playwright test e2e/perf.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/perf.spec.ts --reporter=line` exits 0 with 4 passed tests, and the captured output contains 20 `[perf]` sample lines (4 routes × 5 samples) each showing real nonzero ttfb/dcl/load values
    - `grep -n 'PAGE_BUDGET_MS = 3000' e2e/perf.spec.ts` non-empty (D-08's budget, committed value)
    - The evaluate callback returns an object literal of plain numbers — `grep -n "getEntriesByType('navigation')" e2e/perf.spec.ts` shows field-picking, and no raw entry object is returned
    - Each test contains a per-sample `toBeGreaterThan(0)` assertion positioned before the median assertion
    - No `page.waitForTimeout` calls in the file
  </acceptance_criteria>
  <done>4 route-budget tests green with all 20 samples logged and individually guarded, median DCL < 3000ms per route.</done>
</task>

<task type="auto">
  <name>Task 2: API round-trip budget tests via authenticated page.evaluate(fetch) (PERF-05) + falsifiability probe + full-suite run</name>
  <files>e2e/perf.spec.ts</files>
  <read_first>
    - .planning/phases/27-e2e-coverage-performance-validation/27-RESEARCH.md (Pattern 3 — the verified API-timing skeleton; Pitfall 3 — vacuously-passing budgets)
    - e2e/perf.spec.ts (as written by Task 1 — extend, don't restructure)
    - app/api/activity/route.ts, app/api/stats/route.ts, app/api/cards/due/route.ts (confirm GET handlers — all three verified present at planning time)
  </read_first>
  <action>
    Extend `e2e/perf.spec.ts` with PERF-05 per D-09/D-10 and 27-RESEARCH.md Pattern 3:

    1. `API_BUDGET_MS = 1000` (D-09). One `test(...)` PER endpoint over `['/api/cards/due', '/api/stats', '/api/activity']` — 3 tests (route-attributable failures, timeout headroom). D-09's endpoint set is locked: `/api/cards/due` (named by the ROADMAP requirement) plus the two data-heavy GETs backing Home/Habits via lib/dashboard.ts.
    2. Each test first does one `page.goto('/')` so the chromium project's storageState `ks_auth` cookie context is live — subsequent same-origin fetches inside `page.evaluate` are authenticated automatically (D-10 locks page.evaluate(fetch) over APIRequestContext: it exercises the exact request path a real client takes).
    3. Then 5 samples: each a `page.evaluate` async callback that records `performance.now()`, `fetch`es the endpoint path, awaits `res.text()` INSIDE the timed window (full round-trip including body), and returns plain `{ ms, ok, bytes }`. Per sample: assert `ok === true` and `bytes > 0` (vacuity guards — a fast 401 redirect or 500 must fail the test, not sail under the budget, Pitfall 3), and `console.log` a `[perf] {path} sample {n}: {ms}ms` line. Then assert `median(samples) < API_BUDGET_MS`.
    4. Falsifiability probe (execute now, do not commit the probe state): temporarily set both `PAGE_BUDGET_MS` and `API_BUDGET_MS` to 1, run the spec, and confirm all 7 tests FAIL on their median assertions (proves the budgets are genuinely failing-capable, per 27-VALIDATION.md's anti-tautology contract (b)); restore 3000/1000 and re-run green. Record both run outcomes in the SUMMARY. The committed file must carry 3000 and 1000.
    5. Full-suite integration: run `npx playwright test --reporter=line` and confirm all 7 spec files (4 freshness + smoke + grade-flow + perf) pass together — proves the per-file-reset regime holds with both new files in the alphabetical order.
  </action>
  <verify>
    <automated>npx playwright test --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/perf.spec.ts --reporter=line` exits 0 with 7 passed tests (4 page + 3 API), output containing 15 `[perf] /api/...` sample lines with real millisecond values
    - Each API test asserts `ok === true` and `bytes > 0` per sample, positioned before the median assertion; body consumption (`res.text()`) happens inside the timed window
    - `grep -n 'API_BUDGET_MS = 1000' e2e/perf.spec.ts` non-empty (committed value)
    - Probe evidence captured: a run with 1ms budgets shows 7 failed; the restored run shows 7 passed
    - Full suite `npx playwright test --reporter=line` exits 0 — zero failures across all spec files including grade-flow.spec.ts from plan 27-01
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>PERF-05 green with per-sample vacuity guards, falsifiability demonstrated and reverted, and the complete E2E suite green in one run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright chromium → localhost:3100 prod server → isolated test SQLite | Only surface this plan touches; all existing Phase 25 DB-isolation guards apply unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-03 | Tampering | perf.spec.ts DB access | low | mitigate | Only DB interaction is the shared `resetToBaseline()` subprocess, which pins the test DB URL; no new connection path |
| T-27-04 | Denial of service (suite trust erosion) | flaky perf assertions on slow machines | low | mitigate | Median-of-5 + generous 3000/1000ms budgets + per-sample logging (D-08/D-09) — the design exists specifically to prevent threshold-flake eroding suite trust |

No high-severity threats. Dev-only test file; zero production or dependency changes.
</threat_model>

<verification>
- `npx playwright test e2e/perf.spec.ts --reporter=line` — 7 passed, 35 logged samples total
- Falsifiability probe: 1ms budgets → 7 failed; restored budgets → 7 passed
- `npx playwright test --reporter=line` — full suite green (all spec files)
- `npm run lint` — clean
</verification>

<success_criteria>
- PERF-04 satisfied: generous page-load budgets on the 4 key routes, Navigation Timing API, median-of-5 (D-08)
- PERF-05 satisfied: generous API budgets on `/api/cards/due`, `/api/stats`, `/api/activity` via direct authenticated request timing (D-09/D-10)
- Assertions are failing-capable (probe-proven) and flake-resistant (median + headroom + vacuity guards)
- Suite-wide coexistence proven in a single full run
</success_criteria>

<output>
Create `.planning/phases/27-e2e-coverage-performance-validation/27-02-SUMMARY.md` when done. Record: the observed median values per route/endpoint from the passing run (trend baseline for future eyeballing) and the probe run evidence.
</output>
