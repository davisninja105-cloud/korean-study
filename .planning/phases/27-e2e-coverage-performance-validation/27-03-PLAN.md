---
phase: 27-e2e-coverage-performance-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: false
requirements: [TOOL-01]

must_haves:
  truths:
    - "The Playwright MCP server is registered with the claude CLI and `claude mcp list` output includes the playwright server (D-11 — registered now, not left as a user exercise)"
    - "Registration happened only AFTER a blocking human legitimacy checkpoint for the [SUS]-flagged @playwright/mcp package (Phase 24/25 precedent; the flag is a version-recency artifact — official microsoft/playwright-mcp repo, 6.4M downloads/week, no postinstall scripts — but the human decides)"
    - "CLAUDE.md contains a concise Playwright MCP how-to (D-13): the exact registration command, the start-npm-run-dev-first prerequisite, the login step referencing APP_PASSWORD from .env.local, and 2-3 example tool calls — every tool name verified to exist in @playwright/mcp 0.0.78"
    - "The doc states the :3000 dev-server vs :3100 isolated-harness distinction and that MCP-driven grades/edits write real dev-DB FSRS state (D-12 — MCP complements, never replaces, the spec suite)"
    - "No literal secret value appears in CLAUDE.md — the login step references the APP_PASSWORD env var by name only"
    - "A live MCP exploratory smoke (browser_navigate → login → browser_snapshot against the dev server, in a fresh Claude session) was human-verified to match the documented workflow"
  artifacts:
    - "CLAUDE.md — new 'Playwright MCP (agent-driven exploratory QA)' section"
    - "Claude CLI MCP config — playwright server entry (via claude mcp add)"
  key_links:
    - "CLAUDE.md documented tool names → the verified @playwright/mcp 0.0.78 tool list in 27-RESEARCH.md Code Examples (project rule: verify claims against source before writing them)"
    - "Registration command → D-11's verbatim string; server flags require a -- separator with claude mcp add (Pitfall 7) — the flagless form needs none"
    - "MCP session → npm run dev :3000 → developer's real .env DATABASE_URL (deliberate, documented tradeoff per D-12)"
---

<objective>
Register the Playwright MCP server with Claude Code (D-11) behind the mandated package-legitimacy checkpoint, and add the concise TOOL-01 workflow documentation to CLAUDE.md (D-12/D-13) so any future Claude session can drive the running dev server interactively for exploratory QA.

Purpose: TOOL-01 closes the milestone's agent-tooling requirement — the spec suite covers regressions, but exploratory verification ("does this actually look/behave right?") needs an agent-drivable real browser against whatever is running locally.
Output: registered `playwright` MCP server, new CLAUDE.md section, human-verified live smoke.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-e2e-coverage-performance-validation/27-CONTEXT.md
@.planning/phases/27-e2e-coverage-performance-validation/27-RESEARCH.md
</context>

## Artifacts this phase produces (plan 03 share)

**Modified files:**
- `CLAUDE.md` — new `## Playwright MCP (agent-driven exploratory QA)` section, placed immediately before `## Gotchas / conventions`

**Environment/config artifacts (not repo files):**
- `playwright` MCP server registered in the Claude CLI MCP config via `claude mcp add playwright npx @playwright/mcp@latest`

**No new files, no `data-testid` values, no helper functions, no package.json changes (`@playwright/mcp` is npx-run, never installed as a dependency).**

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy checkpoint — [SUS]-flagged @playwright/mcp before registration</name>
  <action>
    STOP before running any registration or npx command. Present the legitimacy evidence below to the human and block until they respond. This checkpoint is never auto-approvable (package-legitimacy gates ignore workflow.auto_advance). Do not proceed to Task 2 without an explicit approval; carry the latest-vs-pinned choice from the response into Task 2's command.
  </action>
  <what-built>
    Nothing yet — this checkpoint gates the `claude mcp add` registration in Task 2. The package-legitimacy audit (27-RESEARCH.md) flagged `@playwright/mcp` as [SUS] on a version-recency heuristic: the 0.0.78 release was published 2026-07-12 (days old). The countervailing signals, verified against the npm registry and the source repository at research time: official `github.com/microsoft/playwright-mcp` repository, 6,402,173 downloads/week, no postinstall scripts (`postinstall: null`). Phase 24 ran the identical blocking checkpoint for the sibling `playwright` packages and approved.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@playwright/mcp — confirm the publisher is Microsoft and the repository field points at microsoft/playwright-mcp
    2. Confirm the weekly download count is in the millions (supply-chain typosquats do not have 6.4M weekly downloads)
    3. Note: D-11's command uses `@latest` (unpinned). If you prefer the conservative variant, approve with a pin and Task 2 will register `npx @playwright/mcp@0.0.78` instead
  </how-to-verify>
  <resume-signal>Type "approved" to register @latest per D-11, "approved, pin 0.0.78" for the pinned variant, or describe concerns to halt</resume-signal>
  <done>Human explicitly approved (with or without pin) — recorded for Task 2's command choice and the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Register the MCP server and write the CLAUDE.md workflow section</name>
  <files>CLAUDE.md</files>
  <read_first>
    - .planning/phases/27-e2e-coverage-performance-validation/27-RESEARCH.md (Code Examples section — the verified registration command, the verified @playwright/mcp 0.0.78 tool-name list, the 4-step workflow shape; Pitfall 7 — the -- separator rule; Pitfall 8 — wrong-server risk)
    - CLAUDE.md (whole file — match its terse reference-style prose; locate the `## Gotchas / conventions` heading for placement)
  </read_first>
  <action>
    Part A — registration (D-11). Run exactly `claude mcp add playwright npx @playwright/mcp@latest` (or the `@0.0.78`-pinned variant if Task 1's approval requested it). The flagless form needs no `--` separator (Pitfall 7). Then verify with `claude mcp list` and capture the output line showing the playwright server for the SUMMARY (research Open Question 3's resolution: `claude mcp list` / `claude mcp get playwright` is the registration proof).

    Part B — documentation (D-12/D-13). Add a new `## Playwright MCP (agent-driven exploratory QA)` section to CLAUDE.md, placed immediately before `## Gotchas / conventions` (placement is planner's discretion per CONTEXT; this puts it adjacent to the other operational reference material). Match CLAUDE.md's existing terse, reference-density prose — a concise how-to, NOT a worked-example walkthrough (D-13; the fuller walkthrough is an explicitly deferred idea). Content, all sourced from 27-RESEARCH.md's verified facts:

    1. One-time registration: the exact command `claude mcp add playwright npx @playwright/mcp@latest`, with a single sentence noting that server flags require a `--` separator (e.g. for `--headless`).
    2. Prerequisite + target: start `npm run dev` first; MCP drives whatever is running at `localhost:3000` (D-12). State the distinction explicitly: the port-3100 prod-build harness belongs to `npx playwright test`; MCP targets the dev server and the developer's real dev DB — grades/edits made in an MCP session are real FSRS writes (accepted, documented tradeoff).
    3. Login step: navigate to `http://localhost:3000` (redirects to `/login`), enter the password — reference `APP_PASSWORD` from `.env.local` by env-var name ONLY; never write a literal secret value into CLAUDE.md.
    4. Example tool calls (all verified to exist in @playwright/mcp 0.0.78): `browser_navigate` (go to a URL), `browser_snapshot` (accessibility-tree read — the token-cheap default for inspecting state), `browser_click` (interact); mention `browser_type` for the login form. Do not document tool names outside the research-verified list — CLAUDE.md's own convention is verify-before-writing.
  </action>
  <verify>
    <automated>claude mcp list 2>/dev/null | grep -i playwright && grep -n "claude mcp add playwright npx @playwright/mcp" CLAUDE.md && grep -n "APP_PASSWORD" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `claude mcp list` output contains a `playwright` server entry (registration proof, captured for the SUMMARY)
    - CLAUDE.md contains the new section with: the exact registration command string, the `npm run dev` / `localhost:3000` prerequisite, the :3000-dev vs :3100-harness distinction sentence, the real-dev-DB-writes caveat, and the login step referencing the `APP_PASSWORD` env var
    - Only tool names from the research-verified 0.0.78 list appear in the section (`browser_navigate`, `browser_snapshot`, `browser_click`, `browser_type`)
    - The section contains no literal password, token, or other secret value — env-var names only
    - The diff touches only CLAUDE.md (no package.json change — @playwright/mcp is npx-run, never a dependency)
  </acceptance_criteria>
  <done>MCP server registered and listed; CLAUDE.md section present, accurate to the verified tool list, secret-free, and matching house prose style.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live MCP exploratory smoke — verify the documented workflow end-to-end</name>
  <action>
    Present the verification steps below to the human and block. If the human reports a mismatch between the CLAUDE.md section and actual MCP behavior (wrong tool name, different login flow, wrong port), correct the CLAUDE.md section to match reality, commit the fix, and re-present this checkpoint.
  </action>
  <what-built>
    Task 2 registered the `playwright` MCP server and documented the workflow in CLAUDE.md. MCP servers load at Claude Code session start, so the session that ran the registration cannot itself use the new tools — the live smoke needs a fresh session, which is why this verification is human-driven (27-VALIDATION.md classifies TOOL-01's live smoke as manual-only).
  </what-built>
  <how-to-verify>
    1. Ensure the dev server is running: `npm run dev` (localhost:3000)
    2. Open a NEW Claude Code session in this project (so the registered MCP server loads)
    3. Ask Claude to: "Use the playwright MCP to open http://localhost:3000, log in, and describe what's on the Home page" — expect it to use `browser_navigate` (landing on /login), `browser_type` + `browser_click` with the APP_PASSWORD value from .env.local, then `browser_snapshot` showing real Home content (due-count hero, habit tracker)
    4. Cross-check the new CLAUDE.md section against what actually happened — command names, login flow, and the :3000 target must all match reality
    5. Remember: anything graded/edited during this smoke writes real FSRS state to your dev DB (documented tradeoff) — prefer navigate/snapshot over grading
  </how-to-verify>
  <resume-signal>Type "approved" if the live workflow matches the CLAUDE.md doc, or describe any mismatch so the doc can be corrected</resume-signal>
  <done>Human confirmed the live MCP session (fresh Claude session, dev server at :3000) matches the documented workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npx → npm registry → @playwright/mcp | Third-party code fetched and executed on the dev machine at MCP first-run |
| MCP browser session → localhost:3000 dev server → real dev DB | Agent-driven interaction writes real application state |
| CLAUDE.md (committed) → anyone reading the repo | Documentation must not carry secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-SC | Tampering | npx-run @playwright/mcp (npm supply chain, [SUS]-flagged) | high | mitigate | Blocking human legitimacy checkpoint (Task 1) BEFORE registration — verified official microsoft/playwright-mcp repo, 6.4M dl/wk, no postinstall scripts; optional pin to @0.0.78 offered at the checkpoint. Never auto-approvable. |
| T-27-05 | Information disclosure | CLAUDE.md login documentation | medium | mitigate | Doc references the APP_PASSWORD env var by name only; acceptance criteria require zero literal secret values in the committed section |
| T-27-06 | Tampering | MCP session mutating the real dev DB | low | accept | Deliberate D-12 design — exploratory QA targets the live dev server; the CLAUDE.md section states the tradeoff explicitly so future agent sessions know their writes are real |
</threat_model>

<verification>
- Task 1 checkpoint approved by the human before any registration
- `claude mcp list` shows the playwright server
- CLAUDE.md section present, verified-tool-names-only, secret-free
- Task 3 live smoke approved: documented workflow matches actual MCP behavior against `npm run dev`
</verification>

<success_criteria>
- TOOL-01 satisfied: Claude Code can drive the running dev server interactively via Playwright MCP, and the workflow is registered + documented in CLAUDE.md (D-11/D-12/D-13)
- The [SUS] package proceeded only through explicit human approval, consistent with the Phase 24/25 precedent
- Documentation is accurate to @playwright/mcp 0.0.78's actual tool surface and CLAUDE.md's house style
</success_criteria>

<output>
Create `.planning/phases/27-e2e-coverage-performance-validation/27-03-SUMMARY.md` when done. Record: the checkpoint outcome (latest vs pinned), the `claude mcp list` proof line, the CLAUDE.md section heading/placement, and the live-smoke result.
</output>
