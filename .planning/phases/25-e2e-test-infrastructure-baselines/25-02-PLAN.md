---
phase: 25-e2e-test-infrastructure-baselines
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - e2e/helpers/readers.ts
  - e2e/smoke.spec.ts
autonomous: true
requirements: [E2E-04]
must_haves:
  truths:
    - "npx playwright test e2e/smoke.spec.ts passes all 4 route assertions, each checking a specific value traceable to e2e/fixture.ts's FIXTURE constants, never a generic HTTP-200 or skeleton-absence check"
    - "The smoke run logs one nav-timing line per route (4 total), captured informationally with zero pass/fail budget assertions (D-06, Phase 27 adds budgets)"
    - "Smoke assertions are correct regardless of whether freshness spec files (Plan 03) ran first and mutated the DB, because the spec resets to baseline before asserting"
  artifacts:
    - path: "e2e/helpers/readers.ts"
      provides: "Retry-safe per-route DOM readers ported from scripts/diagnose-freshness.mts, reused by both smoke and freshness specs (D-05)"
      exports: ["waitVisible", "dumpUnrecognizedState", "readHomeState", "readStudySelectModeState", "readCardsCount", "readHabitsMasteredCount"]
    - path: "e2e/smoke.spec.ts"
      provides: "E2E-04: 4 per-route real-content assertions plus Navigation Timing capture"
  key_links:
    - from: "e2e/smoke.spec.ts"
      to: "e2e/helpers/readers.ts"
      via: "imports readHomeState, readStudySelectModeState, readCardsCount, readHabitsMasteredCount"
      pattern: "readers"
    - from: "e2e/smoke.spec.ts"
      to: "e2e/fixture.ts"
      via: "imports FIXTURE; assertions compare against FIXTURE.dueCards/totalCards/masteredCards, not hardcoded literals"
      pattern: "FIXTURE"
    - from: "e2e/smoke.spec.ts"
      to: "e2e/seed.ts"
      via: "resetToBaseline() in test.beforeAll guarantees deterministic counts regardless of file execution order relative to Plan 03's mutating freshness specs"
      pattern: "resetToBaseline"
---

<objective>
Port the diagnosis script's four retry-safe per-route DOM readers into a shared `e2e/helpers/readers.ts` module (D-05, D-09), trimming or flagging selectors per 25-RESEARCH.md Ambiguity 3, then write `e2e/smoke.spec.ts` (E2E-04) asserting each of the four main routes (`/`, `/study`, `/cards`, `/habits`) renders a specific seeded value on first paint, never a generic 200/skeleton-absence check, plus an informational-only Navigation Timing capture (D-06; Phase 27 adds budget assertions on top of this capture).

Purpose: this is the phase's first-load performance guard rail (ROADMAP Success Criterion 3). It must stay green through Phase 26's freshness fix and any future refactor, so a vacuous assertion here would silently defeat its entire purpose.

Output: `e2e/helpers/readers.ts` (consumed by both this plan and Plan 03's freshness specs) and a green `e2e/smoke.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-e2e-test-infrastructure-baselines/25-CONTEXT.md
@.planning/phases/25-e2e-test-infrastructure-baselines/25-RESEARCH.md
@.planning/phases/25-e2e-test-infrastructure-baselines/25-PATTERNS.md
@scripts/diagnose-freshness.mts
@components/HomeClient.tsx
@components/StudyClient.tsx
@components/ProficiencyArc.tsx
@components/CardsClient.tsx
</context>

## Artifacts this phase produces

Full phase-wide symbol inventory (this plan produces the rows marked 02; see 25-01-PLAN.md / 25-03-PLAN.md for the others):

| Artifact | Symbol(s) | Plan |
|---|---|---|
| playwright.config.ts, e2e/helpers/test-db.ts, e2e/helpers/build-guard.ts, e2e/fixture.ts, e2e/seed.ts, e2e/global-setup.ts, e2e/auth.setup.ts | see 25-01-PLAN.md | 01 |
| e2e/helpers/readers.ts | waitVisible, dumpUnrecognizedState, readHomeState, readStudySelectModeState, readCardsCount, readHabitsMasteredCount | 02 |
| e2e/smoke.spec.ts | 4 route tests + nav-timing capture | 02 |
| e2e/helpers/rsc.ts, e2e/helpers/resume.ts, e2e/helpers/mutate.ts, freshness spec files | see 25-03-PLAN.md | 03 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port per-route DOM readers into e2e/helpers/readers.ts</name>
  <files>e2e/helpers/readers.ts</files>
  <read_first>
    - scripts/diagnose-freshness.mts:481-568 - dumpUnrecognizedState, waitVisible, readHomeState, readStudySelectModeState, readCardsCount, readHabitsMasteredCount (the exact functions to port)
    - .planning/phases/25-e2e-test-infrastructure-baselines/25-RESEARCH.md - "Ambiguity 3" (which selectors to trim vs. keep vs. flag as fragile)
    - .planning/phases/25-e2e-test-infrastructure-baselines/25-PATTERNS.md - "e2e/helpers/readers.ts" section (concrete port code, already selector-verified against this session's markup)
    - components/HomeClient.tsx:196-243 - confirms hero due-count markup is a span with classes text-6xl font-bold leading-none text-reward (class order differs from the diagnosis script's ordering but CSS class selectors are order-independent) and the "Goal met today" / "All caught up" zero-due-state copy
    - components/StudyClient.tsx:226-251 - confirms select-mode due-count is a p with classes text-5xl font-bold animate-reveal (100% presentational, no semantic-token class - this is Ambiguity 3's flagged fragile locator) and the "No cards due for review right now." / "All caught up in this range!" zero-due-state copy
    - components/CardsClient.tsx:234 - confirms the toggle button text is exactly the template literal Cards (${filteredCards.length}), already role-based via getByRole('button', ...)
    - components/ProficiencyArc.tsx:28-84 - confirms the panel root is a div with classes bg-surface-1 rounded-2xl shadow-md p-6 flex flex-col gap-4 containing an h2 "Proficiency", and the mastered-count element is the ONLY p with classes text-2xl font-bold inside it (the band label at line 73 is a span, not a p, so p.text-2xl.font-bold cannot collide with it)
  </read_first>
  <action>
    Create e2e/helpers/readers.ts importing type Page from '@playwright/test'. Port five functions verbatim in logic, adjusting only the trimmed/flagged selectors below:

    waitVisible(locator, timeout = 5000): Promise&lt;boolean&gt; - exact port of scripts/diagnose-freshness.mts:501-508 (try locator.first().waitFor({ state: 'visible', timeout }), return true; catch, return false).

    dumpUnrecognizedState(page, readerName: string): Promise&lt;void&gt; - exact port of lines 484-489 (logs page.url() plus a 400-char truncated body.innerText() snapshot via console.log).

    readHomeState(page): Promise&lt;string&gt; - port of lines 510-519, with the selector TRIMMED per Ambiguity 3: use page.locator('span.text-reward') (drop .text-6xl, a presentational class not part of this project's documented semantic-token contract) as the primary locator; fall back to page.getByText('Goal met today') then page.getByText('All caught up') (1500ms timeout each) returning the literal string 'zero-due-state'; else call dumpUnrecognizedState and return '(unrecognized state)'.

    readStudySelectModeState(page): Promise&lt;string&gt; - port of lines 521-536 UNCHANGED (page.locator('p.text-5xl.font-bold.animate-reveal') - no semantic-token class exists on this element per the read_first confirmation, so this stays the known-fragile locator per Ambiguity 3; add a one-line comment above the locator noting this explicitly so a future maintainer sees the flag, not just this plan). Fallback: page.getByText matching either "No cards due for review right now." or "All caught up in this range!" returns 'zero-due-state'; else dump plus '(unrecognized state)'.

    readCardsCount(page): Promise&lt;string&gt; - exact port of lines 545-552 (page.getByRole('button', { name: /^Cards \(\d+\)$/ }) - already role-based, zero class dependency, no trim needed).

    readHabitsMasteredCount(page): Promise&lt;string&gt; - exact port of lines 554-568 (page.locator with the class chain bg-surface-1.rounded-2xl.shadow-md.p-6.flex.flex-col.gap-4 and { hasText: 'Proficiency' }, .first(), then a nested locator for p.text-2xl.font-bold, .first() - no trim needed, confirmed unique match against current markup in read_first).

    Each reader must return a plain string (never throw) so callers can call expect() on the result directly - preserves the diagnosis script's non-throwing contract.
  </action>
  <behavior>
    - Test: readCardsCount against a page showing "Cards (8)" returns the string "Cards (8)".
    - Test: readHomeState against a page with no due-count span but visible "Goal met today" text returns "zero-due-state".
    - Test: readHomeState, readStudySelectModeState, readCardsCount, readHabitsMasteredCount never throw - always resolve to a string, including '(unrecognized state)' on total non-match.
  </behavior>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "tests/study-cards.test.ts" | grep -c "e2e/helpers/readers.ts" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - e2e/helpers/readers.ts exports the four read* functions (readHomeState, readStudySelectModeState, readCardsCount, readHabitsMasteredCount) so Plan 03's specs can import them; waitVisible/dumpUnrecognizedState may be exported or module-private.
    - Type-checks clean: npx tsc --noEmit reports zero NEW errors attributable to e2e/helpers/readers.ts (the pre-existing tests/study-cards.test.ts:132,169 errors are excluded, deferred and unrelated).
    - File contains no dead code carried over from the original script (no --log-requests mode branching, no LoggedRequest/CellResult types - those belong to Plan 03's freshness helpers, not readers).
  </acceptance_criteria>
  <done>All four route readers exist, type-check cleanly, and are ready for both this plan's smoke spec and Plan 03's freshness specs to import.</done>
</task>

<task type="auto">
  <name>Task 2: Write e2e/smoke.spec.ts - 4 specific per-route assertions plus Nav Timing capture</name>
  <files>e2e/smoke.spec.ts</files>
  <read_first>
    - .planning/phases/25-e2e-test-infrastructure-baselines/25-PATTERNS.md - "e2e/smoke.spec.ts" section (core assertion shape plus Nav Timing capture snippet)
    - .planning/phases/25-e2e-test-infrastructure-baselines/25-RESEARCH.md - "False-Positive Risks" first bullet (the exact vacuous-smoke anti-pattern to avoid) and the "Phase Requirements to Test Map" E2E-04 row
    - e2e/fixture.ts (from Plan 01) - the FIXTURE constant shape (dueCards: 3, totalCards: 8, masteredCards: 3) that every assertion below must reference, never a hardcoded literal
    - e2e/seed.ts (from Plan 01) - resetToBaseline() signature
    - e2e/helpers/readers.ts (this plan's Task 1) - the four reader function signatures
  </read_first>
  <action>
    Create e2e/smoke.spec.ts importing test and expect from '@playwright/test', the four readers from './helpers/readers', resetToBaseline from './seed', and FIXTURE from './fixture'.

    ORDERING SAFEGUARD (critical, do not skip): Playwright's default file-discovery glob order is alphabetical, which places freshness-client-shell.spec.ts, freshness-fresh-paths.spec.ts, and freshness-router-cache.spec.ts (Plan 03, which mutate the DB per test) BEFORE smoke.spec.ts in a full npx playwright test run. Add a test.beforeAll that awaits resetToBaseline() at the top of the file so this spec's assertions are correct regardless of execution order or which other spec files ran first - this is what this plan's frontmatter must_haves/key_links require.

    Write one test per route, each asserting a SPECIFIC value traceable to FIXTURE (never expect(response.status()).toBe(200) alone, never a generic "no skeleton class" check - this is the exact False-Positive Risk 25-RESEARCH.md calls out):
    - Home: navigate to '/'; expect readHomeState(page) to equal String(FIXTURE.dueCards).
    - Study: navigate to '/study'; expect readStudySelectModeState(page) to equal String(FIXTURE.dueCards).
    - Cards: navigate to '/cards'; expect readCardsCount(page) to equal the literal "Cards (" + FIXTURE.totalCards + ")".
    - Habits: navigate to '/habits'; expect readHabitsMasteredCount(page) to equal String(FIXTURE.masteredCards).

    After each page navigation, capture Navigation Timing informationally (D-06, no assertion): evaluate performance.getEntriesByType('navigation') in-page, JSON.stringify the result, and console.log it prefixed with the literal tag "nav-timing" plus the current page URL. This satisfies D-06's "capture now, Phase 27 asserts budgets on top" split - do not add any expect() around timing values in this plan.

    Do not add a 5th test asserting generic HTTP 200 for any route - the per-route content assertions above already imply a successful page load; an additional vacuous check would violate the "not just HTTP 200" wording of E2E-04/ROADMAP Success Criterion 3 by suggesting that is an acceptable fallback signal.
  </action>
  <verify>
    <automated>npx playwright test e2e/smoke.spec.ts 2>&1 | tee /tmp/smoke-out.log && grep -Eq "4 passed" /tmp/smoke-out.log && test "$(grep -c 'nav-timing' /tmp/smoke-out.log)" -ge 4</automated>
  </verify>
  <acceptance_criteria>
    - npx playwright test e2e/smoke.spec.ts exits 0 and reports 4 passed (one per route), with each assertion comparing against a FIXTURE-derived value (grep the spec source for the literal token "FIXTURE." at least 4 times to confirm no hardcoded literal replaced it).
    - Console output contains at least 4 lines matching the nav-timing prefix (one per route), and zero lines matching an expect() call on a timing value (grep the spec source: zero occurrences of "toBeLessThan" or "toBeGreaterThan" anywhere in the file - confirms no budget assertion snuck in ahead of Phase 27).
    - Running the freshness spec files first (npx playwright test e2e/freshness-router-cache.spec.ts followed by npx playwright test e2e/smoke.spec.ts) still yields 4 passed for smoke - proves the beforeAll reset works regardless of prior DB mutation.
  </acceptance_criteria>
  <done>e2e/smoke.spec.ts is green, asserts specific seeded values per route (not vacuous), captures but does not gate on Navigation Timing, and is immune to execution-order coupling with Plan 03's mutating freshness specs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke spec -> seeded test DB | read-only assertions, but the beforeAll reset performs destructive writes (delete + reseed) against the isolated DB |
| ported reader helpers -> production DOM structure | readers.ts is invoked only from within the guarded Playwright test context (page fixture scoped to the storageState-authenticated chromium project); it has no standalone entry point or CLI, so it cannot be invoked outside a test run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-05 | Tampering | e2e/smoke.spec.ts's beforeAll resetToBaseline() call | medium | mitigate | resetToBaseline() (Plan 01) is itself guarded by assertLocalDb() inside e2e/seed.ts's getTestPrisma(); this plan adds no new DB-write path, it only calls the already-guarded helper |
| T-25-06 | Information Disclosure | console.log nav-timing output | low | accept | Navigation Timing entries contain only performance metrics (no PII, no card content, no auth tokens) - safe to print to stdout/CI logs |
| T-25-07 | Elevation of Privilege | readers.ts helpers invoked outside the guarded test context | low | accept | readers.ts exports plain functions taking a Playwright Page as their only input; they have no side effects of their own (pure DOM reads) and no code path that could reach production data outside a Playwright-managed browser context pointed at the isolated test server |
</threat_model>

<verification>
- npx playwright test e2e/smoke.spec.ts passes 4/4 with FIXTURE-traceable assertions (not vacuous).
- Nav Timing capture present and unbudgeted (grep confirms no expect() on timing values).
- Smoke spec is provably order-independent from Plan 03's mutating freshness specs (explicit re-run check in Task 2 acceptance criteria).
</verification>

<success_criteria>
- E2E-04 satisfied: smoke.spec.ts is green and asserts real, specific, seeded content on all 4 main routes.
- e2e/helpers/readers.ts exists, type-checks cleanly, and is ready for Plan 03 to import without modification.
- Zero changes under app/, components/, lib/, prisma/schema.prisma.
</success_criteria>

<output>
Create .planning/phases/25-e2e-test-infrastructure-baselines/25-02-SUMMARY.md when done. Record the final selector choices for each reader (confirming the Ambiguity 3 trims/flags actually applied) and the smoke spec's ordering-safeguard verification result.
</output>
