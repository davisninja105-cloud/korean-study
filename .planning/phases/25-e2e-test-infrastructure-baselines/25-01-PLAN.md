---
phase: 25-e2e-test-infrastructure-baselines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - playwright.config.ts
  - vitest.config.ts
  - .gitignore
  - e2e/helpers/test-db.ts
  - e2e/helpers/build-guard.ts
  - e2e/fixture.ts
  - e2e/seed.ts
  - e2e/global-setup.ts
  - e2e/auth.setup.ts
autonomous: false
requirements: [E2E-01, E2E-02, E2E-03, E2E-07]
must_haves:
  truths:
    - "DATABASE_URL=libsql://fake npx playwright test --list exits non-zero with a FATAL guard message before any server or browser work"
    - "npx playwright test --project=setup builds and boots the production app on 127.0.0.1:3100 against an isolated, seeded file: SQLite DB and writes playwright/.auth/user.json"
    - "npm test (Vitest) discovers zero files under e2e/ and stays green"
    - "git status --porcelain shows no test artifact (test DB, WAL/SHM sidecars, storageState, report dirs) after a run"
  artifacts:
    - path: "playwright.config.ts"
      provides: "webServer (prod build, port 3100, loopback), setup+chromium projects, trace retain-on-failure, line reporter, workers:1"
      contains: "webServer"
    - path: "e2e/helpers/test-db.ts"
      provides: "TEST_DB_PATH / TEST_DB_URL absolute-path single source of truth"
      exports: ["TEST_DB_PATH", "TEST_DB_URL"]
    - path: "e2e/helpers/build-guard.ts"
      provides: "assertLocalDb allow-list guard (WR-01 shape)"
      exports: ["assertLocalDb"]
    - path: "e2e/global-setup.ts"
      provides: "DB file reset (incl. -wal/-shm), prisma db push, WAL pragma, seed, post-seed sanity counts"
    - path: "e2e/fixture.ts"
      provides: "FIXTURE constants (dueCards:3, masteredCards:3, newCards:2, totalCards:8, lessons:2, studyDays:2)"
      exports: ["FIXTURE"]
    - path: "e2e/seed.ts"
      provides: "seedFixture / resetToBaseline / getTestPrisma"
      exports: ["seedFixture", "resetToBaseline", "getTestPrisma"]
    - path: "e2e/auth.setup.ts"
      provides: "setup-project login via POST /api/login → storageState (D-10)"
  key_links:
    - from: "playwright.config.ts"
      to: "e2e/helpers/test-db.ts"
      via: "webServer.env.DATABASE_URL pinned to TEST_DB_URL"
      pattern: "TEST_DB_URL"
    - from: "e2e/global-setup.ts"
      to: "e2e/seed.ts"
      via: "calls seedFixture() after db push"
      pattern: "seedFixture"
    - from: "e2e/auth.setup.ts"
      to: "app/api/login/route.ts"
      via: "request.post('/api/login', { data: { password } })"
      pattern: "api/login"
    - from: "playwright.config.ts"
      to: "playwright/.auth/user.json"
      via: "chromium project use.storageState + dependencies: ['setup']"
      pattern: "storageState"
---

<objective>
Stand up the Phase 25 Playwright harness foundation: `@playwright/test` install (behind a blocking legitimacy checkpoint), `playwright.config.ts` with a production-build `webServer` on dedicated port 3100 (D-12), the `e2e/` directory (D-11), the DB-isolation allow-list guard (E2E-03, WR-01), the one shared seed fixture + global setup (D-07, D-13), and setup-project auth via a real `POST /api/login` producing a reusable `storageState` (D-10, E2E-02). Also isolates Vitest and Playwright test discovery from each other (E2E-01) and configures failure traces + the `line` reporter (E2E-07). No CI wiring anywhere (D-14). This plan ports the DB-guard piece of `scripts/diagnose-freshness.mts` per D-08/D-09; the remaining ports (readers, rsc, resume) land in Plans 02–03 and the script itself is deleted in Plan 03.

Purpose: Phase 26's freshness fix may only land once these guard rails exist (milestone build-order invariant). Nothing in this phase touches production code — `app/`, `components/`, `lib/`, and `prisma/schema.prisma` are all unchanged (schema-gate: no schema edits in scope; `prisma db push` only materializes the existing schema onto the isolated `file:` test DB, never Turso).

Output: A bootable, authenticated, DB-isolated harness that Plans 02/03 write specs against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-e2e-test-infrastructure-baselines/25-CONTEXT.md
@.planning/phases/25-e2e-test-infrastructure-baselines/25-RESEARCH.md
@.planning/phases/25-e2e-test-infrastructure-baselines/25-PATTERNS.md
@scripts/diagnose-freshness.mts
@lib/auth.ts
@app/api/login/route.ts
@vitest.config.ts
@.gitignore
</context>

## Artifacts this phase produces

Full phase-wide symbol inventory (this plan produces the rows marked 01):

| Artifact | Symbol(s) | Plan |
|---|---|---|
| `playwright.config.ts` | default `defineConfig` export: webServer (port 3100, loopback), `setup`+`chromium` projects, `trace: 'retain-on-failure'`, `reporter: 'line'`, `workers: 1`, `fullyParallel: false`, `retries: 0`, `reducedMotion: 'reduce'` | 01 |
| `e2e/helpers/test-db.ts` | `TEST_DB_PATH`, `TEST_DB_URL` | 01 |
| `e2e/helpers/build-guard.ts` | `assertLocalDb(url)` | 01 |
| `e2e/fixture.ts` | `FIXTURE` constants object | 01 |
| `e2e/seed.ts` | `getTestPrisma()`, `seedFixture()`, `resetToBaseline()` | 01 |
| `e2e/global-setup.ts` | default export global-setup function | 01 |
| `e2e/auth.setup.ts` | `setup('authenticate', …)` | 01 |
| `package.json` | `"test:e2e": "playwright test"` script; `@playwright/test@1.61.1` devDependency | 01 |
| `vitest.config.ts` | `test.exclude` gains `e2e/**` | 01 |
| `.gitignore` | `/playwright/.auth/`, `/playwright-report/`, `/test-results/`, `/e2e/.tmp/` | 01 |
| `e2e/helpers/readers.ts` | `waitVisible`, `dumpUnrecognizedState`, `readHomeState`, `readStudySelectModeState`, `readCardsCount`, `readHabitsMasteredCount` | 02 |
| `e2e/smoke.spec.ts` | 4 route tests + `[nav-timing]` capture | 02 |
| `e2e/helpers/rsc.ts` | `isRscRequest(req)` | 03 |
| `e2e/helpers/resume.ts` | `simulateResume(page, hidden)` | 03 |
| `e2e/helpers/mutate.ts` | `flipOneReviewDueState`, `createMutationCard`, `promoteOneReviewToMastered`, `ensureAllSeededReviewsDue`, `countDue`, `countCards`, `countMastered` | 03 |
| `e2e/freshness-router-cache.spec.ts` | 5 Stale-RouterCache cell tests (RED) | 03 |
| `e2e/freshness-client-shell.spec.ts` | 4 Stale-ClientShell cell tests (RED) | 03 |
| `e2e/freshness-fresh-paths.spec.ts` | 7 confirmed-fresh cell tests (GREEN) | 03 |
| `scripts/diagnose-freshness.mts` | DELETED (D-08) | 03 |

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm @playwright/test package legitimacy ([SUS] heuristic flag)</name>
  <read_first>
    - .planning/phases/25-e2e-test-infrastructure-baselines/25-RESEARCH.md — "Package Legitimacy Audit" section (the [SUS] verdict and its override rationale)
    - .planning/phases/25-e2e-test-infrastructure-baselines/25-VALIDATION.md — Wave 0 Requirements (the `@playwright/test` install line)
  </read_first>
  <what-built>Nothing yet — this gate precedes the only new dependency install of the phase. `gsd-tools query package-legitimacy check` flagged `@playwright/test` as `[SUS]` (reason: `too-new`), which 25-RESEARCH.md's Package Legitimacy Audit identifies as a false positive of the recent-patch-release heuristic: the package is Microsoft-maintained (`github.com/microsoft/playwright`, first published 2020), 44.7M weekly downloads, and version 1.61.1 exactly matches the raw `playwright` package this repo already vetted and installed in Phase 24 (which went through this same checkpoint).</what-built>
  <action>STOP and present this checkpoint to the human before running any install command. Show the [SUS] flag context and the override rationale from 25-RESEARCH.md's Package Legitimacy Audit, then wait for the resume signal. Do not proceed to Task 2 (`npm install -D @playwright/test@1.61.1`) until the human types explicit approval. This gate is exempt from `workflow.auto_advance` — legitimacy checkpoints are never auto-approvable.</action>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@playwright/test
    2. Confirm: publisher is Microsoft, repository is github.com/microsoft/playwright, weekly downloads are in the tens of millions, and latest version is 1.61.x.
    3. Optionally cross-check `npm view @playwright/test version` locally (expected: 1.61.1).
  </how-to-verify>
  <acceptance_criteria>
    - Human has viewed the npmjs.com/package/@playwright/test page and typed explicit approval in this session BEFORE any npm install command runs.
    - No install command was executed prior to approval.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with `npm install -D @playwright/test@1.61.1`, or describe concerns</resume-signal>
  <done>Human has explicitly approved the @playwright/test install in-session; no install command ran before that approval</done>
</task>

<task type="auto">
  <name>Task 2: Install @playwright/test and build the config + isolation layer</name>
  <files>package.json, package-lock.json, playwright.config.ts, vitest.config.ts, .gitignore, e2e/helpers/test-db.ts, e2e/helpers/build-guard.ts</files>
  <read_first>
    - .planning/phases/25-e2e-test-infrastructure-baselines/25-PATTERNS.md — "playwright.config.ts" section (the authoritative config shape, including the WR-02 loopback `-H 127.0.0.1` flag) and "e2e/helpers/build-guard.ts" section (WR-01 allow-list `assertLocalDb`)
    - .planning/phases/25-e2e-test-infrastructure-baselines/25-RESEARCH.md — "Code Examples" + "Ambiguities" 1 and 4 + "Security Domain" (gitignore additions)
    - scripts/diagnose-freshness.mts lines 26-59 — the absolute-path `TEST_DB_PATH`/`TEST_DB_URL` construction and original guard call sites (the port source for test-db.ts / build-guard.ts)
    - vitest.config.ts and .gitignore — current contents (both small; edits are additive)
    - .planning/phases/25-e2e-test-infrastructure-baselines/25-CONTEXT.md — decisions D-07, D-08, D-09, D-11, D-12, D-14
  </read_first>
  <action>
    Run `npm install -D @playwright/test@1.61.1` (exact pin — matches the installed `playwright` 1.61.1; no `npx playwright install` needed, Chromium 1228 is already cached from Phase 24). Add a `"test:e2e": "playwright test"` script to package.json.

    Create `e2e/helpers/test-db.ts` (per D-11 the suite root is `e2e/`): export `TEST_DB_PATH` as an ABSOLUTE path to `e2e/.tmp/e2e-test.db` and `TEST_DB_URL` as `file:` + that path. Use the same absolute-path construction the diagnosis script used at lines 26-32 (path.resolve from the module's own directory; under Playwright's CJS transpilation `__dirname` is available — if it is not, fall back to `path.resolve(process.cwd(), 'e2e/.tmp/e2e-test.db')` with a comment noting the cwd assumption). The absolute path matters: Prisma CLI resolves relative `file:` URLs against the schema directory while the libsql client resolves them against cwd — an absolute URL removes that mismatch class entirely (this is why Phase 24 used one). Note: 25-RESEARCH.md is internally inconsistent on DB location (root `./e2e-test.db` in one section, `e2e/.tmp/` in another); this plan locks `e2e/.tmp/` because one gitignore directory entry then covers the DB plus its WAL/SHM sidecars, matching the proven Phase 24 `scripts/.tmp/` shape.

    Create `e2e/helpers/build-guard.ts`: port `assertLocalDb` from scripts/diagnose-freshness.mts:42-49 but INVERT it to the WR-01 allow-list shape given verbatim in 25-PATTERNS.md — fail (console.error a message containing the word FATAL, then process.exit(1)) whenever the URL is missing or does not start with `file:`, rather than only when it starts with the Turso scheme. Per D-08/D-09 this is a shared helper module, not inline spec code.

    Create `playwright.config.ts` at repo root from the 25-PATTERNS.md shape, with these locked values: `testDir: './e2e'`; `fullyParallel: false`, `workers: 1`, `retries: 0` all at TOP level (Ambiguity 4, D-07); `reporter: 'line'` (E2E-07); `use: { baseURL: 'http://localhost:3100', trace: 'retain-on-failure', reducedMotion: 'reduce' }`; `globalSetup` pointing at `./e2e/global-setup.ts`; projects `setup` (testMatch for auth.setup.ts) and `chromium` (Desktop Chrome device, `storageState: 'playwright/.auth/user.json'`, `dependencies: ['setup']`) per D-10; `webServer` with `command: 'npm run build && npm run start -- -p 3100 -H 127.0.0.1'` (prod build only per D-12 — never dev mode; `-H 127.0.0.1` is the WR-02 loopback carry-forward), `url: 'http://localhost:3100/login'`, `reuseExistingServer: !process.env.CI`, `timeout: 180_000`, and `env` pinning `DATABASE_URL: TEST_DB_URL` (imported from e2e/helpers/test-db.ts), `AUTH_SECRET: process.env.AUTH_SECRET ?? 'e2e-test-secret'`, `APP_PASSWORD: process.env.APP_PASSWORD ?? 'e2e-test-password'`, and `DATABASE_AUTH_TOKEN: ''` (blanked so the real Turso token can never reach the child even via env merge — mirrors the diagnosis script's delete at line 57). Load a gitignored `.env.test` via dotenv at the top (optional file; the fallbacks mean the suite runs with no .env.test present, so there is no user setup). No CI-conditional logic beyond `reuseExistingServer` (D-14 — no CI exists).

    DEVIATION (documented, deliberate): the E2E-07 requirement text parenthetically names `trace: 'on-first-retry'`, but with the locked `retries: 0` (Ambiguity 4 — retries would mask the SQLite-race class Pitfall 10 warns about, and would double-run the 9 deliberately-red freshness tests) `on-first-retry` never fires and NO trace would ever be produced, making E2E-07's actual behavior ("a failed run emits a trace") unsatisfiable. `retain-on-failure` produces a trace for every genuinely failed test with zero retries — it satisfies the requirement's behavior over its parenthetical mechanism. Record this rationale as a comment next to the trace line in the config so the Phase-25 verifier and Phase 26 both see it.

    Guard call sites in the config (E2E-03 defense-in-depth, negative-testable): immediately after the dotenv load, call `assertLocalDb(process.env.DATABASE_URL)` IF `process.env.DATABASE_URL` is set (an ambient remote URL anywhere in the invoking environment is a misconfiguration tripwire — refuse to run even though the harness would not use it), and unconditionally call `assertLocalDb(TEST_DB_URL)` (structural self-check that survives future edits). The harness itself never consumes `process.env.DATABASE_URL` for its own connections — every connection pins `TEST_DB_URL` — so an ambient `file:` URL (e.g. a dev shell) is ignored rather than adopted, which also means the harness can never be pointed at `dev.db` by accident.

    Edit `vitest.config.ts`: add `exclude: [...configDefaults.exclude, 'e2e/**']` inside the `test` block (import `configDefaults` from 'vitest/config'). This is the E2E-01 discovery-isolation clause — Vitest's default include glob would otherwise pick up `e2e/*.spec.ts`.

    Edit `.gitignore`: append `/playwright/.auth/`, `/playwright-report/`, `/test-results/`, `/e2e/.tmp/` (the existing `*.db` and `.env*` entries already cover the DB file and `.env.test`; `/e2e/.tmp/` additionally covers the `-wal`/`-shm` sidecars per WR-03). Leave the `/scripts/.tmp/` entry alone.
  </action>
  <verify>
    <automated>npm ls @playwright/test | grep -q "@playwright/test@1.61.1" && npm test && DATABASE_URL=libsql://fake npx playwright test --list 2>&1 | grep -q FATAL</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls @playwright/test` reports exactly `@playwright/test@1.61.1`; `package.json` has a `test:e2e` script.
    - `DATABASE_URL=libsql://fake npx playwright test --list` exits non-zero AND its output contains the FATAL guard message — before any build, server, or browser activity (E2E-03 negative test, config-level guard).
    - `DATABASE_URL=file:/tmp/whatever.db npx playwright test --list` does NOT print the FATAL message (allow-list passes any `file:` URL; a "no tests found" error is expected and acceptable at this task — auth.setup.ts does not exist yet).
    - `npm test` (Vitest) passes and its output lists only `tests/*.test.ts` files — no `e2e/` path appears.
    - `grep -q "e2e" vitest.config.ts` succeeds (exclude entry present).
    - `git check-ignore e2e/.tmp/e2e-test.db playwright/.auth/user.json test-results/x playwright-report/x` reports all four paths ignored.
  </acceptance_criteria>
  <done>Config + guard layer exists; the libsql:// negative test provably fires at config load; Vitest discovery is provably isolated; all runtime artifacts are gitignored.</done>
</task>

<task type="auto">
  <name>Task 3: Global setup (reset → push → WAL → seed → sanity), fixture, auth setup project, and first full boot</name>
  <files>e2e/fixture.ts, e2e/seed.ts, e2e/global-setup.ts, e2e/auth.setup.ts</files>
  <read_first>
    - .planning/phases/25-e2e-test-infrastructure-baselines/25-PATTERNS.md — "e2e/global-setup.ts", "e2e/seed.ts", "e2e/auth.setup.ts" sections (reset/push shapes to copy, env-first dynamic-import rule, the auth.setup.ts final shape)
    - scripts/diagnose-freshness.mts lines 51-66 (env-first override before dynamic import), 145-200 (reset + `npx prisma db push --accept-data-loss` + WAL pragma + lesson create), 202-300 (nested card/sentence/review create shape)
    - .planning/phases/25-e2e-test-infrastructure-baselines/25-RESEARCH.md — "Seed fixture data model" table (the concrete D-13 spec — treat as authoritative, do not re-derive) and the WR-03 sidecar pitfall
    - lib/dashboard.ts and lib/study-cards.ts — the exact due-count and mastered-count queries the seed must satisfy deterministically (mastered = state 2 with scheduledDays >= 21)
    - lib/habit.ts — `habitDateStr` / `shiftDate` for StudyDay date strings
    - app/api/login/route.ts and lib/auth.ts — confirm the login body shape ({ password }) and Set-Cookie behavior auth.setup.ts relies on (D-10 explicitly rejects direct computeAuthToken injection; read these to understand, not to copy)
    - prisma/schema.prisma — FK relations for delete ordering in resetToBaseline (CardDependency, Sentence, CardReview, ReviewLog, Card, Lesson, StudyDay)
  </read_first>
  <action>
    Create `e2e/fixture.ts` (pure constants, importable by spec files with no Prisma side effects): export `FIXTURE = { lessons: 2, dueCards: 3, masteredCards: 3, newCards: 2, totalCards: 8, dependencyEdges: 1, studyDays: 2 }` plus the literal card fronts/sentences used, so specs assert values traceable to the seed (D-13, and the anti-vacuous-smoke rule in 25-RESEARCH.md False-Positive Risks).

    Create `e2e/seed.ts` implementing the 25-RESEARCH.md "Seed fixture data model" table exactly: 2 Lessons (orderIndex 1, 2); 3 due cards (CardReview state 1, nextReview in the past, 1 blank-safe Sentence each — targetForm 2+ chars appearing exactly once); 3 mastered cards (state 2, scheduledDays 30); 2 new cards; 1 CardDependency edge (a due card's components names a new card's front, edge created the way sync-time linking does); 2 StudyDay rows on consecutive habit-dates (today + yesterday via `habitDateStr(2)`/`shiftDate` from lib/habit so the Habits streak renders 2 deterministically regardless of wall-clock hour). Use `normalizeFront` from lib/card-key for every `normalizedFront`. Export three functions: `getTestPrisma()` — lazy singleton that pins `process.env.DATABASE_URL = TEST_DB_URL` (from e2e/helpers/test-db.ts) and only THEN dynamically imports lib/prisma (env-first dynamic-import rule from 24-PATTERNS; use an extensionless import specifier, e.g. '../lib/prisma' — Playwright's transpiler resolves it; the tsx-era '.js' suffix convention from scripts/ does not apply here); `seedFixture()` — the inserts above; `resetToBaseline()` — deletes all rows in FK-safe order (CardDependency, Sentence, CardReview, ReviewLog, Card, Lesson, StudyDay — confirm order against prisma/schema.prisma; leave Setting alone) then calls `seedFixture()`. resetToBaseline is what Plans 02/03 call in `test.beforeEach` so every spec starts from the same baseline regardless of file ordering or prior mutations.

    NEW-CARD DETERMINISM RULE: before writing the seed, read the due-count queries in lib/dashboard.ts (getStats) and lib/study-cards.ts (getStudyCards) and choose the 2 new cards' CardReview shape (state 0 with far-future nextReview, or no review row — whichever both queries exclude) so the live due count is exactly FIXTURE.dueCards and Cards-page total is exactly FIXTURE.totalCards.

    Create `e2e/global-setup.ts` (Playwright globalSetup default export): (1) call `assertLocalDb(TEST_DB_URL)` and, if `process.env.DATABASE_URL` is set, `assertLocalDb(process.env.DATABASE_URL)` — same two-guard shape as the config (E2E-03 runtime re-check); (2) mkdir `e2e/.tmp` recursive, then rmSync `TEST_DB_PATH`, `TEST_DB_PATH + '-wal'`, and `TEST_DB_PATH + '-shm'` with force (WR-03 — all three files, never just the main one); (3) `execSync('npx prisma db push --accept-data-loss', { env: { ...process.env, DATABASE_URL: TEST_DB_URL }, stdio: 'inherit' })` — the verbatim Phase 24 pattern, works on `file:` URLs (the Turso DDL gotcha does not apply); (4) enable WAL via `$executeRawUnsafe` with the journal_mode pragma on the seeded client. DEVIATION from 25-RESEARCH.md assumption A2 (documented, deliberate): A2's "skip WAL" reasoning covers only the short seed step, but Plan 03's freshness specs re-create Phase 24's exact dual-process contention profile (per-test resetToBaseline plus mid-test Prisma writes from the worker while the server serves RSC loads, across 21 tests) — the P1008/SQLITE_BUSY cascade Phase 24 hit is a live risk, its proven fix costs one line, and the WR-03 sidecar cleanup it requires is already mandated in step (2). Note this rationale in a comment. (5) call `seedFixture()`; (6) post-seed sanity assertions — query live counts (due, total cards, mastered) via `getTestPrisma()` and throw with a diff message if any differs from FIXTURE — this makes fixture drift impossible to miss and is what makes Plan 02's smoke assertions trustworthy; (7) log a distinctive line such as "[global-setup] seeded N cards" so run ordering is verifiable.

    Create `e2e/auth.setup.ts` exactly per the 25-PATTERNS.md / 25-RESEARCH.md code example (D-10): `setup('authenticate', …)` posts `{ password: process.env.APP_PASSWORD ?? 'e2e-test-password' }` to `/api/login` via the `request` fixture, throws on non-OK with status + body text, then `request.storageState({ path: 'playwright/.auth/user.json' })`. No computeAuthToken import, no addCookies — the setup-project pattern replaces Phase 24's direct injection.

    Then run the first full boot: `npx playwright test --project=setup`. Capture stdout+stderr to a scratch file. ORDERING CHECK: confirm the "[global-setup]" log line appears BEFORE Next.js's production-build output in the captured log (Playwright runs globalSetup before launching webServer; if the observed order is inverted on this Playwright version, STOP — move the reset/push/seed steps into a first project that runs before `setup` instead, and record the finding in the SUMMARY — do not let a server boot against an unseeded/re-created DB file go unnoticed).

    Do NOT gate anything on full-project `npx tsc --noEmit` — it has pre-existing, deferred errors in tests/study-cards.test.ts (STATE.md deferred item, predates this phase).
  </action>
  <verify>
    <automated>npx playwright test --project=setup && test -f playwright/.auth/user.json && test -f e2e/.tmp/e2e-test.db && test -z "$(git status --porcelain | grep -v '.planning/')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --project=setup` exits 0: global-setup resets + pushes schema + seeds, webServer runs `npm run build && npm run start -- -p 3100 -H 127.0.0.1`, and the `authenticate` setup test passes (1 passed).
    - The captured run log shows the `[global-setup]` line before the Next.js build output (ordering check), and shows the post-seed sanity counts passing.
    - `playwright/.auth/user.json` exists and contains a `ks_auth` cookie entry (grep for `ks_auth` in the file).
    - `e2e/.tmp/e2e-test.db` exists; `lsof -i :3100` shows nothing bound after the run completes (webServer teardown) OR the server was intentionally left via reuseExistingServer and is on 127.0.0.1 only.
    - `git status --porcelain` shows no `e2e/.tmp/`, `playwright/.auth/`, `test-results/`, or `playwright-report/` entries (gitignore proof).
    - `npm run lint` passes with zero errors (repo constraint: lint stays clean).
  </acceptance_criteria>
  <done>One command boots a seeded, authenticated, DB-isolated production server and produces a reusable storageState; E2E-01/02/03 infrastructure is real and proven, E2E-07's trace/reporter config is in place (trace artifact verification happens in Plan 03 where real failures exist).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → database | global-setup/seed perform destructive writes (rm + db push + deletes) — must be structurally confined to the isolated `file:` DB |
| repo → git remote | storageState (a valid session cookie) and test secrets must never be committed |
| test server → LAN | `next start` binds a weak fixed-password app for the run's duration |
| npm registry → node_modules | one new devDependency installed this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering | e2e/global-setup.ts + webServer env (destructive DB ops hitting production Turso) | critical | mitigate | `assertLocalDb` allow-list (WR-01 shape) called in playwright.config.ts AND global-setup.ts before any file/DB operation; every connection pins the absolute `TEST_DB_URL` (never `process.env.DATABASE_URL`); `DATABASE_AUTH_TOKEN` blanked in webServer.env; negative test (`libsql://fake` → FATAL exit) actually executed in Task 2 acceptance |
| T-25-02 | Information Disclosure | playwright/.auth/user.json (real HMAC cookie, permanent until secret rotation) | medium | mitigate | `/playwright/.auth/` gitignored (Task 2) + cookie is minted against the test-only `AUTH_SECRET` fallback, so it is useless against production |
| T-25-03 | Information Disclosure | playwright.config.ts (committed file) holding secrets | medium | mitigate | secrets read from gitignored `.env.test` with obviously-fake in-code fallbacks (`e2e-test-secret` / `e2e-test-password`); real production values never referenced |
| T-25-04 | Elevation of Privilege | test `next start` server reachable on LAN | low | mitigate | `-H 127.0.0.1` loopback bind in webServer.command (WR-02 carry-forward) |
| T-25-SC | Tampering | npm install of `@playwright/test` | high | mitigate | Task 1 blocking human-verify legitimacy checkpoint ([SUS] heuristic flag; manual verification against npmjs.com/package/@playwright/test + microsoft/playwright repo) — never auto-approved |
</threat_model>

<verification>
- E2E-03 negative test provably fires: `DATABASE_URL=libsql://fake npx playwright test --list` → non-zero exit + FATAL message (must be RUN, not code-reviewed — 25-RESEARCH.md False-Positive Risks).
- `npx playwright test --project=setup` green; storageState file exists with a `ks_auth` cookie.
- `npm test` green with zero `e2e/` files discovered.
- No runtime artifact in `git status`.
</verification>

<success_criteria>
- All Task 2 + Task 3 acceptance criteria pass.
- Requirements E2E-01 (config half), E2E-02, E2E-03 fully satisfied; E2E-07 config in place (behavioral proof deferred to Plan 03).
- Zero changes under `app/`, `components/`, `lib/`, `prisma/schema.prisma`.
</success_criteria>

<output>
Create `.planning/phases/25-e2e-test-infrastructure-baselines/25-01-SUMMARY.md` when done. Record: the exact seed shapes chosen for the 2 new cards (review row present/absent), the globalSetup-vs-webServer ordering observation, and the trace-mode deviation note (retain-on-failure vs the requirement's on-first-retry parenthetical) for the verifier.
</output>
