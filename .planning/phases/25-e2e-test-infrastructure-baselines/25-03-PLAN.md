---
phase: 25-e2e-test-infrastructure-baselines
plan: 03
type: execute
wave: 3
depends_on: ["25-01", "25-02"]
files_modified:
  - e2e/helpers/rsc.ts
  - e2e/helpers/resume.ts
  - e2e/helpers/mutate.ts
  - e2e/freshness-router-cache.spec.ts
  - e2e/freshness-client-shell.spec.ts
  - e2e/freshness-fresh-paths.spec.ts
  - scripts/diagnose-freshness.mts
autonomous: false
requirements: [E2E-06, E2E-07]
must_haves:
  truths:
    - "npx playwright test e2e/freshness-router-cache.spec.ts e2e/freshness-client-shell.spec.ts fails on exactly the 9 cells 24-DIAGNOSIS.md predicts as stale (5 Stale-RouterCache + 4 Stale-ClientShell), using plain expect() assertions, never test.fail()/test.fixme()"
    - "npx playwright test e2e/freshness-fresh-paths.spec.ts passes all 7 confirmed-fresh cells"
    - "Every freshness assertion pairs a DOM-content check with a network-evidence check (isRscRequest on captured requests), per D-03"
    - "A forced-failing run produces a Playwright trace openable via npx playwright show-trace, and stdout is line-reporter formatted"
    - "scripts/diagnose-freshness.mts no longer exists; its reusable pieces are verifiably present in e2e/helpers/"
  artifacts:
    - path: "e2e/helpers/rsc.ts"
      provides: "isRscRequest() ported verbatim (drop dead uppercase-RSC branch per IN-01)"
      exports: ["isRscRequest"]
    - path: "e2e/helpers/resume.ts"
      provides: "simulateResume() ported verbatim (dual document.hidden/visibilityState override)"
      exports: ["simulateResume"]
    - path: "e2e/helpers/mutate.ts"
      provides: "DB mutators + live expected-value queries used by all three freshness specs"
      exports: ["flipOneReviewDueState", "createMutationCard", "promoteOneReviewToMastered", "ensureAllSeededReviewsDue", "expectedDueState", "expectedCardsCount", "expectedMasteredCount"]
    - path: "e2e/freshness-router-cache.spec.ts"
      provides: "5 Stale-RouterCache cells (RED): / back-forward, / resume, /study resume, /cards resume, /habits resume"
    - path: "e2e/freshness-client-shell.spec.ts"
      provides: "4 Stale-ClientShell cells (RED): /study back-forward, /cards back-forward, /habits back-forward, /habits post-mutation-return"
    - path: "e2e/freshness-fresh-paths.spec.ts"
      provides: "7 confirmed-fresh cells (GREEN): 4 plain-Link cells + / and /study post-mutation-return + /cards post-mutation-return"
  key_links:
    - from: "e2e/freshness-router-cache.spec.ts"
      to: "e2e/helpers/rsc.ts"
      via: "isRscRequest(req) classifies captured page.on('request') entries as RSC data fetches"
      pattern: "isRscRequest"
    - from: "e2e/freshness-router-cache.spec.ts"
      to: "e2e/helpers/resume.ts"
      via: "simulateResume(page, true/false) drives the 4 resume cells"
      pattern: "simulateResume"
    - from: "all three freshness spec files"
      to: "e2e/helpers/mutate.ts"
      via: "each cell mutates via a DB helper then reads the live expected value via the matching expected* function"
      pattern: "expected"
    - from: "all three freshness spec files"
      to: "e2e/helpers/readers.ts"
      via: "reuses Plan 02's readHomeState/readStudySelectModeState/readCardsCount/readHabitsMasteredCount for the DOM half of D-03's dual assertion"
      pattern: "readers"
    - from: "all three freshness spec files"
      to: "e2e/seed.ts"
      via: "resetToBaseline() in test.beforeEach isolates each of the 16 cells from every other cell's mutation"
      pattern: "resetToBaseline"
---

<objective>
Port the diagnosis script's `isRscRequest()` predicate and `simulateResume()` simulator verbatim (D-08/D-09), extract its DB mutators and live-expected-value queries into a shared `e2e/helpers/mutate.ts`, and encode the full 16-cell matrix from `24-DIAGNOSIS.md` as three real Playwright spec files split by root cause (D-02): `freshness-router-cache.spec.ts` and `freshness-client-shell.spec.ts` (9 cells total, deliberately RED — this IS the D-04 re-verification of CR-01's 4 flagged back-forward cells) and `freshness-fresh-paths.spec.ts` (7 cells, GREEN — the regression net Phase 26 must not break). Then retire the throwaway script (D-08) now that every reusable piece lives in `e2e/`.

Purpose: E2E-06 is the milestone's central deliverable — Phase 26 cannot be verified as fixing anything without a red spec proving the bug exists today. A fully-green freshness run at the end of this phase is a scope violation, not a pass (25-VALIDATION.md, 25-RESEARCH.md False-Positive Risks).

Output: three freshness spec files matching 24-DIAGNOSIS.md's Summary Matrix exactly (9 red / 7 green), shared DB-mutation/RSC-detection/resume-simulation helpers, and `scripts/diagnose-freshness.mts` deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-e2e-test-infrastructure-baselines/25-CONTEXT.md
@.planning/phases/25-e2e-test-infrastructure-baselines/25-RESEARCH.md
@.planning/phases/25-e2e-test-infrastructure-baselines/25-PATTERNS.md
@.planning/phases/24-freshness-diagnosis-spike/24-DIAGNOSIS.md
@.planning/phases/24-freshness-diagnosis-spike/24-REVIEW.md
@scripts/diagnose-freshness.mts
</context>

## Artifacts this phase produces

Full phase-wide symbol inventory (this plan produces the rows marked 03; see 25-01-PLAN.md / 25-02-PLAN.md for the others):

| Artifact | Symbol(s) | Plan |
|---|---|---|
| playwright.config.ts, e2e/helpers/test-db.ts, e2e/helpers/build-guard.ts, e2e/fixture.ts, e2e/seed.ts, e2e/global-setup.ts, e2e/auth.setup.ts | see 25-01-PLAN.md | 01 |
| e2e/helpers/readers.ts, e2e/smoke.spec.ts | see 25-02-PLAN.md | 02 |
| e2e/helpers/rsc.ts | isRscRequest | 03 |
| e2e/helpers/resume.ts | simulateResume | 03 |
| e2e/helpers/mutate.ts | flipOneReviewDueState, createMutationCard, promoteOneReviewToMastered, ensureAllSeededReviewsDue, expectedDueState, expectedCardsCount, expectedMasteredCount | 03 |
| e2e/freshness-router-cache.spec.ts | 5 Stale-RouterCache tests | 03 |
| e2e/freshness-client-shell.spec.ts | 4 Stale-ClientShell tests | 03 |
| e2e/freshness-fresh-paths.spec.ts | 7 confirmed-fresh tests | 03 |
| scripts/diagnose-freshness.mts | DELETED (D-08) | 03 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port rsc/resume predicates and extract mutate.ts</name>
  <files>e2e/helpers/rsc.ts, e2e/helpers/resume.ts, e2e/helpers/mutate.ts</files>
  <read_first>
    - scripts/diagnose-freshness.mts:109-134 - isRscRequest() and its documenting comment block (the RSC-fetch signature confirmed in 24-DIAGNOSIS.md's "RSC Request Signature" section)
    - scripts/diagnose-freshness.mts:465-477 - simulateResume() (dual document.hidden/document.visibilityState override plus dispatchEvent)
    - scripts/diagnose-freshness.mts:588-696 - withDbRetry, expectedDueState, expectedCardsCount, expectedMasteredCount, flipOneReviewDueState, createMutationCard, promoteOneReviewToMastered, ensureAllSeededReviewsDue (the mutators and live-query helpers to extract)
    - 24-freshness-diagnosis-spike/24-REVIEW.md - finding IN-01 (the dead uppercase-'RSC' branch to drop when porting isRscRequest)
    - .planning/phases/25-e2e-test-infrastructure-baselines/25-PATTERNS.md - "e2e/helpers/rsc.ts" and "e2e/helpers/resume.ts" sections (exact verbatim-port code)
    - e2e/seed.ts (Plan 01) - getTestPrisma() signature; mutate.ts's functions must operate against the SAME Prisma client instance the seed uses, not a second ad-hoc client, since Phase 24's contention issue (Don't Hand-Roll table, RESEARCH.md) was specifically about multiple long-lived clients on one file: DB
  </read_first>
  <action>
    Create e2e/helpers/rsc.ts: import type Request as PwRequest from '@playwright/test'; export isRscRequest(req: PwRequest): boolean returning req.headers()['rsc'] === '1' only - drop the dead uppercase 'RSC' OR-branch scripts/diagnose-freshness.mts:133 has, per Phase 24 code review IN-01 (it never matched in the 17/24 sanity-gate run and is confirmed dead).

    Create e2e/helpers/resume.ts: import type Page from '@playwright/test'; export async simulateResume(page: Page, hidden: boolean): Promise&lt;void&gt; - exact port of scripts/diagnose-freshness.mts:468-477 (page.evaluate defining both document.hidden and document.visibilityState as configurable properties, then dispatching a visibilitychange Event).

    Create e2e/helpers/mutate.ts. Import getTestPrisma from '../seed' (Plan 01's lazy singleton - do NOT create a second Prisma client here; reusing the seed's client is what avoids re-introducing Phase 24's dual-client SQLite contention). Do not port withDbRetry as a separate retry wrapper - Phase 24's Don't Hand-Roll table (25-RESEARCH.md) explains that contention pattern was specific to a long-lived script client racing the server across a ~20 minute sustained run; this harness's per-cell mutations are single short writes against workers:1, so a bare await is sufficient. If a flake does appear during Task 3's verification, wrap the specific failing call in a 3-attempt retry with a 200ms backoff rather than reintroducing the full withDbRetry machinery preemptively.

    Port these functions, adapting each call site from the module-scope prisma/lesson/seededCardIds variables the script used to explicit await getTestPrisma() calls and querying live rows instead of assuming module-scope fixture-creation variables exist:
    - expectedDueState(): Promise&lt;string&gt; - count CardReview rows where nextReview &lt;= now; return the count as a string if &gt; 0, else the literal 'zero-due-state' (scripts/diagnose-freshness.mts:606-609).
    - expectedCardsCount(): Promise&lt;string&gt; - count all Card rows; return the literal "Cards (" + count + ")" (lines 611-614).
    - expectedMasteredCount(): Promise&lt;string&gt; - count CardReview rows where state=2 AND scheduledDays &gt;= 21; return as a string (lines 616-621).
    - flipOneReviewDueState(): Promise&lt;void&gt; - find the earliest-due CardReview with nextReview &lt;= now and push its nextReview 365 days into the future; if none is due, find the earliest CardReview overall and pull its nextReview 60 seconds into the past (lines 624-644).
    - createMutationCard(): Promise&lt;void&gt; - create one new Card with a module-scope incrementing counter in its front/back text (e.g. front: '추가단어' + counter), normalizedFront via normalizeFront from '@/lib/card-key', one nested Sentence (targetForm equal to the front, appearing once, blank-safe), and lessonId set to any existing seeded Lesson's id (query the first Lesson row rather than assuming a module-scope reference, since this module has no seed-time lesson variable in scope) (lines 647-671).
    - promoteOneReviewToMastered(): Promise&lt;void&gt; - find a CardReview NOT already mastered (state=2 AND scheduledDays&gt;=21) and set it to state=2/scheduledDays=30; if all are already mastered, degrade one back to scheduledDays=5 so a future promotion has a candidate (lines 673-687).
    - ensureAllSeededReviewsDue(): Promise&lt;void&gt; - update every CardReview belonging to a card from the seeded fixture to nextReview 60 seconds in the past. Since this module has no seededCardIds array in scope (unlike the throwaway script), implement this by updating ALL CardReview rows unconditionally to nextReview = 60 seconds ago - safe here because resetToBaseline() (called in every spec's beforeEach) guarantees only the fixture's own rows exist in the isolated DB at this point, so "all rows" and "all seeded rows" are equivalent in this harness (unlike the diagnosis script, which ran a single long-lived seed alongside ad-hoc mutation cards across many cells without per-cell reset).
  </action>
  <behavior>
    - Test: isRscRequest returns true for a request object whose headers() returns { rsc: '1' }, and false for one returning {} or { rsc: '0' }.
    - Test: expectedDueState returns 'zero-due-state' when zero CardReview rows are past-due, and the numeric string count otherwise.
    - Test: expectedCardsCount returns the literal string "Cards (N)" where N is the live Card row count.
  </behavior>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "tests/study-cards.test.ts" | grep -cE "e2e/helpers/(rsc|resume|mutate).ts" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - e2e/helpers/rsc.ts exports isRscRequest with no second OR-branch checking an uppercase 'RSC' header key (grep the file: zero occurrences of the literal "RSC'" as a header-key comparison).
    - e2e/helpers/resume.ts exports simulateResume matching the dual-property override shape (grep the file: contains both the substring "document.hidden" and the substring "visibilityState").
    - e2e/helpers/mutate.ts exports all 7 named functions listed above; none of them instantiate a new PrismaClient directly (grep the file: zero occurrences of "new PrismaClient" - all access goes through getTestPrisma() from ../seed).
    - Type-checks clean with zero new errors attributable to these three files.
  </acceptance_criteria>
  <done>All three helper modules exist, type-check cleanly, share the single seeded Prisma client, and are ready for Task 2's spec files to import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the three freshness-regression spec files (9 RED cells, 7 GREEN cells)</name>
  <files>e2e/freshness-router-cache.spec.ts, e2e/freshness-client-shell.spec.ts, e2e/freshness-fresh-paths.spec.ts</files>
  <read_first>
    - .planning/phases/24-freshness-diagnosis-spike/24-DIAGNOSIS.md - read in FULL; this is the primary source. In particular: "Summary Matrix" (all 16 verdicts), "Confirmed-Fresh Paths" #1-7 (exact steps/expected/observed for the GREEN spec), the 5 Stale-RouterCache scenario blocks and 4 Stale-ClientShell scenario blocks (exact steps/expected/evidence for the RED specs), and "Notes for Phase 25/26" plus "Instrumentation Caveats" (the CR-01 detail behind D-04)
    - .planning/phases/24-freshness-diagnosis-spike/24-REVIEW.md - CR-01 finding in full (the page.goto() recovery-path ambiguity for the 4 back-forward cells: /, /study, /cards, /habits)
    - .planning/phases/25-e2e-test-infrastructure-baselines/25-RESEARCH.md - "Ambiguity 2" (native page.goBack(), no goto() recovery - this IS the D-04 re-verification mechanism), "Pattern 3" code example and its note on why a plain expect() must be used instead of test.fail()/test.fixme(), "Anti-Patterns to Avoid", and the "Pitfall: Timing assumptions" section (the justified single waitForTimeout(300) settle margin after networkidle)
    - .planning/phases/25-e2e-test-infrastructure-baselines/25-PATTERNS.md - "freshness-router-cache.spec.ts / freshness-client-shell.spec.ts / freshness-fresh-paths.spec.ts" section (the concrete request-log-capture setup and back-forward test shape)
    - scripts/diagnose-freshness.mts:735-1015 - newContextWithAuth (request-log listener setup), runStandardCell (the plain-link/back-forward/resume driver logic to translate into native Playwright calls), runPrimaryStudyGradingScenario (the real-UI grade-to-completion flow for the / and /study post-mutation-return fresh cells), runDbMutationReturnCell (the D-06 cheap DB-level variant for /cards and /habits post-mutation-return)
    - e2e/helpers/readers.ts, e2e/helpers/rsc.ts, e2e/helpers/resume.ts, e2e/helpers/mutate.ts (this plan's Task 1 and Plan 02's Task 1) - the exact function signatures every assertion below composes
  </read_first>
  <action>
    Shared per-test setup (all three files): test.beforeEach awaits resetToBaseline() from '../seed' (import path './seed' at repo e2e/ root) so every one of the 16 cells starts from the identical fixture baseline, isolated from every other cell's mutation - this is what makes 21 tests (9+4's constituent scenario plus the 7 fresh cells, some of which chain two navigations) independent regardless of run order. Each test registers a request-log listener before any navigation: const requestLog: Array&lt;{ pathname: string; resourceType: string; headers: Record&lt;string,string&gt; }&gt; = []; page.on('request', req =&gt; { const url = new URL(req.url()); requestLog.push({ pathname: url.pathname, resourceType: req.resourceType(), headers: req.headers() }) }) - matching 25-PATTERNS.md's capture shape. Classify new fetches for a route via requestLog.slice(preLen).filter(r =&gt; r.pathname === routePath &amp;&amp; (isRscRequest-shaped check on r.headers OR r.resourceType === 'document')).

    CRITICAL DEVIATIONS FROM THE DIAGNOSIS SCRIPT (do not carry these over):
    - Use page.goBack({ waitUntil: 'networkidle' }) for every back-forward cell - NEVER page.evaluate(() =&gt; window.history.back()) and NEVER a page.goto()-based recovery fallback when the page fails to settle. This is Ambiguity 2's resolution and the literal mechanism that makes D-04's "the first real run IS the re-verification" claim true. If page.goBack() times out or lands on an unexpected URL, let the subsequent expect() fail honestly - do not add a recovery branch.
    - Every stale-cell assertion (9 total, across freshness-router-cache.spec.ts and freshness-client-shell.spec.ts) MUST be a plain expect() call - never test.fail(), never test.fixme(), never test.skip(). These Playwright annotations invert or suppress reporting semantics and would make a genuinely red suite report as green/skipped, which is exactly the false-positive risk 25-RESEARCH.md's Pattern 3 note and False-Positive Risks section both call out.
    - Preserve the single page.waitForTimeout(300) settle margin AFTER an already-awaited page.waitForLoadState('networkidle') on every navigation trigger - this is the one justified waitForTimeout carry-over (React's startTransition commit lagging one tick behind networkidle, empirically necessary in Phase 24). Do not add any other waitForTimeout, and do not remove this one as part of a blanket "no waitForTimeout" cleanup.
    - Every assertion pairs DOM content (via the Plan 02 readers) with network evidence (via isRscRequest against the captured request log) per D-03 - never assert on one without the other.

    e2e/freshness-router-cache.spec.ts (5 tests, ALL expected RED today - Stale-RouterCache root cause, zero new fetches captured):
    - '/ back-forward': goto '/', networkidle, click the Cards nav link (exact name match), networkidle, call mutate.flipOneReviewDueState(), capture preLen, page.goBack({ waitUntil: 'networkidle' }), waitForTimeout(300), assert zero new document/RSC fetches for '/' (this passes today per 24-DIAGNOSIS.md - the Router Cache reuse signature is the defining evidence), then assert readHomeState(page) equals expectedDueState() (this SECOND assertion is expected to fail today per D-01/D-04 - the stale count is what's on screen; do not suppress or skip it).
    - '/ resume', '/study resume', '/cards resume', '/habits resume': for each, goto the route, networkidle, run that route's mutator (flipOneReviewDueState for / and /study, createMutationCard for /cards, promoteOneReviewToMastered for /habits), capture preLen, simulateResume(page, true), waitForTimeout(150), simulateResume(page, false), waitForTimeout(250+300 settle), assert zero new fetches for that route (passes today), then assert the matching reader's return value equals the matching expected* function's return value (expected to fail today for all 4).

    e2e/freshness-client-shell.spec.ts (4 tests, ALL expected RED today - Stale-ClientShell root cause, an RSC fetch DOES occur but the DOM never updates):
    - '/study back-forward', '/cards back-forward', '/habits back-forward': for each, goto '/' then click through to the target route via its real nav link, networkidle, click the Home nav link away, networkidle, run that route's mutator (flipOneReviewDueState for /study, createMutationCard for /cards, promoteOneReviewToMastered for /habits), capture preLen, page.goBack({ waitUntil: 'networkidle' }), waitForTimeout(300), assert exactly ONE new RSC-header fetch was captured for that route (passes today per 24-DIAGNOSIS.md's fetch evidence), then assert the matching reader equals the matching expected value (expected to fail today - the server returned fresh data but the client shell's useState(initialXxx) never adopted it).
    - '/habits post-mutation-return': goto '/habits', click Home nav link away, networkidle, call promoteOneReviewToMastered(), capture preLen, click the Habits nav link back, networkidle, assert exactly one new RSC fetch for '/habits' (passes today - same RSC URL Next.js reuses for this segment), then assert readHabitsMasteredCount(page) equals expectedMasteredCount() (expected to fail today - the ONLY post-mutation-return cell that stays stale per 24-DIAGNOSIS.md's Notes section).

    e2e/freshness-fresh-paths.spec.ts (7 tests, ALL expected GREEN today):
    - Plain-Link cells (4): for each route, goto the OTHER route first (Home starts from /cards, others start from /), networkidle, run that route's mutator, capture preLen, click the target route's real nav link, networkidle, assert one new fetch was captured for the route, then assert the reader equals the expected value (both halves should pass today).
    - '/ post-mutation-return' and '/study post-mutation-return' (the primary D-05 scenario, 2 cells in one flow, matching runPrimaryStudyGradingScenario's shape): call ensureAllSeededReviewsDue(), goto '/study', click "Start studying" then "Flashcards", loop clicking "Show Answer" then an Easy-grade button until "Session complete!" or "Going the extra mile!" appears (cap the loop at a safe bound, e.g. 20 iterations, since the fixture has exactly FIXTURE.dueCards due cards), then click "Back to Dashboard" and assert readHomeState(page) equals 'zero-due-state' plus one new fetch for '/'; then click the Study nav link and assert readStudySelectModeState(page) equals 'zero-due-state' plus one new fetch for '/study'. Both are expected to pass today.
    - '/cards post-mutation-return' (D-06 cheap DB-level variant): goto '/cards', click Home nav link away, networkidle, call createMutationCard(), capture preLen, click the Cards nav link back, networkidle, assert one new fetch for '/cards' plus readCardsCount(page) equals expectedCardsCount() (both expected to pass today).

    Each test name must literally state its expected verdict and root-cause category (e.g. "/ back-forward stays stale (Stale-RouterCache) - expected RED until Phase 26" for the two red files, "/ plain-Link stays fresh - regression net for Phase 26" for the green file) so a bare test-list output is self-documenting about which failures are expected.
  </action>
  <behavior>
    - freshness-router-cache.spec.ts: all 5 tests fail on their final (DOM-vs-expected) assertion; all 5 pass their first (zero-new-fetches) assertion.
    - freshness-client-shell.spec.ts: all 4 tests fail on their final (DOM-vs-expected) assertion; all 4 pass their first (exactly-one-new-fetch) assertion.
    - freshness-fresh-paths.spec.ts: all 7 tests pass both halves of every assertion.
  </behavior>
  <verify>
    <automated>npx playwright test e2e/freshness-router-cache.spec.ts e2e/freshness-client-shell.spec.ts 2>&1 | tee /tmp/fresh-red.log; grep -Eo "[0-9]+ failed" /tmp/fresh-red.log | grep -qx "9 failed"</automated>
  </verify>
  <acceptance_criteria>
    - npx playwright test e2e/freshness-router-cache.spec.ts reports exactly 5 tests run, 5 failed (the final DOM assertion in every one), matching 24-DIAGNOSIS.md's 5 Stale-RouterCache cells exactly.
    - npx playwright test e2e/freshness-client-shell.spec.ts reports exactly 4 tests run, 4 failed, matching 24-DIAGNOSIS.md's 4 Stale-ClientShell cells exactly.
    - npx playwright test e2e/freshness-fresh-paths.spec.ts reports exactly 7 tests run, 7 passed, matching 24-DIAGNOSIS.md's 7 Confirmed-Fresh cells exactly.
    - grep across all three spec files for the literal tokens "test.fail(" and "test.fixme(" and "test.skip(" returns zero matches total (Anti-Pattern gate - Playwright expected-failure annotations are forbidden here).
    - grep confirms every test body contains both an isRscRequest-based assertion (or a zero/one-fetch-count assertion derived from it) AND a reader-based DOM assertion (D-03 dual-evidence gate) - no test asserts only one half.
    - D-04 CR-01 re-verification: after the first real run, compare the 4 back-forward cells' actual pass/fail (all 4 are expected to fail per this task) against 24-DIAGNOSIS.md's verdicts for /, /study, /cards, /habits back-forward. If any cell's actual result diverges from the diagnosis (e.g. it unexpectedly passes), do not silently adjust the spec to match - record the mismatch as a finding in the SUMMARY and add it to STATE.md's Blockers/Concerns per CONTEXT.md's default escalation guidance; continue without blocking phase completion.
  </acceptance_criteria>
  <done>The full 16-cell matrix exists as real, independently-runnable Playwright assertions matching 24-DIAGNOSIS.md's Summary Matrix exactly - 9 genuinely red (never annotation-masked), 7 genuinely green - and the D-04 CR-01 re-verification has been performed against real results, not assumed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify trace/reporter output on a real failure, then delete the throwaway diagnosis script</name>
  <read_first>
    - .planning/phases/25-e2e-test-infrastructure-baselines/25-RESEARCH.md — "Validation Architecture" § E2E-07 row and "False-Positive Risks" (trace/reporter proof must come from a genuine failure)
    - scripts/diagnose-freshness.mts — final confirmation pass before deletion (D-08): every function this script defines should now have a verified home in e2e/helpers/ (rsc.ts, resume.ts, build-guard.ts, readers.ts, mutate.ts) or be intentionally NOT ported (waitForServer/stopServer, superseded by playwright.config.ts's webServer per Ambiguity 1)
  </read_first>
  <what-built>Automated verification that a failed freshness test actually produces an inspectable Playwright trace and parseable line-reporter stdout (E2E-07), followed by deletion of scripts/diagnose-freshness.mts now that every reusable piece (DB guard, RSC predicate, resume simulator, mutators, per-route readers) has a permanent home under e2e/helpers/ (D-08).</what-built>
  <action>
    1. Run npx playwright test e2e/freshness-router-cache.spec.ts (this suite is expected to have failing tests today per Task 2 — that IS the fixture for this trace check) and confirm test-results/ contains a trace.zip for at least one failed test.
    2. Run npx playwright show-trace against that trace.zip (or otherwise validate it) to confirm the file is a valid, non-empty archive; report its file size and confirm the reporter's stdout was single-line-per-test (line reporter format — one line per test, no multi-line stack-trace noise inline).
    3. Confirm scripts/diagnose-freshness.mts has been deleted via git status, and that grep -r "diagnose-freshness" across app/, components/, lib/, package.json, scripts/ returns zero references (no dangling import or npm script pointing at the deleted file). Only delete after steps 1-2 pass.
    4. Report the D-04 CR-01 re-verification outcome from Task 2 (match or mismatch against 24-DIAGNOSIS.md for the 4 back-forward cells) plainly in the summary.
    5. STOP and present this checkpoint to the human with the reported trace file existence, the reporter output shape, and the CR-01 re-verification outcome, before considering the phase closed. This step exists because "is this trace file actually useful/inspectable" and "does this mismatch (if any) warrant a STATE.md blocker vs. just noting it" are judgment calls, not pure automated pass/fail.
  </action>
  <how-to-verify>
    1. Review the reported trace.zip path and file size (non-zero, plausibly a real archive).
    2. Review the reporter stdout excerpt (one line per test, matching Playwright's `line` reporter format).
    3. Review the D-04 CR-01 re-verification outcome for all 4 back-forward cells (match/mismatch against 24-DIAGNOSIS.md).
    4. Confirm scripts/diagnose-freshness.mts no longer exists in the repo.
  </how-to-verify>
  <acceptance_criteria>
    - A genuine (not synthetic) failing test produced a trace.zip that Claude reports as valid and non-empty.
    - Reporter stdout is confirmed line-reporter shaped (one line per test).
    - scripts/diagnose-freshness.mts is deleted with zero dangling references anywhere in the repo.
    - The D-04 CR-01 outcome (match or mismatch, per cell) is stated plainly, not omitted.
  </acceptance_criteria>
  <resume-signal>Type "approved" to finalize the deletion and close out the phase, or describe concerns (e.g. if the CR-01 mismatch should be escalated differently than proposed)</resume-signal>
  <done>Human has reviewed and approved the trace/reporter proof and the CR-01 outcome; scripts/diagnose-freshness.mts is deleted with no dangling references</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| freshness spec DB mutators -> isolated test DB | mutate.ts performs direct Prisma writes (flip due-state, create cards, promote to mastered) against the isolated file: DB on every one of 16 cells |
| ported isRscRequest/simulateResume -> real network/DOM APIs | both operate only within a Playwright-managed browser context scoped to the storageState-authenticated chromium project; no standalone invocation path exists |
| scripts/diagnose-freshness.mts deletion -> no dangling references | any leftover import or npm script referencing the deleted file would silently break at next invocation, not at delete time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-08 | Tampering | e2e/helpers/mutate.ts's direct Prisma writes | critical | mitigate | every mutator routes through getTestPrisma() (Plan 01's env-pinned singleton, itself behind assertLocalDb()); mutate.ts introduces no second, unguarded Prisma client (Task 1 acceptance criteria explicitly greps for this) |
| T-25-09 | Repudiation | freshness-regression spec silently reporting red state as green via expected-failure annotations | high | mitigate | Task 2 acceptance criteria includes a repo-wide grep gate for test.fail(/test.fixme(/test.skip( across all three spec files, required to return zero matches |
| T-25-10 | Tampering | reintroducing CR-01's page.goto() recovery fallback, silently converting a true stale verdict into a false fresh one | high | mitigate | Task 2's action explicitly forbids any goto()-based recovery on back-forward; a stuck/unexpected navigation must fail the assertion honestly (Ambiguity 2) |
| T-25-11 | Tampering | dangling reference to the deleted scripts/diagnose-freshness.mts | low | mitigate | Task 3 step 3 greps app/, components/, lib/, package.json, scripts/ for the literal filename before finalizing the deletion |
</threat_model>

<verification>
- npx playwright test e2e/freshness-router-cache.spec.ts e2e/freshness-client-shell.spec.ts reports exactly 9 failed (never more, never fewer, never masked by annotations).
- npx playwright test e2e/freshness-fresh-paths.spec.ts reports exactly 7 passed.
- A real failed run produces an inspectable trace.zip and line-reporter stdout (E2E-07, verified against a genuine failure, not a forced one).
- scripts/diagnose-freshness.mts is deleted with zero dangling references anywhere in the repo.
</verification>

<success_criteria>
- E2E-06 fully satisfied: the freshness-regression spec set is real, matches 24-DIAGNOSIS.md exactly, uses D-03's dual DOM+network evidence pattern throughout, and is genuinely (not cosmetically) red on the 9 predicted cells.
- E2E-07 fully satisfied against a real failure, not a synthetic one.
- D-08 complete: scripts/diagnose-freshness.mts deleted, zero dangling references.
- D-04's CR-01 re-verification performed and its outcome recorded (match confirmed, or mismatch flagged per the default escalation guidance).
- Zero changes under app/, components/, lib/, prisma/schema.prisma.
</success_criteria>

<output>
Create .planning/phases/25-e2e-test-infrastructure-baselines/25-03-SUMMARY.md when done. Record: the final 9-red/7-green tally against 24-DIAGNOSIS.md's Summary Matrix, the D-04 CR-01 re-verification outcome for all 4 back-forward cells (match/mismatch per cell), the trace file path and size from Task 3, and confirmation that scripts/diagnose-freshness.mts is deleted with no dangling references. If the phase-end validation split (25-VALIDATION.md) does not match exactly (9 red / 7 green / smoke 4 green), state this explicitly as a phase-completion blocker rather than as a passing result.
</output>
