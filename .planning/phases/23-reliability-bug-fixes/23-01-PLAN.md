---
phase: 23-reliability-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/study-cards.ts
  - tests/study-cards.test.ts
autonomous: true
requirements: [RELIABILITY-01]

must_haves:
  truths:
    - "When the known-lemmas query rejects, a console.error whose first argument starts with [study-cards] fires with the rejection reason, before the empty-Set fallback is used"
    - "A known-lemmas failure still returns study cards — the session loads with unknownCount degraded to treat all context words as unknown (non-fatal contract preserved)"
    - "A pool-query failure still throws Error('Database error') so the route 500s — existing contract unchanged"
  artifacts:
    - "lib/study-cards.ts — logging in the knownRowsResult rejected branch"
    - "tests/study-cards.test.ts — regression tests locking all three truths"
  key_links:
    - "knownRowsResult.status === 'rejected' check → console.error('[study-cards] ...') → existing empty-Set fallback (log added, fallback logic untouched)"
---

<objective>
Close RELIABILITY-01: `getStudyCards()` in `lib/study-cards.ts` runs the due-pool query and the known-lemmas query concurrently via `Promise.allSettled`; when the known-lemmas query rejects it silently degrades to an empty Set, invisibly turning the unknownCount ranking signal into "everything is unknown". Add a `[study-cards]`-prefixed log so the degradation becomes observable in Vercel logs, without changing any error-handling behavior.

Purpose: silent ranking-signal degradation is currently undiagnosable — sessions look subtly wrong (bare-word-first gate misfires, sentence choice degrades) with no trace. One log line makes the failure findable.
Output: additive logging in `lib/study-cards.ts` + a new `tests/study-cards.test.ts` locking both the new log and the pre-existing degradation contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/study-cards.ts
</context>

## Artifacts this phase produces (this plan)

- `console.error('[study-cards] ...')` rejected-branch log line in `lib/study-cards.ts` (new conditional block; no new exports)
- `tests/study-cards.test.ts` — new test file (new path)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Log known-lemmas query failure before empty-Set degradation, with regression tests</name>
  <files>lib/study-cards.ts, tests/study-cards.test.ts</files>
  <read_first>
    - lib/study-cards.ts (target file — verify current line positions: allSettled destructuring ~line 39, pool-rejection throw ~lines 67-69, early return on empty pool ~line 72, knownLemmas construction ~lines 90-95)
    - lib/dto.ts (CardDTO/ReviewDTO/SentenceDTO shapes — the mock pool row must survive the serialization block at the end of getStudyCards)
    - lib/sequence.ts (selectSessionCards/sequenceCards signatures — the mock pool row flows through them unmocked; confirm what fields they touch, e.g. review.nextReview)
    - tests/db-errors.test.ts (house test style: describe/it/expect, plain vitest)
    - vitest.config.ts (the `@` alias resolves to repo root — vi.mock('@/lib/prisma') paths work)
  </read_first>
  <behavior>
    - Test 1 (new behavior): when the known-lemmas findMany rejects and the pool findMany fulfills, getStudyCards resolves with the pool card(s), every sentence carries a numeric unknownCount, AND console.error was called with a first argument that starts with the literal prefix "[study-cards]" and a second argument equal to the rejection reason.
    - Test 2 (contract lock): when the pool findMany rejects, getStudyCards rejects with an Error whose message is exactly "Database error" (existing behavior — must not change).
    - Test 3 (happy-path clean): when both queries fulfill, no console.error call with a "[study-cards]"-prefixed first argument occurs.
  </behavior>
  <action>
    In lib/study-cards.ts: immediately AFTER the existing `if (poolResult.status === 'rejected') { throw new Error('Database error') }` block and BEFORE `const cards = poolResult.value` / the `cards.length === 0` early return, add a conditional on `knownRowsResult.status === 'rejected'` that calls `console.error` with a first argument beginning with the exact prefix `[study-cards]` (e.g. describing that the known-lemmas query failed and unknownCount ranking is degrading to an empty known-lemma set) and `knownRowsResult.reason` as the second argument. Placement rationale (preserve it in a short code comment): logging must fire even when the pool is empty, and the requirement is visibility BEFORE degradation — the empty-Set fallback at the knownLemmas construction further down stays byte-for-byte untouched. Do NOT add any new throw, retry, or fallback behavior; RELIABILITY-01 is additive logging only. Do not log lessonFrom/lessonTo, card fronts, or any env value — only the prefix message and the rejection reason.

    In tests/study-cards.test.ts: use `vi.mock('@/lib/prisma', ...)` to replace the prisma singleton (this also neutralizes the real module body, so no DATABASE_URL is needed). The mock needs `card.findMany` and `cardDependency.findMany`. `card.findMany` is called twice with different arg shapes — distinguish inside the mock implementation: the pool call passes `include`, the known-lemmas call passes `select: { normalizedFront: true }`. Have `cardDependency.findMany` resolve to `[]`. Always pass an explicit `sessionSize` (e.g. 20) in StudyCardsParams so `getSessionSize()` from lib/settings is never invoked. The fulfilled pool row must be a minimal but serialization-complete card object: id, front, back, type, notes, normalizedFront, components, distractors, createdAt/updatedAt as Date objects, lesson: null, a review object with nextReview as a past Date, lastReview: null, state: 0, reps: 0 (plus any other fields lib/sequence.ts reads — confirm via read_first), and one sentence with korean/targetForm/translation/orderIndex/createdAt/updatedAt Dates. Spy on console.error via `vi.spyOn(console, 'error').mockImplementation(() => {})` and restore in afterEach. Implement the three behaviors from the behavior block. Write the failing tests first, then apply the lib/study-cards.ts change (RED→GREEN).
  </action>
  <verify>
    <automated>npm test -- tests/study-cards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/study-cards.ts contains a conditional on `knownRowsResult.status === 'rejected'` whose body calls console.error, positioned before the `cards.length === 0` early return
    - `grep -c "\[study-cards\]" lib/study-cards.ts` returns >= 1
    - The knownLemmas Set construction still degrades to an empty array on rejection (the ternary/fallback expression is unchanged)
    - `npm test -- tests/study-cards.test.ts` exits 0 with 3 or more passing tests
    - `npm test` exits 0 (whole suite still green)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Known-lemmas query failure emits a [study-cards]-prefixed console.error with the rejection reason; sessions still load on that failure; pool failure still throws 'Database error'; all three behaviors locked by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB driver → server log | Driver rejection reasons are written to server logs (Vercel) — server-generated, not user-controlled input |

No new user-input boundary: `lessonFrom`/`lessonTo` request params are NOT interpolated into the log message (log-injection surface avoided by construction).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Information Disclosure | console.error in lib/study-cards.ts | low | mitigate | Log ONLY the fixed prefix message and knownRowsResult.reason (driver error). Never log card fronts, lesson-range params, DATABASE_URL, or auth tokens. Logs are server-side only, app is single-tenant and auth-gated. |
| T-23-02 | Repudiation | silent known-lemmas degradation | low | mitigate | This plan IS the mitigation — the prefixed log creates the missing audit trail for a previously invisible failure mode. |

No package installs in this plan — package legitimacy gate not applicable. No high/critical threats; nothing meets the security_block_on=high threshold.
</threat_model>

<verification>
- `npm test` exits 0 (new tests/study-cards.test.ts plus the whole existing suite)
- `npm run lint` exits 0 (react-hooks rules do not apply here — server-only module — but ESLint must stay clean project-wide)
- Source assertion: the `[study-cards]` literal exists in lib/study-cards.ts inside the rejected branch; the throw of Error('Database error') for pool rejection is unmodified
</verification>

<success_criteria>
- RELIABILITY-01 satisfied: known-lemmas query failure logs a `[study-cards]`-prefixed reason before degrading to the empty Set
- Existing graceful-degradation contract provably unchanged (locked by Tests 2 and 3)
- Zero behavior change on the happy path
</success_criteria>

<output>
Create `.planning/phases/23-reliability-bug-fixes/23-01-SUMMARY.md` when done
</output>
