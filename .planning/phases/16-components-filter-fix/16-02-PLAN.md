---
phase: 16-components-filter-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/extract-cards.ts
  - tests/extract-cards.test.ts
autonomous: true
requirements: [GRAPH-01, GRAPH-02]

must_haves:
  truths:
    - "The extraction prompt instructs Claude to list a components[] entry ONLY when this card's own content actually depends on it as a prerequisite — not merely because the item appears elsewhere in the lesson (GRAPH-01)"
    - "A structurally malformed card object (missing/empty front or back, or a present-but-invalid type) is dropped, never coerced into an empty-string card that gets persisted as broken data (GRAPH-02, Success Criterion 2)"
    - "A truncated Claude response still yields the cards that DID complete — the existing array-level salvage logic (lib/extract-cards.ts:165-201) is preserved byte-for-byte, not regressed (RESEARCH.md Pitfall 3)"
  artifacts:
    - "lib/extract-cards.ts exporting a pure parseExtractionResponse(text: string): ExtractedCard[] that runs the existing salvage THEN per-card structural validation (no Anthropic call inside it)"
    - "tests/extract-cards.test.ts covering well-formed / malformed-card-dropped / invalid-type-dropped / truncated-salvage-preserved / non-object-entry cases"
  key_links:
    - "new Anthropic() is constructed INSIDE extractCardsFromNotes (mirroring lib/gloss.ts:112), so lib/extract-cards.ts is importable in Vitest without ANTHROPIC_API_KEY set"
    - "parseExtractionResponse keeps the tolerant JSON salvage (lines 165-201) intact; the new validation is a .filter() layered on parsed[] BEFORE the existing .map() normalization"
---

<objective>
Tighten the extraction prompt so Claude stops inventing prerequisites (GRAPH-01), and add structural validation that rejects malformed/truncated card objects on receipt instead of silently persisting them as broken data (GRAPH-02) — matching the `lib/gloss.ts` LLM-response-validation convention and layered ON TOP of the existing truncation-salvage logic so that behavior is not regressed.

Purpose: Reduce spurious `components[]` at the source (prompt) and make the extraction pipeline reject structurally broken cards, closing Success Criterion 2. This is the extraction-hardening half of the phase; it is independent of the deck-lookup filter wiring (plan 16-03) and can land in parallel with plan 16-01.

Output: A tightened prompt, a pure/testable `parseExtractionResponse()` helper carrying salvage + per-card validation, and full unit coverage in a brand-new `tests/extract-cards.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-components-filter-fix/16-RESEARCH.md
@.planning/phases/16-components-filter-fix/16-PATTERNS.md
</context>

<artifacts>
## Artifacts this plan produces
- `lib/extract-cards.ts` — MODIFIED. (1) GRAPH-01 prompt text tightened in the `"components"` field instructions; (2) `new Anthropic()` moved into the function body; (3) new exported pure `parseExtractionResponse(text: string): ExtractedCard[]` = existing salvage + NEW per-card structural validation + existing `.map()` normalization.
- `tests/extract-cards.test.ts` — NEW. Vitest coverage for `parseExtractionResponse` (imports the module without any live Anthropic call).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Tighten the components[] extraction prompt (GRAPH-01)</name>
  <read_first>
    - lib/extract-cards.ts:110-117 (the exact current `"components"` field instructions — the ONLY prompt region to change)
    - lib/extract-cards.ts:59-118 (surrounding prompt for tone/format consistency — do NOT alter other sections)
    - .planning/phases/16-components-filter-fix/16-RESEARCH.md "Common Pitfalls (concrete example from the actual corpus)" — the 몸에 알이 배겼을 것 같아요 / ~(으)ㄴ 후에 case that motivates this requirement (deck-lookup alone cannot catch a real-but-unrelated card; GRAPH-01 is load-bearing for this class)
  </read_first>
  <files>lib/extract-cards.ts</files>
  <action>
    Edit ONLY the `"components"` field instructions at lib/extract-cards.ts:110-117. Keep the existing base-form guidance ("Use base/dictionary forms (먹다 not 먹어요…)") and the three worked examples intact. ADD explicit tightening language instructing Claude to list a component ONLY when this card's own front/sentences genuinely depend on it as a prerequisite building block a learner must already know to understand THIS card — and to NOT list a word or grammar pattern merely because it is topically related, co-occurs, or appears elsewhere in the same lesson. Include a short negative cue conveying that an unrelated grammar pattern taught in a different part of the lesson must NOT be listed as a prerequisite of a card that does not use it. The added instruction MUST contain, verbatim, the anchor sentence: `Only list it if this card actually depends on it as a prerequisite — never because it merely appears elsewhere in the lesson.` (this exact string is the deterministic verification anchor). This is a pure prompt-text change: do NOT alter any runtime parsing/validation logic in this task. Do NOT weaken the existing "Include items even if they don't yet have their own card in the deck" clause into a scope reduction — keep it, but subordinate it to the new "must actually be a prerequisite" rule (a prerequisite need not yet be its own card, but it MUST be something this card genuinely builds on).
  </action>
  <verify>
    <automated>grep -c "Only list it if this card actually depends on it as a prerequisite" lib/extract-cards.ts</automated>
  </verify>
  <done>The components field instructions contain the tightened prerequisite language including the verbatim anchor sentence; no other prompt section and no runtime logic changed in this task.</done>
  <acceptance_criteria>
    - `grep -c "Only list it if this card actually depends on it as a prerequisite" lib/extract-cards.ts` returns `1` (anchor present).
    - `git diff lib/extract-cards.ts` for this task touches only the `"components"` instructions block (lines ~110-117 region) — no edits to SENTENCE RULES, the type/front/back instructions, or the salvage/map logic.
    - The existing three `components` examples (먹다 / ~(으)면 / 학교) remain present in the prompt.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract pure parseExtractionResponse() with per-card structural validation, add tests (GRAPH-02)</name>
  <read_first>
    - lib/extract-cards.ts:1-5 (module-level `const anthropic = new Anthropic()` at line 5 — to be moved into the function)
    - lib/extract-cards.ts:158-240 (finalMessage() → content extraction → tolerant salvage parser 165-201 → per-card .map() normalization 204-240 — the exact code to relocate into the pure helper; salvage stays byte-for-byte)
    - lib/gloss.ts:112 (the `const anthropic = new Anthropic()` INSIDE the function convention to mirror) and lib/gloss.ts:133-143 (the regex-extract → JSON.parse → shape-check validation convention GRAPH-02 mirrors)
    - tests/known-words.test.ts (FULL — describe/it Vitest structure + Korean-example comments to mirror in the new test file; relative `../lib/...` import convention)
    - .planning/phases/16-components-filter-fix/16-RESEARCH.md "Pitfall 3" (do NOT regress the salvage) and "Architecture Patterns / Pattern 2" (per-card shape validation, not whole-response rejection)
  </read_first>
  <files>lib/extract-cards.ts, tests/extract-cards.test.ts</files>
  <behavior>
    Write tests/extract-cards.test.ts FIRST (RED). One `describe('parseExtractionResponse', ...)` block, each `it()` with a comment naming the exact input shape and expected outcome:
    - Well-formed array of 2 valid cards (front, back, type present) → returns 2 cards; fields normalized (distractors array, sentences filtered, components deduped).
    - One card missing `front` (or front is empty/whitespace) → that card DROPPED, the valid sibling KEPT (length shrinks by exactly 1).
    - One card missing/empty `back` → DROPPED, siblings kept.
    - One card whose `type` is a present-but-invalid string (e.g. "noun") → DROPPED. A card with ABSENT type → KEPT and defaulted to "vocabulary" (matches the existing `c.type ?? 'vocabulary'` behavior — absence is tolerated, present-garbage is rejected).
    - A non-object array entry (a bare string, or null) → DROPPED, not coerced.
    - TRUNCATED input (a raw JSON array text that ends mid-object with no closing `]`, containing 2 complete card objects + 1 partial) → salvage recovers the 2 complete objects, validation keeps the 2 valid ones (asserts the salvage path at lib/extract-cards.ts:165-201 is preserved — RESEARCH.md Pitfall 3 / Success Criterion 2).
    All cases call `parseExtractionResponse(rawText)` with a hand-built string — NO Anthropic API call anywhere in the test.
  </behavior>
  <action>
    In lib/extract-cards.ts: (1) Move `const anthropic = new Anthropic()` from module scope (line 5) INTO the `extractCardsFromNotes` body (top of the function, mirroring lib/gloss.ts:112) so importing the module in a test does not construct the client or require ANTHROPIC_API_KEY. (2) Extract the response-handling pipeline into a new EXPORTED pure function `export function parseExtractionResponse(text: string): ExtractedCard[]` containing, in order: the existing content-is-text guard's text (the caller still extracts `content.text.trim()` and passes it in), the tolerant JSON salvage parser (current lines 165-201) copied VERBATIM — do not alter its logic — producing `parsed: Partial<ExtractedCard>[]`, then a NEW per-card structural validation `.filter()` applied to `parsed` BEFORE the existing `.map()`, then the existing `.map()` normalization (current lines 204-240) UNCHANGED. (3) Define the validation predicate (either inline in the filter or as a small local `isValidExtractedCard(c: Partial<ExtractedCard>): boolean`): KEEP a card iff it is a non-null object AND `typeof c.front === 'string'` and `c.front.trim().length > 0` AND `typeof c.back === 'string'` and `c.back.trim().length > 0` AND (`c.type === undefined` OR `c.type` is one of 'vocabulary' | 'grammar' | 'phrase'). Drop otherwise. This mirrors lib/gloss.ts's "validate shape, reject malformed" convention but applies per-array-element (RESEARCH.md notes the array-level salvage is the closer precedent). (4) Rewrite `extractCardsFromNotes` so that after `.finalMessage()` and the existing `content`/text extraction (lines 160-163), it simply `return parseExtractionResponse(text)`. Keep the ExtractedCard/ExtractedSentence interfaces and the `sentenceMatch`/`normalizeFront` imports. Do NOT change the `.map()` normalization details (types, distractors slice(0,3), sentence filtering via sentenceMatch, component self-dedup) — only relocate them and gate the array on the new validation filter. Run the tests, confirm GREEN.
  </action>
  <verify>
    <automated>npm test -- tests/extract-cards.test.ts</automated>
  </verify>
  <done>parseExtractionResponse is exported, pure (no Anthropic call in its body), preserves the salvage logic, and drops structurally malformed cards while keeping valid ones. tests/extract-cards.test.ts runs green WITHOUT ANTHROPIC_API_KEY. The module still exports extractCardsFromNotes with an unchanged external signature.</done>
  <acceptance_criteria>
    - `npm test -- tests/extract-cards.test.ts` exits 0 with all listed cases passing.
    - The truncated-input test passes, proving the salvage block (lib/extract-cards.ts:165-201) is preserved (Pitfall 3 / Success Criterion 2).
    - A malformed-card test (missing front/back or invalid type) shows the bad card dropped and valid siblings kept (Success Criterion 2).
    - `grep -n "new Anthropic()" lib/extract-cards.ts` shows the construction is inside `extractCardsFromNotes`, not at module top (line 5 no longer holds it) — the test file imports the module without ANTHROPIC_API_KEY and still runs.
    - `parseExtractionResponse` is exported and takes exactly one `string` argument in this plan (a `deckNormalizedFronts` parameter is added later by plan 16-03; do not add it here).
    - `npm run lint` passes clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude extraction response → sync pipeline | The LLM text output is untrusted-ish input (V5 Input Validation): it can be malformed, truncated, or contain unexpected field types before it is persisted via Prisma |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-04 | Tampering (data integrity) | parseExtractionResponse — malformed card object silently coerced (`c.front ?? ''`) into an empty-string card persisted via prisma.card.create | medium | mitigate | Per-card structural validation `.filter()` drops cards missing/empty front/back or with an invalid type BEFORE the `.map()`, so broken cards never reach the DB write (GRAPH-02, matches lib/gloss.ts V5 convention) |
| T-16-05 | Denial of Service (data loss regression) | Over-strict validation discarding a whole truncated response and losing the cards that DID complete | medium | mitigate | Validation is layered as a per-element filter on top of the UNCHANGED array-level salvage (lines 165-201); a dedicated truncated-input test asserts the salvage still yields completed cards (Pitfall 3) |
| T-16-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH.md Package Legitimacy Audit: N/A); zero new dependencies per milestone decision |
</threat_model>

<verification>
- `npm test -- tests/extract-cards.test.ts` green (includes the salvage-preservation and malformed-drop cases).
- `npm run lint` clean.
- `grep -c "Only list it if this card actually depends on it as a prerequisite" lib/extract-cards.ts` returns 1 (GRAPH-01 anchor present).
- Note (operational, not automatable here): GRAPH-01's real-world drop-rate effect is only observable after a resync re-runs extraction with the new prompt; it is confirmed qualitatively via the plan 16-01 dry-run before/after comparison and the plan 16-04 retroactive-cleanup counts, not by a unit assertion.
</verification>

<success_criteria>
- Malformed/truncated Claude extraction responses are rejected on receipt (structurally validated, matching lib/gloss.ts) rather than persisted as broken card data (GRAPH-02, Success Criterion 2).
- The existing truncation-salvage behavior is preserved — a dense/truncated lesson still yields its completed cards (Pitfall 3).
- The prompt instructs Claude to only list genuine prerequisites (GRAPH-01).
</success_criteria>

<output>
Create `.planning/phases/16-components-filter-fix/16-02-SUMMARY.md` when done. Record the exported `parseExtractionResponse` signature and note that plan 16-03 will extend it with a `deckNormalizedFronts: Set<string>` parameter + the filterComponents call.
</output>
