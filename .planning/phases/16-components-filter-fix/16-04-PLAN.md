---
phase: 16-components-filter-fix
plan: 04
type: execute
wave: 3
depends_on: [16-01, 16-03]
files_modified:
  - scripts/retro-filter-cleanup.mts
  - CLAUDE.md
autonomous: false
requirements: [GRAPH-03]

must_haves:
  truths:
    - "Running the cleanup re-applies filterComponents() to every already-persisted Card.components row and rewrites any value that changed (user-locked retroactive cleanup — IN scope, not deferred)"
    - "CardDependency edges are reconciled to the cleaned components using an all-cards keyToId (mirroring the plan 16-03 fix): stale edges from now-dropped spurious components are pruned, and newly-valid leaf-node edges are created (closes Success Criterion 1 against EXISTING corpus data)"
    - "The script is dry-run BY DEFAULT and only mutates production under an explicit --apply flag; it reports before/after counts (cards changed, edges pruned, edges added) and compares them to plan 16-01's recorded dry-run baseline"
  artifacts:
    - "scripts/retro-filter-cleanup.mts — one-off developer-run .mts importing lib/filter-components.ts via the dotenv-first dynamic-import pattern; dry-run default + --apply; batched writes; before/after reporting"
    - "CLAUDE.md Scripts section — a row documenting the new operational script (dry-run-first, developer-run only)"
  key_links:
    - "imports { filterComponents } from ../lib/filter-components.js AFTER dotenv.config() runs (CLAUDE.md local-resync.mts env-loading gotcha — dynamic import so env is read first)"
    - "edge reconciliation builds keyToId from ALL cards (same corrected scope as the plan 16-03 sync-route fix), not the components-not-null subset"
---

<objective>
Clean up the EXISTING live corpus so historical data matches the new extraction rules: re-run the deck-lookup filter against every persisted `Card.components` row, rewrite the ones that changed, and reconcile `CardDependency` edges (prune stale/spurious edges, add newly-valid leaf-node edges). This is the user-locked, explicitly-in-scope retroactive cleanup (NOT deferred to a follow-up phase). Modeled on `scripts/relink-dependencies.mjs` but it also WRITES corrected `Card.components`, and on `scripts/local-resync.mts`'s dynamic-import env pattern so it can import the TypeScript `lib/filter-components.ts` directly.

Purpose: Plan 16-03 fixes the write path going forward; this closes the loop on Success Criterion 1 for cards synced BEFORE this phase, whose stored components and edges were built from unfiltered data.

Output: A dry-run-by-default `.mts` cleanup script, a documented CLAUDE.md entry, and — via the blocking human checkpoint — a developer-executed production cleanup with before/after counts recorded against the 16-01 baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-components-filter-fix/16-RESEARCH.md
@.planning/phases/16-components-filter-fix/16-PATTERNS.md
@lib/filter-components.ts
@.planning/phases/16-components-filter-fix/16-01-SUMMARY.md
</context>

<artifacts>
## Artifacts this plan produces
- `scripts/retro-filter-cleanup.mts` — NEW. Developer-run one-off: dry-run default, `--apply` to mutate, imports `lib/filter-components.ts`, re-filters all `Card.components`, reconciles `CardDependency` edges, reports before/after counts.
- `CLAUDE.md` — MODIFIED. One new row in the Scripts (`scripts/`) section documenting the cleanup script (dry-run-first, developer-run only, never in the `/api/sync` request path).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/retro-filter-cleanup.mts (dry-run-default corpus re-filter + edge reconciliation)</name>
  <read_first>
    - scripts/local-resync.mts:1-27 (the dotenv-first pattern: `config({ path: ... .env })` then `config({ ... .env.local, override: true })` BEFORE the dynamic `await import('../lib/*.js')` — MANDATORY per CLAUDE.md's "local-resync.mts env loading" gotcha; static imports would read the wrong DATABASE_URL)
    - scripts/relink-dependencies.mjs (FULL — the edge-relink model: load cards, JSON.parse components in try/catch, build keyToId, upsert CardDependency; adapt to ALSO write corrected Card.components and to PRUNE stale edges)
    - scripts/local-resync.mts:174-231 (the two-phase link + final relink with the keyToId all-cards scope as corrected by plan 16-03 — mirror the corrected scope here)
    - lib/filter-components.ts (plan 16-01 output — the exact filterComponents(rawComponents, deckNormalizedFronts) to import and call; do NOT re-derive it inline — this is a .mts and CAN import it)
    - .planning/phases/16-components-filter-fix/16-01-SUMMARY.md (the recorded dry-run baseline: total dropped, grammar %, vocabulary % — this script's before/after report is compared against it)
    - .planning/phases/16-components-filter-fix/16-RESEARCH.md "Pitfall 1" (build the deck set / keyToId from ALL cards) and "Pitfall 2" (retroactive cleanup is the separate, explicit task — this plan)
  </read_first>
  <files>scripts/retro-filter-cleanup.mts</files>
  <action>
    Create scripts/retro-filter-cleanup.mts. Header JSDoc: purpose, the Usage lines `npx tsx scripts/retro-filter-cleanup.mts` (dry-run — reports only, NO writes) and `npx tsx scripts/retro-filter-cleanup.mts --apply` (mutates production), and a bold warning that it mutates the live Turso corpus and must be run locally by a developer, never in the sync request path. Load env FIRST via `dotenv` `config()` for `.env` then `.env.local` (override:true), then dynamically `await import('../lib/filter-components.js')`, `await import('../lib/card-key.js')` (normalizeFront), and `await import('../lib/prisma.js')` — exactly the ordering in local-resync.mts:14-27. Define `const APPLY = process.argv.includes('--apply')`.

    Phase A — load: `prisma.card.findMany({ select: { id: true, type: true, normalizedFront: true, components: true } })` for ALL cards (no where clause — RESEARCH.md Pitfall 1). Build `deckSet = new Set(cards.map(c => c.normalizedFront))`.

    Phase B — re-filter components (per card with non-null components): JSON.parse in try/catch (skip + warn on malformed, never crash — mirror relink-dependencies.mjs:80); run the parsed array through `filterComponents(parsed, deckSet)`; if the filtered array differs from the parsed array (length or contents), record a pending update: new value = `filtered.length ? JSON.stringify(filtered) : null`. Tally per Card.type (grammar/vocabulary/phrase): components before, components after, dropped. Keep an in-memory map `cardId → cleanedComponents[]` for Phase C.

    Phase C — reconcile edges: build `keyToId` from ALL cards (normalizedFront → id, no scoping — mirrors the plan 16-03 fix). Compute the DESIRED edge set: for each card, for each CLEANED component, resolve `keyToId.get(normalizeFront(comp))`; if it resolves to a different card id, that (cardId, prereqId) pair is desired. Load existing `prisma.cardDependency.findMany({ select: { id, cardId, prerequisiteId } })`. EDGES-TO-PRUNE = existing edges whose (cardId, prerequisiteId) is not in the desired set. EDGES-TO-ADD = desired pairs with no existing edge. Count both.

    Reporting (ALWAYS, both modes): print a per-type table (grammar/vocabulary/phrase: components before → after, dropped, pct) plus whole-corpus totals; print cards-changed count, edges-to-prune count, edges-to-add count; print a line comparing the dropped totals to the plan 16-01 baseline (echo the baseline numbers read from 16-01-SUMMARY context and the delta). If `!APPLY`: print `DRY RUN — no changes written. Re-run with --apply to persist.` and exit 0 having written NOTHING.

    Writes (ONLY when APPLY): wrap in batched writes — update changed Card.components rows (batch via `prisma.$transaction([...])` in chunks of ~50 to stay within libSQL limits), then `prisma.cardDependency.deleteMany({ where: { id: { in: pruneIds } } })` (chunked), then create the missing edges (chunked createMany or per-edge upsert mirroring relink-dependencies.mjs's INSERT-OR-IGNORE idempotency). After writing, re-count total Card rows changed and total CardDependency rows and print the final state. Never delete a Card or Lesson row — this script only touches `Card.components` values and `CardDependency` rows.
  </action>
  <verify>
    <automated>bash -c 'test -f scripts/retro-filter-cleanup.mts && grep -q "const APPLY = process.argv.includes(.--apply.)" scripts/retro-filter-cleanup.mts && grep -q "filter-components" scripts/retro-filter-cleanup.mts && grep -Eq "deleteMany|cardDependency" scripts/retro-filter-cleanup.mts && echo OK'</automated>
  </verify>
  <done>Script exists, imports filterComponents from lib/filter-components (not re-derived inline), gates ALL writes behind `const APPLY`, prints a per-type before/after report in both modes, and writes nothing in dry-run mode. Full production execution is deferred to the Task 3 human checkpoint (this task does not run it against the corpus).</done>
  <acceptance_criteria>
    - The verify command prints `OK` (file exists; `const APPLY = process.argv.includes('--apply')` present; imports `filter-components`; performs CardDependency writes via deleteMany/upsert).
    - `grep -n "await import" scripts/retro-filter-cleanup.mts` shows `../lib/filter-components.js`, `../lib/card-key.js`, `../lib/prisma.js` imported dynamically AFTER the two `config(...)` dotenv calls (env-first ordering — CLAUDE.md gotcha).
    - No `prisma.card.delete` / `prisma.lesson.delete` anywhere (the script mutates only `Card.components` values and `CardDependency` rows).
    - The card-loading query and the keyToId map are built from ALL cards (no `where: { components: { not: null } }`).
    - `npm run lint` passes clean (script has no unused-import / purity violations).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Document the cleanup script in CLAUDE.md Scripts section</name>
  <read_first>
    - CLAUDE.md "## Scripts (`scripts/`)" section (the existing entries — match the bullet style: script name, one-line purpose, how/when to run)
    - scripts/retro-filter-cleanup.mts (Task 1 output — its usage lines and behavior to describe accurately)
  </read_first>
  <files>CLAUDE.md</files>
  <action>
    Add one bullet to the CLAUDE.md `## Scripts (`scripts/`)` list for `retro-filter-cleanup.mts`, matching the existing entries' voice (e.g. the `relink-dependencies.mjs` / `local-resync.mts` bullets). State: re-applies the deck-lookup component filter to all persisted `Card.components` rows and reconciles `CardDependency` edges (prunes stale, adds newly-valid); DRY-RUN by default (`npx tsx scripts/retro-filter-cleanup.mts`), mutates only with `--apply`; developer-run locally, NEVER in the `/api/sync` request path; idempotent (safe to re-run). Do NOT touch any other section of CLAUDE.md.
  </action>
  <verify>
    <automated>grep -c "retro-filter-cleanup.mts" CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md's Scripts section documents the new script with dry-run-first + developer-run-only guidance; no other section changed.</done>
  <acceptance_criteria>
    - `grep -c "retro-filter-cleanup.mts" CLAUDE.md` returns at least `1`.
    - The new bullet states dry-run-by-default and `--apply` to persist, and that it is developer-run only (never in the request path).
    - `git diff CLAUDE.md` touches only the Scripts section.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    scripts/retro-filter-cleanup.mts — a dry-run-by-default corpus cleanup that re-filters every persisted Card.components row and reconciles CardDependency edges. Running it against production is a data-mutating operation, so it is a blocking, non-auto-approvable developer checkpoint.
  </what-built>
  <how-to-verify>
    1. Dry run: `npx tsx scripts/retro-filter-cleanup.mts` (writes NOTHING). Read the per-type report.
    2. Compare its dropped-component totals (grammar % / vocabulary %) against the plan 16-01 dry-run baseline recorded in 16-01-SUMMARY.md — they should be in the same ballpark. A wildly different grammar drop rate is a red flag (filter logic or deck-set scope regressed) — do NOT proceed to --apply; investigate first.
    3. Sanity-check the edges-to-prune count and a few sample pruned edges if listed: they should be plausibly-spurious (e.g. the ~(으)ㄴ 후에 → unrelated-phrase class), not obviously-correct prerequisite links.
    4. If the dry run looks right: back up / note current counts, then apply: `npx tsx scripts/retro-filter-cleanup.mts --apply`.
    5. Confirm the final report: cards changed, edges pruned, edges added, and total CardDependency rows after. Optionally cross-check with `node scripts/check-edges.mjs`.
    6. Re-run the dry run once more (`npx tsx scripts/retro-filter-cleanup.mts`) — it should now report 0 cards to change and 0 edges to prune/add (idempotency proof).
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded before/after counts (cards changed, edges pruned, edges added), or describe the anomaly if the dry run diverges sharply from the 16-01 baseline.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cleanup script → production Turso corpus (WRITE) | The script mutates live `Card.components` values and deletes/creates `CardDependency` rows — a data-integrity-critical write boundary with no user input, run by a developer |
| Persisted (old, unfiltered) components → cleaned components | Historical fabricated components must be removed without collateral loss of real prerequisite edges |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-08 | Tampering (data integrity, production write) | Unsafe/partial mutation of Card.components or CardDependency corrupting the live corpus | high | mitigate | Dry-run BY DEFAULT (writes gated behind explicit `--apply`); before/after counts reported and compared to the 16-01 baseline; batched `prisma.$transaction([...])` writes; idempotent re-run; blocking-human checkpoint (never auto-approved); script never deletes Card/Lesson rows |
| T-16-09 | Tampering (over-pruning) | Reconciliation deletes valid CardDependency edges (e.g. deck set built too narrowly) | high | mitigate | Desired-edge set recomputed from CLEANED components using an ALL-cards keyToId (Pitfall 1 scope); dry-run surfaces edges-to-prune for human review before any delete; idempotency re-run must report 0 further changes |
| T-16-10 | Tampering (SQL/ORM injection) | Cleanup queries | low | accept | All reads/writes go through Prisma parameterized calls (findMany / $transaction / deleteMany / upsert) — no raw SQL string interpolation, no user-controllable input (fixed corpus-wide operation) |
| T-16-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH.md Package Legitimacy Audit: N/A); `dotenv`/`tsx`/`@prisma/client` already installed; zero new dependencies per milestone decision |
</threat_model>

<verification>
- `bash -c 'test -f scripts/retro-filter-cleanup.mts && grep -q "const APPLY" ... && echo OK'` prints OK (write-safety structural gate).
- `grep -c "retro-filter-cleanup.mts" CLAUDE.md` >= 1 (documented).
- `npm run lint` clean.
- Blocking human checkpoint: developer ran dry-run, compared to the 16-01 baseline, applied, and confirmed idempotent re-run (0 further changes). Not auto-approvable (production data mutation).
</verification>

<success_criteria>
- Every already-persisted Card.components row has been re-filtered against the deck; changed rows rewritten (user-locked retroactive cleanup delivered).
- CardDependency edges reconciled to the cleaned data — stale/spurious edges pruned, newly-valid leaf-node edges added — closing Success Criterion 1 for the EXISTING corpus, not just future syncs.
- The operation is dry-run-first, reports before/after counts against the 16-01 baseline, and is idempotent on re-run.
</success_criteria>

<output>
Create `.planning/phases/16-components-filter-fix/16-04-SUMMARY.md` when done. Record the developer-approved production counts (cards changed, edges pruned, edges added, total edges after) and the comparison to the plan 16-01 dry-run baseline.
</output>