---
phase: 16-components-filter-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/filter-components.ts
  - tests/filter-components.test.ts
  - scripts/dry-run-filter.mjs
autonomous: false
requirements: [GRAPH-03, GRAPH-04, GRAPH-05]

must_haves:
  truths:
    - "filterComponents keeps a component only when it resolves to a real deck card via normalizeFront direct match or splitParticle stem fallback (GRAPH-03)"
    - "Abstract grammar-pattern notation (e.g. ~(으)면) is retained when it exists as its own deck card, because resolution is deck-lookup — NOT literal sentence-text containment (GRAPH-03)"
    - "A read-only dry-run reports the filter's drop rate against the real corpus, broken out separately for grammar- vs vocabulary-type components, BEFORE any write-path wiring exists (GRAPH-05, Success Criterion 4)"
  artifacts:
    - "lib/filter-components.ts exporting filterComponents(rawComponents: string[], deckNormalizedFronts: Set<string>): string[]"
    - "tests/filter-components.test.ts covering direct-match / stem-fallback / no-match / empty / grammar-pattern / real-but-unrelated cases"
    - "scripts/dry-run-filter.mjs — read-only diagnostic printing per-Card.type drop rates"
  key_links:
    - "filterComponents imports normalizeFront from lib/card-key and splitParticle from lib/sentence-match (no Prisma, no Anthropic — pure module, GRAPH-04)"
    - "dry-run builds the deck-lookup Set from ALL cards (no `where: components not null` filter — RESEARCH.md Pitfall 1)"
---

<objective>
Build the pure, unit-tested deck-lookup filter that is the heart of this phase (GRAPH-03/GRAPH-04), and the read-only corpus dry-run (GRAPH-05) that Success Criterion 4 requires to run and be human-reviewed BEFORE the filter is wired into the sync write path.

Purpose: Establish the single source of truth for "does this components[] entry resolve to a real card in the deck?" and prove — against the live corpus, without touching data — that turning the filter on will not disproportionately drop grammar-pattern edges.

Output: `lib/filter-components.ts`, `tests/filter-components.test.ts`, `scripts/dry-run-filter.mjs`, and a human-reviewed drop-rate report gating the downstream wiring plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-components-filter-fix/16-RESEARCH.md
@.planning/phases/16-components-filter-fix/16-PATTERNS.md
</context>

<artifacts>
## Artifacts this plan produces
- `lib/filter-components.ts` — NEW. Exports `filterComponents(rawComponents: string[], deckNormalizedFronts: Set<string>): string[]` (pure; no Prisma/Anthropic import).
- `tests/filter-components.test.ts` — NEW. Vitest coverage for `filterComponents`.
- `scripts/dry-run-filter.mjs` — NEW. Read-only Turso diagnostic; prints per-`Card.type` (grammar / vocabulary / phrase) drop-rate counts. Never writes.
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/filter-components.ts (pure deck-lookup filter) with tests</name>
  <read_first>
    - lib/known-words.ts (FULL — the exact two-phase resolution pattern to mirror; countUnknownWords lines 34-66)
    - lib/card-key.ts (lines 25-40 — normalizeFront, imported not modified)
    - lib/sentence-match.ts (lines 55-89 — splitParticle + PARTICLES_MULTI/SINGLE, imported not modified)
    - tests/known-words.test.ts (FULL — test-file structure to mirror: describe/it, Korean-example comments)
    - tests/card-key.test.ts (FULL — idempotency/edge-case style, e.g. empty string)
  </read_first>
  <files>lib/filter-components.ts, tests/filter-components.test.ts</files>
  <behavior>
    Write tests/filter-components.test.ts FIRST (RED), one `describe('filterComponents', ...)` block, each `it()` with a Korean-example comment explaining the resolution path:
    - Direct match retained: filterComponents(['먹다'], new Set(['먹다'])) === ['먹다']
    - Stem-fallback retained: filterComponents(['학교에서'], new Set(['학교'])) === ['학교에서'] (splitParticle strips 에서 → 학교, in set)
    - No-match dropped: filterComponents(['존재하지않는단어'], new Set(['먹다'])) === []
    - Empty input: filterComponents([], new Set(['먹다'])) === []
    - Grammar pattern retained by DECK-LOOKUP (the GRAPH-03 core case): filterComponents(['~(으)면'], new Set(['~(으)면'])) === ['~(으)면'] — proves retention is by deck membership, not sentence-text containment
    - Real-but-unrelated documented: filterComponents(['~(으)ㄴ 후에'], new Set(['~(으)ㄴ 후에'])) === ['~(으)ㄴ 후에'] — component IS a real card so it is RETAINED by this filter alone; the it() comment must state this filter does not catch the "real-but-wrong-relationship" class (that is GRAPH-01's job, RESEARCH.md "concrete example" section)
    - Mixed input order-preserving: a list with one keeper + one dropped returns only the keeper, original order
  </behavior>
  <action>
    Create lib/filter-components.ts as a PURE module (GRAPH-04) mirroring lib/known-words.ts. Imports: `import { normalizeFront } from './card-key'` and `import { splitParticle } from './sentence-match'` — NO Prisma, NO Anthropic, no Node builtins. Export `filterComponents(rawComponents: string[], deckNormalizedFronts: Set<string>): string[]`. For each component, apply the two-phase resolution from lib/known-words.ts:44-63 as a keep-predicate: (1) if `deckNormalizedFronts.has(normalizeFront(comp))` → keep; (2) else `const { stem } = splitParticle(comp)`; if `stem && stem !== comp && deckNormalizedFronts.has(normalizeFront(stem))` → keep; (3) otherwise drop. Return `rawComponents.filter(predicate)` (preserves input order; caller has already deduped/self-excluded per lib/extract-cards.ts:208-215, so do NOT re-dedupe here). Give the function a leading JSDoc block per the lib/card-key.ts / lib/sequence.ts convention: contract ("keeps a components[] entry only if it resolves to a real card's normalizedFront via direct match or particle-stripped stem match; drops everything else"), the single-source-of-truth caller (lib/extract-cards.ts, wired in plan 16-03), the mirrored analog (lib/known-words.ts), and a "No side-effects; safe server AND client" guarantee. Run the tests, confirm GREEN.
  </action>
  <verify>
    <automated>npm test -- tests/filter-components.test.ts</automated>
  </verify>
  <done>All filterComponents test cases pass. Module has zero Prisma/Anthropic imports (grep confirms). normalizeFront + splitParticle imported from their existing single-source modules.</done>
  <acceptance_criteria>
    - `npm test -- tests/filter-components.test.ts` exits 0 with all listed cases passing.
    - `grep -nE "from '@?/?lib/prisma'|@anthropic-ai/sdk|from 'fs'|from 'crypto'" lib/filter-components.ts` returns no matches (pure module, GRAPH-04).
    - Exported signature is exactly `filterComponents(rawComponents: string[], deckNormalizedFronts: Set<string>): string[]`.
    - Grammar-pattern case `filterComponents(['~(으)면'], new Set(['~(으)면']))` returns `['~(으)면']` (deck-lookup retention, GRAPH-03).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create scripts/dry-run-filter.mjs (read-only per-type drop-rate report)</name>
  <read_first>
    - scripts/check-edges.mjs (FULL — the read-only diagnostic skeleton: parseEnv, .env/.env.local merge, createClient, `n = (r) => r[0].n ?? r[0][0]` row-unwrap)
    - scripts/relink-dependencies.mjs (lines 55-68 — the inline re-derived normalizeFront pattern for .mjs scripts; lines 42-53 — loading cards + building keyToId)
    - lib/filter-components.ts (Task 1 output — the exact keep/drop logic this script's inline re-derivation MUST reproduce)
    - lib/sentence-match.ts (lines 55-89 — splitParticle + particle lists to re-derive inline in JS)
    - prisma/schema.prisma (Card model — confirm `type` is 'vocabulary' | 'grammar' | 'phrase' and `components` is a nullable JSON string column)
  </read_first>
  <files>scripts/dry-run-filter.mjs</files>
  <action>
    Create scripts/dry-run-filter.mjs modeled on scripts/check-edges.mjs. Reuse VERBATIM: the header JSDoc ("Read-only diagnostic … does NOT modify data" + Usage line `node scripts/dry-run-filter.mjs`), the `parseEnv()` helper, the `.env`/`.env.local` merge, `DATABASE_URL`/`DATABASE_AUTH_TOKEN` resolution, and `createClient({ url, authToken })`. This is a plain `.mjs` script run by `node`, so it CANNOT import lib/filter-components.ts (TypeScript) — re-derive `normalizeFront` inline exactly like scripts/relink-dependencies.mjs:55-68, and re-derive `splitParticle` + the two-phase keep-predicate inline in JS so it reproduces lib/filter-components.ts's decisions identically. Query `SELECT "id","type","components","normalizedFront" FROM "Card"` (NO `WHERE` clause — build the deck-lookup Set from ALL cards, RESEARCH.md Pitfall 1; scoping to `components IS NOT NULL` here would be the bug). Build `deckSet` from every card's normalizedFront. Then for each card with non-null components, `JSON.parse` its components, run each through the inline keep-predicate against deckSet, and tally per `Card.type`: total components seen, kept, dropped. Print a report grouped by type — `grammar`, `vocabulary`, `phrase` on separate lines — showing `dropped / total (pct%)` for each, plus a whole-corpus total. GRAPH-05 / Success Criterion 4 REQUIRES grammar vs vocabulary reported separately, because grammar components are the higher-risk class. Wrap the malformed-JSON `JSON.parse` in try/catch (skip + warn, never crash — mirrors scripts/relink-dependencies.mjs:80). The script performs ZERO writes.
  </action>
  <verify>
    <automated>node --check scripts/dry-run-filter.mjs</automated>
  </verify>
  <done>Script is syntactically valid and, when run against the corpus (Task 3 checkpoint), prints per-type (grammar / vocabulary / phrase) drop-rate counts. Contains no INSERT/UPDATE/DELETE statements.</done>
  <acceptance_criteria>
    - `node --check scripts/dry-run-filter.mjs` exits 0 (valid syntax).
    - `grep -iE "INSERT|UPDATE |DELETE|upsert|executeMultiple" scripts/dry-run-filter.mjs` returns no matches (read-only guarantee).
    - The card-loading query has no `WHERE "components"` clause (deck Set built from all cards).
    - Report output distinguishes `grammar` and `vocabulary` drop rates on separate labeled lines.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    scripts/dry-run-filter.mjs — a read-only report of how many stored components[] entries the new deck-lookup filter would drop, broken out by card type (grammar / vocabulary / phrase). This is Success Criterion 4's mandatory pre-wiring gate: the drop rate must be reviewed BEFORE plan 16-03 wires the filter into the sync write path.
  </what-built>
  <how-to-verify>
    1. Run: `node scripts/dry-run-filter.mjs`
    2. Read the per-type report. Confirm the grammar-type drop rate is NOT dramatically higher than the vocabulary-type drop rate. A grammar drop rate far above vocabulary would be the RESEARCH.md Pitfall 4 warning sign (filter gutting grammar-pattern edges) — if you see that, do NOT approve; the filter logic needs review before wiring.
    3. Sanity-check a few dropped entries if listed: they should be plausibly-spurious strings (fabrications), not obviously-real lemmas you can confirm exist as cards.
    4. Note the headline numbers (total dropped, grammar %, vocabulary %) — these are the SUMMARY-recorded baseline that plan 16-04's retroactive cleanup will be compared against.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally with the grammar% / vocabulary% you observed) to unblock the wiring plan, or describe the anomaly if grammar drop rate is disproportionate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude extraction response → sync pipeline | Untrusted-ish external LLM output whose `components[]` strings may be fabrications; the filter is the data-integrity control |
| Dry-run script → Turso (read-only) | Script reads production corpus; no user-controllable input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-02 | Tampering (data integrity) | filterComponents / spurious CardDependency edges | medium | mitigate | Deck-lookup filter drops components that resolve to no real card; unit tests lock the keep/drop contract |
| T-16-03 | Tampering (SQL injection) | scripts/dry-run-filter.mjs | low | accept | Read-only, fixed queries, no user input; if a `--type` flag is later added use `@libsql/client` parameterized `args: [...]`, never string interpolation |
| T-16-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH.md Package Legitimacy Audit: N/A); zero new dependencies per milestone decision |
</threat_model>

<verification>
- `npm test -- tests/filter-components.test.ts` green.
- `npm run lint` clean.
- `node --check scripts/dry-run-filter.mjs` valid.
- Human-reviewed dry-run report confirms grammar drop rate is not disproportionate (Success Criterion 4 gate satisfied).
</verification>

<success_criteria>
- filterComponents is pure, unit-tested, and mirrors lib/known-words.ts two-phase resolution (GRAPH-03, GRAPH-04).
- Grammar-pattern components are retained via deck-lookup (GRAPH-03).
- Dry-run reports grammar-vs-vocabulary drop rate against the real corpus with no writes, reviewed before any wiring (GRAPH-05, Success Criterion 4).
</success_criteria>

<output>
Create `.planning/phases/16-components-filter-fix/16-01-SUMMARY.md` when done. Record the human-approved drop-rate baseline (total dropped, grammar %, vocabulary %) — plan 16-04 compares its retroactive cleanup counts against this.
</output>
