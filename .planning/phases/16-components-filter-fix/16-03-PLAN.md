---
phase: 16-components-filter-fix
plan: 03
type: execute
wave: 2
depends_on: [16-01, 16-02]
files_modified:
  - lib/extract-cards.ts
  - app/api/sync/route.ts
  - scripts/local-resync.mts
  - tests/extract-cards.test.ts
autonomous: true
requirements: [GRAPH-03]

must_haves:
  truths:
    - "filterComponents() runs inside the live extraction pipeline (parseExtractionResponse), so a persisted Card.components contains only entries that resolve to a real deck card via normalizeFront deck-lookup (GRAPH-03, Success Criterion 1)"
    - "The FULL deck normalizedFront set (existingNormalizedFronts — ALL cards, no where clause) is threaded into the filter, NOT the narrow components-not-null subset (RESEARCH.md Pitfall 1)"
    - "app/api/sync/route.ts's keyToId map is built from ALL cards, so a real leaf-node card (no components of its own) is resolvable as another card's prerequisite — spurious entries no longer create edges AND real leaf prerequisites are no longer under-linked (user-locked keyToId fix, closes Success Criterion 1)"
    - "Abstract grammar-pattern components that exist as their own deck card survive the wired-in filter end-to-end (GRAPH-03, Success Criterion 3)"
  artifacts:
    - "lib/extract-cards.ts — parseExtractionResponse(text, deckNormalizedFronts) threads a Set<string> and calls filterComponents at the post-self-dedup insertion point; extractCardsFromNotes builds the Set from its existingNormalizedFronts param"
    - "app/api/sync/route.ts — keyToId seeded from ALL cards (no where: { components: { not: null } })"
    - "scripts/local-resync.mts — the same keyToId scoping corrected for the bulk-resync + final-relink passes"
    - "tests/extract-cards.test.ts — extended with deckSet-driven filtering cases (spurious dropped, in-deck grammar pattern retained)"
  key_links:
    - "extractCardsFromNotes turns existingNormalizedFronts (2nd param — all cards) into new Set(...) and passes it into parseExtractionResponse → filterComponents; local-resync.mts benefits for free (it calls extractCardsFromNotes directly)"
    - "filterComponents imported from lib/filter-components (created + human-approved via plan 16-01's dry-run checkpoint — this plan is GATED on that approval via depends_on: [16-01])"
---

<objective>
Wire the plan 16-01 deck-lookup filter into the live write path and fix the pre-existing `keyToId` scoping bug so that, together, spurious `components[]` entries stop creating `CardDependency` edges (Success Criterion 1) while real leaf-node prerequisites become properly linkable. This is the write-path half of the phase and is intentionally GATED behind plan 16-01's human-reviewed dry-run (via `depends_on`), per ROADMAP.md Success Criterion 4's ordering: the filter is only wired in AFTER the corpus drop-rate has been reviewed.

Purpose: Make GRAPH-03 real in production and honor the user-locked decision to fix the `keyToId` scope in the same phase. Threading the filter INSIDE the extraction pipeline (per RESEARCH.md Assumption A1 / Open Question 2) means both `app/api/sync/route.ts` and `scripts/local-resync.mts` get filtering for free from a single integration point.

Output: filterComponents called inside `parseExtractionResponse`, the deck set threaded from `existingNormalizedFronts`, the `keyToId` scope corrected in the sync route and its bulk-resync sibling, and extended tests proving the end-to-end filtering behavior (SC1 + SC3).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-components-filter-fix/16-RESEARCH.md
@.planning/phases/16-components-filter-fix/16-PATTERNS.md
@lib/filter-components.ts
@.planning/phases/16-components-filter-fix/16-01-SUMMARY.md
</context>

<artifacts>
## Artifacts this plan produces
- `lib/extract-cards.ts` — MODIFIED. `parseExtractionResponse` gains a `deckNormalizedFronts: Set<string>` parameter and calls `filterComponents()` at the insertion point (after self-dedup/self-exclude). `extractCardsFromNotes` builds the Set from `existingNormalizedFronts` and passes it in.
- `app/api/sync/route.ts` — MODIFIED. `keyToId` seed query loses its `where: { components: { not: null } }` clause (built from all cards); the `keyToId.delete(nf)` "losing components" branch is reconciled with the broadened scope (see action).
- `scripts/local-resync.mts` — MODIFIED. The two `where: { components: { not: null } }` card-loading queries feeding `keyToId` (bulk per-lesson link + final relink) are corrected to build the lookup from all cards.
- `tests/extract-cards.test.ts` — MODIFIED. New cases exercising the `deckNormalizedFronts` path end-to-end.
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread the deck set and call filterComponents() inside the extraction pipeline (GRAPH-03)</name>
  <read_first>
    - lib/extract-cards.ts (CURRENT state, post plan 16-02 — read the whole file: parseExtractionResponse now owns salvage + validation + the per-card .map())
    - lib/filter-components.ts (plan 16-01 output — the exact filterComponents(rawComponents, deckNormalizedFronts) signature to call)
    - .planning/phases/16-components-filter-fix/16-RESEARCH.md "Code Examples → lib/extract-cards.ts:204-240" (the INSERTION POINT: after self-dedup/self-exclude, before the returned object) and "Assumption A1 / Open Question 2" (thread inside extractCardsFromNotes, not a second call site)
    - lib/extract-cards.ts:30-38 (extractCardsFromNotes signature — existingNormalizedFronts: string[] is already the 2nd param; currently only used for the prompt dedup-hint text)
    - tests/extract-cards.test.ts (plan 16-02 output — the describe/it structure to extend)
  </read_first>
  <files>lib/extract-cards.ts, tests/extract-cards.test.ts</files>
  <behavior>
    Extend tests/extract-cards.test.ts FIRST (RED). Add cases that call `parseExtractionResponse(rawText, deckSet)` with an explicit `deckSet: Set<string>`:
    - Spurious component dropped end-to-end: a well-formed card whose `components` includes a fabricated string not in `deckSet` → the returned card's `components` excludes it.
    - Real component retained: a component whose normalizeFront IS in `deckSet` → retained.
    - Grammar-pattern retained by deck-lookup (SC3): `deckSet` contains `~(으)면`; a card listing `~(으)면` in components → retained (proves deck-membership retention, not sentence-text containment).
    - Empty/absent deckSet default: calling `parseExtractionResponse(rawText)` with no second arg (default empty Set) drops all components (documents the default; the live caller always passes the real set).
    - Self-dedup preserved: the existing self-exclude (component equal to the card's own normalized front) still drops the headword before the deck filter runs.
  </behavior>
  <action>
    In lib/extract-cards.ts: (1) Add `import { filterComponents } from './filter-components'`. (2) Change the pure helper signature to `export function parseExtractionResponse(text: string, deckNormalizedFronts: Set<string> = new Set()): ExtractedCard[]` (default empty Set keeps unit tests and any other caller safe). (3) At the per-card INSERTION POINT — immediately AFTER the existing self-dedup/self-exclude that produces the deduped `components` array (the `[...new Set(rawComponents.filter(...).map(...).filter(x => normalizeFront(x) !== myKey))]` block), BEFORE the returned object — replace the assigned `components` with `filterComponents(components, deckNormalizedFronts)`. Order matters (RESEARCH.md): cheap self-dedup/self-exclude first, then deck-filter. Do NOT re-dedupe inside filterComponents (it preserves order and the caller already deduped). (4) In `extractCardsFromNotes`, build `const deckSet = new Set(existingNormalizedFronts)` and change the final `return parseExtractionResponse(text)` to `return parseExtractionResponse(text, deckSet)`. Because `scripts/local-resync.mts` calls `extractCardsFromNotes` directly, it inherits the filter with no separate integration point (RESEARCH.md Assumption A1). Do NOT add a second filterComponents call inside app/api/sync/route.ts. Run tests, confirm GREEN.
  </action>
  <verify>
    <automated>npm test -- tests/extract-cards.test.ts</automated>
  </verify>
  <done>parseExtractionResponse filters components against the passed deck set; extractCardsFromNotes threads the full existingNormalizedFronts set through; spurious components are dropped and in-deck grammar patterns retained end-to-end. All extract-cards tests green.</done>
  <acceptance_criteria>
    - `npm test -- tests/extract-cards.test.ts` exits 0, including the new spurious-dropped and grammar-retained (SC3) cases.
    - `grep -n "filterComponents" lib/extract-cards.ts` shows the import and exactly one call site (inside parseExtractionResponse's per-card map).
    - `grep -n "parseExtractionResponse(text, deckSet)" lib/extract-cards.ts` shows extractCardsFromNotes passes the built Set (no second filter call added to the sync route).
    - The self-exclude of the card's own `myKey` still runs BEFORE filterComponents (verified by the self-dedup test case).
    - `npm run lint` passes clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix the keyToId scoping bug in the sync route and the bulk-resync sibling (user-locked decision)</name>
  <read_first>
    - app/api/sync/route.ts:52-54 (existingNormalizedFronts — the CORRECT "all cards, no where clause" shape to mirror)
    - app/api/sync/route.ts:81-86 (the keyToId seed query with `where: { components: { not: null } }` — the bug to fix)
    - app/api/sync/route.ts:170-212 (the keyToId.set / keyToId.delete branches during upsert — must stay consistent with the broadened seed) and 217-247 (the edge-linking loop that consumes keyToId)
    - scripts/local-resync.mts:174-179 (per-lesson link keyToId, same `where: { components: { not: null } }` bug) and 205-211 (final relink pass, same bug)
    - .planning/phases/16-components-filter-fix/16-RESEARCH.md "Pitfall 1" + "Open Question 1 / Assumption A4" (this is the user-locked, in-scope fix — build the lookup from ALL cards so leaf-node cards are resolvable as prerequisites)
  </read_first>
  <files>app/api/sync/route.ts, scripts/local-resync.mts</files>
  <action>
    In app/api/sync/route.ts: change the `seedCards` query at lines 82-85 to select all cards with NO `where` clause — `await prisma.card.findMany({ select: { id: true, normalizedFront: true } })` — so `keyToId` maps every real card's normalizedFront → id (the same broad shape as `existingNormalizedFronts` at 52-54). Update the surrounding comment (77-80) to state that the map now covers ALL cards so leaf-node cards are resolvable as prerequisites (the old comment justified the narrow scope — replace that justification, do not leave it stale). Because the seed is now broad, the `keyToId.delete(nf)` branch at 207-212 (which removed a card that "lost" its components so it could no longer be a prerequisite) is now WRONG under the new semantics — a card with no components of its own is STILL a valid prerequisite target. Remove that delete branch (a card losing its own components must remain resolvable as someone else's prerequisite); keep the `keyToId.set(nf, existing.id)` in the has-components branch (harmless — already seeded, just reaffirms). Do NOT change the edge-linking loop logic at 223-247 (it already skips self-edges and unresolved prereqs); it simply now resolves more real prerequisites. In scripts/local-resync.mts: apply the identical scope fix to BOTH card-loading queries that build a `keyToId` — line 175-178 (per-lesson link) and line 207-210 (final relink) — removing `where: { components: { not: null } }` so both build the lookup from all cards (the `if (!upsertedSet.has(...) || !dbCard.components) continue` and `if (!dbCard.components) continue` guards already skip iterating cards that have no components as edge SOURCES, so broadening the lookup only affects which prerequisites resolve — exactly the intended fix). This is the same bug in a sibling call site; fixing it prevents the next bulk resync from re-narrowing edge resolution. IMPORTANT for the verify gate: fully REMOVE the narrow-scope Prisma clause from both files — do NOT leave it echoed inside any `//` comment or "what changed" note, because the automated check below negative-greps the source for that clause and expects zero occurrences (describe the change by concept in prose, not by quoting the removed clause).
  </action>
  <verify>
    <automated>bash -c 'grep -RnE "where:\s*\{\s*components:\s*\{\s*not:\s*null\s*\}\s*\}" app/api/sync/route.ts scripts/local-resync.mts | grep -v "^#" | wc -l | tr -d " "'</automated>
  </verify>
  <done>Both the sync route and local-resync.mts build their keyToId lookup from all cards; no `where: { components: { not: null } }` scoping remains on any keyToId-feeding query; the stale narrow-scope justification comment and the now-incorrect keyToId.delete branch are removed. Sync route still compiles and lints.</done>
  <acceptance_criteria>
    - The verify command prints `0` — no `where: { components: { not: null } }` clause remains on any keyToId-building card query in either file. (Note: the `componentsJson`/`card.components.length` value checks used elsewhere are unrelated and must remain.)
    - `app/api/sync/route.ts` `seedCards` query has no `where` clause and selects `{ id, normalizedFront }`.
    - The `keyToId.delete(nf)` branch in the update path (previously ~207-212) is removed, and the surrounding comment no longer claims prerequisite resolution is scoped to cards-with-components.
    - `npm run lint` passes clean (both files).
    - `npm test` full suite passes clean (no regression in the pure-lib tests).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude extraction response → sync pipeline | Fabricated `components[]` strings crossing into persisted `Card.components` and `CardDependency` edges; the wired-in deck-lookup filter is the data-integrity control |
| In-memory keyToId map → CardDependency writes | The lookup's SCOPE decides which real cards can be prerequisites; a too-narrow scope silently drops valid leaf-node edges (false negatives) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-06 | Tampering (data integrity) | Unfiltered components[] creating spurious CardDependency edges that corrupt foundation-first sequencing | high | mitigate | filterComponents() wired into parseExtractionResponse drops components resolving to no real card BEFORE persist, fed the full deck set; extended tests assert spurious-dropped end-to-end (GRAPH-03, Success Criterion 1) |
| T-16-07 | Tampering (data integrity, false negative) | keyToId scoped to components-not-null under-links real leaf-node prerequisites | medium | mitigate | keyToId seeded from ALL cards in sync route + local-resync.mts; delete-on-losing-components branch removed so leaf cards stay resolvable (user-locked fix, RESEARCH.md Pitfall 1) |
| T-16-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH.md Package Legitimacy Audit: N/A); zero new dependencies per milestone decision |
</threat_model>

<verification>
- `npm test` full suite green (includes the extended extract-cards deck-filter cases proving SC1 spurious-drop and SC3 grammar-retain).
- `npm run lint` clean across lib/extract-cards.ts, app/api/sync/route.ts, scripts/local-resync.mts.
- keyToId scope grep returns 0 (no `components: { not: null }` on lookup queries).
- Depends on plan 16-01's human-verify checkpoint being APPROVED (dry-run drop-rate reviewed) before this plan executes — enforced via depends_on: [16-01].
</verification>

<success_criteria>
- After a sync, a newly-extracted card's components[] contains only lemmas that resolve to a real deck card, and spurious entries no longer create CardDependency edges (GRAPH-03, Success Criterion 1) — requires BOTH the wired-in filter AND the keyToId fix, both delivered here.
- Real leaf-node cards are resolvable as prerequisites (keyToId fix; no more false-negative under-linking).
- In-deck grammar-pattern components survive the wired-in filter end-to-end (Success Criterion 3).
</success_criteria>

<output>
Create `.planning/phases/16-components-filter-fix/16-03-SUMMARY.md` when done. Note the single integration point (inside extractCardsFromNotes) and that scripts/local-resync.mts inherits filtering for free; record that the keyToId scope is now all-cards in both the sync route and local-resync.mts (input for plan 16-04's retroactive edge reconciliation).
</output>
