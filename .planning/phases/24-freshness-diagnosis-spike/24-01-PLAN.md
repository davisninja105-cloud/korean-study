---
phase: 24-freshness-diagnosis-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/diagnose-freshness.mts
autonomous: false
requirements: [FRESH-01]

must_haves:
  truths:
    - "Running `npx tsx scripts/diagnose-freshness.mts` hard-fails (exit 1) with a refusal message if the resolved DATABASE_URL starts with libsql:// (D-02, T-24-01)"
    - "The script seeds an isolated file: SQLite test DB (1 lesson, 3 cards with sentences, 3 due CardReviews) via prisma db push + nested creates — never touches Turso or prisma/dev.db (D-02)"
    - "The script builds and boots a real production server (npm run build + next start on port 3200) — never next dev (D-01)"
    - "The injected ks_auth cookie authenticates: the script reaches / without redirect to /login (D-03)"
    - "Build output confirms all four routes (/, /cards, /study, /habits) render as dynamic (f marker), re-validating the force-dynamic assumption before any browser work"
  artifacts:
    - "scripts/diagnose-freshness.mts"
    - "package.json (playwright in devDependencies)"
  key_links:
    - "process.env.DATABASE_URL / AUTH_SECRET / APP_PASSWORD are overridden BEFORE any dynamic import of lib/prisma.js or lib/auth.js (module-init env reads)"
    - "One identical childEnv object is passed to BOTH the npm run build and next start child processes (RESEARCH Pitfall 3)"
    - "The script's AUTH_SECRET equals the server child's AUTH_SECRET, so computeAuthToken() output validates on both sides"
---

<objective>
Install the `playwright` devDependency (behind a blocking human legitimacy checkpoint — flagged `[SUS]` by the legitimacy seam per 24-RESEARCH.md / 24-VALIDATION.md) and build the plumbing half of the throwaway diagnosis script `scripts/diagnose-freshness.mts`: DB-isolation hard-fail guard, isolated `file:` SQLite seed, production-server orchestration, deterministic `ks_auth` auth-cookie injection, and an end-to-end smoke check proving the whole rig works.

This implements locked decisions D-01 (fully scripted reproduction via raw `playwright`, no `playwright.config.ts`, no `tests-e2e/`), D-02 (throwaway script, isolated `file:` test DB, `prisma db push`), and D-03 (HMAC cookie injection via `lib/auth.ts:computeAuthToken()`, no UI login).

Purpose: De-risk every piece of environment/DB/auth wiring before Plan 24-02 layers the 16-cell navigation-matrix instrumentation on top. All of this plumbing has failure modes that would silently corrupt the diagnosis (wrong DB, dev-mode server, broken cookie) — proving it with a smoke check first keeps Plan 02 focused on measurement.
Output: `playwright` installed + Chromium binary present; a runnable `scripts/diagnose-freshness.mts` that seeds, builds, boots, authenticates, smoke-asserts, and tears down cleanly.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-freshness-diagnosis-spike/24-CONTEXT.md
@.planning/phases/24-freshness-diagnosis-spike/24-RESEARCH.md
@.planning/phases/24-freshness-diagnosis-spike/24-PATTERNS.md
@scripts/local-resync.mts
@lib/auth.ts
@lib/prisma.ts
@prisma/schema.prisma
</context>

## Artifacts this phase produces

Phase 24's complete artifact list (this plan produces the first three; Plan 24-02 produces the rest):

- `scripts/diagnose-freshness.mts` — throwaway Playwright diagnostic script (this plan: plumbing + smoke; Plan 02 adds `isRscRequest()` predicate, `simulateResume()` helper, per-route observable readers/mutators, and the 16-cell matrix loop)
- `package.json` / `package-lock.json` — `playwright` added to devDependencies
- `scripts/.tmp/24-diagnosis.db` — isolated test DB file (generated at runtime, never committed; covered by the existing `*.db` gitignore rule)
- `.planning/phases/24-freshness-diagnosis-spike/24-DIAGNOSIS.md` — Plan 24-02's deliverable (matrix + scenario blocks)

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve playwright devDependency install ([SUS] legitimacy override)</name>
  <read_first>
    - .planning/phases/24-freshness-diagnosis-spike/24-RESEARCH.md — "Package Legitimacy Audit" section (the [SUS] verdict and its override rationale)
    - .planning/phases/24-freshness-diagnosis-spike/24-VALIDATION.md — "Wave 0 Requirements" and "Manual-Only Verifications" (the gate mandate)
  </read_first>
  <what-built>Nothing yet — this gate precedes the install. The automated legitimacy seam flagged `playwright` as [SUS] on a "too-new" release-cadence heuristic alone (Playwright ships near-weekly). The research override notes: official microsoft/playwright repository, 65.4M weekly downloads, latest publish 2026-06-23, version 1.61.1 verified live via npm view. Per the Package Legitimacy Gate protocol, [SUS] packages require human confirmation before any package-manager install runs, regardless of override rationale. This checkpoint is never auto-approvable.</what-built>
  <action>STOP and present this checkpoint to the human before running any install command. Show the [SUS] flag context and the override rationale from 24-RESEARCH.md's Package Legitimacy Audit, then wait for the resume signal. Do not proceed to Task 2 (npm install -D playwright) until the human types explicit approval. This gate is exempt from workflow.auto_advance — legitimacy checkpoints are never auto-approvable.</action>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/playwright
    2. Confirm the package is published by Microsoft and the linked repository is github.com/microsoft/playwright
    3. Confirm weekly downloads are in the tens of millions (research recorded 65.4M/week on 2026-07-10)
    4. Confirm the version to be installed (latest, ~1.61.x) matches the npm page's latest tag
  </how-to-verify>
  <acceptance_criteria>
    - Human has viewed the npmjs.com/package/playwright page and typed explicit approval in this session BEFORE any npm install command runs
    - No install command was executed prior to approval
  </acceptance_criteria>
  <resume-signal>Type "approved" to allow `npm install -D playwright` to proceed, or describe concerns to halt the phase</resume-signal>
  <done>Human has explicitly approved the playwright install in-session; no install command ran before that approval</done>
</task>

<task type="auto">
  <name>Task 2: Install playwright devDependency + Chromium binary</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json — confirm existing devDependencies block shape and that no playwright entry exists yet
    - .planning/phases/24-freshness-diagnosis-spike/24-RESEARCH.md — "Standard Stack" + "Installation" sections (raw `playwright` package, NOT `@playwright/test`, per D-01)
  </read_first>
  <action>
    Run `npm install -D playwright` (raw library package — deliberately NOT `@playwright/test`, per D-01: this phase must not create test-runner conventions; Phase 25 owns `@playwright/test`). Then run `npx playwright install chromium` to download the Chromium browser binary (Chromium only — no firefox/webkit, per the milestone's Chromium-only scope).

    Do NOT create `playwright.config.ts`, a `tests-e2e/` directory, or any other Phase 25 harness convention (D-01/D-02). Do NOT add any npm script entry to package.json — the diagnosis script is invoked directly via `npx tsx` like the other `scripts/*.mts` utilities.
  </action>
  <verify>
    <automated>npm ls playwright && npx playwright --version && test ! -f playwright.config.ts && test ! -d tests-e2e</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls playwright` exits 0 and shows playwright at ~1.61.x under devDependencies
    - `npx playwright --version` prints a version string
    - `test ! -f playwright.config.ts` passes (no config file created)
    - `test ! -d tests-e2e` passes (no test directory created)
    - package.json diff touches only the devDependencies block
  </acceptance_criteria>
  <done>playwright ~1.61.x is a devDependency with the Chromium binary installed, and zero Phase 25 harness conventions exist in the repo</done>
</task>

<task type="auto">
  <name>Task 3: Write diagnose-freshness.mts plumbing — guard, seed, server orchestration, auth, smoke</name>
  <files>scripts/diagnose-freshness.mts</files>
  <read_first>
    - scripts/local-resync.mts — lines 12-28 (env-first dynamic-import pattern) and lines 122-143 (nested-create Card + Sentence + CardReview shape); also its console-logging style and explicit process.exit convention
    - lib/auth.ts — whole file (34 lines): computeAuthToken() contract and the AUTH_COOKIE export
    - lib/prisma.ts — singleton construction; confirms DATABASE_URL is read at module init (why env override must precede dynamic import)
    - prisma/schema.prisma — FULL Lesson, Card, Sentence, CardReview models (RESEARCH Open Question #2 requires re-verifying every CardReview field name/default before writing the seed)
    - prisma.config.ts — confirms the prisma CLI reads DATABASE_URL from env
    - components/HomeClient.tsx — the due-count hero markup near line 208 (pick the smoke-check selector from real JSX, per RESEARCH Pitfall 5)
    - middleware.ts — auth matcher scope (confirms unauthenticated / redirects to /login)
    - .planning/phases/24-freshness-diagnosis-spike/24-RESEARCH.md — Architecture Patterns 1, 2, 5, 6 and Pitfalls 3, 4, 5
    - .planning/phases/24-freshness-diagnosis-spike/24-PATTERNS.md — Pattern Assignments for this exact file (env override, hard-fail guard, logging style, per-unit try/catch)
  </read_first>
  <action>
    Create `scripts/diagnose-freshness.mts` implementing D-01/D-02/D-03. Structure, in order:

    1. Header comment block (repo `.mts` convention): state this is the Phase 24 throwaway freshness-diagnosis script, usage `npx tsx scripts/diagnose-freshness.mts`, that it is NOT the Phase 25 harness, and that it only ever targets an isolated local `file:` test DB.

    2. Module-scope constants: `DIAG_PORT` (3200), `BASE_URL` (`http://localhost:3200`), `TEST_DB_PATH` resolved to an absolute path at `scripts/.tmp/24-diagnosis.db` via `fileURLToPath(import.meta.url)` + `path.resolve` (same `__dir` technique as local-resync.mts, so cwd never matters), `TEST_DB_URL` (`file:` + TEST_DB_PATH), `THROWAWAY_SECRET` (a literal throwaway string, e.g. `diagnosis-throwaway-secret`).

    3. Env override BEFORE any dynamic import (Pattern 1): assign `process.env.DATABASE_URL = TEST_DB_URL`, `process.env.AUTH_SECRET = THROWAWAY_SECRET`, `process.env.APP_PASSWORD` to a throwaway value, and delete `process.env.DATABASE_AUTH_TOKEN`. Do NOT call dotenv's config() to load the real `.env`/`.env.local` for these vars — the real Turso URL and real AUTH_SECRET must never enter this process (T-24-01, T-24-02; RESEARCH Security Domain).

    4. Hard-fail DB-isolation guard immediately after the override (T-24-01, per the security contribution and PATTERNS.md item 2): if `process.env.DATABASE_URL` starts with the hosted-Turso URL scheme (`libsql://`), print a refusal message to stderr and `process.exit(1)`. Define this as a reusable `assertLocalDb(url)` helper because it must run a second time on the childEnv object right before the server spawn (defense in depth against future edits).

    5. Fresh-run reset: `mkdirSync` the `scripts/.tmp` directory (recursive), `rmSync` any pre-existing TEST_DB_PATH (force) so each run starts from a clean schema+seed.

    6. Schema setup (Pattern 2): `execSync` of `npx prisma db push --skip-generate --accept-data-loss` with an env object spreading `process.env` and pinning DATABASE_URL to TEST_DB_URL, stdio inherit.

    7. Seed (dynamic import AFTER env override): `await import('../lib/prisma.js')` (note the `.js` specifier on a relative TS import — repo tsx convention). Create one Lesson (title `Diagnosis fixture`, rawContent empty string, contentHash `diagnosis-fixture`, orderIndex 1). Create three Cards using the nested-create shape from local-resync.mts lines 122-143: each with unique Hangul front + matching normalizedFront (three distinct vocabulary items, e.g. 안녕/hello, 학교/school, 감사하다/to thank), type `vocabulary`, lessonId from the created lesson, one Sentence each (korean containing the targetForm exactly once with targetForm 2+ characters — blank-safe per the app's rules), and a nested `review: { create: ... } }` CardReview with state 1, stability 1, difficulty 5, and nextReview set to a Date one minute in the past — so all three cards are due. Verify every CardReview field name against the schema read in read_first before writing (Open Question #2); add any non-defaulted required fields the schema demands.

    8. Build (Pattern 5 + Pitfall 3): define ONE `childEnv` object (spread of process.env with DATABASE_URL, AUTH_SECRET, APP_PASSWORD pinned to the throwaway values) and pass that same object to BOTH the build and start child processes. `execSync` `npm run build` with childEnv, capturing stdout to a string. Then implement the Pitfall 4 sanity gate: parse the captured build output's route table and assert each of `/`, `/cards`, `/study`, `/habits` is marked with the dynamic route marker (the f-symbol Next.js prints for server-rendered-on-demand routes), printing a failure and exiting 1 if any of the four shows the static circle marker instead.

    9. Start + readiness poll (Pattern 5): `spawn` of `npm run start -- -p 3200` with childEnv (call `assertLocalDb(childEnv.DATABASE_URL)` immediately before this spawn). Implement `waitForServer(url, timeoutMs)` polling `fetch` against BASE_URL + `/login` every 500ms until a sub-500 status, 60s timeout, throwing on expiry — no fixed sleeps.

    10. Browser + auth (Pattern 6, D-03): static-or-dynamic import of `chromium` from the raw `playwright` package, `chromium.launch()` headless, `browser.newContext()`, then dynamic `await import('../lib/auth.js')` for BOTH `computeAuthToken` and `AUTH_COOKIE` (import the constant — do not hardcode the cookie name), and `context.addCookies` with name AUTH_COOKIE, value from computeAuthToken(), url BASE_URL.

    11. Smoke check: open a page, goto BASE_URL + `/`, wait for load. Assert the final URL pathname is `/` (not `/login` — proves the cookie validated). Compute the expected due count live via a Prisma count query on CardReview where nextReview <= now (do not hardcode 3), locate the Home hero due-count element using a selector derived from the actual HomeClient.tsx JSX read in read_first, print the matched element's full text to the log (Pitfall 5), and assert it contains the expected count. Print a success line starting with `SMOKE OK:` including the count.

    12. Teardown in a finally block (PATTERNS item 4 + RESEARCH Pattern 5 teardown note): close the browser, kill the server child (SIGTERM) and await its exit, `prisma.$disconnect()`. End with explicit `process.exit(0)` on success / `process.exit(1)` on any failure, using local-resync.mts's console style (check/cross prefixes, banner via a repeated `=` separator line).

    Keep every helper (`assertLocalDb`, `waitForServer`, the seed function) top-level and exported-or-plain in this single file — no new lib/ modules, zero production code changes (phase boundary).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx tsx scripts/diagnose-freshness.mts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (script type-checks with the rest of the repo)
    - `npx tsx scripts/diagnose-freshness.mts` exits 0 and prints a line starting with `SMOKE OK:` that includes the live-computed due count
    - The run output includes the build route table and an explicit assertion pass that all four routes (`/`, `/cards`, `/study`, `/habits`) are dynamic
    - The final page URL asserted during smoke is `/`, not `/login` (cookie auth worked)
    - `grep -c "startsWith('libsql://')" scripts/diagnose-freshness.mts` returns 1 or more (hard-fail guard present)
    - After the run, `git status --porcelain` shows no `scripts/.tmp/` DB file staged or trackable (covered by the existing `*.db` gitignore rule)
    - No file named playwright.config.ts and no tests-e2e/ directory exists after this task
  </acceptance_criteria>
  <done>A single run of the script seeds an isolated file: DB, boots a production build on port 3200, authenticates via injected ks_auth cookie, prints SMOKE OK with the seeded due count, and tears down the server and browser cleanly — with the libsql:// hard-fail guard in place</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → npm registry | Third-party package code enters the dev environment at install time |
| script → database | The script holds a live Prisma connection; targeting the wrong URL would mutate production data |
| script → local prod server | Auth cookie crosses here; must be derived from a throwaway secret, not the real one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-SC | Tampering | npm installs (playwright devDep) | high | mitigate | Blocking human checkpoint (Task 1) before `npm install -D playwright` — package is [SUS]-flagged; human confirms microsoft/playwright + download volume on npmjs.com before install proceeds |
| T-24-01 | Tampering | diagnose-freshness.mts DB target | high | mitigate | `assertLocalDb()` hard-fail guard (exit 1 on any hosted-Turso URL scheme) runs at script start AND re-runs on childEnv immediately before the server spawn; DATABASE_URL is pinned to the file: test path in-process, never read from .env |
| T-24-02 | Information Disclosure | AUTH_SECRET / APP_PASSWORD handling | medium | mitigate | Throwaway literal values assigned directly to process.env; the real .env / .env.local is never loaded for these vars, so real secrets cannot appear in script logs or the child server env |
| T-24-03 | Information Disclosure | scripts/.tmp/24-diagnosis.db commit hygiene | low | accept | Synthetic fixture data only; path ends in .db which the repo's existing blanket `*.db` gitignore rule already covers — verified in Task 3 acceptance criteria |
</threat_model>

<verification>
- `npm ls playwright` shows the devDependency; no `playwright.config.ts` and no `tests-e2e/` exist anywhere in the repo (D-01 honored).
- `npx tsx scripts/diagnose-freshness.mts` completes a full plumbing cycle: guard → db push → seed → build (all four routes dynamic) → start → cookie auth → SMOKE OK → clean teardown, exit 0.
- `git status` after the run shows only intended files (package.json, package-lock.json, the script) — no test DB, no build artifacts staged.
- Zero production code changes: `git diff --stat` touches nothing under app/, components/, lib/, prisma/, middleware.ts.
</verification>

<success_criteria>
- playwright ~1.61.x installed as devDependency with Chromium binary, behind an explicitly approved human legitimacy checkpoint.
- `scripts/diagnose-freshness.mts` exists, type-checks, and a full run exits 0 printing `SMOKE OK:` — proving DB isolation, seeding, prod-server orchestration, and cookie auth all work end to end.
- The libsql:// hard-fail guard is present and runs both at startup and before the server spawn.
</success_criteria>

<output>
Create `.planning/phases/24-freshness-diagnosis-spike/24-01-SUMMARY.md` when done. Include: the exact playwright version installed, the smoke-run output tail (SMOKE OK line + due count), the selector chosen for the Home due-count anchor (Plan 24-02 reuses it), and any CardReview seed-field corrections discovered against the schema (Open Question #2 resolution).
</output>
