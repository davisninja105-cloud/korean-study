---
phase: 24-freshness-diagnosis-spike
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - scripts/diagnose-freshness.mts
  - .planning/phases/24-freshness-diagnosis-spike/24-DIAGNOSIS.md
autonomous: true
requirements: [FRESH-01]

must_haves:
  truths:
    - "The RSC re-fetch request signature was empirically confirmed from an unfiltered request/header log of this app's real traffic BEFORE the classification filter was trusted (RESEARCH Open Question #1, Pitfall 1)"
    - "Every one of the 16 route x path cells (4 routes x back/forward, resume, post-mutation return, plain Link — D-04) has an explicit Fresh / Stale-RouterCache / Stale-ClientShell verdict backed by both network evidence and a DOM before/after diff (D-10)"
    - "The primary post-mutation scenario — grade all due cards in a real UI study session, navigate to Home, return to Study select-mode — is exercised through the actual grade buttons, not simulated (D-05)"
    - "The resume simulation overrides BOTH document.hidden AND document.visibilityState before dispatching visibilitychange (D-07; StudySession.tsx reads visibilityState, per RESEARCH Pitfall 2)"
    - "24-DIAGNOSIS.md leads with a 16-row summary matrix including explicitly-confirmed fresh paths (D-08, D-09), followed by one reproducible scenario block per stale path with steps, expected-vs-actual, and evidence (D-08, D-10)"
  artifacts:
    - "scripts/diagnose-freshness.mts (extended with isRscRequest, simulateResume, per-route readers/mutators, 16-cell matrix loop)"
    - ".planning/phases/24-freshness-diagnosis-spike/24-DIAGNOSIS.md"
  key_links:
    - "Request-log snapshot is taken immediately before each triggered nav action, and only entries appended after the snapshot count as evidence (prefetch-pollution guard, RESEARCH Pattern 3 gotcha)"
    - "Each cell runs in a fresh browser context with the auth cookie re-injected, so one cell's Router Cache never pollutes the next"
    - "Expected fresh values are computed at classification time via live Prisma queries against the same file: DB the server reads — never hardcoded"
---

<objective>
Extend `scripts/diagnose-freshness.mts` with the measurement half — empirical RSC-signature confirmation, then the full 16-cell route x navigation-path matrix with binary root-cause classification — run it against the production build, and author the phase's actual deliverable: `.planning/phases/24-freshness-diagnosis-spike/24-DIAGNOSIS.md` (summary matrix + per-stale-path scenario blocks, per D-08/D-09/D-10).

This implements locked decisions D-04 (all 4 routes x 4 paths), D-05 (study-grade post-mutation scenario is primary), D-06 (cheap DB-level mutation variants for the secondary scenarios; UI-driven variants stay deferred), D-07 (visibilitychange resume simulation), and D-08/D-09/D-10 (diagnosis document format and binary attribution).

Purpose: FRESH-01 — produce the empirical, per-route staleness diagnosis that Phase 25's red freshness-regression spec (E2E-06) will encode directly and that keeps Phase 26's fix surgical.
Output: A complete raw evidence run and `24-DIAGNOSIS.md` covering all 16 cells with no ambiguous rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-freshness-diagnosis-spike/24-CONTEXT.md
@.planning/phases/24-freshness-diagnosis-spike/24-RESEARCH.md
@.planning/phases/24-freshness-diagnosis-spike/24-PATTERNS.md
@.planning/phases/24-freshness-diagnosis-spike/24-01-SUMMARY.md
@scripts/diagnose-freshness.mts
</context>

## Artifacts this phase produces

Phase 24's complete artifact list (Plan 24-01 produced the install + plumbing; this plan produces the rest):

- `scripts/diagnose-freshness.mts` — extended with: `isRscRequest(req)` predicate (locked to the empirically observed signature), `simulateResume(page, hidden)` helper (dual-property override), per-route observable readers + DB mutators, the 16-cell matrix loop with per-cell classification, and a `--log-requests` signature-confirmation mode
- `.planning/phases/24-freshness-diagnosis-spike/24-DIAGNOSIS.md` — the phase deliverable: RSC-signature findings, 16-row summary matrix, confirmed-fresh list, per-stale-path scenario blocks, Phase 25/26 handoff notes

<tasks>

<task type="auto">
  <name>Task 1: Empirically confirm the RSC re-fetch signature (--log-requests mode)</name>
  <files>scripts/diagnose-freshness.mts</files>
  <read_first>
    - scripts/diagnose-freshness.mts — the Plan 24-01 plumbing this task extends
    - .planning/phases/24-freshness-diagnosis-spike/24-01-SUMMARY.md — smoke-run findings, chosen selectors
    - .planning/phases/24-freshness-diagnosis-spike/24-RESEARCH.md — Pattern 3 (RSC detection + prefetch-pollution gotcha), Pitfall 1 (signature must be confirmed, not assumed), Open Question #1, Assumptions A1/A3
    - components/Nav.tsx — the bottom-bar/desktop Link markup (needed to click real nav links)
  </read_first>
  <action>
    Add a `--log-requests` CLI mode to the script (checked via process.argv). When present, after the existing smoke check the script performs exactly one navigation cycle — from `/` click the real Nav link to `/study`, wait for settle, then page.goBack(), wait for settle — with a `page.on('request')` listener registered BEFORE the initial goto that prints EVERY request's full URL, resource type, and all request headers, completely unfiltered. Then exit 0 without running any matrix.

    Run the mode once. From the output, identify the request(s) that constitute the RSC payload fetch: a request to the navigated pathname made at click/back time whose headers/query distinguish it from a full-document navigation (community expectation per RESEARCH: a `_rsc` query parameter and/or an `rsc: 1` request header — but trust only what is actually observed on this app under Next.js 16.2.x).

    Lock the finding into a top-level `isRscRequest(req)` predicate function whose implementation matches ONLY the observed signature, with a comment above it recording the observed evidence (capture date, one sample URL, the distinguishing header/param). Also record full-document navigations (HTML response for the pathname) separately as document fetches, so classification can distinguish "RSC fetch", "document fetch", and "no fetch".

    Sanity gate per RESEARCH Pitfall 1's warning sign: a plain Link navigation to a force-dynamic route MUST register a data fetch under the locked predicate (staleTimes.dynamic = 0 guarantees it). If the predicate registers zero fetches for the Link-click leg of the cycle, the filter is wrong — re-examine the unfiltered log and fix it before Task 2 builds on it.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx tsx scripts/diagnose-freshness.mts --log-requests</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/diagnose-freshness.mts --log-requests` exits 0 and prints unfiltered URL + header lines covering both the /study Link-click navigation and the goBack() leg
    - `grep -c "isRscRequest" scripts/diagnose-freshness.mts` returns 1 or more (predicate exists)
    - The predicate's implementation references only signals actually present in the logged output, and the comment above it records the sample URL, distinguishing header/param, and capture date
    - The Link-click leg of the confirmation cycle registers as a data fetch under the locked predicate (Pitfall 1 sanity gate passed, stated in the run output)
  </acceptance_criteria>
  <done>The RSC re-fetch signature for this app on Next.js 16.2.x is empirically confirmed and locked into isRscRequest(), with the raw evidence recorded in a code comment — resolving RESEARCH Open Question #1 before any cell is classified</done>
</task>

<task type="auto">
  <name>Task 2: Implement and run the 16-cell navigation matrix with binary classification</name>
  <files>scripts/diagnose-freshness.mts</files>
  <read_first>
    - scripts/diagnose-freshness.mts — current state after Task 1
    - components/HomeClient.tsx — due-count hero anchor (~line 208)
    - components/StudyClient.tsx — select-mode due-count anchor (~line 252) and how a session starts
    - components/CardsClient.tsx — what count/list text the cards page renders from initialCards
    - components/HabitsClient.tsx and app/habits/page.tsx — which server-prop number is rendered and how masteredCount is computed (pick the mutator to match)
    - components/StudySession.tsx — grade-button markup (mastery-language grade bar) for driving the UI grade flow, and the existing visibilitychange listener (~lines 287-308) that reads document.visibilityState
    - components/ModeSelector.tsx — how to start a flashcard session from select-mode
    - lib/study-cards.ts — the due predicate (nextReview <= now) used to compute expected values
    - .planning/phases/24-freshness-diagnosis-spike/24-RESEARCH.md — Patterns 3-4, Pitfalls 1-2 and 5, Architecture diagram step 4 (per-cell flow)
    - .planning/phases/24-freshness-diagnosis-spike/24-CONTEXT.md — D-04 through D-07, D-10, and the Specific Ideas section (evidence must be E2E-assertion-ready)
  </read_first>
  <action>
    Extend the script's default (no-flag) run: after the smoke check, execute the full matrix of 4 routes (`/`, `/cards`, `/study`, `/habits`) x 4 navigation paths (D-04), classifying each cell per D-10.

    Per-route observable + mutator pairs (all mutations via the script's own Prisma client against the same file: DB the server reads; expected fresh values are ALWAYS computed at classification time via a live Prisma query, never hardcoded, so accumulated mutations across cells stay harmless):
    - `/` — reader: Home hero due-count text; mutator: flip one CardReview.nextReview between past and future (changes dueCards)
    - `/study` — reader: select-mode due-count; mutator: same nextReview flip
    - `/cards` — reader: rendered card count/list text (anchor chosen from the CardsClient JSX read above); mutator: prisma.card.create a new card with a unique normalizedFront (counter-suffixed Hangul front) plus one blank-safe sentence
    - `/habits` — reader: the server-prop-derived number confirmed in read_first (masteredCount or equivalent); mutator: promote one CardReview to the state value that increments that number (verify the computation in app/habits/page.tsx first)

    Four path drivers; one cell = (route, path). Every cell runs in a FRESH browser context with the auth cookie re-injected — a shared context would let cell N's Router Cache entries pollute cell N+1. Every cell: snapshot the request-log length immediately before the triggering action and count only entries appended after the snapshot (prefetch-pollution guard, Pattern 3 gotcha). After the action, wait for network settle before reading the DOM. Log the matched element's full text before and after (Pitfall 5).

    1. plain-link: start on a different route, apply the mutator, click the real Nav Link to the target route, classify.
    2. back-forward: goto target (record observable), Link-nav away, apply the mutator, page.goBack(), classify against the post-mutation expected value.
    3. resume: goto target, apply the mutator, then simulate backgrounding + resume via a `simulateResume(page, hidden)` helper (D-07): page.evaluate that uses Object.defineProperty to override BOTH document.hidden AND document.visibilityState (configurable true) to the hidden state, dispatches visibilitychange, waits ~100ms, then overrides both back to visible and dispatches again. Overriding both properties is mandatory — StudySession.tsx reads visibilityState, not hidden (RESEARCH Pitfall 2); overriding only one silently produces a false "resume does nothing" reading. Classify.
    4. post-mutation same-route return — PRIMARY scenario for `/study` and `/` (D-05, the exact prior-regression case): from /study select-mode, start a flashcard session through the real UI (ModeSelector), reveal and grade every seeded due card via the actual grade buttons (always the highest grade so the queue drains without requeue and the session completes), reach the completion screen, Link-nav to Home (classify the `/` cell: does the hero due-count show the post-grading value?), then Link-nav back to /study (classify the `/study` cell: does select-mode show the refreshed due count?). For `/cards` and `/habits`, use the cheap DB-level variant (D-06): visit the route, Link-nav away, apply the route's mutator (simulating a sync/edit), Link-nav back, classify — and tag these two cells' evidence as DB-level mutation, not UI-driven, so the diagnosis stays honest about method. The UI-driven secondary scenarios (card edit/delete sheet, review undo) stay deferred per CONTEXT's Deferred Ideas — do not build them.

    Classification per D-10, exactly three verdict tokens: no new data fetch (neither RSC nor document) for the target pathname → `Stale-RouterCache`; data fetch observed but the DOM observable does not equal the live-computed expected value → `Stale-ClientShell`; data fetch observed and the DOM shows the expected value → `Fresh`. If the DOM is correct without any observed fetch, classify `Fresh` with an evidence note explaining why (e.g. full-document load). Record per cell: verdict, new-request URLs + types, DOM text before/after, expected value, timestamps.

    Wrap each cell in try/catch so one failure logs a cross-mark and continues (PATTERNS item 5), with [i/16] progress markers. End with the banner summary: a 16-row console table of route | path | verdict | root cause.

    Then run the full diagnosis, teeing output to `scripts/.tmp/24-diagnosis-run.log`. Do NOT commit the log file — it is raw material for Task 3. If the results contradict the staleTimes.dynamic=0 expectation for plain-link cells (RESEARCH Pitfall 1 warning sign: every cell reading Stale-RouterCache, including plain Link), treat the instrumentation as broken and fix it before accepting results.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx tsx scripts/diagnose-freshness.mts 2>&1 | tee scripts/.tmp/24-diagnosis-run.log && test $(grep -cE "\[(1[0-6]|[1-9])/16\]" scripts/.tmp/24-diagnosis-run.log) -ge 16</automated>
  </verify>
  <acceptance_criteria>
    - The full run exits 0 and the log shows all 16 [i/16] cell markers (4 routes x 4 paths, D-04)
    - Every cell line in the final banner summary carries exactly one of the three verdict tokens: Fresh, Stale-RouterCache, Stale-ClientShell — no cell is ambiguous or skipped (D-10)
    - The /study and / post-mutation cells were driven through the real UI grade flow to session completion (D-05) — the log shows grade-button interactions and the completion screen reached
    - The resume cells' log confirms both document.hidden and document.visibilityState were overridden before each visibilitychange dispatch (D-07)
    - Each cell's evidence includes: new-request URLs since the pre-action snapshot, DOM observable text before/after, and the live-computed expected value
    - Not all plain-link cells classify as Stale-RouterCache (Pitfall 1 instrumentation sanity holds)
    - scripts/.tmp/24-diagnosis-run.log is NOT committed (raw material only)
  </acceptance_criteria>
  <done>A complete evidence run exists covering all 16 cells, each with a binary-attributed verdict backed by network + DOM evidence, captured in scripts/.tmp/24-diagnosis-run.log ready to be authored into the diagnosis document</done>
</task>

<task type="auto">
  <name>Task 3: Author 24-DIAGNOSIS.md — matrix, confirmed-fresh list, scenario blocks, handoff notes</name>
  <files>.planning/phases/24-freshness-diagnosis-spike/24-DIAGNOSIS.md</files>
  <read_first>
    - scripts/.tmp/24-diagnosis-run.log — the raw evidence run from Task 2
    - .planning/phases/24-freshness-diagnosis-spike/24-CONTEXT.md — D-08/D-09/D-10 (document format contract) and the Specific Ideas section (evidence granularity examples)
    - .planning/ROADMAP.md — Phase 24 Success Criteria 1-4 (the document must satisfy all four) and the Phase 25/26 goals the document feeds
  </read_first>
  <action>
    Write `.planning/phases/24-freshness-diagnosis-spike/24-DIAGNOSIS.md` from the Task 2 evidence log. Required structure (D-08):

    1. Header: phase, date, method summary — production build (`npm run build` + `next start`, never dev), isolated file: SQLite DB, script name, playwright version.
    2. `## RSC Request Signature (empirically confirmed)` — the observed signature from Plan 02 Task 1 (sample URL, distinguishing header/param, capture date). This section resolves RESEARCH Open Question #1 for Phase 25's spec authors.
    3. `## Summary Matrix` — a markdown table with exactly 16 data rows (4 routes x 4 paths, D-04): columns Route | Navigation path | Verdict | Root cause | Evidence pointer. Verdict uses exactly one of: Fresh, Stale-RouterCache, Stale-ClientShell. Root cause for stale rows is the binary attribution per D-10 ("Router Cache reuse — no RSC re-fetch observed" vs "client-shell state non-resync — RSC re-fetch observed, UI unchanged"); for Fresh rows use an em-dash. Evidence pointer names the scenario block or a one-line evidence note for fresh rows.
    4. `## Confirmed-Fresh Paths` — explicit list of every Fresh cell with one line of evidence each (D-09, ROADMAP Success Criterion 4 — this is what keeps the Phase 26 fix surgical).
    5. `## Stale-Path Scenario Blocks` — one `###` block per stale cell (D-08/D-10), each containing: numbered Steps to Reproduce (concrete enough for Phase 25's E2E-06 spec to encode directly — real selectors/actions, not prose gestures), Expected vs Actual (the live-computed expected value vs the observed DOM text), and Evidence (network log excerpt showing RSC fetch presence/absence + DOM before/after text). Note for the /cards and /habits post-mutation cells whether the mutation was DB-level (D-06 cheap variant).
    6. `## Notes for Phase 25/26` — which client shells exhibited non-resync vs which paths were Router Cache reuse; the D-06 secondary scenarios' deferral status (card edit/delete UI flow, review undo — untested, expected to follow the same shell pattern); any instrumentation caveats (e.g. resume simulation is a visibilitychange proxy, not real PWA backgrounding, per CONTEXT Deferred Ideas).

    Copy evidence verbatim from the run log where possible — do not paraphrase network URLs or DOM text. Every stale row MUST have a scenario block; every row MUST have a verdict; zero rows may be ambiguous (ROADMAP Success Criterion 2).
  </action>
  <verify>
    <automated>test -f .planning/phases/24-freshness-diagnosis-spike/24-DIAGNOSIS.md && test $(grep -cE "\|\s*(Fresh|Stale-RouterCache|Stale-ClientShell)\s*\|" .planning/phases/24-freshness-diagnosis-spike/24-DIAGNOSIS.md) -ge 16 && grep -c "## Summary Matrix" .planning/phases/24-freshness-diagnosis-spike/24-DIAGNOSIS.md && grep -c "## Confirmed-Fresh Paths" .planning/phases/24-freshness-diagnosis-spike/24-DIAGNOSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - 24-DIAGNOSIS.md exists in the phase directory with the six sections listed in the action
    - The Summary Matrix contains exactly 16 data rows, one per route x path cell, each with a verdict token and (for stale rows) a binary root-cause attribution — the automated grep counts at least 16 verdict-bearing table rows
    - Every stale row has a corresponding scenario block with numbered steps, expected-vs-actual, and verbatim evidence
    - Every Fresh path is explicitly listed under Confirmed-Fresh Paths with evidence (ROADMAP Success Criterion 4, D-09)
    - The RSC signature section records the empirically observed signature with a sample
    - No row, block, or attribution uses hedged language (no "possibly", "unclear", "ambiguous") — attribution is binary per D-10
  </acceptance_criteria>
  <done>24-DIAGNOSIS.md satisfies all four ROADMAP Phase 24 success criteria: 16-cell coverage on a production build, binary root-cause attribution, reproducible spec-ready scenarios, and explicit confirmed-fresh paths — FRESH-01 complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → database | The script's Prisma client mutates data mid-run (mutators); it must remain pinned to the isolated file: DB |
| script → local prod server | Auth cookie derived from the throwaway secret; evidence logs must not carry real secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | diagnose-freshness.mts DB target (now with mutators that write mid-run) | high | mitigate | Plan 24-01's assertLocalDb() hard-fail guard remains in place and untouched by this plan's edits; all mutators go through the same env-pinned Prisma client — verified by the guard grep persisting in the script |
| T-24-02 | Information Disclosure | Evidence logs (unfiltered request headers in --log-requests mode include the ks_auth cookie value) | low | accept | The logged cookie is an HMAC of the throwaway literal secret, valid only against the local diagnosis server — worthless outside the run; the run log is never committed (Task 2 acceptance criteria) |
| T-24-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs in this plan (playwright landed in Plan 24-01 behind its blocking human checkpoint) |
</threat_model>

<verification>
- Full script run exits 0 with all 16 [i/16] markers and a 16-row banner summary; no cell ambiguous.
- 24-DIAGNOSIS.md passes the automated structure greps (16+ verdict rows, Summary Matrix and Confirmed-Fresh Paths sections present).
- Manual review (per 24-VALIDATION.md Manual-Only Verifications): read 24-DIAGNOSIS.md against ROADMAP Phase 24 Success Criteria 1-4 — every stale row cites concrete evidence distinguishing "no RSC fetch observed" from "RSC fetch fired but shell UI unchanged"; no attribution is ambiguous.
- Zero production code changes: `git diff --stat` for this plan touches only scripts/diagnose-freshness.mts and the phase directory — nothing under app/, components/, lib/, prisma/, middleware.ts.
</verification>

<success_criteria>
- FRESH-01 satisfied: per-route, per-path staleness diagnosis on a production build with binary root-cause attribution, captured as reproducible scenarios Phase 25's E2E-06 spec can encode directly.
- The RSC re-fetch signature is empirically documented (Open Question #1 closed) — a direct accelerant for Phase 25's spec assertions.
- Confirmed-fresh paths are explicitly listed so Phase 26's fix leaves working paths untouched.
</success_criteria>

<output>
Create `.planning/phases/24-freshness-diagnosis-spike/24-02-SUMMARY.md` when done. Include: the observed RSC signature, the 16-cell verdict summary (counts of Fresh / Stale-RouterCache / Stale-ClientShell), which shells exhibited non-resync, and whether the throwaway script was kept (recommended — it accelerates Phase 25's harness setup) or discarded.
</output>
