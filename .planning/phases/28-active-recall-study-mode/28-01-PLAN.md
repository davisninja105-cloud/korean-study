---
phase: 28-active-recall-study-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ModeSelector.tsx
  - components/StudyClient.tsx
  - components/StudySession.tsx
  - components/FlashcardMode.tsx
  - components/MultipleChoiceMode.tsx
  - components/FillBlankMode.tsx
  - e2e/grade-flow.spec.ts
autonomous: true
requirements: [MODE-01, MODE-02, CLEANUP-01, CLEANUP-02, CLEANUP-04]
must_haves:
  truths:
    - "The /study mode-select sheet shows a single Passive/Active segmented toggle — no 3-mode grid, no Multiple Choice option, no standalone Fill-in-the-Blank option, no Exposure/Recall sub-toggle (MODE-01)"
    - "Passive is the selected segment when the sheet opens, before any user interaction (MODE-02)"
    - "Multiple Choice is fully removed: component file deleted, selector option gone, all MC session state gone from StudySession (CLEANUP-01)"
    - "Fill-in-the-Blank is retired as a standalone mode: component file deleted, sub-toggle gone, all fill session state gone (CLEANUP-02)"
    - "The Passive flow (reveal, grade, undo, requeue, audio, tap-to-gloss) works end-to-end — the updated e2e grade-flow spec passes (CLEANUP-04)"
    - "Survivors are intact: the 'fill-blank' practice-card TYPE in lib/generate-practice.ts, the lib/card-style.ts badge map, and lib/audit-checks.ts distractor checks are unchanged"
  artifacts:
    - path: "components/ModeSelector.tsx"
      provides: "Narrowed StudyMode union ('passive' | 'active'), Passive/Active segmented toggle, begin-session-btn launcher, AI checkbox unchanged"
      contains: "mode-passive"
    - path: "components/StudySession.tsx"
      provides: "Single unconditional FlashcardMode dispatch; zero MC/fill session state; mode-agnostic 1-4 keyboard grading; selectSentence called with literal false"
    - path: "components/FlashcardMode.tsx"
      provides: "Exposure-only front branches (recall arm deleted); sub-mode props removed"
    - path: "e2e/grade-flow.spec.ts"
      provides: "Explicit Passive-toggle session entry + MODE-02 default-state assertion"
      contains: "mode-passive"
  key_links:
    - from: "components/ModeSelector.tsx"
      to: "components/StudyClient.tsx"
      via: "onSelect(mode, includeAI) two-arg callback into handleModeSelect"
      pattern: "onSelect\\("
    - from: "components/StudyClient.tsx"
      to: "components/StudySession.tsx"
      via: "mode prop typed as narrowed StudyMode"
      pattern: "mode="
    - from: "components/StudySession.tsx"
      to: "lib/sentence-selection.ts"
      via: "selectSentence called with literal false for needsBlank (D-13)"
      pattern: "selectSentence\\("
---

<objective>
Retire Multiple Choice and standalone Fill-in-the-Blank, collapse the Exposure/Recall sub-toggle, and replace the /study 3-mode grid with a single Passive/Active segmented toggle (Passive default). Execute the removal exactly per D-14: narrow the `StudyMode` type FIRST and let `tsc` enumerate every stale reference across ModeSelector → StudyClient → StudySession → FlashcardMode in one atomic commit — no manual grep-deletion. Then update the e2e grade-flow spec to drive the new toggle explicitly.

Implements: MODE-01, MODE-02, CLEANUP-01, CLEANUP-02, CLEANUP-04 (grade-flow leg); decisions D-04 (checkbox stays, both modes), D-05 (literal "Passive"/"Active" labels), D-06 (single toggle, Passive default), D-11 (full MC/fill removal), D-12 (Passive unregressed), D-13 (needsBlank collapses to literal false), D-14 (type-narrow-first removal sequence), D-16 (FreshnessWatcher blocks byte-identical).

Purpose: Shrink the 837-line StudySession surface (~250 lines of dead MC/fill state) so Plan 28-02 extends a small, clean FlashcardMode with the Active production branch.

Output: Rewritten ModeSelector (toggle + launcher), subtracted StudyClient/StudySession/FlashcardMode, two deleted component files, updated grade-flow spec.

**Planner decisions recorded (settle at plan time, not execution):**
- **P-01 (launch mechanism):** The old grid fired `onSelect` on mode-card tap. A toggle selects — it must not launch. ModeSelector gains a primary accent button below the AI checkbox, label "Start session", `data-testid="begin-session-btn"`, which fires `onSelect(mode, includeAI)`. Accent fill is correct here: UI-SPEC reserves accent for direct-action buttons.
- **P-02 (Open Q3, heading copy):** The "Choose Study Mode" heading becomes "How do you want to study?" — warm question form matching the app's copy voice; the Sheet title "Study options" in StudyClient stays unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/28-active-recall-study-mode/28-CONTEXT.md
@.planning/phases/28-active-recall-study-mode/28-RESEARCH.md
@.planning/phases/28-active-recall-study-mode/28-PATTERNS.md
@.planning/phases/28-active-recall-study-mode/28-UI-SPEC.md
@components/ModeSelector.tsx
@components/StudyClient.tsx
@components/StudySession.tsx
@components/FlashcardMode.tsx
@e2e/grade-flow.spec.ts
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from current source (verified 2026-07-14). -->

**Current ModeSelector exports (components/ModeSelector.tsx:5-6) — the first edit target:**

```typescript
export type StudyMode = 'flashcard' | 'multiple-choice' | 'fill-blank'  // → narrow to 'passive' | 'active'
export type FlashcardSubMode = 'exposure' | 'recall'                    // → DELETE this export entirely
```

**New ModeSelector contract after rewrite:**

```typescript
export type StudyMode = 'passive' | 'active'

interface Props {
  cardCount: number
  onSelect: (mode: StudyMode, includeAI: boolean) => void   // third param GONE
}
// internal state: const [mode, setMode] = useState<StudyMode>('passive')  // MODE-02
// internal state: const [includeAI, setIncludeAI] = useState(false)      // unchanged
```

**Segmented-toggle styling (copy from the existing sub-toggle at ModeSelector.tsx:42-65, scaled per UI-SPEC §Component Notes 1):**
- Shell: `flex gap-2 bg-surface-3 rounded-lg p-1` inside the existing `w-full max-w-sm` column
- Segment: `flex-1 py-3 px-4 rounded-md transition-colors`; selected `bg-surface-1 text-foreground shadow-sm`; unselected `text-muted hover:text-muted-foreground`
- Segment content: label 14px/600 ("Passive" / "Active", literal per D-05) + descriptor 12px/400 beneath ("recognize & review" / "produce from English") — mirrors the existing two-line segment pattern
- Segment attrs: `data-testid="mode-passive"` / `data-testid="mode-active"`, `aria-pressed={mode === value}`

**D-14 dead-symbol inventory (tsc will enumerate these in StudySession/StudyClient/FlashcardMode after the narrowing; listed here so the executor can confirm completeness — do NOT grep-delete ahead of tsc):**
`MC_ADVANCE_MS` · `normalizeAnswer` · `seededShuffle` · the `seed` memo · `fillInput` / `mcSelected` state · `advanceTimer` · `mcOptions` · `mcRating` · `handleMcSelect` · `advanceMc` · `chosenMatch` · `recallBlanked` · fill derivations (`useChosenForFill`/`fillSentence`/`fillTranslation`/`fillAnswer`/`fillCorrect`) · `FlashcardSubMode` (type + all threading in StudyClient/StudySession/FlashcardMode) · imports that die: `hashStr` (StudySession only), `sentenceMatch`/`blankSentence` from `lib/sentence-match` (StudySession only), `MultipleChoiceMode`, `FillBlankMode` · the `distractors` field on StudySession's LOCAL `Card` interface (read only by the deleted options builder; `CardDTO.distractors` stays — Phase 29 owns the DTO)
<!-- planner-discipline-allow: distractors -->

**StudySession seams that change (cite symbols, not line numbers — they shift during deletion):**
- `Props.mode: StudyMode` stays (narrowed type); `Props.flashcardSubMode` deleted
- The `chosenIdx` useMemo: `needsBlank` was mode-derived — after narrowing it collapses; pass literal `false` as `selectSentence`'s 4th arg and drop the dead dep from the memo array (D-13). Keep `selectSentence`'s lib parameter and its lib tests — accepted debt this milestone.
- `showBareFront`: drop the sub-mode clause; keep `isNewCard && cardSentences.length > 0 && (chosenSentence?.unknownCount ?? 0) > 0` shape
- Keyboard handler: the `if (mode === 'flashcard')` guard on 1–4 grading becomes unconditional (both Passive and Active grade via 1–4); MC/fill key branches delete
- Mode-dispatch ternary (the three-way render at the bottom) collapses to a single unconditional `<FlashcardMode .../>`
- `submitReview` advance block + `handleUndo`: the two deleted reset calls (fill/MC state) simply disappear — do not add replacements in this plan (Plan 28-02 adds the hint reset there)

**StudyClient (mechanical, tsc-driven):** drop the sub-mode import/state/threading; `handleModeSelect(selectedMode, includeAI)` loses its third param. **D-16 HARD CONSTRAINT:** the `prevInitialCards` and `prevFreshStudy` FreshnessWatcher prop-adoption blocks must remain byte-identical — they gate on `phase === 'select-mode'`, never on mode.

**FlashcardMode:** delete the recall front-face arm and its props; keep `frontRef`/`backRef`/`againBtnRef` threading, the inner `key={cursor}` div, testids (`reveal-btn`, `grade-again/hard/good/easy`, `card-front-word`), and the sticky action bar untouched. Update the header prose (it names the old sub-modes); keep the "owns NO session state / only useWordTap()" contract line.

**grade-flow spec seam (e2e/grade-flow.spec.ts:39-40):** the session-entry tap uses the old flashcard-grid testid; everything else (bounded loop, DB backstop poll) is mode-agnostic and stays.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Type-narrow StudyMode, delete MC/fill/sub-toggle, rewrite ModeSelector as the Passive/Active toggle</name>
  <files>components/ModeSelector.tsx, components/StudyClient.tsx, components/StudySession.tsx, components/FlashcardMode.tsx, components/MultipleChoiceMode.tsx, components/FillBlankMode.tsx</files>
  <read_first>
    - .planning/phases/28-active-recall-study-mode/28-PATTERNS.md (§ModeSelector, §StudySession Patterns A–E, §FlashcardMode, §StudyClient — code excerpts for every seam)
    - .planning/phases/28-active-recall-study-mode/28-RESEARCH.md (§Pattern 1, §Verified Code Claims & Drift — the removal inventory)
    - .planning/phases/28-active-recall-study-mode/28-UI-SPEC.md (§Component Notes 1, §Copywriting Contract, §Color — toggle styling contract)
    - components/ModeSelector.tsx, components/StudySession.tsx, components/StudyClient.tsx, components/FlashcardMode.tsx (current source)
  </read_first>
  <action>
    Single atomic commit per D-14 — the type change and its full ripple travel together (this is a locked user decision; the 6-file span is the constraint, not a sizing choice).

    1. In components/ModeSelector.tsx: narrow the StudyMode union to 'passive' | 'active' and delete the sub-mode type export (see interfaces block). Rewrite the component body: heading "How do you want to study?" (P-02), keep the "{cardCount} cards due for review" line, replace the 3-mode grid with the segmented toggle per the interfaces block (literal "Passive"/"Active" labels per D-05, descriptors "recognize & review" / "produce from English", testids mode-passive/mode-active, aria-pressed reflects selection, default useState('passive') per D-06/MODE-02 — no unselected flash). Keep the "Include AI-generated practice questions" checkbox byte-identical below the toggle (D-04 — available in both modes, no conditional hiding). Add the launcher per P-01: full-width primary button below the checkbox, `bg-button text-button-foreground`, min-h-11, label "Start session", data-testid="begin-session-btn", onClick fires onSelect(mode, includeAI). Delete the modes array and the old grid markup entirely.
    2. Run `npx tsc --noEmit` and fix EVERY reported error site — this is the enumeration pass. Do not pre-emptively grep for symbols; let the compiler drive. Expected error sites: StudyClient (sub-mode import/state/threading, handleModeSelect third param), StudySession (Props, dead state/memos/handlers/keyboard branches, mode-dispatch ternary), FlashcardMode (sub-mode props and the recall front arm).
    3. Delete components/MultipleChoiceMode.tsx and components/FillBlankMode.tsx (git rm), plus their imports and the two dead dispatch arms in StudySession — the dispatch collapses to one unconditional FlashcardMode render.
    4. In StudySession: apply the interfaces-block seam changes — literal false into selectSentence with dep-array cleanup (D-13), sub-mode clause dropped from showBareFront, keyboard 1–4 grading made unconditional, local Card interface loses its legacy MC-only column field, dead imports removed. Iterate tsc until clean.
    5. In StudyClient: mechanical tsc-driven cleanup only. Leave the two FreshnessWatcher prop-adoption blocks byte-identical (D-16) — verify with git diff that neither block changed.
    6. In FlashcardMode: delete the recall arm + its props; update the header comment prose to describe Passive-only faces (Plan 28-02 will extend it); do not touch refs threading, key={cursor}, the action bar, or any existing testid.
    7. Run `npm run lint` and `npm test`; fix any violations (project hard rule: lint clean).

    Survivor warning (Pitfall 6): the string 'fill-blank' also names the AI practice-card TYPE in lib/generate-practice.ts and a badge entry in lib/card-style.ts, and lib/audit-checks.ts owns distractor auditing — none of these files may be touched by this task.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` and `npm run lint` exit 0; `npm test` green (survivor lib tests unchanged and passing)
    - components/MultipleChoiceMode.tsx and components/FillBlankMode.tsx no longer exist
    - Dead-symbol grep over components/ returns zero hits for: mcOptions, seededShuffle, MC_ADVANCE_MS, mcSelected, fillInput, normalizeAnswer, advanceTimer, FlashcardSubMode, recallBlanked
    - No component renders the retired testids (mode-flashcard, mode-multiple-choice, mode-fill-blank absent from components/)
    - components/StudySession.tsx no longer contains the string "distractors" (local Card interface field dropped; lib/audit-checks.ts remains the sole reader of the legacy column — threat T-28-01)
    - lib/generate-practice.ts still contains the 'fill-blank' practice type; lib/audit-checks.ts still contains its distractor checks; lib/card-style.ts untouched
    - git diff shows zero changes inside StudyClient's prevInitialCards and prevFreshStudy blocks (D-16)
    - ModeSelector renders mode-passive with aria-pressed="true" on mount (MODE-02) and begin-session-btn fires onSelect(mode, includeAI)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test && test ! -f components/MultipleChoiceMode.tsx && test ! -f components/FillBlankMode.tsx && ! grep -rqE 'mcOptions|seededShuffle|MC_ADVANCE_MS|mcSelected|fillInput|normalizeAnswer|advanceTimer|FlashcardSubMode|recallBlanked' components/ && ! grep -rqE 'mode-flashcard|mode-multiple-choice|mode-fill-blank' components/ && ! grep -q 'distractors' components/StudySession.tsx && grep -q "fill-blank" lib/generate-practice.ts && grep -q 'distractor' lib/audit-checks.ts && grep -q 'mode-passive' components/ModeSelector.tsx && grep -q 'begin-session-btn' components/ModeSelector.tsx</automated>
  </verify>
  <done>StudyMode is 'passive' | 'active' everywhere; MC and standalone fill-blank are fully gone (files, state, dispatch, keyboard branches); the mode-select sheet renders the Passive/Active toggle with Passive selected by default, AI checkbox and a Start session launcher; tsc/lint/unit all green; survivors and FreshnessWatcher blocks untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update e2e grade-flow spec to drive the toggle explicitly and assert the Passive default</name>
  <files>e2e/grade-flow.spec.ts</files>
  <read_first>
    - e2e/grade-flow.spec.ts (current source — bounded-loop and DB-backstop patterns stay)
    - .planning/phases/28-active-recall-study-mode/28-PATTERNS.md (§e2e/grade-flow.spec.ts)
    - .planning/phases/28-active-recall-study-mode/28-VALIDATION.md (MODE-01/MODE-02/CLEANUP-04 rows)
  </read_first>
  <action>
    Update the session-entry sequence only; the bounded loop-until-complete, content-anchored card-front-word assertions, and the seededDueReviewsPersisted DB backstop poll stay unchanged (CLEANUP-04 / D-12).

    New entry sequence after clicking start-studying-btn:
    1. Assert MODE-02: the mode-passive segment has aria-pressed="true" BEFORE any interaction (Passive is the default).
    2. Assert MODE-01: the two retired mode testids resolve to zero elements on the sheet (use toHaveCount(0) — the literal ids are listed in acceptance criteria; this asserts DOM absence, which is stronger than a source grep).
    3. Click mode-passive explicitly (deliberate mode pick per CONTEXT integration note — do not rely on the default for session entry), then click begin-session-btn to start the session.

    Seed cards are state-1, so the Passive session faces are unchanged from before — no other locator changes should be needed. Run the spec and fix any drift the entry change surfaces.
  </action>
  <acceptance_criteria>
    - Spec asserts aria-pressed="true" on mode-passive before any tap (MODE-02)
    - Spec asserts getByTestId('mode-multiple-choice') and getByTestId('mode-fill-blank') each have count 0 (MODE-01)
    - Session entry uses mode-passive + begin-session-btn; no reference to the retired flashcard-grid testid as an interaction target
    - `npx playwright test e2e/grade-flow.spec.ts` passes (CLEANUP-04)
    - e2e/fixture.ts and e2e/seed.ts are NOT modified
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/grade-flow.spec.ts && git diff --name-only HEAD -- e2e/fixture.ts e2e/seed.ts | wc -l | grep -q '^0$'</automated>
  </verify>
  <done>grade-flow spec drives the new toggle explicitly, proves the Passive default, proves the retired modes are gone from the DOM, and the full Passive grade/undo/requeue flow passes green against the rewritten selector.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Unchanged — this plan adds no new routes, inputs, env vars, or schema changes; middleware auth gate untouched |
| DB legacy column → render | Legacy `Card.distractors` JSON was parsed client-side only by the deleted MC options builder |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering/DoS | components/StudySession.tsx (legacy distractors JSON reader) | low | mitigate | Delete the MC-only reader AND the field from StudySession's local Card interface; verify via Task 1 grep that components/ no longer references the column; lib/audit-checks.ts's hardened parser remains the sole reader (ASVS V5, per RESEARCH §Security Domain) |
| T-28-SC | Tampering | npm installs | low | accept | Zero new packages this phase (RESEARCH §Package Legitimacy Audit: none installed; no [ASSUMED]/[SUS]/[SLOP] entries) |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint` clean (project hard rule)
- `npm test` green — survivor lib tests (card-key, audit-checks, sentence-selection, sequence) unaffected
- `npx playwright test e2e/grade-flow.spec.ts` green — Passive flow unregressed (CLEANUP-04 leg)
- Dead-symbol and retired-testid greps over components/ return zero hits; deleted files absent
- Intermediate-state note: after this plan, selecting "Active" starts a session that still renders Passive faces — expected until Plan 28-02 lands; the phase cannot close in this state
</verification>

<success_criteria>
- ROADMAP success criterion 1 satisfied: one Passive/Active toggle on /study, Passive default, no grid/MC/fill/sub-toggle (MODE-01, MODE-02)
- CLEANUP-01 and CLEANUP-02 fully satisfied per the D-11 inventory
- CLEANUP-04 grade-flow leg green with explicit Passive interaction
- StudySession/FlashcardMode surface is clean and narrowed, ready for the Active branch in Plan 28-02
</success_criteria>

## Artifacts this plan produces

| Artifact | Kind | Notes |
|----------|------|-------|
| `StudyMode = 'passive' \| 'active'` | narrowed exported type | components/ModeSelector.tsx; sub-mode type export deleted |
| `onSelect(mode: StudyMode, includeAI: boolean)` | changed callback signature | ModeSelector Props → StudyClient.handleModeSelect |
| `data-testid="mode-passive"` / `data-testid="mode-active"` | new testids | toggle segments, with aria-pressed state |
| `data-testid="begin-session-btn"` | new testid | session launcher button (P-01) |
| "How do you want to study?" | heading copy | P-02 (Open Q3 settled) |
| Deleted: `components/MultipleChoiceMode.tsx`, `components/FillBlankMode.tsx` | file deletions | plus all D-14 inventory symbols |
| Single `<FlashcardMode/>` dispatch | structural change | StudySession mode ternary collapsed |
| Updated `e2e/grade-flow.spec.ts` | test update | Passive-explicit entry + MODE-02 assertion |

<output>
Create `.planning/phases/28-active-recall-study-mode/28-01-SUMMARY.md` when done.
</output>
