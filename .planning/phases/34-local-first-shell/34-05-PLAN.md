---
phase: 34-local-first-shell
plan: 05
type: execute
wave: 3
depends_on: ["34-02", "34-03", "34-04"]
files_modified:
  - components/FreshnessWatcher.tsx
  - e2e/pull-to-refresh.spec.ts
  - e2e/local-cache-offline.spec.ts
autonomous: false
requirements: [LOCAL-01, LOCAL-04, LOCAL-05]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`components/FreshnessWatcher.tsx` still exists, is still mounted in the root layout, still fires `router.refresh()` unconditionally on `visibilitychange`/`popstate`/`pageshow` behind the 300ms coalesce, and still carries the doc comment naming Next.js 16.2.1 as the version the Suspense/Segment-Cache delivery flake was last verified against (D-00b, ROADMAP SC4 carry-forward)."
    - "The JSON half of the dual delivery is gone: no per-route payload fetch, no payload context, and no payload-consuming hook remain anywhere in `components/`, `app/`, or `lib/` — the four `*Client.tsx` shells' own cache-read plus version-checked revalidation is now the single Suspense-independent delivery path (D-00 rule 3, RESEARCH Pitfall 1)."
    - "If this phase's cache did not let the backstop shrink, it was implemented wrong — the shrink is the acceptance criterion for D-00 rule 3, not a side effect."
    - "Pull-to-refresh exists on all four of Home, Study, Cards, and Habits, and on every one of them it fires a network request unconditionally — no cache read and no version comparison gates it (LOCAL-04, D-03, ROADMAP SC5)."
    - "Home's gesture reads `Pull to sync` / `Release to sync` / `Syncing…` and triggers `POST /api/sync`; Study, Cards, and Habits read `Pull to refresh` / `Release to refresh` / `Refreshing…` and trigger no doc sync. The two read as different actions (D-04, UI-SPEC E7 loading)."
    - "Home's gesture failure shows `Sync failed — try again from Settings`; the three route-local gestures show `Couldn't refresh. Check your connection and try again.` with a `Try again` retry link. The same D-04 distinction extends to the failure case (UI-SPEC E7 error)."
    - "With the browser context fully offline, opening the app shows last-known Home stats, the Cards list, and Habits data rather than an error or a blank screen (LOCAL-05, ROADMAP SC2)."
    - "When a cache entry and RSC props are both available at mount, the cache wins for the instant paint and the RSC props are the cold-start fallback; a subsequently-delivered fresher RSC payload outranks the older cache entry and is written through to it. The two sources never merge into a partial hybrid record (LOCAL-05 adjacency edge)."
    - "Cached list order is preserved exactly as written: the `study` entry is consumed in server order (foundation-first sequencing computed by `getStudyCards()`, never re-sorted client-side), each cached `cards` group preserves its `loaded` array order, and `ActivityDTO.days` preserves its server order — a cache round trip is order-stable, including for elements that compare equal (LOCAL-05 ordering edge)."
    - "Opening the app offline runs the cache-read path only: `fetchCacheContext()` resolves `null`, no revalidation is attempted, no partial or blank entry is written, and no unhandled rejection reaches the console (LOCAL-05 concurrency edge)."
    - statement: "Every existing freshness e2e spec that asserted a JSON-backstop payload fetch still asserts a real, non-vacuous behaviour after the narrowing — either the surviving `router.refresh()` RSC delivery, or the client shell's own version-checked revalidation. Any spec that can only pass because the thing it guarded no longer exists is rewritten or deleted with a recorded reason, never left green-but-vacuous."
      verification: backstop
    - statement: "Phase 33's VERS-02 requirement (`the backstop itself is not removed`) is superseded, not violated: the component, its unconditional `router.refresh()` half, and its Next.js-bug documentation all survive; only the redundant JSON delivery half is retired, which is precisely what D-00 rule 3 requires and what 34-RESEARCH.md resolved as the discretion granted by D-00. Recorded in the plan SUMMARY for the milestone audit."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT delete `components/FreshnessWatcher.tsx`, its `router.refresh()` half, its coalesce logic, its three event listeners, or the doc comments recording the unfixed Next.js 16.2.1 Suspense/Segment-Cache delivery flake. It still guards a real bug and is still the sole delivery path for `/` (D-00b, STATE.md Blockers)."
      category: safety
    - statement: "MUST NOT place `router.refresh()` behind any version check, cache check, or conditional. It is unconditional today and stays unconditional (Phase 33 VERS-02 carry-forward, ROADMAP SC4)."
      category: safety
    - statement: "MUST NOT leave the freshness e2e suite green-but-vacuous. A spec that no longer exercises a real delivery path must be rewritten against the surviving mechanism or deleted with the reason recorded — never left passing because its subject was removed."
      category: transparency
    - statement: "MUST NOT ship this phase with both the new IndexedDB cache and the old per-route JSON backstop live. Two independent fetch-and-adopt mechanisms racing on every boundary event is the exact failure mode RESEARCH Pitfall 1 names, and it violates D-00 rule 3's `replace layers, don't add one`."
      category: architecture
    - statement: "MUST NOT introduce a service worker, a precached app shell, or an offline review queue in this phase. Those are Phase 35 (OFFLINE-01/02/03); this phase guarantees viewing last-known data offline, not completing a graded session offline."
      category: scope
  artifacts:
    - path: "components/FreshnessWatcher.tsx"
      provides: "Narrowed boundary watcher — unconditional router.refresh() on visibilitychange/popstate/pageshow behind a 300ms coalesce, with the JSON payload backstop retired"
      contains: "router.refresh"
    - path: "e2e/pull-to-refresh.spec.ts"
      provides: "Playwright proof that all four routes have a working escape hatch that bypasses cache and version check, with Home's sync semantics distinct from the three route-local refreshes"
    - path: "e2e/local-cache-offline.spec.ts"
      provides: "Playwright proof via context.setOffline that Home, Cards, and Habits render last-known data with no network, and that the Offline pill appears"
  key_links:
    - from: "app/layout.tsx"
      to: "components/FreshnessWatcher.tsx"
      via: "root-layout mount, unchanged by this plan"
      pattern: "FreshnessWatcher"
    - from: "components/FreshnessWatcher.tsx"
      to: "next/navigation"
      via: "router.refresh() — the surviving, unconditional RSC delivery half"
      pattern: "router.refresh"
    - from: "e2e/local-cache-offline.spec.ts"
      to: "components/Nav.tsx"
      via: "asserts the Offline pill rendered by plan 34-04 while context.setOffline(true)"
      pattern: "Offline"
---

<objective>
Close the phase by making the replacement real. Now that all four `*Client.tsx` shells own a
Suspense-independent cache read plus version-checked revalidation, `FreshnessWatcher`'s JSON
payload backstop is provably redundant — retire it, keep the `router.refresh()` half that
still guards a real unfixed Next.js bug, and prove the phase's two cross-route guarantees
(the escape hatch on all four routes, and rendering last-known data with no network).

Purpose: D-00 rule 3 is the phase's own stated acceptance criterion — "if this phase's cache
doesn't let the backstop shrink, it has been implemented wrong." Shipping the cache while
leaving the backstop intact would add a layer instead of replacing one, leaving two
independent fetch-and-adopt mechanisms racing on every boundary event.

Output: a narrowed `components/FreshnessWatcher.tsx`, `e2e/pull-to-refresh.spec.ts`
covering all four routes, and `e2e/local-cache-offline.spec.ts` — this repo's first use of
`context.setOffline`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-local-first-shell/34-CONTEXT.md
@.planning/phases/34-local-first-shell/34-RESEARCH.md
@.planning/phases/34-local-first-shell/34-PATTERNS.md
@.planning/phases/34-local-first-shell/34-UI-SPEC.md
@.planning/phases/34-local-first-shell/34-02-SUMMARY.md
@.planning/phases/34-local-first-shell/34-03-SUMMARY.md
@.planning/phases/34-local-first-shell/34-04-SUMMARY.md
</context>

<notes>
**This plan is NOT autonomous.** Task 1 is a blocking `checkpoint:decision` covering a
one-way change (`REVERSIBILITY_GATES` is on and 34-CONTEXT.md rates D-00 rule 3's
relationship to `FreshnessWatcher` as one-way). Execution stops there until the developer
selects an option.

**Wave 3 — do not start before plans 34-02, 34-03, and 34-04 have landed.** All three
remove their own consumption of the payload hook; deleting the export before they do breaks
the build.

**Schema Push Detection Gate** — no `prisma/schema.prisma` change in scope (D-00c).
**Package Legitimacy Gate** — no packages installed by this plan.
</notes>

<flagged_assumptions>
No `SPEC.md` exists for this phase; the deterministic edge probe returned all nine rows
`unresolved`. This plan owns the remaining four.

1. **LOCAL-04 / `unclassified` — `unresolved`, NOT auto-resolved.** The probe engine could
   not classify LOCAL-04's edge category at all, so per the spec-less fallback protocol an
   `unclassified` row stays `unresolved` and is surfaced rather than force-resolved. This
   planner's own reading identifies the live edge as **what "bypasses the cache and version
   check entirely" means when there is no network at all** — a pull gesture offline cannot
   repopulate from the server, and the phase deliberately ships no queued-retry mechanism
   (that is Phase 35). The chosen behaviour is: the gesture fires, the fetch fails, the
   route-local failure copy with its `Try again` link appears, and the cached content stays
   on screen. That behaviour is authored into `e2e/pull-to-refresh.spec.ts` in Task 3. **Any
   OTHER unclassified edge for LOCAL-04 remains genuinely uncovered by this phase and should
   be raised at `/gsd-verify-work` rather than assumed handled.**

2. **LOCAL-05 / `adjacency` — resolved (explicit).** "When two things are exactly equal or
   just touch, do they merge, collide, or separate?" Resolved as: cache and RSC props never
   merge into a hybrid record — the cache wins the mount paint, a later fresher RSC delivery
   outranks it and is written through. Authored as an explicit truth above.

3. **LOCAL-05 / `ordering` — resolved (explicit).** "When elements compare equal, is output
   order specified and stable?" Resolved as: a cache round trip is order-preserving by
   construction (the entry stores the array as received), and the client never re-sorts —
   `/study`'s foundation-first order is computed server-side inside `getStudyCards()`.
   Authored as an explicit truth above.

4. **LOCAL-05 / `empty` — resolved in plan 34-01** (a miss, a reject, and an
   IndexedDB-unavailable environment all resolve to `undefined` and fall back to RSC props).
   Not re-litigated here; cross-referenced so it is not double-counted as a drop.

**UI-SPEC lift.** E7 `loading` and E7 `error` (both ✅ covered) are authored as explicit
truths above. E7 `long-text` is ❌ dismissed in `34-UI-SPEC.md` (all pull-to-refresh copy is
short, fixed, non-data-driven English chrome). The UI-SPEC bucket-tally discrepancy is
flagged in plan 34-04's `<flagged_assumptions>` and is not re-flagged here.

**Prohibition recall.** Stage 1 over-produced ~10 candidates for the narrowing and the
offline surface. Stage 2 dropped routine-engineering items (coalesce-window tuning,
`pageshow`/`persisted` handling, offline-render performance) and two canon items with
breadcrumbs: `middleware.ts`'s `/api/*` auth gating is canon; generic offline-UX guidance is
canon and not a project prohibition. Five bespoke prohibitions survived and are authored
descriptor-less above.
</flagged_assumptions>

<artifacts_this_phase_produces>
| Artifact | Kind | Location |
|---|---|---|
| `e2e/pull-to-refresh.spec.ts` | new file | Playwright spec |
| `e2e/local-cache-offline.spec.ts` | new file | Playwright spec |
| (removals) payload context, payload hook, per-route payload fetch, payload types | deleted exports/internals | `components/FreshnessWatcher.tsx` |
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the one-way FreshnessWatcher narrowing scope</name>
  <decision>How far to shrink `components/FreshnessWatcher.tsx`'s JSON backstop now that all four client shells own their own cache-read and version-checked revalidation.</decision>
  <context>
    34-CONTEXT.md D-00 rates this change **one-way**: narrowing the backstop touches the
    route-payload fetch logic that works around a real, unfixed Next.js 16.2.1
    Suspense/Segment-Cache bug, and getting the sequencing wrong risks resurrecting a flake
    that took a full root-cause investigation (Plan 26-03) to diagnose. D-00b is explicit that
    `FreshnessWatcher` itself must not be deleted.

    It also interacts with a shipped requirement. Phase 33's VERS-02 reads "the backstop
    itself is not removed (works around a real Next 16.2.1 flake)". Option A retires the JSON
    delivery half while keeping the component, its unconditional `router.refresh()`, its
    coalesce logic, its three listeners, and its Next.js-bug documentation — which
    34-RESEARCH.md concluded is exactly what D-00 rule 3 requires, and which D-00's own
    "Claude's Discretion" list assigned to research to resolve against the real
    implementation. That conclusion is code-grounded: the backstop exists ONLY as a second,
    Suspense-independent delivery of the same per-route JSON, and each shell's own mount-time
    and revalidation `fetch` now IS a second, Suspense-independent delivery.

    Whichever option is chosen, `router.refresh()` stays unconditional and the component
    stays mounted.
  </context>
  <options>
    <option id="option-a">
      <name>Full narrowing — retire the JSON half (34-RESEARCH.md's recommendation)</name>
      <pros>Satisfies D-00 rule 3's stated acceptance criterion exactly. Removes the dual fetch-and-adopt race RESEARCH Pitfall 1 warns about. Deletes ~120 lines of now-dead machinery plus three gated-adoption blocks already removed in plans 34-02/03/04. One mechanism, one place to reason about freshness.</pros>
      <cons>One-way. Retires the mechanism VERS-02's requirement text describes, so the milestone audit must record the supersession. If the client-shell revalidation turns out to have a gap the backstop was silently covering, recovery means re-implementing it rather than flipping a flag.</cons>
    </option>
    <option id="option-b">
      <name>Partial narrowing — keep the version-gated backstop for `/study` only</name>
      <pros>Hedges against an unknown gap on the route with the most complex adoption guards. Smaller diff.</pros>
      <cons>Leaves two adopt paths live on `/study`, which is the exact condition D-00 rule 3 and RESEARCH Pitfall 1 forbid. Keeps the whole context/hook/type surface alive for one branch, so almost none of the simplification lands. Half-measures here are harder to reason about than either extreme.</cons>
    </option>
    <option id="option-c">
      <name>Defer the narrowing to a follow-up phase</name>
      <pros>Zero risk to the Next.js flake this release. The cache ships on its own.</pros>
      <cons>Directly violates D-00 rule 3 ("if this phase's cache doesn't let the backstop shrink, it has been implemented wrong") and RESEARCH Pitfall 1 ("execute the narrowing in the SAME plan/wave, not as a follow-up"). Ships the phase with two independent fetch-and-adopt mechanisms racing on every boundary event — strictly more surface area than before the phase started.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Narrow FreshnessWatcher to its router.refresh() half</name>
  <reversibility rating="one-way">Retiring the JSON backstop removes the workaround for an
  unfixed Next.js 16.2.1 Suspense/Segment-Cache delivery flake diagnosed in Plan 26-03.
  Recovery is re-implementation against a bug whose reproduction is intermittent, not a
  config flip — hence the blocking decision checkpoint in Task 1.</reversibility>
  <files>components/FreshnessWatcher.tsx</files>
  <read_first>
    - `components/FreshnessWatcher.tsx` — full file (278 lines), read in this order:
      lines 1-53 (imports, `COALESCE_MS = 300`, `HabitsFreshPayload`, `FreshPayloads`,
      `EMPTY_PAYLOADS`, the context, and the hook),
      lines 55-121 (`fetchRoutePayload` — the three per-route branches and their
      response-time pathname re-check),
      lines 123-166 (the component doc comment: the dual-delivery description, the Plan 26-03
      root-cause finding, the WHY-the-JSON-half-is-gated paragraph, and the Next.js 16.2.1
      TODO),
      lines 167-231 (the component body: `lastRefreshRef`, `lastVersionRef`, the `payloads`
      state, the mount-time baseline seed, and `fetchBackstop`'s version gate),
      lines 232-278 (`refresh`, the three event handlers, the listener registration/cleanup,
      and the provider wrapper).
    - `app/layout.tsx` — the `FreshnessWatcher` mount site, to confirm no prop or wrapper
      change is needed.
    - `.planning/phases/34-local-first-shell/34-02-SUMMARY.md`, `34-03-SUMMARY.md`,
      `34-04-SUMMARY.md` — confirm all three shells already removed their payload-hook
      consumption before deleting the export.
    - `.planning/phases/34-local-first-shell/34-RESEARCH.md` — §Summary's integration finding
      and Pitfall 1 (including its stated warning sign).
    - `.planning/phases/34-local-first-shell/34-PATTERNS.md` — the `FreshnessWatcher.tsx`
      section's explicit "what stays" / "what is removed" split.
    - `.planning/REQUIREMENTS.md` — the VERS-02 text, so the supersession is recorded
      accurately in the SUMMARY.
  </read_first>
  <action>
    Apply the option selected in Task 1. The instructions below describe **option-a**; if
    option-b or option-c was selected, follow that option's scope instead and record the
    deviation in the plan SUMMARY.

    <!-- planner-discipline-allow: useFreshPayload -->
    <!-- planner-discipline-allow: FreshPayloadContext -->
    <!-- planner-discipline-allow: fetchRoutePayload -->

    **Delete** from `components/FreshnessWatcher.tsx`:
    - The `fetchRoutePayload` module-internal function and all three of its per-route branches.
    - The `HabitsFreshPayload` and `FreshPayloads` interfaces and the `EMPTY_PAYLOADS` const.
    - The payload React context, its provider wrapper, and the exported consumer hook.
    - The `payloads` `useState` and its setter.
    - The `lastVersionRef` ref, the mount-time `/api/version` baseline-seed fetch, and the
      `fetchBackstop` function including its version gate — all of them existed solely to
      decide whether to call the now-deleted payload fetch. The version-check logic itself is
      not lost: it relocated into each `*Client.tsx`'s own revalidation check in plans
      34-01/02/03/04.
    - The `Dispatch`/`SetStateAction` type imports, the `createContext`/`useContext`/`useState`
      imports, and the `CardDTO`/`CardsPageDTO`/`ActivityDTO`/`StatsDTO` type import — every
      one becomes unused. Remove exactly the unused ones; do not remove `useEffect`, `useRef`,
      or `useRouter`.
    - The `fetchBackstop()` call inside `refresh()`.

    **Keep, byte-for-byte where possible:**
    - `COALESCE_MS = 300` and its comment.
    - `lastRefreshRef` and the `Date.now()`-inside-the-handler coalesce, including the
      `react-hooks/purity` comment explaining why the clock read is inside the closure.
    - The unconditional `router.refresh()`.
    - `onVisibilityChange` (including the `document.visibilityState` read that matches
      `e2e/helpers/resume.ts`'s override), `onPopState` (including the `setTimeout(refresh, 0)`
      macrotask deferral and its rationale comment), and `onPageShow` (including the
      `e.persisted` guard).
    - The listener registration and the cleanup returning all three removals.
    - The `[router]` dependency array.

    **Rewrite the component doc comment** rather than deleting it. It must still state: that
    the component is mounted once in the root layout; that `router.refresh()` is unconditional
    and is the sole delivery path for `/`, which has no `loading.tsx`; the Plan 26-03
    root-cause finding about Next.js 16.2.1 fetching a fresh RSC payload and never applying it
    on routes with a `loading.tsx`; and the TODO naming **Next.js 16.2.1** as the version this
    was last verified against, to be re-tested after any Next upgrade. Add a new paragraph
    recording that the JSON backstop half was retired in Phase 34 because each `*Client.tsx`
    now performs its own Suspense-independent cache read and version-checked revalidation —
    which is structurally the same second delivery path the backstop provided — and that
    Phase 33's VERS-02 is superseded, not violated, since the component and its RSC half
    survive.

    **Return** `<>{children}</>` instead of the provider wrapper. Do not change the component's
    name, its default export, its props signature, or its mount site in `app/layout.tsx`.

    Then reconcile the existing freshness e2e suite. Read each of
    `e2e/freshness-version-gate.spec.ts`, `e2e/freshness-fresh-paths.spec.ts`,
    `e2e/freshness-gate.spec.ts`, `e2e/freshness-router-cache.spec.ts`, and
    `e2e/freshness-client-shell.spec.ts`, and for every assertion that depended on the JSON
    backstop firing (or deliberately not firing), decide explicitly:
    - rewrite it against the surviving mechanism (`router.refresh()`'s RSC/document fetch, or
      the client shell's own revalidation request), OR
    - delete it and record the reason in the plan SUMMARY.
    A spec that now passes only because the thing it guarded no longer exists is
    green-but-vacuous and is forbidden by this plan's prohibitions — do not leave one.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npx playwright test e2e/freshness-version-gate.spec.ts e2e/freshness-fresh-paths.spec.ts e2e/freshness-gate.spec.ts e2e/freshness-router-cache.spec.ts e2e/freshness-client-shell.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "useFreshPayload\|FreshPayloadContext\|FreshPayloads\|HabitsFreshPayload\|fetchRoutePayload" components/ app/ lib/` returns zero lines (RESEARCH Pitfall 1's stated warning sign).
    - `grep -c "router.refresh" components/FreshnessWatcher.tsx` is at least 1 and the call sits inside `refresh()` with no surrounding conditional other than the coalesce guard (inspection).
    - `grep -c "16.2.1" components/FreshnessWatcher.tsx` is at least 1 — the version TODO survives.
    - `grep -c "visibilitychange\|popstate\|pageshow" components/FreshnessWatcher.tsx` is at least 6 (three registrations plus three removals).
    - `grep -n "FreshnessWatcher" app/layout.tsx` still returns a match — the component is still mounted.
    - `npx tsc --noEmit` reports zero errors and zero unused-import warnings.
    - `npm run lint` reports zero errors.
    - All five freshness e2e specs pass, and the plan SUMMARY lists every assertion that was rewritten or deleted, with its reason.
  </acceptance_criteria>
  <done>`FreshnessWatcher` is reduced to its unconditional `router.refresh()` half with its Next.js-bug documentation intact and updated; the JSON payload backstop, its context, its hook, and its version gate are gone from the codebase; the existing freshness suite is reconciled with no vacuous assertion left behind.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-gate e2e — escape hatch on all four routes, and rendering with no network</name>
  <files>e2e/pull-to-refresh.spec.ts, e2e/local-cache-offline.spec.ts</files>
  <read_first>
    - `e2e/local-cache-first-paint.spec.ts` — plan 34-01's spec, for the `resetToBaseline()`
      `beforeEach`, the `page.evaluate` IndexedDB-inspection helper, and its existing
      `context.setOffline` usage.
    - `e2e/freshness-version-gate.spec.ts` — lines 1-60: `registerRequestLog`,
      `newDataFetchesForRoute`, and the request-capture discipline (`isRscRequest` must be
      called on the real Playwright `Request` object at capture time).
    - `e2e/helpers/` — the full directory listing plus `resume.ts` and `mutate.ts`, to reuse
      existing helpers rather than adding new ones.
    - `e2e/fixture.ts` — `FIXTURE.dueCards`, `FIXTURE.totalCards`, `FIXTURE.studyDays`, and the
      card fronts, so every assertion traces to the seed rather than a hardcoded number.
    - `playwright.config.ts` — `workers: 1`, `retries: 0`, port 3100, prod build, `storageState`
      auth dependency.
    - `lib/usePullToRefresh.ts` — `PULL_THRESHOLD = 70`, `MAX_PULL = 110`, the resistance factor
      `0.5`, and the `window.scrollY > 0` gate. A synthesised gesture must exceed
      `PULL_THRESHOLD` AFTER the 0.5 resistance factor, i.e. a raw travel of more than 140px,
      and must start at scroll-top.
    - `.planning/phases/34-local-first-shell/34-UI-SPEC.md` — the Copywriting Contract rows, so
      every copy assertion uses the locked string.
    - `components/Nav.tsx` — the `Offline` pill's `role="status"` and label, as shipped by plan
      34-04.
  </read_first>
  <action>
    **`e2e/pull-to-refresh.spec.ts` (new)** — covers LOCAL-04 across all four routes. Copy the
    `resetToBaseline()` `beforeEach` and `registerRequestLog` from
    `e2e/freshness-version-gate.spec.ts`.

    Synthesise the gesture with `page.dispatchEvent` / CDP touch emulation rather than
    `page.mouse` — `lib/usePullToRefresh.ts` listens for `touchstart`/`touchmove`/`touchend`
    only. Extract a single `pullToRefresh(page)` helper in this file that scrolls to top,
    dispatches a `touchstart` and a sequence of `touchmove` events totalling more than 140px of
    raw downward travel, then a `touchend`, and awaits the resulting network activity. Assert
    the helper actually triggers by checking that the indicator text becomes visible at least
    once — a helper that silently no-ops would make every test in this file vacuous.

    Tests:
    1. `/` — the gesture fires exactly one `POST /api/sync`, and the indicator text passes
       through `Pull to sync` / `Release to sync` / `Syncing…`.
    2. `/study`, `/cards`, `/habits` (one test each, or one parameterised loop) — the gesture
       fires at least one request to that route's own data endpoint and ZERO `POST /api/sync`
       requests, and the indicator text uses `Pull to refresh` / `Release to refresh` /
       `Refreshing…`.
    3. Cache-and-version bypass — warm each route, then register a request log and perform the
       gesture; assert the route's own data request fires even though the cached entry's
       `dataVersion` still matches `GET /api/version` (no server-side mutation between the warm
       visit and the gesture). This is the assertion that proves the escape hatch is
       unconditional rather than incidentally firing because something changed.
    4. `/cards` boundedness — with two groups expanded, assert every `GET /api/cards` request
       fired by the gesture carries a `take` value equal to that group's currently-loaded row
       count, and that no request carries a `take` larger than the loaded count (RESEARCH
       Pitfall 5).
    5. Offline gesture (the LOCAL-04 `unclassified` edge this planner resolved) — with
       `context.setOffline(true)`, perform the gesture on `/study` and assert the route-local
       failure copy `Couldn't refresh. Check your connection and try again.` appears with a
       `Try again` control, and that the previously-rendered cached content is still on screen.

    **`e2e/local-cache-offline.spec.ts` (new)** — covers LOCAL-05. Same `beforeEach` and
    helpers.

    Tests:
    1. Warm the cache by visiting `/`, `/cards`, and `/habits` online in one context, then
       `context.setOffline(true)` and reload each. Assert real seed-traceable content renders on
       all three — the Home hero due-count, at least one card row front from `FIXTURE.cards`,
       and the Habits streak hero — and that no error state or empty shell is shown.
    2. Assert the `Offline` pill (`role="status"`, label `Offline`) is visible in the header on
       each of those three routes while offline, and disappears after `context.setOffline(false)`
       and a reload.
    3. Assert no unhandled page error occurs while offline: register a `page.on('pageerror')`
       collector and assert it stays empty across the three offline loads (the concurrency edge
       — `fetchCacheContext()` resolving `null` must not produce an unhandled rejection).

    Do not add a service worker, a manifest change, or any Phase 35 behaviour. A cold
    first-ever launch with no prior visit and no network is explicitly out of scope for this
    phase (34-UI-SPEC.md Copywriting Contract) — do not write a test asserting it works.
  </action>
  <verify>
    <automated>npx playwright test e2e/pull-to-refresh.spec.ts e2e/local-cache-offline.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/pull-to-refresh.spec.ts` passes every test, including the `/cards` boundedness assertion and the offline-gesture failure-copy assertion.
    - `npx playwright test e2e/local-cache-offline.spec.ts` passes all three tests.
    - `grep -c "setOffline" e2e/local-cache-offline.spec.ts` is at least 2 (enable and disable).
    - `grep -n "pageerror" e2e/local-cache-offline.spec.ts` returns a match.
    - `grep -n "FIXTURE" e2e/pull-to-refresh.spec.ts e2e/local-cache-offline.spec.ts` returns matches in both — assertions trace to the seed, not to hardcoded values.
    - The `pullToRefresh` helper's own trigger assertion is present, so a silently-no-op gesture fails the suite rather than passing it vacuously.
  </acceptance_criteria>
  <done>All four routes have a proven, unconditional escape hatch with the correct per-route copy and semantics; Home, Cards, and Habits render last-known data with the browser context offline, with the Offline pill visible and no unhandled page errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boundary events (`visibilitychange`/`popstate`/`pageshow`) → `router.refresh()` | The surviving delivery path; untrusted only in the sense that event timing is browser-controlled |
| offline browser state → the four cached route entries | With no network, the cache is the sole rendering source; nothing can correct it until connectivity returns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-18 | Denial of Service (self-inflicted) | resurrecting the Next.js 16.2.1 Suspense/Segment-Cache delivery flake by over-narrowing | high | mitigate | A blocking `checkpoint:decision` (Task 1) gates the one-way change; `router.refresh()` and all three listeners are preserved verbatim with explicit grep-backed acceptance criteria; the doc comment and the 16.2.1 TODO survive so a future Next upgrade re-tests it; all five existing freshness e2e specs must pass and are reconciled rather than deleted wholesale |
| T-34-19 | Tampering | stale cached data rendered indefinitely while offline | medium | accept | With no network there is nothing to revalidate against; this is the requirement (LOCAL-05), not a defect. The `Offline` pill tells the user they are looking at saved data, and the pull-to-refresh escape hatch works the moment connectivity returns. Phase 35 owns genuine offline operation |
| T-34-20 | Repudiation | a green-but-vacuous freshness suite hiding a real regression | medium | mitigate | An explicit prohibition forbids leaving a vacuous assertion; Task 2 requires an explicit rewrite-or-delete decision per assertion with the reason recorded in the SUMMARY; `e2e/pull-to-refresh.spec.ts`'s helper carries its own trigger assertion so a no-op gesture fails rather than passes |
| T-34-21 | Information Disclosure | cached data readable on a shared/lost device with no network | low | accept | Same disposition and rationale as `T-34-02` in plan 34-01 — study content only, no credentials, single-tenant device-locked app. Going offline changes exposure duration, not exposure surface |
</threat_model>

<verification>
- `npm run lint` — zero errors.
- `npx tsc --noEmit` — zero errors, zero unused imports in `components/FreshnessWatcher.tsx`.
- `npm test` — full Vitest suite green.
- `npx playwright test` — the FULL Playwright suite green, including all five existing freshness specs and the four new phase specs.
- `grep -rn "useFreshPayload\|FreshPayloadContext\|fetchRoutePayload" components/ app/ lib/` — zero lines.
- `git diff --name-only` does not list `prisma/schema.prisma`.
- The plan SUMMARY records: the option selected at the Task 1 checkpoint, every freshness-spec assertion rewritten or deleted with its reason, and the VERS-02 supersession note for the milestone audit.
</verification>

<success_criteria>
1. The developer has selected a narrowing option at the blocking checkpoint and the selection is recorded in the SUMMARY.
2. `components/FreshnessWatcher.tsx` still exists, is still mounted, still fires `router.refresh()` unconditionally on all three boundary events, and still documents the Next.js 16.2.1 flake.
3. No payload context, payload hook, or per-route payload fetch remains anywhere in `components/`, `app/`, or `lib/`.
4. All five existing freshness e2e specs pass, with every backstop-dependent assertion explicitly rewritten or deleted and the reason recorded.
5. Pull-to-refresh works on all four routes, fires unconditionally with no cache read or version comparison, uses the D-04-locked per-route copy, and keeps `/cards` bounded to its already-loaded groups.
6. With the browser context offline, Home, Cards, and Habits render last-known data, the `Offline` pill is visible, and no unhandled page error occurs.
</success_criteria>

<output>
Create `.planning/phases/34-local-first-shell/34-05-SUMMARY.md` when done.
</output>
