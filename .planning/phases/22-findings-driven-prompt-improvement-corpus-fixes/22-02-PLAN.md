---
phase: 22-findings-driven-prompt-improvement-corpus-fixes
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - scripts/prompt-eval.mts
  - lib/extract-cards.ts
  - .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/prompt-eval-baseline.json
autonomous: true
requirements: [PROMPT-01, PROMPT-02]
must_haves:
  truths:
    - "A non-persisting scripts/prompt-eval.mts re-runs extraction on the targeted lesson sample (orderIndex 4, 12, 17 — runtime-verified against the live DB) and tallies audit-check counts by reusing lib/audit-checks.ts functions"
    - "A baseline tally captured under the PRE-edit prompt exists as a committed JSON file before any prompt edit lands"
    - "The extract-cards.ts prompt no longer teaches English-descriptive-label grammar fronts, adds the Hangul-only Sino-Korean root gloss rule, the 동사/형용사 collision-disambiguation convention, the loanword/acronym exception, and the isolated-single-char blank-safety rule — each edit annotated with the error class and decision ID it addresses"
    - "An after-run diff against the baseline shows the targeted error classes improve per the D-12 bar (must improve, not necessarily hit zero; no class regresses)"
  artifacts:
    - path: "scripts/prompt-eval.mts"
      provides: "PROMPT-02 non-persisting eval: real extraction calls + audit-check diff vs saved baseline"
      contains: "frontHasRomanization"
    - path: ".planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/prompt-eval-baseline.json"
      provides: "Pre-edit tally (the BEFORE side of the PROMPT-02 diff)"
    - path: "lib/extract-cards.ts"
      provides: "Revised prompt with per-error-class annotation ledger (PROMPT-01)"
      contains: "형용사"
  key_links:
    - from: "scripts/prompt-eval.mts"
      to: "lib/audit-checks.ts"
      via: "dynamic import of frontHasRomanization / sentenceHasRomanization / classifyBlankSafety — never reimplemented"
      pattern: "audit-checks"
    - from: "scripts/prompt-eval.mts"
      to: "lib/extract-cards.ts"
      via: "dynamic import of extractCardsFromNotes (real claude-opus-4-8 call, same as production sync)"
      pattern: "extract-cards"
    - from: "scripts/prompt-eval.mts"
      to: ".planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/prompt-eval-baseline.json"
      via: "--save-baseline writes it; default diff mode reads it and fails fast if missing"
      pattern: "prompt-eval-baseline"
---

<objective>
Deliver PROMPT-01 and PROMPT-02 in the only order that produces a trustworthy diff: (1) build the non-persisting eval script and capture the BEFORE baseline while the prompt is still unedited, (2) revise the `lib/extract-cards.ts` prompt against the audit evidence (D-06, D-07, D-08, D-09, plus D-01/D-02 blank-safety alignment), (3) re-run the eval and prove the targeted error classes drop per the D-12 bar.

Both eval runs execute AFTER plan 22-01, so `classifyBlankSafety` uses the same new predicate on both sides — the diff isolates prompt changes only.

Purpose: the revised prompt must be validated on real lessons before it is trusted (phase goal), and the eval must reuse the audit's own check functions so before/after counts stay comparable.
Output: `scripts/prompt-eval.mts`, committed `prompt-eval-baseline.json`, revised annotated prompt in `lib/extract-cards.ts`, and a PASS diff table recorded in the plan SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-CONTEXT.md
@.planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-RESEARCH.md
@.planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-PATTERNS.md
@lib/extract-cards.ts
@lib/audit-checks.ts
@scripts/audit-cards.mts
@scripts/retro-filter-cleanup.mts
</context>

## Artifacts this phase produces (this plan's slice)

- `scripts/prompt-eval.mts` (net new) — internal symbols: `TARGET_ORDER_INDEXES` const (`[4, 12, 17]`), `ANCHOR_CARDS` const (runtime sample-verification map), a per-lesson tally shape `{ lessonOrderIndex, cardsExtracted, frontRomanization, sentenceRomanization, zeroSafe, zeroSentence }`, `BASELINE_PATH` const, and a `main()` with two modes (`--save-baseline` / default diff)
- `.planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/prompt-eval-baseline.json` (net new, committed) — shape `{ capturedAt, targetOrderIndexes, perLesson, totals }`
- Revised prompt text in `lib/extract-cards.ts`: rewritten "front" field bullet, net-new loanword/acronym exception bullet, updated BLANK-SAFETY GUARANTEE clause (b), plus a "Phase 22 prompt revisions" annotation ledger block comment above the prompt template

## Eval script CLI contract (binding for Task 1 and Task 3)

```
npx tsx scripts/prompt-eval.mts --save-baseline
  → runs extraction on the 3 target lessons, prints per-lesson tallies,
    writes prompt-eval-baseline.json, exits 0.

npx tsx scripts/prompt-eval.mts
  → runs extraction, loads the baseline JSON (exit 2 with a clear message if
    missing), prints a before/after diff table + PASS/FAIL verdict,
    exits 0 on PASS, 1 on FAIL.

Exit 2 also fires when the runtime sample-verification (ANCHOR_CARDS) fails.
```

PASS verdict (D-12: "must improve, not necessarily hit zero"):
1. `after.totals.frontRomanization < before.totals.frontRomanization` when the baseline value is > 0; if the baseline is 0, after must remain 0.
2. No class regresses: `after.totals.sentenceRomanization <= before.totals.sentenceRomanization` (CRT/DST accepted loanwords keep this non-zero on BOTH sides per D-09 — expected, report-only).
3. `after.totals.zeroSafe === 0 && after.totals.zeroSentence === 0` (Phase 20's normalizeExtractedCards code-enforces this by construction — non-zero signals a pipeline regression, not a prompt issue).

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/prompt-eval.mts and capture the pre-edit baseline</name>
  <files>scripts/prompt-eval.mts, .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/prompt-eval-baseline.json</files>
  <read_first>
    - .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-PATTERNS.md sections "scripts/prompt-eval.mts" and "Shared Patterns" (env preamble + audit-check reuse code excerpts)
    - scripts/audit-cards.mts lines 30-60 (env preamble + path conventions to mirror)
    - lib/audit-checks.ts (exported signatures: frontHasRomanization(front), sentenceHasRomanization(korean), classifyBlankSafety(sentences: AuditSentence[]) where AuditSentence = { korean, targetForm, orderIndex })
    - lib/extract-cards.ts lines 40-82 (ExtractedCard/ExtractedSentence shapes and the extractCardsFromNotes signature)
  </read_first>
  <action>
    Create scripts/prompt-eval.mts implementing the CLI contract above (PROMPT-02, D-11, D-12).

    Structure, in order:
    1. Env-first preamble copied from the established pattern (dotenv config for ../.env then ../.env.local with override, BEFORE any dynamic import of lib modules — static lib imports are forbidden in this file because ESM hoisting reads stale env). Fail fast with a clear message when ANTHROPIC_API_KEY or DATABASE_URL is absent.
    2. Dynamic imports: extractCardsFromNotes from ../lib/extract-cards.js; frontHasRomanization, sentenceHasRomanization, classifyBlankSafety from ../lib/audit-checks.js; prisma from ../lib/prisma.js. IMPORTANT correction to the PATTERNS.md snippet: extractCardsFromNotes returns Promise of ExtractedCard[] DIRECTLY — assign the awaited result to a plain `cards` variable; do NOT destructure a `cards` property off it.
    3. Runtime sample verification (D-11 requires verifying the sample against the live DB before trusting hardcoded indexes): define ANCHOR_CARDS mapping orderIndex 4 → card id cmqllei7z009i0gsanauns8au (modifier-grammar class), 12 → cmqlmj2il01up0gsarwkragfi (loanword class), 17 → cmqln565802oc0gsat0vy110z (Sino-Korean root class). For each, fetch the card's lessonId and the lesson with that orderIndex; if the anchor card does not belong to that lesson, print which anchor failed and exit 2 (sample must be re-derived, never silently evaluated).
    4. Per target lesson (read-only queries only): fetch all deck normalizedFront values, fetch this lesson's own cards' normalizedFront values, and pass the SET DIFFERENCE as the dedup list (RESEARCH.md Pitfall 1 — passing the lesson's own fronts makes extraction legitimately skip everything and return ~0 cards, trivially no-oping the eval). Then call extractCardsFromNotes with the lesson's rawContent, that filtered list, and an empty emphasized array.
    5. Tally per lesson using ONLY the imported audit-check functions (never a local Latin regex or a blank-safety reimplementation): frontRomanization = count of cards where frontHasRomanization(card.front) is true; sentenceRomanization = total sentences where sentenceHasRomanization(s.korean) is true; zeroSafe/zeroSentence = cards whose classifyBlankSafety result is 'zero-safe'/'zero-sentences', synthesizing orderIndex 0,1,2... onto each ExtractedSentence (they lack it natively). Also record cardsExtracted per lesson and print a loud warning when it is 1 or less (Pitfall 1 symptom).
    6. Mode handling per the CLI contract: --save-baseline writes the JSON to BASELINE_PATH (.planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/prompt-eval-baseline.json) and exits 0; default mode loads it, prints a per-lesson + totals before/after diff table, applies the three PASS rules, prints the verdict, exits 0/1. Always print "READ-ONLY EVAL — no DB writes performed" in both modes.
    7. The script performs no Prisma write operations of any kind and never persists extracted cards — its only Prisma calls are the read queries in steps 3-4.

    Then RUN the baseline capture command (`--save-baseline`) against the live DB with the CURRENT unedited prompt. This makes 3 real claude-opus-4-8 calls (~30-90s each, RESEARCH.md Pitfall 2 — budget exactly this one baseline run; do not iterate). Sanity-check the printed tallies: lessons 4 and 17 should show frontRomanization above 0 (the old prompt reproduces English-labeled fronts), and every lesson should show cardsExtracted well above 1. Commit script + baseline JSON as `feat(22-02): prompt-eval script + pre-edit baseline (PROMPT-02)`.
  </action>
  <verify>
    <automated>test -f scripts/prompt-eval.mts && node -e 'const b=JSON.parse(require("fs").readFileSync(".planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/prompt-eval-baseline.json","utf8")); if(!b.perLesson||!b.totals) process.exit(1)' && ! grep -Eq 'prisma[.][A-Za-z]+[.](create|update|upsert|delete)' scripts/prompt-eval.mts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/prompt-eval.mts exists, uses the env-first dynamic-import preamble, and contains zero Prisma write calls (create/update/upsert/delete in any model namespace)
    - prompt-eval-baseline.json exists, parses, and contains perLesson entries for orderIndex 4, 12, 17 plus a totals object
    - The baseline was captured BEFORE any edit to lib/extract-cards.ts (git history: the baseline commit precedes the prompt-edit commit)
    - ANCHOR_CARDS runtime verification passed during the baseline run (no exit-2)
    - cardsExtracted was 2 or more for every lesson in the baseline (Pitfall 1 not triggered)
  </acceptance_criteria>
  <done>Eval script committed; pre-edit baseline JSON committed; the BEFORE side of PROMPT-02's diff is locked in.</done>
</task>

<task type="auto">
  <name>Task 2: Revise the extraction prompt per error class (PROMPT-01)</name>
  <files>lib/extract-cards.ts</files>
  <read_first>
    - lib/extract-cards.ts lines 120-221 (the full prompt template: "front" field bullet at lines 154-157, BLANK-SAFETY GUARANTEE at lines 195-201, General rules at lines 210-215)
    - .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-PATTERNS.md section "lib/extract-cards.ts" (edit-target excerpts)
    - .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-CONTEXT.md decisions D-06 through D-09
  </read_first>
  <action>
    <!-- planner-discipline-allow: Action verb -->
    <!-- planner-discipline-allow: direction particle -->
    Make exactly four prompt-text edits inside the template literal in lib/extract-cards.ts, plus one annotation ledger. Text edits only — do not touch the zod schema, output_config, streaming/salvage logic, or any code outside the prompt string.

    Edit 1 — rewrite the "front" field bullet (currently lines 154-157). The current bullet actively teaches the flagged pattern: its own example strings "Action verb ~는 + noun (present modifier)" and "~(으)로 (direction particle)" are English-labeled grammar fronts and MUST be removed entirely (the first produced 4 of the 10 audit-flagged fronts). Replace with a bullet that states: front is Korean (Hangul); never romanization (keep the existing kkujunhada counter-example); GRAMMAR cards use bare pattern notation only (e.g. ~(으)면, ~고) with NO English descriptive label anywhere on the front — descriptive English belongs in "back" only (D-07); when two distinct grammar points would collide on the identical bare marker, disambiguate with a short Korean grammar-term prefix such as 동사 ~(으)ㄴ for the action-verb past modifier vs 형용사 ~(으)ㄴ for the descriptive-verb modifier — never an English tag (D-08); SINO-KOREAN ROOT vocabulary keeps its parenthetical gloss Hangul-only, the traditional root reading such as 소 (작을 소) — never mixed with an English word, English meaning goes in "back" alone (D-06); other vocabulary/phrase cards MAY still carry a short English clarifying gloss in parentheses only when genuinely needed for disambiguation; Hangul-in-parens is always fine.

    Edit 2 — add a net-new loanword/acronym exception bullet in the General rules section, immediately after the "NEVER include romanization" rule at lines 214-215 (D-09): untranslated English acronyms and loanwords used in authentic Korean speech (CRT, DST, PC방-style borrowings) are NOT romanization — they may appear inline in fronts and sentences exactly as Koreans write them.

    Edit 3 — update BLANK-SAFETY GUARANTEE clause (b) (currently lines 195-197, which encode the OLD single-char rule). New clause: a single-syllable targetForm is blank-safe only when it stands as an isolated word — space, punctuation, or sentence edge on both sides (e.g. 다 in 밥을 다 먹었어요) — and never when embedded inside a longer word; 2+ character targetForms follow the existing rules unchanged. This aligns the prompt with the plan 22-01 code rule so future single-syllable vocabulary is no longer silently dropped by Phase 20's code-enforced blank-safety (D-01/D-02, zero-safe/zero-sentence error class).

    Edit 4 — annotation ledger (PROMPT-01 success criterion "each edit annotated to the specific error class it addresses"): the prompt lives inside one template literal where TS comments are impossible, so add a block comment in the code immediately above the prompt template (above the messages array construction) titled "Phase 22 prompt revisions (card-audit-2026-07-07)" with one line per edit mapping it to error class + decision ID: front-bullet rewrite → romanization-flagged-fronts (D-06/D-07/D-08); loanword exception → romanization false-positive class (D-09); blank-safety clause (b) → zero-safe/zero-sentence class (D-01/D-02).

    Run `npm test` (prompt-text change must not break parseExtractionResponse/normalizeExtractedCards tests) and `npm run lint`. Commit as `feat(22-02): revise extraction prompt per audit error classes (PROMPT-01, D-06/D-07/D-08/D-09)`.
  </action>
  <verify>
    <automated>! grep -q 'Action verb' lib/extract-cards.ts && ! grep -q 'direction particle' lib/extract-cards.ts && grep -q '동사' lib/extract-cards.ts && grep -q '형용사' lib/extract-cards.ts && grep -q '작을 소' lib/extract-cards.ts && grep -Eq 'CRT|DST' lib/extract-cards.ts && grep -q 'D-06' lib/extract-cards.ts && grep -q 'D-07' lib/extract-cards.ts && grep -q 'D-08' lib/extract-cards.ts && grep -q 'D-09' lib/extract-cards.ts && npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The English-labeled grammar-front example strings are gone from the prompt; no prompt text instructs or exemplifies English descriptive labels on grammar fronts
    - The 동사/형용사 Hangul grammar-term disambiguation convention is present and framed as the general rule for bare-marker collisions (D-08 — applies beyond the two known colliding fronts)
    - The Sino-Korean root rule with a Hangul-only gloss example is present (D-06)
    - The loanword/acronym exception bullet is present near the romanization rule (D-09)
    - BLANK-SAFETY clause (b) describes the isolated-single-char allowance and matches the plan 22-01 code rule (D-01/D-02)
    - The annotation ledger block comment exists above the prompt template and names all decision IDs
    - Full suite + lint green; zod schema and extraction code untouched (git diff confined to the prompt string and the ledger comment)
  </acceptance_criteria>
  <done>Prompt revised, every edit traceable to its error class; suite and lint green.</done>
</task>

<task type="auto">
  <name>Task 3: After-run diff — validate the revised prompt against the baseline (PROMPT-02)</name>
  <files>scripts/prompt-eval.mts</files>
  <read_first>
    - .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/prompt-eval-baseline.json (the BEFORE tallies captured in Task 1)
  </read_first>
  <action>
    Run the eval in diff mode (`npx tsx scripts/prompt-eval.mts`, no flag) — 3 more real API calls. The script prints the before/after table and the PASS/FAIL verdict per the D-12 bar defined in the CLI contract.

    If FAIL: diagnose from the per-lesson table (which class regressed or failed to improve), make ONE further wording refinement to the relevant prompt bullet in lib/extract-cards.ts (staying within the D-06/D-07/D-08/D-09 rules — the rules themselves are locked, only prose wording is discretionary per RESEARCH.md Open Question 1), and re-run ONCE. Hard budget: maximum 2 after-runs total (Pitfall 2 — real Opus cost/latency). If still FAIL after the second run, STOP — do not loop; record both diff tables in the SUMMARY, report the failure to the orchestrator as a blocker, and leave the prompt edits committed for human review.

    On PASS: copy the printed diff table verbatim into the plan SUMMARY (it is the PROMPT-02 evidence that plan 22-03's fix report references), noting that sentenceRomanization staying at its baseline level is the expected D-09 accepted-loanword outcome, and that the eval script fixes only its own diff-mode bugs if any surfaced (no tally-logic changes after baseline capture — that would invalidate the comparison). Commit any script fix + the SUMMARY notes as `feat(22-02): prompt-eval after-run PASS (PROMPT-02, D-12)`.
  </action>
  <verify>
    <automated>npx tsx scripts/prompt-eval.mts</automated>
  </verify>
  <acceptance_criteria>
    - Diff mode exits 0 (PASS): frontRomanization strictly improved vs baseline (or 0→0), no class regressed, zeroSafe and zeroSentence are 0 in the after-run
    - No more than 2 after-runs were executed
    - The tally logic in scripts/prompt-eval.mts is unchanged since the baseline commit (only diff-mode/reporting fixes permitted)
    - The final diff table is recorded in the plan SUMMARY
  </acceptance_criteria>
  <done>Revised prompt validated on real lessons per the D-12 bar; evidence table recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → Anthropic API | Lesson content (developer's own tutor notes, trusted) sent to an already-authenticated SDK integration |
| script → Turso DB | Read-only queries only; the script must never gain a write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-02 | Tampering (of LLM output) | extractCardsFromNotes prompt / lesson content | low | accept | Prompt injection via lesson content is a pre-existing accepted characteristic since Phase 20 — lesson notes are the developer's own trusted tutor content, not third-party input (RESEARCH.md Security Domain). No new mitigation introduced. |
| T-22-03 | Tampering | scripts/prompt-eval.mts accidentally persisting eval output | medium | mitigate | PROMPT-02 mandates non-persisting: script contains zero Prisma write calls, enforced by Task 1's automated negative grep gate; extractCardsFromNotes itself imports no Prisma (verified in RESEARCH.md). |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed or upgraded in this plan (RESEARCH.md Package Legitimacy Audit: none this phase). Any install task appearing during execution is out of plan scope and must stop for human review. |
</threat_model>

<verification>
- `git log` shows baseline commit strictly before the prompt-edit commit (BEFORE side captured under the old prompt)
- `npx tsx scripts/prompt-eval.mts` exits 0 against the committed baseline
- Prompt-edit annotations reviewed manually per 22-VALIDATION.md ("Prompt wording quality per error class" is manual-only)
- `npm test` and `npm run lint` green after all three tasks
</verification>

<success_criteria>
- PROMPT-01: every prompt edit exists, is scoped to the prompt string, and is annotated to its error class + decision ID
- PROMPT-02: a committed baseline + a passing after-run diff on lessons 4/12/17, using the same lib/audit-checks.ts functions the audit used, with no DB writes and no full-corpus re-run
- The eval sample was runtime-verified against the live DB (ANCHOR_CARDS), satisfying the CONTEXT.md instruction to verify lessons 4/12/17 before hardcoding
</success_criteria>

<output>
Create `.planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-02-SUMMARY.md` when done.
</output>
