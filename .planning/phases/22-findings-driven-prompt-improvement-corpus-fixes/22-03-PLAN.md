---
phase: 22-findings-driven-prompt-improvement-corpus-fixes
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - scripts/fix-corpus-2026-07.mts
  - .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-POST-FIX-AUDIT.md
  - .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-FIX-REPORT.md
autonomous: false
requirements: [FIX-01, FIX-02]
must_haves:
  truths:
    - "The 9 audit-flagged fronts carry their new Hangul-only values in the live DB, mutated in place by id with front and normalizedFront updated in the same write — no card deleted or recreated, FSRS state and ReviewLog history intact"
    - "Card 철 has natural example sentences (철은/철이/철로 forms, targetForm 철) and is no longer zero-sentence; it studies in Exposure and multiple-choice modes (Recall/fill-blank ineligibility accepted per D-04)"
    - "The fix script defaults to dry-run, writes only with --apply, re-verifies normalizedFront collisions at write time, and prints its target DB and mode before any write"
    - "A post-fix audit re-run proves the targeted findings cleared for the fixed card ids, and 22-FIX-REPORT.md documents every applied fix and every accepted residual (CRT/DST loanwords per D-09, 철 zero-safe per D-04, both near-duplicate clusters reviewed-not-duplicate per D-10)"
  artifacts:
    - path: "scripts/fix-corpus-2026-07.mts"
      provides: "Dry-run-gated in-place corpus fix (FIX-01/FIX-02): 9 front rewrites + 철 sentence creation"
      contains: "REWRITES"
    - path: ".planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-POST-FIX-AUDIT.md"
      provides: "Post-fix audit-cards.mts output preserved at a stable path (Phase 21 evidence report never overwritten)"
    - path: ".planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-FIX-REPORT.md"
      provides: "Phase fix report: applied fixes + accepted residuals + reviewed-not-duplicate clusters"
      contains: "reviewed-not-duplicate"
  key_links:
    - from: "scripts/fix-corpus-2026-07.mts"
      to: "lib/card-key.ts"
      via: "dynamic import of normalizeFront — front and normalizedFront recomputed together in the same update"
      pattern: "normalizeFront"
    - from: "scripts/fix-corpus-2026-07.mts"
      to: "prisma Card rows"
      via: "prisma card update by id inside a transaction — update-only write path"
      pattern: "where: \\{ id"
---

<objective>
Correct the high-confidence audit findings in the live database, in place by id (FIX-01), via a dry-run-gated script following the retro-filter-cleanup.mts convention (FIX-02): rewrite the 9 romanization-flagged fronts to their locked Hangul-only values (D-06/D-07/D-08), create natural sentences for the zero-sentence card 철 (D-04), and then prove the findings cleared with a post-fix audit re-run. Document the deliberate non-fixes — CRT/DST loanwords (D-09), 다 resolved by the plan 22-01 rule change alone (D-03), and both near-duplicate clusters as reviewed-not-duplicate (D-10) — in the phase fix report so no future audit pass re-investigates them.

Purpose: this is the "then high-confidence existing-card issues are corrected in place" half of the phase goal, executed only after the prompt work so the eval runs saw a stable deck.
Output: `scripts/fix-corpus-2026-07.mts`, applied DB fixes, `22-POST-FIX-AUDIT.md`, `22-FIX-REPORT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-CONTEXT.md
@.planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-RESEARCH.md
@.planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-PATTERNS.md
@.planning/audits/card-audit-2026-07-07.md
@scripts/retro-filter-cleanup.mts
@scripts/audit-cards.mts
</context>

## Artifacts this phase produces (this plan's slice)

- `scripts/fix-corpus-2026-07.mts` (net new) — internal symbols: `REWRITES` const (9 id→newFront entries, table below), `CHEOL_CARD_ID` const (`cmqlmqdoa02430gsax79l6oza`), `CHEOL_SENTENCES` const, `APPLY` argv gate, write-time collision re-check, chunked `$transaction` apply block
- `.planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-POST-FIX-AUDIT.md` (net new) — copy of the post-fix `audit-cards.mts` report
- `.planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-FIX-REPORT.md` (net new) — applied-fixes ledger + accepted-residuals register
- New `Sentence` rows for card 철 (DB artifact, not a file)

## REWRITES table (locked values — D-06/D-07/D-08; collision-free per RESEARCH.md live check)

| Card id | Current front | New front | Decision |
|---|---|---|---|
| cmqln565802oc0gsat0vy110z | 소 (작을 소, small) | 소 (작을 소) | D-06 |
| cmqln56i902of0gsajqgbyk9a | 고 (높을 고, high) | 고 (높을 고) | D-06 |
| cmqln56tv02oi0gsa7lmwtm4p | 식 (알 식, knowledge) | 식 (알 식) | D-06 |
| cmqlngfdh036t0gsaguju6n2h | 용 (~용, for use) | 용 (~용) | D-06 |
| cmr42yyvi000gwhsa3uw44l4v | 료 (~료, fee/fare) | 료 (~료) | D-06 |
| cmqllei7z009i0gsanauns8au | (English-labeled present modifier) | 동사 ~는 | D-07 |
| cmqlleits009n0gsaym0ytmh0 | (English-labeled past modifier) | 동사 ~(으)ㄴ | D-07/D-08 |
| cmqllejjt009s0gsavynqplsf | (English-labeled future modifier) | 동사 ~(으)ㄹ | D-07 |
| cmqllejxv009x0gsa14saekdl | (English-labeled descriptive modifier) | 형용사 ~(으)ㄴ | D-07/D-08 |

CRT 렌즈 (`cmqlmj2il01up0gsarwkragfi`) is intentionally ABSENT from this table — no change, accepted loanword (D-09). Card 다 (`cmqlm1w0u014k0gsa6eydclfd`) is intentionally absent — resolved by the plan 22-01 rule change alone, no DB write (D-03).

## 철 sentence defaults (D-04 — Claude's discretion on wording; checkpoint below lets the user adjust)

For card `cmqlmqdoa02430gsax79l6oza`, create Sentence rows with targetForm `철` (the bare root — embedded against its particle, so the card intentionally stays Recall/fill-blank-ineligible per D-04):

1. korean: `철은 아주 단단한 금속이에요.` — translation: "Iron is a very hard metal." — orderIndex 0
2. korean: `이 다리는 철로 만들어졌어요.` — translation: "This bridge is made of iron." — orderIndex 1
3. korean: `지하철을 타고 회사에 가요.` — translation: "I take the subway to work." — orderIndex 2 (shows the root inside a compound, matching the card's back-field note about train words)

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/fix-corpus-2026-07.mts and run the dry-run</name>
  <files>scripts/fix-corpus-2026-07.mts</files>
  <read_first>
    - scripts/retro-filter-cleanup.mts (whole file — the binding FIX-02 template: env preamble, APPLY argv gate, always-print report, chunked transaction writes)
    - .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-PATTERNS.md sections "scripts/fix-corpus-2026-07.mts" and "Shared Patterns" (collision re-check + atomic front/normalizedFront excerpts)
    - prisma/schema.prisma Sentence model (fields: korean, targetForm, translation, orderIndex, cardId)
  </read_first>
  <action>
    Create scripts/fix-corpus-2026-07.mts following the retro-filter-cleanup.mts conventions exactly (FIX-02):

    1. Env-first preamble (dotenv for ../.env then ../.env.local override, BEFORE dynamic imports of ../lib/prisma.js and ../lib/card-key.js).
    2. APPLY gate: writes happen only when the --apply argv flag is present; without it, print the full report followed by the DRY RUN message and exit 0 with zero writes.
    3. Print-before-write (security convention): before any query, print the resolved DATABASE_URL host only (never the auth token) and the mode (DRY RUN / APPLY).
    4. Load the REWRITES table (9 entries, verbatim from this plan) and CHEOL_SENTENCES (verbatim defaults above, subject to Task 2 approval). For each rewrite: fetch the current card by id (abort with a clear error if any id is missing — the deck may have drifted via cron sync); compute the new normalizedFront via the imported normalizeFront; re-run the collision check at execution time (find any OTHER card holding that normalizedFront; abort the whole run on any hit — research verified zero collisions, but the deck may have grown since, per CONTEXT.md).
    5. Report: one line per rewrite showing id, old front → new front, old normalizedFront → new normalizedFront, collision check result; plus the three proposed 철 sentences. Print the identical report in both modes.
    6. Apply block (only under --apply): a single chunked $transaction (well under the 50-op chunk convention) containing 9 card updates — each setting front AND normalizedFront together in the same update call (CLAUDE.md hard rule; never one without the other) — plus Sentence creations for 철 (cardId CHEOL_CARD_ID, fields per the defaults, orderIndex 0/1/2). Guard idempotency: if 철 already has sentences, skip its creation and say so in the report (safe to re-run).
    7. The script never deletes or re-creates Card rows and touches no other model — front rewrites do not affect Card.components or CardDependency edges, so NO edge-reconciliation work is included (RESEARCH.md Pitfall 4 explicitly rules it out of scope).

    Run the dry-run (`npx tsx scripts/fix-corpus-2026-07.mts`, no flag) against the live DB and confirm the report lists all 9 rewrites collision-free plus the 철 sentences. Commit as `feat(22-03): corpus fix script, dry-run verified (FIX-01/FIX-02)`.
  </action>
  <verify>
    <automated>npx tsx scripts/fix-corpus-2026-07.mts | grep -Fq 'DRY RUN' && ! grep -Eq 'card[.](delete|deleteMany|create|createMany)[(]' scripts/fix-corpus-2026-07.mts && grep -q 'normalizeFront' scripts/fix-corpus-2026-07.mts</automated>
  </verify>
  <acceptance_criteria>
    - Dry-run exits 0, prints the full 9-rewrite report + 철 sentences + DB host + mode, performs zero writes
    - The script contains no Card delete/create operations of any kind (update-by-id only; Sentence creation is the sole insert path)
    - Every card update sets front and normalizedFront in the same update call
    - Collision re-check runs at execution time and aborts on any hit
    - Missing-id and already-has-sentences cases are handled with clear messages (idempotent re-run safe)
  </acceptance_criteria>
  <done>Fix script committed; dry-run report verified against the live deck with no writes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve the dry-run report before writing to production</name>
  <files>none — approval gate only, no files modified</files>
  <action>PAUSE before any production write (T-22-04 mitigation). Present the Task 1 dry-run report to the user for approval: the 9 in-place front rewrites (locked values, D-06/D-07/D-08) and the 3 proposed 철 sentences (Claude-authored wording under D-04 discretion — the only content in this phase the user has not already reviewed). Do not run --apply until the resume signal arrives.</action>
  <what-built>The corpus fix script and its dry-run report against the live Turso deck: 9 in-place front rewrites and 3 new Korean example sentences for card 철.</what-built>
  <how-to-verify>
    1. Read the dry-run output pasted above this checkpoint (or re-run: npx tsx scripts/fix-corpus-2026-07.mts).
    2. Confirm the 9 old → new front pairs match the locked decisions and every collision check reads clear.
    3. Read the 3 proposed 철 sentences — natural Korean, 철은/철로/지하철 forms, correct translations. Suggest replacements if any wording feels off (D-04 locked the approach, not the exact sentences).
    4. Confirm the printed DB host is the production Turso instance you expect.
  </how-to-verify>
  <verify>
    <human-check>User has read the dry-run report and explicitly approved (or adjusted) the rewrites and 철 sentences.</human-check>
  </verify>
  <done>User responded with "approved" (or adjustments were applied and re-presented) — the --apply run in Task 3 is authorized.</done>
  <resume-signal>Type "approved" to run --apply, or describe sentence/front adjustments to make first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply, re-audit, and write the phase fix report</name>
  <files>.planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-POST-FIX-AUDIT.md, .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-FIX-REPORT.md</files>
  <read_first>
    - .planning/audits/card-audit-2026-07-07.md (Phase 21 evidence — the BEFORE side of the fix report; must survive unmodified)
    - scripts/audit-cards.mts lines 55-60 (report path is date-stamped: a same-day re-run OVERWRITES the Phase 21 report at card-audit-2026-07-07.md)
  </read_first>
  <action>
    1. Apply any adjustments requested at the checkpoint (update CHEOL_SENTENCES / REWRITES accordingly; any NEW or reworded front must get a fresh collision re-check — the script already enforces this at write time). Run `npx tsx scripts/fix-corpus-2026-07.mts --apply`. Confirm the apply report shows 9 updates + 철 sentence creation with no aborts.
    2. Re-run the audit: `npx tsx scripts/audit-cards.mts`. Copy its freshly written report to .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-POST-FIX-AUDIT.md. Preservation guard: if the audit's dated output path collides with the committed Phase 21 report (same UTC date), restore the original afterward with git checkout on .planning/audits/card-audit-2026-07-07.md so the pre-fix evidence is never lost; if the date differs, leave the new dated report in .planning/audits/ as well.
    3. Verify the post-fix findings by CARD ID, not by raw counts (the deck may have drifted via Phase 19 cron auto-sync, so absolute counts are informational only): the 다 card id must appear nowhere in the post-fix report (zero-safe cleared by the plan 22-01 rule — D-03); zero-sentence must read 0 (철 fixed; Phase 20 blocks any new zero-sentence card, so this is drift-proof); none of the 9 REWRITES ids may appear in the flagged-fronts section; the CRT front id still appears there (accepted D-09 residual); the 철 id now appears under zero-safe (expected and accepted — its sentences embed 철 against particles, so the card is Exposure/multiple-choice-only per D-04); both near-duplicate clusters still appear (accepted D-10; the 고 rewrite does not change its superNormalize fuzzy key). If a count differs from expectation because of NEW cards absent from the Phase 21 report, record the new ids in the fix report as post-audit arrivals, out of this phase's bounded fix scope.
    4. Write 22-FIX-REPORT.md with three sections: (a) Applied fixes — the 9 rewrites (id, old → new) + 철 sentences, all mutated in place by id, FSRS/ReviewLog untouched; (b) Resolved without DB writes — 다 via the sentenceMatch word-boundary rule (D-03), with the prompt-eval PASS diff from plan 22-02's SUMMARY referenced as the PROMPT-02 evidence; (c) Accepted residuals register — CRT/DST loanword flags in fronts and sentences (D-09, documented false positive, will keep appearing in future audits), 철 as zero-safe (D-04), and both clusters (보다 / 고) marked reviewed-not-duplicate (D-10) so future audit passes do not re-investigate them.
    5. Run `npm test` and `npm run lint` (unchanged code, but the phase gate requires green). Commit everything as `feat(22-03): apply corpus fixes in place + post-fix audit + fix report (FIX-01/FIX-02)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-POST-FIX-AUDIT.md && ! grep -Fq 'cmqlm1w0u014k0gsa6eydclfd' .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-POST-FIX-AUDIT.md && grep -Fq '| Zero-sentence cards | 0 |' .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-POST-FIX-AUDIT.md && ! awk '/### Flagged fronts/,/### Flagged sentences/' .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-POST-FIX-AUDIT.md | grep -Eq 'cmqln565802oc0gsat0vy110z|cmqln56i902of0gsajqgbyk9a|cmqln56tv02oi0gsa7lmwtm4p|cmqlngfdh036t0gsaguju6n2h|cmr42yyvi000gwhsa3uw44l4v|cmqllei7z009i0gsanauns8au|cmqlleits009n0gsaym0ytmh0|cmqllejjt009s0gsavynqplsf|cmqllejxv009x0gsa14saekdl' && awk '/### Flagged fronts/,/### Flagged sentences/' .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-POST-FIX-AUDIT.md | grep -Fq 'cmqlmj2il01up0gsarwkragfi' && grep -Fq 'cmqlmqdoa02430gsax79l6oza' .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-POST-FIX-AUDIT.md && grep -q 'reviewed-not-duplicate' .planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-FIX-REPORT.md && git diff --quiet HEAD -- .planning/audits/card-audit-2026-07-07.md && npm test</automated>
  </verify>
  <acceptance_criteria>
    - --apply completed with 9 in-place updates + 철 sentence creation, no aborts, no Card deletes/creates
    - 22-POST-FIX-AUDIT.md proves by id: 다 absent everywhere, zero-sentence 0, all 9 rewrite ids gone from flagged fronts, CRT front still flagged (accepted), 철 present as zero-safe (accepted)
    - .planning/audits/card-audit-2026-07-07.md is bit-identical to its committed Phase 21 state
    - 22-FIX-REPORT.md contains all three sections including the reviewed-not-duplicate cluster register (D-10) and the D-09 loanword acceptance covering both fronts and sentences
    - Full suite + lint green
  </acceptance_criteria>
  <done>Corpus corrected in place; evidence preserved at stable paths; every residual documented as accepted with its decision ID.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → production Turso | The fix script's --apply writes cross into production data holding irreplaceable FSRS/ReviewLog history |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-04 | Tampering | fix script run with --apply against the wrong environment | high | mitigate | Env loaded only from .env/.env.local with no CLI override of the DB target; script prints the DATABASE_URL host + mode before any query ("print before write" convention from RESEARCH.md Security Domain); dry-run is the default; a blocking human checkpoint (Task 2) gates the --apply run. |
| T-22-05 | Tampering | destroying FSRS state / ReviewLog history via delete-and-recreate | high | mitigate | STATE.md v1.5 hard rule: mutate in place by id only. Script structurally contains no Card delete/create operations (Task 1 automated negative gate); Sentence creation is additive; cascade paths are never triggered. |
| T-22-06 | Repudiation | untraceable production data changes | medium | mitigate | Identical always-printed report in dry-run and apply modes; post-fix audit re-run + 22-FIX-REPORT.md create a committed, dated record of exactly which rows changed and why (per-fix decision IDs). |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed or upgraded in this plan (RESEARCH.md Package Legitimacy Audit: none this phase). Any install task appearing during execution is out of plan scope and must stop for human review. |
</threat_model>

<verification>
- Dry-run → checkpoint approval → --apply sequence observed in git/session history (FIX-02)
- Post-fix audit gates all pass by card id (drift-proof against cron-sync deck growth)
- Phase 21 evidence report unmodified in git
- Manual spot-check per 22-VALIDATION.md: open the Cards page, confirm one rewritten front (e.g. 동사 ~는) renders and its card still shows review history; open 철 in CardEditor and confirm its sentences render with 철 highlighted
</verification>

<success_criteria>
- FIX-01: every high-confidence finding corrected in place by id — 9 fronts rewritten with front+normalizedFront atomic, 철 no longer zero-sentence, zero cards deleted or recreated
- FIX-02: the script is dry-run by default, --apply-gated, collision-re-checked at write time, and report-printing in both modes — matching retro-filter-cleanup.mts
- All deliberate non-fixes are documented as accepted residuals with decision IDs, closing the audit findings loop end-to-end (phase goal)
</success_criteria>

<output>
Create `.planning/phases/22-findings-driven-prompt-improvement-corpus-fixes/22-03-SUMMARY.md` when done.
</output>
