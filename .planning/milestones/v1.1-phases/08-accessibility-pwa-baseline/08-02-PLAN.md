---
phase: 08-accessibility-pwa-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/StudySession.tsx
  - components/GlossProvider.tsx
  - components/AudioButton.tsx
  - components/Nav.tsx
  - app/page.tsx
  - app/study/page.tsx
autonomous: false
requirements: [A11Y-01, A11Y-03, A11Y-04]
user_setup: []
must_haves:
  truths:
    - "Every interactive icon button named in the audit (Nav tabs, gloss close, gloss add, StudySession Undo, mode-selector Sheet) exposes a screen-reader-readable accessible name via aria-label, not title"
    - "Every interactive element listed as failing in the spec gains an explicit :active press state"
    - "The gloss close, gloss add, StudySession Undo, and StudySession End controls all meet the 44px minimum touch target"
    - "All 7 pages render correctly in both light and dark mode after the changes (no invisible text, no harsh contrast, no broken highlight)"
  artifacts:
    - "components/StudySession.tsx — Undo button with aria-label (title removed) + min-h-[44px] px-3 + active:bg-surface-3; End button with min-h-[44px] px-3 + active:bg-surface-3"
    - "components/GlossProvider.tsx — close button min-h-[44px] min-w-[44px] + active:bg-surface-3; Add-as-card button dynamic aria-label + min-h-[44px] + active:opacity-70"
    - "components/AudioButton.tsx — active state on idle/play (active:bg-button-soft) and playing (active:opacity-80)"
    - "components/Nav.tsx — aria-label on each bottom-tab Link"
    - "app/page.tsx — band-up dismiss button active:opacity-80 added"
    - "app/study/page.tsx — mode-selector Sheet given title=\"Study options\""
  key_links:
    - "AudioButton idle/play active state must use the existing --button-soft token utility (active:bg-button-soft); the playing state uses active:opacity-80 — these match the hover patterns already on each state"
    - "The gloss Add-as-card aria-label must be dynamic: it interpolates the tapped word from popover state so screen readers announce the specific word"
    - "Replacing title with aria-label on Undo must remove the title attribute entirely (title is not reliably read by screen readers — F-12)"
---

<objective>
Apply all component-level accessibility fixes for Phase 8: aria-label additions/corrections
(A11Y-01), explicit :active press states and 44px touch targets (A11Y-03 per-element), and the
final 14-screen dark-mode regression test (A11Y-04, verification-only). These are surgical edits
to six existing files — no new components, pages, tokens, or copy strings.

Purpose: Close the accessibility audit findings (F-03, F-07, F-12, F-26) so the polish shipped
in Phases 5–7 is screen-reader-readable, reduced-motion-safe, and free of dark-mode regressions.

Output: Edits to StudySession.tsx, GlossProvider.tsx, AudioButton.tsx, Nav.tsx, app/page.tsx,
and app/study/page.tsx, followed by a human-verified dark-mode pass across all 7 pages.

NOTE: Several spec items are already satisfied in the codebase and require NO change — they are
called out explicitly in each task's read_first so the executor verifies rather than re-applies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-accessibility-pwa-baseline/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: aria-label corrections — Nav tabs, mode-selector Sheet, Undo button (A11Y-01)</name>
  <files>components/Nav.tsx, app/study/page.tsx, components/StudySession.tsx</files>
  <read_first>
    - components/Nav.tsx — the bottom-tab `<Link>` block (lines ~82-95). Note each tab Link already has `aria-current` and `active:opacity-70` but NO `aria-label`. The Settings gear (lines ~60-71) already has `aria-label="Settings"` — do NOT touch it.
    - app/study/page.tsx — the mode-selector `<Sheet open={showModeSheet} ...>` call (line ~279) which has NO `title` prop. The lessons `<Sheet ... title="Lessons">` (line ~284) is already titled — do NOT touch it.
    - components/StudySession.tsx — the Undo `<button>` (lines ~550-556) currently uses `title="Undo last rating"` and no aria-label.
    - .planning/phases/08-accessibility-pwa-baseline/08-UI-SPEC.md — "A11Y-01 — aria-label Audit" table (lines ~101-117), specifically the Nav bottom-tab row, the Sheet row (F-26), and the StudySession Undo row (F-12).
    - Sheet component contract: Sheet renders `aria-label={title}` on its dialog container, so passing `title="Study options"` provides the accessible name.
  </read_first>
  <action>
    Three edits across three files:
    (1) components/Nav.tsx — on each bottom-tab `<Link>` inside the `links.map(...)` for the mobile bottom bar, add `aria-label={label}` (the `label` is already destructured from the map item). Add it only to the bottom-tab Links, not the desktop inline links and not the Settings gear.
    (2) app/study/page.tsx — add the prop `title="Study options"` to the mode-selector Sheet call (`<Sheet open={showModeSheet} onClose={() => setShowModeSheet(false)}>`). This gives the otherwise-untitled dialog an accessible name. Leave the ModeSelector child and the already-titled Lessons Sheet unchanged.
    (3) components/StudySession.tsx — on the Undo button, replace the `title="Undo last rating"` attribute with `aria-label="Undo last rating"` (remove `title` entirely; `title` is not reliably read by screen readers per F-12). Keep the existing onClick and className for now (touch-target/active-state changes happen in Task 2).
  </action>
  <verify>
    <automated>grep -q 'aria-label={label}' components/Nav.tsx && grep -q 'title="Study options"' app/study/page.tsx && grep -q 'aria-label="Undo last rating"' components/StudySession.tsx && ! grep -q 'title="Undo last rating"' components/StudySession.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'aria-label={label}' components/Nav.tsx` returns `1` (added to the bottom-tab Link).
    - `grep -q 'title="Study options"' app/study/page.tsx` succeeds; the lessons Sheet still has `title="Lessons"`.
    - `grep -q 'aria-label="Undo last rating"' components/StudySession.tsx` succeeds AND `grep -q 'title="Undo last rating"' components/StudySession.tsx` FAILS (title removed).
    - The Settings gear in Nav.tsx still reads `aria-label="Settings"` (untouched).
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Nav bottom tabs, the study mode-selector Sheet, and the Undo button all expose proper aria-labels; the Undo title attribute is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Active states + 44px touch targets — GlossProvider, AudioButton, StudySession, app/page (A11Y-03)</name>
  <files>components/GlossProvider.tsx, components/AudioButton.tsx, components/StudySession.tsx, app/page.tsx</files>
  <read_first>
    - components/GlossProvider.tsx — the close `<button aria-label="Close gloss">` (lines ~129-135, currently `w-6 h-6 ... text-xs`, no active state) and the "Add as card" `<button onClick={onAddCard}>` (lines ~176-182, currently `text-xs text-button hover:text-button-hover hover:underline`, no aria-label, no min-h, no active state). Note the `word` value is available in this component via `state.word` (destructured as `word` at line ~78). The close button is absolutely positioned (`absolute top-2 right-2`).
    - components/AudioButton.tsx — `btnBase` (line ~95, already includes `min-h-11 min-w-11`); the playing-state button class (line ~118, `text-button hover:bg-button-soft`); the idle button class (line ~132, `text-muted hover:text-muted-foreground hover:bg-surface-3`). Touch target is ALREADY 44px via min-h-11/min-w-11 — do NOT add min-h to AudioButton.
    - components/StudySession.tsx — the Undo button (lines ~550-556, `px-2 py-1 rounded-md hover:text-muted-foreground hover:bg-surface-3`) and End button (lines ~558-563, same base classes). After Task 1 the Undo already has aria-label.
    - app/page.tsx — the band-up dismiss button (lines ~159-165). It ALREADY has `min-h-11 min-w-11` and `active:bg-surface-3`; per the spec it needs ONLY `active:opacity-80` added (do not duplicate the existing classes).
    - .planning/phases/08-accessibility-pwa-baseline/08-UI-SPEC.md — "A11Y-03 — Active States & Tap Highlight" per-element table (lines ~147-160) and "Touch target fixes" table (lines ~162-169).
  </read_first>
  <action>
    Per-file edits (apply exactly the spec's target classes — do not invent variants):
    (1) GlossProvider close button — add `min-h-[44px] min-w-[44px]` and `active:bg-surface-3` to its className. Keep the existing centering (`flex items-center justify-center`), rounding, and absolute positioning; the larger hit area expands around the ✕ glyph. (Replaces the 24px `w-6 h-6` target — you may keep or drop `w-6 h-6` but the rendered tappable box must be 44px via the min-h/min-w.)
    (2) GlossProvider "Add as card" button — add a dynamic `aria-label={`Add ${word} as a card`}` (interpolating the destructured `word`), add `min-h-[44px]`, and add `active:opacity-70`. Keep the existing text label "+ Add as card" and the `text-xs text-button hover:text-button-hover hover:underline` classes. Add `flex items-center` if needed so the text vertically centers within the 44px height.
    (3) AudioButton — on the idle/play button (the default return, line ~132 class) add `active:bg-button-soft`; on the playing-state button (line ~118 class) add `active:opacity-80`. Do not change the loading state, the icons, or btnBase.
    (4) StudySession Undo button — add `min-h-[44px] px-3` and `active:bg-surface-3` to its className. The existing `px-2 py-1` may be replaced by `px-3` for horizontal padding; ensure `min-h-[44px]` sets the height. Keep `rounded-md hover:text-muted-foreground hover:bg-surface-3 transition-colors`.
    (5) StudySession End button — same as Undo: add `min-h-[44px] px-3` and `active:bg-surface-3`; keep `rounded-md hover:text-muted-foreground hover:bg-surface-3 transition-colors`. The text content "End" is its accessible name; no aria-label needed.
    (6) app/page.tsx band-up dismiss button — add ONLY `active:opacity-80` to its existing className (it already has `min-h-11 min-w-11`, `hover:bg-surface-3`, and `active:bg-surface-3`). Do not duplicate the existing classes.
    The `--button-soft` and `--surface-3` tokens already exist as Tailwind utilities (`bg-button-soft`, `bg-surface-3`) per globals.css `@theme inline`.
  </action>
  <verify>
    <automated>grep -q 'active:bg-button-soft' components/AudioButton.tsx && grep -q 'active:opacity-80' components/AudioButton.tsx && grep -q 'Add ${word} as a card' components/GlossProvider.tsx && grep -Eq 'min-h-\[44px\].*active:bg-surface-3|active:bg-surface-3.*min-h-\[44px\]' components/StudySession.tsx && grep -q 'active:opacity-80' app/page.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - GlossProvider close button className contains `min-h-[44px]`, `min-w-[44px]`, and `active:bg-surface-3`.
    - GlossProvider Add-as-card button has `aria-label={`Add ${word} as a card`}`, `min-h-[44px]`, and `active:opacity-70`; the visible text "+ Add as card" is preserved.
    - AudioButton idle/play button className contains `active:bg-button-soft`; playing-state button className contains `active:opacity-80`. `btnBase` still contains `min-h-11 min-w-11` (unchanged).
    - StudySession Undo and End buttons each contain `min-h-[44px]` and `active:bg-surface-3`.
    - app/page.tsx band-up dismiss button className contains `active:opacity-80` in addition to its pre-existing `active:bg-surface-3` and `min-h-11 min-w-11`.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>All five spec-flagged controls have explicit :active states; the gloss close, gloss add, Undo, and End controls meet 44px; no token escapes introduced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 14-screen dark-mode regression test (A11Y-04)</name>
  <files>(no files — verification only)</files>
  <read_first>
    - .planning/phases/08-accessibility-pwa-baseline/08-UI-SPEC.md — "A11Y-04 — Dark-Mode Regression Test" (lines ~171-185): the 7-screen × 2-theme protocol and the toggle-via-DevTools method.
    - app/globals.css — confirm the dark blocks (`@media (prefers-color-scheme: dark)` and `:root[data-theme="dark"]`) define `--highlight-bg`/`--highlight-fg` so the Study highlight check has a reference.
  </read_first>
  <action>
    Verification-only — write no code. Run `npm run build` (must exit 0), start the dev server, then
    walk all 7 pages (Home, Study, Cards, Habits, Settings, Wrapped, Login) in BOTH light and dark
    themes (14 screens total), checking for invisible text, harsh contrast, broken highlight colors,
    and that the newly-enlarged touch targets / active states render correctly. Record the outcome in
    the SUMMARY's A11Y-04 sign-off. If any screen regresses, do NOT mark approved — report the screen,
    theme, and issue so a follow-up fix can be planned.
  </action>
  <what-built>
    All Phase 8 accessibility edits (globals.css from plan 08-01 + the component edits in tasks 1-2
    of this plan) are now in place. This checkpoint verifies no light/dark regression was introduced
    and that the new active/touch-target changes render correctly in both themes. No code is written
    in this task — it is verification only.
  </what-built>
  <how-to-verify>
    1. Run `npm run build` and confirm it exits 0 (Prisma generate + Next build succeed).
    2. Run `npm run dev` and open http://localhost:3000.
    3. For EACH of the 7 pages — Home (/), Study (/study), Cards (/cards), Habits (/habits),
       Settings (/settings), Wrapped (/wrapped), Login (/login) — view in BOTH themes:
       toggle via Settings ▸ Appearance (System/Light/Dark), or set `data-theme="light"` then
       `data-theme="dark"` on the `<html>` element in browser DevTools.
    4. On each of the 14 screens confirm: no invisible text, no harsh/low-contrast surfaces,
       the sentence highlight (Study page) renders with its amber pairing in dark mode, and the
       newly-enlarged touch targets (gloss popover close/add on Study, Undo/End on Study,
       band-up dismiss on Home) look correct and are tappable.
    5. On the Study page specifically: tap a word to open the gloss popover and confirm the close
       (✕) button and "+ Add as card" button are comfortably tappable (44px) with a visible press
       state; tap Undo/End and confirm the press state.
  </how-to-verify>
  <resume-signal>Type "approved" if all 14 screens pass in both themes, or describe any contrast/layout/touch-target issues found.</resume-signal>
  <verify>
    <human-check>Operator confirms all 7 pages × 2 themes (14 screens) render with no invisible text, no harsh contrast, intact highlight colors, and correct active/touch-target rendering; `npm run build` exited 0.</human-check>
  </verify>
  <done>All 14 screens approved by the operator; build is clean; A11Y-04 sign-off recorded in the SUMMARY.</done>
</task>

</tasks>

<verification>
- `npm run lint` exits 0 (CLAUDE.md requires lint clean).
- `npm run build` exits 0.
- Nav bottom tabs have `aria-label={label}`; Settings gear still `aria-label="Settings"`.
- Mode-selector Sheet has `title="Study options"`; lessons Sheet still `title="Lessons"`.
- StudySession Undo uses `aria-label="Undo last rating"` and no longer has `title`.
- GlossProvider close: `min-h-[44px] min-w-[44px] active:bg-surface-3`; Add-as-card: dynamic aria-label + `min-h-[44px] active:opacity-70`.
- AudioButton idle/play: `active:bg-button-soft`; playing: `active:opacity-80`.
- StudySession Undo/End: `min-h-[44px] px-3 active:bg-surface-3`.
- app/page.tsx band-up dismiss: `active:opacity-80` added.
- A11Y-04 human checkpoint: all 7 pages × 2 themes verified, approved by operator.
</verification>

<success_criteria>
- A11Y-01: every audited interactive icon button has a meaningful aria-label (Nav tabs labelled, mode Sheet named, Undo uses aria-label not title; AudioButton/Nav gear/SwipeRow already compliant).
- A11Y-03 (per-element half): every spec-flagged control has an explicit :active state and meets 44px.
- A11Y-04: 14-screen dark-mode regression test passes (operator-approved).
- Lint and build stay clean; no new tokens, components, or copy.
</success_criteria>

<output>
Create `.planning/phases/08-accessibility-pwa-baseline/08-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 08-02)

| Artifact | File | Symbol / Change |
|----------|------|-----------------|
| Bottom-tab aria-labels | components/Nav.tsx | `aria-label={label}` on each bottom-tab `<Link>` |
| Mode-selector Sheet title | app/study/page.tsx | `title="Study options"` on the mode-selector `<Sheet>` |
| Undo accessible name | components/StudySession.tsx | `aria-label="Undo last rating"` (replaces `title`) |
| Undo/End touch + active | components/StudySession.tsx | `min-h-[44px] px-3 active:bg-surface-3` on Undo and End |
| Gloss close touch + active | components/GlossProvider.tsx | `min-h-[44px] min-w-[44px] active:bg-surface-3` on close button |
| Gloss add label + touch + active | components/GlossProvider.tsx | dynamic `aria-label={`Add ${word} as a card`}` + `min-h-[44px] active:opacity-70` |
| AudioButton active states | components/AudioButton.tsx | `active:bg-button-soft` (idle/play), `active:opacity-80` (playing) |
| Band-up dismiss active | app/page.tsx | `active:opacity-80` added to dismiss button |
| Dark-mode regression sign-off | (no file) | A11Y-04 human checkpoint pass |
