---
phase: 08-accessibility-pwa-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
autonomous: true
requirements: [A11Y-02, A11Y-03]
must_haves:
  truths:
    - "Every @keyframes-driven animation utility has a prefers-reduced-motion counterpart that disables or simplifies it (.animate-backdrop is no longer the lone exception)"
    - "Tapping any interactive element on iOS no longer flashes the default gray tap highlight (suppressed globally)"
  artifacts:
    - "app/globals.css — .animate-backdrop reduced-motion rule inside the existing @media (prefers-reduced-motion: reduce) block"
    - "app/globals.css — -webkit-tap-highlight-color: transparent on the html selector"
  key_links:
    - "The reduced-motion .animate-backdrop rule must live inside the existing @media (prefers-reduced-motion: reduce) block (around line 249-258), not a new block"
    - "The tap-highlight declaration must sit on the existing html { color-scheme: light dark; } selector (line 107-109) so it applies app-wide"
---

<objective>
Apply the two global CSS accessibility fixes for Phase 8: (1) complete reduced-motion
coverage by adding the missing `.animate-backdrop` counterpart (A11Y-02), and (2) suppress
the default iOS gray tap-flash globally via `-webkit-tap-highlight-color` (A11Y-03 global rule).

Purpose: These are single-file, no-dependency CSS edits that close the only two non-component
accessibility gaps. Doing them in their own plan keeps the component plan (08-02) free of
globals.css conflicts so both plans can run in the same wave.

Output: Two edits to `app/globals.css` — one new rule inside the reduced-motion block, one new
declaration on the `html` selector. No new tokens, no new keyframes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-accessibility-pwa-baseline/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reduced-motion counterpart for .animate-backdrop (A11Y-02)</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css — read the `@keyframes fadeBackdrop` definition (lines ~174-177), the `.animate-backdrop` utility (lines ~198-200), and the existing `@media (prefers-reduced-motion: reduce)` block (lines ~249-258). Confirm `.animate-backdrop` is the only animation utility absent from that block.
    - .planning/phases/08-accessibility-pwa-baseline/08-UI-SPEC.md — section "A11Y-02 — Reduced-Motion Coverage" (the table at lines ~123-134 lists `.animate-backdrop` as the one unhandled animation; the required fix is at lines ~136-139).
  </read_first>
  <action>
    Inside the existing `@media (prefers-reduced-motion: reduce)` block in app/globals.css (the block currently containing `.animate-reveal`, `.animate-card-in`, `.animate-burst`, `.card-flip-inner`, `.animate-sheet`, `.animate-slide-in`, `.ring-fill`), add one new rule for `.animate-backdrop` that sets `animation: none` and `opacity: 1`. Place it adjacent to the related `.animate-sheet` line so the backdrop and sheet reduced-motion rules read together. Do NOT create a second `@media (prefers-reduced-motion: reduce)` block — add the rule inside the existing one. Do NOT modify the `@keyframes fadeBackdrop` definition or the default `.animate-backdrop` utility. The reduced-motion intent: a user who requested reduced motion sees the backdrop appear fully opaque with no fade animation.
  </action>
  <verify>
    <automated>grep -A 12 'prefers-reduced-motion: reduce' app/globals.css | grep -c 'animate-backdrop' | grep -qx 1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 12 'prefers-reduced-motion: reduce' app/globals.css` shows a line matching `.animate-backdrop` with `animation: none` and `opacity: 1`.
    - The file contains exactly ONE `@media (prefers-reduced-motion: reduce)` block: `grep -c 'prefers-reduced-motion: reduce' app/globals.css` returns `1`.
    - The default `.animate-backdrop` utility (outside the media query) still reads `animation: fadeBackdrop 0.2s ease forwards;` — unchanged.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>The reduced-motion block disables the backdrop fade; all eight animation utilities now have a reduced-motion counterpart.</done>
</task>

<task type="auto">
  <name>Task 2: Suppress default iOS tap-highlight globally (A11Y-03 global rule)</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css — read the existing `html { color-scheme: light dark; }` selector (lines ~107-109) and the `body { ... }` selector (lines ~146-150) so the new declaration lands on the correct existing rule rather than creating a duplicate selector.
    - .planning/phases/08-accessibility-pwa-baseline/08-UI-SPEC.md — section "A11Y-03 — Active States & Tap Highlight", the "Global rule" paragraph (lines ~141-143): `-webkit-tap-highlight-color: transparent` must be applied globally on `html` or `body`.
  </read_first>
  <action>
    Add the declaration `-webkit-tap-highlight-color: transparent;` to the existing `html { color-scheme: light dark; }` rule in app/globals.css so the result reads `html { color-scheme: light dark; -webkit-tap-highlight-color: transparent; }` (each declaration on its own line, matching the file's formatting). Do NOT introduce a new `html { ... }` block — extend the one that already exists. This removes the gray overlay iOS Safari paints on tap for every interactive element across the whole app.
  </action>
  <verify>
    <automated>grep -c -- '-webkit-tap-highlight-color: transparent' app/globals.css | grep -qx 1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '-webkit-tap-highlight-color: transparent' app/globals.css` returns `1`.
    - The declaration sits inside the existing `html` rule that also contains `color-scheme: light dark;` (verify both appear in the same brace block).
    - No duplicate `html {` selector was introduced.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Tapping any button/link on iOS shows no gray flash; the rule applies globally from the `html` element.</done>
</task>

</tasks>

<verification>
- `npm run lint` exits 0 (project requires lint clean — CLAUDE.md).
- `grep -c 'prefers-reduced-motion: reduce' app/globals.css` returns `1` (single block, augmented not duplicated).
- `grep -A 12 'prefers-reduced-motion: reduce' app/globals.css` includes an `.animate-backdrop` rule with `animation: none`.
- `grep -c -- '-webkit-tap-highlight-color: transparent' app/globals.css` returns `1`.
- No new CSS tokens, keyframes, or selectors beyond the two edits above.
</verification>

<success_criteria>
- A11Y-02: every `@keyframes` animation utility in globals.css has a reduced-motion counterpart; `.animate-backdrop` is no longer the lone exception.
- A11Y-03 (global half): `-webkit-tap-highlight-color: transparent` is applied app-wide from `html`.
- Build and lint stay clean.
</success_criteria>

<output>
Create `.planning/phases/08-accessibility-pwa-baseline/08-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 08-01)

| Artifact | File | Symbol / Rule |
|----------|------|---------------|
| Reduced-motion backdrop rule | app/globals.css | `.animate-backdrop { animation: none; opacity: 1; }` inside `@media (prefers-reduced-motion: reduce)` |
| Global tap-highlight suppression | app/globals.css | `-webkit-tap-highlight-color: transparent;` on `html` |
