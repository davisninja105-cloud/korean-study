---
phase: 06-home-dashboard-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03]

must_haves:
  truths:
    - "Home hero renders three visually distinct states: State A (cards due, prominent count), State B (goal met today, reward-tinted celebratory), State C (all reviewed, goal not yet met, neutral)"
    - "State A, B, and C differ by at least two of: background treatment, heading size/weight, accent color, icon presence (not text-only)"
    - "Time-of-day greeting renders at text-xl font-medium text-foreground (visually prominent section header, not a small muted subtitle)"
    - "Page reads top-to-bottom: greeting → due count/heading → primary CTA → StatsBar → HabitTracker → ProficiencyArc → My Korean card"
    - "State B only appears when dailyGoalSeconds > 0 AND todaySeconds >= dailyGoalSeconds (never on a zero-goal first launch)"
    - "Primary CTA, band-up dismiss button, and My Korean link card each have an explicit active: state"
    - "npm run lint exits 0 and npm run build exits 0"
  artifacts:
    - "app/page.tsx — modified single file (no new files created)"
  key_links:
    - "loadActivity callback → /api/activity GET → setActivityData (parallel to existing loadStats)"
    - "heroState derivation useEffect watches [stats, activityData], calls habitDateStr inside the effect (never in render)"
    - "heroState value ('A'|'B'|'C'|'loading') selects which hero JSX branch renders"
---

<objective>
Rework `app/page.tsx` so the Home hero adapts to the learner's actual situation that day with three visually distinct states, elevate the time-of-day greeting to a prominent section header, and add explicit active-press feedback to the three interactive elements. This is the entire Phase 6 scope — a single file, no new packages, no new files.

Purpose: The morning-ritual entry screen must read clearly at a glance (HOME-03) and tell the learner the truth about their day — cards waiting (A), goal already met (B), or caught up but goal not yet hit (C) (HOME-01) — anchored by a greeting that has name-like authority (HOME-02). The current page conflates "done and finished" with "done but keep going" in a single two-branch conditional.

Output: A modified `app/page.tsx` with new `ActivityData` state, a `loadActivity` callback, a `heroState` derivation effect, a three-branch hero render, elevated greeting typography, and F-07 active states on the CTA, dismiss button, and My Korean card.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-home-dashboard-polish/06-UI-SPEC.md
@.planning/phases/06-home-dashboard-polish/06-RESEARCH.md

# Source files the executor will read in <read_first> on each task:
# app/page.tsx (the only file modified), app/api/activity/route.ts (response shape),
# lib/habit.ts (habitDateStr purity), app/globals.css (token availability)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add activity fetch, ActivityData state, and heroState derivation effect</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx — current implementation; you MUST replicate the existing loadStats `.then(setX)` callback shape and the existing greeting-in-effect pattern (lines 29–60). Do not invent a new fetch style.
    - app/api/activity/route.ts — confirm the GET response is exactly `{ days: Array<{ date: string; seconds: number; reviews: number }>; dailyGoalSeconds: number; dayStartHour: number }`. Type ActivityData to match.
    - lib/habit.ts — confirm `habitDateStr(dayStartHour: number, d?: Date): string` and that it calls `new Date()` internally (IMPURE). It MUST be called inside useEffect, never in render/JSX.
    - app/globals.css — no token work in this task, but confirm `--color-reward`, `--color-surface-2` exist (they do) since the loading skeleton stays `bg-surface-2`.
    - .planning/phases/06-home-dashboard-polish/06-RESEARCH.md — Pitfalls 1 (zero-goal guard) and 2 (loadActivity is mount-only, NOT refetched after sync).
  </read_first>
  <action>
    In `app/page.tsx`, add the data layer that lets the hero distinguish three states, WITHOUT changing any JSX yet (JSX is Task 2).

    1. Add a new interface near the existing `interface Stats`:
       `interface ActivityData { days: { date: string; seconds: number; reviews: number }[]; dailyGoalSeconds: number; dayStartHour: number }`.
    2. Add a `HeroState` type alias: `type HeroState = 'loading' | 'A' | 'B' | 'C'`.
    3. Add two new state hooks alongside the existing ones: `const [activityData, setActivityData] = useState<ActivityData | null>(null)` and `const [heroState, setHeroState] = useState<HeroState>('loading')`.
    4. Add a `loadActivity` callback mirroring the existing `loadStats` shape: `const loadActivity = useCallback(() => { fetch('/api/activity').then((r) => r.json()).then((data: ActivityData) => setActivityData(data)).catch(() => {}) }, [])`. Result MUST flow through `.then(setActivityData)` (react-hooks/set-state-in-effect forbids synchronous setState in the effect body).
    5. In the existing mount `useEffect`, call `loadActivity()` alongside `loadStats()`, and add `loadActivity` to its dependency array (it becomes `[loadStats, loadActivity]`). Keep the existing greeting microtask logic unchanged. Do NOT call `loadActivity` inside `handleSync` — activity data is mount-only per RESEARCH Pitfall 2.
    6. Add a NEW dedicated derivation `useEffect` with dependency array `[stats, activityData]`. Inside it: early-return if `!stats || !activityData`. Then compute `const todayStr = habitDateStr(activityData.dayStartHour)` (called HERE inside the effect — this is the only ESLint-safe place because habitDateStr is impure), `const todaySeconds = activityData.days.find((d) => d.date === todayStr)?.seconds ?? 0`, and `const goalMet = todaySeconds >= activityData.dailyGoalSeconds && activityData.dailyGoalSeconds > 0` (the `> 0` guard is mandatory — RESEARCH Pitfall 1; without it a zero-goal first launch shows State B). Then set heroState: `if (stats.dueCards > 0) setHeroState('A'); else if (goalMet) setHeroState('B'); else setHeroState('C')`. This `setHeroState` is allowed because it is conditional/derived, not an unconditional synchronous set on every render — but if lint flags it, wrap it the same way the codebase wraps greeting (`Promise.resolve().then(() => setHeroState(next))`).
    7. Add the import `import { habitDateStr } from '@/lib/habit'` at the top with the other `@/lib` imports.

    Do not touch the JSX `return (...)` block in this task. The hero still renders via the existing two-branch conditional — Task 2 replaces it. The build/lint must stay green with the new state unused-but-declared is acceptable only transiently; this task's verify allows the heroState variable to be set-but-not-yet-read.
  </action>
  <verify>
    <automated>cd /Users/main/Documents/claude-test && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx contains `interface ActivityData` with `dailyGoalSeconds: number` and `dayStartHour: number` fields
    - app/page.tsx contains `type HeroState = 'loading' | 'A' | 'B' | 'C'`
    - app/page.tsx contains `const [activityData, setActivityData] = useState<ActivityData | null>(null)`
    - app/page.tsx contains `const [heroState, setHeroState] = useState<HeroState>('loading')`
    - app/page.tsx contains `const loadActivity = useCallback(` and `fetch('/api/activity')` flowing through `.then(setActivityData)`
    - app/page.tsx contains `import { habitDateStr } from '@/lib/habit'`
    - app/page.tsx contains a useEffect with `habitDateStr(activityData.dayStartHour)` called inside the effect body (grep: habitDateStr appears, and NOT inside the JSX return)
    - The goalMet computation includes the literal guard `activityData.dailyGoalSeconds > 0`
    - `loadActivity` is NOT called inside `handleSync` (grep handleSync block contains only loadStats)
    - npm run lint exits 0
  </acceptance_criteria>
  <done>New ActivityData/heroState state, loadActivity fetch, and the [stats, activityData] derivation effect exist in app/page.tsx; habitDateStr is called only inside the effect; the dailyGoalSeconds > 0 guard is present; lint is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Render three hero states, elevate greeting typography, add active states (HOME-01/02/03 + F-07)</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx — the current JSX (lines 91–188) you are rewriting: the greeting `<p>`, the two-branch hero `<section>`, the band-up dismiss `<button>`, and the My Korean `<Link>`. Preserve the surrounding structure (pull-to-refresh indicator, sync message, StatsBar, HabitTracker, ProficiencyArc placement and order).
    - .planning/phases/06-home-dashboard-polish/06-UI-SPEC.md — the APPROVED ground truth. Use exact class strings and copy from: "Three Hero State Color Contracts", "Copywriting Contract", "Interaction Contract", "Typography" (Greeting rule, Due count rule), and "Layout & Visual Hierarchy Contract".
    - .planning/phases/06-home-dashboard-polish/06-RESEARCH.md — Pitfall 4 (active: on CTA Link) and Pitfall 5 (State B tint via INNER div bg-reward/10, not on the outer section).
    - app/globals.css — confirm `bg-reward-soft`, `text-reward`, `bg-surface-1/2/3`, `text-muted`, `text-foreground`, `bg-button`, `bg-button-hover`, `text-button-foreground` utilities are all available (verified — they are).
  </read_first>
  <action>
    Rewrite the hero JSX and the two F-07 elements in `app/page.tsx`. Use the `heroState` variable from Task 1 to select the branch (render the skeleton when `heroState === 'loading'` OR `!stats || !activityData`).

    GREETING (HOME-02 / F-08): change the greeting `<p>` from `className="text-sm text-muted"` to `className="text-xl font-medium text-foreground"`. Keep the `{greeting && (...)}` truthiness guard. This element stays at the top of the `<section>` above all three state branches.

    HERO SECTION — replace the existing two-branch conditional with a three-way render keyed on `heroState`:

    STATE A (`heroState === 'A'`) — cards due:
    - Outer panel: `<section className="bg-surface-1 rounded-2xl shadow-md p-6 flex flex-col gap-4">`.
    - Due count: a `<span>` at `text-6xl font-bold leading-none text-reward` showing `stats.dueCards`. (UI-SPEC "Due count rule" specifies text-6xl; this is the largest element on the page.) Prefer the `text-reward` utility class over the inline `style={{ color: 'var(--reward)' }}` so it reads as a token.
    - Supporting label: `<span className="text-muted text-base mb-1">card{stats.dueCards !== 1 ? 's' : ''} ready to review</span>`.
    - CTA Link to `/study`: full-width `min-h-14 bg-button text-button-foreground rounded-2xl text-lg font-semibold hover:bg-button-hover transition-colors active:scale-[0.98] active:opacity-90` with copy "Study now →".

    STATE B (`heroState === 'B'`) — goal met today:
    - Outer panel: `<section className="bg-surface-1 rounded-2xl shadow-md p-6 flex flex-col gap-4">`.
    - INNER tint wrapper (RESEARCH Pitfall 5 — tint goes on the inner div so the card border stays surface-1): `<div className="bg-reward/10 rounded-xl p-4 flex flex-col gap-3">` containing the icon + heading + supporting copy.
    - Reward icon: lucide-react `CheckCircle2` at `className="w-10 h-10 text-reward"` (text-4xl-equivalent visual weight per UI-SPEC). Add `import { CheckCircle2 } from 'lucide-react'` to the top imports.
    - Heading: `<p className="text-2xl font-semibold text-foreground">Goal met today</p>`.
    - Supporting copy: `<p className="text-sm text-muted">You've hit your daily goal. Nice work.</p>`.
    - CTA Link to `/study` BELOW the inner wrapper (inside the section, full-width): `min-h-14 bg-button text-button-foreground rounded-2xl text-base font-semibold hover:bg-button-hover transition-colors active:scale-[0.98] active:opacity-90` with copy "Study ahead →" (note `text-base` not `text-lg` — receded weight per UI-SPEC Interaction Contract).

    STATE C (`heroState === 'C'`) — all reviewed, goal not yet met:
    - Outer panel: `<section className="bg-surface-1 rounded-2xl shadow-md p-6 flex flex-col gap-4">` (NO tint — neutral state).
    - Heading: `<p className="text-xl font-semibold text-foreground">All caught up</p>`.
    - Supporting copy: `<p className="text-sm text-muted">Keep going to hit your daily goal.</p>`.
    - CTA Link to `/study`: `min-h-14 bg-button text-button-foreground rounded-2xl text-base font-semibold hover:bg-button-hover transition-colors active:scale-[0.98] active:opacity-90` with copy "Study ahead →".

    LOADING (skeleton): when `heroState === 'loading'` or stats/activityData not yet resolved, render the existing `<div className="h-28 animate-pulse bg-surface-2 rounded-xl" />`. The greeting `<p>` still renders above it.

    Copy discipline (UI-SPEC Copywriting rules): no exclamation marks or marketing language in headings. Use exactly "Goal met today" / "All caught up" / "{N} card(s) ready to review". The State B supporting line is the one approved sentence with a period.

    F-07 BAND-UP DISMISS BUTTON: add `active:bg-surface-3` to the dismiss `<button>` className (it already has `hover:bg-surface-3` and `min-h-11 min-w-11` — keep those).

    F-07 MY KOREAN LINK CARD: the `<Link href="/wrapped">` currently has `hover:shadow-lg transition-shadow`. Add `active:shadow-sm active:bg-surface-2 transition-colors` (keep the existing hover:shadow-lg and transition-shadow). Confirm the row resolves to >= 44px tap height (current `px-5 py-4` on a two-line card already exceeds 44px — no min-h needed, but verify visually).

    Keep StatsBar, HabitTracker, ProficiencyArc, and their `{stats && ...}` guards exactly as they are, in the same order. Do not reorder sections — the top-to-bottom hierarchy (greeting → hero → StatsBar → HabitTracker → ProficiencyArc → My Korean) IS HOME-03.
  </action>
  <verify>
    <automated>cd /Users/main/Documents/claude-test && npm run lint 2>&1 | tail -5 && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Greeting renders at `text-xl font-medium text-foreground` (grep: app/page.tsx contains `text-xl font-medium text-foreground` and does NOT contain `text-sm text-muted` on the greeting element)
    - app/page.tsx contains `import { CheckCircle2 } from 'lucide-react'`
    - State A branch: due count span has classes `text-6xl font-bold` and `text-reward`; CTA copy is `Study now →`
    - State B branch: contains an inner `bg-reward/10 rounded-xl` div, a `CheckCircle2` with `text-reward`, heading literal `Goal met today`, supporting literal `You've hit your daily goal. Nice work.`, CTA copy `Study ahead →` at `text-base`
    - State C branch: heading literal `All caught up`, supporting literal `Keep going to hit your daily goal.`, CTA copy `Study ahead →`
    - All three CTA Link elements contain `active:scale-[0.98] active:opacity-90`
    - Band-up dismiss button className contains `active:bg-surface-3`
    - My Korean Link className contains `active:shadow-sm` and `active:bg-surface-2`
    - Section render order unchanged: greeting/hero `<section>` precedes `StatsBar`, then `HabitTracker`, then `ProficiencyArc`, then My Korean `<Link>`
    - No heading contains an exclamation mark (grep: the strings "Amazing!", "✓" celebratory heading removed; "All caught up ✓" no longer present)
    - npm run lint exits 0
    - npm run build exits 0
  </acceptance_criteria>
  <done>The hero renders three distinct branches via heroState; greeting is text-xl font-medium text-foreground; State B uses an inner reward-tinted div + CheckCircle2; all CTAs and the two F-07 elements have active states; section order is unchanged; lint and build are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/activity (GET) | Activity data crosses here; already auth-gated by middleware.ts (ks_auth cookie). No new boundary introduced. |
| client → /api/stats (GET) | Existing; unchanged this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | /api/activity GET response rendered on Home | low | accept | Endpoint already guarded by the shared-password auth cookie via middleware.ts; single-tenant app; data shown is the user's own study seconds. No new exposure. |
| T-06-02 | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: none). No T-06-SC checkpoint required. |
| T-06-03 | Denial of Service | extra /api/activity fetch on mount | low | accept | One additional read-only GET on page mount, mount-only (not refetched on sync). Negligible load; no loop. |
</threat_model>

<verification>
- `npm run lint` exits 0 (react-hooks/purity and react-hooks/set-state-in-effect both pass — habitDateStr is called only inside useEffect; all setState flows through callbacks).
- `npm run build` exits 0.
- Manual browser check (RESEARCH Validation Architecture — these are manual-only by nature of a client component with live fetches):
  1. Open app with cards due → State A: large text-reward due count + "Study now →".
  2. Open app after clearing all due cards with daily goal met → State B: reward-tinted inner panel + CheckCircle2 + "Goal met today" + "Study ahead →".
  3. Open app after clearing all due cards but daily goal NOT met → State C: neutral panel + "All caught up" + "Keep going to hit your daily goal." + "Study ahead →".
  4. First launch with no configured daily goal (dailyGoalSeconds = 0), zero study → State C (NOT State B) — confirms the > 0 guard.
  5. Greeting reads large and prominent (text-xl) at the top of the hero in all states.
</verification>

<success_criteria>
- HOME-01: Hero shows three visually distinct states (A/B/C) distinguishable by at least two of background, heading weight, accent, icon.
- HOME-02: Greeting renders at text-xl font-medium text-foreground.
- HOME-03: Page reads greeting → due count/heading → CTA → StatsBar → HabitTracker → ProficiencyArc → My Korean, top to bottom.
- F-07: CTA, band-up dismiss, and My Korean card all have explicit active: states.
- lint and build both green; no new files, no new packages.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New symbols added to `app/page.tsx`:
- `interface ActivityData` — `{ days: { date: string; seconds: number; reviews: number }[]; dailyGoalSeconds: number; dayStartHour: number }`
- `type HeroState = 'loading' | 'A' | 'B' | 'C'`
- State variable `activityData` (`useState<ActivityData | null>`) with setter `setActivityData`
- State variable `heroState` (`useState<HeroState>`) with setter `setHeroState`
- `loadActivity` — `useCallback` fetching `/api/activity` → `setActivityData`
- A new derivation `useEffect([stats, activityData])` computing `todayStr` / `todaySeconds` / `goalMet` and setting `heroState`
- New import `habitDateStr` from `@/lib/habit`
- New import `CheckCircle2` from `lucide-react`
- New JSX: three hero state branches (State A / B / C) replacing the prior two-branch conditional; State B inner `bg-reward/10` tint wrapper with `CheckCircle2`

Modified existing JSX in `app/page.tsx`:
- Greeting `<p>` className: `text-sm text-muted` → `text-xl font-medium text-foreground`
- Primary CTA Link(s): added `active:scale-[0.98] active:opacity-90`
- Band-up dismiss `<button>`: added `active:bg-surface-3`
- My Korean `<Link>`: added `active:shadow-sm active:bg-surface-2 transition-colors`

No new files. No new packages. `app/globals.css` untouched (all required tokens already exist).
</artifacts_produced>

<output>
Create `.planning/phases/06-home-dashboard-polish/06-01-SUMMARY.md` when done
</output>
