---
phase: 04-design-system-tokens-sweep
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/page.tsx
  - app/study/page.tsx
  - app/cards/page.tsx
  - app/habits/page.tsx
  - app/settings/page.tsx
  - app/wrapped/page.tsx
autonomous: true
requirements: [DS-03]
must_haves:
  truths:
    - "[D-05] Every gray-* / bg-white / ring-offset-white utility in the six app/ page files is replaced with its semantic token equivalent per the UI-SPEC replacement map"
    - "Light-mode appearance is visually unchanged (token values are direct analogs of the gray utilities they replace)"
    - "Dark mode renders correctly for all six pages (no token escapes drift)"
  artifacts:
    - "app/page.tsx, app/study/page.tsx, app/cards/page.tsx, app/habits/page.tsx, app/settings/page.tsx, app/wrapped/page.tsx with zero gray-*/bg-white escapes"
  key_links:
    - "Depends on Plan 1: text-muted, text-muted-foreground, border-border utilities must already exist in @theme inline"
    - "inputCls multi-line string constant in cards/page.tsx must be replaced as a whole (RESEARCH Pitfall 5)"
---

<!-- This is a token-sweep plan: actions must name the legacy utility classes that the verify
     greps negative-assert. The literals below are part of the verification regexes themselves. -->
<!-- planner-discipline-allow: gray- -->
<!-- planner-discipline-allow: bg-white -->
<!-- planner-discipline-allow: ring-offset-white -->

<objective>
Sweep all six remaining `app/` page files (login was done in Plan 1), replacing every `bg-gray-*`, `text-gray-*`, `border-gray-*`, `bg-white`, and `ring-offset-white` utility with the semantic token equivalent from the authoritative UI-SPEC replacement map.

This is a mechanical class-string substitution pass — no logic, API, or layout changes. The replacement map is fixed; the executor must not invent mappings. Wave 2 is gated on Plan 1 because the target utilities (`text-muted`, `border-border`, etc.) only exist after Plan 1 registers them in `@theme inline`.

Purpose: Resolve DS-03 for the app-pages half of the codebase (findings F-06, F-09, F-16, F-19, F-21, plus the wrapped-page gray text). After this plan, no app/ page (except the intentionally-kept SVG track) drifts in dark mode due to token escapes.
Output: Six swept page files. Combined with Plan 3 (components), `grep -rn 'bg-gray-\|text-gray-\|border-gray-' app/ components/` returns 0.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-system-tokens-sweep/04-UI-SPEC.md
@.planning/phases/04-design-system-tokens-sweep/04-RESEARCH.md
@.planning/phases/04-design-system-tokens-sweep/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep app/page.tsx and app/wrapped/page.tsx</name>
  <files>app/page.tsx, app/wrapped/page.tsx</files>
  <read_first>
    - app/page.tsx (the file being modified — ~11 gray instances across pull-refresh, band-up banner, hero, link card; hover:bg-gray-100 dark:hover:bg-gray-700 at L116)
    - app/wrapped/page.tsx (the file being modified — ~9 text-gray instances; note: the hero gradient hardcoded hex at L125 is F-23, OUT of scope per D-07 — do NOT touch it)
    - .planning/phases/04-design-system-tokens-sweep/04-UI-SPEC.md ("Token Escape Sweep — Replacement Map" semantic-equivalents table — the authoritative mapping)
    - .planning/phases/04-design-system-tokens-sweep/04-RESEARCH.md ("Gray Utility Replacement Pattern" table with verified line numbers)
  </read_first>
  <action>
    Apply the UI-SPEC replacement map to both files. Use this exact mapping for every occurrence:
    `text-gray-500 dark:text-gray-400` → `text-muted`;
    `text-gray-400 dark:text-gray-500` → `text-muted`;
    `text-gray-600 dark:text-gray-300` → `text-muted-foreground`;
    `text-gray-700 dark:text-gray-200` → `text-muted-foreground`;
    `text-gray-800 dark:text-gray-100` → `text-foreground`;
    `text-gray-900 dark:text-gray-100` → `text-foreground`;
    `bg-gray-100 dark:bg-gray-800` → `bg-surface-3`;
    `bg-gray-200 dark:bg-gray-700` → `bg-surface-3`;
    `hover:bg-gray-100 dark:hover:bg-gray-700` → `hover:bg-surface-3`;
    `border-gray-200 dark:border-gray-700` → `border-border`;
    `border-gray-100 dark:border-gray-700` → `border-border`.

    For app/wrapped/page.tsx: replace the text-gray-* utilities only. The hero gradient `linear-gradient(135deg, #3b82f6 …)` at ~L125 is finding F-23, deferred to Phase 7 per locked decision D-07 — leave it exactly as-is.

    Do not change any layout, spacing, copy, or logic — only color/border class tokens.
  </action>
  <verify>
    <automated>cd /Users/main/Documents/claude-test && test "$(grep -c 'gray-\|bg-white \|bg-white"\|ring-offset-white' app/page.tsx)" -eq 0 && test "$(grep -c 'text-gray-\|bg-gray-\|border-gray-\|bg-white \|bg-white"' app/wrapped/page.tsx)" -eq 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gray-\|bg-white \|bg-white"\|ring-offset-white' app/page.tsx` returns 0
    - `grep -c 'text-gray-\|bg-gray-\|border-gray-\|bg-white \|bg-white"' app/wrapped/page.tsx` returns 0
    - `grep -c 'linear-gradient(135deg, #3b82f6' app/wrapped/page.tsx` still returns 1 (F-23 gradient untouched, deferred to Phase 7)
    - `npm run lint` passes clean
  </acceptance_criteria>
  <done>Both files have zero gray-*/bg-white escapes; all secondary text uses text-muted/text-muted-foreground/text-foreground; the wrapped hero gradient (F-23) is untouched; lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep app/study/page.tsx and app/cards/page.tsx (including multi-line inputCls)</name>
  <files>app/study/page.tsx, app/cards/page.tsx</files>
  <read_first>
    - app/study/page.tsx (the file being modified — skeleton loaders at L194-296 use bg-gray-200/100/700/800; session-complete text-gray-*; note text-green-500 at ~L407 is F-10, OUT of scope per D-06 — do NOT touch)
    - app/cards/page.tsx (the file being modified — the multi-line `inputCls` constant at L167-169 concatenates border-gray-300/bg-white/text-gray-900; segmented control bg-gray-100/bg-white at L212/219; ~25 instances total)
    - .planning/phases/04-design-system-tokens-sweep/04-UI-SPEC.md (replacement map + cards row in "File-by-file sweep targets")
    - .planning/phases/04-design-system-tokens-sweep/04-RESEARCH.md ("Pitfall 5: inputCls String Constant Missed" — replace the whole multi-line string)
  </read_first>
  <action>
    Apply the replacement map (same mapping as Task 1) plus these control-specific entries:
    `bg-white dark:bg-gray-900` → `bg-surface-1`;
    `bg-white dark:bg-gray-700` → `bg-surface-1`;
    `border-gray-300 dark:border-gray-600` → `border-border`;
    `border-gray-200/60 dark:border-gray-700/60` → `border-border/60`.

    For app/study/page.tsx: replace skeleton-loader grays (`bg-gray-200 dark:bg-gray-700` and similar) with `bg-surface-3` (the semantic zero-state background). Leave `text-green-500` at ~L407 (correct-count stat) unchanged — it is F-10, deferred to Phase 5 per D-06, and is not a gray utility.

    For app/cards/page.tsx: handle the multi-line `inputCls` constant at L167-169 (RESEARCH Pitfall 5) by replacing the ENTIRE concatenated string — `border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 …` becomes `border border-border bg-surface-1 text-foreground …` (preserve the trailing rounded/padding/text-sm classes). The segmented control: `bg-gray-100 dark:bg-gray-800` → `bg-surface-3` (track), active tab `bg-white dark:bg-gray-700` → `bg-surface-1`. Apply the standard text/border map to all remaining instances.

    No logic, layout, or behavior changes — class tokens only.
  </action>
  <verify>
    <automated>cd /Users/main/Documents/claude-test && test "$(grep -c 'gray-\|bg-white \|bg-white"' app/cards/page.tsx)" -eq 0 && test "$(grep -c 'bg-gray-\|text-gray-\|border-gray-\|bg-white \|bg-white"' app/study/page.tsx)" -eq 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gray-\|bg-white \|bg-white"' app/cards/page.tsx` returns 0
    - `grep -c 'bg-gray-\|text-gray-\|border-gray-\|bg-white \|bg-white"' app/study/page.tsx` returns 0
    - `grep -c 'inputCls' app/cards/page.tsx` is unchanged (constant still defined and used; only its value swept)
    - `grep -c 'text-green-500' app/study/page.tsx` still returns 1 (F-10 untouched, deferred to Phase 5)
    - `npm run lint` passes clean
  </acceptance_criteria>
  <done>Both files have zero gray-*/bg-white escapes; the cards inputCls constant uses border-border/bg-surface-1/text-foreground; study skeletons use bg-surface-3; text-green-500 (F-10) is untouched; lint passes.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep app/habits/page.tsx and app/settings/page.tsx (form controls + toggle)</name>
  <files>app/habits/page.tsx, app/settings/page.tsx</files>
  <read_first>
    - app/habits/page.tsx (the file being modified — ~34 instances: bar/legend bg-gray-* at L217/256, border-gray-100/700 at L179/185, text-gray-* throughout)
    - app/settings/page.tsx (the file being modified — ~41 instances: select bg-white/gray-900 + border-gray-300/600 at L182/208/231/382/414; toggle inactive track bg-gray-300/600 at L294; toggle thumb bg-white; palette borders border-gray-300/600 and border-gray-200/800; border-gray-100/700 at L354)
    - .planning/phases/04-design-system-tokens-sweep/04-UI-SPEC.md (replacement map; habits + settings rows in "File-by-file sweep targets"; "Interaction Contract" — toggle inactive must stay visually distinct from active bg-button)
    - .planning/phases/04-design-system-tokens-sweep/04-RESEARCH.md (replacement table verified line numbers)
  </read_first>
  <action>
    Apply the full replacement map (Task 1 + Task 2 mappings) plus:
    `bg-gray-300 dark:bg-gray-600` → `bg-surface-3` (toggle inactive track);
    `bg-white dark:bg-gray-900` → `bg-surface-1` (form selects, color-picker background);
    `border-gray-300 dark:border-gray-600` → `border-border` (form control + palette borders);
    `border-gray-100 dark:border-gray-700` → `border-border`.

    For app/settings/page.tsx form controls (F-21): the select/input backgrounds become `bg-surface-1` with `border-border`. The toggle inactive track (`bg-gray-300 dark:bg-gray-600`) becomes `bg-surface-3`; the toggle thumb hardcoded `bg-white` becomes `bg-surface-1`. Per the UI-SPEC Interaction Contract, after substitution the inactive `bg-surface-3` track must remain visually distinct from the active `bg-button` state — this holds because surface-3 and the action color are far apart in both themes.

    For palette-swatch borders (the App-colors grid): any `border-gray-300 dark:border-gray-600` or `border-gray-200 dark:border-gray-800` selection borders map to `border-border`.

    For app/habits/page.tsx (F-19): zero-bar/legend `bg-gray-100 dark:bg-gray-700` and `bg-gray-100 dark:bg-gray-800` → `bg-surface-3`; legend/divider `border-gray-*` → `border-border`; text per the standard map. Do NOT touch any `bg-reward`/`bg-reward-soft` bar fills — those are already correct tokens.

    No logic, layout, or behavior changes — class tokens only.
  </action>
  <verify>
    <automated>cd /Users/main/Documents/claude-test && test "$(grep -c 'gray-\|bg-white \|bg-white"' app/habits/page.tsx)" -eq 0 && test "$(grep -c 'gray-\|bg-white \|bg-white"' app/settings/page.tsx)" -eq 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gray-\|bg-white \|bg-white"' app/habits/page.tsx` returns 0
    - `grep -c 'gray-\|bg-white \|bg-white"' app/settings/page.tsx` returns 0
    - Settings toggle inactive track uses `bg-surface-3` and active uses `bg-button` (still visually distinct)
    - `bg-reward` / `bg-reward-soft` references in habits are unchanged (count not reduced)
    - `npm run lint` passes clean
  </acceptance_criteria>
  <done>Both files have zero gray-*/bg-white escapes; settings form controls use bg-surface-1/border-border, the toggle inactive track uses bg-surface-3 (distinct from active bg-button); habits bars/legends use bg-surface-3/border-border with reward tokens untouched; lint passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time CSS / PostCSS | Class-string substitutions are validated by the Tailwind build; an unregistered utility produces no CSS (visual, not security, defect) |
| client ↔ server (pages) | These are RSC/client page files; the sweep changes only className strings — no data fetching, validation, or API contract is touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | app/ page className strings | low | accept | Pure class substitution; a wrong class is a visual regression caught by the dark-mode review + lint, with no data or auth impact (RESEARCH Security Domain: zero security-sensitive changes this phase) |

No npm/pip/cargo installs in this plan — package legitimacy gate not applicable.
</threat_model>

<verification>
After all tasks (combined with Plan 1 + Plan 3 this yields the phase-wide 0-hit grep):
1. `grep -rn 'bg-gray-\|text-gray-\|border-gray-\|bg-white \|bg-white"\|ring-offset-white' app/page.tsx app/study/page.tsx app/cards/page.tsx app/habits/page.tsx app/settings/page.tsx app/wrapped/page.tsx` returns 0 hits
2. F-23 gradient in wrapped and text-green-500 in study are both still present (correctly deferred)
3. `npm run build` and `npm run lint` pass clean
</verification>

<success_criteria>
- All six app/ page files have zero gray-*/bg-white/ring-offset-white escapes
- Deferred items (F-23 wrapped gradient, F-10 study text-green-500) remain untouched
- Light-mode appearance unchanged; dark mode renders correctly
- Build and lint clean
</success_criteria>

<output>
Create `.planning/phases/04-design-system-tokens-sweep/04-02-SUMMARY.md` when done
</output>
