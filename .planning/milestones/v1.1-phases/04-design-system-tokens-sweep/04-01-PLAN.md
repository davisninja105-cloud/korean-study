---
phase: 04-design-system-tokens-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/login/page.tsx
autonomous: true
requirements: [DS-01, DS-02]
must_haves:
  truths:
    - "--muted, --muted-foreground, and --border tokens are defined in all three globals.css CSS blocks (light :root, @media dark, [data-theme=dark]) and registered in @theme inline"
    - "The sentence highlight (--highlight-bg/--highlight-fg) renders as dark amber background with light amber text in both dark blocks, no longer harsh"
    - "[D-04] The login password input uses semantic tokens and a --button-derived focus ring instead of hardcoded gray/blue utilities"
  artifacts:
    - "app/globals.css with --muted, --muted-foreground, --border tokens + @theme inline registrations + dark --highlight-bg/--highlight-fg"
    - "app/login/page.tsx with no gray-*/bg-white/focus:ring-blue-* utilities"
  key_links:
    - "@theme inline registration (--color-muted etc.) is what makes text-muted/border-border Tailwind utilities exist for Plans 2 and 3"
    - "Dark values must appear in BOTH @media dark AND [data-theme=dark] or the manual theme toggle breaks"
---

<!-- This is a token-sweep plan: actions must name the legacy utility classes that the verify
     greps negative-assert. The literals below are part of the verification regexes themselves. -->
<!-- planner-discipline-allow: gray- -->
<!-- planner-discipline-allow: bg-white -->
<!-- planner-discipline-allow: focus:ring-blue- -->

<objective>
Establish the complete semantic-token foundation that the entire phase depends on, and resolve both P0 gate findings (F-02 dark highlight, F-25 login focus ring).

This plan does three things in `app/globals.css`: (1) adds the new `--muted`, `--muted-foreground`, and `--border` tokens in all three CSS blocks plus `@theme inline` so Tailwind generates `text-muted`, `text-muted-foreground`, and `border-border` utilities (DS-01); (2) adds dark-mode values for the pre-existing `--highlight-bg`/`--highlight-fg` tokens using the locked reversed-amber pairing (DS-02, F-02 P0). It then fully sweeps `app/login/page.tsx`, including the F-25 P0 focus-ring fix.

Purpose: The token utilities created here MUST exist before Plans 2 and 3 can replace gray utilities with them. This plan is the hard Wave-1 gate. The two P0 findings (F-02, F-25) are resolved here so the Phase 3 P0 gate stays satisfied before Phase 5 begins.
Output: `app/globals.css` (tokens + dark highlight) and a fully-swept `app/login/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-system-tokens-sweep/04-CONTEXT.md
@.planning/phases/04-design-system-tokens-sweep/04-RESEARCH.md
@.planning/phases/04-design-system-tokens-sweep/04-UI-SPEC.md
@app/globals.css
@app/login/page.tsx
@components/AudioButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --muted, --muted-foreground, --border tokens to all 3 blocks + @theme inline (DS-01)</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (the file being modified — note the 3 token blocks at lines 17-49 light :root, 51-61 @media dark, 66-73 [data-theme="dark"], and @theme inline at 87-115)
    - .planning/phases/04-design-system-tokens-sweep/04-RESEARCH.md ("globals.css Structure — Exact Map" and "Adding New Token to globals.css" code example with exact hex values)
    - .planning/phases/04-design-system-tokens-sweep/04-UI-SPEC.md ("New Tokens to Add in Phase 4" — per-block value tables)
  </read_first>
  <action>
    Add three new tokens following the existing two-step Tailwind v4 pattern (CSS variable definition + @theme inline registration). Use these exact hex values per block.

    In the light `:root` block (after the `--cat-phrase: #14b8a6;` line, around line 45), add:
    `--muted: #6b7280;` (gray-500 analog, secondary text), `--muted-foreground: #4b5563;` (gray-600 analog, medium-emphasis), `--border: #e5e7eb;` (gray-200 analog, default border).

    In the `@media (prefers-color-scheme: dark) :root` block (after `--surface-3: #0b0f1a;`, around line 59), add the dark overrides:
    `--muted: #9ca3af;` (gray-400 analog), `--muted-foreground: #d1d5db;` (gray-300 analog), `--border: #374151;` (gray-700 analog).

    In the `:root[data-theme="dark"]` block (after `--surface-3: #0b0f1a;`, around line 71), mirror the SAME dark values exactly: `--muted: #9ca3af;`, `--muted-foreground: #d1d5db;`, `--border: #374151;`. This mirror is mandatory per CLAUDE.md "When adding a dark value, mirror it in BOTH the @media block AND the [data-theme=dark] block" — omitting it breaks the manual theme toggle.

    Do NOT add entries to the `:root[data-theme="light"]` block — its light values equal the base `:root` values, so no override is needed (per RESEARCH.md).

    In the `@theme inline` block (after `--color-cat-phrase: var(--cat-phrase);`, around line 114), register all three for Tailwind utility generation: `--color-muted: var(--muted);`, `--color-muted-foreground: var(--muted-foreground);`, `--color-border: var(--border);`. Use the `--color-*: var(--*)` form (NOT `--*-color`) — this is required for opacity modifiers like `border-border/60` to work via color-mix (RESEARCH Pitfall 3).
  </action>
  <verify>
    <automated>cd /Users/main/Documents/claude-test && test "$(grep -c '\-\-muted:' app/globals.css)" -eq 3 && test "$(grep -c '\-\-muted-foreground:' app/globals.css)" -eq 3 && test "$(grep -c '\-\-border:' app/globals.css)" -eq 3 && test "$(grep -c '\-\-color-muted:' app/globals.css)" -eq 1 && test "$(grep -c '\-\-color-muted-foreground:' app/globals.css)" -eq 1 && test "$(grep -c '\-\-color-border:' app/globals.css)" -eq 1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\-\-muted:' app/globals.css` returns exactly 3 (light + @media dark + [data-theme=dark])
    - `grep -c '\-\-muted-foreground:' app/globals.css` returns exactly 3
    - `grep -c '\-\-border:' app/globals.css` returns exactly 3
    - `grep -c '\-\-color-muted:' app/globals.css` returns 1, `grep -c '\-\-color-muted-foreground:' app/globals.css` returns 1, `grep -c '\-\-color-border:' app/globals.css` returns 1 (each registered once in @theme inline)
    - `npm run build` succeeds (confirms Tailwind generates the new utilities without error)
  </acceptance_criteria>
  <done>All three new tokens exist with the specified hex values in the light :root, @media dark, and [data-theme="dark"] blocks, and each is registered in @theme inline as --color-muted / --color-muted-foreground / --color-border. The build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix dark-mode --highlight-bg / --highlight-fg with reversed amber pairing (DS-02, F-02 P0)</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (lines 38-40 light highlight tokens — keep unchanged; lines 51-61 @media dark block; lines 66-73 [data-theme="dark"] block)
    - .planning/phases/04-design-system-tokens-sweep/04-CONTEXT.md (D-01, D-02, D-03 locked decisions)
    - .planning/phases/04-design-system-tokens-sweep/04-RESEARCH.md ("Fixing Dark Highlight Token (DS-02)" code example; contrast verified at 7.28:1 AAA)
  </read_first>
  <action>
    Add dark-mode values for the pre-existing `--highlight-bg` and `--highlight-fg` tokens (per locked decision D-01: reversed amber pairing — same colors as light, swapped roles).

    Do NOT change the light `:root` values at lines 39-40 (`--highlight-bg: #fde68a;` amber-200, `--highlight-fg: #78350f;` amber-900) — they stay as-is.

    In the `@media (prefers-color-scheme: dark) :root` block (after the `--surface-3` and new --muted/--border lines from Task 1), add:
    `--highlight-bg: #78350f;` (amber-900, the reversed background), `--highlight-fg: #fde68a;` (amber-200, the reversed text).

    In the `:root[data-theme="dark"]` block, mirror the same two lines: `--highlight-bg: #78350f;`, `--highlight-fg: #fde68a;`.

    Contrast (D-03): amber-200 (#fde68a) text on amber-900 (#78350f) background was independently verified at 7.28:1 — passes WCAG AAA. No nudge to the values is needed; use them exactly.

    No `@theme inline` change is needed — `--color-highlight-bg`/`--color-highlight-fg` are already registered at lines 108-109. No component change is needed — `components/HighlightedSentence.tsx` reads these via `var(--highlight-bg)`/`var(--highlight-fg)` inline. Per D-02, the grammar-particle tint inherits from `--highlight-bg`, so fixing the parent token is sufficient; do NOT add a separate particle dark token.
  </action>
  <verify>
    <automated>cd /Users/main/Documents/claude-test && test "$(grep -c 'highlight-bg: #78350f' app/globals.css)" -eq 2 && test "$(grep -c 'highlight-fg: #fde68a' app/globals.css)" -eq 2 && test "$(grep -c 'highlight-bg: #fde68a' app/globals.css)" -eq 1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'highlight-bg: #78350f' app/globals.css` returns 2 (one in @media dark, one in [data-theme=dark])
    - `grep -c 'highlight-fg: #fde68a' app/globals.css` returns 2
    - The light `:root` still has `--highlight-bg: #fde68a;` (1 hit) and `--highlight-fg: #78350f;` (1 hit) — light values unchanged
    - `grep -n 'highlight-bg\|highlight-fg' app/globals.css | wc -l` returns at least 8 (2 tokens × {light + @media + [data-theme] + @theme inline})
    - No new particle token added (no `--highlight-particle` or similar in the file)
  </acceptance_criteria>
  <done>Both dark blocks contain --highlight-bg: #78350f and --highlight-fg: #fde68a; light values are untouched; no separate particle token was introduced. The sentence highlight will render dark amber on light amber text in dark mode.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep app/login/page.tsx — fix F-25 focus ring (P0) + gray utilities (DS-03)</name>
  <files>app/login/page.tsx</files>
  <read_first>
    - app/login/page.tsx (the file being modified — lines 41, 42, 51 hold the gray/blue escapes)
    - components/AudioButton.tsx (reference: already uses focus:ring-button/50 — the established pattern for F-25)
    - .planning/phases/04-design-system-tokens-sweep/04-UI-SPEC.md ("Token Escape Sweep — Replacement Map" semantic-equivalents table; "File-by-file sweep targets" login row)
    - .planning/phases/04-design-system-tokens-sweep/04-RESEARCH.md ("F-25 Fix Pattern" before/after code example)
  </read_first>
  <action>
    Replace every gray/blue utility in login per the UI-SPEC replacement map. The new `text-muted`/`text-foreground`/`border-border`/`bg-surface-1` utilities exist after Tasks 1-2 (Plan 1 Wave 1). Make exactly these substitutions:

    Line 41 heading: `text-gray-800 dark:text-gray-100` → `text-foreground`.
    Line 42 subtitle: `text-gray-500 dark:text-gray-400` → `text-muted`.
    Line 51 input (the F-25 P0 fix): `border-gray-300 dark:border-gray-600` → `border-border`; `bg-white dark:bg-gray-900` → `bg-surface-1`; `text-gray-900 dark:text-gray-100` → `text-foreground`; and `focus:ring-blue-300` → `focus:ring-button/50` (matching the AudioButton pattern so the focus ring follows the user's configurable action color).

    Leave `text-red-500 dark:text-red-400` (error text, line 60) unchanged — it is a semantic error color, not a gray escape and not in scope. Do not touch any layout, spacing, or logic.
  </action>
  <verify>
    <automated>cd /Users/main/Documents/claude-test && test "$(grep -c 'gray-\|bg-white\|focus:ring-blue-' app/login/page.tsx)" -eq 0 && grep -q 'focus:ring-button/50' app/login/page.tsx && grep -q 'bg-surface-1' app/login/page.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gray-\|bg-white\|focus:ring-blue-' app/login/page.tsx` returns 0
    - `grep -q 'focus:ring-button/50' app/login/page.tsx` matches (F-25 resolved)
    - The input line now contains `border-border`, `bg-surface-1`, `text-foreground`, and `focus:ring-button/50`
    - `text-red-500 dark:text-red-400` is still present (error text untouched)
    - `npm run lint` passes clean
  </acceptance_criteria>
  <done>app/login/page.tsx contains zero gray-*/bg-white/focus:ring-blue-* utilities; the password input uses focus:ring-button/50 (F-25 P0 resolved), border-border, bg-surface-1, and text-foreground; error text is unchanged; lint passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time CSS / PostCSS | Tailwind v4 generates utility classes from @theme inline; malformed tokens fail the build (caught immediately, not a security risk) |
| client → /api/login | Pre-existing auth boundary; this plan only restyles the login input's visual focus ring — no change to data flow, validation, or the auth route |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | app/globals.css token values | low | accept | Token values are static CSS hex; a wrong value is a visual regression caught by build + dark-mode review, not a security defect |
| T-04-02 | Information Disclosure | app/login/page.tsx focus-ring change | low | accept | The F-25 fix only swaps the focus-ring Tailwind class; auth logic in lib/auth.ts and app/api/login/route.ts is untouched (RESEARCH Security Domain: zero security-sensitive changes) |

No npm/pip/cargo installs in this plan — package legitimacy gate not applicable.
</threat_model>

<verification>
After all tasks:
1. `grep -c '\-\-muted:' app/globals.css` == 3; `grep -c '\-\-border:' app/globals.css` == 3 (DS-01)
2. `grep -n 'highlight-bg\|highlight-fg' app/globals.css | wc -l` >= 8; both dark blocks have #78350f bg / #fde68a fg (DS-02)
3. `grep -c 'gray-\|bg-white\|focus:ring-blue-' app/login/page.tsx` == 0 (login swept, F-25 fixed)
4. `npm run build` succeeds (Tailwind generates text-muted/border-border utilities)
5. `npm run lint` passes clean
</verification>

<success_criteria>
- `--muted`, `--muted-foreground`, `--border` exist in all 3 globals.css blocks + @theme inline
- `--highlight-bg`/`--highlight-fg` have correct reversed-amber dark values in both dark blocks
- Login page has zero gray/blue escapes; F-25 P0 focus ring uses `focus:ring-button/50`
- Build and lint are clean; the new Tailwind utilities are available for Plans 2 and 3
</success_criteria>

<output>
Create `.planning/phases/04-design-system-tokens-sweep/04-01-SUMMARY.md` when done
</output>
