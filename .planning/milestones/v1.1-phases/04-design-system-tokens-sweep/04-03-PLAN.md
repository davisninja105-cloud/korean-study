---
phase: 04-design-system-tokens-sweep
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - components/StudySession.tsx
  - components/ModeSelector.tsx
  - components/Sheet.tsx
  - components/AudioButton.tsx
  - components/GlossProvider.tsx
  - components/HabitTracker.tsx
  - components/ProficiencyArc.tsx
  - components/Nav.tsx
  - components/CardEditor.tsx
  - components/LessonRangeFilter.tsx
  - components/StatsBar.tsx
  - components/MilestoneCelebration.tsx
  - components/HabitHeatmap.tsx
  - components/SyncPanel.tsx
autonomous: true
requirements: [DS-03]
must_haves:
  truths:
    - "[D-05] Every gray-* / bg-white / ring-offset-white utility in the 14 component files is replaced with its semantic token equivalent per the UI-SPEC replacement map"
    - "The 6 RESEARCH-discovered files (Nav, CardEditor, LessonRangeFilter, StatsBar, MilestoneCelebration, HabitHeatmap) and SyncPanel are swept so the phase-wide grep returns 0"
    - "Intentional non-changes are preserved: ProgressRing SVG track and Sheet bg-black/40 backdrop"
  artifacts:
    - "14 component files with zero gray-*/bg-white/ring-offset-white escapes (except the documented SVG track and modal backdrop)"
  key_links:
    - "Depends on Plan 1: text-muted / text-muted-foreground / border-border must already exist"
    - "HabitTracker ring-offset-white must become ring-offset-surface-1 (not caught by a bare gray-* grep — RESEARCH Pitfall 4)"
    - "inputCls constants in CardEditor.tsx and LessonRangeFilter.tsx must be replaced as whole strings (RESEARCH Pitfall 5)"
---

<!-- This is a token-sweep plan: actions must name the legacy utility classes that the verify
     greps negative-assert. The literals below are part of the verification regexes themselves. -->
<!-- planner-discipline-allow: gray- -->
<!-- planner-discipline-allow: bg-white -->
<!-- planner-discipline-allow: ring-offset-white -->

<objective>
Sweep all 14 component files, replacing every `bg-gray-*`, `text-gray-*`, `border-gray-*`, `bg-white`, and `ring-offset-white` utility with the semantic token equivalent from the authoritative UI-SPEC replacement map — while preserving the two documented intentional non-changes.

This includes the 6 files RESEARCH discovered that are NOT in the UI-SPEC sweep table (Nav.tsx, CardEditor.tsx, LessonRangeFilter.tsx, StatsBar.tsx, MilestoneCelebration.tsx, HabitHeatmap.tsx) plus SyncPanel.tsx — if these are skipped, the final phase-wide grep fails. It also includes HabitTracker's undocumented escapes (`bg-white dark:bg-gray-800` and `ring-offset-white dark:ring-offset-gray-800`), which a bare gray-* grep does not catch.

Purpose: Resolve DS-03 for the components half of the codebase (findings F-13, F-27, plus all discovered escapes). Wave 2, parallel with Plan 2 — no file overlap. Combined with Plans 1 and 2, the phase-wide `grep -rn 'bg-gray-\|text-gray-\|border-gray-' app/ components/` returns 0.
Output: 14 swept component files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-system-tokens-sweep/04-UI-SPEC.md
@.planning/phases/04-design-system-tokens-sweep/04-RESEARCH.md
@.planning/phases/04-design-system-tokens-sweep/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep StudySession.tsx, ModeSelector.tsx, Sheet.tsx, AudioButton.tsx, GlossProvider.tsx</name>
  <files>components/StudySession.tsx, components/ModeSelector.tsx, components/Sheet.tsx, components/AudioButton.tsx, components/GlossProvider.tsx</files>
  <read_first>
    - components/StudySession.tsx (the file being modified — ~34 instances: MC borders/text at L710-716, fill-blank input bg-white/gray-900 + border-gray-300/600 at L771, MC inactive bg-gray-100, text-gray-* throughout; note: text-green-500 here belongs to Phase 5 F-10 — leave any green untouched)
    - components/ModeSelector.tsx (the file being modified — ~10 instances: segmented bg-gray-100/800 at L41, active bg-white/gray-700 at L46/56, border-gray-200/700 at L33, text-gray-* at L25/26/36/70)
    - components/Sheet.tsx (the file being modified — drag handle bg-gray-300/600 at L85 → bg-surface-3; text-gray-800/100 at L87 → text-foreground. **DO NOT touch bg-black/40 at L73 — intentional modal backdrop**)
    - components/AudioButton.tsx (the file being modified — L132 text-gray-400 hover:text-gray-600 dark:text-gray-500 dark:hover:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700)
    - components/GlossProvider.tsx (the file being modified — F-27: border-gray-200/60 dark:border-gray-700/60 at L126, hover:bg-gray-100/700 at L132, text-gray-* at L138/145/153/158)
    - .planning/phases/04-design-system-tokens-sweep/04-UI-SPEC.md (replacement map + the StudySession/ModeSelector/Sheet/AudioButton/GlossProvider rows in "File-by-file sweep targets")
  </read_first>
  <action>
    Apply the UI-SPEC replacement map to all five files. Standard text/bg/border mappings:
    `text-gray-500 dark:text-gray-400` → `text-muted`;
    `text-gray-400 dark:text-gray-500` → `text-muted`;
    `text-gray-600 dark:text-gray-300` → `text-muted-foreground`;
    `text-gray-700 dark:text-gray-200` → `text-muted-foreground`;
    `text-gray-800 dark:text-gray-100` → `text-foreground`;
    `text-gray-900 dark:text-gray-100` → `text-foreground`;
    `bg-gray-100 dark:bg-gray-800` → `bg-surface-3`;
    `bg-gray-200 dark:bg-gray-700` → `bg-surface-3`;
    `bg-white dark:bg-gray-900` → `bg-surface-1`;
    `bg-white dark:bg-gray-700` → `bg-surface-1`;
    `bg-gray-300 dark:bg-gray-600` → `bg-surface-3`;
    `border-gray-200 dark:border-gray-700` → `border-border`;
    `border-gray-300 dark:border-gray-600` → `border-border`;
    `border-gray-200/60 dark:border-gray-700/60` → `border-border/60`;
    `hover:bg-gray-100 dark:hover:bg-gray-700` → `hover:bg-surface-3`.

    For AudioButton.tsx (L132): the compound `text-gray-400 hover:text-gray-600 dark:text-gray-500 dark:hover:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700` collapses to `text-muted hover:text-muted-foreground hover:bg-surface-3`.

    For StudySession.tsx (F-13): fill-blank input `bg-white dark:bg-gray-900 border-gray-300 dark:border-gray-600` → `bg-surface-1 border-border`; multiple-choice inactive `bg-gray-100 dark:bg-gray-700/50` → `bg-surface-3`; divider hr `border-gray-200 dark:border-gray-700` → `border-border`. Leave any `text-green-*` correct/grade colors untouched (Phase 5 / F-10 scope).

    For Sheet.tsx: replace the drag handle and text grays. **Critically, do NOT modify `bg-black/40` at L73** — it is the intentional pure-black modal backdrop, not a token escape (UI-SPEC + RESEARCH explicitly preserve it).

    No logic, layout, or behavior changes — class tokens only.
  </action>
  <verify>
    <automated>cd /Users/main/Documents/claude-test && test "$(grep -c 'gray-\|bg-white \|bg-white"' components/StudySession.tsx)" -eq 0 && test "$(grep -c 'gray-\|bg-white \|bg-white"' components/ModeSelector.tsx)" -eq 0 && test "$(grep -c 'gray-\|bg-white \|bg-white"' components/Sheet.tsx)" -eq 0 && test "$(grep -c 'gray-' components/AudioButton.tsx)" -eq 0 && test "$(grep -c 'gray-' components/GlossProvider.tsx)" -eq 0 && grep -q 'bg-black/40' components/Sheet.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gray-\|bg-white \|bg-white"' components/StudySession.tsx` returns 0
    - `grep -c 'gray-\|bg-white \|bg-white"' components/ModeSelector.tsx` returns 0
    - `grep -c 'gray-\|bg-white \|bg-white"' components/Sheet.tsx` returns 0
    - `grep -c 'gray-' components/AudioButton.tsx` returns 0 and `grep -c 'gray-' components/GlossProvider.tsx` returns 0
    - `grep -q 'bg-black/40' components/Sheet.tsx` still matches (modal backdrop preserved)
    - `npm run lint` passes clean
  </acceptance_criteria>
  <done>All five files have zero gray-*/bg-white escapes; AudioButton uses text-muted/hover:text-muted-foreground/hover:bg-surface-3; StudySession fill-blank uses bg-surface-1/border-border; Sheet's bg-black/40 backdrop is preserved; lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep HabitTracker.tsx (incl. ring-offset), ProficiencyArc.tsx (keep SVG track), HabitHeatmap.tsx, MilestoneCelebration.tsx, StatsBar.tsx</name>
  <files>components/HabitTracker.tsx, components/ProficiencyArc.tsx, components/HabitHeatmap.tsx, components/MilestoneCelebration.tsx, components/StatsBar.tsx</files>
  <read_first>
    - components/HabitTracker.tsx (the file being modified — L97 bg-gray-100/800 dot; L98 ring-offset-white dark:ring-offset-gray-800 → ring-offset-surface-1 (RESEARCH Pitfall 4); L116 bg-white dark:bg-gray-800 card → bg-surface-1; text-gray-* throughout)
    - components/ProficiencyArc.tsx (the file being modified — L28 bg-white dark:bg-surface-1 → bg-surface-1 (simplify); L29/74/84/99 text-gray-500/400 → text-muted; L73 text-gray-800/100 → text-foreground; L88 text-gray-600/300 → text-muted-foreground; L91 progress-bar track bg-gray-200/700 → bg-surface-3. **L47 text-gray-200 dark:text-gray-700 is the SVG arc track — KEEP unchanged per UI-SPEC**)
    - components/HabitHeatmap.tsx (the file being modified — L47 bg-gray-100 dark:bg-gray-700 → bg-surface-3)
    - components/MilestoneCelebration.tsx (the file being modified — L52 text-gray-900/100 → text-foreground; L53/59 text-gray-500/400 → text-muted)
    - components/StatsBar.tsx (the file being modified — L22 text-gray-700/200 → text-muted-foreground; L23 text-gray-500/400 → text-muted)
    - .planning/phases/04-design-system-tokens-sweep/04-RESEARCH.md ("Files NOT in UI-SPEC Sweep Table" table for HabitHeatmap/MilestoneCelebration/StatsBar; "Pitfall 4: HabitTracker ring-offset"; "Anti-Patterns to Avoid" for the SVG-track and ring-offset notes)
  </read_first>
  <action>
    Apply the standard replacement map (same mappings as Task 1) to all five files, with these file-specific rules:

    HabitTracker.tsx: the week-strip dot empty state `bg-gray-100 dark:bg-gray-800` → `bg-surface-3`; the today-marker ring offset `ring-offset-white dark:ring-offset-gray-800` → `ring-offset-surface-1` (this does NOT match a bare `gray-` grep, so it is easy to miss — RESEARCH Pitfall 4); the card container `bg-white dark:bg-gray-800` → `bg-surface-1`. Apply the text map to all stat labels.

    ProficiencyArc.tsx: card container `bg-white dark:bg-surface-1` → `bg-surface-1` (simplify the mixed escape); the progress-bar track `bg-gray-200 dark:bg-gray-700` (at ~L91) → `bg-surface-3`; text per map. **KEEP L47 `text-gray-200 dark:text-gray-700` exactly as-is** — it colors the SVG arc track stroke and is the documented acceptable exception (a `bg-surface-3` substitution is not meaningful in SVG `stroke="currentColor"` context).

    HabitHeatmap.tsx: the empty-cell `bg-gray-100 dark:bg-gray-700` → `bg-surface-3`.

    MilestoneCelebration.tsx and StatsBar.tsx: text-only swaps per the map.

    No logic, layout, or behavior changes — class tokens only.
  </action>
  <verify>
    <automated>cd /Users/main/Documents/claude-test && test "$(grep -c 'gray-\|bg-white \|bg-white"\|ring-offset-white' components/HabitTracker.tsx)" -eq 0 && test "$(grep -c 'gray-\|bg-white \|bg-white"' components/HabitHeatmap.tsx)" -eq 0 && test "$(grep -c 'gray-' components/MilestoneCelebration.tsx)" -eq 0 && test "$(grep -c 'gray-' components/StatsBar.tsx)" -eq 0 && test "$(grep -c 'text-gray-200 dark:text-gray-700' components/ProficiencyArc.tsx)" -eq 1 && grep -q 'ring-offset-surface-1' components/HabitTracker.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gray-\|bg-white \|bg-white"\|ring-offset-white' components/HabitTracker.tsx` returns 0 and `grep -q 'ring-offset-surface-1' components/HabitTracker.tsx` matches (Pitfall 4 resolved)
    - `grep -c 'gray-\|bg-white \|bg-white"' components/HabitHeatmap.tsx` returns 0
    - `grep -c 'gray-' components/MilestoneCelebration.tsx` returns 0 and `grep -c 'gray-' components/StatsBar.tsx` returns 0
    - `grep -c 'text-gray-200 dark:text-gray-700' components/ProficiencyArc.tsx` returns exactly 1 (the SVG arc track is correctly KEPT); all other gray instances in that file are gone
    - `npm run lint` passes clean
  </acceptance_criteria>
  <done>HabitTracker uses ring-offset-surface-1 and bg-surface-1 with zero gray escapes; ProficiencyArc keeps only the SVG arc track gray and is otherwise swept; HabitHeatmap/MilestoneCelebration/StatsBar are fully swept; lint passes.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep Nav.tsx, CardEditor.tsx, LessonRangeFilter.tsx, SyncPanel.tsx (incl. inputCls constants)</name>
  <files>components/Nav.tsx, components/CardEditor.tsx, components/LessonRangeFilter.tsx, components/SyncPanel.tsx</files>
  <read_first>
    - components/Nav.tsx (the file being modified — L23 dark:border-gray-800; L25 text-gray-800/100; L39/54 text-gray-500/400 hover:bg-gray-100/800; L64 border-gray-200/800; L77 text-gray-500/400 hover:text-gray-600/300)
    - components/CardEditor.tsx (the file being modified — L96 single-line inputCls 'border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 …'; L128/201/202/222 text-gray-*; L142 border-gray-200/700; L222 hover:bg-gray-100/700)
    - components/LessonRangeFilter.tsx (the file being modified — L15-16 multi-line inputCls 'border-gray-300/600 bg-white/gray-900'; L41/54 text-gray-500/400)
    - components/SyncPanel.tsx (the file being modified — L57 text-gray-700/200 → text-muted-foreground; L58 text-gray-400/400 → text-muted)
    - .planning/phases/04-design-system-tokens-sweep/04-RESEARCH.md ("Files NOT in UI-SPEC Sweep Table" — Nav/CardEditor/LessonRangeFilter/SyncPanel rows with exact replacements; "Pitfall 5: inputCls String Constant Missed")
  </read_first>
  <action>
    Apply the standard replacement map to all four files, with these specifics:

    Nav.tsx: `dark:border-gray-800` and `border-gray-200 dark:border-gray-800` → `border-border` (use `dark:border-border` where only the dark variant exists); `text-gray-800 dark:text-gray-100` → `text-foreground`; tab `text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-800` → `text-muted hover:bg-surface-3`; settings-gear `text-gray-500 dark:text-gray-400 hover:text-gray-600 dark:hover:text-gray-300` → `text-muted hover:text-muted-foreground`.

    CardEditor.tsx (Pitfall 5): replace the ENTIRE single-line `inputCls` constant at L96 — `border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 rounded-lg px-3 py-2 text-sm w-full` becomes `border border-border bg-surface-1 text-foreground rounded-lg px-3 py-2 text-sm w-full`. Then sweep remaining text-gray-* → text-muted, the divider `border-gray-200 dark:border-gray-700` → `border-border`, and `hover:bg-gray-100 dark:hover:bg-gray-700` → `hover:bg-surface-3`.

    LessonRangeFilter.tsx (Pitfall 5): replace the multi-line `inputCls` at L15-16 as a whole string — `border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-900` portion becomes `border-border bg-surface-1` (preserve all other classes in the concatenation); then text-gray-500/400 → text-muted.

    SyncPanel.tsx: L57 `text-gray-700 dark:text-gray-200` → `text-muted-foreground`; L58 `text-gray-400 dark:text-gray-400` → `text-muted`.

    No logic, layout, or behavior changes — class tokens only.
  </action>
  <verify>
    <automated>cd /Users/main/Documents/claude-test && test "$(grep -c 'gray-\|bg-white \|bg-white"' components/Nav.tsx)" -eq 0 && test "$(grep -c 'gray-\|bg-white \|bg-white"' components/CardEditor.tsx)" -eq 0 && test "$(grep -c 'gray-\|bg-white \|bg-white"' components/LessonRangeFilter.tsx)" -eq 0 && test "$(grep -c 'gray-' components/SyncPanel.tsx)" -eq 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gray-\|bg-white \|bg-white"' components/Nav.tsx` returns 0
    - `grep -c 'gray-\|bg-white \|bg-white"' components/CardEditor.tsx` returns 0 (inputCls constant fully swept per Pitfall 5)
    - `grep -c 'gray-\|bg-white \|bg-white"' components/LessonRangeFilter.tsx` returns 0 (multi-line inputCls fully swept)
    - `grep -c 'gray-' components/SyncPanel.tsx` returns 0
    - `npm run lint` passes clean
  </acceptance_criteria>
  <done>All four files have zero gray-*/bg-white escapes; the CardEditor and LessonRangeFilter inputCls constants use border-border/bg-surface-1/text-foreground; Nav tabs use text-muted/hover:bg-surface-3; SyncPanel uses text-muted-foreground/text-muted; lint passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time CSS / PostCSS | Class-string substitutions validated by the Tailwind build; an unregistered utility produces no CSS output (visual, not security) |
| client components | Sweep changes only className strings — no event handlers, fetch calls, or auth-related logic are modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | component className strings | low | accept | Pure class substitution; a wrong class is a visual regression caught by per-file 0-hit greps + lint, with no data/auth impact (RESEARCH Security Domain: zero security-sensitive changes this phase) |

No npm/pip/cargo installs in this plan — package legitimacy gate not applicable.
</threat_model>

<verification>
After all tasks (combined with Plans 1 + 2 this yields the phase-wide 0-hit grep):
1. `grep -rn 'bg-gray-\|text-gray-\|border-gray-\|bg-white \|bg-white"\|ring-offset-white\|ring-offset-gray-' components/ | grep -v 'ProgressRing.tsx:50' | grep -v 'bg-black/40' | grep -v 'text-gray-200 dark:text-gray-700.*ProficiencyArc'` returns 0 hits (only the documented SVG tracks and modal backdrop remain)
2. ProgressRing.tsx:50 SVG track and Sheet.tsx bg-black/40 backdrop and ProficiencyArc SVG arc track are all still present (intentional non-changes)
3. `npm run build` and `npm run lint` pass clean
</verification>

<success_criteria>
- All 14 component files have zero gray-*/bg-white/ring-offset-white escapes (except the documented SVG arc track in ProficiencyArc and the Sheet bg-black/40 backdrop)
- The 6 RESEARCH-discovered files + SyncPanel are swept (phase-wide grep would otherwise fail)
- HabitTracker uses ring-offset-surface-1 (Pitfall 4); CardEditor/LessonRangeFilter inputCls constants fully swept (Pitfall 5)
- Build and lint clean
</success_criteria>

<output>
Create `.planning/phases/04-design-system-tokens-sweep/04-03-SUMMARY.md` when done
</output>
