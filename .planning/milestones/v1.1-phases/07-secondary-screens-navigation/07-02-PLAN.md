---
phase: 07-secondary-screens-navigation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/cards/page.tsx
  - components/SwipeRow.tsx
  - app/habits/page.tsx
  - app/settings/page.tsx
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "Cards-page view-toggle buttons are at least 44px tall and announce pressed state to assistive tech via aria-pressed"
    - "The SwipeRow delete button has an explicit 44px minimum height (no longer depends only on items-stretch)"
    - "Habits and Settings section headings (<h2>) render at text-lg (18px), one step larger than before"
    - "The Settings reading-aid switch has a 44px effective tap area without changing the visible track height"
    - "The Habits 'My Korean' link has an explicit active press state consistent with the Home page card"
  artifacts:
    - "app/cards/page.tsx — view toggle buttons with min-h-[44px] flex items-center + aria-pressed"
    - "components/SwipeRow.tsx — delete button with min-h-[44px]"
    - "app/habits/page.tsx — four text-lg <h2> + active press state on My Korean link"
    - "app/settings/page.tsx — seven text-lg <h2> + min-h-[44px] wrapper around the reading-aid switch"
  key_links:
    - "aria-pressed on each Cards view-toggle button binds to activeView state so the toggle exposes its selected state"
---

<objective>
Bring Cards, Habits, and Settings to the same polish bar as the high-priority screens: a spacing/typography consistency pass (SEC-01) and the 44px touch-target minimum on the remaining secondary-screen interactive elements (SEC-02).

Purpose: The audit found the Cards view toggle below the 44px tap minimum and missing `aria-pressed`; the SwipeRow delete button relies on a fragile `items-stretch` for its height; Habits/Settings section `<h2>` headings sit one step too small (F-20) breaking the typographic hierarchy; the Settings reading-aid switch track is 28px tall (below 44px); and the Habits "My Korean" link lacks the `active:` press state its Home-page twin received in Phase 6. Each is a small, mechanical Tailwind change against tokens already confirmed present in `app/globals.css`.

Output: Updated `app/cards/page.tsx`, `components/SwipeRow.tsx`, `app/habits/page.tsx`, and `app/settings/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-secondary-screens-navigation/07-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
Newly added attributes/wrappers in this plan (exclude from source-drift verification):

- `aria-pressed` attribute on each Cards-page view-toggle `<button>` (bound to `activeView === v`)
- `min-h-[44px] flex items-center` utility classes on the Cards view-toggle buttons
- `min-h-[44px]` utility class on the `components/SwipeRow.tsx` delete `<button>`
- A `<div className="flex items-center min-h-[44px]">` wrapper element around the existing reading-aid `<button role="switch">` in `app/settings/page.tsx`
- `text-lg` utility added to existing `<h2>` headings in `app/habits/page.tsx` and `app/settings/page.tsx`
- `active:shadow-sm active:bg-surface-2 transition-colors` utilities on the Habits "My Korean summary" `<Link>`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Cards view-toggle touch target + aria-pressed, and SwipeRow delete 44px height</name>
  <read_first>
    - app/cards/page.tsx (the view-toggle block at lines 214-231: the `.map` over `['cards', 'reading-practice']` rendering `<button>` with className `px-4 py-1.5 text-sm font-medium rounded-md transition-colors ...`; note the container is already `bg-surface-3` and the active tab already `bg-surface-1` — only height + aria are missing)
    - components/SwipeRow.tsx (the delete `<button>` at lines 112-119 with className `bg-red-500 hover:bg-red-600 text-white text-sm font-semibold min-w-[72px] px-5 rounded-xl transition-colors`, inside an `items-stretch` flex parent)
    - .planning/phases/07-secondary-screens-navigation/07-UI-SPEC.md ("Interaction Contract → Cards page — view toggle" and "Cards page — SwipeRow delete action")
  </read_first>
  <files>app/cards/page.tsx, components/SwipeRow.tsx</files>
  <action>
    In `app/cards/page.tsx`, on each view-toggle `<button>` (the `.map` over the `ActiveView[]` array `['cards', 'reading-practice']`): (a) add `aria-pressed={activeView === v}` as a JSX attribute on the `<button>`, and (b) add `min-h-[44px] flex items-center` to the static portion of its className template literal (current static portion is `px-4 py-1.5 text-sm font-medium rounded-md transition-colors`). Keep the existing active/inactive ternary unchanged (`bg-surface-1 text-foreground shadow-sm` vs `text-muted hover:text-muted-foreground`). Do NOT change the container `<div className="flex bg-surface-3 rounded-lg p-1 self-start">` — its tokens are already correct. `aria-pressed` is the correct ARIA pattern for a two-state view toggle on a plain `<button>` (not `role="tab"`, which would require a paired `role="tabpanel"`).

    In `components/SwipeRow.tsx`, on the delete `<button>` (className currently `bg-red-500 hover:bg-red-600 text-white text-sm font-semibold min-w-[72px] px-5 rounded-xl transition-colors`), add `min-h-[44px]` so the 44px constraint is explicit and no longer depends on the parent flex container's `items-stretch`. Do not change `min-w-[72px]`, the colors, or the `aria-label={deleteLabel}` / `tabIndex` logic.
  </action>
  <verify>
    <automated>grep -q "aria-pressed={activeView === v}" app/cards/page.tsx && grep -q "min-h-\[44px\] flex items-center" app/cards/page.tsx && grep -q "min-w-\[72px\] px-5 rounded-xl" components/SwipeRow.tsx && grep -q "min-h-\[44px\]" components/SwipeRow.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: each Cards view-toggle `<button>` has `aria-pressed={activeView === v}`.
    - Source assertion: the Cards view-toggle `<button>` className contains `min-h-[44px] flex items-center`.
    - Source assertion: the Cards view-toggle container `<div>` still reads `flex bg-surface-3 rounded-lg p-1 self-start` (unchanged).
    - Source assertion: the SwipeRow delete `<button>` className contains `min-h-[44px]` and still contains `min-w-[72px]`.
    - Behavior: `npm run lint` passes with zero errors.
  </acceptance_criteria>
  <done>Cards view-toggle buttons are ≥44px tall and expose `aria-pressed`; the SwipeRow delete button has an explicit 44px height; lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Habits page — upgrade <h2> headings to text-lg and add active state to My Korean link</name>
  <read_first>
    - app/habits/page.tsx (the four section `<h2 className="font-semibold text-foreground">` elements at lines 154 "All-time totals", 177 "Averages &amp; consistency", 204 "Last 30 days", 240 "History"; and the "My Korean summary" `<Link>` at lines 268-271 with className `flex items-center justify-between bg-surface-1 rounded-2xl shadow-md px-5 py-4 hover:shadow-lg transition-shadow`)
    - .planning/phases/07-secondary-screens-navigation/07-UI-SPEC.md ("Heading upgrade rule (F-20 — SEC-01)" and "Habits page — My Korean link")
  </read_first>
  <files>app/habits/page.tsx</files>
  <action>
    Upgrade all four section `<h2>` headings from `className="font-semibold text-foreground"` to `className="text-lg font-semibold text-foreground"`. These are the headings "All-time totals", "Averages &amp; consistency", "Last 30 days", and "History". Do NOT change the `<h1 className="text-2xl font-bold text-foreground">Habits</h1>` page title or the `text-3xl` streak number — only the `<h2>` section headings change.

    On the "My Korean summary" `<Link href="/wrapped">`, change `hover:shadow-lg transition-shadow` to `hover:shadow-lg active:shadow-sm active:bg-surface-2 transition-colors`. This mirrors the F-07 press-state fix applied to the Home page "My Korean" card in Phase 6, giving a consistent tap-down affordance across the codebase. Keep the rest of the className (`flex items-center justify-between bg-surface-1 rounded-2xl shadow-md px-5 py-4`) unchanged.
  </action>
  <verify>
    <automated>grep -c "text-lg font-semibold text-foreground" app/habits/page.tsx | grep -qx 4 && grep -q "active:shadow-sm active:bg-surface-2 transition-colors" app/habits/page.tsx && ! grep -q "hover:shadow-lg transition-shadow\"" app/habits/page.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `text-lg font-semibold text-foreground` appears exactly 4 times in `app/habits/page.tsx` (the four section headings).
    - Source assertion: the `<h1>` page title still reads `text-2xl font-bold text-foreground` (unchanged).
    - Source assertion: the My Korean `<Link>` className contains `active:shadow-sm active:bg-surface-2 transition-colors` and no longer contains the bare `hover:shadow-lg transition-shadow` pairing.
    - Behavior: `npm run lint` passes with zero errors.
  </acceptance_criteria>
  <done>All four Habits section headings render at 18px; the My Korean link has a press state consistent with the Home page; lint clean.</done>
</task>

<task type="auto">
  <name>Task 3: Settings page — upgrade <h2> headings to text-lg and wrap reading-aid switch in a 44px tap area</name>
  <read_first>
    - app/settings/page.tsx (the seven section `<h2 className="font-semibold text-foreground">` elements at lines 146 "Appearance", 172 "Daily study goal", 197 "Habit day starts at", 220 "Study session size", 246 "Korean text size", 281 "Reading aid", 309 "App colors"; and the reading-aid `<button role="switch">` at lines 287-302 with track classes `relative shrink-0 w-12 h-7 rounded-full transition-colors ...`, currently a direct child of `<div className="flex items-center justify-between gap-4">`)
    - .planning/phases/07-secondary-screens-navigation/07-UI-SPEC.md ("Heading upgrade rule (F-20 — SEC-01)" and "Settings — reading-aid toggle (F-22, SEC-02)")
  </read_first>
  <files>app/settings/page.tsx</files>
  <action>
    Upgrade all seven section `<h2>` headings from `className="font-semibold text-foreground"` to `className="text-lg font-semibold text-foreground"`. These are: "Appearance", "Daily study goal", "Habit day starts at", "Study session size", "Korean text size", "Reading aid", and "App colors". Do NOT change the `<h1 className="text-2xl font-bold text-foreground">Settings</h1>` page title. Replace all occurrences of the exact class string `font-semibold text-foreground` on `<h2>` elements with `text-lg font-semibold text-foreground` (all seven `<h2>` use the identical class string, so a replace-all of `<h2 className="font-semibold text-foreground">` → `<h2 className="text-lg font-semibold text-foreground">` is the precise edit).

    Wrap the reading-aid `<button role="switch">` (the toggle with track classes `w-12 h-7`) in a new `<div className="flex items-center min-h-[44px]">`. The visible switch track stays `w-12 h-7` (48×28px) — do NOT change the track width/height, the thumb `<span>`, the `translate-x-5` thumb position, `aria-checked`, `aria-label`, or the `onClick`. Only the surrounding wrapper changes: the effective tap target grows to 44px tall via the `min-h-[44px]` wrapper while the switch's visual height is unchanged. The wrapper sits inside the existing `<div className="flex items-center justify-between gap-4">` row in place of where the `<button>` currently sits.
  </action>
  <verify>
    <automated>grep -c "text-lg font-semibold text-foreground" app/settings/page.tsx | grep -qx 7 && grep -q "flex items-center min-h-\[44px\]" app/settings/page.tsx && grep -q 'role="switch"' app/settings/page.tsx && grep -q "w-12 h-7 rounded-full" app/settings/page.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `text-lg font-semibold text-foreground` appears exactly 7 times in `app/settings/page.tsx`.
    - Source assertion: the `<h1>` page title still reads `text-2xl font-bold text-foreground` (unchanged).
    - Source assertion: a `<div className="flex items-center min-h-[44px]">` wraps the `<button role="switch">`; the button still contains `w-12 h-7 rounded-full` and the thumb `<span>` still uses `translate-x-5` for the on state.
    - Source assertion: the switch `<button>` still has `aria-checked={readingAid}` and `aria-label="Reading aid"`.
    - Behavior: `npm run lint` passes with zero errors.
  </acceptance_criteria>
  <done>All seven Settings section headings render at 18px; the reading-aid switch has a 44px effective tap area with its visible track unchanged; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits only presentational Tailwind classes, ARIA attributes, and a wrapper `<div>`. No data flow, input handling, or trust boundary changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering | Cards/Settings/Habits JSX class + ARIA edits | low | accept | Changes are static class strings and `aria-pressed`/wrapper markup; no executable logic or user input is introduced. Verified by lint + grep assertions. |

No npm/pip/cargo install tasks in this plan — package legitimacy gate not applicable.
</threat_model>

<verification>
- `npm run lint` passes with zero errors after all three tasks.
- `grep` confirms: Cards view-toggle has `min-h-[44px] flex items-center` + `aria-pressed`; SwipeRow delete has `min-h-[44px]`; Habits has 4 `text-lg` `<h2>` + active state on My Korean link; Settings has 7 `text-lg` `<h2>` + a `min-h-[44px]` wrapper around the reading-aid switch (track still `w-12 h-7`).
- Already-resolved findings confirmed in source and NOT re-touched: F-19 (Habits trend bars `bg-surface-3` at line 217, heatmap legend at line 256), F-21 (Settings `<select>`/color inputs already `bg-surface-1`), F-16 container/active tokens (already `bg-surface-3`/`bg-surface-1`).
</verification>

<success_criteria>
- ROADMAP Success Criterion 2 met: Cards, Habits, and Settings receive a spacing/typography consistency pass aligned to the design-system tokens (heading-size upgrade + confirmed token usage).
- ROADMAP Success Criterion 3 (Cards portion) met: Cards-page interactive elements (view toggle, SwipeRow delete) and the Settings reading-aid switch meet the 44px minimum.
- SEC-01 and the Cards/Settings portion of SEC-02 are satisfied; lint clean.
</success_criteria>

<output>
Create `.planning/phases/07-secondary-screens-navigation/07-02-SUMMARY.md` when done.
</output>
