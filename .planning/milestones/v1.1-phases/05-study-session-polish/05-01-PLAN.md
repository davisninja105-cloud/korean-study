---
phase: 05-study-session-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
autonomous: true
requirements:
  - STUDY-03
  - STUDY-04
must_haves:
  truths:
    - "Korean flashcard text renders with line-height 1.7 (both .hangul and .hangul-sentence)"
    - "word-break: keep-all remains present on both Korean text classes"
    - "An .animate-card-in utility exists that fades a freshly-mounted card in within 120ms"
    - "Reduced-motion users see no card-in animation (animation disabled, content visible)"
  artifacts:
    - "app/globals.css contains .animate-card-in in the normal animation-utilities block"
    - "app/globals.css contains .animate-card-in inside the @media (prefers-reduced-motion: reduce) block"
  key_links:
    - ".animate-card-in reuses the existing @keyframes fadeIn (already defined at app/globals.css:153)"
    - "The .animate-card-in class name must match the className the component plan (05-02) applies to its card wrapper"
---

<objective>
Make the CSS foundations for Phase 5 study-session polish: bring Korean typography to the W3C KLREQ-recommended line-height, and add the inter-card transition utility (with its mandatory reduced-motion counterpart) that the component plan will apply.

Purpose: STUDY-03 (Korean readability) and the CSS half of STUDY-04 (smooth inter-card transition) are pure `app/globals.css` edits with zero JavaScript risk. Landing them first means the component plan (05-02) can reference an already-defined `.animate-card-in` class.
Output: Two `.hangul`/`.hangul-sentence` line-height edits and one new `.animate-card-in` utility (defined once in the normal block, once in the reduced-motion block) in `app/globals.css`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-study-session-polish/05-RESEARCH.md
@.planning/phases/05-study-session-polish/05-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (exclude from drift / source-grounding checks):
- CSS utility class `.animate-card-in` (app/globals.css) — defined in the normal animation block and again in the `@media (prefers-reduced-motion: reduce)` block.

Symbols this plan modifies (already exist in source):
- `.hangul` line-height (app/globals.css, currently 1.65)
- `.hangul-sentence` line-height (app/globals.css, currently 1.65)
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: line-height: 1.7 -->
<!-- planner-discipline-allow: line-height: 1.65 -->
<!-- planner-discipline-allow: word-break: keep-all -->

<tasks>

<task type="auto">
  <name>Task 1: Raise Korean text line-height to 1.7 (STUDY-03)</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (current `.hangul` block at ~line 256 and `.hangul-sentence` block at ~line 263; both use `line-height: 1.65`. `word-break: keep-all` is already present on both and must stay.)
    - .planning/phases/05-study-session-polish/05-UI-SPEC.md (Typography section: "Korean line-height contract (STUDY-03)" mandates 1.7 on both classes)
  </read_first>
  <action>
    In `app/globals.css`, inside the `@layer utilities` block, change the `line-height` value from `1.65` to `1.7` in BOTH the `.hangul` rule and the `.hangul-sentence` rule. There are exactly two `line-height: 1.65` occurrences in this file and both must become `line-height: 1.7`. Do NOT alter `font-family`, `letter-spacing`, `word-break: keep-all`, or the `font-size: calc(...)` line on `.hangul-sentence` — only the line-height number changes. Do not touch `.hangul-gloss` or `.hangul-spaced .hangul-sentence`, which do not declare line-height.
  </action>
  <verify>
    <automated>test "$(grep -c 'line-height: 1.7' app/globals.css)" -ge 2 &amp;&amp; test "$(grep -c 'line-height: 1.65' app/globals.css)" -eq 0 &amp;&amp; test "$(grep -c 'word-break: keep-all' app/globals.css)" -ge 2 &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'line-height: 1.65' app/globals.css` returns 0 (no stale 1.65 values remain).
    - `grep -c 'line-height: 1.7' app/globals.css` returns 2 or more (both Korean classes updated).
    - `grep -c 'word-break: keep-all' app/globals.css` returns 2 or more (keep-all preserved on both classes).
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Both `.hangul` and `.hangul-sentence` declare `line-height: 1.7`; `word-break: keep-all` is intact on both; no `1.65` line-height remains; lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add .animate-card-in utility with reduced-motion gate (STUDY-04 CSS)</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (the `@keyframes fadeIn` definition at ~line 153; the `/* ── Animation utilities ── */` block with `.animate-reveal { animation: fadeIn 0.15s ease forwards; }` at ~line 185; the `@media (prefers-reduced-motion: reduce)` block at ~line 244 which already lists `.animate-reveal { animation: none; opacity: 1; transform: none; }`)
    - .planning/phases/05-study-session-polish/05-UI-SPEC.md (STUDY-04 "Animation spec" and "Reduced-Motion Compliance" sections: class name `animate-card-in`, reuse `fadeIn`, duration `0.12s`, easing `ease-out`, fill `forwards`)
    - .planning/phases/05-study-session-polish/05-RESEARCH.md (Common Pitfalls §1: every new keyframe utility added in Phase 5 must get a reduced-motion counterpart in the same PR — do not defer to Phase 8)
  </read_first>
  <action>
    Add a new `.animate-card-in` utility in the `/* ── Animation utilities ── */` block (next to `.animate-reveal`). It must reuse the existing `fadeIn` keyframe with duration `0.12s`, easing `ease-out`, and fill mode `forwards` — do NOT define a new keyframe. Then, inside the existing `@media (prefers-reduced-motion: reduce)` block (the one that already disables `.animate-reveal` and `.animate-burst`), add a sibling rule for `.animate-card-in` that sets `animation: none; opacity: 1; transform: none;` so reduced-motion users land on a fully-visible, untransformed card with no animation. The class name must be exactly `animate-card-in` (kebab-case) because plan 05-02 applies this exact className to the card wrapper.
  </action>
  <verify>
    <automated>test "$(grep -c 'animate-card-in' app/globals.css)" -eq 2 &amp;&amp; grep -q 'fadeIn 0.12s ease-out forwards' app/globals.css &amp;&amp; awk '/@media \(prefers-reduced-motion: reduce\)/{f=1} f &amp;&amp; /animate-card-in/{found=1} END{exit !found}' app/globals.css &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'animate-card-in' app/globals.css` returns exactly 2 (one normal-block definition, one reduced-motion-block override).
    - The normal-block rule reads `animation: fadeIn 0.12s ease-out forwards;` (reuses existing fadeIn keyframe; 120ms ≤ 150ms requirement; ease-out; forwards).
    - The `animate-card-in` reduced-motion rule appears inside the `@media (prefers-reduced-motion: reduce)` block and sets `animation: none` (verified by the awk range check).
    - No new `@keyframes` block was added (fadeIn is reused).
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>`.animate-card-in` exists exactly twice — a 120ms ease-out fadeIn forwards in the animation-utilities block, and a motion-disabling override inside the reduced-motion media query; lint passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits only static CSS in `app/globals.css`. No input crosses a trust boundary; no data, network, or auth surface is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | app/globals.css (CSS only) | low | accept | Pure presentational CSS (line-height + opacity/transform animation). No script, no data, no state. ASVS L1 has no applicable control; risk accepted. |

No package-manager installs in this plan (no npm/pip/cargo add), so no package-legitimacy gate applies.
</threat_model>

<verification>
- `npm run lint` passes clean (project requires zero lint errors).
- `grep -c 'line-height: 1.65' app/globals.css` == 0; `grep -c 'line-height: 1.7' app/globals.css` >= 2.
- `grep -c 'animate-card-in' app/globals.css` == 2, with one occurrence inside the reduced-motion media query.
- No new `@keyframes` were introduced (fadeIn reused).
</verification>

<success_criteria>
- Korean text classes use line-height 1.7 with keep-all preserved (STUDY-03 CSS portion complete).
- `.animate-card-in` is defined and reduced-motion-gated, ready for plan 05-02 to apply (STUDY-04 CSS portion complete).
- Lint is clean; no visual regression in light or dark mode (changes are color-free).
</success_criteria>

<output>
Create `.planning/phases/05-study-session-polish/05-01-SUMMARY.md` when done.
</output>
