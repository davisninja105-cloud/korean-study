---
phase: 05-study-session-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - components/StudySession.tsx
autonomous: true
requirements:
  - STUDY-01
  - STUDY-02
  - STUDY-04
must_haves:
  truths:
    - "A 'Card N of Total' counter is visible throughout a session and updates after each answered card"
    - "Correct grades (Good/Easy, rating >= 3) fire a distinct success haptic; needs-review grades (Again/Hard, rating < 3) fire the softer selection haptic"
    - "The Easy grade button uses the warm reward token instead of raw teal utilities"
    - "Advancing to the next card plays the 120ms fade-in (via key={cursor} on the card wrapper) instead of an abrupt swap"
  artifacts:
    - "components/StudySession.tsx renders the progress text as 'Card {n} of {initialCount}'"
    - "components/StudySession.tsx wraps the card-render conditional in a <div key={cursor} className=\"animate-card-in w-full\">"
  key_links:
    - "The wrapper className 'animate-card-in' must match the utility added in plan 05-01 (app/globals.css)"
    - "Haptic differentiation lives in submitReview() (the grade path), NOT in handleReveal() (the reveal path stays 'selection')"
    - "The Easy button change applies only to the flashcard grade bar; the fill-blank grade bar has no Easy button"
---

<objective>
Apply the three StudySession.tsx polish changes for Phase 5: the "Card N of Total" progress counter (STUDY-01), correct-vs-needs-review haptic differentiation plus the warm Easy-button color (STUDY-02), and the inter-card fade-in wrapper (STUDY-04 JSX half) that consumes the `.animate-card-in` utility added in plan 05-01.

Purpose: These are the JavaScript/JSX-side polish changes that make the daily study loop feel oriented (you always know where you are), encouraging (a distinct success buzz on correct answers, a warm Easy color), and smooth (no abrupt card swap).
Output: Four scoped edits in `components/StudySession.tsx` — progress text, haptic split, Easy button token, and a keyed card-in wrapper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-study-session-polish/05-RESEARCH.md
@.planning/phases/05-study-session-polish/05-UI-SPEC.md
@components/StudySession.tsx
@lib/haptics.ts
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (exclude from drift / source-grounding checks):
- (none — no new exports, functions, or files; all edits modify existing JSX/handlers in components/StudySession.tsx)

Symbols this plan consumes from plan 05-01:
- CSS class `.animate-card-in` (defined in app/globals.css by plan 05-01) — applied as a className to the card wrapper.

Symbols this plan reuses (already exist in source):
- `stats.reviewed`, `initialCount`, `cursor`, `queue` (StudySession.tsx state)
- `haptic('success')` / `haptic('selection')` (lib/haptics.ts named patterns)
- `bg-reward-soft` / `text-reward` Tailwind utilities (tokens defined in app/globals.css across all three theme blocks)
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: bg-teal-50 -->

<tasks>

<task type="auto">
  <name>Task 1: Progress counter + haptic differentiation + warm Easy button (STUDY-01, STUDY-02)</name>
  <files>components/StudySession.tsx</files>
  <read_first>
    - components/StudySession.tsx (the progress strip at ~lines 549-560 with `<p className="text-sm text-muted">{stats.reviewed} reviewed · {queue.length} left</p>`; `initialCount` derived at ~line 128; `submitReview` at ~line 382 with `haptic('selection')` at ~line 386; `handleReveal` at ~line 471 with its own `haptic('selection')` at ~line 473; the flashcard Easy button at ~line 841-848 with className `bg-teal-50 text-teal-600 dark:bg-teal-500/10 dark:text-teal-400 hover:bg-teal-100 dark:hover:bg-teal-500/20`)
    - lib/haptics.ts (confirms `'success'` is the burst pattern [10,50,20] and `'selection'` is the single 10ms buzz)
    - .planning/phases/05-study-session-polish/05-UI-SPEC.md (Interaction Contract STUDY-01 copy "Card {stats.reviewed + 1} of {initialCount}"; STUDY-02 haptic rule and the grade button color contract showing Easy → `bg-reward-soft text-reward`)
  </read_first>
  <action>
    Make three scoped edits in `components/StudySession.tsx`:

    (1) STUDY-01 progress text: In the progress-strip paragraph, replace the existing backward-looking counter (the current body reads as the reviewed-count followed by a middot separator and the remaining-count, i.e. the "N reviewed / M left" phrasing) with the forward-looking position counter `Card {Math.min(stats.reviewed + 1, initialCount)} of {initialCount}`. Keep the surrounding `<p className="text-sm text-muted">` element and the ProgressRing untouched. The `Math.min(..., initialCount)` cap handles the requeue edge case where reviewed+1 could momentarily exceed the initial total. `stats.reviewed` and `initialCount` are both already in scope at this render site.

    (2) STUDY-02 haptic split: In `submitReview` only, change the call `haptic('selection')` to `haptic(rating >= 3 ? 'success' : 'selection')`. This is the call at the top of `submitReview` (immediately before `reviewBuffer.current += 1`). Do NOT change the separate `haptic('selection')` inside `handleReveal` — the reveal action is not a grade and must stay 'selection'.

    (3) STUDY-02 warm Easy button: On the flashcard-mode Easy grade button, replace the color utilities `bg-teal-50 text-teal-600 dark:bg-teal-500/10 dark:text-teal-400 hover:bg-teal-100 dark:hover:bg-teal-500/20` with `bg-reward-soft text-reward hover:opacity-90`. Keep all layout/sizing utilities on that button (`flex-1 min-h-14 py-2 px-1 rounded-xl font-medium text-xs ... transition-colors flex flex-col items-center justify-center gap-0.5`) unchanged. This Easy button appears only in the flashcard grade bar; the fill-blank grade bar has Wrong/Correct only and is not touched.
  </action>
  <verify>
    <automated>grep -q 'Card {Math.min(stats.reviewed + 1, initialCount)} of {initialCount}' components/StudySession.tsx &amp;&amp; grep -q "haptic(rating >= 3 ? 'success' : 'selection')" components/StudySession.tsx &amp;&amp; grep -q 'bg-reward-soft text-reward' components/StudySession.tsx &amp;&amp; test "$(grep -c 'reviewed · ' components/StudySession.tsx)" -eq 0 &amp;&amp; test "$(grep -c 'bg-teal-50' components/StudySession.tsx)" -eq 0 &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The progress strip renders `Card {Math.min(stats.reviewed + 1, initialCount)} of {initialCount}`; the old `reviewed · ` text no longer appears (grep count 0).
    - `submitReview` contains `haptic(rating >= 3 ? 'success' : 'selection')`; `handleReveal` still contains a bare `haptic('selection')`.
    - The flashcard Easy button uses `bg-reward-soft text-reward`; no `bg-teal-50` utility remains anywhere in the file (grep count 0).
    - `npm run lint` exits 0 (no `react-hooks/purity` violations — `Math.min` of existing scalars is pure render-safe).
  </acceptance_criteria>
  <done>The session shows a live "Card N of Total" counter, correct grades fire the success haptic while needs-review grades stay on selection, the reveal action haptic is unchanged, and the Easy button uses the warm reward token; lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Inter-card fade-in wrapper (STUDY-04 JSX)</name>
  <files>components/StudySession.tsx</files>
  <read_first>
    - components/StudySession.tsx (the card-render conditional that opens at the `{/* Card — flashcard mode uses 3D flip */}` comment then `{mode === 'flashcard' ? (` at ~line 562-563, runs through the flashcard branch, the `) : (` non-flashcard branch at ~line 688 with the `/* Non-flashcard modes — no flip */` div, and closes with `)}` at ~line 798 — immediately before the `{/* ── Action bar ... ── */}` comment at ~line 800)
    - .planning/phases/05-study-session-polish/05-UI-SPEC.md (STUDY-04 "JSX" + "Constraint": the key must go on the OUTER card wrapper, NOT on .card-flip-container; the action bar stays outside the wrapper)
    - .planning/phases/05-study-session-polish/05-RESEARCH.md (Common Pitfalls §2: key={cursor} must be on the outer card container so the useLayoutEffect height measurement re-runs correctly; Pitfall details that frontRef/backRef re-attach cleanly on remount)
    - app/globals.css (confirm `.animate-card-in` exists — added by plan 05-01; this wrapper consumes it)
  </read_first>
  <action>
    Wrap the entire card-render conditional (the `{mode === 'flashcard' ? ( ... ) : ( ... )}` expression — from `{mode === 'flashcard' ? (` through its closing `)}`) in a single new wrapper element `<div key={cursor} className="animate-card-in w-full">...</div>`. The `key={cursor}` forces React to remount this div on each card advance (cursor increments in `submitReview`), replaying the `.animate-card-in` fade-in from scratch. Constraints: the `key` goes on this NEW outer wrapper only — do NOT add a key to `.card-flip-container`, `.card-flip-inner`, or the non-flashcard card div. The sticky action bar block (the `{/* ── Action bar ... ── */}` div and everything after it) must remain OUTSIDE and AFTER this wrapper so the grade buttons do not re-animate per card. `cursor` is already in scope at this render site. Leave the `{/* Card — flashcard mode uses 3D flip */}` comment in place above the new wrapper.
  </action>
  <verify>
    <automated>grep -q 'key={cursor} className="animate-card-in w-full"' components/StudySession.tsx &amp;&amp; test "$(grep -c 'animate-card-in' components/StudySession.tsx)" -eq 1 &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one `<div key={cursor} className="animate-card-in w-full">` wrapper exists in components/StudySession.tsx (grep count for `animate-card-in` == 1).
    - The wrapper encloses both the `mode === 'flashcard'` branch and the non-flashcard branch; the JSX still has balanced tags (verified by `npm run build` succeeding).
    - The sticky action bar div (`{/* ── Action bar ... ── */}`) is NOT inside the keyed wrapper (it appears after the wrapper's closing tag).
    - No `key=` was added to `.card-flip-container` or `.card-flip-inner`.
    - `npm run build` exits 0 (full type-check + production build pass).
  </acceptance_criteria>
  <done>The card area is wrapped in a single `key={cursor}`/`animate-card-in` div so each advance fades the new card in over 120ms; the action bar stays outside the wrapper; the project builds cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | All edits are inside `components/StudySession.tsx`, a client component that runs only within an already-authenticated session (middleware gates the route). No new network call, no new user input, no new persisted state. The haptic change reads the local `rating` argument; the progress text reads existing client state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Tampering | StudySession.tsx grade/haptic path | low | accept | The haptic branch derives from the locally-passed `rating` (1-4), not from network or user free-text; no integrity surface changes. ASVS L1: no applicable control. Risk accepted. |
| T-05-03 | Information Disclosure | "Card N of Total" progress text | low | accept | The counter exposes only the learner's own session position (count of own cards) inside an authenticated single-tenant app. No cross-tenant or sensitive data. Risk accepted. |

No package-manager installs in this plan (no npm/pip/cargo add), so no package-legitimacy gate applies.
</threat_model>

<verification>
- `npm run lint` passes clean (no `react-hooks/purity` / `react-hooks/set-state-in-effect` violations introduced).
- `npm run build` passes (type-check + production build) — confirms JSX tag balance after the wrapper edit.
- `grep -c 'reviewed · ' components/StudySession.tsx` == 0 and the new "Card {n} of {total}" string is present.
- `grep -c 'bg-teal-50' components/StudySession.tsx` == 0; `bg-reward-soft text-reward` is present.
- `grep -c 'animate-card-in' components/StudySession.tsx` == 1.
- `handleReveal` still uses `haptic('selection')`; `submitReview` uses `haptic(rating >= 3 ? 'success' : 'selection')`.
</verification>

<success_criteria>
- STUDY-01: "Card N of Total" visible and updates each card.
- STUDY-02: correct vs needs-review grades are haptically distinct; Easy button is warm (reward token).
- STUDY-04: inter-card advance fades in over 120ms (≤150ms) using the keyed wrapper + `.animate-card-in` from plan 05-01.
- Lint and build both clean; no visual or behavioral regression to FSRS review logic (untouched).
</success_criteria>

<output>
Create `.planning/phases/05-study-session-polish/05-02-SUMMARY.md` when done.
</output>
