---
context: phase
phase: 28-active-recall-study-mode
task: complete
total_tasks: 7
status: paused
last_updated: 2026-08-05T00:00:00Z
---

# PHASE 28 — PAUSED (Ready for Archival)

> Phase 28 is **complete and verified**. This file documents final state for reference during Phase 30+ if that work depends on active-recall internals.

## Status Summary

✅ **All gates passed:**
- UAT complete (2/2 tests passed, 0 issues)
- Security verified (0 open threats, L1 audit passed)
- Code review complete (fixes applied)
- Deployed to production

## Completed Work

### Delivered Features
- **Passive/Active toggle** — Mode-select screen now presents a single "Passive/Active" toggle replacing the old 3-mode grid
- **Passive mode** — Unchanged flashcard exposure (sentence/word shown, tap to reveal, self-rate)
- **Active mode** — New production exercise: English translation shown, Korean fully hidden, tap to reveal correct Korean + audio, self-rate on grading bar
- **Multiple Choice deleted** — `MultipleChoiceMode.tsx` removed; distractor selection logic purged from `StudySession.tsx`; related tests removed
- **Fill-in-the-Blank retired** — Superseded by Active mode; standalone FillBlankMode deleted
- **UI polish** — New hint control (pill above reveal), grade bar text, card type indicator
- **E2E coverage** — New test for mid-session state graduation (1→2 visible flip)

### Documentation Updated
- `.planning/codebase/STRUCTURE.md` — StudySession decomposition, mode architecture, flow diagram
- `CLAUDE.md` ▸ `components/` ▸ StudySession, FlashcardMode, ModeSelector sections updated
- `.planning/codebase/ARCHITECTURE.md` — Passive/Active modal description

### Code Artifact Deletions
- `components/MultipleChoiceMode.tsx` — fully removed
- `components/FillBlankMode.tsx` — fully removed
- `components/StudySession.tsx` — distractor-selection and MC/FillBlank orchestration removed
- `e2e/specs/study.spec.ts` — MC and FillBlank test suites removed

<remaining_work>
None — phase complete. All requirements satisfied.
</remaining_work>

<decisions_made>
- Chose **full Passive/Active toggle** over a 3-mode grid — simpler mental model, focus on production exercise
- **Active as default** — new learners land on the productive exercise, not passive review
- **Hint control** — kept minimal (gloss, one-tap, disabled after use) to prevent knowledge leakage
- Decided to **fully delete MC/FillBlank** (not deprecate) — simpler codebase, clearer single-mode path going forward
</decisions_made>

<blockers>
None — all gates passed.
</blockers>

## Key Files (for Phase 30+ reference)

### New/Modified in Phase 28
- `components/StudySession.tsx` — Now orchestrates only Passive + Active modes via `mode: 'passive' | 'active'` state
- `components/FlashcardMode.tsx` — Renamed from `PassiveMode.tsx`; now holds both exposure and production (Active) logic behind `mode` prop
- `components/ModeSelector.tsx` — Single Passive/Active toggle + optional exposure sub-toggle (Passive only)
- `lib/sentence-match.ts` — Unchanged; core substring/blank logic still used by both modes
- `app/api/generate` — Still generates practice, now only for Active mode
- `prisma/schema.prisma` — `Card.distractors` column deprecated (no longer written; left in place for safety)

### Deleted in Phase 28
- `components/MultipleChoiceMode.tsx`
- `components/FillBlankMode.tsx`

### Test Coverage
- `e2e/specs/study.spec.ts` — New test for mid-session state graduation (1→2 visible flip in same session)
- Unit tests for `StudySession` removed for deleted MC/FillBlank paths; Passive/Active core paths verified via E2E

## Security & Production Notes

- **0 threats** — Legacy `Card.distractors` JSON reader deleted; no parsing in StudySession
- **Package surface unchanged** — Zero new npm packages installed
- **XSS mitigation** — All `card.back` / `chosenSentence.translation` render as React text nodes (auto-escaped)
- **E2E test DB isolated** — New `helpers/mutate.ts` used only in e2e subprocess harness, not imported by app/components/lib

## How to Resume Phase 28 Work (if needed)

If a Phase 30+ milestone requires digging into active-recall internals:
1. Check git history: `git log --oneline --grep="Phase 28" -- app/components/lib/e2e`
2. Review `components/FlashcardMode.tsx` for mode orchestration
3. Read `.planning/phases/28-active-recall-study-mode/28-VERIFICATION.md` for formal acceptance criteria
4. Test against a production build (`npm run build && npm start`) — don't rely on `npm run dev` for E2E

---

## Next Milestone

**Phase 29: distractor-write-side-retirement**

Goal: Remove the last traces of the distractor system — retire `Card.distractors` column writes, purge distractor checks from `lib/audit-checks.ts`, update all documentation to reflect the new Passive/Active reality.

Start with: `/gsd-new-milestone` (will advance PROJECT.md and route to Phase 29 execution)

