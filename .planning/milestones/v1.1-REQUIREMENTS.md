# Requirements Archive: Korean Study — v1.1 UI/UX Polish

**Archived:** 2026-06-29
**Status:** ✅ SHIPPED — all 19 requirements delivered

---

## v1 Requirements (All Complete)

### Audit

- [x] **AUDIT-01**: All screens (Home, Study, Cards, Habits, Settings, Wrapped, Login) are walked through 6 dimensions (token escapes, dark-mode parity, spacing/density, interaction feedback, typography hierarchy, accessibility) with findings severity-classified P0/P1/P2
  - **Outcome:** Validated — 27 findings (F-01..F-27) documented; all 7 screens × 6 dimensions covered
- [x] **AUDIT-02**: Audit findings documented in `.planning/ui-reviews/audit.md`; P0 items fixed before any subsequent polish phase begins
  - **Outcome:** Validated — audit.md exists; all 3 P0 findings (F-02/F-25/F-23) fixed in Phases 4 and 6

### Design System

- [x] **DS-01**: `--muted` (secondary text) and `--border` (default border) semantic tokens added to `app/globals.css` with values in all three blocks: light `:root`, `@media (prefers-color-scheme: dark)`, and `:root[data-theme="dark"]`
  - **Outcome:** Validated — grep confirmed 3 hits each for --muted and --border
- [x] **DS-02**: `--highlight-bg` and `--highlight-fg` gain correct dark-mode values in both dark blocks (fixes pre-existing regression where the sentence highlight renders harshly in dark mode)
  - **Outcome:** Validated — reversed amber pairing (`#78350f` bg / `#fde68a` fg) in both dark blocks
- [x] **DS-03**: All ad hoc `bg-gray-*`, `text-gray-*`, `border-gray-*` Tailwind utilities across all components replaced with the new semantic token equivalents in a single focused sweep
  - **Outcome:** Validated — phase-wide grep confirmed zero remaining gray utilities (only intentional SVG arc track preserved)

### Study Session

- [x] **STUDY-01**: Study session displays a "Card N of Total" progress indicator throughout the session, updating after each card
  - **Outcome:** Validated — counter tracks unique first-seen reviews; VERIFICATION.md passed
- [x] **STUDY-02**: Grade buttons are visually and haptically distinct for correct vs. needs-review answers (warm color differentiation + distinct haptic pattern)
  - **Outcome:** Validated — correct → `haptic('success')`, needs-review → `haptic('selection')`, warm Easy button styling; VERIFICATION.md passed
- [x] **STUDY-03**: Korean flashcard text uses `line-height: 1.7` and `word-break: keep-all`; card front/back have clear visual hierarchy with appropriate spacing
  - **Outcome:** Validated — .hangul/.hangul-sentence line-height 1.7 per W3C KLREQ; VERIFICATION.md passed
- [x] **STUDY-04**: Inter-card transition is smooth (≤150ms slide or fade), replacing the current abrupt swap
  - **Outcome:** Validated — `.animate-card-in` (fadeIn 0.12s ease-out) via `key={cursor}` wrapper; VERIFICATION.md passed

### Home Dashboard

- [x] **HOME-01**: Home hero has three visually distinct states: (a) cards due — prominent due count, (b) goal already met today — celebratory/complete state, (c) all cards reviewed but daily goal not yet met
  - **Outcome:** Validated — `heroState` derived from parallel activity+stats fetch; VERIFICATION.md passed
- [x] **HOME-02**: Time-of-day greeting is visually prominent on the home hero (not just a small subtitle)
  - **Outcome:** Validated — elevated to `text-xl font-semibold`; VERIFICATION.md passed
- [x] **HOME-03**: Home page visual hierarchy reads clearly at a glance: due count → primary CTA → secondary stats strip
  - **Outcome:** Validated — F-07 active states added; top-to-bottom hierarchy confirmed; VERIFICATION.md passed

### Navigation & Secondary Screens

- [x] **NAV-01**: Bottom navigation correctly applies `env(safe-area-inset-bottom)` on iPhone after `<Link>` navigation (fixed via JS-set CSS variable, not live `env()` reference)
  - **Outcome:** Validated (behavior-unverified on physical device) — `--sab` CSS variable written at mount via `useEffect`; pre-paint `<script>` in layout.tsx; extended to Sheet.tsx and StudySession.tsx in Phase 8.1
- [x] **SEC-01**: Cards, Habits, and Settings pages receive a spacing and typography consistency pass aligned to the refined design system tokens
  - **Outcome:** Validated — all section headings upgraded to `text-lg`; active states added; VERIFICATION.md passed
- [x] **SEC-02**: Nav tabs and Cards page interactive elements meet 44px minimum touch target (Apple HIG)
  - **Outcome:** Validated — `min-h-[44px]` on nav tabs, view-toggle buttons, SwipeRow delete; VERIFICATION.md passed

### Accessibility & PWA

- [x] **A11Y-01**: All interactive icon buttons have meaningful `aria-label` attributes (AudioButton, Nav tabs, gloss popover dismiss, SwipeRow delete)
  - **Outcome:** Validated — aria-labels corrected/added across Nav, StudySession, GlossProvider, AudioButton; VERIFICATION.md passed
- [x] **A11Y-02**: All CSS `@keyframes` animations in `globals.css` have a `prefers-reduced-motion: reduce` counterpart that disables or simplifies the motion
  - **Outcome:** Validated — `.animate-backdrop` reduced-motion rule added adjacent to `.animate-sheet`; VERIFICATION.md passed
- [x] **A11Y-03**: All interactive elements have explicit `:active` state styles; `-webkit-tap-highlight-color: transparent` applied globally to remove the default iOS tap flash
  - **Outcome:** Validated — `-webkit-tap-highlight-color: transparent` on `html` selector; active states across AudioButton, GlossProvider, StudySession, Home; VERIFICATION.md passed
- [x] **A11Y-04**: 14-screen dark-mode regression test passes after all changes land (7 pages × light mode + dark mode)
  - **Outcome:** Validated (operator override) — all 14 screens reviewed and approved via operator sign-off 2026-06-29

---

## Out of Scope (Carried Forward)

| Feature | Reason |
|---------|--------|
| FSRS algorithm or review flow changes | Polish-only milestone; study logic untouched |
| New features (new pages, new study modes, etc.) | Out of scope; audit surfaced ideas for v1.2 |
| Structural refactors surfaced by audit | Architecture issues logged to backlog; not fixed in v1.1 |
| Framer Motion or any new animation library | Tailwind v4 CSS keyframes covered everything at zero cost |
| Social / gamification features | Single-user personal app; not applicable |
| Push notifications | Out of scope for this milestone |

## Future Requirements (v1.2 candidates)

- Low-severity audit findings deferred from v1.1
- View Transitions API (Next.js `experimental.viewTransition`) for page crossfades
- 3D flip performance profiling (if iOS jank confirmed)
- Cards view-toggle `role="group"` (IN-01)

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| AUDIT-01 | Phase 3 | Complete |
| AUDIT-02 | Phase 3 | Complete |
| DS-01 | Phase 4 | Complete |
| DS-02 | Phase 4 | Complete |
| DS-03 | Phase 4 | Complete |
| STUDY-01 | Phase 5 | Complete |
| STUDY-02 | Phase 5 | Complete |
| STUDY-03 | Phase 5 | Complete |
| STUDY-04 | Phase 5 | Complete |
| HOME-01 | Phase 6 | Complete |
| HOME-02 | Phase 6 | Complete |
| HOME-03 | Phase 6 | Complete |
| NAV-01 | Phase 7 + 8.1 | Complete |
| SEC-01 | Phase 7 | Complete |
| SEC-02 | Phase 7 | Complete |
| A11Y-01 | Phase 8 | Complete |
| A11Y-02 | Phase 8 | Complete |
| A11Y-03 | Phase 8 | Complete |
| A11Y-04 | Phase 8 | Complete |

**Coverage:** 19/19 requirements delivered
